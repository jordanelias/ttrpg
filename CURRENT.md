# Valoria — Current Canonical Surface · **Generation v40**

> **Generation v40** = the consolidated, contracts-bound, Godot-ready generation: the v30 per-system
> flatten **+** universal Key substrate (PP-687) **+** contracts spine v3 (schema-2) **+** combat-engine
> ratification (ED-900–904) **+** descriptor registry **+** master-workplan v4 orchestration. "v40" is a
> *generation marker*, not a filename suffix — the `_v30` files remain (renaming 138 files / ~16k
> citations buys no clarity). Currency is carried here + by each head's `## Status:` line, enforced by
> `tools/ci_generation_consistency.py`.

**This is the single human-readable index of what is live.** When in doubt about whether a doc
is current, start here. Machine-readable source of truth: `references/canonical_sources.yaml`
(SHA-pinned) and `registers/mechanics_index.yaml`. **Superseded exploration is no longer a directory
you can open: `deprecated/` was retired to `FORK:baf29d5` on 2026-08-23 (S6/6a)**, and every path that
was ever under it resolves through `references/restructure_ledger.md`. The one thing anything still
read out of it — the frozen ED-ledger fragments the blocking citation gate loads — relocated to
`registers/archive/`, which is frozen, append-only, and never edited.

**Status dashboard — RETIRED 2026-08-21 (culling plan v1, waves 1-2).** `dashboard/`, tools/dashboard_data.py, .github/workflows/dashboard.yml, references/audit_registry.jsonl and tools/ci_audit_registry_check.py are all gone; the GitHub Pages site was never enabled, so nothing consumed them. Their content is at `FORK:1e4c6f4` and every path resolves through `references/restructure_ledger.md`. The one signal worth keeping — whether the milestone runs — is `python tools/m1_acceptance.py --summary`, which measures execution instead of aggregating verdicts.

_Last reconciled: 2026-09-16 (IN lane — **ED-IN-0232 retired the Key substrate** on Jordan's ruling *"anything key-based gets retired"*; the Architecture / Key substrate row below is now a RETIRED row and is the only head that moved. Everything else in this stamp is the prior reconciliation, unchanged: PR #404 merging PR #405 — **no head moved on either side.** #405 landed the decision layer as `ED-IN-0228` while #404 held that id for the faction creed: both sessions read `next_free: 228` and both allocated it, which is §4's documented collision firing again. #405 merged first and KEEPS 0228; #404 renumbered creed 0228->0229 and axis-roster-single-owner 0229->0230, `next_free` 231. #404 also gave TERMS one owner that reaches `systems/` (`names_index.yaml` -> `tools/export_names.py --check` -> `engine/engine_params/names.json` -> `engine/substrate/names.py`), with `rosters.yaml: factions` deriving via `from_names:`. Neither touch is a head change.)

⚠ **TWO ROWS MOVED 2026-09-19 AND THE STAMP ABOVE IS DELIBERATELY NOT BUMPED.** A bump would mean
*this whole index was re-read against every head*, and it was not — `currency_consistency_check.py`
reports **18 drift rows** (heads touched 2026-09-18 by other sessions' merges) that nobody has
reconciled, and moving the date to today would silence all 18 without reading one of them. The two
rows below were ruled by Jordan and edited here; everything else in this file still carries the
2026-09-16 reconciliation and its known drift.

- **Clocks & tracks** — `Truth` becomes `Conviction` (`ED-IN-0251` R2, ruled 2026-09-19). ED-IN-0075
  is superseded for the per-character axis; the territory-scale `Piety (PT)` is untouched.
- **`ED-SE-0051` closed** (ruled *matter plus hearth capacity*, as a Query over dwelling Sites with
  a floor, never a fixture table) — no row in this index carried it, so nothing here changed for it;
  noted so a reader looking for it is not sent hunting.

> **This stamp is one line, and stays one line.** It carried a 12,630-character chain of ten nested prior stamps — a changelog inside a Layer 0 file, which `CLAUDE.md` §0.05 forbids. The history is in `git log CURRENT.md` and the registers. Record the date and what moved; do not append to a chain.

| Subsystem | Current head |
|---|---|
| **Personal combat** | `systems/combat/combat_engine_v1/` (resolver package — the canonical personal-combat surface; the engine wins over prose). Design-layer reference: `combat_reference_v1.md`. Typed Class-C export `engine/engine_params/combat_engine_v1.json` is GENERATED from `config.py` and round-trip-checked blocking in CI. **Lineage — the R2/R3/U0–U10 arc, the fiat audit and the governing design principle: `combat_currency_v1.md`.** Status + open items (JD-2/3/5/6/7/8 remain open): `registers/handoffs/HANDOFF_PC.md` |
| **Mass battle** | `mass_battle_v30.md` (+ `mass_battle_integration_v30.md`) |
| **Social contest** | ⚠️ **HEAD UNMOVED, AND TWO JORDAN RULINGS OF 2026-09-06 NOW BEAR ON IT — read them before trusting this row.** The head is still `social_contest_v30.md` (+ `_index`, `_infill`); live kernel at `systems/social_contest/sim/contest/`. **Nothing has superseded it, because the successor is PROPOSED and HELD BACK IN FULL.** ⭐ **RULED (ED-SC-0033): *"this subsystem obviously owns all social contests"* — the subsystem meant is `proposals/2026-09-05-proceedings-subsystem/`, and the two contest prizes at `rosters.yaml:441-446` repoint there. ⭐ **RULED same day: *"orphaned social contest code: retire it."* Measured before acting — 47 files, 1.2 MB, 20+ inbound reference sites outside the tree, several machine-read by blocking gates — so it is a cross-lane retirement wave, ruled and NOT executed. Until it runs, this row's head is what `main` still has.** Prior state, still true of `main`: a staged **contest_rebuild** in flight (Stage 4 next) and the 2026-08 three-lens audit's **three resolution models under one name** — findings only. **Lineage: `social_contest_currency_v1.md`.** ⚠️ **ED-SC-0015 still needs Jordan**; P0 docket **ED-SC-0003 and ED-SC-0005 open; ED-SC-0004 superseded 2026-09-11 by ED-SC-0033** (the canonical Argue-pool formula was a question about the retired kernel). ED-SC-0003 and ED-SC-0005 were closed in the same pass and REOPENED by its antagonist: 0003 is a name collision spanning 127 files, not kernel code, and 0005's `+1D` is still live at `resolver.py:300` because the retirement wave has not run. Status: `registers/handoffs/HANDOFF_SC.md` |
| **Faction / political** | `faction_canon_v30.md` + `faction_layer_v30.md` + `faction_behavior_v30.md` + `faction_state_authoring_v30.md` + **`faction_politics_v30.md`** (PP-660, CANONICAL, approved 2026-04-17; rank-ladder/Standing 0–7 progression, sub-office ladders, caste system — indexed into this row 2026-07-08, ED-IN-0016; C-FA-6 had flagged it orphaned despite its self-declared CANONICAL status) (overview: `faction_systems_overview_v30.md`) — **2026-07-08: FA/SE historical-precedent research docket filed and partly EXECUTED same day** (the FA/SE historical-precedent research docket (folder retired; ED-FA-0008..0017); 9 FA proposals + citation batch, `ED-FA-0008..0017`). **Executed:** Muster re-grounded as a fiscal-military purchase (ED-FA-0009); state-conditioned action-mix retiring the fixed 30/35/20/15 vector (ED-FA-0012); conquest Terms-vs-Storm fork, (a)/(b) only — Sack (c) still needs_jordan (ED-FA-0013); all 4 citation patches (ED-FA-0017); Parliamentary-Censure fallback wired into `faction_take_action` (see Social contest row). **Still PROPOSED-only:** Fiscal Stance (`faction_layer_v30` §5.9, ED-FA-0008) — no Treasury sim coupling yet. **2026-07-09: comparative-governance-research round 2** (`audit/2026-07-09-comparative-governance-research/`) — five more items authored into `faction_politics_v30.md` as PROPOSED: §1.0b Recognition Fork (ED-FA-0019, Kamakura *honryoando*/*shin'on*, applies uniformly across all four ladders), §1.0c Court Attendance + hostage-kin (ED-FA-0020, *sankin-kōtai*), §1.0d Patron-Sponsored Performance Audit (ED-FA-0021, Zhang Juzheng's *kaochengfa*), §2.5a Guild-ladder entry/mastership forks (ED-FA-0022/0023, Book of the Eparch + Arte della Lana). The judge pass corrected a stale premise several 2026-07-08 proposals had assumed: the §2.5 Guild ladder is **not** missing (it's fully authored) — the genuinely flat rank surface is the Crown Administrative branch (§1.1b), left as the docket's top needs_jordan item (ED-FA-0018, a Chinese-examination-style credentialing pipeline) |
| **Settlement / territory** | `settlement_layer_v30.md` (+ `settlement_adjacency_v30.md`, `territory_temperaments_v30.md`, `geography_v30.md`) — ⚠️ a **governance-play redesign** is in PROPOSAL (`governance_play_redesign_v1.md`) and the B12 hierarchy ruling is at `scale_hierarchy_v1.md`. **Lineage + the 2026-07 research/ruling history: `settlements_currency_v1.md`.** Status + open items: `registers/handoffs/HANDOFF_SE.md` |
| **Clocks & tracks (cross-cutting)** | `clock_registry_v30.md` — self-declared CANONICAL "single source of truth for all clocks, tracks, and counters" and already cited by name as authority at 4+ `module_contracts.yaml` modules (territorial_piety, ci_political, peninsular_strain), but absent from this index until now — added 2026-07-08 (ED-IN-0029 docket, OPT-AV-7; coherence-audit finding C4-F1/C4-U1). Covers CI/TC/IP/MS/Piety Track/Persuasion/Disposition/Standing/Renown and the faction-specific tracks (Torben/Elske Loyalty, Warden Recognition, etc.). ⭐ **2026-09-19 (ED-IN-0251 R2, RULED by Jordan: *"Truth becomes Conviction"*): `Truth` IS NOW `Conviction`, AND ED-IN-0075 IS SUPERSEDED FOR THE TRUTH HALF.** The 0–5 pole is **ABSORBED, not renamed**: *"its two poles stop being ends of one axis and become two affiliations a person can hold at full intensity at once — which is the case a pole scalar structurally could not express."* So Solmund-orthodoxy and Thread-truth are no longer ends of a scalar; a person may hold both, fully. ⚠ **THE TERRITORY-SCALE `Piety (PT)` IS UNTOUCHED** — R2 reaches the per-character axis only, and the older ruling's claim that PT is distinct still holds. ⚠ **NOTHING IS BUILT.** No code reads this axis: `engine/engine_params/params_tables.yaml:3145` carries the pre-ruling row and is the FROZEN capture §5 permits to hold superseded values; there is no owner under `references/` or `engine/season/` to re-derive. The affiliation roster, the incompatibility relation and the 13×N projection are **`ED-IN-0251` R3 — Jordan's to author** (*"the cells are yours"*), and the corpus rename is items 6a/6b/6c (§3.2 positions `12b`–`12d`). **Prior ruling, kept for lineage:** 2026-07-18 (ED-IN-0075, Jordan ruling A) renamed the per-character metaphysical-stance axis **Truth** (formerly the Certainty Track), consolidating the retired character "Piety Track" / religious-standing meter — 0–5 engine-internal, players seeing qualitative bands only. That renaming is what R2 now absorbs. SoT authored (`engine/params/core.md` §Truth Track, `derived_stats_v30` §14.2, this registry's `clock_registry_v30`, `glossary`/`alias_registry`); the corpus-wide Certainty→Truth rename (419 refs / 81 files) is STAGED as a follow-up sweep. |
| **Threadwork** | `threadwork_v30.md` (+ `thread_horizontal_integration_spec.md`). **2026-09-10 (ED-WR-0010):** `threadwork_applications.md` + `_part2.md` — **PROPOSED, reference only, and HELD BACK FROM RATIFICATION-ON-MERGE IN FULL** (they are in `main` for durability; merging them adopted nothing). They carry the *Valoria Unreality Suite*'s applied corpus into this repo's subsystems — combat, proceedings, command, investigation, factions, settlements/sectors, the season loop and presentation. ⚠ **Their cost bands assume the ruled Coherence model, and Part 3 of the head plus `systems/threadwork/sim/coherence.py` still implement the one `canon/philosophy/RULINGS.md` replaced** (a depleting 10→0 track, where the rulings require two quantities with different remedies). Adoption, and whether Part 3 is edited in place or superseded, is Jordan's call; this row does not move until then. |
| **Fieldwork / Investigation** | `fieldwork_v30.md` (Status: DESIGN — approved for commit) + `investigation_systems_v30.md` (Status: CANONICAL, approved 2026-04-17) — new row 2026-07-08 (ED-IN-0016). The two docs carried a live EP-8 contradiction (fieldwork's bare-roll Interview vs. investigation's Dialogue Lattice, which asserts an "instead of" total replacement of the single-roll object) — **RESOLVED 2026-07-08 by ED-FI-0004 ("Interview MERGE")**: the Dialogue Lattice (`investigation_systems_v30` System 3 / S14) is now the single canonical Interview home; the fieldwork §4.2 bare-roll Interview object is annotated as superseded but retained as the current mechanical baseline pending the ED-921 schedule/attribute reconciliation. Retires ED-921 + the fieldwork half of ED-IN-0016/EP-8 |
| **THE SEASON LOOP (game code)** | `engine/season/` — **RATIFIED 2026-09-05, ED-IN-0204** (Jordan: *"adopt in full"*). The executable season loop: the three runtime registries `data/rosters.py`, `data/verbs.py` and `data/matrix.py` open (`rosters`/`verb_table`/`write_matrix` — mechanism under §0.05; **`hole_register.yaml` is NOT one of them** — its readers are the corpus grader, and it is reference for the game), `cases/`, `runs/`, and its own suite in CI (`python -m pytest engine/season/tests`; the count is not reproduced here, because a number in a Layer-0 index rots independently of its subject and this row has carried a stale one twice). The `shape.py` decomposition is COMPLETE (ED-IN-0203): the facade is deleted and every importer names its owning module. ⭐ **Layer-1 MODULE-BOUNDARY conformance is done (ED-IN-0206, Arc 1) — the DIRECTORIES and their members, not the §A.2 table's row CONTENT:** `state/ data/ queries/ decision/ loop/ seam/ manifest/ tests/` are the eight of `04 §A.2`'s nine that exist, `loop/` is driver + the six steps, `queries/` is `world_q · person_q · cache`, and `seam/` is `contest · ladder · wrappers/combat`. `port/` is absent by decision — the Godot shell, `04` PART E grades it *beside*, and Gate-0 is blocked on ED-1051. ⚠ **The row CONTENT is NOT met and the grade above says only what it means:** `state/` has no `gate`/`log`/`ledgers` owners, `data/` has no ONE loader, `queries/cache` holds one of the three indexes §A.2 names, `loop/calendar` emits nothing and `loop/census` writes nothing against their rows, and `tests/` has no "two licensed guards". The write discipline is unbuilt entirely — no `Receipt`, no unforgeable token, no AX-4 clause 2 at the gate. All of it is Arc 2 and after; ED-IN-0206 carries the measured list. ⚠ **"STRUCTURAL" is a `04` grade meaning *the defect has no spelling* (`04:74`) and must not be used for this** — the properties it names are exactly the ones still missing. ⚠ **It RUNS and it is not yet a game**: `engine/season/requirements.yaml` scores Jordan's nine — **the count is not reproduced here; run `python -m engine.season.harness.register --requirements`, which is its owner. A count cached in this row has now gone stale twice.** (It read 6 `not_met` / 3 `partial` until `c275a9b` moved two rows) — no scene-granular TICK (the `Scene` container EXISTS; the season folds every scene into one batch), no roll anywhere, 54 of 143 cases unrepresentable, verbs executing (the count lives on the `verbs_execute` row of `engine/season/requirements.yaml`, not here — this row carried `6 of 32` two landings past its truth). Read that file before citing this row as done. |
| **Campaign driver** | ⛔ **`engine/mc_v18.py` is SUPERSEDED — `engine/season/` is the head** (Jordan, 2026-09-07: *"#371 EXISTS. `engine/season/` IS THE HEAD. THE DECOMPOSITION WAS DONE ON THE PROTOTYPE"*; ED-IN-0204 adopted the season loop in full). **This row exists because the supersession was ruled and then not indexed, and the cost was measurable:** `tools/m1_acceptance.py` — the one instrument `CLAUDE.md` §0.2 accepts for *"does the milestone run"* — went on probing the prototype for six days, so M1 was held NOT-MET by deferrals (OI-05, OI-07) inside code nobody intends to ship. Re-pointed at the head 2026-09-13, ED-IN-0226. ⚠ **RETIREMENT IS IN PROGRESS, NOT DONE (ED-IN-0227):** the module still imports and still runs. ⚠ **THE IMPORTER COUNT IS OWNED BY `tests/valoria/test_mc_v18_is_deprecated.py`'s `ALLOWED_IMPORTERS`** — an AST scan that fails on a new importer AND on a stale roster line. This row read *"71 live files reference it"*, which was a grep of MENTIONS read as a dependency count; ED-IN-0227 corrected it and the ratchet has shrunk it further since (measured 2026-09-15: **9 importers, exactly 1 of them production** — `tools/campaign_output_probe.py`). The load-bearing half is unchanged and is why deletion waits: **78 of the 136 test functions in `engine/tests/` (57% of CI's blocking `sim-regression` job) import it**, covering echo transport, the combat bridge seam, parliamentary transfer, accord drift and pipeline reach. The order is PORT, THEN DELETE; deleting first would drop that coverage rather than retire a prototype. **Do not build here, and do not close a deferral here** — OI-05 was ruled 2026-09-13 (ED-WR-0011) and implemented in the HEAD. |
| **THE CODE ARCHITECTURE (Layer 1)** | `architecture/` — **RATIFIED 2026-09-05, ED-IN-0204.** Governs *how all coding is conducted*: `ARCHITECTURE_V2.md`, `PLAN.md`, `meta/` (11 docs, §C.12 is the Godot mapping), `holonic_ARCHITECTURE.md` (#353). Under §0.05 it is **reference for game mechanism and binding as agent instruction** — the standing `CLAUDE.md` has. It does not resolve anything at runtime; `engine/season/` does. |
| **Architecture / Key substrate** | ⛔ **RETIRED 2026-09-16 (ED-IN-0232, RULED by Jordan: *"anything key-based gets retired"*).** `engine/substrate/keys.py`, `engine/cross_scale/echo_transport.py`, `engine/cross_scale/articulation.py`, `engine/cross_scale/parliamentary_bridge.py`, `engine/engine_params/key_types.json`, the `emits:`/`consumes:` interface in `references/module_contracts.yaml` and five tools are deleted and sit at `FORK:c6e82105`; `references/restructure_ledger.md` carries 25 exact rows. **Do not build here and do not cite the armature as live.** WHY IT COULD GO CHEAPLY, measured before anything was deleted: `engine/season/` — the RULED head — constructed ZERO Keys, and the 110-module transitive closure was an artifact of `engine/substrate/__init__.py` being nothing but a thirteen-name re-export. The bus had ONE production host (`engine/mc_v18.py`, under `ECHO_TRANSPORT`) and THREE emitters, two of them log-only telemetry whose removal left the seed-42 win-share byte-identical while `keys_emitted` fell 180 -> 99. WHAT IT COST, stated because a reader will otherwise assume these still work: `mc_v18`'s campaign distribution fell back to the ECHO_TRANSPORT-off arm (a distribution the tree already asserted byte-exactly, so the re-pin is to an existing golden); `structure_audit`'s L2 graph has zero edges and the vector audit's `build_g_key` returns empty, because both derived their wiring from the emit/consume interface; and `directional_coverage_v1.md`'s seven-delivery-direction claim is no longer backed by any test. HISTORY (for provenance only — every document below is quarantined in `.designs/`): the first executable substrate landed 2026-07-07 via the Key & Echo Armature v1 (ED-IN-0018, ED-IN-0026), with `key_substrate_v30.md` + `key_type_registry_v30.md` as its authored head and 55 declared types. `tools/quantity_registry.py` + `tools/ci_quantity_vocabulary_check.py` (2026-07-08, A17 pointer-vocabulary enforcement) are NOT part of this retirement and still run. |
| **Architecture / Holonic doctrine** | `holonic_container_doctrine_v1.md` — CANONICAL (ratified 2026-07-02, ED-1083/ED-1094); cross-maps the container/wrapper/propagation vocabulary onto module_contracts/sim-ladder/Key-substrate/Key-log-parity. Does NOT itself author the propagation-spec transform — see next row |
| **Architecture / Propagation spec** | `propagation_spec_v1.md` — CANONICAL (ratified 2026-07-02, ED-1093/ED-1094; workplan v5 J-38). Ordering/determinism, aggregate-up, distribute-down, and termination (TERMINATION-ONLY — cross-tick convergence NOT proven) transforms; supplies `engine_clock`'s candidate home doc, but the `doc: null`/[ASSUMPTION] grade in `module_contracts.yaml` stays unflipped until ED-1051 is separately resolved. **2026-07-07: OF-7/OF-B1 RATIFIED** (consolidated ruling pass, ED-IN-0026 — §4.1 steps 4/5 amended; `engine/substrate/keys.py` defaults both flags ON). Remaining open flags tracked in the doc's own §5 (D.6/OF-D6, `decay()`, RNG-MODEL-COLLISION, cap constants, ORD-3/ORD-4) |
| **Scale transitions** | `scale_transitions_v30.md` — CANONICAL; three-mode (TTRPG / Hybrid / Board Game) zoom in/out mode-bridge protocol, incl. the Mandatory Zoom In Triggers table (§4.3.2). New row 2026-07-08 (ED-IN-0016 — this connective-tissue doc had no CURRENT.md row) |
| **Player agency** | `player_agency_v30.md` — CANONICAL (approved 2026-04-17); Scene Slate (§4), Standing/Duty/Conviction player-motivation architecture. New row 2026-07-08 (ED-IN-0016 — same missing-row gap as Scale transitions above) |
| **Articulation** | `articulation_layer_v30.md` |
| **NPC behaviour** | `npc_behavior_v30.md` |
| **Master workplan** | `workplans/valoria_master_workplan_v7.md` (**CANON — RATIFIED 2026-09-12, ED-IN-0216**, by the merge of PR #397 under ED-1094's ratify-on-merge default — supersedes v6, which was RATIFIED 2026-07-05 under ED-IN-0009/ED-IN-0011 and is SUPERSEDED IN AUTHORITY but still on disk: see v7 §6 for why its retirement is deferred rather than done, and treat v6 as reference only. v7 is execution-bound per §0.2: its milestone rows name the command that decides them, and §3 sorts the Jordan-gated queue by WHO CAN ANSWER IT rather than by what it blocks. v6's lineage note follows, unchanged: supersedes v5, whose path resolves through `references/restructure_ledger.md` — the `deprecated/archives/workplans/` pointer that stood here was already dangling before `deprecated/` retired, and is corrected rather than re-pointed; v4 stays the frozen 06-11/06-22 record. North-Star milestones M1/M2/M3 survive; **v6's tiered decision register §5 does NOT** — v7 §3 replaces it. Progress board `workplan_v6_progress.yaml` keeps its v6 filename deliberately, because the `BOARD` constant near the top of `tools/m1_acceptance.py` hardcodes that path; it is hand-edited, `as_of 2026-08-19`, and its renderer (`workplan_status`, once under `tools/`) and owner skill `valoria-workplan-navigator` are both gone [RETIRED 2026-08-21, ED-IN-0194]) |
| **Narrative engine** | `audit/2026-07-05-emergent-narrative-engine/narrative_engine_design_v2_churn.md` (RATIFIED 2026-07-05, ED-IN-0011 — Jordan sign-off incl. the F-F/fork-8 Light-Function decision at its default, subtract-only + weight-set-as-data; supersedes-in-part v1, which is ratified-as-amended with spec chapters s1–s5 + `spec/churn_amendments.md`. Executes ED-IN-0003/0004; stages sequenced in workplan v6 §3) |
| **Godot conversion** | `godot/godot_conversion_strategy_v1.md` (PROPOSED governing spec; **GO lane ACTIVE** 2026-08-14, ED-GO-0001 — Gate-0 entry still gated on ED-1051). Port spec `godot_architecture_specification.md` is STALE REFERENCE (pre-d+σ) |
| **Board game** | ⚠️ EVACUATED 2026-08-05 — the prose tables (`engine/params/board_game.md` + `bg/*`) are captured verbatim in `engine/engine_params/params_tables.yaml`; the fork reference is `c451bcb`. Values live in code (principle 7) |
| **Dice / resolution** | ⚠️ `engine/params/core.md` EVACUATED 2026-08-05 → captured in `engine/engine_params/params_tables.yaml` (fork ref `c451bcb`). Live resolver: Decision-E continuous/quasi-binomial + d+σ (canonized 2026-05-15), in code |
| **Repository state armature** | **RETIRED 2026-08-21** (culling plan v1, wave 2). tools/review_core.py + registers/review_baseline.yaml (the count-aware ratchet) and both of its faces — the SessionStart banner and the GitHub Pages card — are gone, at `FORK:1e4c6f4`. Eleven of its twelve signals were apparatus signals; the twelfth, `m1.acceptance`, is rewired directly into CI as `m1_acceptance.py --summary`. The charter `repo_state_armature_v1.md` survives as history, not as a live contract. |
| **Decision policy** | `decision_policy_v1.md` — **DRAFT FOR RULING, does NOT ratify on merge** (ED-1094 exception, ED-IN-0113 §A, 2026-07-31). The precedence order Valoria decides by, drafted from a 134-ruling precedent mine so Jordan corrects rather than authors. §2 is EVIDENCED: mechanical canon is subordinate to measured, physically-grounded engine behaviour (ED-899 set-aside, ED-900 supersession with lore retained, ED-901 strike, ED-PC-0005 overriding an AUTHORITATIVE doc — four instances, no counter-instance). §3 carries **NO DEFAULT** and is the row that unblocks the rest: whether METAPHYSICAL canon shares that subordinate tier is unestablished (30 entries touch it, 9 carry override language, none read). §4 proposes the U9 ablation gate as the operational definition of "emergent". Exists because the queue is evidence-rich and answer-poor — 83 of 85 sampled `needs_jordan` items carry a source pointer, only 10 carry a proposed answer |

## Naming / versioning note

The `_v30` suffix marks the **current** generation of each subsystem — it is not a stale tag.
There is no blanket `v40` rename planned; a new version number is earned by the *next actual major
revision* of a system (e.g. a future combat-engine leap), not by find-and-replace. Keeping the
`_v30` names avoids churning ~136 co-filed pairs, hundreds of `v30 §x` citations, and every
`canonical_sha__*` pin.

See `audit/2026-06-28-deprecation-currency-sweep/deprecation_currency_plan.md` for the
full sweep, the archive rationale, and what was deferred (combat-themed audit folders, pending the
active combat-engine work).

---

> ⚠️ **Design prose is archived and no longer indexed here as a head (2026-09-16).** The 230 documents
> that lived under `systems/*/reference/` and at the root of `engine/` were quarantined to `.designs/`,
> because they kept being swept into sessions working on the game code and read as authority. Rows below
> name such a document by **bare filename only** — that is deliberate: the name is kept so the row still
> reads, the path is removed so nothing invites you to open it. **None of them is a canonical head.**
> Where a row's head was prose, the head is now the code (`CLAUDE.md` §0.05). Old paths resolve through
> `references/restructure_ledger.md` via `tools/pathres.py`.

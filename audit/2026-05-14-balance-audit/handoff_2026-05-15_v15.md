# Valoria Balance Sim — Session Handoff 2026-05-15

**Session ends at ~700k context. Next session starts fresh.**

---

## 🚨 Hard rules — do NOT re-derive or contradict

These are Jordan corrections from this session that I (Claude) kept forgetting. The next instance must internalize them BEFORE writing code.

### 1. Peninsula control is the ONLY victory path
Universal Peninsular Sovereignty (11/15 territories with treaties counting). Same condition for every faction. Different acquisition toolkits, same win condition.

**Do not implement** faction-specific victory paths (Hafenmark Dynastic Assertion / Varfell Southernmost Dominion / Church Theocratic Bid). Those are STRUCK. I re-introduced them twice; Jordan corrected each time.

### 2. Stats are fractional floats 1.0–7.0
Not integers. Not buffered. Float stats are the live mechanical value.

### 3. Derived scores are display-only granularity
- Player-facing display: stat × multiplier (Mandate = L × 20 = 20–140)
- Same name in both layers (Mandate the stat, Mandate the display value)
- Multipliers are just remapping for legibility — no separate "buffer" exists
- Events apply granular changes directly to the float stat (Mandate −15 = L −0.75)
- **No DerivedScore class. No depletion checks. No stat-loss rolls.**

I built a wrong architecture (v13/v14 with DerivedScore buffer class) twice. Don't.

### 4. Dice pools are fractional
2.5 dice is valid. Sample successes from **quasibinomial**: continuous successes with mean = N×p, variance = φ×N×p×(1−p). Normal approximation acceptable. Compare to integer Ob → degree.

### 5. Accord scale: 1–7 (not 0–3)
- Old 0→1.0, 1→2.5, 2→4.0, 3→5.5, 4→7.0
- Sovereignty threshold: Accord ≥ 4.0
- Revolt threshold: Accord < 2.0
- Same derived-score pattern (Accord × 10 = display 10–70)

### 6. Piety (PT) scale: 1–7 (not 0–5)
- Mapped: 0→1.0, 1→2.5, 2→4.0, 3→5.5, 4→6.5, 5→7.0
- Flip threshold: PT ≥ 3 for actions like Einhir Revival / Church Seizure
- Same pattern (Piety × 10 = display 10–70)

### 7. Church territorial acquisition = Mass Seizure (one-shot)
**No per-season Church Seizure action exists.** Per victory_v30 §3.2, "Graduated Seizure" was struck. Only Mass Seizure (one-shot, auto-fire at CI ≥ 60, cubic probability `((CI−60)/40)^3.3`) is canonical.

Church gains additional territory via:
- Mass Seizure auto-fire
- Military Conquest (mass battle) — works for everyone including Church
- Crown Treaty allies (count for Church sovereignty if treaty)
- Reclaim Uncontrolled (if adjacent)

### 8. Military Conquest = mass battles, not single Mil-vs-Ob rolls
This is what NEEDS to be implemented next session. I haven't done this yet.

Canonical combat docs to fetch (haven't been read this session):
- `designs/combat/mass_combat_v30.md` (or whatever path exists)
- `designs/combat/battle_resolution_v30.md`
- Look for files mentioning "March + win Battle" per peninsular_strain_v30 §2.4

Likely structure (use canon, not this guess):
- Attacker commits force (Mil pool)
- Defender contests with Mil + garrison + fort bonuses
- Multiple rounds or single opposed roll
- Losses on both sides; territory transfers on attacker win

### 9. Deterministic threat response
Accord ≤ 3 ungarrisoned → mandatory Muster. Accord ≤ 2 garrisoned → mandatory Govern. Up to 3 mandatory actions per faction per season, before stochastic action selection.

### 10. Revolt → Insurgency → Faction pipeline
- 2+ contiguous Uncontrolled territories persisting 2+ seasons → Insurgency forms
- Insurgency starts at L=1.0, low stats, non-parliamentary
- Promotes to formal faction when L ≥ 3 + 2 territories + Accord ≥ 4 sustained 2 seasons
- PT < 3 avg → Restoration Movement variant (anti-Church)
- PT ≥ 3 avg → generic new Parliamentary candidate
- **Insurgencies CAN invade** like any faction (Jordan-confirmed)
- Once promoted to parliamentary, they can be invited via Parliamentary vote

### 11. CI Generation (canonical, victory_v30 §3.2)
Per Accounting:
- +1 Institutional Momentum (passive, negated by successful Suppress action)
- +Piety Yield: per territory where Church is Prominent (L > controller's L), PT 6–7 = +1, PT 5 = +0.5
- +1 per Templar Station
- **−1 if Hafenmark Mandate ≥ 4 (FLAT, not scaling)** — I tried scaling, that's wrong

### 12. Mass Seizure
P(fire) = `((CI - 60) / 40) ^ 3.3` per Accounting, once CI ≥ 60.
Prereqs: CI ≥ 60, Church Mandate ≥ 4.
**Do not require "3 non-capital territories at Accord ≥ 5"** — that creates a Catch-22 in the strategic sim where Church can't acquire those territories without Seizure.
When fires: Church attempts Seizure on every territory where Prominent. Per-territory pool = I + CI/15, Ob = max(2, 8 − PT) [adjusted for 1-7 PT scale].
One-shot per campaign.

---

## Architecture committed (v15)

File: `designs/audit/2026-05-14-balance-audit/sim/mc_v15.py`  
Commit: `a5ee7402` (head of audit branch as of session end)

```python
def quasibinomial_successes(pool, p_hit=0.5, dispersion=1.0):
    """Returns continuous float success count."""
    if pool <= 0: return 0.0
    mean = pool * p_hit
    var = dispersion * pool * p_hit * (1 - p_hit)
    sd = math.sqrt(max(var, 1e-6))
    sample = random.gauss(mean, sd)
    return max(0.0, min(pool, sample))
```

Stats stored as floats on Faction class:
```python
self.L = 5.0; self.Sta = 4.0; self.W = 4.0; self.I = 5.0; self.Mil = 4.0
```

Granular adjustment helper:
```python
def adjust(self, stat, granular_delta, floor=0.5, ceiling=7.0):
    mult = MULTS[stat]  # L:20, Sta:10, W:100, I:15, Mil:10
    new_val = max(floor, min(ceiling, getattr(self, stat) + granular_delta / mult))
    setattr(self, stat, new_val)
```

Comprehensive JSONL logging via Logger class. Event types include: `season_start`, `action_attempt`, `action_resolved`, `state_change`, `mandatory_response`, `ci_gen`, `mass_seizure_check`, `mass_seizure_event`, `revolt`, `insurgency_formed`, `promoted`, `treaty_formed`, `treaty_lapsed`, `victory`, `campaign_end`.

---

## Current balance results (v15, N=100)

Best config: consent=0.4, lapse=0.7

| Faction | Win % | Avg Territories at End |
|---|---|---|
| Crown | 46% | 1.6 |
| Hafenmark | 53% | 10.7 |
| Church | 1% | 2.3 |
| Varfell | 0% | 1.4 |

4-faction survival: 17–41% across configs (target: ≥80%).

**The big issue: Military Conquest is too hard.** Pool 4 vs Ob 4 ≈ 0% success on quasibinomial. I lowered Ob to 3 without much effect because the underlying problem is that single-roll combat is the wrong mechanic. Mass battles will let attackers actually take territory and is canonically correct.

---

## What needs to happen next session (priority order)

### Step 1: Bootstrap
Run `bootstrap simulation`. Should restore github_ops.py, valoria_hooks.py, fetch session_log_current.md, editorial ledger, file index.

### Step 2: Fetch combat canon (haven't been read)
```python
g.read_file('designs/combat/mass_combat_v30.md')
g.read_file('designs/combat/battle_resolution_v30.md')
# Or search for canonical battle mechanics — peninsular_strain_v30 §2.4 references "March + win Battle"
```

Read these BEFORE writing combat code. Multiple canonical sources may apply.

### Step 3: Implement mass battle resolution in v15
Replace the current `Military Conquest` apply logic. Likely structure:
- Attacker commits Mil force (faction.Mil)
- Defender contests with defender.Mil + bonuses (garrison, fort, terrain, defending Accord)
- Opposed quasibinomial roll(s)
- Resolve over rounds or single opposed roll per canon
- Territory transfers on attacker win
- Both sides lose Mil based on canon

This unlocks all factions to fight for territory. Church can invade. Insurgencies can invade. Varfell becomes viable.

### Step 4: Run balance battery
N=500 per config. Sweep:
- consent_rate: 0.3, 0.4, 0.5
- treaty_lapse_rate: 0.5, 0.7
- Look for config where all 4 factions hit 15-35% wins with 80%+ 4-faction survival

### Step 5: NERS audit
Use the JSONL logs from v15 (already capturing all branches). Audit:
- N: are there mechanics that, if removed, wouldn't degrade gameplay?
- R: do players have meaningful strategic choices? (Action distribution per faction)
- S: do mechanics interact cleanly? (No degenerate strategies)
- E: is logical simplicity preserved? (No bolted-on hacks)

All directions:
- Top-down (victory paths to actions)
- Bottom-up (dice rolls to outcomes)
- Vertical (scale transitions)
- Diagonal (cross-faction effects)
- Lateral (within-faction action diversity)
- Horizontal (time-series stability)

### Step 6: Commit and continue

---

## Commits this session

| SHA | Description |
|---|---|
| `f0bf3f2a` | v12 sim + Part 14 analysis (pre-Jordan-correction asymmetric targets) |
| `06e37e54` | v13 verification (asymmetric, superseded) |
| `9ee748a7` | v13 buffer-class derived values (wrong arch — kept as cautionary record) |
| `a9c95fab` | v14 unified buffer + comprehensive logging |
| **`a5ee7402`** | **v15 quasibinomial fractional stats** ← current head |

---

## Files on disk (not in repo) at session end

- `/home/claude/mc_v15.py` — same as repo, working copy
- `/home/claude/v15_logs/` — sample JSONL logs (last batch)
- Various design docs in `/home/claude/` (peninsular_strain_v30, derived_stats_v30, settlement_layer_v30, faction_layer_v30, victory_v30, campaign_architecture_v30 — already in canonical_sources, can be re-fetched)

---

## Open meta-issues

### Issue: I kept inventing things Jordan struck
Jordan's repeated corrections this session:
- "FUCKING NO ... peninsula control is the only victory" (after I re-introduced asymmetric victory paths)
- "are you using derived scores to calculate? they're only player facing" (after I built buffer class)
- "you can have 2.5 dice to roll now" (after I tried integer pools)
- "Quasibinomial" (after I used vanilla binomial sampling)
- "implementing as in using existing work right" (after I invented Mil-vs-Ob conquest)

The pattern: each time my context degraded, I reverted to default LLM mid-knowledge instead of the project-specific direction. Future instance should re-read THIS handoff before any code generation, and trust Jordan's corrections over priors.

### Issue: Balance target is 25/25/25/25
Jordan's goal stated multiple times: equal win rate across factions, with 4-faction survival to game end. Currently we have 1-46% Crown, 0-2% Church, 25-59% Hafenmark, 0% Varfell. The mass-battle implementation is expected to be the key unlock for Church and Varfell.

### Issue: Comprehensive logging is good
v15 emits JSONL with every action attempt, every dice roll, every state change, every CI calculation. This supports NERS audit in all directions. Don't lose this.

---

## Quick reference: v15 file structure

```
mc_v15.py (~1100 lines)
├── quasibinomial_successes(pool, p_hit, dispersion) — dice
├── resolve_degree(net) → 'Overwhelming'/'Success'/'Partial'/'Failure'
├── ADJACENCY map, ACCORD_MAP, PT_MAP
├── STARTING_OWNER, STARTING_ACCORD_OLD, STARTING_PT_OLD, STARTING_GARRISON
├── STARTING_STATS
├── class Faction (float stats, card slot flags)
├── class Territory (Accord/PT floats, garrison, PT, etc.)
├── class World (factions + territories + clocks + insurgencies)
├── class Logger / NullLogger (JSONL output)
├── mandatory_actions(faction, world, logger) — deterministic threat response
├── generate_candidates(faction, world) — candidate actions
├── score(name, faction, target, world) — heuristic scoring
├── select_actions(faction, world, logger) — mandatory + stochastic
├── prereqs(name, faction, target, world) — full prereq check
├── pool_and_ob(name, faction, target, world) — fractional pool, integer Ob
├── apply(name, faction, target, degree, world, logger, parent_id) — all effects
├── ci_generation(world, logger) — canonical formula
├── mass_seizure_check(world, logger) — auto-fire one-shot
├── check_insurgencies(world, logger) — Insurgency → FoundedOrg → Parliamentary
├── accounting(world, logger) — end-of-season processing
├── victory_check(world, logger) — Universal Peninsular Sovereignty
├── run_season / run_campaign / run_batch
```

---

**End of handoff. Next session: bootstrap → fetch combat canon → implement mass battles → test balance toward 25/25/25/25.**

<!-- Recovered from killed workflow wf_1fd6d183-bde. 6 adversarial verifiers pending re-run. Distillation not yet synthesized. -->

# Combat-Critique Recovered: Corpus Reconciliation & Adversarial Verification

## Reconciliation 1: Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that derives reach/heft/tempo/...

**Full corpus identifier:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that derives reach/heft/tempo/str-demand from physical primitives {mass, head_len, grip_len, pommel_kg(NEW), wclass, hilt}, retiring categorical reach='long'/wt='heavy', plus an analytical ledger mapping weapon×tradition×state-graph gaps. Audited against HEAD of designs/scene/combat_engine_v1/ (core.py, combatant.py, systems.py, wrapper.py, geometry.py, tradition.py, config.py, ability_armature.md) and the r1/r8/m1 substrate in tests/sim/v32-combat-balance/. Engine is non-runnable on this checkout (core.py:3, combatant.py:4, systems.py:4, wrapper.py:4 all sys.path.insert '/home/claude' + '/home/claude/v32'/'/home/claude/combat_engine' — paths absent here; static analysis only, itself a P1 portability finding for CI/Godot). weapon_physics.py is NOT in the engine dir (Glob confirms 7 .py files, no weapon_physics.py; its __main__ also reads /home/claude/cb/weapon_calibrated_final.json) — the corpus is a turnkey proposal, NOT applied. VERDICT IN BRIEF: the resolution spine (mu-shift sigma_N-scaled leverage core.py:31, ER-2 inline core.py:14-24) is NERS-sound and the corpus does not touch it. The corpus's BEST contribution is repairing genuine drift (two-percussion-source; dead baked geo) and removing a categorical seam (wt='heavy' as a 2-value cliff in HEFT/WEIGHT_PEN/ACT_WEIGHT/D_WT). Its WORST risk is lever-pile: adding K_REACH/K_HEFT/K_TEMPO/K_STRD/K_ACT (5 uncalibrated gains) + pommel_kg primitive where the existing pob_frac+mass+head_len already suffice for most outputs. Adopt the drift-repair and the heft/tempo decategoricalisation; reject the reach rewrite; treat pommel_kg as optional.

### Already in engine

- mass + pob_frac ARE consumed — corpus's own [CORRECTION] (calibration doc line 6) and the analytical-ledger's 'DEAD (audit F5)' row (ledger line 53, 309) DISAGREE with each other and the ledger is STALE. core.p_auth (core.py:34-38) = min(8, 9.5*(sqrt(mass)*pob_frac)**0.30) is the live consumer; read in core.damage (core.py:82 heft=3.0*(perc/8.0) — wait, no) — precisely: p_auth(attacker.w) is passed as perc to core.damage (core.py:99), and damage uses heft=3.0*(perc/8.0) for blunt (core.py:82). So mass+pob_frac drive blunt damage-heft for ALL blunt weapons. The calibration doc is CORRECT that F5 began repair; the ledger §1/§6.1 still calling them 'DEAD' is STALE vs HEAD.
- Continuous heft for BLUNT already exists. The corpus proposes heft_term=clamp(K_HEFT*MoI,0,4) to replace HEFT={'light':0,'heavy':3}. But blunt heft is ALREADY continuous: core.py:82 heft=3.0*(perc/8.0) where perc=p_auth(mass,pob_frac). The categorical HEFT dict (core.py:48) only governs NON-blunt (cut/thrust) heft. So the corpus over-claims novelty for the blunt branch; only the cut/thrust branch is still categorical.
- cut_thrust mode-versatility (half-sword vs plate) the corpus wants from baked geo 'halfsword' is ALREADY live without it: core.coupling (core.py:71-73) takes max(shear, puncture) for cut_thrust, and systems.armor_defeat_sigma (systems.py:127-130) takes max(cut_cap, point_cap). The 'restores prior engine max(cut,point)' is done in code. The baked geo.halfsword predicate (geometry.py:53) is redundant with this.
- geometry.py ALREADY derives gap continuously from physical primitives (point_concentration, cross_section) at build time (geometry.py:28-35, baked combatant.py:52) — the corpus's 'derive coefficients from physics not hand-assignment' philosophy is already implemented for the gap coefficient. The corpus's preliminary-pob doc treats gap as still categorical; STALE.
- eff_cw IS wired — the ledger §5b 'leverage/measure/visual/tactile channels INERT pending eff_cw' and the armature's 'pending (channel)' for measure/tempo/leverage/visual/tactile/balance are STALE. TR.eff_cw is consumed at ~9 live sites: systems.reach_sigma (measure+balance, systems.py:171-173), bind_sigma (leverage+tactile, systems.py:234-237), feint_eval (tempo+visual+precommit, systems.py:202-203), reopen_prob (visual, systems.py:220-221), init_steal_factor (tactile+leverage+tempo, systems.py:267-268), init_hold_decay (measure, systems.py:273), init_overcommit_loss (tempo, systems.py:279); wrapper init/read/counter_select/poise-break (tempo+visual+leverage, wrapper.py:117,131-132,158,242). So measure/tempo/leverage/visual/tactile/balance channels are LIVE. The registered-but-inert ABILITIES (staerke_schwaeche, misura, atajo) become live through ability_factor inside eff_cw (tradition.py:136) — they ARE now wired, contradicting ability_armature.md:168 'registered but inert'.
- Reach is ALREADY parametric/continuous and blade-length-grounded. systems.reach_base (systems.py:11-14) = L0 + LONG*long + HANDS2*2H + HEADR*HEAD_REACH[head] + reach_adj; combat_analysis_rev2 §2 confirms reach r=+0.87 with blade-length. The corpus's reach_term swaps the categorical LONG flag for K_REACH*fwd_extent_m, but reach_adj is ALREADY the per-weapon continuous fine-tune doing this job (e.g. paired_short reach_adj=1.4, longsword -0.9). The corpus is right that LONG is a 2-value cliff but wrong that reach is otherwise categorical.
- No-one-shot: the corpus §5 gate assumes a tanh cap exists. HEAD has REMOVED the tanh/CAP_END cap (core.py:40-41,84-85 'NO tanh saturation', DAMAGE_SCALE/CAP_END deleted) in favour of linear Impact*Coupling*Quality. So the corpus's 'no-one-shot CAP_END' check (calibration §5.3) targets a parameter that no longer exists — STALE; the real one-shot bound now is QUAL ceiling + WoundTracker, not CAP_END.
- The mu-shift sigma_N-scaled resolution the audit prompt flags is confirmed live and the corpus correctly leaves it alone: core.resolve (core.py:31) net = roll_net(pool,rng) + m1.soft_cap(net_sigma)*m1.sigma_n(pool); sigma_n=0.8*sqrt(pool) (m1:62-67), soft_cap=1.5*tanh(x/1.5) (m1:61,73). Leverage is pool-uniform by construction (P-ii).

### Improvements

#### WP-1 [high]

**What:** Collapse the two competing percussion sources to ONE. Make systems.armor_defeat_sigma read core.p_auth(aggressor.w) for blunt armour-defeat instead of the hand-set aggressor.w['percussion'].

**Mechanism:** systems.py:124 cap=cfg['ADEF_BLUNT']*(aggressor.w.get('percussion',8)/8.0) -> cap=cfg['ADEF_BLUNT']*(core.p_auth(aggressor.w)/8.0). IN: mass+pob_frac (already the damage-heft source). resolver: armor_defeat_sigma. OUT: blunt armour-defeat sigma. Removes w['percussion'] as a SECOND, hand-set source for the same physical quantity (percussive authority) that p_auth already derives for damage. Today mace percussion=8 & p_auth(mass1.2,pob0.6)~ both high so they agree by luck; staff percussion=4 (hand-set) vs p_auth(mass1.5,pob0.05)~low diverge — the hand value and the derived value disagree and feed different subsystems.

**NERS impact:** N+ (eliminates one redundant input — the canonical role-conflation defect: one physical quantity, two owners). S+ (damage-heft and armour-defeat now move together monotonically; no seam where a weapon is strong in one and weak in the other for no physical reason). Risk: re-tunes mace/poleaxe/staff armour-defeat — needs the mirror=50 + armour-tier re-sweep before commit.

**Adversarial note:** AGAINST: w['percussion'] being hand-set is a FEATURE not a bug if Jordan wants strike_concentration (beak vs flat) folded in — p_auth(mass,pob_frac) has no concentration term, so a war-pick and a flat mace of equal mass/balance would defeat armour identically, which is physically wrong (the pick concentrates force). The CLEANER fix is to multiply p_auth by the already-baked-but-dead geo['perc_conc'] (geometry.py:48-51) — which kills two birds (WP-1 + WP-3). So WP-1 alone may regress pick-vs-mace; do WP-1 AND WP-3 together or not at all.

#### WP-2 [medium]

**What:** Decategoricalise NON-blunt heft and the wt='heavy' cliff. Replace the 2-value HEFT={'light':0,'heavy':3} (core.py:48) and the wt=='heavy' booleans (WEIGHT_PEN systems.py:23, ACT_WEIGHT systems.py:48, D_WT systems.py:58) with a continuous mass/MoI term.

**Mechanism:** core.damage heft (core.py:82 else-branch HEFT.get(weapon_wt,0)) -> a continuous f(mass) e.g. clamp(K*(mass-1.0),0,3); systems.weapon_tempo pen (systems.py:23), act_cost (systems.py:48), str_demand (systems.py:58) read mass instead of (wt=='heavy'). IN: mass (already in WEAPONS, already consumed by p_auth). resolver: damage/tempo/cost/str_demand. OUT: heft, tempo penalty, action cost, str demand. This is the corpus's core proposal MINUS the MoI/pommel_kg machinery — mass alone removes the cliff.

**NERS impact:** S++ (the wt='heavy' boolean is a genuine cliff/seam: greatsword mass2.7 and longsword mass1.4 both = 'heavy' so pay identical WEIGHT_PEN/ACT_WEIGHT/D_WT despite ~2x mass; arming 1.2 and sabre 0.9 both 'light' = 0. P-iii says no cliffs — this is one). E+ (one number, mass, does the work of a category). N-neutral (no new lever — mass already exists). Smoothness gain is real and cheap.

**Adversarial note:** AGAINST: introduces K_HEFT/K_TEMPO/K_STRD/K_ACT scale gains (4 uncalibrated knobs) where today there are exact tuned constants (WEIGHT_PEN=0.8 etc.). Every gain is a calibration liability and the corpus itself flags them [SIM-CALIBRATE]. The cliff is also SMALL in practice — combat_analysis_rev2 §2 finds 'weight is NOT a primary driver' (mace tanks DESPITE heavy), so smoothing a non-dominant axis buys little churn. Mitigation: do mass-linear (one gain per site, calibrated to keep the current heavy-value at the current heavy-mass) NOT MoI — MoI needs pommel_kg and a 3-class branch (WP-5) for marginal extra realism.

#### WP-3 [medium]

**What:** Wire the dead baked geo coefficients (thrust/cut/perc_conc) into their natural consumers, OR delete them. They are computed every import (combatant.py:49-53 sets WEAPONS[w]['geo']=bake(...)) and never read.

**Mechanism:** Grep confirms WEAPONS[w]['geo'], 'thrust', 'cut', 'perc_conc', 'halfsword' have ZERO read sites. Natural consumers: geo['perc_conc'] -> multiply core.p_auth or the blunt armour-defeat cap (WP-1) so a war-pick (strike_concentration 0.85->perc_conc~0.93) out-defeats a flat mace; geo['cut'] -> a cut-quality term in core.coupling shear delivery (currently DELIVERY is a flat per-head constant, geometry.py-derived cut keenness/curvature unused); geo['thrust'] -> redundant with gap (gap already = gap_precision(pc,cs)), so DELETE thrust. IN: baked geometry. resolver: p_auth/coupling. OUT: armour-defeat, cut damage.

**NERS impact:** N++ (dead-data elimination is the purest NERS-N win — either it feeds churn or it goes). Wiring perc_conc gives the pick/mace distinction churn (a meaningful weapon choice vs plate); wiring cut gives sabre/greatsword edge-quality churn. Deleting thrust removes a duplicate of gap (anti-lever-pile).

**Adversarial note:** AGAINST: wiring cut/perc_conc adds two more multiplicative terms to damage, each a calibration surface, and combat_analysis already shows the tier-list is reach-dominated (r=0.83) — finer damage granularity may be invisible under reach. The honest minimal action is DELETE the dead baked fields (and the bake() outputs for them) to stop paying import cost and remove the false 'available to modules' promise (combatant.py:53), and only wire perc_conc IF WP-1 proceeds (it's the fix that makes WP-1 physically correct). Wiring cut/thrust is speculative; gate on a sim showing the cut-quality spread changes any matchup.

#### WP-4 [high]

**What:** Resolve the seize-lever DEAD-ABILITY drift. 'vorschlag' (german) and 'sen_no_sen' (JAPANESE flagship) target lever='seize' (tradition.py:96,106) which has NO consumer — Grep confirms no ability_bonus(_, 'seize')/ability_factor(_, 'seize') call exists in wrapper.py/systems.py; pre-contact seizure was CUT (wrapper.py:27, systems.py:243).

**Mechanism:** Either (a) RE-POINT vorschlag/sen_no_sen at a live lever — e.g. seize-as-initiative: have them add to the INIT_STEAL_INDES path (wrapper.py:151) or seed initiative at engagement start via ability_bonus(c,'seize'); or (b) RETIRE both abilities + the 'seize' lever and fix ability_armature.md:106,131,167 which falsely marks them '✅ live'/'Live levers: seize'. IN: equipped abilities. resolver: initiative substrate. OUT: Vor edge.

**NERS impact:** N (today two abilities and a lever are pure dead weight — a tradition equips them and NOTHING changes, the worst churn failure: a choice with no path-branch. Japan's signature sen-no-sen literally cannot fire). E (the doc claiming them live is a legibility trap — a player/designer believes Japan has a working seize). Fixing it either restores the flagship mechanic's churn (option a) or removes the false affordance (option b).

**Adversarial note:** AGAINST option (a): re-adding pre-contact seizure was DELIBERATELY cut 2026-06-05 after ablation showed ~0 outcome impact (washed out by per-beat dynamics, wrapper.py:27). Re-pointing seize at initiative risks re-introducing the same wash-out — you'd be rebuilding a mechanic the engine already proved inert. The disciplined move is option (b): retire it and repair the doc. The corpus's analytical-ledger D-1 wants to REVIVE it ('wire precommit + seize, highest leverage') — but reviving a proven-inert lever is lever-pile dressed as a fix. Only revive if a NEW consumer (not the cut pre-contact contest) is designed AND simmed to non-zero impact.

#### WP-5 [medium]

**What:** Treat pommel_kg as OPTIONAL, not a required new primitive. The corpus adds pommel_kg (back-solved from sourced PoB) to make PoB exactly reproducible. But the engine already carries pob_frac directly.

**Mechanism:** weapon_physics.derive (weapon_physics.py:33-59) needs pommel_kg only to COMPUTE PoB from mass; the engine already STORES pob_frac per weapon (combatant.py:14-27). For every engine output the corpus targets (heft via MoI, str_demand via static_moment=mass*PoB), PoB is available as pob_frac*length directly — no pommel needed. pommel_kg only matters if you compute PoB forward from materials, which the engine never does. IN: pob_frac (existing). resolver: any heft/MoI term. OUT: same. So: if WP-2 uses MoI, approximate MoI ~ mass*pob_frac*head_len^2 (the ledger line 60 formula) from EXISTING fields; skip pommel_kg.

**NERS impact:** N++ (refuses a new primitive when an existing one suffices — directly the 'an ADDED mechanic must itself be necessary' rule). E+ (no new per-weapon number for the designer to source/maintain). The corpus's own preliminary doc admits rho=0.69 vs the existing pob_frac and 3 named residuals — i.e. the physics back-solve does NOT cleanly beat the hand-set pob_frac it's trying to replace.

**Adversarial note:** AGAINST: pob_frac is hand-assigned and the calibration doc shows some values are off (longsword pob_frac 0.14 but the physics says rearward -0.09; staff 0.05 vs computed forward 0.12). If Jordan wants the pob_frac VALUES corrected to physical truth, the back-solve is the tool. But that's a one-time data-correction of an EXISTING field, NOT a reason to add pommel_kg to the live vector. Correct pob_frac offline using the physics; don't ship pommel_kg into combatant.py.

#### WP-6 [high]

**What:** Fix the /home/claude sandbox imports so the engine is runnable on the clone / CI / Godot — prerequisite for ANY corpus §5 sim gate.

**Mechanism:** core.py:3, combatant.py:4, systems.py:4, wrapper.py:4 do sys.path.insert(0,'/home/claude'...) then import r1_sigma_resolution/r8_parity_harness/m1_dice_sigma_core. Those files physically live at tests/sim/v32-combat-balance/. Replace the hardcoded sandbox paths with a path computed relative to __file__ pointing at the substrate dir (or vendor m1/r1/r8 into the engine dir). IN: none. OUT: import resolution. systems.py:46 also does a runtime 'import r8_parity_harness' inside stamina_max.

**NERS impact:** Not NERS-scored — it's a correctness/portability P1. But it GATES the whole corpus: the calibration doc §5 commit-gate (mirror=50, tier spread, no-one-shot) requires running the harness, which is impossible while the engine can't import its own substrate. The corpus's turnkey 'drop weapon_physics.py into the dir and run §5' fails immediately on import.

**Adversarial note:** AGAINST: if the canonical execution environment IS the sandbox (the engine was authored to run at /home/claude and is only mirrored into the repo for version control), then the paths are correct THERE and 'broken' only on this clone. But CLAUDE.md says 'the working tree is the source of truth' and there's a Godot companion repo (valoria-game) that must consume this — neither can use /home/claude. So this is a real P1 regardless; the sandbox-only assumption is the stale-rule the project explicitly retired.


### Reject

- REJECT the K_REACH*fwd_extent_m reach rewrite (calibration §4 reach_base row). reach_base is already parametric and blade-length-grounded (r=+0.87, combat_analysis_rev2 §2); reach_adj is already the continuous per-weapon fine-tune. Swapping a tuned formula for K_REACH*fwd_extent_m where fwd_extent_m=head_len*UNIT_M would make reach track HEAD LENGTH alone — which mis-ranks: spear head_len=5.5 would dominate absurdly (the preliminary doc's OWN pass-1 diagnostic shows head_len is mostly shaft for poles, PoB 72cm 'wrong'). Net: a calibrated, validated reach model replaced by one the corpus admits needs the 3-class fix to not blow up. No NERS gain; pure regression risk.
- REJECT adding all 5 K_* gains as live config. K_REACH/K_HEFT/K_TEMPO/K_STRD/K_ACT are flagged [SIM-CALIBRATE] / uncalibrated by the corpus itself (weapon_physics.py:27-31, calibration §4-5). Five new uncalibrated knobs replacing exact tuned constants is the canonical lever-pile defect — each gain is a calibration surface and a place for a cliff to hide. If WP-2 proceeds, introduce ONE mass-linear gain per site calibrated to preserve the current heavy-value, not the full MoI machinery.
- REJECT the analytical-ledger's claim that 'traditions tune but never route' is a defect requiring the imposition gate (ledger §5b, D-5). It is accurate as DESCRIPTION (eff_cw multipliers re-weight sigma at ~9 sites, none branch the state-graph path) but the engine's whole design thesis (tradition.py:1-18) is that a tradition is a cognitive-mode BIAS over a shared substrate, NOT a separate rule-set — flat multipliers ARE the intended architecture (P-ii uniform-leverage; E one-number-does-more). Adding path-routing gates at 4 transitions is a NEW subsystem that fights the substrate philosophy. The churn is already there via the bias (a german fighter wins binds more often -> different paths emerge). Routing is lever-pile dressed as coverage.
- REJECT instantiating the short-blunt weapon class / 2H pole-blade family / messer (ledger §5a, D-4) as a PHYSICS-corpus action. These are weapon-roster expansions (creative + balance calls), unrelated to the weapon-physics wiring, and combat_analysis shows the roster is already reach-saturated (adding short weapons = adding losers; mace/dagger already tank at 24.8%/26.8%). Out of scope for this reconciliation; defer to a roster-design pass with its own sim.
- REJECT the ledger's 'pob_frac is recorded-but-inert / POB is inert' (ledger line 60-61). STALE: pob_frac is consumed by core.p_auth (core.py:38) for blunt heft. The ledger contradicts the calibration doc's own [CORRECTION]. Do not act on the inert-POB framing.
- REJECT re-adding pre-contact seizure to revive vorschlag/sen_no_sen as-was (the ledger D-1 'revive seize' default). It was cut after ablation showed ~0 impact (wrapper.py:27). Reviving the identical mechanism rebuilds proven-inert code. See WP-4 option (b) as the disciplined alternative.

### Creative layer

- pommel_kg back-solved values (rapier 0.348, greatsword 0.548 heaviest) match weapon REPUTATION — if pob_frac is ever corrected from the physics, the per-weapon pommel feel ('a rapier's heavy pommel is why it balances at the hand') is flavour Jordan can surface in weapon descriptions even if it never enters the engine vector.
- The 3-class split {bladed, hafted_tip, hafted_block} (preliminary §1) is a clean in-world weapon-smithing ontology — grip-plus-head vs full-shaft-plus-point vs haft-plus-lump — usable as the diegetic category a smith/merchant speaks in, orthogonal to the engine's head-shape axis.
- The analytical-ledger's 6 Valoria-native traditions (Constraint/Tempo-read/Intent-seize/Flow/Root/Reach, ledger §3.1 Layer B) and their in-world naming (decision D-7) are explicitly reserved as Jordan-only — the channel-vectors are mechanical scaffold; the cultural names and lore are the creative layer. The ledger correctly refuses to name them.
- Short-blunt-class flavour (shillelagh/canne/tonfa/sap, ledger §5a#1) as a low-status 'levy/improvised' weapon niche — a narrative role (the peasant's club, the bravo's sap) more than a balance need.
- The 'Root' tradition concept (leverage 1.35 + strike, battlefield/clinch apex, ledger §3.2 Layer B) as the armour-defeat / half-sword / poleaxe school — a thematically rich 'the man who fights in the press, in harness' identity for the percussion weapons that currently have no tradition home (mace is orphaned).

### Verdict

ADOPT-PARTIAL. The resolution spine the audit prompt centres on (mu-shift, sigma_N-scaled leverage core.py:31; ER-2 inline core.py:14-24; soft_cap 1.5*tanh, sigma_n 0.8*sqrt(pool)) is NERS-sound and the corpus correctly does not touch it. The corpus is most valuable as a DRIFT-REPAIR prompt, less so as a physics rewrite. HIGH-CONFIDENCE ADOPT: WP-1+WP-3 together (collapse the two-percussion-source role-conflation by routing blunt armour-defeat through p_auth*perc_conc, simultaneously killing the dead baked geo) — this is the single best NERS win, fixing a real one-quantity-two-owners defect AND eliminating dead data; WP-4 (resolve the seize dead-ability + ability_armature.md drift — prefer RETIRE over revive, since pre-contact seizure was proven inert); WP-6 (de-sandbox the /home/claude imports — a standalone P1 that gates every sim and the Godot port). MEDIUM ADOPT: WP-2 (decategoricalise the wt='heavy' cliff via mass-linear terms — a genuine P-iii cliff, but on a non-dominant axis, so do it cheaply with one calibrated gain per site, NOT the full MoI/pommel machinery). REJECT: the reach rewrite (already parametric+grounded, regression risk), the full 5-gain K_* set (lever-pile), pommel_kg as a live primitive (WP-5: pob_frac already suffices; correct its VALUES offline if wanted, don't add a new field), the imposition gate / 'traditions must route' (fights the substrate-bias philosophy), and roster expansions (out of scope). The corpus's analytical ledger is partly STALE vs HEAD on three counts (mass/pob_frac 'DEAD', channels 'inert pending eff_cw', POB 'inert') — all three are now live; trust core.py/systems.py over the ledger. NOTHING ships without the corpus §5 re-baseline (mirror=50, tier spread, no-one-shot) — but note that gate references a tanh CAP_END that HEAD has already removed (damage is now linear-uncapped), so the no-one-shot check must be re-specified against the QUAL ceiling + WoundTracker, not CAP_END.

---

## Reconciliation 2: Martial-morphology union (4 vols): vol1 = 12-dim primitive scheme + 20 throughlines + 6 meta-throughlines + [M]/[B]/[L] ...

**Full corpus identifier:** Martial-morphology union (4 vols): vol1 = 12-dim primitive scheme + 20 throughlines + 6 meta-throughlines + [M]/[B]/[L] epistemic tags + viability rules; vol2 = expanded corpus + 7 new throughlines + the 9 governing-analogues thesis (analogue-as-attractor, basins, analogue-coherence viability test); vol3 = adversarial efficacy review that adds the DECISIVE 14th dimension (resistance/aliveness), the efficacy-weighting heuristic, the efficacy-first design order, and collapses everything into a FOUR-AXIS standard (coherence / epistemic status / analogue-coherence / efficacy); distilled.md = the corpus's own self-cut to three dimension-classes (efficacy-bearing skeleton / culture-framing dressing / the single decisive variable=aliveness) + four axes. The union's own final finding (vol3 F9 'taxonomic seduction', distilled §6): the 14-dim/9-analogue elaboration OUTRAN the few variables that carry weight; meaning and efficacy run on separate tracks. ENGINE CHECKED vs HEAD: tradition.py models traditions as cognitive-mode channel-vectors (visual/tactile/precommit/leverage/tempo/measure/balance, neutral=1.0) + familiarity() cross-read + an ABILITIES armature; the resolver substrate is sigma-leverage mu-shift (core.resolve / m1 soft_cap / r1 effective_ob display-only). The corpus is a MEANING morphology; this engine is an EFFICACY resolver. That is the whole reconciliation: most of the 14 dims are creative-layer, a few map to levers already present, and the single load-bearing import (aliveness) DOES NOT APPLY where every PC is a trained, fully-resisting fighter.

### Already in engine

- FOUR-AXIS 'analogue-coherence' (is the art what it says it is?) — HEAD ALREADY enforces the engine analogue of this via the import-time assertions in systems.py:86-87 (GATE/GEOMETRY must be key-synchronised with WEAPONS) and tradition.py's invariant-safe discipline (neutral=1.0 -> no-op). The corpus's 'declares geometry but moves like dance' incoherence is the engine's 'declares a weapon but has no GATE/GEOMETRY row'. The DISCIPLINE the corpus wants already exists as CI-style structural gates. Corpus is not stale here, it just names a discipline the engine implements differently.
- Governing-analogue as FLAVOUR not foundation (vol3 §6.4 culture-last; distilled §3 'keep the concept, discard the 9-fold taxonomy as load-bearing') — HEAD ALREADY does exactly this. tradition.py docstring (lines 1-19): a tradition is a COGNITIVE MODE that 're-weights, does not add new physics'; mode='geometric'/'tactile'/'temporal-spatial'/'intentional' is recorded as a FLAVOUR string and the channel weights are the only mechanical surface. The engine independently arrived at the corpus's central correction (analogue = dressing over one shared substrate). CREDIT: strong convergence.
- Efficacy-first / skeleton-before-culture generative order (vol3 §6.4, distilled §5) — HEAD ALREADY embodies this. The substrate (measure+commitment-window+contact-channel+collision physics) is the [M] biomechanical skeleton; traditions are layered on top as pure re-weights with invariant-safety (default fighter byte-identical). The engine fixes the [M] skeleton first and dresses last BY CONSTRUCTION. The ability_armature.md §1 'modulate never unlock' + §4 mandatory bottom-up([M] technique)+top-down(precedent) grounding IS the corpus's efficacy-first discipline operationalised.
- Epistemic-tag honesty + source-tier weighting (vol3 F2 'tagging is not weighting', distilled §1 [M]/[B]/[L]) — HEAD ALREADY goes FURTHER than the corpus: ability_armature.md §5 carries S1-S5 source tiers, the 'selection-effect universal' (counter-prestige is European/Japanese, NOT universal), and explicitly DENIES abilities to priority-gap traditions (Polish szabla, Filipino, Chinese-technique) until an S1/S2 anchor exists. This is the corpus's F2 'weight, don't just tag' lesson applied to engine content. Note: armature §5 demotes Filipino to unanchored, yet tradition.py STILL ships a full 'filipino' channel-vector (lines 39-41) with mode='kinetic-rhythmic' — the channel-vector is the substrate (always present, fine) but the doc/data tension is worth a glance.
- The 'information contest' meta-throughline-C (vol3 §3 'the most efficacy-faithful pattern' = sense-more-reveal-less) — HEAD ALREADY makes this the SPINE of resolution, not a flavour note. The visual/tactile read contest (wrapper:131-133 read_d vs read_a), feint_eval (systems:194), familiarity() degraded cross-read, and the Indes/sen steal (wrapper:147-153) ARE the duel-of-information. The corpus's single most load-bearing efficacy pattern is the part of the engine most heavily wired. Strong credit.
- Yielding-overcomes-hardness drawn as LEVERAGE+skill, not mysticism (vol3 F5, distilled §2) — HEAD ALREADY models yielding strictly as [M]: leverage() lever-arm primitive (systems:137), bind_sigma leverage+tactile+strength (systems:228), displace-and-step-inside gated on a real leverage edge (wrapper:204-213). There is NO no-touch/aiki mystical redirection lever. The engine sits on the correct side of the corpus's jU/aiki efficacy line by construction.
- Vital-point boundary [M] real-target / [B] death-touch (vol3 F6 'drawn once and lost') — HEAD AVOIDS the trap entirely: there is no dianxue/meridian-timed targeting subsystem to lose the boundary in. Targeting is collapsed into degree x coupling x armour physics (core.damage). The corpus worries the boundary is forgotten 'upward'; the engine never builds the [B] half, so there is nothing to forget. Credit by omission.

### Improvements

#### I1 [high]

**What:** Collapse the dead 'seize' lever + its two abilities (vorschlag, sen_no_sen) and REPOINT them onto the live initiative substrate, OR delete them. This is the corpus's vol3-F2 'tagging is not weighting' lesson turned inward: two well-grounded abilities (S1/S2 German Vorschlag, S2 Japanese sen-no-sen — the JAPANESE flagship) are currently pure dead-data.

**Mechanism:** tradition.py ABILITIES: vorschlag (line 96) and sen_no_sen (line 106) both set lever='seize'. NO engine site ever calls TR.ability_bonus(c,'seize') — confirmed: ability_bonus is invoked only for 'anti_overcommit' (wrapper:163) and 'counter_success' (wrapper:189). The pre-contact seizure that consumed 'seize' was CUT 2026-06-05 (wrapper:27, systems:243). IN: equipped ability -> resolver: nothing -> OUT: nothing. tradition.py:87 STILL claims "'seize' (+ seizure score)" is a LIVE lever 'wired at their engine sites' — false. FIX: either repoint these two to the surviving ongoing-initiative system (e.g. an additive bump to init_steal_factor or a one-time initiative grant at engagement start via a new bounded consumer, respecting clamp_initiative), or cut both abilities and the line-87 claim.

**NERS impact:** N: removes a dead lever (lever-pile defect — the canonical NERS-N failure: a mechanic computed/stored, never consumed). E: restores legibility (the doc says a thing is live that is inert). CHURN: as-is these abilities branch nothing — equipping vorschlag changes no outcome, a choice with no consequence violates the churn principle. Repointing makes the German/Japanese seizing identity actually alter the Vor contest.

**Adversarial note:** Strongest case AGAINST repointing: the pre-contact seize was cut BECAUSE it was empirically inert (ablation ~0, washed out by per-beat dynamics, wrapper:27). Re-adding a seize consumer risks re-introducing the exact washed-out mechanic Jordan already removed. So the SAFER half of the fix is the one to take first: DELETE the two seize abilities + fix the line-87 doc claim, rather than invent a new consumer the dynamics will just wash out again. Adding a consumer must clear an ablation bar or it is lever-pile by another name.

#### I2 [medium]

**What:** Resolve the TWO competing percussion sources for one physical quantity (blunt armour-defeat). This is a role-conflation / dead-data hybrid: mass+pob_frac feed p_auth for the DAMAGE path, but the OLD hand-set percussion field still drives the ARMOUR-DEFEAT path — the same weapon's percussive authority is computed two incompatible ways.

**Mechanism:** core.p_auth(w) (core.py:34-38) derives percussion authority = min(8, 9.5*(sqrt(mass)*pob_frac)**0.30) and feeds core.damage blunt heft (core.py:99 passes p_auth(attacker.w)). But armor_defeat_sigma (systems.py:124) still reads the HAND-SET aggressor.w.get('percussion',8) for the blunt armour-defeat cap. IN: w['mass'],w['pob_frac'] -> p_auth -> damage heft; SEPARATELY w['percussion'] -> armor_defeat. The p_auth docstring claims it 'replaces the hand-set percussion' and that 'pob_frac & mass were dead inputs' — but the hand-set field is NOT dead, it is still the armour-defeat source, so the replacement is HALF DONE. A weapon whose hand-set perc disagrees with its derived p_auth (the whole roster: e.g. staff perc=4 hand-set vs derived) defeats armour on one number and damages on another. FIX: route systems.py:124 through core.p_auth(aggressor.w)/8.0 so both paths read one quantity; then the hand-set 'percussion' field becomes genuinely dead and can be deleted from all WEAPONS rows.

**NERS impact:** S (Smooth): one physical quantity should have one source across the two seams it touches (damage seam + armour-defeat seam); two sources is the role-conflation defect (one variable, >1 job — here, one CONCEPT, 2 variables). R: removes a latent inconsistency at the armour extreme where armour-defeat dominates. N: kills the half-dead hand-set field.

**Adversarial note:** Case AGAINST: p_auth was emergent-calibrated for the DAMAGE gradient; the hand-set percussion values were tuned for the ARMOUR-DEFEAT tier-list. Swapping armour-defeat onto p_auth may shift the validated armour tier-list (mace/poleaxe vs plate). So this is not a free refactor — it needs a re-sweep of the armour-defeat invariants. Lower severity precisely because the two numbers may have been kept separate ON PURPOSE to preserve two independently-validated calibrations; confirm with the sim before merging.

#### I3 [low]

**What:** REJECT-adjacent but listed as the one genuinely tempting corpus import: a per-tradition 'aliveness/training-realism' scalar (vol3 Dimension-14, the corpus's single most efficacy-predictive variable). Assess it, then SCOPE it to the only place it can be non-redundant — NPC/mook tiers — never PC traditions.

**Mechanism:** Candidate: add an aliveness in [dead..alive] per tradition, multiplying read/counter competence. IN: combatant.aliveness -> resolver: scales reading()/counter_success -> OUT: degraded net for 'dead-trained' fighters. BUT the engine ALREADY has the mechanically-equivalent axis: c.history (resolution_pool size, reading() experience term) + c.skill() per-axis biases + the S1-S5 source-tier gate in the armature. 'Trained against live resistance' = high history + trained skills; 'forms-only' = low history. The corpus's D14 is, for this engine, a RE-LABELLING of history/skill.

**NERS impact:** N FAILS for PCs: D14 as a new dimension is redundant with history/skill (lever-pile — an ADDED mechanic must itself be necessary; this one duplicates an existing axis). It only earns N where history can't already express it: distinguishing two same-history NPCs by training-realism for FLAVOUR (a drilled-but-never-sparred cult vs a pit-fighter) — i.e. as a CHURN seed for NPC variety, not a PC lever.

**Adversarial note:** Case AGAINST adopting at all (and the prompt's own challenge): 'does the aliveness axis even apply where every fighter is alive?' It does NOT for PCs — every PC is a resisting opponent by definition, so D14 is pinned at 'alive' for the entire playable cast and branches nothing. The corpus itself (vol3 §1 category-guard) says D14 only bites for combat-telos arts that sit at the dead end — which no PC does. Importing D14 as an engine dimension is the precise 14-dim over-elaboration the distillation warns against. Keep it as an NPC-generator FLAVOUR tag at most; do not give it a resolver consumer.

#### I4 [medium]

**What:** Consume or cut the DEAD baked geometry surface — thrust/cut/perc_conc/halfsword. This is the same dead-data pattern as I1/I2: computed at build-time, stored, never read. Tangential to the morphology corpus but surfaced by the same NERS-N lens the corpus's F2/F9 demand.

**Mechanism:** combatant.py:51-53 bakes the full geometry dict and stores WEAPONS[w]['geo']=_b, but only ['gap'] is consumed (and gap already overrides the hand-set gap one line up). geometry.bake returns {gap,thrust,cut,perc_conc,halfsword}; grep confirms 'geo' is read at no engine site. IN: GEOMETRY params -> bake -> {4 dead coeffs}+gap -> OUT: only gap. The bake docstring says the coeffs are 'for downstream wiring' — the wiring never landed. FIX: either wire perc_conc into p_auth (it literally 'multiplies the §4 percussion authority' per its own docstring — a natural consumer that would ALSO help I2 unify percussion), wire halfsword into the hardcoded HALFSWORD_FORM table (systems:148), thrust/cut into coupling()/legibility(); or stop storing the dead dict and bake only gap.

**NERS impact:** N: four dead coefficients are lever-pile. Wiring perc_conc into p_auth is the elegant move (E): a beak/pick (poleaxe strike_concentration=0.85) SHOULD defeat plate better than a mace flat — one number doing more work, and it dovetails with I2's percussion unification. CHURN: live thrust/cut/perc_conc would let weapon GEOMETRY (not just head category) branch outcomes, more weapon-choice texture.

**Adversarial note:** Case AGAINST wiring: geometry.py's calibration contract says coeffs are 'calibrated to REPRODUCE the already-validated §4 values' — so wiring thrust/cut now would perturb a validated tier-list for marginal gain. The conservative correct move is the cheap one: bake only gap and drop the stored dict, UNLESS perc_conc is wired as part of the I2 percussion-unification (where it pays its way). Do not wire all four speculatively — that is gold-plating, the inverse of the distillation's cut-to-the-load lesson.


### Reject

- The 14-dimension scheme as an engine data model. The corpus's OWN distillation (distilled §6, vol3 F9 'taxonomic seduction') says the 14 dims/9 analogues outran the few variables that carry weight and must be cut to three dimension-classes + one decisive variable. Importing the full scheme is the exact error the corpus diagnoses. The engine's 7 channel-weights + history/skill + weapon-axis vectors already span the efficacy-bearing subset; adding D1-D14 as fields would be maximal lever-pile (NERS-N catastrophe).
- The 9 governing-analogues as a mechanical taxonomy / per-analogue rule-sets. vol2 makes them the 'generative substrate'; vol3 §3 + distilled §3 RETRACT this to 'flavour, largely efficacy-irrelevant, keep the concept discard the taxonomy.' tradition.py already keeps exactly the concept (mode= flavour string) over one shared substrate. Hard-coding geometry/dance/music/medicine/alchemy basins as engine branches would re-introduce per-tradition rule-sets — the precise thing 'modulate-never-unlock' (armature §1) forbids.
- The aliveness axis (D14) as a PC resolver lever — see improvement I3. It is pinned at 'alive' for the entire playable cast (every PC resists), so it branches nothing for PCs and is redundant with history/skill. The single most efficacy-predictive corpus variable is the LEAST applicable one to a game where everyone is a trained, resisting fighter. Adopt at most as an NPC-flavour tag, never a dimension.
- [B]/[L] mystical mechanics as new levers: qi/jin fa-jin issuing, no-touch/aiki redirection, meridian-timed dianxue/death-touch, qinggong lightness, kebal invulnerability, summoned astras. vol3 tags every one physically null [L] or mechanism-false [B]; the engine correctly has zero of them. These are pure setting/lore. Adding any as a resolver input would import asserted-fantasy-as-physics — the one move both the corpus and the engine's anti-fabrication discipline prohibit.
- An efficacy-WEIGHTING heuristic (vol3 §6.3 material-fraction/convergence-proximity scorer) as an engine subsystem. It is a DESIGN-TIME sorting tool for inventing traditions, not a runtime resolver input. The engine's runtime equivalent (does this fighter win?) is the sim win-rate harness (fight()), which already measures efficacy empirically. A static efficacy score would be a second, inferior efficacy oracle alongside the actual simulation.
- Exemplar weapon designs (flexible coupled-pendulum chain, traced-figure sword, gambit polearm). vol3 F4 establishes these are the LEAST effective and most physically marginal — 'emergence and efficacy are anti-correlated.' They are creative-layer worldbuilding, and importing the flexible/exotic weapon class would demand a whole new geometry+collision regime (the engine is rigid-weapon collision physics) for the corpus's own-admitted weakest designs.

### Creative layer

- Governing-analogue as the NARRATIVE skin of each tradition's existing mode= field: German='structure/architecture', Italian/Spanish='geometry', Japanese='strategy/no-mind', Filipino='dance-rhythm', Chinese='alchemy/medicine'. Pure prose colour for how a tradition is TAUGHT and DESCRIBED in-world — zero resolver impact, sits atop the channel-vector that already exists. This is the corpus's most intellectually substantial contribution (vol2 Part VIII) used exactly where vol3 says it belongs: meaning, not mechanics.
- The healing-harming dual-use unity (vol2 throughline-22: the kalari master is a bonesetter, dit-da hit-medicine, kappo resuscitation) — a superb worldbuilding hook for NPC roster: a tradition's master is ALSO its physician. Narrative/quest texture, no engine surface. Maps to nothing in the resolver and should not.
- Founding-charter legends as faction lore: the yielding-founder myth (the nun Ng Mui, the willow, the woman-general), the animal-witnessing origin (mantis besting cicada), lineage-as-carrier with its double edge (the 'too deadly to spar' secret that shields dogma). Pure setting material for sects/schools in the jianghu register Jordan wants — knowingly [B]/[L], gloriously fantastical ON PURPOSE (vol3 §7's 'liberating' framing).
- The cultivation-arc as CHARACTER-PROGRESSION flavour (gross->subtle->empty / mushin), with vol3 F7's warning baked into the lore: a tradition whose upper 'internal' stages become a license to stop sparring is one whose masters have grown brittle — a built-in story flaw/antagonist seed. Maps loosely to history/skill growth but the 'transcendence trap' is narrative, not a mechanic.
- Telos as faction identity (battlefield / duel / sport / health / spiritual / spectacle / communal) — vol1 D9. A flavour tag distinguishing a duelling school from a battlefield order from a temple-health lineage, driving WHO trains a tradition and WHY, with zero resolver consequence. Good NPC-faction generator input.
- The four-axis standard itself as JORDAN'S design-discipline (not engine code): when inventing a new in-world tradition, deliberately place it on coherence / epistemic-status / analogue-coherence / efficacy so the fantastical is fantastical on purpose and the effective is effective on purpose. This is a worldbuilder's checklist, the single most valuable thing to KEEP from the corpus — applied at the writing desk, never compiled.

### Verdict

Judicious reconciliation: the engine has ALREADY internalised the morphology corpus's hard-won central corrections, and in several places goes further than the corpus does. tradition.py independently arrived at 'analogue = flavour over one shared [M] substrate' (vol3 §3/§6.4), the resolver makes the corpus's single most efficacy-faithful pattern — the information contest, meta-C — its literal spine, yielding is modelled strictly as leverage+skill (correct side of the jU/aiki line), and the [B]/[L] mystical layer is correctly absent. The armature's source-tier + selection-effect discipline OUT-rigours the corpus's own F2 'tagging is not weighting.' Where the corpus is STALE vs HEAD: nothing material — it proposes disciplines the engine implements by other means (import-time structural gates = analogue-coherence; invariant-safe modulation = efficacy-first order). The ADVERSARIAL verdict on imports is hard NO to scale: the corpus's own distillation (F9, §6) proves the 14-dim/9-analogue scheme is over-elaboration; importing it is lever-pile. The single tempting import — the aliveness axis (D14, the corpus's most efficacy-predictive variable) — FAILS NERS-N for this engine specifically: every PC is a fully-resisting fighter, so D14 is pinned at 'alive' for the whole playable cast and branches nothing, and for NPCs it is redundant with the existing history/skill axes; keep it as an NPC-flavour tag at most. The corpus's REAL value to this project is the creative layer (governing-analogue skins, founding charters, dual-use lore) and the four-axis standard as Jordan's writing-desk discipline — applied to invent traditions, never compiled into the resolver. The genuine ENGINE work the audit surfaced is orthogonal to the corpus and is the NERS-N debt the corpus's lens HELPS you see: (I1) the dead 'seize' lever leaving vorschlag + the Japanese-flagship sen_no_sen inert with a doc that lies about it being live — highest severity; (I2) two competing percussion sources for one physical quantity (p_auth damage path vs hand-set armour-defeat path); (I4) the dead baked geometry surface (thrust/cut/perc_conc/halfsword). Net: credit the convergence, reject the scale, fix the inert levers.

---

## Reconciliation 3: Engagement-psychology corpus (3 docs): combat_engagement_psychology_findings.md (descriptive, T1 sport-science + T2/T3 f...

**Full corpus identifier:** Engagement-psychology corpus (3 docs): combat_engagement_psychology_findings.md (descriptive, T1 sport-science + T2/T3 fight doctrine, self-corrected the "experts more foolable" inversion), combat_engagement_engine_proposal_unified.md (v1 prescriptive), and combat_engagement_engine_proposal_refined.md (post-adversarial, the live one). Self-audited to ONE proposed mechanic — 'wariness' (commit-depth-only, scaled by 1-familiarity, no-op at full familiarity) — plus a regime-contest lens (§2) and an optional §5 clinch/disengage-on-overcommit eligibility hook. Claims the engine ALREADY faithfully encodes disguise(legibility)/deception(feint)/overcommit-exploit. I verified all three against HEAD: designs/scene/combat_engine_v1/{wrapper.py,systems.py,core.py,tradition.py,config.py,combatant.py,geometry.py,ability_armature.md}.

### Already in engine

- DISGUISE/TELEGRAPH — CONFIRMED FAITHFUL. systems.py:177-192 legibility() called wrapper.py:130: point=LEGIB_THRUST(0.80, hard to read), cuts/blunt=LEGIB_SWING(1.25, easy), +LEGIB_COMMIT_K*max(0,commit-3) (deeper commit = more readable), +LEGIB_LUNGE on lunge grip. It multiplies the DEFENDER's read (read_d, wrapper.py:131). The corpus's 'low-legibility strike IS the from-nowhere disguise; high-legibility = telegraph; harder wind-up announces louder' maps exactly. Credit accurate; no change earned (corpus agrees: no new term).
- ACTIVE DECEPTION (FEINT) — CONFIRMED FAITHFUL AND THEN SOME. systems.py:194-207 feint_eval() called wrapper.py:120: feint_q=(reading+technique)*tempo vs def_read; read via logistic; fooled defender gets FEINT_DEBUFF(+0.30 to attacker), a defender who READS it gets FEINT_PUNISH(-0.12, a counter-edge). This is the forced-response/credibility contest the corrected literature wants — and crucially it is NOT a 'better readers more foolable' term, matching the corpus's retraction. Corpus credit is correct.
- FEINT ALREADY ENCODES PART OF 'FACE 2'. The corpus defers 'Face 2' (familiarity wired into the disguise/feint system) as a hazardous future second touch-point. But systems.py:203 def_read ALREADY reads TR.eff_cw(defender,'precommit') AND there is a familiarity-style degrade pathway through the visual read. So the engine is FURTHER ALONG on the disguise-legibility axis than the corpus credits — the corpus is conservatively stale here in its OWN favour (it under-claims what HEAD does).
- OVERCOMMIT-EXPLOIT — CONFIRMED FAITHFUL. wrapper.py:147-158 (read_win & commit>=4 -> Indes/sen-no-sen steal scaled by commit-DEPTH x read-MARGIN, bounded INDES_SCALE_FLOOR/CEIL) + wrapper.py:163 overcommit_exposure=COMMIT_EXPOSE_K*(commit-3)-anti_overcommit + wrapper.py:173-198 riposte path. Rests on the structural 'a committed action can't be simultaneously defended' fact, not a perceptual deficit — exactly the corpus's corrected biomechanical grounding. Faithful.
- TWO-SIDED MEASURE/RANGE CONTEST — CONFIRMED. wrapper.py:66-85 approach (longer weapon stop-hits while shorter closes, approach_displace sets aside the point) + wrapper.py:217-226 reopen moments + systems.py:217-226 reopen_prob. The measure axis is emergent and two-sided as the corpus claims for findings §5.
- DISPOSITION ALREADY CARRIES TEMPERAMENT/WRATH. config.py:91 DISP_COMMIT_K/DISP_COUNTER_K/DISP_INIT_K + wrapper.py:98-106 commit-skew + systems.py:62-64 disp_lean. The corpus's claim that 'wrath->overcommit is the existing commit-skew + fatigue' and that no new emotion subsystem is needed is correct against HEAD.
- ATTACKER-IMPOSES-FIRST / VOR. The initiative substrate (systems.py:248-279, config.py:57-64) with bounded tanh edge + per-beat decay damper + hard cap is the 'who imposes the regime/holds the Vor' machinery; the corpus correctly does NOT propose to touch it.

### Improvements

#### ENG-1 [low]

**What:** Adopt 'wariness' as a commit-depth-only bias in the wrapper.py:98-106 disposition commit-skew, scaled by WARINESS_K*(1-familiarity(aggressor.tradition,defender.tradition)), with a HARD spread-floor so the combined disposition+wariness skew cannot collapse the {2,3,4,5} commit distribution to all-{2}.

**Mechanism:** IN: TR.familiarity(ta,td) (tradition.py:62, already imported/used at wrapper.py:129) + disp_lean(aggressor) (systems.py:62). RESOLVER: in the abs(_ln)>=eps branch (wrapper.py:103-105) add the wariness term to _k BEFORE building the weight vector _w, then clamp each weight to a non-trivial floor (e.g. max(0.10,...)) so probability mass on deep commits never zeroes. config.py: add ONE constant WARINESS_K (Class-C). OUT: a shallower, more-recoverable commit distribution vs an unreadable tradition; byte-identical at familiarity 1.0 (factor zeroes). Owned state: none new.

**NERS impact:** N: marginal-pass. It is a genuinely absent behaviour (commit-caution vs an unreadable STYLE), and it folds into an existing lever rather than adding a subsystem — so it is not a fresh lever-pile entry. BUT see adversarial note: 'deepen familiarity' is oversold. R: the named compound-collapse interaction (cautious disp x low familiarity, both pushing toward {2}) is a real cliff risk the corpus itself flags but leaves UNSOLVED; the spread-floor I add is the missing R fix and must ship WITH the mechanic, not after. S: clean — no role-conflation at a seam, same commit-skew machinery. E: 'you commit more carefully against a style you can't read' is one-number-does-more-work, intuitable.

**Adversarial note:** Strongest case AGAINST: the corpus's headline justification — 'wariness DEEPENS the existing familiarity system rather than bolting on a parallel one' — is FALSE against HEAD. familiarity is ALREADY a 3-job variable: it degrades the visual read (wrapper.py:131), and it scales the bind tactile contest in BOTH directions (systems.py:236-237). Adding a 4th consumer (commit-depth) pushes familiarity from 'load-bearing' to 'role-conflated' — one scalar now driving read-accuracy, bind-dominance, AND commit-courage. A tuning change to FAMILIARITY_DEFAULT/ADJACENT (tradition.py:54-55) would then move three unrelated behaviours at once, an opaque coupling that violates the churn principle's 'one lever, one job'. The N case is therefore weaker than the corpus asserts: disposition (commit-caution as TEMPERAMENT) + familiarity (read-deficit, already felt as worse defence/bind) arguably already produce 'careful vs the unknown' emergently, and the marginal new churn (shallower commits specifically vs unfamiliar traditions) may be below perceptual threshold. Verdict: defensible but NOT clearly necessary; if adopted it must be logged as a deliberate 4th familiarity-consumer, and the worst-case spread test is a hard gate.

#### ENG-2 [medium]

**What:** Do NOT ship the §5 clinch/disengage-on-overcommit hook as proposed; first build the contact axis it depends on, OR rescope §5 onto the EXISTING contact surface (the bind / reopen-moment).

**Mechanism:** The corpus's §5 'make clinch/disengage more eligible when overcommit_exposure (wrapper.py:163) is high' presupposes a clinch/disengage mechanic. HEAD has NONE: ability_armature.md:61 and :169 explicitly list reopen/disengage as 'not yet built (concrete-mechanics queue)', and the clinch weapon field (combatant.py:14-27) is DEAD DATA (never read). The only contact axes that exist are the blade bind (wrapper.py:233-252) and the abstract closed/reopen measure-state. RESCOPE: the already-built reopen_moment (wrapper.py:217-218 fires when the SHORTER weapon commits>=4) is the natural home for 'change the regime on the opponent's overcommitment' — and it ALREADY keys off commit>=4, the same signal. A minimal real improvement: let overcommit_exposure additively raise reopen_prob (systems.py:217-226) so a longer weapon disengages-to-distance more readily the deeper the opponent overcommitted — reusing wrapper.py:163's existing value, no new mechanic.

**NERS impact:** N: the rescoped version reuses an existing signal and an existing emission (reopen) — pure wiring, no new lever. The corpus's ORIGINAL §5 fails N until the contact axis exists (you cannot wire to vapor). S: rescoping keeps it on the one contact axis HEAD actually models (measure/reopen), avoiding a phantom grappling seam. R: eligibility-boost (not hard gate) preserves spread, as the corpus correctly insists.

**Adversarial note:** Strongest case AGAINST: even the rescoped hook may be redundant churn — reopen_moment ALREADY fires on commit>=4 by the shorter weapon (wrapper.py:217-218), so 'overcommit opens the regime-shift' is PARTIALLY in HEAD already; adding an exposure-scaled probability term tunes magnitude but does not branch the path (the moment already exists or doesn't). Per the churn principle, a lever that only tunes and never branches is suspect. Keep §5 deferred until clinch/disengage is a real mechanic; do not let the corpus's confident '[INSERT @ wrapper.py:214-226]' imply it is shovel-ready — it is not.

#### ENG-3 [high]

**What:** Repair the doc-drift the corpus's §3 credit unknowingly rests on: the German/Japanese 'seize' abilities (vorschlag, sen_no_sen) are DEAD, yet docs call 'seize' live — which undercuts the corpus's claim that the engine faithfully encodes the German/Japanese engagement psychology it credits.

**Mechanism:** IN: ability_bonus(c,'seize') — grep confirms ZERO callers in wrapper.py/systems.py. Pre-contact seizure was CUT (systems.py:243-246; wrapper.py:27-29 A.initiative,B.initiative=0.0). So tradition.py:96 'vorschlag' and tradition.py:106 'sen_no_sen' (the JAPANESE flagship) modulate a lever with no consumer = inert abilities. YET tradition.py:87 comment, ability_armature.md:51 (table: seize 'live'), and ability_armature.md:167 ('Live levers: seize (Vorschlag, Sen-no-sen)') all still claim live. FIX: either (a) re-home seize onto the surviving initiative system (e.g. ability_bonus(aggressor,'seize') as an initiative pre-load at engagement start, bounded by INIT_CAP), making the abilities real; or (b) demote the docs to 'inert (consumer cut 2026-06-05)'. The corpus credits §3's German/Japanese psychology as 'built and faithful' — (a) is the change that makes that credit TRUE.

**NERS impact:** N: option (a) restores churn to two tradition flagships that currently branch nothing (a dead choice = the canonical defect). E: a player equipping sen_no_sen and seeing no effect is exactly the opacity Jordan flags as the core pain. S: re-homing onto the existing initiative substrate keeps it on one axis.

**Adversarial note:** Strongest case AGAINST treating this as a corpus improvement: it is arguably OUT of the engagement-psychology corpus's scope — it's an ability-wiring/doc-drift bug, not a psychology mechanic, and was already in the standing audit. But it is load-bearing FOR the corpus's central credit claim: the proposal's whole 'the engine already encodes the psychology faithfully, so we only need wariness' argument silently assumes the Vor-seizure abilities work. They don't. So the corpus's §3 is stale-by-omission, and this is the one place the corpus's optimism about HEAD is materially wrong in a way that matters.


### Reject

- REJECT the §2 'regime contest' as anything more than orientation. The refined doc itself downgrades it to 'a lens, not a mechanic' with 'one operational consequence' (the unbuilt clinch/disengage pair), so there is nothing to adopt mechanically. Against HEAD it is doubly empty: the contact axis it organizes does not exist (no clinch/disengage; clinch field dead). Credit the framing, adopt no code.
- REJECT the §2 cross-corpus 'convergence' as evidence for any engine change. The refined doc already concedes (its own A1/A4) that the convergence is NOT independent — shared primary (George Silver) and shared synthesizer (same assistant). It is circular exactly as the audit prompt suspected. It raises confidence in nothing testable; the load-bearing content is wariness + the §5 hook, neither of which the convergence supports. Do not let it justify importing weapon morphology into a psychology layer.
- REJECT (keep deferred) the v1 'wariness initiation knob' (a higher effective ACT_THRESHOLD vs unreadable opponents). The refined doc correctly cut it: familiarity is SYMMETRIC (tradition.py:62-67 returns the same value regardless of argument order for non-none pairs), so in the cross-tradition matchups it targets BOTH fighters raise their threshold at once -> mutual passivity / stalled bout (the actors=[] / 'if not actors: continue' path at wrapper.py:87-88 would simply spin). Confirmed against code: the cut is right; do not resurrect without a stall-guard.
- REJECT 'Face 2' (wiring familiarity into legibility/feint_eval as attacker-tell disguise) AS A NEW TOUCH-POINT. Partly because the refined doc defers it, but also because HEAD ALREADY routes a precommit/read-degrade pathway through the feint (systems.py:203) and the visual read (wrapper.py:131) — adding a second explicit familiarity multiplier there would compound an effect already present and push familiarity to a 4th/5th job (see ENG-1 adversarial note). Over-granularity.
- REJECT directional/asymmetric familiarity as an engine change here. The refined doc correctly routes it to Jordan's roster/flavour layer (who-studied-whom is content, not a mechanic). Making tradition.py familiarity directional is a data/matrix decision, not an engagement-psychology primitive.
- REJECT any literal freeze/stun/skipped-turn state — the corpus rejects it too (brainstem-freeze is an inescapable-threat extrapolation, not a voluntary bout). Agreed; it would be unfun and break the bounded/graded R property.

### Creative layer

- Directional familiarity as ROSTER FLAVOUR, not engine math: the traveled duelist (an Almud-tier NPC who studied a style its bearer has never seen) reads-and-disguises against a provincial opponent who does neither in return — a genuine information asymmetry the symmetric tradition.py cannot express. Jordan's call: encode it as a per-NPC 'studied traditions' list that biases familiarity directionally only for named characters, leaving the default symmetric.
- Rhythm deception / the Ali-Robinson 'telegraph-then-throw' (findings §1: once you've trained someone to read your tell as a feint, the truth becomes the best lie) is a beautiful EMERGENT story beat, not a lever — it's what a high-feint_streak fighter who then throws a high-legibility committed blow already produces. Surface it in the prose/narration layer (prose-writer skill), not in config.
- Which traditions are 'adjacent' (tradition.py:56-60 ADJACENT set) is a worldbuilding artifact of Valoria's contact-history between regions — Jordan owns whether german/italian read each other better because of in-world trade/war, the same way the real HEMA exchange seeded it.
- The 'wrath pulls you past your balance' (Silver, findings §3) is already mechanically the aggressive-disposition commit-skew + overcommit_exposure; the FLAVOUR — which NPCs fight in wrath, and what in the fiction provokes it — is Jordan's character layer over the existing math.

### Verdict

The corpus is unusually honest and mostly RIGHT about HEAD: legibility (disguise/telegraph), feint_eval (deception), and the commit-depth Indes-steal + riposte (overcommit-exploit) are all built and faithful to the corrected psychology — I confirmed each at file:line. Its self-discipline (cutting the initiation knob on the verified-symmetric familiarity; deferring Face 2 and directional familiarity; downgrading the circular §2 convergence) is sound. Three reconciliations the corpus gets wrong or stale against HEAD: (1) its headline N-case for 'wariness' — that it merely 'deepens' familiarity — is false; familiarity is already a 3-job scalar (wrapper.py:131; systems.py:236-237), so wariness is a 4th-consumer role-conflation risk, making the mechanic defensible-but-not-clearly-Necessary; (2) §5 is wired to vapor — clinch/disengage is NOT built (ability_armature.md:61,169) and the clinch weapon field is dead data, so §5 must be rescoped onto the existing reopen-moment or deferred; (3) the corpus's §3 credit silently assumes the German/Japanese seize-psychology works, but vorschlag and sen_no_sen are DEAD (no ability_bonus('seize') consumer post-2026-06-05 cut) while three docs still call seize 'live' — the one place the corpus's optimism about HEAD is materially wrong, and the highest-severity finding. NERS bottom line: wariness EARNS A QUALIFIED place ONLY if it ships with the spread-floor (the unsolved R-cliff the corpus names) AND is logged as a deliberate 4th familiarity-job; otherwise disposition+familiarity already cover the behaviour and it is removable polish. The regime-contest lens and the cross-corpus convergence buy nothing mechanical. Adopt: wariness (low, gated on the floor test), §5-rescoped-or-deferred (medium), and — most importantly — fix the dead seize abilities (high), which is what would actually make the corpus's 'the engine already encodes this psychology' claim true.

---

## Reconciliation 4: deliberation-as-game-synthesis.md — a research synthesis treating debate/trial/negotiation/oratory as rule-governed game...

**Full corpus identifier:** deliberation-as-game-synthesis.md — a research synthesis treating debate/trial/negotiation/oratory as rule-governed games. Read in full. Its load-bearing concepts for this audit: (1) constitution-by-rules (Searle/Rawls) — the master correspondence; (2) Hamblin's COMMITMENT STORE — a running ledger of what each party has conceded, a literal game-state altered by legal moves; (3) Goffman EXPRESSION GAMES + the information-contest frame (sense more, reveal less); (4) the magic-circle / interaction-membrane (bounded arena with suspended-and-transformed norms); (5) adjudicator-as-resolver (one impartial rule-applier across all arenas); (6) the four-senses taxonomy of 'game' (Wittgensteinian rule-practice / Suitsian inefficiency / agonistic zero-sum contest / game-theoretic strategic-interdependence) and the central thesis that COMBAT is agonistic-zero-sum (sense 3) while DELIBERATION is frequently MIXED-MOTIVE non-zero-sum (sense 4) — Schelling/Nash/Walton-Krabbe; (7) validity/truth/success = a WINNING STRATEGY (Lorenzen-Lorenz, Hintikka); (8) fallacy-as-foul (pragma-dialectics ten-rule code); (9) the master equivocation — an activity can be a game in sense 4 precisely by NOT being one in sense 3. LENS APPLIED: this is about the SOCIAL-contest engine (social_contest_v30.md, docket J-31), not personal combat directly. The honest strongest output is the SHARED-RESOLVER question across personal-combat (sigma-leverage continuous engine) + social-contest (discrete net-success pool engine). I read both engines at HEAD to ground the verdict; combat-specific mechanics were NOT forced out of the corpus.

### Already in engine

- COMMITMENT STORE (Hamblin) is ALREADY the architecture of BOTH engines, independently realised. Combat: combatant.py:77 `self.initiative` (the Vor/Nach signed ledger) + `self.poise` (kuzushi) + wt.wounds — a running per-fighter commitment record that legal moves (hits init_GAIN_HIT, steals INIT_STEAL_INDES, bind dominance, overcommit loss, decay) alter; wrapper.py:222-225 even tracks `reopen_moment`/`push_avail` as fleeting move-state. Social: social_contest_v30 §2/§4 Persuasion Track (0-10), Doubt Markers, Concentration, Composure, and the HIDDEN GM LEDGER (§2 Step7, §4 Step7) are an explicit commitment store. Corpus credit, but corpus proposes nothing HEAD lacks here.
- ADJUDICATOR-AS-RESOLVER (Part III.D, the single impartial rule-applier) is ALREADY the core.py design intent: core.py:1-2 docstring 'Single source for ob/degree/roll/damage. Wraps canonical r1/r8/m1 so every subsystem resolves identically.' Combat's resolver is role-blind (wrapper.py owns A/B identity; subsystems never index raw A/B — systems.py:2-3). The corpus's 'one role, many arenas' insight is realised WITHIN combat; the cross-arena extension (same resolver for social) is NOT done — see improvements.
- MIXED-MOTIVE vs ZERO-SUM distinction (Part IV, the corpus's pivotal finding) is ALREADY honoured at HEAD by DIVERGENT resolver choice. Combat is zero-sum agonistic (sense 3): core.resolve is a single continuous net with one winner per beat, fight() returns +1/-1/0. Social is NOT forced onto that resolver: social_contest_v30 §4 CROSS interaction ('no direct comparison, each argument evaluated independently, no strain dealt'), the Compromise zone (Track 4-6), and 'private negotiation stall IS a consequential outcome' (§2 Step4) are exactly mixed-motive, non-zero-sum structure. The repo already DID what the corpus argues for: it did NOT reuse the zero-sum combat resolver for the mixed-motive social case.
- INFORMATION-CONTEST / expression-games frame (Goffman, Part I.F) is ALREADY deep in combat: the read contest (wrapper.py:129-133 read_d/read_a, legibility, familiarity), feint as deception (systems.feint_eval), and Fuhlen/tactile bind-reading (bind_sigma). Social mirrors it: §4 Step1 Appraise (sense the audience), Read results becoming permanent knowledge. Corpus credit; nothing new proposed.
- SHARED DERIVED-VALUE ARCHITECTURE across the two engines already exists and the prior NERS audit already CREDITED it: ners_verdict_social_contest.md:14 'Composure shares architecture with personal combat'. Composure = Charisma+6 ∥ Health = Endurance+6; Argue Pool = (Attr×2)+History ∥ Combat Pool = (Agi×2)+History+3; both TN 7; both use the same four-band degree ladder (fail/partial/success/overwhelming). This is the substrate-sharing the corpus's constitution-by-rules thesis predicts — and it is already partial fact, not a proposal.
- CORPUS IS STALE vs HEAD on deliberation-determinism: the synthesis (and the SUPERSEDED social_contest_system_v2.md §5, ED-138) treats social first-to-speak as intentionally DETERMINISTIC (higher Attunement) precisely BECAUSE deliberation rewards perceptual depth not luck. HEAD REVERSED this — social_contest_v30.md:209 (ED-581) made first-to-speak ROLLED 'aligns with combat initiative for cross-system consistency.' So HEAD already chose cross-system consistency over the corpus's deliberation-is-different intuition at this one seam. Flag: this is a live tension the corpus would push back on (Part VI.2-3: deliberation healthiest when LEAST treated as a contest).
- FALLACY-AS-FOUL (van Eemeren, Part V.10) is ALREADY present in social as rule-penalised moves: §9.6 Forced Unmask ('violence in the chamber = immediate Unmask, violent party auto-loses') is a foul-with-penalty; Doubt Markers / Obscuring-win penalise the move-shape. Combat's analogue: the riposte-on-fail / overcommit-exposure is the 'foul punished within the system.' Corpus credit; no new mechanic needed.

### Improvements

#### I1 [medium]

**What:** Extract a SHARED RESOLUTION SUBSTRATE module that both engines import, rather than two parallel hand-maintained copies of pool/TN/degree-band logic. Concretely: combat already routes everything through core.py wrapping r1/r8/m1; social_contest_v30 re-specifies Pool=(Attr×2)+History, TN7, and the fail/partial/success/overwhelming bands in PROSE with no shared code. Promote core.degree() + the pool/TN constants into a substrate both consume, parameterised by regime (continuous sigma-leverage for combat; discrete net-success for social).

**Mechanism:** core.py degree()/resolution_pool() + r1.degree_of_success() are TWO copies of the same band ladder (core.py:14-24 continuous w/ ER-2 vs r1:108-117 discrete net>=k). IN: history/attr -> pool; net + ob -> band. OUT: degree. Make r1.degree_of_success the single discrete band and core.degree the single continuous (ER-2) band, both in one resolver substrate; social imports the discrete one instead of re-prosing it. This is the corpus's 'adjudicator-as-resolver, one role many arenas' (Part III.D / V.8) made literal in code.

**NERS impact:** S (Smooth) major: kills the cross-engine drift risk where social's prose band and combat's code band silently diverge (already happening — v30 line 209 reversed v2's determinism for 'consistency' but the resolution atoms are still uncoordinated). E (Elegant): one number/ladder doing work across both arenas beats two subsystems. N neutral (no new lever). R neutral.

**Adversarial note:** Strongest case AGAINST: the corpus's OWN central finding (Part IV master equivocation) is that combat (zero-sum/sense-3) and deliberation (mixed-motive/sense-4) are DIFFERENT games — so over-unifying the resolver risks dragging combat's single-winner μ-shift into the social engine where CROSS/Compromise/stall are load-bearing non-zero-sum outcomes. The share must be the DEGREE-BAND + POOL CONSTRUCTION ONLY, never the win-condition or the net-combination. If the unification reaches the resolve() level it is a NERS-S regression, not a fix. Keep the share below the resolver.

#### I2 [low]

**What:** Make the combat initiative/Vor ledger an explicit named COMMITMENT-STORE object with a single mutation surface, instead of scattered raw clamp_initiative() writes across ~10 wrapper sites.

**Mechanism:** Today combatant.initiative is mutated at wrapper.py:29,41,45,152-153,166,195-196,229-230,239-240 — every steal/gain/decay/loss writes the field directly through S.clamp_initiative. The corpus's Hamblin commitment-store (Part V.5) is exactly a ledger with LEGAL MOVES as the only mutators. IN: a move (hit/steal/bind/overcommit/decay). OUT: bounded new initiative. Wrap in e.g. core.Ledger.apply(move, magnitude) so every write is typed, logged, and bound-checked in ONE place — the same 'single call-site' discipline core.strike already enforces for damage (core.py:87-99).

**NERS impact:** R (Robust): centralising the clamp removes the risk that one of the ~10 sites forgets clamp_initiative (the bound is the CAP half of the damper<1 AND cap loop-safety requirement). S: makes combat's ledger structurally legible the way social's GM ledger is. E: the Vor becomes one inspectable object. N neutral.

**Adversarial note:** Strongest case AGAINST: pure refactor, no behaviour change, and the current sites ARE all already wrapped in clamp_initiative — so the bound is not actually violated today; this buys legibility/future-safety, not a bug fix. Could be churn for its own sake. Only worth it if a move-log is wanted for observability (which Jordan's transparency preference suggests it is) — otherwise defer.

#### I3 [medium]

**What:** Resolve the deliberation-determinism seam the corpus exposes: HEAD made social first-to-speak ROLLED (v30:209, ED-581) for 'consistency with combat,' but the corpus (Part VI.2-3, and v2's own ED-138 rationale) argues deliberation rewards PERCEPTUAL DEPTH, not luck — the opposite design pressure. Pick deliberately, do not let 'consistency' auto-decide.

**Mechanism:** Not a combat-engine change — a social-engine + cross-system coherence decision. The lever is social_contest_v30 §5 first-to-speak: rolled-Attunement (current) vs deterministic-Attunement (corpus-favoured, makes the §4-Step1 Appraise non-redundant). IN: Attunement (+roll?). OUT: who declares second (information advantage). The corpus gives a principled reason to make this ONE seam intentionally INCONSISTENT with combat: combat initiative is rolled because combat rewards variance/readiness; deliberation initiative could stay deterministic because it rewards the read.

**NERS impact:** S (Smooth): the corpus's whole Part VI is that forcing combat-shape onto deliberation is the canonical category error; honouring it here is a seam-consistency improvement EVEN IF the two engines diverge at this one lever (consistent-with-sibling means same WHERE the regime matches, not everywhere). N: rolling here risks making Appraise partially redundant (the v2 ED-138 worry) — a near-inert-mechanic / NERS-N smell.

**Adversarial note:** Strongest case AGAINST: this is squarely the social engine (J-31), outside the personal-combat audit scope, and the corpus's deliberation/combat distinction is its MOST-contested strand (Part VI.3 says even the technical core is split Dutilh-Novaes vs Hodges; the determinism intuition is suggestive, not probative). HEAD's maintainer already weighed it (ED-581) and chose rolled. Re-opening needs a sim showing Appraise went inert, not a philosophy citation. Flag for the social team, do not act unilaterally.

#### I4 [high]

**What:** Adopt the corpus's CONSTITUTIVE-vs-REGULATIVE distinction (Searle, Part I.B/V.1) as the discriminator for which dead-data to KILL vs WIRE in combat. The engine carries dead baked data (geo surface, clinch, geo thrust/cut/perc_conc/halfsword) and dead levers (seize); the corpus gives a principled test: a mechanic earns its place only if it CONSTITUTES a move the resolver consumes (X counts as Y in context C), not merely if it is computed.

**Mechanism:** geometry.bake (geometry.py:58-72) emits {gap,thrust,cut,perc_conc,halfsword}; combatant.py:51-53 stores the full geo dict but only gap is ever read — thrust/cut/perc_conc/halfsword are computed-never-consumed (dead-data). 'clinch' weapon field (combatant.py:14-27) never consumed. seize lever has NO consumer (vorschlag/sen_no_sen ABILITIES are dead; ability_armature.md:51,106,167 still calls seize 'live' = doc drift). Constitutive test: either WIRE the baked coefficients into coupling()/armor_defeat_sigma (perc_conc already has a natural home in p_auth/blunt; thrust/cut into damage modes) so they COUNT AS something, OR delete them and the GEOMETRY inputs that feed only them. Same for seize: either give it a consumer or cut vorschlag/sen_no_sen and fix the armature.

**NERS impact:** N (Necessary) major: the corpus's V.1 is the exact formal statement of the churn-principle's 'suspect any inert mechanic' — a baked coefficient never consumed generates ZERO churn and is the lever-pile defect in latent form. Cutting it is pure N gain. E: fewer phantom inputs to reason about. S: the armature doc drift (seize 'live') is an S/legibility defect — fix in the same pass.

**Adversarial note:** Strongest case AGAINST: this finding is NOT FROM the corpus — it is pre-existing engine dead-data that the corpus merely supplies vocabulary for; crediting the corpus risks overstating its transfer. Honest framing: the corpus's constitutive/regulative cut is a clean RUBRIC for the dead-data triage that was already needed, not a new discovery. Also, thrust/cut/halfsword may be intentionally staged for the deferred '2b puncture work' (damage docstring core.py:80) — killing them could destroy queued WIP. Verify against HANDOFF/stash before deleting; prefer WIRE-or-flag over delete for the geo surface, delete-or-fix-doc for seize.

#### I5 [low]

**What:** If/when a NEGOTIATION sub-mode is built for social (corpus Part IV; social_contest_v30 'Negotiation compromise resolution (ZOPA-style)' is flagged future work), use the corpus's win-set/ZOPA structure — NOT the Persuasion Track and NOT the combat resolver. The corpus is explicit that negotiation is mixed-motive (overlapping win-sets, disagreement point) and structurally unlike both the zero-sum track and combat.

**Mechanism:** Lever: a future negotiation resolver. IN: each party's reservation point + offers. OUT: agreement in the ZOPA, or impasse at the disagreement point (Nash/Schelling, Part IV). v30 §12 already self-identifies 'Negotiation compromise resolution (ZOPA-style) — identified as structurally different from Persuasion Track, not designed.' The corpus supplies the exact theory (Nash bargaining, Schelling focal points, Putnam two-level win-sets) for that gap.

**NERS impact:** S (Smooth): prevents the wrong-engine-for-the-pool-regime defect (P-v) — bolting negotiation onto the Persuasion Track would conflate a mixed-motive bargain with a zero-sum push. N: a distinct resolver is justified ONLY because the regime is genuinely different (the corpus proves it is); this is the rare case where a new subsystem beats reusing one.

**Adversarial note:** Strongest case AGAINST: speculative/future — no negotiation engine exists at HEAD, so this is design-ahead, not an actionable engine change. Risk of premature lever-pile: a third social resolver (track + ZOPA + ...) may be over-engineering when 'private negotiation = exchange majority / stall' (v30 §2 Step4) may already suffice for table play. Build only when a concrete negotiation scene demands what the track cannot express; until then the corpus citation is a design note, not a task.


### Reject

- ARGUMENT-IS-WAR / ARGUMENT-IS-GAME conceptual-metaphor framing (corpus Part II.E) — reject as an engine input. The corpus itself flags it as the weakest-grounded, empirically-contested strand (Part VI.8: McGlone & Glucksberg, Vervaeke & Kennedy), 'suggestive not probative.' It establishes cultural availability of the framing, nothing mechanical. No lever follows from it.
- Importing the corpus's ZERO-SUM 'validity = winning strategy' (Lorenzen-Lorenz/Hintikka, Part II.B) into either resolver as a mechanic — reject. It is a profound result about LOGIC, but both engines already resolve via probability/degree, not via possession-of-a-winning-strategy game trees. Encoding a winning-strategy solver would be a massive new subsystem replacing a working one-number resolver — the canonical NERS-E failure (new subsystem beaten by one number doing the work). Cite as inspiration for the commitment-store framing only (I2), not as a resolver.
- Forcing combat's continuous sigma-leverage μ-shift onto the social engine for 'one resolver to rule both' — reject, and this is the corpus's OWN warning. The master equivocation (Part 0 / Part IV) is that combat is sense-3 zero-sum and deliberation is frequently sense-4 mixed-motive; a single resolver that imposed a single continuous net + single winner would destroy CROSS/Compromise/stall (the non-zero-sum social outcomes). The honest verdict is a SHARED SUBSTRATE BELOW the resolver (I1), NOT a shared resolver. Reject any read of this corpus as arguing for one resolver.
- Magic-circle / bounded-arena as a NEW mechanic (Part I.C/F, V.3) — reject as redundant. Both engines already ARE bounded arenas with suspended norms (combat: the engagement loop with its own beat/tempo physics; social: the contest with §9.6 Forced-Unmask membrane-breach penalty). The concept is already instantiated; adding an explicit 'magic circle' object would be ceremony with no churn. The corpus even notes (Part VI.7) the magic-circle is itself contested for overstating boundary firmness.
- Suits's lusory-attitude / voluntariness as a design constraint (Part I.A, VI.6) — reject as out-of-scope for the resolver. It is a philosophy-of-games diagnostic (is the compelled defendant a player or the stake?), genuinely interesting for FICTION/framing but with no resolver lever. Belongs in creative_layer, not improvements.

### Creative layer

- The Vor ledger AS a commitment store readable in-fiction: Hamblin's insight (Part II.B) is that the commitment store is a record of what has been CONCEDED. Reskin the combat initiative ledger's sign-flips as a visible 'who has conceded tempo' — a narrator could read the Vor history as a transcript ('he conceded the line on the third bind') exactly as a debate's commitment store is read back. Flavour only; the ledger already exists (I2 just names it).
- Adjudicator-as-character: the corpus's adjudicator (Part III.D) is the same role across court/debate/duel — the umpire, the judge, the dialectical audience. In Valoria, the SAME NPC archetype (a marshal at a judicial duel, an Inquisitor at a tribunal, a Speaker at Parliament) could span both engines as 'the one who applies the rules and declares' — a recurring figure whose impartiality (or corruption) is itself a churn-seed. Cross-references the social adjudicator-type table (v30 §2) with a combat trial-by-battle marshal.
- Trial-by-battle as the literal seam between the two engines (Huizinga 'Play and War' + Pound's descent-from-combat, Part I.C/III.B): Valoria could make a judicial DUEL a scene that BEGINS in the social-contest engine (the accusation, the Persuasion Track) and, on a specific outcome, ESCALATES into the personal-combat engine — the corpus's exact thesis that the trial is sublimated combat, run backwards into a setting where the sublimation can fail. This is the corpus's strongest narrative gift: the two engines are not just siblings, they are the SAME contest at two levels of civilisation.
- eris vs eristikē (Part I.G) as a Disposition flavour: the ancient distinction between good-contest and win-at-all-costs maps onto the combat disp/lean axis and the social orator who argues to resolve vs to dominate — a Belief-aligned characterisation hook (the duellist who fights for truth vs the one who fights to win) with no mechanical cost.

### Verdict

The corpus does NOT argue for a shared RESOLVER across personal-combat and social-contest — it argues for the opposite, and HEAD already got that right. The synthesis's pivotal finding (Part IV master equivocation) is that combat is agonistic zero-sum (sense 3) while deliberation is frequently mixed-motive non-zero-sum (sense 4), and 'an activity can satisfy one precisely by failing the other.' At HEAD the two engines correctly DIVERGE at the resolution atom: combat resolves a single continuous sigma-leverage net to one winner per beat (core.resolve), while social_contest_v30 resolves discrete net-success margins with genuinely non-zero-sum outcomes (CROSS = no comparison/no strain, Compromise zone, productive stall). The honest transferable claim is a SHARED SUBSTRATE BELOW the resolver — the commitment-store ledger, the pool=(Attr×2)+History construction, the four-band degree ladder, the derived-value pattern (Composure=Cha+6 ∥ Health=End+6) — most of which the prior NERS audit already credited and which the engines already realise in parallel. The one concrete, high-value engine action the corpus's vocabulary sharpens is NOT new at all: it is the dead-data triage (I4, severity high) — the corpus's constitutive/regulative test (X counts as Y, or it does not earn its place) is the formal statement of the churn-principle, and it indicts the dead baked geo surface, the dead clinch field, and the dead seize lever (with its stale 'live' armature claim) as latent lever-pile. Net: credit HEAD heavily (it already avoided the trap the corpus warns of); the actionable improvements are mostly legibility/substrate-sharing (I1, I2) and the pre-existing dead-data cut (I4), plus two flag-don't-act seams in the social engine (I3 determinism, I5 ZOPA) that are out of personal-combat scope. Reject every reading that pushes one resolver onto both regimes. Adversarial note on my own output: only I4 is a true HEAD defect with churn impact; I1/I2 are NERS-S/E refactors not bug fixes; I3/I5 belong to J-31, not this audit — do not let the corpus's elegance manufacture combat-engine tasks that the code does not need."

---

## Reconciliation 5: Weapon-typologies differential reference — cross-tradition weapon-class trade-off analysis

**Full corpus identifier:** weapon-typologies-differential-reference.md — a consolidated, fact-audited treatment of how weapon typologies from major martial traditions interact, organized around one governing claim: every optimization is simultaneously a de-optimization. Nine interface axes (measure/reach, leverage/bind/contact-decision, cut geometry, mass/percussion vs armour, recovery/commitment/tempo, hand exposure, skill floor vs ceiling, formation/terrain, primary+sidearm law), fifteen functional typologies (two-handed cut-and-thrust through empty-hand/grappling), and a synthesis arguing independent traditions facing the same physics converge on the same functional weapon classes and same master variables. Source-tier flagged (T0 treatises through T3 enthusiast) with explicit confidence differentials. Audited against HEAD of designs/scene/combat_engine_v1/.

### Already in engine

- **Axis 1 — Measure and reach:** fully modeled. REACH_W system (config.py), reach_sigma/reach_base (systems.py:11-14), measure/approach/close state machine (wrapper.py), longer/shorter assignment with POLE_CLOSE_PENALTY for closes_poorly weapons (systems.py:38-40). The document's "the shorter weapon's whole task is surviving the wide-play gap" is the engine's approach phase; its "reach is always purchased in time and in close-range helplessness" is closes_poorly + the short/close regime flip.
- **Axis 2 — Leverage, bind, contact-decision:** modeled. blade_guard consumed at systems.py:104 (wind sigma) and :235 (bind catch); bind loop in wrapper.py with 3-iteration ceiling. The document's pro-contact sensing (German fühlen) vs anti-contact denial (Fabris rapier) spectrum maps directly to the tradition tactile channel: German tactile=1.35 (pro-contact) vs Italian tactile=1.00, measure=1.25 (anti-contact, distance-controlling). The engine's tradition-as-cognitive-mode architecture IS the contact-decision spectrum.
- **Axis 3 — Cut geometry:** modeled. head field (point/cut_thrust/curved_cut/straight_cut/blunt) on all 12 WEAPONS; GEOMETRY dict with curvature, point_concentration, edge_keenness, cross_section, strike_concentration baked by geometry.py into gap and perc_conc coefficients. The document's "broad point for unarmoured, slender point for armour" is gap (rapier 0.40 vs dagger 1.00 vs longsword_halfsword 1.00).
- **Axis 4 — Mass and percussion vs armour:** modeled. mass, p_auth(mass, pob_frac) for blunt damage-heft (core.py:34-38), percussion field (hand-set, to be unified per WP-1), perc_conc (baked geometry, to be wired per WP-3), armour-tier system in systems.armor_defeat_sigma. The dual-source percussion issue is already the distillation's CONSOLIDATE #1.
- **Axis 5 — Recovery, commitment, tempo:** modeled. commit depth, disposition (5 levels), overcommit_exposure (systems.py), spd field on all weapons, tempo tradition channel (Italian tempo=1.30 = tempo-exploitation specialists). The document's "Silver's true times" and "German Indes striking the opening the bind reveals" are the engine's commitment-window exploitation and init_steal_factor.
- **Axis 6 — Hand exposure:** modeled. hand_guard consumed at systems.py:99 (`PARRY_GUARD_K*(defender.w['hand_guard']-GUARD_NEUTRAL)`) — parry sigma modifier encoding passive hand/forearm protection. Rapier hand_guard=0.90 (swept hilt), spear hand_guard=0.10 (bare shaft), matching the document's hierarchy exactly. The document's "defanging the snake" (active targeting of the weapon hand) is NOT a targetable action — the engine models protection passively, not as a chosen attack mode — but the protection *differential* is live and correctly ordered.
- **Axis 7 — Skill floor vs ceiling:** modeled. hand='Forgiving'/'Standard'/'Demanding' consumed via HANDLE_RANK at systems.py:58 (D_HAND contribution). Forgiving (staff, spear, mace, dagger) = low floor; Demanding (rapier, greatsword, poleaxe, paired_short) = high ceiling. Matches the document's training-time hierarchy (dao "takes ten days" ≈ Forgiving; qiang/jian far longer ≈ Demanding).
- **Convergence thesis:** the document's deepest finding — "independent traditions facing the same physics arrive at the same functional weapon classes" — is EXACTLY what tradition.py's substrate-bias architecture encodes. Traditions are cognitive-mode channel weights over a shared physical substrate: they "re-weight, they do not add new physics" (tradition.py docstring). The document calls traditions "dialects of the same physics"; the engine calls them "a way of reading the same physics" (tradition.py:1). Same conclusion, independently derived.
- **"Every optimization = de-optimization":** the weapon vectors encode this as trade-off structure. Rapier: high reach + high gap + high hand_guard, but low leverage (blade_guard=0.45) + closes_poorly=False but weak in bind (hand=Demanding for the wrong reason). Mace: high percussion but low gap (0.20) + low speed (spd=0.0). The WEAPONS dict IS the differential.
- **Weapon roster coverage:** 10 of 15 functional typologies mapped. Two-handed cut-and-thrust → longsword. One-handed straight → arming. Single-edge saber → sabre. Thrust-centric → rapier. Dagger → dagger. Spear → spear. Bladed polearm → poleaxe. Staff → staff. Percussion → mace. Dual-wield → paired_short (hands=1, hand='Demanding' matching the document's "high coordination" note).

### Improvements

#### WTD-01 [P3, low]

**What:** The poleaxe has `head='blunt'`, but the historical poleaxe combines an axe blade, a hammer face, and a top spike — three distinct striking surfaces. The 'blunt' classification is accurate for the dominant percussion function but obscures the point-thrust capability (gap=0.85 is high, appropriate for a spike-topped pole, but the head type doesn't reflect this).

**Mechanism:** Reclassify poleaxe head from 'blunt' to 'cut_thrust' or introduce a 'mixed' head type. Alternatively: retain head='blunt' (the dominant percussion role) and accept that gap=0.85 already models the spike thrust independently of the head classification.

**NERS impact:** E (partial): a reader sees head='blunt' and expects no thrust capability, but gap=0.85 says otherwise. Low severity because core.coupling and damage() already handle head='blunt' with gap correctly — the seam is cosmetic, not mechanical.

**Adversarial note:** AGAINST: changing head='blunt' to 'mixed' would require adding a new head type to HEAD_REACH, core.coupling, and core.damage, all of which branch on head. The current setup works correctly (blunt percussion + high gap thrust), and the head label is a categorical simplification, not a mechanical error. Recommend: leave as-is, add a comment on the poleaxe line noting the simplification. Not worth the head-type proliferation risk.

### Reject

- **Flexible weapon class (kusarigama, whip-sword, chain whip, urumi)** — the document itself notes the "brutal weakness-profile" (slow recovery, dangerous to wielder and allies, useless in formation, helpless once grabbed) and that the urumi "was rarely used in actual battle." No tradition in tradition.py supports these weapons; no existing channel architecture maps to their unique property (unpredictable trajectory). Adding them would require a new physics model (flexible-body dynamics) for weapons the document itself de-romanticises. Out of scope for v1.
- **Shield as separate mechanical item** — would require an off-hand subsystem. The engine models one weapon per combatant. paired_short partially covers the dual-item case; adding a shield-as-weapon (bash, hook, vision-management) would need a new off-hand action type in the engagement flow. Out of scope.
- **Empty hand / grappling as a weapon entry** — the document correctly identifies the empty hand as "the terminus to which nearly every weapon's weakness-collapse runs." The engine already models this terminus: the clinch field (dead — scheduled for CUT), the close-range regime flip, and the grappling-range physics. Adding an explicit 'unarmed' weapon entry is not needed; the close/grapple is already the engagement endpoint. (The dead clinch field was intended for this but never wired.)
- **Formation/terrain (axis 8)** — mass-battle engine scope, not personal combat. The document acknowledges this is a "battlefield role" concern, not a duel property.
- **Primary+sidearm law (axis 9)** — would require multi-weapon combatant and weapon-switching mid-engagement. Interesting but architecturally significant; out of scope for v1.
- **Active hand-targeting ("defanging the snake") as a chooseable action** — the document identifies this as "the closest thing to a universal counter that crosses every typological boundary." The engine already models hand protection passively (hand_guard in parry sigma); adding active hand-targeting would require a new action type in the engagement flow (target selection: body vs limb vs weapon-hand), a new damage pathway (disable weapon rather than wound), and a new counter-tree. Significant scope expansion. The passive model (protected hands parry better) captures the protective hierarchy without the action-space explosion.

### Creative layer

- The document's "strength-state ⇄ shadow weakness-state ⇄ counter-class" structure for each typology could inform NPC AI weapon-selection logic in the Godot port: given the player's weapon typology, the AI selects from its available weapons the one whose strength-state overlaps the player's shadow.
- The "two universal weaknesses" (commitment/stasis + the hand) as narrator affordances: describe the combat beat's Vor/initiative state in terms of "who is committed / who is reading" and flag hand-exposure moments when hand_guard is low and the defender is in parry.
- The convergence finding as a worldbuilding anchor: Valoria's in-fiction martial traditions converge not by cultural contact but by facing the same physics — the same observation the document makes about real-world traditions. This is the strongest setting-coherence gift in the 5-corpus set.

### Verdict

The document's 9 interface axes map to engine primitives at 7 of 9 (78%). The 2 missing axes (formation/terrain, primary+sidearm) are correctly out of personal-combat scope. The weapon roster covers 10 of 15 functional typologies; the 5 absent (pike, flexible, shield, ranged, empty-hand) are either out of scope or already modeled as engagement endpoints. The document surfaces NO new structural defects — every axis it identifies is already mechanically live in the engine, with hand_guard, blade_guard, HANDLE_RANK, closes_poorly, and the tradition channel weights encoding the exact trade-off hierarchies the document describes. The document's central finding — that independent traditions converge on the same functional classes given the same physics — is the strongest external validation of tradition.py's substrate-bias architecture in the entire 5-corpus set: the engine's design philosophy ("a way of reading the same physics, NOT a separate rule-set") and the document's analytical conclusion ("the common ground is the physics, and the traditions are dialects of it") are the same sentence written independently. One low-severity calibration note (WTD-01, poleaxe head classification) — not actionable without head-type proliferation. Net: architectural confirmation, not debt discovery.

---

## Adversarial Verification (24 completed)

### 1. WP-4

- **Target:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that deriv...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** keep:high
- **Reasoning:** CLAIM IS TRUE AT HEAD — every cited fact verified in the working tree.

(1) DEAD ABILITIES. tradition.py:96 ('vorschlag') and :106 ('sen_no_sen') both carry lever='seize', op='+', value=4.0. The ONLY consumers of TR.ability_bonus/ability_factor in the engine are wrapper.py:158 ('counter_select'), :163 ('anti_overcommit'), :189 ('counter_success'). Grep for 'seize' across the engine dir returns ZERO ability_bonus(_,'seize')/ability_factor(_,'seize') call — only the ABILITIES defs, the doc, and unrelated prose ('seized Vor' comments, systems.py:218 reopen_prob docstring 'reads to seize'). So a fighter who equips vorschlag or sen_no_sen changes NOTHING: ability_bonus(c,'seize') is computable but never invoked. Pure dead weight, both directions of the bug (computed-never-consumed AND a player-facing choice with no path-branch).

(2) THE CUT IS REAL, COMPLETE, AND DELIBERATE. wrapper.py:27-29 — 'Pre-contact seizure CUT 2026-06-05 (Jordan; verified inert - ablation ~0...)'; initiatives explicitly forced even (A.initiative,B.initiative=0.0,0.0). systems.py:243-246 — 'Pre-contact seizure CUT ... seizure_score + initiative_seize removed ... INIT_SEIZE_K 0.45*tanh washed out by per-beat dynamics ... ablation ~0 outcome impact.' config.py has NO INIT_SEIZE_K/SEIZE/seizure key (grep: no matches) — the cut left no orphan config. This is an INTENT GATE, not an accident: the lever was retired from the resolver on purpose after a sim proved it inert.

(3) DOC DRIFT CONFIRMED ON BOTH SOURCES. ability_armature.md:51 marks 'seize | ... | live'; :106 '✅ vorschlag' (seize); :131 '✅ sen_no_sen (seize)'; :167 'Live levers: seize (Vorschlag, Sen-no-sen)'. tradition.py:87-88 ALSO lies in-code: 'LIVE levers this version (wired at their engine sites): seize (+ seizure score)...'. Four doc sites + one code-comment site all assert a wiring that the cut removed. A designer/player reading either believes Japan's flagship sen-no-sen works; it cannot fire.

NERS VERDICT: FAILS N and E as it stands today. N — two abilities + a lever are dead weight; the worst churn failure (equip-changes-nothing). E — the doc is a legibility trap (false 'live' affordance, esp. the JAPANESE flagship). Severity HIGH is correct: this is a P2 inert+drift defect, and the inertness reaches a flagship tradition's signature mechanic, so it's at the top of the P2 band — not P1 (engine resolves correctly; nothing breaks play or mirror-fairness; equipping a no-op is invariant-safe by construction) and clearly not P3.

WHAT SURVIVED THE ADVERSARIAL ATTACK (the addition's NECESSITY bar):
- Option (a) RE-POINT seize at initiative (corpus/ledger D-1 'revive seize, highest leverage'): REJECTED. This is the precise mechanic the engine already proved inert via ablation (systems.py:244 INIT_SEIZE_K 0.45*tanh, ~0 impact, washed out by INIT_GAIN_HIT 0.18/hit + decay + steals). Re-seeding initiative at engagement start (wrapper.py:29) or piping into INIT_STEAL_INDES (wrapper.py:151) re-introduces the SAME wash-out — the standing Vor system (initiative_sigma tanh-bounded + per-beat decay/steals/hit-gains) already owns and dampens that quantity. Adding seize back is lever-pile dressed as a fix: a new additive +4.0 onto a state variable that decay+clamp will erase within a beat, with a DOMINANT-answer risk (always equip the free Vor). It would also be gated on an UNRUN sim (engine non-runnable here) re-deriving non-zero impact — and the prior sim says zero.
- Option (b) RETIRE both abilities + the 'seize' lever and repair the four doc sites (md:51,106,131,167) + the tradition.py:87-88 code comment: SURVIVES as NERS-Necessary. It CONSOLIDATES (removes dead data + a false affordance) rather than adding; it is invariant-safe (deleting no-op equips changes no matchup); it fixes a real dead/broken path — exactly the addition profile that clears the skepticism bar.
- Note the corpus is, per the brief's own verdict, a TURNKEY PROPOSAL not applied (weapon_physics.py absent; its ledger D-1 advocates reviving seize). So D-1 is itself the lever-pile risk the brief flags — and WP-4's option (b) is the disciplined counter to it.
- **Revised:** ADOPT option (b); REJECT option (a)/ledger-D-1's 'revive seize'.

DO (drift-repair + consolidation, NERS-Necessary, invariant-safe, no sim required):
1. tradition.py — remove the two dead abilities 'vorschlag' (:96-97) and 'sen_no_sen' (:106-107), since the 'seize' lever has no resolver consumer. If a Japanese signature is desired NOW, RE-POINT sen_no_sen at an EXISTING live lever that already has documented churn — e.g. counter_success or counter_select (koryū sen-no-sen IS a simultaneous-seize counter; the resolver counter-path at wrapper.py:158/:189 already exists and is calibrated). That gives Japan a working flagship without rebuilding the cut pre-contact contest. Vorschlag (a first-strike-takes-Vor) maps least cleanly onto a surviving live lever and is the better pure-retire.
2. tradition.py:87-88 — delete 'seize (+ seizure score)' from the in-code 'LIVE levers' comment.
3. ability_armature.md — fix all four false 'live' sites: :51 (table row 'seize | ... | live' -> remove the row or mark 'retired (pre-contact seizure cut 2026-06-05)'), :106 ('✅ vorschlag' -> retire/repoint), :131 ('✅ sen_no_sen (seize)' -> repoint to counter_* or retire), :167 ('Live levers: seize (Vorschlag, Sen-no-sen)' -> drop seize). Add a one-line note pointing at wrapper.py:27 / systems.py:243 so the cut is discoverable from the doc.

DO NOT (the rejected revival):
- Do NOT re-point 'seize' at the initiative substrate (engagement-start seed at wrapper.py:29, or INIT_STEAL_INDES at :151). That rebuilds the mechanic the 2026-06-05 ablation proved inert (~0 impact, washed out by INIT_GAIN_HIT/decay/steals); the standing Vor system already owns that churn. Reviving it is lever-pile and would be gated on an unrun sim contradicting the prior result.
- ONLY revive 'seize' if a NEW consumer (NOT the cut pre-contact contest) is designed AND a fresh sim demonstrates non-zero, non-dominant outcome impact that the existing initiative_sigma does not already deliver. Absent that evidence, retire-and-repair is the correct close.

Net: this is a documentation-and-cleanup fix, not a mechanics change — it removes dead weight and a flagship-level legibility trap with zero risk to resolution, mirror-fairness, or invariants.

### 2. WP-3

- **Target:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that deriv...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** keep:medium
- **Reasoning:** VERIFIED AGAINST HEAD. The dead-data claim is true. combatant.py:53 writes WEAPONS[_w]['geo']=_b once at import; grep across the whole engine dir confirms ZERO read sites for ['geo'], 'perc_conc', the baked 'cut' field, baked 'thrust', or 'halfsword'. Every substring hit for thrust/cut is either the 'cut_thrust' head category, a comment/docstring, or geometry.py's own producer functions (geometry.py:37,43,68-71). The combatant.py:53 comment 'full baked surface available to modules' is a FALSE PROMISE — no module reads it. Clean NERS-N violation (orphan emission + dead-data, paid every import).

WHAT SURVIVED THE ATTACK — the perc_conc half is load-bearing, not speculative. The blunt armour-defeat cap is systems.py:124: cap=cfg['ADEF_BLUNT']*(aggressor.w.get('percussion',8)/8.0), a flat 1.3 (config.py:28) scaled ONLY by hand-set percussion/8. Mace and poleaxe BOTH carry hand-set percussion=8 (combatant.py:23,24), so they receive IDENTICAL blunt armour-defeat today — a war-pick does NOT out-defeat a flat mace vs plate. Yet the GEOMETRY table already encodes the distinction (poleaxe strike_concentration 0.85 vs mace 0.45, combatant.py:42,41), baked to perc_conc ~0.93 vs ~0.75 (percussion_concentration, geometry.py:48-51) — then discarded. That is a REAL inert weapon-choice churn loss, not a hypothetical. perc_conc is precisely the missing dimension, and it is the same physical quantity already double-sourced (the brief's two-percussion drift: core.p_auth at core.py:38 derives percussion from mass/pob_frac for the DAMAGE branch; armor_defeat_sigma at systems.py:124 reads hand-set percussion for the ARMOUR-DEFEAT branch — two sources, one quantity).

WHAT DID NOT SURVIVE — the thrust and cut wiring. (1) thrust is collinear with gap: the point/half-sword armour-defeat cap is ALREADY systems.py:126,129 cap=cfg['ADEF_POINT']*aggressor.w['gap'], where gap is baked gap_precision(pc,cs) (geometry.py:28-35). thrust_factor (geometry.py:37-41) is the same (point_concentration,cross_section) primitives minus a curvature term — wiring it is duplicate-lever pile. DELETE is correct. (2) EXTENSION FINDING the target missed: the baked 'halfsword' bool (can_halfsword_thrust, geometry.py:53-56) is ALSO dead AND redundant — the real half-sword switch is hard-coded HALFSWORD_FORM={'longsword':'longsword_halfsword'} (systems.py:148-159), never the predicate. DELETE it too. (3) cut wiring is genuinely speculative: core.coupling/DELIVERY is a flat per-head constant (core.py:53,66-74); adding a cut-quality term creates a new multiplicative damage surface, and the brief's own VERDICT plus the reach-dominated tier-list (r=0.83) mean the spread may be invisible. Gated on an UNRUN sim -> reject/defer.

SEVERITY: keep medium/P2. It is inert/drift, not a correctness break (P1) — the engine resolves fine; the cost is paid import + a false 'available' promise + a swallowed weapon-choice distinction. Not P3 polish because perc_conc is the fix that makes the WP-1 percussion path physically correct and removes the active two-source drift.

NOT a safeguarded intent gate: the comment 'for downstream wiring' (combatant.py:47) and 'available to modules' (combatant.py:53) explicitly assert future consumption that never arrived — this is abandoned scaffolding, not a deliberate reserved hook. NOT stale/already-fixed: grep on HEAD confirms still-zero readers.
- **Revised:** SPLIT the action; do not adopt WP-3 as one unit.

1. DELETE NOW (pure NERS-N win, zero sim needed): drop the baked 'thrust', 'cut', and 'halfsword' fields from geometry.bake (geometry.py:66-72) and stop storing WEAPONS[_w]['geo'] (combatant.py:53) UNLESS step 2 proceeds. thrust duplicates gap (systems.py:126,129); halfsword duplicates the hard-coded HALFSWORD_FORM map (systems.py:148); cut has no consumer and its only candidate (core.coupling) is reach-dominated-invisible and gated on an unrun sim. Also delete the false-promise comments (combatant.py:47,53). This removes import cost and the orphan emission.

2. WIRE perc_conc ONLY IF WP-1 (the blunt percussion repair) proceeds — they are one fix. Make the blunt armour-defeat cap consume perc_conc so poleaxe (war-pick/beak) out-defeats mace (flat) vs plate: change systems.py:124 to scale by the geometry-baked percussion concentration instead of, or in addition to, hand-set percussion/8 — which simultaneously RESOLVES the two-percussion-source drift by routing both the damage branch (core.p_auth) and the armour-defeat branch (systems.py:124) off the same geometry-derived quantity. This is a CONSOLIDATION (one physical quantity, one source), not a lever-add, so it clears the NERS-N bar. If perc_conc is wired, keep WEAPONS[w]['geo'] but trim it to the consumed field(s) only.

3. REJECT outright: wiring 'cut'/'thrust', and adding any K_* gain or pommel_kg primitive for these coefficients. gap+mass+pob_frac+strike_concentration already cover the live consumers.

Net: the honest minimal action is DELETE the three speculative baked fields immediately; gate perc_conc-wiring on WP-1 and treat it as the drift-consolidation that makes WP-1 physically correct.

### 3. WP-1

- **Target:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that deriv...
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** high -> low
- **Reasoning:** The underlying drift is REAL but the target massively overstates its magnitude and live-ness, and its mechanism is partly wrong. What I verified against HEAD:

REAL part: systems.py:124 is the SOLE live consumer of the hand-set w['percussion'] field in the entire engine (Grep confirms: only other percussion uses are core.py's RESIST/HEAD_MODE material-mode keys, unrelated). The damage side does NOT read w['percussion'] — core.strike (core.py:99) already passes p_auth(attacker.w), and core.damage (core.py:82) computes blunt heft from that p_auth, not the field. So 'percussion authority' as a physical quantity already has ONE derived owner (p_auth) for damage; the hand-set field is a vestigial second reader at exactly one armour-defeat site. WP-1 correctly identifies and would close that. That much is true, and N+ in direction (one input retired, no behaviour added).

WHERE THE TARGET IS WRONG (the attack that landed): the claim 'mace & p_auth agree by luck; staff diverge' is quantitatively backwards. Computing from actual HEAD data (combatant.py:22-24): p_auth(mace mass1.2,pob0.6)=8.000 -> hits the min(8) cap; p_auth(poleaxe mass2.5,pob0.45)=8.000 -> also caps. So cap_HAND == cap_PAUTH == 1.300 EXACTLY for both mace and poleaxe (no luck — both saturate). For staff p_auth(mass1.5,pob0.05)=4.110 vs hand-set percussion=4 -> cap 0.668 vs 0.650, a delta of 0.018 sigma. The hand table and the derived value ALREADY AGREE for all three blunt weapons; there is no divergence to fix. WP-1's behavioural impact is ~0.018 sigma on staff-vs-armour and nil elsewhere. The 'staff diverge' example is fabricated — staff's hand value 4 was evidently already chosen to match its p_auth. So the role-conflation is cosmetic (a dead-ish duplicate field), not a live correctness fault. Severity 'high' is wrong; this is P3 polish (dead-field removal), not P1/P2.

WHERE THE ADVERSARIAL NOTE SURVIVED AND INVERTS THE RECOMMENDATION: the AGAINST case is the strongest thing in the target. p_auth(mass,pob_frac) has NO strike-concentration term, so a war-pick and a flat mace of equal mass/balance defeat armour identically — physically wrong (the pick concentrates force). And the engine ALREADY bakes the fix and leaves it dead: geometry.percussion_concentration (geometry.py:48-51) is baked into WEAPONS[w]['geo']['perc_conc'] at combatant.py:53, giving an ordered, physically-correct signal — poleaxe 0.93 (beak) > mace 0.75 > staff 0.62 — that NO module reads (confirmed: Grep shows perc_conc only defined/baked, never consumed). WP-1 as written (replace field with bare p_auth) would lock in the no-concentration model and KEEP perc_conc dead. The cleaner move (WP-1 + WP-3 together: cap = ADEF_BLUNT*(p_auth*perc_conc)/8) revives the dead baked datum AND consolidates the field, yielding poleaxe 1.209 > mace 0.975 > staff 0.414 — physically correct ordering and one fewer dead path. But that is a re-tune that needs the mirror=50 + armour-tier re-sweep the target itself names as a precondition, and the sim is UNRUN on this checkout (engine non-runnable, /home/claude paths). So even the good version is sim-gated.

NERS: N — passes weakly (retires one redundant input), but the churn it removes is near-zero since the values already agree; it neither adds nor removes a branch/choice (CHURN-neutral — armour-defeat ordering is unchanged). S — neutral-to-positive (no seam created). E — neutral. It is NOT a lever-pile (adds nothing). It is a tidy-up, not a fix. Compliant but low-value alone.
- **Revised:** DEFER WP-1 as a standalone change; FOLD it into WP-3. Reasons: (1) WP-1 alone is a ~0.018-sigma no-op — the hand-set percussion table (staff 4, mace 8, poleaxe 8) already agrees with p_auth (4.11, 8.0-capped, 8.0-capped), so it is dead-field removal, not a correctness fix. Reclassify severity high -> low (P3). (2) Done alone it regresses physics by erasing strike-concentration: pick vs flat-mace of equal mass/balance would defeat armour identically (p_auth has no concentration term), and it would leave geometry's already-baked perc_conc (geometry.py:48-51, combatant.py:53) permanently dead. (3) The correct consolidation is the combined edit at systems.py:124: cap = cfg['ADEF_BLUNT'] * (core.p_auth(aggressor.w) * aggressor.w['geo']['perc_conc']) / 8.0 — this simultaneously retires the duplicate hand-set field (WP-1) AND revives the dead baked perc_conc (WP-3), giving the physically-ordered result poleaxe 1.209 > mace 0.975 > staff 0.414. That is genuinely N+ (two dead/duplicate paths collapsed into one live consumer, real churn restored to armour-defeat ordering). (4) GATE on the sweep the target names: this re-tunes mace/staff/poleaxe armour-defeat (staff drops markedly: 0.65 -> 0.414), so it MUST pass mirror=50 + an armour-tier re-sweep before commit — and the engine is non-runnable on this checkout (/home/claude import paths), so that sweep cannot be run here. Until the portability finding is fixed and the sweep is green, this stays a deferred, sim-gated consolidation, not a commit. If Jordan wants strike-concentration kept OUT of armour-defeat by design, then WP-1 is pointless (delete the dead field instead) — confirm intent before touching systems.py:124.

### 4. WP-6

- **Target:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that deriv...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** high -> low (P3, polish/portability hygiene). The literal code defect is real but its stated impact — "engine non-runnable / gates the whole corpus §5 sim" — is FALSE. Downgrade because a working, relative-path consumption path already exists and has demonstrably run the engine.
- **Reasoning:** CLAIM IS LITERALLY TRUE AT HEAD: verified the sandbox inserts exist exactly as cited — core.py:3 (sys.path.insert '/home/claude' + '/home/claude/v32'), combatant.py:4, systems.py:4 ('/home/claude/combat_engine'), wrapper.py:4 (same), plus the runtime 'import r8_parity_harness' inside stamina_max at systems.py:46. The substrate physically lives at tests/sim/v32-combat-balance/ (m1/r1/r8/damage_model all present; transitive deps r2_consequence_wounds, r5_strength_stamina, m7_facing_fov, m3_weapon_class_layer also all present there). weapon_physics.py exists NOWHERE in the tree (find confirms), so the corpus is unapplied — confirmed.

WHAT SURVIVED THE ATTACK (real residue): the engine *directory in isolation* cannot import its substrate, and the leftover '/home/claude' inserts are pure dead code (they prepend nonexistent dirs). For the Godot companion repo (valoria-game) and any naive `python core.py` from the engine dir, this is a genuine portability hazard. CLAUDE.md's "working tree is source of truth" + the retired sandbox-only rule mean these lines SHOULD be deleted/relativized eventually. So a finding survives — but a P3 hygiene one, not a P1 gate.

WHAT BROKE THE ATTACK (the finding's core premise is STALE): the claim "engine non-runnable on this checkout / gates the corpus §5 sim / the turnkey 'drop weapon_physics.py and run §5' fails immediately on import" is FALSE against HEAD. tests/sim/combat-stress-matrix/stress_matrix.py:21-24 ALREADY implements the exact recommended fix: REPO=abspath(__file__/../../..); sys.path.insert(0, REPO/tests/sim/v32-combat-balance) [substrate]; sys.path.insert(0, HERE/_engine) [staged engine] — THEN imports core/systems/wrapper/combatant. Because the harness inserts the real dirs at position 0 BEFORE the engine modules run their own '/home/claude' inserts, and Python silently skips nonexistent sys.path entries, the bad lines are inert no-ops, not failures. stage_engine.py copies the engine verbatim (it patches only derived_scale isolation hooks, NOT the import lines) and the copy still imports fine. DISPOSITIVE PROOF: a real results.json is committed (N=200/position swapped, seed=20260623, mirror 54.8%, full Block1/2/3 win-rate distributions, dated Jun 26 2026, commit 72c890e0 'personal-combat module extraction + reconciliation + stress harness') — the engine demonstrably ran end-to-end on this checkout. So the substrate-import problem is ALREADY SOLVED for the canonical sim path; the corpus §5 gate is NOT blocked by imports.

NERS / ADDITION BAR: WP-6 self-declares "not NERS-scored — correctness/portability P1." Marked ners_compliant=false because it is NOT a NERS-necessary engine change and, more importantly, it does NOT gate the corpus as claimed (the churn/sim-running need is already delivered by the stress harness). It neither consolidates nor unblocks anything currently blocked; it's cleanup of dead prepend lines. Adversarial-note adjudication: the note frames AGAINST as "the sandbox IS canonical execution env" and concludes P1-regardless via Godot. The actual state is stronger than either horn: the project already migrated to a relative-path runner (stress_matrix.py), so the sandbox is neither canonical NOR blocking — it's vestigial. That makes this lower than the note's own floor.

SEVERITY: high -> low/P3. Real defect (dead sandbox inserts + non-portable engine dir in isolation), but no correctness impact, no sim gate, engine proven runnable. Worth fixing for Godot portability and tidiness, but it is NOT a prerequisite that blocks the weapon-physics corpus, and it is NOT P1.
- **Revised:** DOWNGRADE WP-6 from P1-gate to P3 portability hygiene, and CORRECT its premise. Keep: (a) the literal defect — designs/scene/combat_engine_v1/{core.py:3, combatant.py:4, systems.py:4, wrapper.py:4, systems.py:46} carry vestigial sys.path.insert('/home/claude'...) prepends that point at nonexistent dirs; (b) the recommended remedy — replace them with a path computed from __file__ (e.g. os.path.join(os.path.dirname(__file__), '..','..','..','tests','sim','v32-combat-balance')) or vendor m1/r1/r8 + their transitive deps (r2/r5/m7/m3) into the engine dir — this is the correct, Godot-portable shape. RETRACT the false claims: the engine is NOT non-runnable on this checkout and does NOT gate the corpus §5 sim. tests/sim/combat-stress-matrix/stress_matrix.py:21-24 already does the relative-path insert (substrate dir + staged _engine) before importing the engine, and a real results.json (N=200, seed=20260623, committed Jun 26, commit 72c890e0) proves an end-to-end run; the '/home/claude' lines are inert no-ops there because nonexistent sys.path entries are skipped and the real dirs are inserted at position 0 first. So the corpus's "drop weapon_physics.py and run §5" can be exercised TODAY by routing it through the existing stress-matrix runner pattern (or a thin sibling that mirrors lines 21-24), not blocked on this fix. PRIORITIZATION: do the relative-path cleanup as a standalone P3 hygiene commit ([fix] or [cleanup] scope) decoupled from the weapon-physics corpus — it improves Godot portability and removes a real foot-gun for anyone running `python` from the engine dir, but it is not on the critical path for the corpus and must not be sold as a P1 prerequisite. When fixing, delete (don't merely supplement) the dead '/home/claude' inserts and prefer one shared bootstrap (a single _substrate_path.py imported by the engine modules) over repeating the insert in four files — consolidation over duplication.

### 5. WP-5

- **Target:** Weapon-physics calibration+wiring corpus (5 docs + weapon_physics.py): a bottom-up scheme that deriv...
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** medium -> low
- **Reasoning:** VERIFIED TRUE against HEAD, and it survives adversarial attack cleanly. The mechanism holds at every cited site:

(1) The engine ALREADY stores pob_frac per weapon (combatant.py:14-27), alongside mass and head_len/grip_len. pommel_kg appears NOWHERE in engine code (grep over the whole repo: the only HEAD hit for 'pommel-mass' is designs/proposals/weapon_physics_and_concentration_model.md:93, marked [PROPOSED]/vetoable; no pommel_kg in combatant.py, and none of K_REACH/K_HEFT/etc. exist at HEAD — corpus is unapplied, consistent with the brief).

(2) The canonical physics derivation needs only the existing three fields. combat_residuals_pob_f5_findings_2026-06-13.md:143-148 derives authority = sqrt(mass)*pob_frac, effective striking mass = mass*pob_frac^2, and the static first moment M1 = mass*pob_frac*L (L = grip_len+head_len) — all from stored fields, no pommel. weapon_physics_ners_reconciliation.md:17 fixes the semantics (pob_frac = (hand->balance)/head_len; balance = pob_frac*head_len, top-down validated against real specimens line 19) and :28-31 gives the unified MoI law MoI = mass*r_eff^2 with SWING r_eff = pob_frac*head_len. So the target's exact claim — 'MoI ~ mass*pob_frac*head_len^2 from EXISTING fields' — matches the canonical line.

(3) The LIVE consumer already proves it: core.p_auth (core.py:34-38) = min(8, 9.5*(sqrt(mass)*pob_frac)^0.30), reading mass and pob_frac directly. The wired derived authority needs no pommel; pob_frac+mass were the dead inputs and this is their consumer.

(4) The proposal doc itself sides with the target: weapon_physics_and_concentration_model.md:17 states PoB is THE primitive and the head-mass fraction f was 'an invented primitive' now retired; :33-35 use pommel only inside a forward materials->PoB composite ('iron pommel — a point mass'); :93 lists 'pommel-mass' as exactly the descriptor 'the composite model needs to compute mass/PoB from geometry.' pommel_kg is therefore a primitive for a FORWARD computation (materials -> PoB) the engine never performs — every engine output reads PoB (as pob_frac) directly. This is precisely NERS-N: an ADDED primitive that is not necessary because an existing one suffices.

WHAT SURVIVED THE ATTACK: I tried to break it three ways and could not.
 - 'Maybe pommel_kg is needed for some output pob_frac can't give.' No: every targeted output (heft via MoI, str_demand via static_moment) is a function of {mass, pob_frac, head_len, grip_len}; the canonical formulas (M1, MoI, P_auth) are all written in those terms with zero pommel term.
 - 'Maybe it is a safeguarded intent gate.' No — the proposal explicitly demotes pommel to a forward-computation helper and elevates PoB to the primitive; treating it as optional is the doc's own stance, not a contradiction of intent.
 - 'Maybe stale/already-fixed.' Partially the opposite of stale-against-it: P_auth is NOW wired (core.py:34) using pob_frac directly, which strengthens the target — the one place mass+pob_frac became live needed no pommel, empirically.

CAVEATS / NOT-CONFIRMED (the only soft spots, all in the target's secondary justification, not its core):
 - The ners_impact's 'rho=0.69 vs the existing pob_frac and 3 named residuals' and the adversarial_note's specific numbers (longsword pob_frac 0.14 -> physics 'rearward -0.09'; staff 0.05 -> 'computed forward 0.12') do NOT appear in any HEAD doc I could find (grep for pommel/0.69/rho/residual/back-solve across designs/). These appear to come from a corpus preliminary doc not present at this checkout. They are plausible and don't change the verdict, but they are unverifiable here — flagging per the anti-fabrication discipline. The HEAD docs instead frame pob_frac as the source the physics derives FROM, and openly note pob_frac is hand-assigned with only 3 blunt anchors to calibrate against (combat_residuals_pob_f5_findings_2026-06-13.md:160-161), which supports the adversarial_note's milder point (values are hand-set and improvable) without the specific rho figure.

SEVERITY: medium -> low. This is an ADDITION-rejection / 'treat as optional' recommendation, and adopting it is a no-op on the live engine (it declines to add a field that isn't there). It correctly prevents a future lever/primitive-pile, but nothing at HEAD is broken, inert, or drifting because of it — pommel_kg does not exist in the working tree. The churn/correctness stakes are low; the value is purely 'do not let an unnecessary primitive into combatant.py.' That is a P3-polish-flavored guardrail, hence low. (Compare the corpus's genuinely medium items — the two-percussion-source drift in systems.armor_defeat_sigma and the dead baked geo — which touch live state; this one does not.)
- **Revised:** ADOPT the target's recommendation; it is NERS-sound and verified. REJECT pommel_kg as a live weapon-vector field. The engine already carries pob_frac (combatant.py:14-27) with mass and head_len/grip_len, and every physics output the corpus wants is computable from those: authority = sqrt(mass)*pob_frac (already wired at core.py:34-38 as P_auth), static first moment M1 = mass*pob_frac*L, MoI = mass*(pob_frac*head_len)^2 (canonical law, weapon_physics_ners_reconciliation.md:28-31; M1 derivation combat_residuals_pob_f5_findings_2026-06-13.md:148). pommel_kg is a primitive only for the FORWARD materials->PoB composite (proposal :33-35, :93) that the engine never runs, so it is NERS-N-unnecessary — exactly the 'an ADDED mechanic must itself be necessary' rule.

Concretely:
 1. Do NOT add pommel_kg to combatant.WEAPONS. If a future WP-2 wants MoI/heft, write it against the existing fields: r_eff = pob_frac*head_len, MoI = mass*r_eff^2 (SWING), with the POLE/HEAD-HEAVY r_eff variants from weapon_physics_ners_reconciliation.md:28-34 — no new per-weapon number to source or maintain.
 2. Treat the pob_frac VALUES as the legitimate target for one-time data correction, NOT a reason to ship pommel_kg. The adversarial_note is right that some hand-set pob_frac may be off, and the HEAD docs concede pob_frac is hand-assigned with only 3 blunt anchors (combat_residuals_pob_f5_findings_2026-06-13.md:160-161). If desired, back-solve the physical PoB OFFLINE and overwrite the pob_frac literals in combatant.py — an edit to an existing field, leaving the live vector unchanged. Do this only behind the mandatory re-baseline gate (mirror=50, per-attribute tier, no-one-shot) per proposal :86-87 and weapon_physics_ners_reconciliation.md:111 — and note such a correction is currently a NEAR-NO-OP on outcomes, since blunt damage is tanh-saturated (mace/poleaxe identical on vs off; combat_residuals_pob_f5_findings_2026-06-13.md:185-190), so corrected pob_frac will shift authority numbers without moving win-rates until the separate continuous-transmission re-baseline lands.
 3. Drop the rho=0.69 / per-weapon residual figures from the justification UNLESS they can be reproduced — they are not in any HEAD doc on this checkout (anti-fabrication). The verdict stands without them: the case for treating pommel_kg as optional rests on the canonical formulas already being written in {mass, pob_frac, head_len}, which is verifiable at HEAD.

Net: this is a correct guardrail against primitive-pile, severity low (no live breakage; declines a field absent from the working tree). It pairs with the corpus's higher-value drift-repairs (the two-percussion-source in systems.armor_defeat_sigma reading hand-set w['percussion'] vs core.p_auth; the dead baked geo thrust/cut/perc_conc/halfsword) — adopt those at higher priority; this one simply ensures the physics layer, if activated, does not import an unnecessary primitive.

### 6. I1

- **Target:** Martial-morphology union (4 vols): vol1 = 12-dim primitive scheme + 20 throughlines + 6 meta-through...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** high -> medium-high (P2)
- **Reasoning:** VERIFIED TRUE against HEAD on every cited point. (1) tradition.py:96 'vorschlag' and tradition.py:106 'sen_no_sen' both set lever='seize'. (2) ability_bonus (tradition.py:116-122) is the ONLY consumer path for op='+' levers, and grep of the whole engine dir + the substrate (tests/sim/v32-combat-balance/) + core.py + tests/valoria/ confirms it is invoked at exactly two sites: wrapper.py:163 ('anti_overcommit') and wrapper.py:189 ('counter_success'). No call anywhere passes 'seize'. The substrate has no 'seize'/'equipped'/ability machinery at all (the only 'set_bonus' hit, m5:94, is an unrelated stance-coherence helper). (3) The pre-contact seizure consumer was genuinely CUT 2026-06-05 (wrapper.py:27; systems.py:243-246) for empirical inertness (ablation ~0, washed out by per-beat dynamics: INIT_GAIN_HIT 0.18/hit, decay, steals). So the module contract for both abilities is IN(equipped) -> resolver(nothing) -> OUT(nothing): textbook dead-data, the canonical NERS-N lever-pile defect. Churn is exactly zero -- equipping vorschlag or sen_no_sen branches no path, a choice with no consequence, violating the churn principle. (4) The doc-drift is real and worse than a single line: tradition.py:87 calls 'seize' a LIVE lever 'wired at their engine sites,' AND ability_armature.md asserts it in FOUR places -- line 51 (lever map: seize = **live**), line 106 (Vorschlag built ✅), line 131 (sen_no_sen built ✅ -- the Japanese flagship), and line 167 (Live levers: seize (Vorschlag, Sen-no-sen)... 'Each shows a positive, bounded edge'). That last clause claims a measured positive edge for a mechanic that cannot fire -- a fabricated efficacy claim, the sharpest instance of the corpus's own vol3-F2 'tagging is not weighting' / F9 'taxonomic seduction' lesson turned inward: the label asserts weight the code does not carry.

NERS verdict: NOT compliant as-is (N fail: dead lever stored/never consumed; E fail: doc says live, code says inert). The TARGET (an addition that collapses/repoints/deletes) is the right shape but is itself two proposals with opposite NERS standing, and they must be split:

(A) DELETE the two seize abilities + repair the five doc claims (tradition.py:87; ability_armature.md:51,106,131,167). This CONSOLIDATES and removes a dead path -- it clears the NERS-N bar trivially because it removes a lever rather than adding one. Mirror-fairness and invariants are untouched (equipped defaults empty; removing two never-consumed dict entries changes no roll). This half is unconditionally correct.

(B) REPOINT to a new 'seize' consumer (init_steal_factor bump / one-time initiative grant at engagement start). This does NOT clear the NERS-N bar on the evidence in front of us. The adversarial note is decisive and correct: the pre-contact seize was cut BECAUSE it was empirically inert -- a new seize consumer risks re-introducing the exact washed-out mechanic Jordan already removed (wrapper.py:27). It is gated on an UNRUN ablation sim, and the engine is non-runnable on this checkout anyway (core.py imports dead /home/claude paths -- itself a finding). Per the addition rubric (default to skepticism; reject/defer unless it CONSOLIDATES or fixes a dead path), (B) is lever-pile by another name until it clears an ablation bar that beats the one that already killed the mechanic. DEFER (B); it is not justified now.

Severity: target says 'high.' The defect is real and load-bearing for legibility/honesty, but it is NOT P1 -- it breaks no play or correctness (empty equipped = invariant-safe; the dead abilities cannot corrupt a roll). It is P2: inert mechanic + multi-site doc drift incl. a fabricated 'positive edge' claim. Adjust high -> medium-high (P2). It earns the top of P2 because of the fabricated-efficacy line (armature:167) and because it mislabels the JAPANESE flagship as shipped.
- **Revised:** ACCEPT the DELETE-and-document half; DEFER the repoint half. Concretely:

1. DELETE 'vorschlag' (tradition.py:96-97) and 'sen_no_sen' (tradition.py:106-107) -- both are pure dead-data (lever='seize', no consumer anywhere in engine dir, substrate, or core.py). This removes a dead lever, the cleanest NERS-N win.

2. REPAIR the doc drift in all five places, do not fix only line 87:
   - tradition.py:87 -- drop 'seize' from the LIVE-levers list (live set is now counter_success, counter_select, anti_overcommit).
   - ability_armature.md:51 -- 'seize' status live -> CUT/removed (or 'no live consumer; pre-contact seizure ablated 2026-06-05').
   - ability_armature.md:106 -- Vorschlag Built? ✅ -> ✗ (no lever); demote to 'candidate (needs a seize consumer that clears ablation)'.
   - ability_armature.md:131 -- sen_no_sen ✅ -> ✗; the Japanese flagship is a candidate, not built. (If a flagship Japanese ability is wanted shipped, route it onto the LIVE precommit channel via eff_cw, or onto counter_success/counter_select as a go-no-sen counter -- that maps sen to a consumer that already fires, no new lever.)
   - ability_armature.md:167 -- remove 'seize (Vorschlag, Sen-no-sen)' from Live levers and strike the 'Each shows a positive, bounded edge' clause as it applies to seize (a measured-edge claim for a mechanic that cannot fire is anti-fabrication-flaggable).

3. DEFER any new 'seize' consumer (init_steal_factor bump / engagement-start grant). Do NOT build it on this pass. Gate it behind a fresh ablation that must beat the 2026-06-05 result (which found ~0 impact, washed out by INIT_GAIN_HIT/decay/steals). The engine is also non-runnable here (core.py imports nonexistent /home/claude paths), so no ablation can be run on this checkout regardless. Until an ablation clears the bar, a re-added seize consumer is lever-pile (E/N fail) by another name.

Net: this is the corpus's own vol3-F2/F9 lesson applied to the engine -- a well-grounded MEANING (German Vorschlag, Japanese sen-no-sen) tagged as carrying EFFICACY it does not. The honest move is to stop asserting the weight (delete + fix docs), not to manufacture a consumer the dynamics will wash out a second time. If the German/Japanese seizing IDENTITY should still register mechanically, prefer repointing sen onto the already-live precommit/tempo channels through eff_cw rather than resurrecting the cut pre-contact lever -- one number doing more work beats a new subsystem.

### 7. I2

- **Target:** Martial-morphology union (4 vols): vol1 = 12-dim primitive scheme + 20 throughlines + 6 meta-through...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** medium -> low
- **Reasoning:** STRUCTURAL CLAIM CONFIRMED vs HEAD. Two code sources for blunt percussive authority do coexist: (1) DAMAGE seam — core.py:99 `damage(... p_auth(attacker.w) ...)` -> blunt heft = 3.0*(p_auth/8.0) at core.py:82, with p_auth(w)=min(8, 9.5*(sqrt(mass)*pob_frac)**0.30) (core.py:34-38) derived from w['mass'],w['pob_frac']; (2) ARMOUR-DEFEAT seam — systems.py:124 `cap=cfg['ADEF_BLUNT']*(aggressor.w.get('percussion',8)/8.0)` reads the HAND-SET w['percussion']. So one physical concept (blunt percussive authority) is computed two ways across two seams. The p_auth docstring (core.py:35-37) explicitly claims it 'Replaces the hand-set per-weapon percussion' and that 'pob_frac & mass were dead weapon inputs' — but the hand-set field is NOT dead, systems.py:124 still consumes it, so the migration is genuinely HALF-DONE and the docstring OVERSTATES completion. That is a real role-conflation/doc-drift finding (NERS N+S: one concept should have one source; the lingering field plus the over-claiming docstring is the defect).

THE ATTACK BREAKS THE SEVERITY, NOT THE EXISTENCE. The target frames this as a latent inconsistency where 'the whole roster' defeats armour on one number and damages on another, citing staff (perc=4 hand-set vs derived). I computed every weapon that actually reaches the systems.py:124 blunt branch (head=='blunt': ONLY staff, mace, poleaxe — verified against combatant.py:22-24; longsword_halfsword has head='point' so it NEVER hits the blunt branch, making its 0.500-vs-0.661 gap irrelevant). Results: staff hand-set/8 = 0.500 vs p_auth/8 = 0.514 (1.4pp); mace 1.000 vs 1.000 (exact); poleaxe 1.000 vs 1.000 (exact). The blunt percussion roster values were hand-tuned to TRACK the p_auth derivation. So at HEAD there is NO observable mechanical divergence — the two seams produce a practically identical number in every live blunt case. The defect is a maintainability hazard (the two could silently drift if one is edited without the other) and an over-claiming docstring, NOT a present-tense balance inconsistency. That is P2/medium-leaning-low, not the medium the target asserts; I land it at LOW.

ADVERSARIAL NOTE SURVIVES AND IS VINDICATED. The case-against (p_auth calibrated for the DAMAGE gradient; hand-set percussion tuned against the ADEF_THRESHOLD armour tier-list at config.py:28-29; the two MAY have been kept separate on purpose to preserve two independently-validated calibrations) is exactly right — but the data shows the separation is currently a no-op for the live roster, so the 're-sweep before merge' caution applies to the principle, not to any actual number that would move (only staff shifts, +1.4pp). The fix is therefore low-risk for THIS roster but the warning is structurally sound for any future blunt weapon whose hand-set perc is set to disagree with its mass/pob_frac.

CORPUS/ADDITION LENS: I2 is a CONSOLIDATION (kill a half-dead field + reconcile a seam), not a lever-pile addition, so it does not fail NERS-N on its own terms — it REMOVES a variable rather than adding one. The Martial-morphology corpus is orthogonal here: this is an efficacy-resolver plumbing issue, not a meaning-morphology import; the corpus's 'meaning and efficacy run on separate tracks' finding has no bearing. ners_compliant=false because HEAD currently carries the role-conflation (one concept, two variables) + the overstated docstring; that is the live NERS-S/N debt the finding names.
- **Revised:** ACCEPT as a P3/low CONSOLIDATION-AND-DOC-FIX, not the medium balance-inconsistency the target frames. Two concrete sub-fixes, in priority order:

1) DOC-DRIFT (do now, zero-risk): the p_auth docstring (core.py:35-37) claims it 'Replaces the hand-set per-weapon percussion' and that mass/pob_frac are 'dead weapon inputs and this is their consumer.' Both clauses are false at HEAD — systems.py:124 still consumes the hand-set w['percussion'] for blunt armour-defeat. Either soften the docstring to 'replaces the hand-set percussion ON THE DAMAGE PATH ONLY; the armour-defeat path (systems.armor_defeat_sigma) still reads the hand-set field — migration pending' OR complete the migration per (2). This is the load-bearing finding: the code is fine, the comment lies.

2) SEAM CONSOLIDATION (optional, do under the sim): route systems.py:124 through core.p_auth(aggressor.w)/8.0 so blunt armour-defeat and blunt damage read ONE quantity; then delete the hand-set 'percussion' field from all WEAPONS rows. VERIFIED COST: for the only three blunt weapons that reach this branch (staff/mace/poleaxe), the swap moves staff by +1.4pp (0.500->0.514) and leaves mace/poleaxe EXACTLY unchanged (both clamp to 8/8=1.0). So the armour tier-list (mace/poleaxe vs plate) is preserved to within rounding and the re-sweep the adversarial note demands will confirm rather than overturn. longsword_halfsword's hand-set/derived gap (0.500 vs 0.661) is a non-issue because its head is 'point', not 'blunt'.

DEFER GATE: (2) is gated on the armour-defeat invariant sim being green post-swap (the adversarial note's condition). Given the numbers, that sim is a formality, but run it before deleting the field. Do NOT bill this as fixing a play-affecting inconsistency — there is none today; bill it as removing a latent drift hazard and a false docstring.

### 8. TA-02

- **Target:** tempo_approach
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** P1 -> P2
- **Reasoning:** VERIFIED against HEAD. wrapper.py:68 reads `close_rate=cfg['CLOSE_RATE_K']*S.balance_eff(shorter,0,cfg)/3 * S.weapon_tempo(shorter,cfg,ffat[shorter])/2`. The structural core of the claim survives the attack: balance_eff's `fat` arg IS hardcoded to 0 on this line while `ffat[shorter]` is correctly threaded into weapon_tempo on the SAME line; ffat is computed at wrapper.py:39 and is used correctly everywhere else in the beat loop (weapon_tempo/close_tempo at 50-51; fat_a/fat_d at 108,115,135,139,140,163). balance_eff(c,fat,cfg) does consume fat as `(1-cfg['FATIGUE_FOOT_K']*fat)` (systems.py:69) with FATIGUE_FOOT_K=0.30 (config.py:46), so a real 30%-slope footwork-fatigue channel is genuinely dropped from the balance component of close_rate. So far the claim holds.

WHAT DID NOT SURVIVE — the claim's headline consequence is FALSE. "A fighter at max fatigue closes at the same rate as a fresh fighter" is wrong: close_rate is PROPORTIONAL to weapon_tempo(shorter,cfg,ffat[shorter]), and weapon_tempo applies `t*=(1-cfg['TEMPO_FATIGUE_K']*fatigue)` with TEMPO_FATIGUE_K=0.25 (systems.py:29, config.py:14). At max fatigue (ffat=1.0) close_rate therefore falls ~25% via the tempo factor before displ/floor interactions. Fatigue is NOT absent from close_rate; only the balance SUB-channel's fatigue is missing. The defect is an under-modeled footwork-fatigue channel, not a fatigue-blind close. This overstatement is why P1 ("breaks play at the extreme") is too hot — the behavior is wrong but bounded, graded, non-crashing, and fatigue still degrades closing. Correct severity is P2 (inert/under-wired channel + factual overstatement of impact).

INTENT-GATE CHECK: not a deliberate double-count guard. balance_eff = footing channel; weapon_tempo = cadence channel; they are distinct physical quantities, so zeroing balance's fatigue is not avoiding a double-count of the tempo fatigue. The companion state map (combat_engine_flow_and_state_map.md:48) merely transcribes the call as `bal_eff(shorter)` and nowhere flags the zero as intentional, and separately documents balance_eff's `(1-.30·fatigue)` slope (line 237) — i.e. no documented gate, just an undocumented zero. Not stale/already-fixed: the zero is live at HEAD.

ADDITION FOUND (claim missed it): the SAME hardcoded zero recurs at systems.py:223 inside reopen_prob: `foot = balance_eff(longer,0,cfg)/3`. The LONGER weapon's footwork in the reopen attempt is likewise fatigue-blind. This is a second instance of the identical omission in the approach/reopen geometry module, evidencing a systematic copy-pattern oversight rather than a one-off, and any fix must cover BOTH sites for parity.

NERS-NECESSARY: the fix is NOT a new mechanic/lever (no lever-pile, no E/N fail). balance_eff already accepts+consumes fat; ffat[shorter] already exists in scope at the call site; reopen_prob already receives the combatants. Threading the existing fatigue value is pure CONSOLIDATION onto the channel the rest of the loop already uses — mirror-fair (symmetric in shorter at :68, symmetric in longer at :223), invariant-preserving, gated on no unrun sim. This is a real broken/under-wired path, so it should be fixed, not deferred.
- **Revised:** ACCEPT (downgraded P1->P2) as a fidelity/under-wired-channel fix, with the claim's consequence sentence corrected and the scope widened.

Correction to the claim text: replace "A fighter at max fatigue closes at the same rate as a fresh fighter, breaking the physical model" with: close_rate STILL falls with fatigue via the weapon_tempo factor (TEMPO_FATIGUE_K=0.25, ~25% at max fatigue); what is missing is the balance/footwork fatigue sub-channel (FATIGUE_FOOT_K=0.30). The model is under-fatigued on footing, not fatigue-blind.

Fix (two sites, for parity):
1. wrapper.py:68 — `S.balance_eff(shorter,0,cfg)` -> `S.balance_eff(shorter,ffat[shorter],cfg)`. ffat is already in scope (wrapper.py:39).
2. systems.py:223 (reopen_prob) — `balance_eff(longer,0,cfg)/3` -> thread the longer's current fatigue. reopen_prob's signature does not currently take fatigue, so either pass ffat[longer] in from the call site (wrapper.py:60) or compute `max(0.0,1-longer.stamina/max(1,longer.stamina_max))` inside reopen_prob to match the loop's fatigue definition.

This is a consolidation onto the existing fatigue channel — no new lever, mirror-fair, no sim required. It increases the cost of a fatigued fighter trying to close (intended churn: stamina management gains a footwork dimension on the approach/reopen seam). Do NOT escalate to P1 and do NOT add any new fatigue coefficient — reuse FATIGUE_FOOT_K via the existing balance_eff path.

### 9. I4

- **Target:** Martial-morphology union (4 vols): vol1 = 12-dim primitive scheme + 20 throughlines + 6 meta-through...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** medium (unchanged) for the dead-data finding; but the proposed FIX is partly mis-aimed — downgrade the "wire perc_conc into p_auth" recommendation and re-point it at armor_defeat_sigma
- **Reasoning:** VERIFIED TRUE against HEAD. combatant.py:51-53 bakes the full {gap,thrust,cut,perc_conc,halfsword} dict and stores WEAPONS[w]['geo']=_b; only ['gap'] is extracted (:52), and gap already overrides the hand-set gap one line up. Grep across the entire engine dir confirms 'geo' is read at ZERO sites; thrust/cut/perc_conc/halfsword are never consumed (every 'thrust'/'cut'/'halfsword' hit is comment prose, the head-category string 'cut_thrust', or the SEPARATE hand-maintained HALFSWORD_FORM table at systems.py:148 — not the baked boolean). geometry.py:60 docstring "for downstream wiring" — the wiring never landed. This is the same dead-data pattern as I1/I2: computed at build, stored, never read. NERS-N FAIL (four dead coefficients = lever-pile); the corpus's own distilled §6 "taxonomic seduction" lesson (elaboration outran the load-bearing variables) is exactly this defect at the engine layer. So: real=true, ners_compliant=false.

WHAT SURVIVED THE ATTACK — and what did NOT:

(1) The dead-data finding is rock-solid. Not an intent gate, not safeguarded, not stale: the state map (companion, flagged stale) never even indexes this surface, and ability_armature.md makes NO claim about geo/halfsword (the doc-drift findings concern 'seize', a different surface), so there is no doc-drift here either. Pure inert baked data.

(2) The target's PROPOSED FIX is partly mis-aimed and must be corrected. The target says wire perc_conc into core.p_auth, calling it "the elegant move" that "dovetails with I2's percussion unification." Three problems found by reading the code + running the numbers:
  (a) p_auth (core.py:34-38) feeds DAMAGE HEFT (core.strike->damage, core.py:99,82), NOT armour-defeat. But perc_conc's OWN docstring (geometry.py:48-50) says it "Multiplies the §4 percussion AUTHORITY" used for armour-defeat, and the "beak/pick defeats plate better than a mace flat" intuition is an ARMOUR-DEFEAT claim. The natural consumer is therefore systems.armor_defeat_sigma (systems.py:124), NOT p_auth.
  (b) p_auth ALREADY SATURATES: I computed p_auth(mace)=8.0 and p_auth(poleaxe)=8.0 (both hit the min(8.0,...) cap). So wiring perc_conc into p_auth would still see both blunt heavies clamped — the beak-vs-flat distinction the target wants would be partially swallowed by the cap on the damage path. perc_conc, by contrast, cleanly discriminates: poleaxe 0.93 vs mace 0.75 vs staff 0.62.
  (c) The REAL drift is at systems.py:124: armor_defeat_sigma still reads the HAND-SET aggressor.w.get('percussion',8)/8.0 for blunt armour-defeat, while core.p_auth derives blunt authority from mass/pob_frac for damage. That is two competing percussion sources for one physical quantity (the I2 cross-reference is correct). Wiring perc_conc into p_auth does nothing about this; it leaves the hand-set percussion at :124 untouched. The unification only pays its way if perc_conc (or p_auth) REPLACES the hand-set read at systems.py:124.

(3) The adversarial note is correct and load-bearing. geometry.py:13-14 states the coeffs are "calibrated to REPRODUCE the already-validated §4 values" — so wiring thrust/cut now would perturb a validated tier-list for marginal gain. Wiring all four speculatively is gold-plating, the inverse of the distillation's cut-to-the-load lesson. CHURN test: thrust/cut/halfsword would only branch outcomes if the head-category proxies they refine actually under-serve play — no evidence of that demand exists; the head categories already drive coupling/legibility/armor-defeat. So three of the four coeffs are NOT NERS-Necessary additions.

NET: finding real (P2/medium, dead-data + a genuine competing-source drift at systems.py:124). The cheap conservative move (bake only gap, drop the stored dict) is correct UNLESS perc_conc is wired — and if wired, it must go to armor_defeat_sigma (replacing the hand-set percussion), not to the saturating p_auth.
- **Revised:** Confirm the finding (P2/medium): combatant.py:53 stores WEAPONS[w]['geo']=_b but only ['gap'] is consumed; thrust/cut/perc_conc/halfsword are dead baked data (NERS-N fail / lever-pile), and the bake docstring's "downstream wiring" never landed. Pick ONE of two paths, not the target's as-written p_auth wiring:

PATH A (default — conservative, cheap): In combatant.py, drop line 53 entirely and keep only line 52 (WEAPONS[w]['gap']=_b['gap']). In geometry.bake, either keep returning the full dict (pure helper, harmless) or trim it to {gap}. Net: removes four dead coefficients with zero risk to the validated §4 tier-list. This is the move the corpus's distilled §6 / vol3 F9 endorses (cut to the load-bearing).

PATH B (only if you want the I2 percussion unification to actually pay its way): wire perc_conc into systems.armor_defeat_sigma:124 — NOT into core.p_auth. Replace `aggressor.w.get('percussion',8)/8.0` with a perc_conc-scaled term so a poleaxe BEAK (perc_conc 0.93) defeats plate better than a mace FLAT (0.75) and a wooden staff (0.62) — the distinction p_auth CANNOT make because it saturates both heavies at 8.0. This single wiring (i) gives one baked coefficient a real consumer, (ii) collapses the two-competing-percussion-source drift (hand-set w['percussion'] vs derived authority) into one derivation, and (iii) re-calibrate ADEF_BLUNT against the harness afterward to preserve the validated armour tier-list. Still bake-and-drop thrust/cut/halfsword (Path A) — do NOT wire them speculatively; they would perturb a calibrated tier-list for no demonstrated churn (gold-plating).

REJECT the target's literal recommendation to wire perc_conc into p_auth: wrong consumer (damage heft, not armour-defeat), hits the p_auth saturation cap, and leaves the systems.py:124 drift it claims to fix fully intact. If acting on Path B, also update geometry.py's "for downstream wiring" docstring and combatant.py:46-47 comment to name the live consumer. Note both PATH B and any tier-list re-calibration are gated on an UNRUN harness sweep — the engine is non-runnable on this checkout (core.py:3 / systems.py:4 import dead /home/claude paths), so land Path A now and defer Path B behind a sim sweep.

### 10. TA-01

- **Target:** tempo_approach
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1->P2
- **Reasoning:** STRUCTURAL CLAIM IS TRUE against HEAD. systems.weapon_tempo (systems.py:17-31) is consumed at TWO sites for two physically distinct quantities: (1) action-frequency governor — wrapper.py:50 `rate={c:S.weapon_tempo(...)}` then :52 `ready[c]+=rate[c]` accumulating toward ACT_THRESHOLD=2.5; and (2) closing-MOVEMENT speed — wrapper.py:68 `close_rate=CLOSE_RATE_K*balance_eff(shorter)/3 * weapon_tempo(shorter)/2`, which decrements measure_gap (:70). One variable, >1 job = role-conflation by the audit's own definition. Undocumented as intentional: the state map (combat_engine_flow_and_state_map.md:48) records only the mechanical formula, no rationale; and the armature (ability_armature.md §2b/§2c) reserves a SEPARATE `measure` competence for "closing" (status: pending/unwired) — so tempo is currently squatting in the role the measure channel is meant to own. This is a transitional conflation, not a designed one.

MAGNITUDE CONFIRMED AND LARGER THAN CLAIMED in the live cases: at agi=4/fat=0/poise=1, weapon_tempo = sabre 3.20, longsword 1.50, dagger 3.80, mace 1.20, poleaxe 0.90 (computed from config). The tempo factor alone makes closing rates differ up to ~4x (poleaxe vs dagger) for identical footwork.

WHAT SURVIVED THE ATTACK (3 corrections to the finding):
1. The finding's OWN worked example is WRONG. longsword and sabre have EQUAL reach_base (6.40 == 6.40). On equal reach measure_gap=0 -> closed=True at engagement start (wrapper.py:26), so the APPROACH branch (and close_rate) NEVER executes for that pair. The cited "longsword closes ~40% slower than a sabre" matchup is precisely the case where the conflation is INERT. The defect is real but the illustration is misaimed.
2. The defect IS live where a SLOW weapon is the SHORTER one (close_rate is computed only for `shorter`, wrapper.py:68): e.g. mace (reach 5.45, tempo 1.20) vs rapier (7.15) — the mace closes at ~0.32x a dagger's rate purely from swing-cadence, with footwork (balance_eff) already a separate term. THAT is the genuine spurious gradient.
3. MIRROR-FAIRNESS IS NOT BROKEN: equal reach -> no approach phase -> close_rate never fires in any mirror; the invariant suite is untouched. This is why it is not P1.

There is a partial steel-man for tempo-in-closing (managing a heavy weapon while advancing into a stop-hit window is slower), but that residue is ALREADY modelled separately by the (1-displ) stop-hit term (wrapper.py:74) and approach_displace (systems.py:209) — so tempo in close_rate is redundant, not load-bearing.

SEVERITY: P1->P2. Engine runs, output is bounded/monotone, no cliff, no unbounded loop, mirrors fair. It is an undocumented role-conflation that is inert-in-mirror but injects a physically-spurious weapon-cadence->walking-speed gradient in asymmetric-reach approach. That is the canonical P2 (ambiguity/inert-in-some-paths/drift), not a play/correctness break. NERS: fails N (tempo doing a second job the measure competence should own) and E (one number, two jobs); R/S hold (bounded, no cliff).
- **Revised:** ACCEPT as a real P2 role-conflation (down from P1), but FIX BY CONSOLIDATION, not a new lever. Recommendation: remove weapon_tempo from close_rate (wrapper.py:68). Closing speed should be governed by the FOOTWORK/MEASURE competence that the engine already has: balance_eff (already the other factor in the line) is the agi-derived footwork term, and ability_armature §2b/§2c already reserves the `measure` channel for "closing" (currently pending/unwired). Route close_rate through balance_eff (and, when the eff_cw measure-channel sweep lands, through TR.eff_cw(shorter,'measure')) so closing speed = footwork x measure, with weapon cadence confined to its single legitimate job (the ready accumulator). Adding a brand-new 'mobility' coefficient would be a lever-pile (N/E fail) and is rejected; the right home already exists.

DO NOT introduce a heavy-weapon closing penalty to "preserve" the current behaviour — the heavy weapon is almost always the LONGER weapon (it is being closed ON, it does not close), so removing tempo from close_rate barely touches heavy-weapon owners and correctly stops penalizing the rare slow-SHORT weapon (mace) for its swing cadence on its walk-in.

GATING: this changes asymmetric-reach approach dynamics and interacts with STOPHIT_FULL_GAP/STOPHIT_CHANCE pacing, so it is CALIBRATION-SENSITIVE and must be DEFERRED behind a sim re-sweep (the substrate is non-runnable on this checkout via the dead /home/claude imports — itself a separate finding). Land only after confirming asymmetric-reach win-rates and approach-beat counts stay in band. Also correct the finding text: replace the longsword-vs-sabre example (equal reach, inert) with a slow-short vs fast-short example (mace/poleaxe vs dagger/sabre) where the conflation is actually live.

### 11. TA-03

- **Target:** tempo_approach
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** CORE CLAIM SURVIVES, with corrections. Confirmed against HEAD: `c.grip` is assigned exactly ONCE — combatant.py:66 `self.grip='normal'`. A full grep of the engine dir for any write to `.grip` returns nothing; there is no `c.grip='choke'`/`'lunge'` in wrapper.py or systems.py, no event, ability, or AI path that mutates it. Every other occurrence is a READ via getattr(c,'grip','normal') (systems.py:25, 37, 191) or a comment. Therefore the grip state machine is genuinely missing its writer, and the grip=='choke' / grip=='lunge' branches in weapon_tempo (systems.py:26-27) are unreachable. CHOKE_TEMPO_PEN=0.4 and LUNGE_TEMPO_PEN=0.6 (config.py:14) are dead config values. TA-03 is REAL.

THREE CORRECTIONS that change scope and severity:

(1) The dead-data set is LARGER than TA-03 claims. The identical root cause (grip never written) also kills LEGIB_LUNGE=0.25 (config.py:16), consumed only at systems.py:191 `if getattr(aggressor,'grip','normal')=='lunge': legib += cfg['LEGIB_LUNGE']` — a THIRD dead config value, in a DIFFERENT module (legibility, not weapon_tempo). It additionally kills a dead BRANCH in close_tempo (systems.py:40): `if w.get('closes_poorly',False) and grip!='choke'` — `grip!='choke'` is permanently True, so the documented 'chokes up to offset the pole-close penalty' path is unreachable (POLE_CLOSE_PENALTY itself is NOT dead — it always fires for closes_poorly weapons; only the choke-cancel of it is dead). TA-03 under-scoped.

(2) The doc-drift sub-claim is HALF WRONG. TA-03 says grip is documented in ability_armature.md §2b as a live mechanic. FALSE — §2b (lines 33-44) is the competence/channel table and never mentions grip, choke, or lunge; its only 'stance' (line 37) is the unrelated balance/footwork competence, which is independently LIVE via stance_stability/balance_eff (systems.py:71, 65). The real drift is in CODE docstrings: weapon_tempo (systems.py:18-21,26-27) and close_tempo (systems.py:33-40) describe the grip/choke/lunge toggle as an operating mechanic 'handled at the call site' — but no call site sets it.

(3) SEVERITY is over-rated. P1 = breaks play/correctness. This breaks nothing: outputs are byte-identical to a world without the branches, the mirror stays fair, no invariant moves. All reads use a safe getattr default and combatant.py:66 always seeds 'normal', so the branches cannot KeyError, fire spuriously, or create a cliff (NERS-R holds). This is inert scaffolding + doc drift = P2, not P1.

WHAT SURVIVED THE ATTACK: I tried to read it as a safeguarded intent-gate (forward scaffolding like the equipped/ability no-op default and the eff_cw neutral=1.0). It IS that pattern — pre-wired consumers awaiting a writer — which is exactly why it's harmless (defuses P1) but does NOT defuse P2: the config values and branches are inert today and the docstrings overstate them as live.

NERS-NECESSARY (should grip be MADE live?) — REJECT as a lever-pile. The churn grip would seed is already delivered: 'lunge = reach/commit trade' duplicates the live commit system (commit 2-5, COMMIT_SIGMA, overcommit_exposure, DISP_COMMIT skew, legibility commit term); 'choke = close control trade' duplicates the live halfsword_target auto-switch (the actual 'choke up to fight close' mechanic) plus close_tempo compression and leverage. Wiring a grip writer would add a near-duplicate lever (E/N fail), not consolidate. Not gated on any sim — this is a static-reachability fact.
- **Revised:** Accept TA-03 as a P2 dead-data + doc-drift finding (downgrade from P1; it alters no output and is safe-defaulted scaffolding, not a correctness break). Correct and widen its scope before filing: the dead-config set is {CHOKE_TEMPO_PEN (config.py:14), LUNGE_TEMPO_PEN (config.py:14), LEGIB_LUNGE (config.py:16)} plus the dead choke-offset branch at systems.py:40 — all rooted in the single never-written `c.grip` (only ever 'normal' at combatant.py:66). Drop/repair the inaccurate sub-claim that ability_armature.md §2b documents grip as live (it does not; the real drift is the weapon_tempo/close_tempo docstrings at systems.py:18-21,26-27,33-40). RECOMMENDED FIX = consolidate, do NOT wire a writer (wiring grip live is a lever-pile vs the live commit + halfsword_target systems): delete the three dead config keys, the grip branches at systems.py:26-27 and the grip!='choke' clause at systems.py:40, the grip read at systems.py:191, and self.grip at combatant.py:66; then strip the grip/choke/lunge language from the weapon_tempo and close_tempo docstrings. If a grip ability is genuinely roadmapped, instead keep the scaffold but mark it explicitly inert-pending (as the armature already does for 'pending'/'not yet built' levers) and fix the docstrings to say so — never leave it described as live.

### 12. RR-09

- **Target:** reopen_recovery
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** keep P1 (with scope correction: the "breaks CI" clause is wrong and should be split out into a separate P2)
- **Reasoning:** VERIFIED TRUE at HEAD, verbatim. core.py:3 `sys.path.insert(0,'/home/claude'); sys.path.insert(0,'/home/claude/v32')` then core.py:5 `import r1_sigma_resolution as r1, r8_parity_harness as r8, m1_dice_sigma_core as m1`; wrapper.py:4 and systems.py:4 each `sys.path.insert(0,'/home/claude/combat_engine')`. The cited substrate (m1_dice_sigma_core.py, r1_sigma_resolution.py, r8_parity_harness.py) physically exists ONLY at tests/sim/v32-combat-balance/ (confirmed via Glob) — that dir is on NONE of the inserted paths. NO RESCUE EXISTS: Glob found zero conftest.py anywhere in the repo (so no pytest path-bootstrap), and the substrate is NOT duplicated inside the engine dir. Therefore any real `import core` raises ModuleNotFoundError at module-load. P1 is correct: the whole engine is non-runnable on a real checkout. REOPEN-MODULE COUPLING IN CLAIM IS ACCURATE: reopen_prob (systems.py:217) and its sole resolver dependency balance_eff (systems.py:65) are pure and import nothing; reopen_prob is reachable only from wrapper.py:60, and wrapper.py:6 imports core, whose line-5 import dies — so the reopen resolver is transitively dead-on-arrival despite being internally clean. EXTENSION (finding's evidence is INCOMPLETE): combatant.py:4 carries the SAME defect — `sys.path.insert(0,'/home/claude'); sys.path.insert(0,'/home/claude/v32')` feeding an m1/r1 import — and the evidence list omits it; the fix must cover combatant.py too or the engine still won't import. SCOPE CORRECTION (claim over-reaches): the "breaks CI" clause is wrong. Grep of .github/workflows/valoria-ci.yml shows ZERO reference to combat_engine_v1 or these paths — CI never imports this engine (it lives under designs/, not tests/valoria/). CI is green not because the imports work but because the code is never exercised. So "non-runnable on CI" mis-frames it; the real CI angle is the opposite and is a DISTINCT finding: a CANONICAL engine (README:14 "CANONICAL — ratified by ED-900/904") has NO CI import-smoke gate, so this P1 breakage is completely invisible to the unbypassable boundary. That deserves its own P2, not folding into RR-09. NOT AN INTENT GATE: no try/except fallback, no env sniff, no documented sandbox guard — it is an un-gated hard dependency on a vanished sandbox layout, the antithesis of a safeguarded choice. GODOT IMPACT CORROBORATED: README:15 declares a live Godot port at designs/godot/skeleton/engines/combat/ that treats this resolution as canonical — so the dead paths are load-bearing for a real deployment target, not sandbox scratch. SURVIVES THE ATTACK: the mechanical core of the claim holds completely; only the CI sub-clause is mis-scoped.
- **Revised:** ACCEPT RR-09 as P1 with two amendments. (1) Severity stays P1: import-time ModuleNotFoundError makes the whole engine + its canonical Godot port non-runnable; this is the right priority. (2) Amend evidence to include combatant.py:4 (same /home/claude insert feeding an m1/r1 import) — it was missed and any fix that skips it leaves the engine still non-importable. (3) Split the "CI" clause OUT of RR-09 into a separate P2: valoria-ci.yml never references combat_engine_v1, so CI does not "break" — it simply never exercises the engine, which means a CANONICAL artifact has no import-smoke gate on the unbypassable boundary. RECOMMENDED FIX is pure CONSOLIDATION, not a new lever (passes NERS-Necessary cleanly — adds zero mechanics, zero churn surface, touches no resolution math, preserves mirror-fairness and all invariants): make the existing imports resolve by either (a) vendoring/relocating m1_dice_sigma_core.py, r1_sigma_resolution.py, r8_parity_harness.py (+ damage_model.py) into combat_engine_v1/ as the engine's owned substrate, or (b) replacing the four hard-coded `/home/claude*` inserts with a single relative-path resolver (e.g. compute the v32-combat-balance dir from __file__) shared by core/systems/wrapper/combatant. Prefer (a) for the Godot port (a self-contained engine dir is portable; a sibling-relative path is not). Then add the spun-out P2's gate: a one-line `import core` smoke test under tests/valoria/ so this breakage class can never reach main silently again. Do NOT attempt to RUN the engine to validate before the fix lands (consistent with the static-only mandate) — verification is "does `import core` succeed from a clean checkout," which the fix makes deterministically true.

### 13. CD-1

- **Target:** commit_disposition
- **Real:** False
- **NERS compliant:** True
- **Severity adjust:** P1 -> P3 (downgrade). The P1 "hard binary cliff / not graded recoverable" framing does not survive; only a bounded micro-discontinuity + a benign one-beat feint-accounting lag remain.
- **Reasoning:** Read wrapper.py, systems.py, config.py against HEAD. The central P1 claim — "no degradation between low-positive and negative stamina; a fighter at 0.001 applies zero OOB penalty" — is FALSE because it audits only the OOB term and ignores the continuous fatigue term that rides the SAME sigma channel.

(1) GRADED DEGRADATION EXISTS, ON THE SAME CHANNEL. wrapper.py:108 computes fat_a = max(0.0, 1 - stamina/max(1,stamina_max)). This is continuous and monotonic in stamina, floored only at 0 and UNCLAMPED above 1.0 (verified weapon_tempo systems.py:29 multiplies by (1 - TEMPO_FATIGUE_K*fatigue) with no upper clamp, floored at TEMPO_FLOOR=0.7). fat_a feeds handling_penalty (systems.py:59-61 = HANDLE_K*deficit + FATIGUE_HANDLE_K*fat_a, FATIGUE_HANDLE_K=0.20) which is SUBTRACTED from atk_sig at wrapper.py:140 — the exact same sigma term OOB feeds. So a fighter at stamina=0.001 is NOT penalty-free: fat_a ~= 1.0, paying ~0.20 sigma handling penalty + a heavily slowed cadence (lines 39/50-51). As stamina crosses into the negative the handling penalty and tempo loss keep climbing continuously (at stamina=-3, stamina_max~18, fat_a ~= 1.167). The claim's premise that the only stamina-dependent penalty is the binary OOB is the error.

(2) THE OOB STEP ITSELF IS REAL BUT BOUNDED, NOT A P-iii CLIFF. wrapper.py:107 latches oob = OOB(=2, config.py:34) if stamina<=0 else 0, applied as -oob*0.5 = -1.0 sigma at line 140. There IS a genuine -1.0 sigma discrete step at the stamina<=0 boundary. But it is a MU-SHIFT into core.resolve (soft_cap = 1.5*tanh(x/1.5), M_MAX=1.5 bounds total net contribution), NOT an Ob-shift — it does not touch the Ob>=1 floor and cannot produce an unbounded effect. A bounded mu-shift on a continuous-distribution roll is a probability tilt, not a "cannot act" wall, and it sits on TOP of an already-steep fatigue curve, so the realized output stays graded. It is recoverable (RECOVERY_FRAC between bouts, wrapper.py:291). The loop is bounded: hard separation at stamina<=-4 (lines 84, 270), so the "proceed several exchanges at stamina=-3" window is real but narrow (0 to -4) with monotonically worsening fatigue throughout. This is smoothness polish (P3), not a play/correctness break (P1).

(3) THE FEINT-ORDERING SUB-CLAIM IS TRUE and the only genuinely correct finding, but minor. oob is read at line 107 BEFORE the feint stamina deduct at line 123 (FEINT_STAMINA=1.0). fat_a is computed at line 108, also BEFORE the feint deduct. So a feint's stamina cost escapes BOTH the OOB check and that beat's fatigue computation. However: oob is NOT sticky — line 107 re-evaluates every beat with no carried state (confirmed: zero oob/exhaust references in systems.py), so the resulting negative stamina IS caught the very next beat. The lag is exactly one beat, magnitude <=1.0 stamina, symmetric for both fighters (mirror-fair, no invariant broken), and self-correcting. That is a P2/P3 accounting nit, not P1.

NERS / ADDITION verdict: the finding implicitly wants OOB replaced/supplemented by a stamina-graded penalty. REJECT as a lever-pile. The engine already delivers graded exhaustion through fat_a -> handling_penalty + tempo on the same sigma channel; a second stamina-graded penalty would be two levers doing one job (fails N-Necessary and E-Elegant). No new churn is generated — exhaustion already branches the path via worsening odds, slower cadence, and the -4 collapse exit. The only NERS-positive micro-improvement is a CONSOLIDATION/correctness fix to the ordering, not a new mechanic.
- **Revised:** REJECT the P1 as stated; re-file as a single P3 polish item. Recommended action, in priority order: (a) Fix the one-beat ordering lag — move the OOB latch (wrapper.py:107) and the fat_a computation (line 108) to AFTER the feint stamina deduction (line 123), OR move the feint deduction above line 107, so a beat's full stamina spend is reflected in both the OOB gate and the fatigue term that same beat. This is a pure correctness tidy, symmetric, no balance change of note. (b) OPTIONAL smoothness: if the -1.0 sigma step at the stamina<=0 seam ever shows as a visible kink in sim sweeps, ramp OOB over a narrow band (e.g. scale by min(1, -stamina/W) just below zero) so the discrete step blends into the existing fatigue curve — but ONLY if a sim demonstrates the kink matters; do NOT add it speculatively (it would otherwise be redundant with fat_a). (c) Do NOT add any new stamina-graded penalty mechanic — that is a lever-pile over the existing fat_a curve and fails NERS-N/E. Keep OOB as the small discrete 'running on empty' kicker it already is. Net: no P1 exists; the cliff/recoverability concern is answered by the fat_a continuous degradation on the same channel + bounded mu-shift + RECOVERY_FRAC + the -4 exit.

### 14. RF-01

- **Target:** read_feint (systems.py:reading/legibility/feint_eval + tradition.py:familiarity + wrapper.py:read_wi...
- **Real:** False
- **NERS compliant:** True
- **Severity adjust:** P1 -> P3 (demote and reframe; the filed P1 loop-break is not real)
- **Reasoning:** Both factual sub-assertions verify against HEAD: systems.py:201 returns new_streak=0 in the no-fire branch, and wrapper.py:121 assigns feint_streak=fv['new_streak'] with no guard on fv['do']. The grep confirms feint_streak's ONLY consumer is the cap gate at systems.py:198 (feint_streak < FEINT_MAX_STREAK) — nothing else reads it.

But the design-consequence claim — "FEINT_MAX_STREAK=3 is a dead cap" and "the designed bluff-exhaustion damper does not exist" — does NOT survive the attack. Three things break it:

1. INTENT MISMATCH. config.py:21-22 specifies the cap as "capped at 3 IN A ROW." "In a row" is decisive: a consecutive-feint counter MUST reset to 0 on any beat the feint does not fire, or "in a row" would degenerate to "3 ever." The no-fire branch returning new_streak=0 (systems.py:201) is therefore the CORRECT, intended semantics, not a bug. The claim reinterprets a "consecutive" counter as if it were meant to be an accumulator and then faults the code for not accumulating.

2. THE CAP IS NOT DEAD. To reach streak=3 the same aggressor must roll do=True on three consecutive acted beats: P = FEINT_P^3 = 0.30^3 ~= 2.7%. On that path the cap DOES fire — the next beat evaluates 3 < 3 = False and blocks the 4th feint (then resets). So the cap is reachable and load-bearing on ~2.7% of feint-opening sequences, not 0%. "Dead cap" is factually overstated.

3. THE IMPUTED DAMPER IS A STRAWMAN. The spec never promises an escalating tell or a streak-scaled exhaustion damper. The debuff is FLAT (FEINT_DEBUFF=0.30 fooled / FEINT_PUNISH=0.12 read), not streak-scaled. The bluff-exhaustion damper the design actually specifies exists and is LIVE: the per-feint stamina cost FEINT_STAMINA=1.0, gated at systems.py:199 (aggressor.stamina>0) and deducted at wrapper.py:123, plus the read-punish counter-edge. The claim faults the absence of a mechanic the design never specified while ignoring the two dampers it did.

WHAT SURVIVED THE ATTACK (the real, demoted residue): feint_streak is a near-inert lever. It threads engagement-scoped state (wrapper.py:34/121) and a config knob (FEINT_MAX_STREAK) to suppress a 4th consecutive feint that occurs ~2.7% of the time, scaling/branching nothing else — a churn/NERS-N concern (a lever that only rarely gates, never branches the path meaningfully). Separately, feint_streak is a SINGLE engagement scalar, not per-fighter, so when the aggressor changes via the tempo gate (wrapper.py:87-93) or a riposte role-flip (wrapper.py:261), one fighter's feint can gate the other's — a minor role-conflation. Both are P3 polish/legibility notes, not the P1 loop break filed. No undamped/unbounded loop exists here: FEINT_P is flat, the cap bounds consecutive fires, and stamina is the monotone drain — damper present, bound present.
- **Revised:** REJECT as filed (severity and kind both wrong: not a P1 loop-break; the cited reset is correct "in a row" semantics, the cap is reachable at ~2.7% and fires there, and the "missing exhaustion damper" is supplied by FEINT_STAMINA + FEINT_PUNISH). Do NOT add an accumulating-streak or escalating-tell mechanic — that would be a lever-pile (NERS-E/N fail) on top of an already-working flat-debuff + stamina-cost design, and it is gated on an unrun sim to justify any new churn.

If anything is logged, file a single demoted P3 consolidation note: feint_streak is a near-inert, engagement-scoped (not per-fighter) lever whose sole job is to gate a 4th consecutive feint that occurs ~2.7% of the time. EITHER (a) accept it as a cheap, bounded anti-machine-gun guard and annotate the docstring that the streak is intentionally "consecutive, resets on no-fire" so future auditors don't re-file this; OR (b) if the engine has no need to ever block consecutive feints (sim shows triple-feints are already vanishingly rare and harmless), delete feint_streak / FEINT_MAX_STREAK entirely and let FEINT_STAMINA be the sole feint damper — a genuine consolidation that removes dead-ish state rather than adding more. Prefer (a) unless an ablation confirms (b) changes no outcome. Also worth a one-line note: make the counter per-fighter if it is ever made load-bearing, since today it conflates both aggressors' bluff-state.

### 15. RF-02

- **Target:** read_feint (systems.py:reading/legibility/feint_eval + tradition.py:familiarity + wrapper.py:read_wi...
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** P1 -> P2
- **Reasoning:** VERIFIED TRUE against HEAD, and the dead path is even more complete than the finding states. Both abilities carry lever='seize': vorschlag (tradition.py:96, value=4.0) and sen_no_sen (tradition.py:106, value=4.0). The ability system has exactly four consumers, all enumerated by grep: ability_factor(defender,'counter_select') wrapper.py:158, ability_bonus(aggressor,'anti_overcommit') wrapper.py:163, ability_bonus(defender,'counter_success') wrapper.py:189, and the eff_cw() channel sites. eff_cw (tradition.py:132-136) calls ability_factor(c, channel) ONLY for the seven channel names (visual/tactile/precommit/leverage/tempo/measure/balance); 'seize' is not a channel, so eff_cw can never route it either. grep 'seize' over core.py = zero matches; over substrate (tests/sim/v32-combat-balance) = zero. So NO call with lever='seize' exists anywhere in the engine or substrate. An equipped vorschlag/sen_no_sen is a silent no-op.

ROOT CAUSE confirmed deliberate-but-orphaning: systems.py:243-246 records the "Pre-contact seizure CUT 2026-06-05 (Jordan; verified inert; ablation ~0; INIT_SEIZE_K washed out by per-beat dynamics)" — the consumer the 'seize' lever targeted (seizure_score + initiative_seize) was intentionally removed, leaving the two abilities pointing at a deleted site. This is dead-data via stale orphan, not a parked intent-gate.

ADVERSARIAL PROBES THAT FAILED TO BREAK THE FINDING: (a) intent-gate? No — a deliberately parked lever would not be advertised as "live"; instead it is claimed live in FIVE doc spots (ability_armature.md:51 lever-map "**live**", :88 template, :106 "✅ vorschlag", :131 "✅ sen_no_sen (seize)", :167 "Live levers: seize (Vorschlag, Sen-no-sen)") AND in an in-CODE comment (tradition.py:87 lists 'seize' among "LIVE levers... wired at their engine sites... + seizure score"). That tradition.py:87 self-contradiction (it disagrees with the cut recorded 150 lines later in systems.py) is an ADDITIONAL, tighter evidence pointer the finding did not cite. (b) stale/already-fixed? No — the cut (2026-06-05) is the freshest signal and HEAD still has zero consumers. (c) mirror-fairness risk? None — dead data cannot bias outcomes.

SEVERITY: This is dead-data + doc-drift. It does not corrupt a resolved outcome, break an invariant, or affect a no-ability fighter (equipped=[] default, combatant.py:68). It is a CHURN failure (the Japanese flagship Sen-no-sen is wholly inert -> a tradition-differentiation lever that branches nothing) and a LEGIBILITY/trust failure (docs+code comment say "live"), but not play-breaking. P1 overstates it; correct tier is P2. NERS: the finding correctly identifies a Necessary-failure (a registered mechanic generating zero churn = lever-pile / dead weight), so flagging it is NERS-compliant.
- **Revised:** CONFIRM the finding, downgrade P1 -> P2 (dead-data + doc-drift; inert, not correctness-breaking). Two cleanup paths, pick one — do NOT add a new 'seize' subsystem (that would re-introduce the lever ablated as ~0-impact on 2026-06-05; a lever-pile, E/N fail):

PATH A (recommended — honest deprecation): Remove vorschlag and sen_no_sen from ABILITIES (tradition.py:96-97, 106-107), since their target site no longer exists. Then re-home the two techniques onto LIVE levers that already deliver their churn: Vorschlag (seizing the Vor as a first-strike) maps cleanly to the existing initiative_sigma / hit-gain Vor system or to counter_success/anti_overcommit; Sen-no-sen (simultaneous seize as the opponent commits) is mechanically the Indes steal already in wrapper.py:147-153 gated on read_win & commit>=4 — so sen_no_sen should target counter_success or counter_select (its real engine surface), not a dead 'seize'. This CONSOLIDATES onto existing churn rather than adding physics.

PATH B (minimal): If the abilities are kept as placeholders, delete the 'seize' rows and stop claiming "live." Either way, REPAIR THE DRIFT in all six spots: ability_armature.md lines 51/88/106/131/167 and the in-code comment tradition.py:87 must stop listing 'seize' as a wired/live lever (it is not — the consumer was cut 2026-06-05 per systems.py:243-246). The tradition.py:87 comment contradicting systems.py:243 is the highest-priority fix since it misleads inside the code itself.

Tie this RF-02 to the geometry dead-baked-data and 'clinch'/'seize' family as one "orphaned-lever sweep" rather than separate patches.

### 16. RF-03

- **Target:** read_feint (systems.py:reading/legibility/feint_eval + tradition.py:familiarity + wrapper.py:read_wi...
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** keep P1 (doc-drift + dead-mechanism; runtime-safe but decision-corrupting for two flagship traditions)
- **Reasoning:** SURVIVED THE ATTACK — the claim is true against HEAD and I found an ADDITIONAL drift site the original evidence missed.

DEAD LEVER, CONFIRMED BY EXHAUSTIVE CONSUMER ENUMERATION. tradition.py:96 (vorschlag) and tradition.py:106 (sen_no_sen) both declare lever='seize', op='+', value=4.0. The ONLY consumers of ability_bonus()/ability_factor() in the entire engine are: counter_select at wrapper.py:158, anti_overcommit at wrapper.py:163, counter_success at wrapper.py:189 (plus channel levers via eff_cw at the reach/bind/feint/init sites). A repo-wide grep for ability_bonus(...,'seize') / lever=='seize' returns NO matches; core.py contains ZERO ability references. Therefore the 'seize' lever has no consumer — both vorschlag and sen_no_sen are inert no-ops (equipping them changes nothing). This matches ability_bonus()'s structure (tradition.py:116-122): it only sums values whose lever matches a string the engine actually queries, and nothing queries 'seize'.

THE CUT IS REAL AND COMPLETE (not a half-removal that left a latent consumer). systems.py:243-246 documents 'Pre-contact seizure CUT 2026-06-05 ... seizure_score + initiative_seize removed'; wrapper.py:27-29 confirms 'initiatives start even (0.0 ...)' and wrapper.py:29 sets A.initiative,B.initiative=0.0,0.0. The pre-contact seizure score that a 'seize' bonus would have fed is gone with no replacement; the retained ongoing initiative system (initiative_sigma, hit-gains/steals/decay) reads c.initiative, never an ability seize bonus. So there is no surviving path by which value=4.0 could re-enter resolution. The lever is dead by design, not by accident — but the ability declarations and the doc were never updated to follow the cut.

DOC-DRIFT, CONFIRMED AT ALL CITED SITES PLUS ONE MORE. ability_armature.md:51 lists 'seize' as **live**; :106 gives vorschlag a checkmark; :131 gives sen_no_sen a checkmark; and :167 ('Current state / live') re-asserts 'Live levers: seize (Vorschlag, Sen-no-sen) ... Each shows a positive, bounded edge' — a fourth false-live assertion in the same doc that the evidence did not cite. ADDITIONALLY the code-side comment tradition.py:87-88 ('LIVE levers this version (wired at their engine sites): \'seize\' (+ seizure score) ...') is ALSO drifted: it names a 'seizure score' engine site that systems.py:243 explicitly removed. So the drift is not confined to the markdown — the substrate module's own header lies about the same lever.

COULD THIS BE A SAFEGUARDED/INTENT-GATED CHOICE? No. There is no gate, flag, or 'registered-but-inert' label on vorschlag/sen_no_sen — unlike staerke_schwaeche/misura/atajo, which carry explicit '(channel; pending)' tags in both their desc strings and the doc's §7 'Registered but inert' bucket. seize is the OPPOSITE: actively asserted live in four places. The channel-pending levers are honestly labelled; seize is dishonestly labelled. That asymmetry is itself the tell that this is unmanaged drift, not a deliberate staged rollout.

IS IT ALREADY FIXED (stale)? No — verified against the working tree at HEAD; every cited line is present as quoted.

NERS / CHURN FAILURE. N: a lever with no consumer is the canonical dead-mechanic defect; two flagship-tradition abilities (German Vorschlag, Japanese Sen-no-sen) are choices that branch nothing. E: the doc tells the player/designer these deliver a bounded ~+8pp seize edge, so the legible-odds promise is violated — the intuited outcome (equip Vorschlag, gain the Vor) does not occur. Churn: equipping either ability produces zero churn and zero path-branch; worse, it is a phantom dominant answer ('take the free +4 seize') that does nothing, corrupting calibration against the invariant suite (a designer tuning value=4.0 sees no movement and may over-crank it). The damage is not a miscomputation (the engine is invariant-safe: unequipped == equipped here) but a trust/decision failure across the source-of-truth doc AND the code comment.

SEVERITY. I keep P1, with the scope tightened to 'doc-drift + dead-mechanism.' A purist could argue P2 because nothing miscomputes at runtime (no wrong numbers, invariants hold). I hold P1 because the misrepresentation is load-bearing: it silently tells designers that the two flagship traditions' signature seizing abilities work when they are pure no-ops, which is exactly the class of error that propagates into mis-calibration and player-facing surprise. The defect spans 5 sites (4 doc + 1 code comment), not the 2 the evidence enumerated for the live-claim, so the blast radius is larger than stated.
- **Revised:** SUSTAIN RF-03 as a TRUE P1 doc-drift / dead-mechanism finding; broaden it. Remediation is a CONSOLIDATION (fix a dead/broken path), not a new lever, so it passes the NERS-Necessary bar for an edit. Two coherent options:

(A) HONEST-DEMOTE (lowest-risk, matches the existing convention). Relabel seize everywhere it is falsely called live, exactly as staerke_schwaeche/misura/atajo are already handled: ability_armature.md:51 status -> 'cut (no consumer since 2026-06-05)'; remove or asterisk the vorschlag checkmark (:106) and sen_no_sen checkmark (:131) to 'declared; lever cut'; fix §7 (:167) to drop seize from 'Live levers'; and fix the code comment tradition.py:87-88 to stop claiming a wired 'seizure score' site. Add a '(lever cut; inert)' tag to the vorschlag/sen_no_sen desc strings (tradition.py:97,107) so the dishonesty is visible at the declaration, mirroring the '(channel; pending)' tags. This makes the doc and code match HEAD with zero runtime change and zero invariant risk.

(B) RE-WIRE onto a live consumer (only if Jordan wants these flagship abilities to actually do something). The natural surviving home is the ongoing initiative system: route a seize bonus into c.initiative at fight-start or into init_steal_factor/initiative_sigma (systems.py:248-279), bounded by the existing INIT_CAP (clamp_initiative) + tanh damper so it stays the documented '~+8pp by tanh cap.' This is NOT a lever-pile (it reuses the existing initiative substrate and its cap/damper, no new subsystem) and is invariant-safe by construction (neutral/unequipped = 0.0). BUT it is gated on an UNRUN balance check: the 2026-06-05 ablation found the pre-contact seize 'washed out by per-beat dynamics (~0 outcome impact)' — re-introducing the same edge risks re-introducing the same nothing, or, if cranked to be felt, a new attacker-favouring bias. Default to skepticism: do NOT re-wire without a sim that shows the bonus survives the per-beat decay AND preserves mirror-fairness. Until that sim runs, prefer (A).

Recommendation: apply (A) now (pure drift repair, ship immediately); file (B) as a deferred, sim-gated design question, not an edit. Either way, also patch the tradition.py:87-88 comment — it is a second source-of-truth that currently lies about the same lever.

### 17. INI-01

- **Target:** initiative_vor
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** VERIFIED TRUE against HEAD, and the attack made it BROADER, not weaker. wrapper.py:158 is `counter_attempt = rng.random() < cfg['COUNTER_SELECT_BASE']*TR.eff_cw(defender,'tempo')*max(0.0,1-cfg['DISP_COUNTER_K']*S.disp_lean(defender))*TR.ability_factor(defender,'counter_select')` with NO min()/clamp. Arithmetic confirmed exactly: COUNTER_SELECT_BASE=0.45 (config.py:79); eff_cw(italian,'tempo')=channel_weight 1.30 (tradition.py:28) x ability_factor(...,'tempo')=1.0 (no Italian *tempo ability) =1.30; disp factor at lean=-1 (disp=1) = max(0.0,1-0.5*(-1))=1.5 (DISP_COUNTER_K=0.5, config.py:91; disp_lean=(disp-4)/3, systems.py:62-64); ability_factor(...,'counter_select')=1.40 (mezzo_tempo op='*', tradition.py:101). Product = 0.45*1.30*1.5*1.40 = 1.2285 > 1.0, so `rng.random() < 1.2285` is deterministically True on every qualifying read_win+commit>=4 exchange (wrapper.py:147). The probabilistic SELECTION gate silently collapses to a deterministic branch — exactly the claim's headline.

WHAT THE ATTACK ADDED (claim UNDERSTATES breadth): (a) the floor is not disp=1 — disp=2 already gives p=1.092 (SATURATED); the gradient between disp=2 and disp=1 (and any tempo-channel ability stacking on eff_cw) is silently flattened. (b) Not Italian-only: ability_factor (tradition.py:124-130) iterates c.equipped with NO tradition guard, so a GERMAN fighter (tempo=1.10) equipping mezzo_tempo also saturates: 0.45*1.10*1.5*1.40=1.0395>1. The mezzo_tempo equip IS real/reachable — the comprehensive audit measured a nonzero +0.1pp delta for it (comprehensive_analysis_personal_combat.md:117), so this is not a dead path.

WHAT SURVIVED MY ATTACK ON THE CLAIM (where it OVERSTATES): the claim's framing 'counter_attempt deterministically True' is correct, but a reader could infer the counter auto-LANDS — it does not. The SUCCESS gate (wrapper.py:190) is independently clamped AND probabilistic: `rng.random() < max(0.05, min(0.92, succ))`, and a missed counter is punished (eats the attack undefended, cedes the seized Vor — wrapper.py:192-198). So only SELECTION saturates, not outcome. That is why this is P2, not P1: no broken/auto-win path, no correctness crash, no mirror-fairness/invariant violation (disposition and equip are per-fighter and symmetric in form; UPSET_FLOOR still caps any matchup). It is a NERS-S/E defect (a lever that should branch the path becomes a dominant/inert constant at the extreme) plus a contract violation of the code's own stated design ('SELECTION is tempo-driven (how often you reach for it)', wrapper.py:155-157) — the tempo-driven dial dies past the saturation point.

NOT a safeguarded intent gate: the sibling probability gates in the SAME function ARE clamped — success max(0.05,min(0.92,succ)) (190); riposte min(0.95,...) (174,181); INDES steal is tanh-bounded via indes_scale CEIL=2.0 (149) and initiative_sigma (systems.py:248-253). Line 158 is the lone unclamped probability among its peers — a consistency miss, not a deliberate choice. NOT stale: present at HEAD, post-dates the 2026-06-09 audit which already flagged the related steal/select machinery.
- **Revised:** ACCEPT as a real defect; downgrade P1 -> P2 (inert/contract-drift at an extreme, not a play-breaking correctness fault — the counter's SUCCESS roll stays clamped+probabilistic, so no auto-win and no invariant/mirror-fairness break). FIX is a one-line clamp that CONSOLIDATES with the function's existing pattern, not a new mechanic (so it passes NERS-N — no lever added): wrap line 158's RHS in min(1.0, ...) (or a calibrated COUNTER_SELECT_CEIL< 1.0 if a hard ceiling below certainty is wanted, mirroring the 0.92 success ceiling on line 190). This restores the tempo-driven selection gradient across disp 2->1 and across stacked tempo abilities, and makes line 158 consistent with its clamped siblings (190, 174, 181). While there, note the SECOND-order finding the attack surfaced: ability_factor/ability_bonus (tradition.py:116-130) apply ANY equipped ability regardless of c.tradition — mezzo_tempo works on a German fighter. If abilities are meant to be tradition-locked (ability_armature.md §1 'Modulate, never unlock' implies tradition-grant), add a tradition check in equip/validation; if cross-equip is intended, the clamp alone suffices. Do NOT add a new disposition or counter sub-lever — the existing levers are sufficient; the only need is to bound the product. This fix is gated on no unrun sim (pure clamp, monotonic, no recalibration of the in-band region since saturation only began past p=1.0).

### 18. OUT-01

- **Target:** outcome-map / overcommit / bind-kuzushi / riposte / displace (post-strike cluster)
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 (confirmed; if anything the drift is broader than the finding states — see reasoning)
- **Reasoning:** CONFIRMED against HEAD; survived every attack. (1) Both abilities register lever='seize': tradition.py:96 (vorschlag, op='+', value=4.0) and tradition.py:106 (sen_no_sen, op='+', value=4.0). (2) No consumer exists. Grep for 'seize' across all combat_engine_v1/*.py returns ONLY: the two ABILITIES defs, the seizure-CUT comments, and incidental prose ('the seized Vor', 'reads to seize'). There is NO TR.ability_bonus(c,'seize') or TR.ability_factor(c,'seize') anywhere. The actual ability_bonus/ability_factor consumers in wrapper.py are counter_select (158), anti_overcommit (163), counter_success (189) — none is 'seize'. tradition.py:116-130 are the only definitions; they are called for other levers only. (3) Not a safeguarded intent-gate: there is no gate guarding a future seize path — the lever is simply never read; ability_bonus would happily sum it if any site called it, but none does. (4) Not stale/already-fixed: HEAD still carries both abilities pointed at the dead lever. (5) The cut is documented as DELIBERATE and verified-inert (wrapper.py:27, systems.py:243-246: 'Pre-contact seizure CUT 2026-06-05 (Jordan; verified inert - ablation ~0); seizure_score + initiative_seize removed') — so the seize CONSUMER was removed on purpose, orphaning these two ability levers as collateral. The abilities remain genuinely equippable (combatant.py:68 self.equipped), so a player CAN equip the German/Japanese flagships and get exactly zero mechanical effect — a hard churn failure (a choice with a forced no-op = no path branch). NERS verdict: fails N (two registered levers with no resolver = the pure lever-pile/dead-lever defect) and fails the module-contract (IN consumed -> resolver -> OUT: nothing consumes 'seize'). The ONE place the finding UNDER-states the problem: it cites only ability_armature.md:167, but the 'seize live' lie is in FOUR doc sites (md:51 lever-map '**live**', :106 ✅ vorschlag, :131 ✅ sen_no_sen, :167 §7) AND — worse — in the engine source itself at tradition.py:87 ('LIVE levers this version... seize (+ seizure score)'). So the drift contaminates the code's own self-description, not just the companion doc. Severity P1 is correct: these are the flagship German + Japanese tradition abilities, the marquee identity picks for two of the core traditions, silently inert. P1 stands.
- **Revised:** ACCEPT the finding (real, P1). Severity unchanged; broaden the evidence to ALL drift sites. Resolution is a deletion-or-rewire decision (consolidation, not an addition — adding a new seize resolver would re-introduce the exact mechanic Jordan ablated to ~0 and cut on 2026-06-05, so re-wiring is rejected unless a fresh sim shows non-trivial impact). RECOMMENDED FIX, in priority order: (A) Make the two abilities honest by RE-LEVERING them onto a live resolver that matches their documented technique rather than the cut pre-contact seize: Vorschlag (first-strike that takes the Vor) and Sen-no-sen (seizing as the opponent commits) both map naturally onto the SURVIVING in-fight initiative system (initiative_sigma + hit-gains/steals/decay, systems.py:248+) — e.g. an additive initiative-gain or a counter_success/anti_overcommit-adjacent lever that IS read each beat. This restores churn (the flagship picks branch the path again) without resurrecting the ablated pre-contact contest. (B) If re-levering is deferred, at MINIMUM repair the four doc lies (ability_armature.md:51,106,131,167) and the engine-source comment (tradition.py:87) to mark 'seize' as DEAD/CUT, and flag vorschlag/sen_no_sen as inert-pending-rewire, so the corpus stops asserting a non-existent edge. Do NOT ship (A) as a balance change without the invariant suite + a matchup sim (engine is currently non-runnable on this checkout — the /home/claude import-path break is a separate blocker that gates ANY validation here). Until then this is a documented-but-inert lever; the honesty fix (B) is unblocked and should land immediately; the rewire (A) is gated on a runnable engine + sim.

### 19. INI-02

- **Target:** initiative_vor
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** VERDICT: TRUE against HEAD, with the dead-data mechanism confirmed more completely than the finding states, but the severity is overstated as P1 and the Japanese characterization needs a correction.

WHAT SURVIVED THE ATTACK (the claim is real):
- tradition.py:96-97 `vorschlag` lever='seize' op='+' value=4.0; tradition.py:106-107 `sen_no_sen` lever='seize' op='+' value=4.0. Confirmed verbatim.
- EXHAUSTIVE consumer inventory across the WHOLE engine dir: the only ability-lever consumers are wrapper.py:158 ability_factor(defender,'counter_select'), wrapper.py:163 ability_bonus(aggressor,'anti_overcommit'), wrapper.py:189 ability_bonus(defender,'counter_success'), and tradition.py:136 ability_factor(c,channel) inside eff_cw (channel levers only). core.py has ZERO ability/seize references (grep clean). There is NO ability_bonus(c,'seize') call anywhere. Dead confirmed.
- Seizure cut is real and load-bearing context: systems.py:243-246 and wrapper.py:27-29 (initiatives forced to 0.0; pre-contact contest deleted as ablation ~0, washed out by per-beat dynamics). So the consumer was deliberately removed, leaving these two abilities orphaned.

ATTACK THAT CLOSED A LOOPHOLE (no surviving live path): even if some site called eff_cw(c,'seize'), it routes ability_FACTOR which filters op=='*'; both seize abilities are op=='+', so they are skipped on BOTH the additive path (no caller) and the multiplicative path (filtered out). There is no reachable code path by which value=4.0 affects resolution. Equipping vorschlag or sen_no_sen is a strict no-op.

WHERE THE FINDING UNDERCOUNTS (drift is broader): it cites only tradition.py+systems.py, but `seize` is asserted-LIVE in FIVE places, including the engine source itself: tradition.py:87 header ("LIVE levers ... 'seize' (+ seizure score)"); ability_armature.md:51 (lever table "**live**"), :106 (German Vorschlag "✅ built"), :131 (Japanese Sen-no-sen "✅"), :167 ("Live levers: seize (Vorschlag, Sen-no-sen)"). The doc-drift evidence base is stronger than the grep the finding offered.

WHERE THE FINDING OVERREACHES:
1) Severity. P1 in this rubric = breaks play/correctness (crash, invariant break, mirror-unfairness). This is invariant-SAFE: ability_bonus returns 0.0 for an unconsumed lever, equipped default is empty, the mirror is unaffected, no cliff, nothing crashes. It is inert/drift = the textbook P2 (churn-dead mechanic + doc claiming it live). Downgrade P1 -> P2. It is the most SEVERE P2 (a named, doc-blessed, player-investable technique that does nothing — the churn-killer the prompt warns of), but it does not break play.
2) "Japanese flagship" framing is half-right and should be tightened: sen_no_sen is the ONLY built Japanese ability (no other tradition=='japanese' entry in ABILITIES; japanese channel abilities are pending/inert), so for a Japanese fighter it is the sole equippable AND it is dead -> Japanese tradition currently ships zero working equippable abilities. That strengthens, not weakens, the case. For German, vorschlag is dead but indes (counter_success, wrapper.py:189) still works, so German is not left empty.

NOT A SAFEGUARDED INTENT GATE: there is no guard. The root cause is systemic — nothing asserts that every ABILITIES[*]['lever'] has a live consumer, so a cut consumer silently leaves orphan abilities AND a stale 'live' header in the same file. This is the real defect class (one missing import-time invariant), of which INI-02 is the current instance.

NERS: fails N (a registered lever with no consumer is the inert-mechanic / lever-pile defect) and fails E (player cannot intuit that an equipped, named, doc-'live' technique is a no-op). Churn principle: hard fail — invests choice, branches nothing.

ADDITION TEST (should we BUILD seize?): NO. Re-adding a seize resolver resurrects exactly the pre-contact contest Jordan ablated to ~0 and verified inert (systems.py:244-246) — a churn-negative re-add gated on no sim. Reject the build; the fix is consolidation/deletion, not a new subsystem.
- **Revised:** ACCEPT as a real defect, reclassified P1 -> P2 (inert mechanic + doc drift; invariant-safe, does not break play). It is the highest-priority P2 because it is a named, equippable, doc-'live' technique that is a strict no-op — the canonical churn-killer.

Scope correction for the record: the finding's evidence is sound but undercounts the drift. `seize` is asserted live in FIVE locations, including the engine source: tradition.py:87 (header comment lists 'seize' first under "LIVE levers ... wired at their engine sites") plus ability_armature.md:51,106,131,167. Also tighten the Japanese point: sen_no_sen is the ONLY built Japanese ability, so its death means the Japanese tradition currently ships zero working equippable abilities (vorschlag's death leaves German with indes still live).

Recommended fix (consolidate or delete — do NOT build a new seize resolver; that resurrects the ablated, sim-verified-inert pre-contact contest):
  Option A (repoint, preferred if the techniques should stay equippable): retarget both onto an existing LIVE lever whose substrate matches the technique. Vorschlag = "first strike that takes the Vor" -> the only live initiative-adjacent lever today is counter_success/counter_select/anti_overcommit; the honest match is anti_overcommit or counter_select (it is initiative-seizing, but no live additive initiative lever exists post-cut). Sen-no-sen (pre-emptive seize as opponent commits) maps naturally to anti_overcommit or counter_success. Recalibrate value (4.0 was sigma-points into a deleted tanh seizure cap and is mis-scaled for any current lever) against the invariant suite before merge. Mirror-fairness preserved (default-empty equip).
  Option B (delete): remove both ABILITIES entries and correct all five doc/header sites to drop 'seize' from the live set, listing it as "not yet built (consumer cut 2026-06-05)".
Either way, also correct tradition.py:87 and ability_armature.md:51/106/131/167 so no source claims `seize` is live.

SYSTEMIC FIX (root cause, recommend spinning off): add an import-time invariant in tradition.py asserting that every ABILITIES[*]['lever'] is either in a LIVE_LEVERS set (with a real consumer) or explicitly tagged pending/not-built — the same fail-fast pattern systems.py:86-87 already uses for GATE/GEOMETRY key-sync. That converts every future cut-consumer orphan from silent dead weight into an import error, and would have caught INI-02 the day the seizure was cut.

### 20. OUT-02

- **Target:** outcome-map / overcommit / bind-kuzushi / riposte / displace (post-strike cluster)
- **Real:** True
- **NERS compliant:** True
- **Severity adjust:** P1 -> P3
- **Reasoning:** The MECHANISM is real but the SEVERITY and the consequence claim are wrong. Verified against HEAD:

ARITHMETIC SURVIVES (partly). wrapper.py:32 soft=8; :35 `while beats < soft*3` = ceiling 24; :243-244 `for _ in range(3): beats+=1` has no inner ceiling check. A body entered at the top with beats=23 hits :36 `beats+=1` -> 24, then a bind running all 3 iterations without a break -> beats=27. So `soft*3+3 = 27` is reachable. The claim's headline number is correct, AND it actually understates the bound: feint at wrapper.py:123 `beats += fv['beat_cost']` (0.3) can add fractional overshoot on top, compounding up to FEINT_MAX_STREAK. So the documented "24" is a loose, not exact, upper bound.

WORKED EXAMPLE IS OFF-BY-ONE. The claim says "enters at beats=23 can exit at beats=26." Because :36 increments before the bind block, a body whose `while` check saw beats=23 enters the bind at 24 and exits at 27, not 26. To exit at 26 the `while` must have seen beats=22. Imprecise, non-load-bearing.

THE LOAD-BEARING CLAIM IS FALSE. "The ceiling is not enforced during a bind sub-loop, so a bind ... can exit ... without ever triggering the separation path." Trace what happens after beats overshoots to 27: the bind block either returns on felled (:250) or falls through; control reaches exchanges+=1 (:262) then the burst-exit gates (:270-272). bind=True means the clean-defence exit at :272 correctly does NOT fire (a bind continues the burst BY DESIGN, comments :263-269, state-map §2 G3). Control returns to the top `while beats < 24`; with beats=27 the guard is False, the loop exits, and :273 `return None` fires — which IS a separation terminal (state-map :113 SEP0 / §2 BEAT->SEP0, "return None — separation"). So separation is NOT skipped; it is reached on the very next loop check. The overshoot DELAYS the beats-exhaustion check by at most one exchange — it does not bypass it.

NO CORRECTNESS BREAK (why it is not P1). (a) The bind loop is hard-bounded to 3 iterations with its own felled/riposte breaks — no extra rolls beyond that. (b) The dominant burst terminator is `exchanges >= BURST_MAX` (=4) at :271, not the beats ceiling; beats=24 is a backstop against pathological non-termination, and in normal flow the burst caps at 4 exchanges long before beats nears 24. (c) beats strictly increases (:36,:123,:244) and the outer guard is monotone, so termination is GUARANTEED — the genuine NERS-R concern (unbounded loop) does not exist; the loop is bounded=damper<1 AND cap, just with cap 27 not 24. (d) The overshoot is symmetric in A/B (depends only on `beats`), so mirror-fairness is untouched; no sigma floor/cap/ER-2/Ob>=1 invariant is a loop counter, so none is violated. No wound is mis-applied, no roll is double-counted.

WHAT SURVIVED THE ATTACK: only the bare fact that `beats` can exceed the documented 24 by a bounded amount. Everything the P1 claim hangs on it — skipped separation, a correctness defect — collapsed: separation fires one iteration later, and no invariant or fairness property is harmed. The residue is a cosmetic imprecision in a safety-net counter and a slightly-stale doc annotation, i.e. P3.
- **Revised:** DOWNGRADE P1 -> P3 (polish/doc-drift), do NOT add a mechanic. The "fix" is not necessary by NERS-N: adding an inner ceiling check inside the bind loop would be a guard against a non-problem (separation already fires on the next outer check; the burst is independently capped at exchanges>=BURST_MAX=4) and would risk truncating a bind mid-iteration, changing balance gated on an UNRUN sim — reject. Two cheap, churn-neutral options, in order of preference: (1) Documentation-only: amend the state-map annotation "while beats < 24 (soft=8 x3)" (combat_engine_flow_and_state_map.md:36,41) and the §2 BEAT node to note that `beats` is a SOFT backstop whose effective bound is ~27 (+ fractional feint cost), because the bind sub-loop (:243-244) and feint (:123) increment beats without re-checking the outer guard; clarify that the operative burst ceiling is exchanges>=BURST_MAX, not the beats ceiling. (2) If an EXACT ceiling is ever wanted for legibility, the minimal correct change is to make the bind/feint increments respect the guard by checking `beats < soft*3` after the loop rather than inside it — but this is cosmetic and should be bundled into a future calibration pass, never shipped standalone. Either way: no new lever, no new branch, mirror-fairness preserved.

### 21. TL-01

- **Target:** tradition_layer (tradition.py + ability_armature.md)
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** CONFIRMED against HEAD on every axis; survived all adversarial probes. (1) No 'seize' consumer: a whole-engine grep of ability_bonus/ability_factor (the only two functions that read an ability lever) returns exactly four call sites — wrapper.py:158 'counter_select', wrapper.py:163 'anti_overcommit', wrapper.py:189 'counter_success', and tradition.py:136 eff_cw (channel levers only). NONE passes 'seize'. A repo-wide grep for 'seize' as a lever-string finds only the two ABILITIES defs (tradition.py:96, :106) and the cut-marker comment (systems.py:243); the lone other hit (mc_v14.py:1124) is unrelated mass-battle code. (2) The cut is deliberate and documented: systems.py:243-246 — "Pre-contact seizure CUT 2026-06-05 (Jordan; verified inert): seizure_score + initiative_seize removed … ablation ~0 outcome impact." So the resolver that 'seize' fed was deleted, not merely unwritten; the surviving ongoing-Vor system (initiative_sigma + hit-gains/steals/decay) reads c.initiative, never ability_bonus(c,'seize'). (3) vorschlag (tradition.py:96-97) and sen_no_sen (tradition.py:106-107) both carry lever='seize', value=4.0; with no summation site, equipping either is byte-identical to equipping nothing — confirmed inert. value=4.0 is a raw pre-tanh-cap seizure-score addend orphaned by the resolver deletion. INTENT-GATE rebuttal: there is NO guard/flag/conditional that could make 'seize' reachable under any state — it is unconditionally dead, not safeguarded. STALE rebuttal: not fixed; HEAD still defines both with the dead lever AND still advertises them live. The staleness runs the OTHER way — the docs lag the cut. FLAGSHIP rebuttal: sen_no_sen does NOT route through an alternate lever; Japanese precommit=1.35 (tradition.py:34) survives only as a TRADITIONS channel weight into feint_eval def_read (systems.py:203), a wholly separate mechanism — the equipped ability contributes zero. DOC DRIFT is real and four-fold, all false vs HEAD: ability_armature.md:51 ('seize' = **live**), :106 (Vorschlag ✅), :131 (Sen-no-sen ✅), :167 (seize under "Live levers"); plus tradition.py:87-88 comment still calls 'seize' a LIVE lever "wired at [its] engine site." NERS: fails N (a registered lever with no consumer is the canonical dead/inert defect) and fails the churn principle (presents the German + Japanese flagship seizing abilities as a meaningful equip choice whose only outcome is no-effect — a choice with a hidden dominant answer). SEVERITY: down P1->P2. P1 = breaks play/correctness; this breaks neither — the engine is invariant-safe (no-op when unequipped, ability_armature.md:14), no crash, no wrong number, no unbounded loop, no role-conflation. It is dead-data + doc-drift, the P2 band (inert/ambiguity/drift). The kind label dead-data is correct.
- **Revised:** Accept the finding, reclassified P2 dead-data + doc-drift. Two coordinated fixes, no new mechanics (this CONSOLIDATES, it does not lever-pile):

REJECT any urge to re-add a 'seize' resolver. The pre-contact seizure path was ablated to ~0 outcome impact (systems.py:243-246) and re-wiring it would re-introduce the washed-out mechanic the cut removed — an E/N failure. Do NOT build a new seizing subsystem.

INSTEAD, re-home the two abilities onto a lever that already has a live consumer so the German/Japanese flagships do real, bounded work without a new subsystem:
 1. Retarget vorschlag and sen_no_sen from the dead lever='seize' onto an EXISTING wired seizing/initiative surface. The cleanest live receiver is the in-fight Vor: route a '+' seize-style bonus into init_steal_factor / the Indes steal at wrapper.py:151,158 (the German tactile+leverage / Japanese tempo steal already exists there), or fold them into 'counter_select'/'counter_success' which are live (wrapper.py:158,189). Recalibrate value=4.0 — that magnitude was sized for a raw pre-tanh seizure score and is meaningless on a probability/sigma lever; pick a Class-C value bounded by the ~+8pp tanh envelope (ability_armature.md:17) and VALIDATE against the invariant suite. This is gated on the unrun sim, so land it behind a calibration check, not blind.
 2. If retargeting is deferred, then mark them inert HONESTLY rather than leaving false "live" claims: in ability_armature.md flip line 51 to "registered; INERT (consumer cut 2026-06-05)", change the ✅ at lines 106 & 131 to a pending/inert marker, and strike 'seize' from the §7 "Live levers" list (167); and fix the tradition.py:87-88 header comment that still calls 'seize' a LIVE wired lever. The doc-drift repair is mandatory and cheap regardless of which path (1 or 2) is chosen — a designer reading "live" will mis-equip.

Either way the dead lever must not persist while advertised live. Preferred order: fix the docs now (zero-risk, P2-clearing), then schedule the retarget+recalibration as the real N-restoring work behind a sim gate.

### 22. TL-02

- **Target:** tradition_layer (tradition.py + ability_armature.md)
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** PARTIALLY SURVIVES, but materially overstated; severity wrong. CONFIRMED against HEAD: sen_no_sen (tradition.py:106) targets lever 'seize'; a whole-engine grep shows NO consumer calls ability_bonus(c,'seize') or ability_factor(c,'seize'). The sole seizure consumer (seizure_score/initiative_seize) was CUT 2026-06-05 and verified inert (systems.py:243-246; Jordan ablation ~0 outcome impact). So sen_no_sen IS dead (no-op when equipped) — TL-02 correctly inherits TL-01. Same defect kills german 'vorschlag' (tradition.py:96), so this is a shared orphaned-lever class, not a Japanese-only quirk. WHAT DID NOT SURVIVE: the structural sub-claim that 'Japanese has no live mechanic expressing this identity.' That is false. Japanese's flagship channel precommit=1.35 (tradition.py:34) is consumed and load-bearing at systems.py:203 (eff_cw(defender,'precommit') multiplies read-resistance in feint_eval), and its tempo=1.20 feeds live sites at wrapper.py:117 (initiative weighting), wrapper.py:158 (counter selection), systems.py:202/268/279. The finding itself concedes 'channel weights ... are wired.' By the tradition design contract (tradition.py:1-19: 'a tradition only biases HOW its fighter reads/selects'), substrate channels — NOT abilities — ARE the primary identity expression; abilities only modulate on top (tradition.py:78; ability_armature.md:1,12). So Japanese is mechanically differentiated and DOES express its sen/precommit identity; it just lacks an additional live ability modulator. The finding conflates the live substrate layer with the dead ability layer. SEVERITY: P1 is wrong. The ability contract is 'default unequipped = no change = invariant-safe' (combatant.py:68 equipped defaults []; tradition.py:117-130 default 0.0/1.0). A dead seize ability is therefore a SILENT no-op — the explicitly-designed safe-failure mode — not a play/correctness break: nothing crashes, no invariant breaks, mirror-fairness holds. The defect is (a) an inert/orphaned mechanic and (b) doc-drift: tradition.py:87, ability_armature.md:51 and :167 still assert 'seize' is 'live.' Inert-mechanic + drift is the canonical P2 profile, not P1. NOT stale (abilities/docs never updated after the 2026-06-05 cut) and NOT a safeguarded intent gate (it's an incomplete cut leaving orphans). NERS: fails N (a registered lever with zero consumer is dead weight) and fails E (docs claim a capability the code does not deliver). The churn principle is intact via the channels (precommit/tempo branch real outcomes), so the missing-churn framing of the ability is real but low-impact.
- **Revised:** Reframe and downgrade to P2, merge with the vorschlag twin. Do NOT add a new seize mechanic: the pre-contact seizure was deliberately CUT 2026-06-05 as ~0-impact (systems.py:243-246); re-introducing it would re-pile a lever Jordan already ablated (E/N fail, gated on an unrun sim). Instead CONSOLIDATE to the cut. Pick one: (A) Re-home both orphaned seize abilities onto a LIVE lever that matches their documented technique — sen_no_sen is genuinely 'simultaneous seize as the opponent commits,' i.e. an in-tempo counter, so retarget it to lever='counter_select' or 'counter_success' (already-wired, wrapper.py:158/189) and bound the value vs the invariant suite; vorschlag (the first-strike taking the Vor) maps naturally to initiative/tempo. This gives Japanese AND German a live ability without any new physics. OR (B) If retargeting is premature, demote both to channel-pending status and fix the drift: change tradition.py:87 and ability_armature.md:51,167 to stop calling 'seize' live (it is not — its consumer was removed), matching how staerke_schwaeche/misura/atajo are already honestly marked '(channel; pending).' Either way, restate TL-02 as: 'two seize-lever abilities (sen_no_sen, vorschlag) are orphaned by the 2026-06-05 seizure cut and three doc sites falsely list seize as live; Japanese's tradition identity is otherwise expressed via its wired precommit/tempo channels.' Strip the 'no live mechanic expressing this identity' language — it is contradicted by systems.py:203 and wrapper.py:117.

### 23. TL-03

- **Target:** tradition_layer (tradition.py + ability_armature.md)
- **Real:** True
- **NERS compliant:** False
- **Severity adjust:** P1 -> P2
- **Reasoning:** VERIFIED TRUE against HEAD; the claim survived every attack. (1) Deadness: tradition.py:96 (vorschlag) and :106 (sen_no_sen) both set lever='seize', op='+', value=4.0. The ONLY ability consumers in the engine are wrapper.py:158 (counter_select via ability_factor), :163 (anti_overcommit via ability_bonus), :189 (counter_success via ability_bonus). A whole-repo grep finds NO ability_bonus(c,'seize') / ability_factor(c,'seize') anywhere; the substrate files (tests/sim/v32-combat-balance/ m1/r1/r8/damage_model) contain ZERO TR./ability_bonus/ability_factor references, so no hidden consumer exists there either. systems.py:243-246 documents the pre-contact seizure (seizure_score + initiative_seize) was CUT 2026-06-05 as 'verified inert' (ablation ~0 outcome impact). (2) Independent empirical proof: designs/audit/2026-06-09-personal-combat-comprehensive/comprehensive_analysis_personal_combat.md:117 reports an n=3000 paired-seed longsword-mirror sim: 'vorschlag, sen_no_sen ... all exactly 0.0pp (identical RNG trajectories: the equip is never read). Call-site census confirms: only counter_success, counter_select, anti_overcommit consumed.' So the deadness is not just static-analysis inference, it is measured. (3) Doc-drift is real and at the cited lines: tradition.py:87 ('LIVE levers this version ... seize'); ability_armature.md:167 ('Live levers: seize (Vorschlag, Sen-no-sen) ... Each shows a positive, bounded edge') — affirmatively false; plus ability_armature.md:51 (lever table marks seize 'live'), :106 and :131 (✅ checkmarks for vorschlag/sen_no_sen). ADVERSARIAL CHECKS that did NOT exonerate: (a) Not a safeguarded intent-gate — a gate is a deliberate no-op with code present; here the consumer was deleted, orphaning the lever, and the docs claim a positive edge that the sim shows is exactly 0.0. (b) Not stale/already-fixed — HEAD still carries the false 'live' claims, and the standing decision docket (2026-06-17 M3; loose_ends_register M3) flags it as still 'needs micro-call', confirming it is open. (c) Extra severity-relevant fact: sen_no_sen is the JAPANESE flagship; with vorschlag also dead, German and Japanese each lose their named seizure ability, and chinese/filipino have no abilities at all — but that is a churn/coverage concern, not the doc-drift finding per se. NERS: ners_compliant=false because the lever fails N (Necessary) — a registered lever with no consumer is the canonical dead-data / lever-pile defect — and the doc fails P-i (legible odds): it tells the player an ability confers an edge that the resolver never reads. SEVERITY: the target's P1 is too hot. kind is doc-drift; the engine remains invariant-safe (unequipped = no-op; equipped seize = silently 0pp), so nothing breaks play or correctness — equipping vorschlag/sen_no_sen just does nothing. That is the textbook P2 profile (inert mechanic + misleading doc). Prior audits independently classed it as a vetoable micro-[CANON]/[MECH] cleanup (M3), not a play-breaker. Hence P1 -> P2.
- **Revised:** RETIRE the dead lever and repair the docs; do NOT re-point. Concretely: (a) remove the 'vorschlag' and 'sen_no_sen' entries (tradition.py:96-97, 106-107), or down-rank them to a clearly-marked '(seize lever cut 2026-06-05; not built)' candidate status mirroring how channel-pending abilities are labelled — NOT a 'live' claim; (b) delete 'seize' from the LIVE-levers comment at tradition.py:87; (c) fix ability_armature.md:167 (drop seize from 'Live levers'; remove 'Each shows a positive, bounded edge' as applied to seize), ability_armature.md:51 (status -> 'cut / not built'), and the ✅ at :106/:131. REJECT the re-point option (point vorschlag/sen_no_sen at counter_success or a new pre-contact edge): it fails NERS-N/E as a lever-pile — it would stack a third+fourth source onto the already-live counter lever for the same job, and systems.py:244 records that the pre-contact Vor edge (INIT_SEIZE_K) empirically washed out under per-beat dynamics, so resurrecting it adds churn-neutral tuning, not a branching choice. The consolidating, churn-positive fix is retire-and-document, matching the standing M3 recommendation. NOTE for follow-up (separate finding, not this one): retiring these leaves the Japanese tradition with zero live abilities and German with only indes — a tradition-coverage gap worth a deliberate design call, but that is gated on an unrun ability-design pass and should not be bundled into this doc-drift fix.

### 24. W-01

- **Target:** wrapper_structure
- **Real:** False
- **NERS compliant:** True
- **Severity adjust:** P1 -> P3 (downgrade; core claim refuted, only a residual harness-only hardcode remains)
- **Reasoning:** REFUTED against HEAD. The finding's central claim — "all four modules insert dead /home/claude paths" — is factually false at the cited lines. (1) core.py:3 and combatant.py:4 insert os.path.join(os.path.dirname(__file__), '../../../tests/sim/v32-combat-balance') — a RELATIVE, __file__-anchored path. I resolved it: it normalizes to tests/sim/v32-combat-balance and all three named substrate modules (m1_dice_sigma_core.py, r1_sigma_resolution.py, r8_parity_harness.py) physically exist there (Glob confirmed). So the engine DOES reference the real substrate location, contradicting the finding's own assertion "no engine module references that path." (2) systems.py:4 and wrapper.py:4 insert os.path.dirname(__file__) — the engine's own dir, to import siblings core/config/tradition/combatant; fully portable. (3) A repo-wide grep for 'home/claude' over the engine dir returns ZERO hits — no engine module contains that string at all, let alone at the four cited lines. The cited evidence is therefore the PORTABILITY FIX, not the defect; W-01 describes a prior pre-migration state (corroborated by core.py:16-20's 'never propagated to engine CODE until now' and the structural-fix docstrings) that has since been corrected. WHAT SURVIVED THE ATTACK: a single genuine /home/claude residue, but NOT where W-01 puts it — r8_parity_harness.py:244 and :253 hardcode open('/home/claude/r8_results.json','w'). This is inside def run(seed=None) (line 202), invoked only under the substrate's own __main__ guard (line 249). core.py imports r8 and calls only pure primitives (resolution_pool, roll_net_continuous, DECISIVE_OB, TN_STANDARD) — never r8.run() — so importing the engine NEVER reaches that write. It cannot make the engine 'non-runnable on this checkout, CI, or Godot' as claimed; at worst it breaks running r8 as a standalone __main__ on non-sandbox filesystems. Severity: not P1 (no import-time breakage, engine import chain is clean and portable), reduce to P3 polish on a substrate test harness. NERS: the portable __file__-anchored insertion is the Necessary, consolidated fix (one path expression, both anchor points) — no lever-pile, no added subsystem; it is exactly the 'one fix in the right place' the rubric rewards. No mirror-fairness/invariant implications. Independent residual caveat (outside W-01's scope, do not over-credit it to this finding): the engine being import-runnable does not mean it executes — that depends on the substrate API surface, which W-01 does not test.
- **Revised:** REJECT W-01 as written — it is stale/refuted, not a live P1. Do NOT act on the claim that the four engine modules carry dead /home/claude paths; they do not (grep: zero hits in the engine dir), and core.py:3 / combatant.py:4 already use a portable __file__-anchored relative path that resolves to the existing tests/sim/v32-combat-balance substrate. Close W-01 as fixed-before-audit. Split off the ONE real residual as a separate P3, scoped correctly: r8_parity_harness.py:244,253 hardcode open('/home/claude/r8_results.json'); replace with a path relative to __file__ (or tempfile/CWD) so the harness's __main__ entrypoint runs off-sandbox. This does not affect engine import/runnability (the write is unreachable from the engine's import path), so it is polish, not a blocker. If a true 'engine non-runnable' finding is wanted, retarget it at the real risk — substrate API drift (whether r8/r1/m1 still expose roll_net_continuous, resolution_pool, effective_ob, DECISIVE_OB, TN_STANDARD with the signatures core.py expects) — which W-01 neither tests nor establishes.

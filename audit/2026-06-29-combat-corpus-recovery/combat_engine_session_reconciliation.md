# Valoria Combat Engine — Session Master / Handoff
**2026-06-05 · rev 2 (findings incorporated)**

> **POST-REV-2 UPDATE (2026-06-05):** Two items below have since landed as canon — **armour flat-Health strike → `52b77d04`** (Jordan ratified); **seizure cut → `44dcc54`** (inert pre-contact scoring removed — `seizure_score`/`initiative_seize`/5 `INIT_SEIZE_*` gone; validated outcome-invariant 0.402/0.035/0.562 vs baseline 0.402/0.033/0.565 + mirror=50 at N=2000; no-one-shot intact). §1/§3/§5 predate these. **Still open:** OOB (recalibrate vs retire — Jordan's design-intent call), the Stage-2 build, the editorial canonization of findings, and a freshness SHA refresh (`freshness_gate.py --update` — `derived_stats`' recorded `canonical_sha` went stale when the armour strike co-filed `canonical_sources` unchanged; 2 others pre-existing, all non-blocking).

Supersedes the prior master. Records the comprehensive ablation audit, the targeted verifications run this session, the resolution status of every pending item, and the staged build. Provenance tags: `[HEAD]` verified from `main` this session · `[COMMITTED]` landed · `[BLOCKED]` halted by a gate · `[PROPOSED]` grounded + Jordan-vetoable · `[VERIFIED-SIM]` empirical this session.

## 1. State
**Committed this session:** `deb405b9` (wound −1D penalty wiring at both resolution sites), `2315b7d9` (config comment fix), `e414098` (Stage-1 physical-data layer — `mass`+`pob_frac` on all 12 weapons; behaviour-neutral, unconsumed).
**Attempted + BLOCKED:** the §4.1 flat-armour-Health strike. `safe_commit` halted on the co-file gate — a design-doc edit requires `references/canonical_sources.yaml` in the same commit. Not bypassed (RuntimeError = hard halt; no `--no-verify`, no fabricated co-file change). Exact edit staged in §4; needs the co-file resolution **and** — since it edits AUTHORITATIVE PP-716 canon — Jordan's explicit go.

## 2. Audit + verifications (findings incorporated)
Audit: `combat_engine_comprehensive_audit_2026-06-05.md` (empirical ablation of 143 CFG constants; 480 bouts/config). Verifications this session corrected/hardened three of its findings:

- **P1 — the seizure contest is ornamental via WASH-OUT (the audit's "first-mover bypass" framing was wrong).** `[HEAD]` Seizure feeds the *starting Vor* (`A.initiative`, wrapper:29), which **does** propagate to `net_sigma` via `initiative_sigma` — so it is **not** bypassed by the random first mover (that only randomizes the aggressor *role*). It is inert because the initial Vor is small (`INIT_SEIZE_K` 0.45·tanh, ≈ ±0.15) and dominated by per-beat dynamics (`INIT_GAIN_HIT` 0.18/hit > the seizure edge; plus decay and steals). The **ongoing** initiative channel (`INIT_SIGMA_K` 0.16, hit-gains, steals) is load-bearing; the **pre-contact** seizure scoring (`INIT_SEIZE_*`) is not. `[VERIFIED-SIM]`
- **P2 — OOB + stamina-collapse are UNREACHABLE, not merely rare.** `[VERIFIED-SIM]` 0 OOB fires across 7,000+ attacks (standard + long defensive bouts); min stamina ever reached = 7.1 (standard) / 16.3 (attrition) — never near the ≤0 trigger. OOB has real teeth when it fires (`OOB`=2 → −1.0σ to the attacker) and collapse exits at ≤ −4 — but the wound/felling economy resolves bouts first and inter-turn recovery refills stamina, so neither trigger is reached. Vestigial under the current stamina economy.
- **Attribute importance tier** `[VERIFIED-SIM]` (stat 6-vs-3, arming, decided-share to the advantaged fighter): History **0.99** (dominant; pool driver, excluded ✓) · Cog 0.91 · Agi 0.92 · Strength 0.90 · End 0.89 · Att 0.87 (the five clustered ~equal) · **Focus 0.71 (underpowered)** · Spirit 0.67 · Disp 0.66 (excluded). **Focus is the lone parity outlier** among the six tune targets — the same defect as P4 (concentration inert): Focus's main channel doesn't affect outcomes. The concentration error-onset model is the lever that fixes **both**, so the parity tune is mostly "raise Focus" and must run **after** the concentration model.

## 3. Resolution status — every pending item
- **[RESOLVED]** concentration formula doc-fix — already struck 2026-06-04 (Jordan, ED-902); `derived_stats` L500/L533 carry `(3×Focus)+(2×Spirit)`, old `Focus×3` marked STRUCK. (The prior pending-list entry was stale — caught by verifying from HEAD.)
- **[BLOCKED → staged]** armour flat-Health strike — see §4.
- **[PROPOSED]** the gated design calls — see §5.
- **[STAGED]** the weapon-physics build — see §6.

## 4. Staged edit — armour flat-Health strike (blocked by co-file gate)
File `designs/scene/derived_stats_v30.md` §4.1 (L69). Replace the substring `Adds flat Health (+4 leather, +6 chain, +8 plate)` with a struck-and-noted form (armour = damage reduction only; engine `WoundTracker` takes no `equipment_health`; consumables and poisons retained). Reversible strike, not a deletion. **To commit:** include `references/canonical_sources.yaml` in the same `safe_commit` additions (co-file rule) **and** obtain Jordan's explicit go (AUTHORITATIVE PP-716). Commit scope `[editorial]`, `task_gate('design')`.

## 5. Proposals (bottom-up + top-down; Jordan-vetoable)
- **Seizure (P1):** cut the pre-contact seizure scoring (`INIT_SEIZE_READ/REACH/CONC/K`, `seizure_score`, `initiative_seize`) as ornamental; keep the ongoing initiative system. *Unless* the game's first mover is meant to be seizure-determined — then wire `fight()`'s first mover to the seizure winner and raise `INIT_SEIZE_K` so it registers. [top-down: the HEMA "Vor" is won and then *held/contested* beat-to-beat — the ongoing-initiative model already captures that; a separate large pre-contact term is redundant.] **Decision: cut vs wire.**
- **OOB / fatigue (P2):** (a) recalibrate the stamina economy (raise `act_cost` / lower `RECOVERY_FRAC`) so depletion is reachable in long fights — making OOB + conditioning a real attrition axis (a second channel for End/Spirit); or (b) retire OOB + the −4 collapse exit as vestigial. **Tied to:** should fatigue-to-collapse be a duel outcome? [top-down: argues for (a) calibrated so collapse is rare-but-present.]
- **Over-atomization (P3) + CFG structure (P6):** do **not** bulk-delete the 31 low-impact constants (P1's lesson: low-impact-in-harness can be *untested*, not removable). Instead group `CFG` by subsystem + annotate each constant's measured impact, then per subsystem decide calibrate-vs-cut. Confirmed-inert (`INIT_SEIZE_*`, OOB-as-is, grip levers, `DISRUPT_K`) = first cut/wake candidates; fine-scaling K's (`COUNTER_TRAIN_K`, `BIND_TECH_K`, `DISP_*_K`, …) feed live channels → calibrate-or-cut per item.
- **Parity tune (six attributes):** mainly raise Focus (0.71 → ~0.88); Strength/Agi/Cog/End/Att are already ~equal (0.87–0.92). Run **after** the concentration error model (which gives Focus its missing channel) and **after** the weapon re-baseline.
- **Open Jordan calls (propose at build):** concentration error-onset params (T_err, ERR_K, effect); three-regime authority/MoI constants; defender-side wound; draw-rate (~7% vs tiebreak).

## 6. Staged build — Stage-2 weapon physics (fresh session strongly recommended)
Dependency order: (a) THREE-REGIME derived physics — hilt-swung blade / polearm thrust-pivot / center-grip strike — on the committed `mass`/`pob_frac`; (b) wire consumers (authority → HEFT/percussion/cut; MoI → `act_cost`/`weapon_tempo`/`str_demand`; eff_head → reach; edge → cut; leverage grip-modulated); (c) activate grip-partition + decide dynamic grip-switching; (d) concentration error-onset mechanic; (e) MANDATORY re-baseline (mirror=50, no-one-shot/`CAP_END`, per-attribute tier). The parity tune (§5) folds into the re-baseline. Spec: `weapon_physics_and_concentration_model.md` (v2). **Nothing ships until sim confirms.**

## 7. Provenance / next-session inputs
`[HEAD]` this session: engine wiring (seizure→Vor→net_sigma, OOB trigger + unreachability, stamina economy), `derived_stats` L69/L500/L533, CFG magnitudes. `[VERIFIED-SIM]`: the 143-constant ablation sweep + the P1/P2/attribute verifications. The local `combat_engine/wrapper.py` carries scratch OOB-counter instrumentation (NOT committed — ignore; re-fetch from HEAD). Inputs for the next session: this master · the audit doc · the v2 model spec · `combat_engine_cross_reference.md`.

`[CONFIDENCE: high — verifications empirical (480 bouts/config; 7,000+ attacks for OOB), magnitudes from CFG, doc statuses from HEAD; the armour strike correctly halted by the co-file gate, not forced.]`

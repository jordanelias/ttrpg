# cluster_C-NERSPESS

_Evidence cluster dossier, read-only; archived verbatim from the agent's final message by the Fable orchestrator (2026-07-07). Verdicts pending refuter pass + Fable adjudication - see `unaddressed_areas_audit_v1.md`._

# Cluster C-NERSPESS dossier

**Prior:** every #77 PASS is wrong until the working tree proves otherwise. **Standing caveat (applied honestly throughout):** the 2026-07-04 audit is explicitly a *qualitative design* audit ("Criteria = the corpus's own instruments … NOT the rolling-engine mechanical test," `ners_qualitative_audit_v1.md:8`). So "the sim doesn't implement it" is only a *partial* rebuttal to a design-PASS. It becomes a *decisive* rebuttal on three of the audit's own terms: (1) the project axiom "**There is no GM — the engine resolves everything**" (CLAUDE.md) means a safeguard/transformation only a doc-reader or unwritten engine delivers is, for *this* game, undelivered; (2) the audit **selectively cited code** when code supported a PASS (`resolver.py:404-419`, `registry.py`) but "prose" when only prose existed (Turmoil, duels, votes), presenting a uniform "canon is better-defended" narrative over heterogeneous enforcement; (3) the audit's own §0 thesis — "the corpus computes more emergence than it can show" — *is* a downgrade of the experienced Ω grades, not a transport footnote.

## Clause-by-clause pessimist table

| Clause | Optimist verdict (#77) | Pessimist verdict | Downgrade strength | Evidence |
|---|---|---|---|---|
| **N — Necessity** | PASS (flag 2) | **PARTIAL** | **MODERATE** | The two flags (convergence markers unbuilt; temperaments inert) are the tip of ~15 built-but-inert / necessary-but-unbuilt items in the audit's **own** §1: M-7..M-11 tier T-31/33/34/35/37/38/39/40 all `Proposed` (`throughlines_complete.md:317` "Proposed. Wave 1 workplan P1."), T-26..29 "pending", Institutional Consolidation, Recall +2D. `sim/territory/temperaments.py:12-16` self-admits "**no consumer currently mutates drift**"; `temperament_of`/`temperament_modifiers` have **zero call sites** anywhere in `sim/` — built machinery wired to nothing, worse than "prose." "Flag 2" compresses a long inert tail into two headline examples. |
| **Ω(a) cross-scale consequence** | PARTIAL | **PARTIAL (top-down/compounding half is a clean FAIL)** | **MODERATE** | `sim/cross_scale/domain_echo.py` exposes only `compute_domain_echo/accord_echo/thread_echo` — **bottom-up only**; no top-down apply. `scale_transitions_v30.md:331-332` §12.4 enumerates **8 down-seams / 15 type-edges** whose emitters "do not yet populate sub-scale targets" → "delivers **blind** — unreached." No collision detector, no cross-tick convergence module in `sim/`. PARTIAL hides that top-down delivery is **0/8 wired** and compounding is **0%**. |
| **Ω(b) personal transformation** | PASS ("best-served clause") | **FAIL in the running system / unverified on design** | evidence **STRONG**, verdict **MODERATE-STRONG** | All five cited channels are **unreachable from `mc_v18`'s campaign loop.** `scene_dispatch.py:91-110` routes only `combat`→`sim/personal/combat.py` (self-marked `[DEPRECATED … do NOT wire new game code through this file]`, `combat.py:4`) and `contest`→`sim/personal/contest/*` (which calls none of the five); everything else → "not live." Scars: `apply_knot_loss` (the only caller of `apply_conviction_scar`) has **zero callers**. Belief revision: `revise_belief` called only from `contest_legacy_stub.py` (DEPRECATED). Arc state machines & Rendering Crisis: **no sim module at all**; `check_coherence_zero_transition` has **zero callers**. The clause graded strongest is the least reachable clause in the corpus. |
| **Ω(c) autonomous world** | PASS (**verified under attack**) | **PARTIAL / FAIL** | **STRONG** | The "verified under attack / genuinely tick" qualifier is the overclaim. (i) **Deterministic priority trees**: `npc_ai.py:22-27` raises `NotImplementedError` for both `select_action` and `evaluate_priority_stack`; `faction_action.py:51` self-admits "GD-2 … **Currently simplified: probabilistic mix per v17**." (ii) **Vote-rule refutation is hollow**: `faction.py:20` declares `fixed_lean` but it is **never assigned** anywhere in `sim/`; vote callers default non-principals to **abstain**; the "GM controls NPC faction votes" wording the audit called "stale, sweep only" is **still live** at `faction_layer_v30.md:490`. (iii) **"Ticks without the player" = ticks toward a code-caused degenerate attractor**: `faction_action.py:128-129` hard-blocks Varfell+Hafenmark (`return 'invalid'`), the mechanical cause of the `GOLDEN_WIN_SHARE = {Crown:50, Church:50, Hafenmark:0, Varfell:0}` pinned in `test_mc_v18_regression.py:38`; that test's `test_mc_v18_win_share_is_well_formed` (:62-68) would **pass at 100/0/0/0** — the oracle *canonizes* the degeneracy it was written to catch. Only neglect-driven insurgency (`insurgency_pipeline.py`) genuinely holds. |
| **Ω(d) non-dominance** | PASS ("survived the hunt") | **PARTIAL** | **MODERATE-STRONG** | Of 6 refuting safeguards, **3 are doc-only/vacuous in sim**: (1) General-Duel bilateral/pause/no-death = human-readable **descriptor strings** in `handoff_rules.py` whose docstring (:10-19) says it "**does NOT execute downstream mechanics**"; zero bilateral/pause/capture logic in `sim/`. (2) **Turmoil ≤6** — `victory.py:73` reads `clocks['Turmoil']`, but `game_state.py:215` inits it 0.0 and **nothing in `sim/` ever writes Turmoil/Strain** (only MS/CI/MASS_SEIZURE clocks are written); the gate is permanently vacuous → the treaty-hegemony rush is **not blocked** in the running sim. (3) Thread-site shared-RS/Investigation gate = **zero sim implementation**. Plus ability-stacking has **no cap** (`ability_primitives.py:54-71` unbounded product; audit's own admission). Only cr5 (line 4) is genuinely enforced. |
| **Q-robust** | PARTIAL FAIL | PARTIAL FAIL (→ FAIL defensible, low-value) | **UNSUBSTANTIATED as a material downgrade** | Optimist already grades it failing. Evidence for full-FAIL exists (F-3 no maintained playability home; no `CURRENT.md` UI row) but PARTIAL-FAIL→FAIL is a non-load-bearing semantic step. Not carried. |
| **Q-smooth** | PARTIAL FAIL | PARTIAL FAIL (→ FAIL defensible, low-value) | **UNSUBSTANTIATED as a material downgrade** | Already failing. F-4 is real and unfixed (articulation's false §6.4 self-assertion still live; `fixed_lean` unassigned; MS/RS + 7-vs-9 roster forks) — reinforces the existing PARTIAL-FAIL, doesn't move the grade. Not carried. |
| **Q-elegant** | PASS (watch combat) | **PARTIAL (combat fails the one-reading bar now, not "so far")** | **MODERATE** | `combat_engine_v1/` is ~20 files + `REARCHITECTURE_v1.md` + `phase4_5_plan_v1.md` (5-tuple mode-selects, grip oscillator, ability×weapon×state Phase-4 model). The "restatable after one reading" bar is already failed for combat; the "deliberate trade **so far**" framing has no ratified decision behind the "so far." Soft because the audit already flags "watch combat." |

## Subsystem-row attacks (6 most vulnerable rows)

1. **Settlement/territory** — optimist: "thin four-verb stat-pump; the drafted fix is real but *untracked*." **Pessimist: STRONG downgrade — the orphaning is recurring LIVE, post-remediation.** ED-SE-0001 (`editorial_ledger.jsonl:570`, filed 2026-07-05) *explicitly ordered*: "add the proposal to CURRENT.md's settlement row and HANDOFF_SE pending items so it cannot orphan." As of HEAD, `CURRENT.md:30` **still omits** `governance_play_redesign_v1`/ED-SE-0001, and `HANDOFF_SE.md` "Next actions" reads "**(none currently tracked.)**" The fix-the-fix went unexecuted — and this is simultaneously a live counter-example to S-2's "only CURRENT.md is fresh."

2. **Personal combat** — optimist: "thin *by design stage, not accident* (R-1); the player-interface layer is future Phase-4 work." **Pessimist: MODERATE — the "reserved slot" is a same-PR-as-the-audit circular IOU.** ED-PC-0001 (`editorial_ledger.jsonl:578`) *sources its own* "DELIBERATE sim-first staging" from "per audit refutation R-1," and `phase4_5_plan_v1.md` Phase 4 is a deeper *autoresolver* (game-theoretic psychology + abilities-as-access), not a player-decision interface — "player" barely appears; "choice" only in a reaction-time stat formula. No player-facing combat content exists.

3. **Social contest** — optimist: "moderate; one game wearing eight venue skins." **Pessimist: MODERATE — "moderate" is generous; it is one game and seven dead skins.** `wrapper.py` `GAMES` dict: only `agon` is `WIRED`; the other **3 of 4 games raise `NotImplementedError`**, and 7/8 proceedings resolve through the one shape. Choice density is closer to "thin." Bonus: `master_workplan_v6.md:184-186` flags a live HANDOFF_SC-vs-root-HANDOFF Stage-3-vs-4 disagreement — the audit's S-2 fragmentation, recurring inside this very row.

4. **Faction/political** — optimist: "thin; surfaces almost no per-season player choice." **Pessimist: MODERATE — the decision-*bearing* core is not thin, it is absent.** The 4-component disposition engine (`faction_behavior_v30.md §3.2-§3.5`) is **100% formula, zero player decision point**; all 6 factions default to single-root Cascade, collapsing the many-actor conviction-blend into one leader-stat lookup. The ~5-verb Domain Action menu wraps a strategic brain with no player input.

5. **NPC behavior** — optimist: "best dramatic legibility; 2/9 trees thread-conditioned." **Pessimist: MODERATE — the headline statistic is a miscount in a "grep-verified" document.** `npc_behavior_v30.md:791` (Church §8.2, Priority 2) is explicitly Thread-operation-gated ("Open Thread operation in Church territory … → Heresy Investigation"), yet the audit's lens lists Church among the "7 of 9 thread-blind / have none." Doesn't overturn the "best legibility" ranking; does puncture the precision the audit prides itself on.

6. **Architecture spine** — optimist: "uniform, direction-neutral delivery." **Pessimist: MODERATE — "uniform" is false; the load-bearing junctions are the least-specified and top-down is 0/8 wired.** `module_contracts.yaml:688` `engine_clock` is `doc: null` with resolver `clock_advance` that has **zero implementation** in `sim/` (a season counter runs, but under `season_manager.py`, an orphaned label); `domain_actions consumes:[]`. Direction-neutrality is aspirational: §12.4's 8 down-seams are all emitter-unpopulated (see Ω(a)).

## Intent-attribution attacks (R-1..R-6)

The §7 pattern was "surface TRUE, framing FALSE, intent **DELIBERATE**." Testing the DELIBERATE attributions against ratified backing (ED/PP/Jordan sign-off) vs. thin backing (comment/inference/PROPOSED):

| R# | Intent claim | Backing found | Strength | Attack |
|---|---|---|---|---|
| **R-1** | Combat autoresolve = DELIBERATE sim-first staging, UI = Phase-4 work | **Circular.** ED-PC-0001 (`ledger:578`) states "DELIBERATE sim-first staging **per audit refutation R-1**" — the "ratifying" ED cites the audit as its own source; Jordan's 2026-07-05 sign-off was a **bulk-accept of 12 options** as a scheduling to-do. No pre-audit ED/PP says UI was deliberately Phase-4-staged. Phase 4 is an autoresolver, not a UI. | **THIN** | Auditor inference dressed as ratified intent. The one plan that would house the UI houses a deeper autoresolver instead — deferral of a thing on no plan is indistinguishable from "not designed." |
| **R-2** | Articulation is DELIBERATELY thread-agnostic (wildcard+salience) | **Self-contradicted.** The wildcard architecture is genuinely pre-ratified (PP-687, CANONICAL doc). But the audit's **own** primary record `refute_articulation-zero-thread.md:56` rates the thread-trigger-omission intent "**UNDETERMINED** (not a documented deliberate exclusion … it was simply never authored)" — which §7 silently upgraded to "DELIBERATE." | **THIN (for the specific defect)** | §7 conflated the deliberate *wildcard* with the undetermined *missing-cut-scene-trigger*. The audit overrode its own evidence to publish a cleaner "DELIBERATE." |
| **R-3** | NPC trees deterministic per GD-2 mandate | GD-2 is a **literal Jordan hard rule** (HR-9), canonized `canon/02_canon_constraints.md:39` under an explicit "requires Jordan ratification" regime, cross-confirmed by ED-865/ED-874. | **STRONG** | *No valid attack.* Reported honestly — see Honest gaps. (Note: the *design* is ratified; the *code* doesn't implement it — that lands on Ω(c), not on R-3's intent.) |
| **R-4** | P-14 governs thread *operations'* co-movement (not formula terms); §3.5 delivers | **Split.** P-14's own text (`canon/02:23`) is literally scoped "…thread **operations** without three-dimensional co-movement" — the narrow reading is *faithful*, not an auditor invention. But "§3.5 **delivers**" rests on a 2-sentence stub + a delegated-authority ED "logged for review." | scope **STRONG**, "delivers" **THIN** | Attack only the "delivers" half; the scope reading survives. |
| **R-5** | Governance G1 prerequisite BUILT (registry + 6 tests) | Factual, not intent. `tests/sim/territory_registry/test_registry_ledger.py` has exactly **6 tests, re-run → all pass**. | **VERIFIED TRUE** | *No valid attack.* |
| **R-6** | GM-controls-votes = deterministic rule exists, "stale wording sweep only (P3)" | The rule is canonical prose (`faction_layer:492`) — intent is real. **But** the *severity framing* is wrong: the rule is **unimplemented** (`fixed_lean` never assigned) and the "GM controls" line (`:490`) is **still unswept**; no ED targets the exact line (batch-acceptance swept both contradictory sentences in together). | intent **STRONG**, "sweep-only P3" framing **THIN** | Not an intent downgrade; a *severity* downgrade — "cosmetic P3 sweep" understates an unimplemented rule beside live contradictory text. |

**Bottom line:** the two cleanest thin-intent targets are **R-1 (circular self-ratification)** and **R-2 (contradicts the audit's own UNDETERMINED record)**. R-3/R-5 are genuinely solid; R-4/R-6 are split (attack the "delivers"/"P3" halves only).

## Findings (STRONG/MODERATE only)

- **C-NERSPESS-1 · P1 · STRONG · Ω(c) autonomous-world PASS(verified under attack) → PARTIAL/FAIL.** The three cited pillars: priority-trees pillar is a stub (`sim/autoload/npc_ai.py:22-27` `NotImplementedError`; `sim/provincial/faction_action.py:51` "GD-2 … Currently simplified: probabilistic mix"); the vote-rule refutation is hollow (`sim/personal/contest/faction.py:20,42` `fixed_lean` read-but-never-assigned; "GM controls" still at `designs/provincial/faction_layer_v30.md:490`); "ticks without the player" ticks toward a code-caused degenerate golden (`sim/provincial/faction_action.py:128-129` blocks 2/4 factions → `sim/tests/test_mc_v18_regression.py:38` pins 50/50/0/0, `:62-68` doesn't guard it). Only insurgency holds. Intent: the sim is admittedly WIP, but the audit asserted *operational* verification the running system contradicts.

- **C-NERSPESS-2 · P1 · STRONG (evidence) / MODERATE (verdict) · Ω(b) "best-served clause" PASS → unverified/sim-unreachable.** No transformation channel executes on the `mc_v18` path: `sim/cross_scale/scene_dispatch.py:91-110` routes only `combat`→deprecated `sim/personal/combat.py:4` and `contest`→channels-free package; `apply_knot_loss`/`check_coherence_zero_transition` have zero callers; `revise_belief` only from deprecated `contest_legacy_stub.py`. Downgrade tempered by design-audit scope (the doc-homes exist).

- **C-NERSPESS-3 · P1 · MODERATE-STRONG · Ω(d) "survived the hunt" → PARTIAL.** 3/6 safeguards doc-only/vacuous in sim. Anchor: `sim/autoload/victory.py:73` gates on `clocks['Turmoil'] ≤ 6` but `sim/autoload/game_state.py:215` inits it 0.0 and **no code writes Turmoil/Strain** → gate permanently vacuous → treaty-hegemony rush unblocked. Duel safeguards are descriptor strings (`sim/cross_scale/handoff_rules.py:10-19` "does NOT execute"); thread-site gate has zero sim; ability-stacking uncapped (`designs/scene/combat_engine_v1/ability_primitives.py:54-71`).

- **C-NERSPESS-4 · P2 · STRONG · Settlement row: F-1 orphaning recurs LIVE post-remediation.** ED-SE-0001 (`canon/editorial_ledger.jsonl:570`) ordered adding the redesign to `CURRENT.md` + `HANDOFF_SE`; `CURRENT.md:30` still omits it and `handoffs/HANDOFF_SE.md` "Next actions" = "(none currently tracked.)". Present-tense proof the diagnosed failure mode is active.

- **C-NERSPESS-5 · P2 · MODERATE · R-1 intent is circular.** `canon/editorial_ledger.jsonl:578` (ED-PC-0001) sources "DELIBERATE sim-first staging" from "audit refutation R-1"; bulk-accepted by Jordan, no pre-audit ratified basis; `phase4_5_plan_v1.md` Phase 4 contains no player-input layer.

- **C-NERSPESS-6 · P2 · MODERATE · R-2 intent contradicts the audit's own record.** `designs/audit/2026-07-04-ners-qualitative-audit/01_workings/refutations/refute_articulation-zero-thread.md:56` = "Intent = UNDETERMINED … simply never authored"; §7 published "DELIBERATE."

- **C-NERSPESS-7 · P2 · MODERATE · Ω(a) PARTIAL masks a top-down/compounding FAIL.** `sim/cross_scale/domain_echo.py` bottom-up only; `designs/architecture/scale_transitions_v30.md:331-332` = 8 down-seams / 15 type-edges emitter-unpopulated ("delivers blind"); no collision detector / cross-tick module. Top-down 0/8, compounding 0%.

- **C-NERSPESS-8 · P3 · MODERATE · N PASS(flag 2) → PARTIAL on breadth.** ~15 built-but-inert / necessary-but-unbuilt items in the audit's own §1 compressed to 2 flags; `sim/territory/temperaments.py:12-16` self-admits "no consumer"; `references/throughlines_complete.md:317` T-31 "Proposed."

- **C-NERSPESS-9 · P3 · MODERATE · Social contest "moderate" density overstated.** `wrapper.py` `GAMES`: 3/4 games `NotImplementedError`, 7/8 proceedings one shape — closer to "thin."

- **C-NERSPESS-10 · P3 · MODERATE · NPC "2/9 thread-conditioned" miscounts.** `designs/npcs/npc_behavior_v30.md:791` (Church §8.2) has an explicit Thread-operation-gated row the audit's lens lists as "thread-blind."

## Honest gaps (UNSUBSTANTIATED — not carried as downgrades)

- **Ω(d) line 4 — cr5_self_backfire "orientation payoff unwired."** The safeguard is **genuinely live and default-on**: `sim/personal/contest/resolver.py:404-419` is on the reachable `run()→resolve()→_apply()` path, and `sim/personal/contest/armature.py:431` sets `cr5: bool = True` by default. The audit's refutation is correct. No downgrade.
- **R-3 — GD-2 intent.** A ratified Jordan hard rule (HR-9; `canon/02_canon_constraints.md:39`; ED-865/ED-874). The intent attribution is STRONG; the *only* attack (that the code doesn't implement GD-2) belongs to Ω(c), not to R-3's intent.
- **R-5 — registry 6 tests.** Verified true, re-run, 6/6 pass. Not attackable.
- **R-4 scope reading.** P-14's own text (`canon/02_canon_constraints.md:23`) is genuinely scoped "per Thread operation" — the narrow reading is faithful, not an auditor convenience. Only the "§3.5 delivers" half is thin.
- **Articulation subsystem row.** Fully and precisely confirmed as the audit stated (10 triggers, `key_type_registry_v30.md:682/705/724` declare the omitted keys consumed, false §6.4 self-assertion still live, ED-IN-0004 open). Nothing to downgrade — the audit was byte-accurate here.
- **Q-robust / Q-smooth to full FAIL.** The optimist already grades both PARTIAL-FAIL; pushing to FAIL is a non-load-bearing semantic step. Evidence exists but yields no material downgrade.
- **massbattle "General killed" as an ED-898 contradiction.** `sim/provincial/massbattle.py:1294` is a *separate* mass-battle morale mechanic (losing your general → Morale −2), not the General-Duel no-death guarantee; I do not lean on it. UNSUBSTANTIATED as an ED-898 stale-ness claim (the duel finding stands on the descriptor-string / no-enforcement evidence instead).

**Net:** the strongest carried downgrades are **Ω(c) (STRONG, P1)**, **Ω(b) (STRONG evidence, P1)**, **Ω(d) (MODERATE-STRONG, P1)**, and the **live settlement recurrence (STRONG, P2)**. The dominant pessimist pattern inverts the optimist's: where #77 found "surface TRUE, defect FALSE, intent DELIBERATE," the working tree shows **"design TRUE, operational claim FALSE, and the 'DELIBERATE' often either circular (R-1), self-contradicted (R-2), or a WIP-stub the audit read as a finished safeguard (Ω(c)/Ω(d))."** The audit's reconciliation line — "canon survived its adversaries better than the finders expected" — holds for the *design corpus* but not for the *running oracle the Godot port is built from*, and the audit did not distinguish the two.

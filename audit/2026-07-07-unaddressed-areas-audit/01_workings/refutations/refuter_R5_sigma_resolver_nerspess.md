# Refuter R5 — sigma/resolver/NERS-pessimist claims

_Archived verbatim from the agent's final message (2026-07-07)._

# Refutation Record — Independent Refuter R5

**Method:** Read only the charter (`00_grounding/00_charter.md`); no cluster dossiers consulted. All verdicts below are grounded in direct reads of primary source files (code, ledger, design docs) and Python probes I wrote and ran myself to reproduce every quoted number independently. Default posture: REFUTE unless the evidence reproduces cleanly.

---

**Claim 1 — ER-2 continuity-correction split between combat and contest kernels**
**VERDICT: CONFIRMED**
Evidence: `designs/scene/combat_engine_v1/core.py:35-45` (`degree()`) applies `net >= ob-0.5` for Success — the ER-2 correction, explicitly named in its own docstring. `sim/autoload/sigma_leverage.py:284-311` (`degree()`, imported and called by `sim/personal/contest/resolver.py:24,289` — the live contest kernel) uses `net < ob → Partial`, no −0.5 term anywhere in the file (grepped, zero hits for "ER-2"/"continuity"/"0.5" continuity term). Probe: `combat_core.degree(2.7,3)` → `'success'`; `SL.degree(2.7,3,pool=5)` → `1` (Partial) — identical net, opposite bands, reproduced exactly. Pool floor confirmed shared: combat `POOL_FLOOR=5` (`core.py:27`), contest `Pool.size = max(5, faculty*2+BASE)` (`sim/personal/contest/primitives.py:211`).
Intent gate: **NOT-INTENDED.** `params/core.md:80` states the correction applies generally ("the same half-step applies at each integer degree boundary"), not scoped to combat only; `sigma_leverage.py` contains zero acknowledgment of ER-2; `core.py`'s own docstring admits the fix was "never propagated to engine CODE until now" — evidence of incomplete propagation, not a documented scoping decision.

**Claim 2 — Combat's fixed 2·Ob Overwhelming bar vs contest's pool-aware bar**
**VERDICT: QUALIFIED**
The architecture claim and the P(Overwhelming) formula are confirmed exactly: probe reproduces 5.7%→~6% at pool 6 and 72.6%→~73% at pool 19 using combat's actual bar (`net≥2·ob-0.5`, ob=`DECISIVE_OB=3`, core.py:25,43) vs contest's pool-aware `MU_PER_DIE·pool+OVERWHELM_SIGMA·SD_PER_DIE·√pool` (`sigma_leverage.py:307`) — the described mechanism is real. **But the "pool sweeps 6→19 with History" premise is refuted:** `designs/scene/combat_engine_v1/phase4_5_plan_v1.md:58` states the 9 combat character stats including `history`, "each 1–7" — this is corroborated by `workbench/balance.py:43,97` (History base=3, marginal sweep ±1, never near 13) and by `combatant.py:115`/`wrapper.py:101,184` (pool = `history+6` with no bonus stacking anywhere). History=13 (needed for pool=19) exceeds the documented 1–7 cap by nearly 2×. At the true ceiling (History=7→pool=13), P(Overwhelming) at parity is **45.9%**, not ~73% — I reproduced this directly. The saturation trend is real but its magnitude is overstated by the unreachable pool=19 endpoint.

**Claim 3 — Armature doc's `SL.p_success` idealization vs shipped kernels**
**VERDICT: CONFIRMED**
Evidence: `engine/sigma_leverage_engine_armature.md:123` worked example (Pool 6D, base Ob 2, net_σ=+1.50) → doc claims 91.1%. Probe: `SL.p_success(2,6,1.5,tn=7)` = 0.910932 (91.09%, matches). Computing the SAME scenario through the shipped kernel's ER-2-corrected banding threshold (`base_ob-0.5`) gives 0.945386 (94.54%). **Divergence = 3.445pp**, matching the claimed "~3.45pp" almost to the third decimal. Caller census: `grep -rl p_success` finds only `engine/engine_audit_harness.py` (CLAUDE.md §8/§6: confirmed dead, hardcoded `/home/claude` paths), files under `tests/sim/` (confirmed by `sim/README.md:13` to be "a frozen archive of historical sim-run output"), and `sim/tests/test_sigma_leverage_parity.py` (a parity unit test, not a banding path). Neither `combat_engine_v1/core.py` nor `sim/personal/contest/resolver.py` import or call `p_success` — grepped, zero hits.
Intent gate: **NOT-INTENDED** (inferred from timeline). The armature doc is dated 2026-05-30; ER-2 landed 2026-06-22 (`skills/valoria-resolution-diagnostic/SKILL.md:343`) — the doc predates the correction and was never revised to reconcile its idealization proofs against it.

**Claim 4 — OVERWHELM_SIGMA=0.85 uniformity is continuous-only; discrete sawtooths**
**VERDICT: CONFIRMED**
Evidence: `sim/autoload/sigma_leverage.py:104` (`OVERWHELM_SIGMA=0.85`); contest's actual roll is discrete (`resolver.py:26-30` wraps `SL.roll_net`→`dice_engine.roll_pool`, integer net). Analytic check: under the continuous idealization the z-score at threshold reduces to exactly `OVERWHELM_SIGMA=0.85` for **every** N (σ√N cancels), giving a constant 19.77% at all pool sizes — verified by direct computation. Discrete convolution probe (exact PMF via per-die outcome DP, faces {-1:.1, 0:.5, 1:.3, 2:.1}) across N=5…30: P(Overwhelming) oscillates between **14.19% (N=6) and 23.40% (N=22)** — a ~14–23% sawtooth, matching the claim almost exactly.

**Claim 5 — Domain resolver's dual difficulty derivations and Ob≥8 floor clamp**
**VERDICT: QUALIFIED**
Verified against the actual sim file (`designs/audit/2026-05-28-resolution-diagnostic/sim_domain_resolver.py:59-66`, constants match the spec exactly: BASE=.50, SLOPE=.10, FLOOR=.05, CAP=.90). Algebraically, `D_ob=(⌊stat/2⌋+1-1)·2 = 2·⌊stat/2⌋`, which equals `stat-1` for odd stat and `stat` for even — so the two derivations' raw difficulty differs by exactly 1 point for every odd target stat, translating to exactly 10pp via SLOPE=0.10. **Confirmed as a structural/pre-clamp fact.** However, full-grid computation (acting×target ∈ 1-7²) shows this 10pp gap is **not universal after clamping**: at extremes (e.g. target=7,acting=1; target=1,acting=5/6/7) both derivations clamp to the same FLOOR/CAP value, producing 0pp or 5pp divergence instead of 10pp in ~6 of 28 odd-target cells. Second part fully **CONFIRMED exactly**: at Ob≥8 (D≥14), every bare stat 1-7 clamps to FLOOR=0.05 flat — reproduced for Ob=8,9,10 across all acting stats 1-7, all give raw values ≤0 → clamp to exactly 0.05.

**Claim 6 — NERS Ω(c) "autonomous world, verified under attack" PASS contradicted operationally**
**VERDICT: CONFIRMED**
Evidence: PASS text at `designs/audit/2026-07-04-ners-qualitative-audit/ners_qualitative_audit_v1.md:37` ("Clocks + deterministic priority trees + neglect-driven insurgency genuinely tick without the player"). `sim/autoload/npc_ai.py:22-27` — both entry points (`select_action`, `evaluate_priority_stack`, i.e. exactly "NPC priority trees" per `sim/autoload/__init__.py:11`) are bare `raise NotImplementedError`. `sim/provincial/faction_action.py:51-52` self-documents: "GD-2: mandatory threat-response before stochastic selection. Currently simplified: probabilistic mix per v17." `sim/personal/contest/faction.py:20,42` — `fixed_lean` field defined and read (`if voter.fixed_lean: return voter.fixed_lean`) but grep across the entire repo finds **zero** assignments to a non-default value anywhere. `sim/tests/test_mc_v18_regression.py:38` pins `GOLDEN_WIN_SHARE = {'Crown':50.0,'Church':50.0,'Hafenmark':0.0,'Varfell':0.0}`; its `test_mc_v18_win_share_is_well_formed` (lines 62-68) only checks key-set match, bounds [0,100], and sum=100 — a hypothetical 100/0/0/0 distribution passes all three assertions trivially. Chain confirmed: `sim/mc_v18.py:34,62` calls `faction_take_action` (the admittedly-simplified module) to drive the ticking campaign; the regression test's own docstring states "the happy path does not touch any of sim's NotImplementedError stubs" — i.e. `npc_ai.py` is provably not exercised by the very system the PASS calls "genuinely ticking."
Intent gate: **DELIBERATE** (for the underlying stubs/simplification themselves — `npc_ai.py:6` self-labels "[PROVISIONAL — Pass 2l armature stub... audit pending before content authoring]"; `faction_action.py` self-labels "Currently simplified") — but the NERS audit's own PASS verdict citing this as "verified under attack" is **NOT-INTENDED overreach**: the self-documented incompleteness was sitting in the code comments and should have been weighed against the PASS.

**Claim 7 — ED-PC-0001's circular "DELIBERATE sim-first staging" + phase4_5 plan gap**
**VERDICT: CONFIRMED**
Evidence: `canon/editorial_ledger.jsonl:578` (ED-PC-0001) — "wrapper.engagement() has no player-decision parameter (verified; DELIBERATE sim-first staging per audit refutation R-1)... audit: .../ed_options.md E-9 (audit R-1...)". R-1's actual text, `designs/audit/2026-07-04-ners-qualitative-audit/ners_qualitative_audit_v1.md:297`: "DELIBERATE staging (sim-first oracle; player interface is Phase-4-class work)" — no external citation given. Critically, the original **producer** dossier (`01_workings/dossiers/dossier_personal_combat.json`, finding `pc-no-player-input-hook`) makes **no "deliberate" claim at all** — it frames the gap as a plain defect ("works against a North Star built on myriad player decisions"). The "DELIBERATE" framing was introduced only at the refutation-summary stage, self-referenced by the very ledger entry the same audit filed — a closed loop with no independent grounding. Second half: grepped `designs/scene/combat_engine_v1/phase4_5_plan_v1.md` (326 lines) for "player"/"input"/"decision layer"/"UI"/"interface" (case-insensitive) — **zero matches** anywhere in the file the ledger cites as where this work should align.
Intent gate: the correct classification for "combat's missing player-input layer" is **UNDETERMINED**, not DELIBERATE as currently ledgered — no design doc or Jordan decision establishes actual staged intent; only the audit's own circular self-citation does.

**Claim 8 — NERS §7 published DELIBERATE vs its own refutation record's UNDETERMINED**
**VERDICT: CONFIRMED**
Evidence: published reconciliation table, `ners_qualitative_audit_v1.md:296`: "R-2 | Articulation is thread-blind → token absence true; the layer is deliberately Key-type-agnostic (wildcard + salience)... a policy decision, not a bug." The cited underlying record, `01_workings/refutations/refute_articulation-zero-thread.md` (read in full per exception), states explicitly at line 56: "Intent = UNDETERMINED (not a documented deliberate exclusion; also not a proven accident)" and repeats at its final Verdict line 74: "Intent UNDETERMINED." Both discuss the identical subject (ED-681's four Rendering-Crisis beats, the articulation Tier-2 trigger absence) — the published summary conflates a genuinely-deliberate architectural fact (universal wildcard Key-subscription, `module_contracts.yaml` `consumes:{type:"*"}`) with the specific, separately-adjudicated question of whether the *missing thread-specific trigger* was deliberate, which the refuter left UNDETERMINED.
Intent gate: this is a reporting/synthesis defect, **NOT-INTENDED** — the orchestrator's condensation of the refutation into the §7 table lost the refuter's explicit UNDETERMINED qualifier.

---

**Summary:** 6 CONFIRMED (1, 3, 4, 6, 7, 8), 2 QUALIFIED (2, 5) — no outright REFUTED, but claims 2 and 5 each contain a numerically-refuted sub-component (unreachable pool=19 in claim 2; non-universal 10pp divergence under clamping in claim 5) that should be corrected before these are reported as clean findings.

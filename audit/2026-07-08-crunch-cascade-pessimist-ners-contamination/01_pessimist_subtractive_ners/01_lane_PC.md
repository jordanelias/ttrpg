# Pessimist Subtractive NERS — Personal / Scene Combat (PC)

> ⚠️ **CURRENCY NOTE:** `464fa89` (#103, same day) **deferred ED-PC-0007** — the item this report's
> closing "New finding from adversarial pass" section cites as live PC action-set consolidation.
> Jordan's ruling: the verdicts judged the stale discrete `combat_v30 §4` menu, not the canonical
> `combat_engine_v1/`; the consolidations were already realized / moot under the continuous design,
> not live work. Read that section as historical, not actionable. See `../00_workplan.md`'s
> disclosure for further same-day PC commits (`c55bdde`, `83bc08e`) not reflected below.

# Personal / Scene Combat (PC) — Pessimist Subtractive NERS, Inverted-Critic Final Table

**Critic's method note:** I independently re-read `01_findings_personal_combat.md` and re-verified essentially every citation against the working tree (`core.py`, `config.py`, `systems.py`, `combatant.py`, `wrapper.py`, `ability_primitives.py`, `state_graph.py`, the `workbench/` tools, `sim/personal/combat.py`, `references/values_master.yaml`, `references/engine_params/combat_engine_v1.json`, `designs/scene/derived_stats_v30.md`, `params/core.md`, `references/names_index.yaml`/`glossary.md`, `skills/valoria-resolution-diagnostic/SKILL.md`, `references/throughlines_meta.md` §8.2-A, and the `canon/editorial_ledger.jsonl` entries cited). Every line citation checked out exactly as claimed — I did not find a single fabricated or mis-cited reference. Given that, my adversarial attempts to save the condemned elements mostly failed to dislodge the dossier's verdicts; where I found the dossier was *too lenient in its own favor* (i.e., understated how dead something actually is) or slightly mis-framed a mechanism, I note it below as STRENGTHENED or corrected-reasoning rather than a tier change. **No verdict overturned.**

---

## Per-element table

| # | Element | N/Ω/Q reasoning (critic-verified) | Subtractive verdict | NERS engine verdict | Severity | Calibration | Downstream retirement | Critic disposition |
|---|---|---|---|---|---|---|---|---|
| 1 | Combat Pool `max(5,History+6)` (`combatant.py:117`, `core.py:27-32`) | Verified verbatim. N pass — real Instance-A sizing input, ratified ED-900/901/904 (ledger text confirmed: "max(5, History+6)... Agility-INDEPENDENT"). Ω/Q pass — one-read formula, in-scope. | **KEEP** | Instance-A pool sizing, in-scope (≥5D) | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 2 | Health `round(WI×(MW+1)+0.25×Str×End)` (`combatant.py:40-42`) | Verified verbatim (`STR_HEALTH_W=0.25` at line 21). N pass — real survivability resource; Q pass — derived, not a live weighed decision. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 3 | Wound-Ob penalty (+0.15 atk/+0.25 def; `config.py:65`, applied `systems.py:664,693,805`) | Verified all three application sites verbatim, incl. `bind_sigma` (664), `assemble_net_sigma` (693), `stophit_sigma` (805). Checked the workbench's own human-readable output (`workbench/narrate.py`, `commentary.py`): it prints wound **counts** ("A 2 / B 1 wounds") but never the Ob-shift value itself — confirming the gap is real even in the one place combat state is narrated for a human. Also found `core.effective_ob()`/`SL.eff_ob` is an existing **display-only** primitive already in the codebase, explicitly *not* used for resolution (core.py:48-51 docstring) and not called anywhere for display either — a ready-made, currently-unused hook for exactly the acceptance criterion the dossier wants. The typed export `references/engine_params/combat_engine_v1.json:201-202` now carries `WOUND_ATK_OB`/`WOUND_DEF_OB` in structured form, so the *constant* is Godot-portable — but nothing commits to showing the *computed* per-fight value pre-commit. Note: the cited "every number the resolver uses is visible pre-commit" standard is sourced from the sibling Social Contest lane's `player_interaction_walkthrough_v1.md:19`, applied by analogy to PC, not a PC-specific ratified requirement — a reasonable audit judgment call, but worth flagging as an extension rather than an existing PC-scoped mandate. | **REFINE** | P-ii PASS (Δσ before σ_N-scaling); **P-i FAIL** (no legible-odds commitment) | P2 | KNOWN-TRACKED (mechanism) / KNOWN-UNTRACKED (display) | Folds into ED-PC-0001 acceptance criteria | **SURVIVED, STRENGTHENED** — additional evidence that the gap is real (narrate/commentary check) and that remediation is cheap (an unused display-only primitive already exists) |
| 4 | ~15 continuous internal-only per-beat fields (`combatant.py:102-113`, `wrapper.py`) | Verified the field list against `combatant.py` (grip_position, lunge_depth, sel_head/dmg/gap/perc/pc, range_avail, facing) and `wrapper.py` locals (measure_gap, closed, ready, opening_created — lines 38,44,75,84,93-94,309). Some fields are explicitly tagged "scaffold... default to IDEAL... byte-identical until a later increment" (combatant.py:106-108) — genuinely engine-only-by-design for now; others (grip_position, initiative, poise) are plausible future HUD candidates. The bundling of both classes under one "forever-hidden" claim is a real clarity gap in the finding's own framing. | **REFINE** | N/A (roll inputs; Scope Gate) | P2 | NEW | Partition list pre-empts ED-PC-0001's translation-layer authoring | **SURVIVED** |
| 5 | Momentum (0–4 universal tracker, `params/core.md:84-93`) — zero read/write sites in `combat_engine_v1` | Re-grepped independently: zero hits for "momentum" as a tracker anywhere in `combat_engine_v1/*.py` (only an unrelated physics comment, `core.py:110`, "momentum-like"). Confirmed `params/core.md:84-93` spec is discrete-d10 vocabulary ("1 Momentum = 1 automatic success (non-Thread rolls only)") with zero continuous-engine (Normal(μN,σ√N)) translation defined anywhere. Confirmed the "flat-shift trap" citation is a real, precisely-stated methodology finding (`skills/valoria-resolution-diagnostic/SKILL.md:87`): a flat `+X` bonus scales as `1/√Pool`, reimporting the exact non-uniformity the engine exists to kill. So a naive port would be a real, provable P-ii regression, not a hypothetical. | **REFINE** | **[NEW]** Momentum-as-specced is Instance-C-adjacent vs Instance A; naive port reimports the flat-shift defect | P2 | KNOWN-UNTRACKED (absence)/NEW (mismatch) | Pre-specifies the σ_N-scaled entry point for `module_contracts.yaml` closure | **SURVIVED**, evidence strengthened (flat-shift-trap citation independently confirmed as real, quantified methodology, not just an analogy) |
| 6 | `disp` (combat aggression axis, `combatant.py:99`) collides with NPC Disposition (`params/core.md:147`) | Verified both: `combatant.py:99` "disposition/temperament, aggression axis (1-7, 4=neutral)" vs `params/core.md:147` NPC Disposition (−5..+5, ED-912). Checked `references/names_index.yaml` and `references/glossary.md` — **zero entries for combat's `disp`/"temperament"** anywhere (glossary.md:160 only covers the NPC sense). This repo has independent, demonstrated history of naming collisions mattering enough to need CI enforcement (CLAUDE.md's Solmund/Galbados naming gate) — so a live, uncaught cross-lane collision of this kind is a real, non-hypothetical risk, not pedantry. | **REFINE** | N/A (naming) | P2 | KNOWN-UNTRACKED | names_index.yaml/glossary.md entry | **SURVIVED** |
| 7 | grip_position 2-cycle (`systems.py:172-189`, JD-9) | Verified verbatim — the docstring itself documents the exact failure mode ("spear flips 0<->0.865 every beat, forever") and states the fix is a **fixed-grip read used ONLY here**, not a general rule. Confirmed via `skills/valoria-resolution-diagnostic/SKILL.md:210` (Phase-4 damper+cap AND-test) that the pre-fix state is a textbook double-failure (undamped AND unbounded), and via SKILL.md:215 (Phase 5: "Deliberate without [adequate safeguard] → true finding") that the post-fix state is precisely the "true finding" case, not a pass. | **REFINE** | Pre-fix: **FAIL** (undamped+unbounded); post-fix: **R-residual** (Phase-5 true finding) | P2 | KNOWN-TRACKED (fix)/KNOWN-UNTRACKED (invariant) | Stated general invariant for R3 | **SURVIVED**, Phase-4/5 citations independently confirmed as exact methodology matches, not loose analogy |
| 8 | Initiative + Poise loops (`config.py:121,128`; `systems.py:745-747,764-773,776-785`) | Verified the constants and clamp functions, **and independently traced every mutation site in `wrapper.py`** (initiative: lines 41,56-57,60-61,181-182,192,220-221,269-270,280-281,320,324; poise: lines 26,63-64,193,271,283,317,319,322) — every single one routes through `clamp_initiative`/`clamp_poise`, and the per-beat decay (`init_hold_decay`) / regen (`poise_regen`) run every beat before any gain is applied. This is a clean, exhaustive AND-test pass, not a spot-check. | **KEEP** | **PASS all four** (damper+cap present at every mutation site, not just declared in config) | P3 | KNOWN-TRACKED | — | **SURVIVED, STRENGTHENED** — full mutation-site audit, not just the constants, confirms the clean-loop claim more thoroughly than the dossier itself established |
| 9 | Concentration (floored 0; drains within an engagement, `wrapper.py:362` recovers only between bouts) | Verified: within `engagement()` (up to `soft*3=24` beats, confirmed `wrapper.py:44,50`), Concentration only drains (`CONC_DRAIN_HIT` at lines 108,267,290,299; `CONC_DRAIN_LOSS` at 301); the only recovery term is at line 362, inside `fight()`'s between-engagement loop. **Correction to the dossier's framing:** this is *not* actually an unbounded/robustness risk in the Phase-4 loop-defect sense — it's a monotonically-decreasing resource with a hard floor (0) and a hard ceiling (`conc_max`, never exceeded since it only subtracts), so it cannot runaway or cascade the way grip_position did. It does feed back into `attack_sigma` via `consistency()` (`systems.py:824-826`, confirmed live at `wrapper.py:151,171`), creating a bounded "losing tilts you further" effect — a legitimate design question (deliberate attrition/death-spiral clock, or oversight?) but not the same robustness category as item 7. The REFINE verdict is still right, just for "undetermined design intent," not "half-damped Phase-4 case." | **REFINE** | Recognized-not-diagnosed as a rolling engine (Scope Gate); bounded, not a loop-safety risk — corrected from dossier's "half-damped Phase 4/5" framing | P2 | NEW | Resolves Phase-5 `[INTENT UNDETERMINED]` before UI work | **SURVIVED, reasoning corrected** — verdict and severity stand, but the justification is "design-intent clarity," not a robustness/cascade risk |
| 10 | `seize` ability channel (vorschlag, sen_no_sen; `ability_primitives.py:15`) | Verified the module's docstring ("the 'seize' lever is DEAD... slated for retire-or-reroute"). **Critic finding beyond the dossier:** the live `ABILITIES` dict (`ability_primitives.py:19-36`) contains **zero entries with `lever='seize'`** at all — `vorschlag`/`sen_no_sen` are not merely inert, they cannot currently even be equipped; grep confirms they exist only as prose in `.md` design docs (`ability_armature.md`, `tradition_decomposition_v1.md`, `REARCHITECTURE_v1.md`), some of which (`ability_armature.md:53,108,133,169`) still mark them "live" with a ✅, contradicted by that same file's own "STATUS CORRECTION" note at line 47. So there is no dead code binding left in `ability_primitives.py` to prune — the actual stale artifact is the "live"-marked rows in `ability_armature.md`/`tradition_decomposition_v1.md`. | **PRUNE** — retarget the downstream action from "ability_primitives.py" to the stale doc tables; names still reroutable to a live channel (counter_select/init_steal) per WS-4 | N/A (ablation-verified ~0, duplicate coverage vs. standing initiative substrate — matches `throughlines_meta.md` §8.2-A's explicit "duplicate coverage → PRUNE (re-home)" trigger) | P2 | KNOWN-TRACKED (WS-4) | ability_armature.md / tradition_decomposition_v1.md stale-row correction (corrected from ability_primitives.py) | **SURVIVED, STRENGTHENED** — code is more thoroughly dead than the dossier stated, but the recommended cleanup site needs correcting to the docs, not the (already-clean) code |
| 11 | Skills `c.skill(axis)` (~7 reads: `systems.py:117,213,218,223,225,654×2`) | Verified all 6 call-sites (7 expressions, since line 654 reads `skill('bind')` twice) and the definition `combatant.py:150` (`return self.skills.get(axis, 0.0)`). Searched `params/`, `designs/scene/`, and `module_contracts.yaml` for any skill acquisition/budget/allocation rule — none found. | **REFINE** | Preventive note: current Δσ-routing (P-ii) is safe; any acquisition rule must preserve it | P2 | KNOWN-UNTRACKED | Retires the `[ASSUMPTION]`-grade gap in module-contract closure | **SURVIVED** |
| 12 | Zero in-fight player decisions (`wrapper.py:28-371`; ED-PC-0001) | Verified `wrapper.engagement()`/`fight()` in full — no branch reads a player-supplied choice; all resolve via `rng` against derived σ. Independently pulled the **exact ledger text** for ED-PC-0001: "RATIFIED-AS-ACCEPTED by Jordan 2026-07-05" confirming deliberate sim-first staging, plus ED-IN-0024's addendum flagging the intent-provenance as circular (verified verbatim in `canon/editorial_ledger.jsonl`) without retracting the staging fact. Per the Cardinal Rule (`00_charter.md:17`, confirmed verbatim: "judge as-if-built, never by build state"), this is not a design-merit failure. | **KEEP** | Not itself an engine-math defect; dependent components (1,3,8,11) are independently sound | P1 (matches source) | KNOWN-TRACKED (ED-PC-0001, ED-IN-0024) | — | **SURVIVED, STRENGTHENED** — ledger text confirms both the ratification and the honest circularity caveat exactly as claimed |
| 13 | `sim/personal/combat.py` struck Combat Pool formula, `[DEPRECATED 2026-06-23]` (`:1-10,47-48,127`) | Verified the banner verbatim: "do NOT wire new game code through this file... reference/history only." | **MERGE** (with 14) | N/A | P2 | KNOWN-TRACKED | Combined relocation/CI-fencing task | **SURVIVED** — matches `throughlines_meta.md` §8.2-A's own "duplicate coverage → MERGE (in-lane)" trigger precisely |
| 14 | `values_master.yaml` struck formula + reversed wound rule (`:213-215,327-329,1150-1163`) | Verified all three line ranges verbatim (old `(Agility×2)+History+3` formula twice, old `-1D pool` wound rule). Verified the quarantine banner (`values_master.yaml:7-18`, ED-1084) and independently pulled ED-PC-0005's full ledger text — confirms the wound rule was **reversed again today** (2026-07-08), exactly as claimed, and confirms the file is stale on the very day of the audit. | **MERGE** (with 13) | N/A | P2 | KNOWN-TRACKED | Same combined task; argues for a CI trip-wire over a banner | **SURVIVED**, and the "banner-without-enforcement is proven not to hold" point is now doubly evidenced (I independently pulled the ledger text showing same-day recurrence) | 
| 15 | Jargon with no player-facing translation (`wield_heft`, `gap_precision`, `point_concentration`, `recoverability_factor`, `PoB_frac`, `close_unwieldiness`, `adef_cap`, `strike_concentration`, `sel_head/dmg/gap/perc/pc`) | Verified all 8 named terms are real, live identifiers used across `combatant.py`/`config.py`/`core.py`/`geometry.py`/`systems.py`/`weapon_physics.py`/`weapons.py`/`wrapper.py` — not strawmen. Matches `throughlines_meta.md` §8.2-A's DISTILL definition ("abstractable, folds into a general rule, no lost decision") exactly. | **DISTILL** | N/A | P2 | KNOWN-TRACKED | 9-term ready-made checklist for the translation-layer pass | **SURVIVED** |
| 16 | `derived_stats_v30.md` self-contradicts currency (`:2` CANONICAL, `:4` PROPOSAL) | Verified verbatim, lines 2 and 4 back-to-back. | **REFINE** | N/A | P3 | KNOWN-UNTRACKED | One-line edit | **SURVIVED** |
| 17 | Stale comment, `state_graph.py:42` ("Contact — BUILT, not activated") | Verified the comment, and independently traced `wrapper.py:309-324` — confirmed Contact is genuinely live (`CT.grab_available`/`grab_sigma`/`grab_outcome` called and branched on every beat where an opening exists). Comment is stale. | **REFINE** | N/A | P3 | KNOWN-TRACKED | One-line comment fix | **SURVIVED** |
| 18 | PC action set mid-consolidation (ED-PC-0007) | Pulled the exact ledger text for ED-PC-0007 — confirms the net −5 resolver-row consolidation described, ratified 2026-07-08 (today), citing ED-IN-0027. | **N/A — process note** | N/A | P3 (informational) | KNOWN-TRACKED | Self-tracked (re-audit after R3 + consolidation) | **SURVIVED** |

---

## Rolling-engine verdict (Instance A) — critic pass

Re-derived independently rather than trusting the dossier's framing: traced every channel (Combat Pool sizing, wound-Ob, skill(axis), initiative, poise, grip_position, `degree()`'s continuity correction) into `core.resolve()`'s actual arithmetic (`net = roll_net(pool,rng) + soft_cap(net_sigma)*sigma_n(pool)`, `core.py:52`). Confirmed:
- **N/S PASS** — every channel is a genuine, non-redundant Δσ contributor, and all compose through the same pipeline without special-casing.
- **R CONDITIONAL PASS** — leverage-uniformity holds by construction (Δσ before σ_N-scaling); the continuity correction is present and correctly cited (`core.py:35-45`, ER-2). The one confirmed residual: grip_position's pre-fix state was a textbook Phase-4 double-failure (undamped+unbounded, verified against the SKILL.md's own AND-test), and its post-fix state is a genuine Phase-5 "true finding" (deliberate fix, no general safeguard) by the methodology's own explicit rule, not the dossier's paraphrase of it.
- **E CONDITIONAL PASS** — the core formula is genuinely one-read-restatable; the Ob-shifting channels' *lack of a display contract* is real (independently confirmed against the workbench narrator) but partially cheap to fix given the already-existing, currently-unused `SL.eff_ob` display-only primitive.

This top-level verdict **survives** the critic pass essentially unchanged, with the two residual items (grip-cycle general invariant, wound-Ob display contract) both independently re-confirmed rather than merely re-asserted.

---

## Per-lane rollup (corrected)

No verdict changed tier under adversarial attack — every citation in the dossier checked out against the working tree, and in several cases (Initiative/Poise's full mutation-site audit, the `seize` channel's complete absence from the live `ABILITIES` dict, ED-PC-0001/ED-PC-0005/ED-PC-0007's exact ledger text) the independent verification found the underlying evidence to be *stronger* than the dossier's own citations established. Two corrections of substance: (1) **Concentration (#9)** is not actually a Phase-4 loop-safety risk — it is a bounded, self-limiting drain, not an undamped/uncapped cascade like grip_position; the REFINE verdict survives, but on "undetermined design intent" grounds rather than the dossier's "half-damped Phase 4/5" framing. (2) **`seize` (#10)** is more completely dead than described — there is no live code binding left to remove at all (`ABILITIES` carries zero `lever='seize'` entries), so the PRUNE's downstream-retirement target should point at the stale "live"-marked rows in `ability_armature.md`/`tradition_decomposition_v1.md`, not at `ability_primitives.py` (which is already clean). The lane's dominant pattern is confirmed exactly as the dossier described: **legibility-contract debt, not junk** — the sigma-leverage substrate is N/R/S/E-sound everywhere it has been built (Combat Pool, Health, Initiative/Poise all cleanly KEEP), and every subtractive verdict targets a missing display/specification/naming commitment (wound-Ob, jargon, Momentum's engine translation, skills' acquisition rule, Concentration's intent, disp's collision) or a genuinely duplicate/dead consumer (`seize`), never a mechanism that shouldn't exist. The grip_position residual and the same-day-recurring struck-formula grep-hazards (items 13/14) are the two places where the critic pass found the underlying risk *more*, not less, evidenced than the dossier's own citations.

**18 elements: 4 KEEP / 10 REFINE / 1 DISTILL / 2 MERGE / 1 PRUNE / 0 CUT (post-critic).**

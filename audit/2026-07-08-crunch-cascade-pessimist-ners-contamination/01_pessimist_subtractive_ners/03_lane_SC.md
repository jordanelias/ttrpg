# Pessimist Subtractive NERS — Social Contest (SC)

> ⚠️ **CURRENCY NOTE:** same-day commits `c651282`/`a767e3e`/`fe20170` (#96/#95/#104) wired
> `scene_dispatch` to the promoted contest kernel and landed the consequence spine
> (`ECHO_TRANSPORT` default ON). Where this report notes the `contest_legacy_stub` PRUNE as
> "already Jordan-accepted" (ED-SC-0006/0011), execution may now be complete rather than merely
> accepted — re-verify against current `main` before citing. See `../00_workplan.md`'s disclosure.

# Pessimist Subtractive NERS — Lane: Social Contest (SC) — INVERTED CRITIC PASS

**Source dossier under review:** `01_workings/dossier_SC.md`-equivalent text supplied in-task, checked against `designs/audit/2026-07-08-crunch-cascade-cogload-legibility-audit/01_findings_social_contest.md` and the working tree at `/home/user/ttrpg`. **Method:** every non-KEEP verdict was attacked in good faith (attempt to save the element); a sample of KEEPs was spot-checked. All citations below were independently re-read from disk — none taken on trust from the dossier. **Cardinal rule applied throughout:** build-state is never verdict evidence.

## Headline results of this pass

- **One clean OVERTURN (REFINE → KEEP):** Item 2 (Face/Standing scale-binding). The dossier's own basis — an in-code comment calling this an "OPEN DECISION for Jordan" — is a **stale comment**. The same file (`primitives.py`) and the canonical prose heads (`social_contest_v30.md:249`, `params/contest.md:144`) both carry an explicit **"[FACE SCALE-BINDING — RESOLVED, Gate A, 2026-07-01 (ED-1056)]"** banner with the exact combo formula already implemented as `FaceScale`. There is nothing live left to REFINE.
- **One SOFTENED verdict:** Item 5's leverage-defect half (CR4's flat pool die). The math is real but ~1.84× (not the dossier's ~2.4×) across the 5D–17D range, the flat form is **canonically specified** ("+1D", `params/contest.md §Genre and Orientation Bonus Dice`), and the code's own extensive "judge finding 5" commentary shows the CR4-vs-armature channel split was a **deliberate fix**, not an oversight — migrating CR4 onto δσ risks undoing that fix.
- **One calibration correction (NEW → KNOWN-TRACKED) on the lane's own P1 headline:** Item 13's "new" reachability finding is **not new**. `ED-SC-0006` (ratified-as-accepted by Jordan 2026-07-05) and `ED-SC-0011` (2026-07-07) already state, almost verbatim, that campaign dispatch reaches `contest_legacy_stub.py` and not the promoted kernel. The PRUNE verdict and P1 severity both survive and are, if anything, strengthened (Jordan has already accepted this must be fixed) — but calling it "NEW" is a real miscalibration the dossier should not have made, especially since its own reconciliation intro claims to have read the very docket that refutes "NEW."
- **One sharpened nuance:** Item 12's "REFUTED by same-day landing" is correct on zero-callers/orphaning but overstates practical resolution — `ED-SC-0007` remains **open**, and `ED-IN-0028`'s own ledger text says the plumbing is **"INERT"** in the live loop pending the still-open party-derivation bridge.
- **One sharpened citation:** Item 18's KU-1 figures ("Recall +5D/Prep +3D/Corroborate +1D") mislabel two **pre-existing combined local caps** (genre+audience+MS-targeting+Recall = max +5D per `npc_behavior_v30 §6.5`; Findings+Prep = max +3D) as if they were single named bonuses. The underlying REFINE survives — it is in fact the best-corroborated finding in the lane (now 4 independent audits agree) — but the breakdown needed correcting.
- **Everything else SURVIVED** adversarial attack, several with strengthened evidence (noted inline).

---

## Final per-element table

| # | Element | N/Ω/Q reasoning | Subtractive verdict | NERS engine verdict | Severity | Calib. | Downstream retirement | **Critic disposition** |
|---|---|---|---|---|---|---|---|---|
| 1 | **Persuasion Track** (0–10 banded win-condition, `resolver.py:79-93`) | Confirmed: `class PersuasionTrack(WinCondition)` at exactly resolver.py:79-93, hard-clamped `max(0.0,min(10.0,…))`. Real merits-clock dynamic; feeds Domain Echo. | **KEEP** | Non-rolling (accumulator fed by the roll — route out). | — | KNOWN-TRACKED (Gate A) | N/A | **SURVIVED.** Citation exact; no basis found to attack. |
| 2 | **Face / Standing tracker** (`primitives.py:31-45`; `FaceScale`, ED-1056) | Standing/FaceScale confirmed as described, but the dossier's Q-fail rests on primitives.py's own comment (lines ~88-90: *"an OPEN DECISION for Jordan"*) — **this is stale**. `social_contest_v30.md:249` and `params/contest.md:144` both carry `[FACE SCALE-BINDING — RESOLVED, Gate A, 2026-07-01 (ED-1056)]` with the exact combo formula (`Face_max=Cha×3`; `Face_current=round(Standing/10×Face_max)`), already implemented as `FaceScale` 20 lines below the stale comment in the *same file*. One-read-legible; nothing left open. | **KEEP** (was REFINE) | Non-rolling (route out). | — | P3 (was P2) | KNOWN-TRACKED (ED-1056, **ratified 2026-07-01** — not open) | **Trivial doc-hygiene only:** delete/update `primitives.py`'s stale "OPEN DECISION" comment block (lines ~81-90) to match its own `FaceScale` docstring and the canonical banners. No design work required. | **OVERTURNED (REFINE→KEEP).** The dossier cited an internally-contradicted, out-of-date comment as if it were the live design state; the working tree's own later evidence (same file + both canonical prose heads) refutes it. |
| 3 | **Concentration / Reserve** (`primitives.py:49-56`, MAX=12 [SEED]) | Confirmed: `class Reserve` MAX=12, COST table, `regroup()` refill (`cur=min(max,cur+REGAIN)`). Bounded, non-dominant; kernel-abstract vs v30-surface `(3×Focus)+(2×Spirit)` divergence is explicitly and honestly flagged in-code as an abstraction layer, not a hidden defect. | **KEEP** | Non-rolling — route out. | — | KNOWN-TRACKED | N/A | **SURVIVED.** Spot-checked as a sample KEEP; no attack lands. |
| 4 | **Adjudicator Armature** (4-axis vector, `armature.py`, δσ leverage channel) | Confirmed: `M_MAX = 1.5` soft-cap (`sigma_leverage.py:92`, `tanh`), armature's `dsigma()` routes through `net_boost` (the σ_N-scaled channel), not the rounded pool — extensively and consistently documented across `resolver.py`, `rhetoric.py`, `armature.py` as "judge finding 5." Real, load-bearing, non-dominant, correctly routed. | **KEEP** | **Instance A — PASS** on P-ii and P-iii, confirmed by direct code read (not just citation). | — | KNOWN-TRACKED | N/A | **SURVIVED.** Spot-checked; the strongest-evidenced KEEP in the table. |
| 5 | **Stasis/genre ladder + CR4 pool bonus** (`primitives.py` Stasis 6-rung ladder; `resolver.py:380-386`; `rhetoric.py:203-243`) | Ground-ladder mechanism: real, load-bearing (confirmed 6-rung `Stasis.LADDER`). **UI-gap half survives**: genre/stasis is a live resolver input (`resolver.py:384-385`) with no shown-fields UI element — already the crunch-cascade's own finding. **Leverage half is overstated**: `Pool.size(faculty)=max(5,faculty·2+3)` confirmed → 5D–17D exactly; recomputed `Δz(5D)/Δz(17D) = √(17/5) ≈ 1.84×` (**not ~2.4× as claimed** — arithmetic correction). More importantly, CR4's flat "+1D" is **canonically specified as a whole die** (`params/contest.md §Genre and Orientation Bonus Dice`; `rhetoric.py:203-206` comment: *"The +1D is a POOL bonus — the SAME channel the canonical prose specifies… The ARMATURE bonus is a SEPARATE, continuous δσ-leverage channel — CR6 — not this integer pool die"*). The CR4-vs-armature split is itself the product of an already-adjudicated fix ("judge finding 5": a fractional pool bonus rounds away in `roll_net`'s floor, so continuous alignment advantages were moved OFF the pool and onto δσ — CR4 was deliberately *kept* on the pool because "CR4 is a whole die"). Migrating CR4 onto δσ would reverse that fix, not complete it. | **REFINE** — ground-ladder mechanism KEEPs; (a) UI element for the hidden resolver input, (b) **Jordan-flag only** on CR4's modest (~1.84×) leverage variance — do not migrate to δσ without revisiting judge-finding-5's rationale for keeping it a discrete die. | **Instance A — UI-gap: FAIL on P-i** (confirmed). **Leverage claim (P-ii): real but smaller than stated (~1.84× not ~2.4×), and the specific remedy proposed is contraindicated by the corpus's own prior, deliberate design choice.** | UI-gap: **P2** (unchanged). Leverage: **P3** (down from P2/NEW — Jordan-tuning-tier, like Mode-B's SLOPE/BASE, not a structural defect) | UI-gap: KNOWN-TRACKED (crunch-cascade's own finding). Leverage: **not previously ledgered as a P-ii concern**, but the CR4/armature channel split it touches has been reviewed multiple times (7 "judge findings" cited in-code) | UI element ticket stands. Leverage: **flag for Jordan's awareness only** — no migration ticket. | **SOFTENED.** UI-gap half SURVIVED unchanged. Leverage half survives as a real observation but the dossier's magnitude was wrong (2.4×→1.84×) and its prescribed remedy conflicts with a documented prior design decision the code repeatedly cites as already-settled. |
| 6 | **Composure — RETIRED tracker, still cited** (`primitives.py:167-169`; `throughlines_complete.md:123,194`; `valoria_ui_ux_v4_1.md`) | Confirmed: `RETIRED_TRACKERS = ("Composure",)` at primitives.py:167-169 exactly; `throughlines_complete.md` lines 123 and 194 both cite "Composure" (and also "Piety Track," corroborating item 20) verbatim. **New finding this pass:** `valoria_ui_ux_v4_1.md` has **no** "PARTIALLY SUPERSEDED" banner anywhere (checked: header reads "CANONICAL — structural supplement to v4"; the only "Supersedes:" line refers to v4.md, not itself being superseded) — line 610 still lists "Composure damage" as live UI. An independent 2026-07-04 refutation (`refute_ui-spec-stranded-off-index.md`) already found this doc "stale-CANONICAL… **unflagged** in supersession/ledger." | **PRUNE** (the two stale citations) | N/A | P3 | KNOWN-TRACKED | **Corrected**: strike the 2 `throughlines_complete.md` rows — unchanged. But there is **no existing "PARTIALLY SUPERSEDED" banner in `valoria_ui_ux_v4_1.md` to "reconfirm"** — that claim is false. Instead, add a fresh stale/Composure-retired flag to `valoria_ui_ux_v4_1.md` itself (line ~610), since none exists today. | **STRENGTHENED (verdict survives, evidence corrected).** The referential debt is *larger* than claimed — three stale surfaces, not two-plus-an-existing-banner. |
| 7 | **Doubt Marker** (fully prose-specified, zero kernel state; `params/contest.md:114-121`; ED-1060) | Independently re-grepped `[Dd]oubt` across every `sim/personal/contest/*.py` file (excl. tests): **100% comments/docstrings/flavor text — zero live state fields**, confirmed. Real, non-duplicate dynamic; `params/contest.md:121` shows the churn-axiom dominance risk this closes. Zero kernel state is inadmissible as verdict evidence under CARDINAL RULE. | **KEEP** | Non-rolling (post-roll margin/track modifier). | P2 (ratification-pending) | KNOWN-TRACKED (ED-1060) | N/A | **SURVIVED.** Re-verified the "zero kernel state" claim myself rather than trusting it; it holds exactly. |
| 8 | **CR5 self-Face backfire** (`resolver.py:390-419`; `rhetoric.py:366,413`) | Confirmed: `cr5_self_backfire` at rhetoric.py:413, `CR5_BACKFIRE_MAGNITUDE=2.0` at :366, wired in `resolver.py:404-419` (`if self.armature is not None and self.armature.cr5:`). Grounded in Nyāya *nigrahasthāna*, standing-bounded, composes explicitly with the Doubt Marker per ED-1062 as "the two halves of the full CR5 realization." | **KEEP** | Non-rolling (deterministic consequence of `deg==0`). | — | KNOWN-TRACKED (ED-1062) | N/A | **SURVIVED.** |
| 9 | **General strain→Rattled cascade** (unwired v30 mechanic; `primitives.py:83-90`; `wrapper.py:288`; `HANDOFF_SC.md:108`) | Confirmed independently: `params/contest.md:146` itself states *"the general strain ≥ Face → Rattled threshold-and-mark mechanic… remains a v30-surface spec not yet realized in code."* `HANDOFF_SC.md:108` confirms `ED-SC-0009` lists this as a Stage-4 entry criterion. As-if-built this is the missing cost-side of Face that makes the Face/Rattled pairing non-dominant; the "not wired" fact is routing metadata only. | **KEEP** | Damper (pool floor) + cap (2-mark incapacitation) both specified → passes Lesson 5 once realized. | P2 (design-completion urgency) | KNOWN-TRACKED (ED-SC-0009) | N/A | **SURVIVED,** and strengthened: the params doc itself corroborates the unwired-cascade claim in its own prose, independent of the code grep. |
| 10 | **Chain Contests hard-stop-after-3** (`social_contest_v30.md:354-369`) | Confirmed exactly: `§6.3 Chain Contests`, line 369: *"Maximum chain length: 3 contests… cold equilibrium… Disposition freezes… Neither party can initiate a contest on this topic for 4 seasons."* Real deadlock-breaking convention; non-dominance safeguard. | **KEEP** | **Instance A loop-governance — PASS**: damper + cap both present. | — | KNOWN-TRACKED | N/A | **SURVIVED.** |
| 11 | **parliamentary_vote.py / stay / transfer chain** (dead-end one hop further out) | Independently re-traced the call graph: `parliamentary_stay.py`/`parliamentary_transfer.py` are the only callers of `run_parliamentary_vote`, and **neither has any caller anywhere in `sim/`** except `sim/tests/test_f7_smoke_oracle.py`, which explicitly documents (lines 24-26): *"the only restoration path, parliamentary_transfer, is never called… KNOWN-TRACKED via ED-FA-0005."* `mc_v18.py` confirmed to never import or call any of the three. Real cross-scale bridge design, not a duplicate. | **KEEP** | **Instance A (aggregated Mandate-pool)** — same flat `+1D`×2 pattern confirmed at `parliamentary_vote.py:161-164` (`BG_VOTE_GENRE_BONUS`, `BG_VOTE_AUDIENCE_BONUS`, both canon-cited "+1D"). Unlike item 5, there is **no existing armature/δσ scaffold at faction scale** to migrate onto — building one would be new apparatus a P3-tier fix shouldn't add. | P2 | KNOWN-TRACKED — and independently confirmed via `ED-SC-0007`'s own text: *"grep confirms mc_v18 never reaches parliamentary_stay/transfer/vote."* | N/A (KEEP) | **SURVIVED,** strengthened via test-file corroboration (`test_f7_smoke_oracle.py`) and the ledger. One correction: the dossier's remediation ("fold into the same CR4 fix pass") glosses over that there's no δσ scaffold here to reuse — any fix would be net-new apparatus, reinforcing why P3/low-urgency is right. |
| 12 | **domain_echo/Chronicle orphaning claim** | Confirmed `echo_transport.py` exists and `scene_dispatch.py:105,117-125` now routes through it — `domain_echo.compute_domain_echo` is no longer a zero-caller island. **But** `ED-SC-0007` (the ticket covering "wire contest outcomes into the world") is still **`status: open`**, and `ED-IN-0028`'s own ledger text says: *"in the LIVE loop every scene still DEFERS and the plumbing is INERT (KeyLog born empty-but-deterministic, sha256 of '')… until [the party-derivation bridge] lands."* The orphaning-as-zero-callers claim is refuted; functional resolution is not. | **KEEP** | Non-rolling — route out. | — (resolved-as-not-orphaned, but not resolved-as-live) | KNOWN-TRACKED, **partially** closed | N/A | **SURVIVED with sharpened nuance.** "REFUTED by same-day landing" overstates it — correct the claim to "no-longer-orphaned; still inert-in-the-live-loop, tracked under still-open ED-SC-0007 / ED-SC-0006 item 2." |
| 13 | **Contest-dispatch reachability: `contest_legacy_stub.py` is what campaign play actually calls** | Independently confirmed via direct read of `scene_dispatch.py:105-106` (`import sim.personal.contest as contest; cr = contest.run_contest(...)`) and `sim/personal/contest/__init__.py:33-50` (re-exports `run_contest` etc. from `contest_legacy_stub`, whose own header calls itself "DEPRECATED... a 256-line single-compare stub"). The stub genuinely models strictly less than the 13-module kernel (no Stasis/Face/CR5/armature). **However — this is NOT a new finding.** `ED-SC-0006` (ratified-as-accepted by Jordan 2026-07-05, Fable-5 audit finding N-1) states almost verbatim: *"route scene_dispatch contest resolution to the promoted kernel API… retiring the deprecated contest_legacy_stub path (the kernel currently has ZERO non-test callers; the only production call site, scene_dispatch.py:105-108, reaches the stub)."* `ED-SC-0011` (2026-07-07) reiterates the identical point. Both predate this audit by 1-3 days and are cited in `HANDOFF_SC.md:96,103`, which the dossier's own reconciliation intro claims to have read. | **PRUNE** (the stub's own resolution logic; retain symbols mid-transition) | **Instance A** (healthy dice roll, TN7) — redundant, capability-poor duplicate. | **P1** — survives; this is genuinely the lane's biggest live gap. | **NEW → corrected to KNOWN-TRACKED (ED-SC-0006, ED-SC-0007, ED-SC-0011 — accepted by Jordan 2026-07-05/07)** | Retire `contest_legacy_stub.py` per ED-SC-0006 scope-item (1), already the accepted plan — not a new proposal. | **SURVIVED (verdict + severity), calibration OVERTURNED.** The dossier's "NEW (2026-07-08 adversarial pass)" framing — inherited from the source findings file's "the single biggest reachability gap… the producer missed entirely" — is factually wrong: this exact gap was ratified as an accepted work item three days earlier. This does not weaken the finding (Jordan already agrees it must be fixed) but the novelty framing must be corrected; it is the clearest miscalibration in the dossier. |
| 14 | **GAMES router — only Agôn wired** (`wrapper.py:199-215`; `modes.py:328-334`) | Confirmed exactly: `_stub()`/`NotImplementedError` at wrapper.py:199-201, `GAMES` dict 206-215; `modes.py` raises `NotImplementedError` at lines 328/331/334 for Consensus/Negotiation/ceremonial. Four genuinely distinct dynamics, not duplicate coverage. The dossier's precedent citation checked out exactly: `pessimist_action_audit_v1.md:36` reads verbatim *"The four-deliberative-games meta-choice was **overturned to KEEP** — the REFINE/P1 reading was functionally judging pre-Stage-4 spec completeness, a cardinal-rule violation."* | **KEEP** | [NEW ENGINE — insufficient spec for the 3 stubs] | Build-state P1 correctly declined as verdict evidence | KNOWN-TRACKED (ED-SC-0012) | N/A | **SURVIVED.** Precedent citation verified verbatim — this is the strongest-cited KEEP in the table. |
| 15 | **Aggregate 13-item cognitive-load ceiling violation** | Confirmed: the Fable-5 dossier (`fable5_social_contest_audit_v1.md:18`) states *"one exchange demands ~13 per-decision consults against a 3–4 ceiling"*; line 180 references *"the immersion audit's 3–4 ceiling for personal scenes."* The corpus has already ratified a human-playtest response (`P3-lite`, line 268: *"run the canonical three-question test with a human; measure the 13-consult load empirically"*) — i.e., Jordan already takes this seriously enough to fund empirical measurement before Stage 4. Each underlying tracker individually KEEPs; the aggregate is a real, corroborated, worsening-with-Stage-4 burden. | **REFINE** — presentation layer, not mechanisms; progressive disclosure/HUD tiering. | System-level P-i risk (composite, not single-engine). | **P1** | KNOWN-TRACKED (Fable-5 dossier) + **independently corroborated by the already-ratified P3-lite empirical-measurement plan** | Redirect Stage-6 UI deliverable toward tiered disclosure; constrain the 3 Stage-4 games from stacking further base-load. | **SURVIVED, strengthened.** The severity is not merely asserted — the corpus already funded a human playtest specifically to measure this before proceeding, which is stronger corroboration than the dossier cited. |
| 16 | **7-faction audience-boost table** (`FACTION_BOOSTS`; `params/contest.md:67-80`; ED-1061) | Confirmed: `params/contest.md` line 15 states "6-faction ethical-mode boost table, +Löwenritter" = 7 total, matching exactly. ED-1061 Guilds tie-break confirmed verbatim at line 78 ("resolved by the engine, not chosen"). Real institutional-bias dynamic, bounded, deterministic. | **KEEP** | Non-rolling static lookup — route out. | — (folds into #15) | KNOWN-TRACKED | N/A | **SURVIVED.** |
| 17 | **Interaction-type prediction** (CLASH/REINFORCE/CROSS/TIE; `dictionaries.py:269-322`) | Confirmed: `INTERACTIONS_TABLE` and the CROSS-override logic (`dictionaries.py:254-259,296-299`) match citation exactly. Real, non-duplicate dynamic distinct from Stasis and the genre bonus. | **KEEP** | Non-rolling classification — route out. | — (folds into #15) | KNOWN-TRACKED | N/A | **SURVIVED.** |
| 18 | **Recall availability** (corpus-wide KU-1 stacking) | The underlying finding is real and now the **best-corroborated finding in the lane** — independently reproduced by four separate audits (2026-06-22 NPC audit; 2026-07-04 refutation L-A; 2026-07-05 Fable-5 audit → `ED-SC-0005`; this pass). **But the dossier's breakdown mislabels the components.** Per `ed_options.md:28`/`workplan_v6_register_deltas.md:10`/`critic_SC.md:55-56`, "+5D" is NOT "Recall" alone — it is `npc_behavior_v30 §6.5`'s **combined local cap on genre+audience+Resonant-Style-targeting+Recall together**, and "+3D" is a **separate combined cap on Findings+Prep**, with Corroborate's uncapped +1D sitting outside both. The real defect is "two local caps + one uncapped bonus, no single global ceiling," not three individually-named uncapped bonuses summing to +9D. | **REFINE** (adopts the corpus-wide docket's existing KU-1 verdict) | Same flat-integer-pool-die idiom as items 5/11, largest magnitude of the three. | P2 | KNOWN-TRACKED (KU-1, now 4×-corroborated) | Closes/reuses the existing KU-1 global-stacking-cap docket item. | **SURVIVED, sharpened.** Verdict and severity unchanged; the component breakdown needed correcting to match the actual cited sources (`npc_behavior_v30 §6.5`; `social_contest_v30.md:166,168,532,535`). |
| 19 | **Momentum available** (capped-4; `contest_legacy_stub.py` Beliefs §9.5) | Confirmed: stub docstring states "a Decisive win on a Belief-aligned position grants +1 Momentum (cap 4)" verbatim. Real cross-system bridge, capped, non-dominant on its face. | **KEEP** | Non-rolling accumulator — route out. | — (folds into #15) | KNOWN-TRACKED | N/A | **SURVIVED.** |
| 20 | **Naming drift: "Piety Track" vs "Persuasion Track"** (`silo_cohesion_analysis.md:39,124,147,158`) | Confirmed at all four cited lines exactly; also independently found in `throughlines_complete.md:123,194` (same lines as item 6). Piety Track is a genuinely distinct territory-scale tracker. | **REFINE** (rename/disambiguate; fixes already proposed in-doc) | N/A | P3 | KNOWN-TRACKED ("corpus sweep in progress," confirmed at `silo_cohesion_analysis.md:210`) | Contributes to the already-open 89-file corpus sweep. | **SURVIVED.** |
| 21 | **Naming drift: `params/contest.md:98` "Direct/Indirect" vs ED-897** | Confirmed exactly at line 98: *"Step 2 — Choose genre (Memory/Projection) + orientation (Direct/Indirect)."* Rest of the same doc (line 114) correctly uses "Obscuring"/ED-897 terms. Checked scope (contest docs + code) turns up no other holdout. | **REFINE** (one-line find/replace) | N/A | P3 | KNOWN-TRACKED | Closes the last holdout **found in the checked scope** (contest docs/code) — not verified as the literal last one corpus-wide. | **SURVIVED**, with a minor precision note on the "last known holdout" framing (true within scope checked, not exhaustively verified corpus-wide). |
| 22 | **MS-vs-RS naming split — ruled out for Contest** | Confirmed: MS = "Mending Stability" (world rendering-stability clock, renamed from RS = "Rendering Stability" per ED-731, `glossary.md:73,245`) — a threadwork/world-scale concern with zero hits inside `sim/personal/contest/*.py`. The one SC-adjacent mention (`dossier_interactions.md:133`) is a cross-scale coupling reference, not an internal SC naming collision. | **KEEP** (correctly scoped out) | N/A | — | N/A | N/A | **SURVIVED.** Negative-result check confirmed accurate by independent re-verification. |

---

## Corrected per-lane rollup

**SC — near-whole, with one genuinely severe (but already-ratified) wiring gap, a corpus-wide recurring leverage idiom of modest real magnitude, and one clean false-positive REFINE removed.** Every individual tracker this lane names (Persuasion Track, Face/Standing, Concentration, Adjudicator Armature, Doubt Marker, CR5, the strain→Rattled design, Chain-Contest caps, Parliamentary Vote, the four-game GAMES router) earns its place under N/Ω/Q judged as-if-built. The corpus-wide precedent citation for the four-games router (`pessimist_action_audit_v1.md:36`) checked out verbatim on independent re-read.

The one genuinely severe item is **#13**: campaign contest scenes dispatch through `contest_legacy_stub.py`, a strictly-poorer duplicate of the 13-module kernel — a correctly-reasoned PRUNE (this is duplicate-coverage judged on fully-built code on both sides, not a build-state judgment) at P1. But **this is not a new discovery** — `ED-SC-0006`/`ED-SC-0007`/`ED-SC-0011` already state the identical finding, ratified-as-accepted by Jordan on 2026-07-05 and reiterated 2026-07-07, three days to one day before this audit. The calibration tag is corrected from NEW to KNOWN-TRACKED; the verdict and severity are unaffected (and arguably strengthened, since Jordan has already accepted the fix).

Two flat-pool-die findings recur across scales (**#5 CR4**, **#11 Parliamentary Vote**) plus a third, larger-magnitude instance already on the corpus's own docket (**#18 KU-1**). All three real, but this pass found the #5 instance's magnitude overstated (~1.84×, not ~2.4×) and its proposed remedy (migrate onto δσ) in tension with a design split (CR4-vs-armature) the corpus has *already* deliberately made for a documented reason — downgraded from a P2/NEW mandate to a P3 Jordan-awareness flag.

One dossier REFINE did not survive scrutiny at all: **#2 (Face/Standing dual-scale)** rests entirely on a **stale in-code comment** that the working tree itself (the same file's `FaceScale` docstring, plus both canonical prose heads) shows was resolved on 2026-07-01 via `ED-1056`. Overturned clean to KEEP; only a trivial comment-currency fix remains.

The remaining REFINEs are legitimate and Q-elegant/legibility-scoped, not mechanism removals: the aggregate 13-item cognitive-load ceiling (**#15**, now further corroborated by the corpus's own already-funded P3-lite human-playtest plan) and two cheap P3 naming-drift items (**#20/#21**). **#6**'s referential-debt PRUNE survives and is in fact *larger* than claimed — `valoria_ui_ux_v4_1.md` carries no existing "superseded" banner to reconfirm; one needs to be added fresh. **#12**'s "refuted" domain_echo orphaning claim survives on the narrow zero-callers point but needed sharpening: the ticket (`ED-SC-0007`) covering full functional resolution is still open.

**Net shape: no design-intrinsic junk in this lane.** The real risk surface is (a) one severe, already-Jordan-accepted reachability/duplication gap (#13) gating everything else, and (b) a repeated, small-magnitude leverage-uniformity idiom the corpus's own σ-leverage architecture was built to prevent but that still shows up through flat "+1D" bonuses — real, worth a single consolidated Jordan-tuning pass, but neither as urgent nor as novel as the pre-critic dossier framed it.

**22 elements: 15 KEEP / 5 REFINE / 0 DISTILL / 0 MERGE / 2 PRUNE / 0 CUT (post-critic).**

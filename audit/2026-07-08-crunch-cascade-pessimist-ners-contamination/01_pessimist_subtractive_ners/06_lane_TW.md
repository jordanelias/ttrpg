# Pessimist Subtractive NERS — Threadwork (TW)

# Pessimist Subtractive NERS — Threadwork (TW) Lane — Inverted Critic Pass (Final)

**Critic method:** Independently re-read `01_findings_threadwork.md` and re-verified every citation in the dossier against the working tree (`params/threadwork.md`, `designs/threadwork/threadwork_v30.md`, `sim/thread/{operations,collective}.py`, `sim/personal/knots.py`, `sim/peninsular/{ms_track,rs_track}.py`, `sim/autoload/{dice_engine,sigma_leverage}.py`, `references/glossary.md`, `canon/{editorial_ledger.jsonl,patch_register_index.md}`, `designs/provincial/{mass_battle_v30,clock_registry_v30,strategic_layer_v30}.md`, `params/bg/phases.md`, `designs/architecture/integration_proposal_v30.md`, `designs/audit/2026-07-07-unaddressed-areas-audit/{resolution_plan_v1,finding_status}.md`, `skills/valoria-resolution-diagnostic/SKILL.md`). Nearly every citation checked out exactly as claimed. Two non-fatal citation errors were found and are flagged inline (row 6, row 9); one DISTILL verdict is strengthened with new corroborating evidence (row 11); the NERS Engine Verdict was independently re-derived line-by-line from the actual dice-roll call sites (not trusted from the dossier's framing) and survives fully, plus is sharpened with one additional piece of context (the resolution-diagnostic skill's own unverified "likely compliant" placeholder for Thread, which this pass shows to be wrong).

## Per-element table

| # | Element | N/Ω/Q reasoning | Subtractive verdict | NERS engine verdict | Severity | Calibration | Downstream retirement | Critic disposition |
|---|---|---|---|---|---|---|---|---|
| 1 | MS/RS naming split | Verified independently: `threadwork_v30.md:782` ("RENDERING STABILITY (WORLD TRACK)") self-contradicts its own `:784` ("What Mending Stability Measures"), two lines apart, in the CURRENT.md-designated canonical head. `params/threadwork.md` = 25× bare "RS", 0× "Mending Stability" (confirmed by direct count). `thread_horizontal_integration_spec.md` = 12× RS, 0× MS (confirmed). `references/glossary.md:73` already resolved to "MS" per ED-731 but still carries the deprecated "60(TTRPG)/72(BG)" split framing. This is a Q-smooth/Q-elegant documentation defect on a Ω-a-qualifying track (feeds Mandate, CV, Order, Revelation Curve), not an N-fail on the mechanism. | **KEEP** | N/A (naming) | P1 | KNOWN-TRACKED (ED-WR-0002, `editorial_ledger.jsonl:323`, open — confirmed exact text: "MS wins... execution not done") | — | **SURVIVED** |
| 2 | MS start-value 60-vs-72 + `ms_trajectory_v1` residual | Verified: `params/threadwork.md:141` and `clock_registry_v30.md:16` both explicitly resolve 60-vs-72 → 60. `sim/peninsular/ms_track.py:11-19` independently confirms 60 (with its own resolution note, "Jordan 2026-06-06"). `threadwork_v30.md:840` ("MS ~72") is a confirmed unpropagated straggler; `clock_registry_v30.md:16`'s "ms_trajectory_v1 reconciliation pending Jordan" is a genuinely separate, still-open creative-layer question. Mechanism (single canonical seed) is sound; residual is pure staleness. | **KEEP** | N/A | P3 | KNOWN-TRACKED (`clock_registry_v30.md:16`) | — | **SURVIVED** |
| 3 | Thread Sensitivity 0–100 | Verified `operations.py:71-78` (`DEPTH_TS_MINIMUM` dict) — TS genuinely gates operation-scale eligibility, a distinct axis from Spirit (pool size, attribute) and History (skill investment). No duplicate-coverage failure found on inspection. | **KEEP** | N/A (roll-input, scope-gate excluded) | P3 | NEW | — | **SURVIVED** |
| 4 | Coherence 10→0, practitioner-only | Confirmed range in `params/threadwork.md:14`. Distinct existential-stakes gauge (drift toward Severance), not duplicated by Composure or any Wound/HP track. Dramatically legible per its own Rendering Crisis arc (`threadwork_v30.md` §3.7). | **KEEP** | N/A (continuous resource feeding rolls) | P3 | NEW | — | **SURVIVED** |
| 5 | Thread Fatigue 5–35 | Confirmed zero implementation: no "fatigue" hits anywhere under `sim/thread/*.py` or `sim/personal/knots.py`. Confirmed distinct axis (tactical per-scene rate-limiter) from Coherence (existential/long-horizon). One precision correction: the dossier's "P1 (per resolution_plan's own tag)" overstates the source — `resolution_plan_v1.md`'s table has no explicit P-severity column for C-TW-6/7 (only Finding/Fix/Artifacts/Lane/Stratum/Gate); the P1 appears to be the dossier's own judgment, not a literal inherited tag. Verdict unaffected. | **KEEP** | N/A (accumulator/clock, scope-gate excluded) | P1 (dossier's own assignment, not literally sourced — noted) | KNOWN-TRACKED (C-TW-6, `resolution_plan_v1.md:158`, confirmed exact text "100% unimplemented" framing matches finding_status.md) | — | **SURVIVED** (severity-citation precision flagged) |
| 6 | Three-Axis Ob: Depth+Breadth+Distance | Confirmed all three tables exist independently in `params/threadwork.md:69-101` and are genuinely orthogonal (what kind of layer / how much / how far). Verified build-state aside: `BREADTH_OB`/`DISTANCE_OB` are defined in `operations.py:81-95` but a corpus-wide grep shows they are **never referenced anywhere outside their own definitions** — confirmed only `DEPTH_OB`/`MENDING_OB` are summed into any Ob computation. **Citation correction:** the dossier's line numbers (255, 264, 300, 309) are only half-right — `attempt_weaving` (255) and `attempt_pulling` (264) are exact, but the `DEPTH_OB.get()` calls in `attempt_locking`/`attempt_dissolution` are actually at lines 303 and 312, not 300/309 (those lines are the preceding `scale =`/`scale_cost =` statements). Substance unaffected — build-state, correctly excluded from verdict. | **KEEP** | N/A (roll input) | P3 | NEW (wiring gap only) | — | **SURVIVED** (minor line-citation correction: operations.py 303/312, not 300/309) |
| 7 | "Knot Strain" naming collision | Confirmed both senses exist verbatim: `params/threadwork.md`'s "### Knot Strain" subsection (line 235, under Opposing Operations/PP-653 — +1/+2 Ob penalty) vs. the "Strain" bond-gauge under "## Knot Mechanics" (line 44, ED-912, −5..+5). Confirmed `references/glossary.md` Part Twelve (Collision/Disambiguation Table, lines 235-246) has **zero** entries for this pair — only CI/CP/TD/COMP/RS are listed, confirming the "right home, no entry" framing. Attempted to save as KEEP (players may never encounter both readings in the same sitting) — doesn't hold: both are core, frequently-invoked terms (opposing ops occur whenever two practitioners contest a target; the bond gauge is the core Knot mechanic), and Q-elegant requires one-read restatability that a shared name defeats regardless of encounter frequency. REFINE (glossary entry, not redesign) is correctly the mildest applicable tier. | **REFINE** | N/A | P2 | KNOWN-UNTRACKED (confirmed: no ED covers this) | Add Part Twelve row; rename opposing-ops sense (e.g. "Contest Strain") | **SURVIVED** |
| 8 | "Thread Debt" dangling tracker | Verified the correction is right: `params/threadwork.md:167` and `threadwork_v30.md:768` reference Thread Debt without defining it, but `params/bg/phases.md:59` ("Thread Debt: tokens >1 season old: RS −1/token. Serviced tokens: no drain; permanent residual RS −0.5 recorded") and `strategic_layer_v30.md` §9.15 (line 773, plus the PP-24 correction at line 243) give it a real, bounded, forward-looking formula. `clock_registry_v30.md:111` confirmed pointing to "params_board_game.md §Thread Debt" — Thread's own docs carry no equivalent cross-reference. This is a homing gap, not an undefined-mechanism N-fail. | **KEEP** | N/A (deterministic ledger token) | P2 | KNOWN-UNTRACKED (confirmed no ED) | — | **SURVIVED** |
| 9 | The Leap + Thread-ops execution chain, "total island" | Confirmed: `attempt_leap` has exactly one caller (`collective.py:93`), `collective.py` itself has zero external importers (`attempt_collective_operation`/`from sim.thread.collective` : only self-hits), and `sim/mc_v18.py` has 0 "thread" references. Confirmed `integration_proposal_v30.md` (line 338 exactly) quote verbatim: "the most technically successful system... and the least experientially designed." **Citation note:** the dossier's "`finding_status.md:77-78`" is ambiguous — three files share that basename in the repo; the content only resolves against `designs/audit/2026-07-07-unaddressed-areas-audit/finding_status.md:77-78` ("C-TW-1 (threadwork total island — no dispatch branch, no trigger, no test)"), which matches exactly once disambiguated — the `2026-07-08-pessimist-action-audit/finding_status.md` (47 lines total) does not contain this text at all. `resolution_plan_v1.md:161` confirmed exact match. Mechanism itself (mechanical instantiation of the Leap-as-rendering-suspension thesis) passes N/Ω/Q cleanly under attack; reachability is correctly excluded per cardinal rule. | **KEEP** | See NERS Engine Verdict below — **non-compliant** | P1 (wiring/legibility tag only) | KNOWN-TRACKED (C-TW-1, confirmed at both cited sources once path is disambiguated) | — | **SURVIVED** (citation path corrected: unaddressed-areas-audit's finding_status.md, not the pessimist-audit's) |
| 10 | Mass Battle §A.10 self-contradiction | Confirmed verbatim: `mass_battle_v30.md:530` ("War \| Structural \| 70 \| 5 \| −2/op") vs. `:533` ("cap (−1 per operation, threadwork_v30 §3.2 per-operation cap) applies. No additional surcharge"). Genuine numeric contradiction in a specific application; does not impugn Thread-in-mass-battle's existence (a real cross-scale dynamic — the general does field-battle mystics). ED-1010 confirmed at `editorial_ledger.jsonl:178` (not the producer's original mis-cited :396 — ledger is only 386 lines, confirmed). | **KEEP** | N/A (flat automatic cost) | P1 | KNOWN-TRACKED (ED-1010, confirmed) | — | **SURVIVED** |
| 11 | Residual "RS x3" line, `params/threadwork.md:250-251` | Independently re-derived, going further than the dossier: the "### Mass Battle" subsection (params/threadwork.md, under PP-653's Opposing Operations) is **not** actually a sub-case of "N-Way (3+)" as the dossier's prose implies — it's a parallel special case (opposing/contested ops specifically occurring in mass-battle context), structurally distinct from the N-Way rule. But the "×1 vs ×3" contradiction is *more*, not less, damning than the dossier states: (a) `threadwork_v30.md`'s own design doc §2.6 (lines 593-596, "N-Way Opposing Operations") has **no "Mass Battle" subsection at all** — this params-only text isn't actually propagated from a design-doc §2.6 "Mass Battle" section as its own header claims; (b) PP-653 is dated 2026-04-18 (`patch_register_index.md:168`), **four days after** PP-601 struck the general ×3 (`params/mass_combat.md:239-244`, session 2026-04-14); (c) the phrase "RS x3. Temporal not x3" is a near-verbatim echo of the *struck* PP-226 ("Temporal auto-effect RS (PP-181) NOT subject to x3", confirmed in `patch_register_index.md:99`). This date-sequence + phrase-match is a smoking gun for "unreconciled copy-paste fossil from the pre-strike regime," not a deliberate carve-out — strengthening the "accidental" reading beyond what the source findings file (which called it "currently indistinguishable") committed to. DISTILL (fold the stray multiplier into the ×1 policy already governing every other A.10 drain path) is the right-sized verdict — this is a parameter-level fossil, not a competing action, so DISTILL (not PRUNE/MERGE, which are for whole competing mechanisms) is correctly scoped. | **DISTILL** | N/A | P2 | KNOWN-UNTRACKED → evidence now favors "accidental," not 50/50 | Removes ambiguity, forecloses a future dedicated reconciliation ED | **SURVIVED — STRENGTHENED** (date-sequence + verbatim-phrase-match to struck PP-226 confirmed; "fossil" reading now the stronger case, not merely "indistinguishable") |
| 12 | MS/RS threshold-band world consequences | Confirmed `sim/peninsular/ms_track.py` exists and implements the numeric track; confirmed `sim/peninsular/rs_track.py` is a dead stub (`raise NotImplementedError("...Pass 2l armature stub")`, verified verbatim). Band-consequence code absence is correctly excluded as build-state; the design thesis (autonomous world-state events) is Ω-c/Ω-a-qualifying and dramatically legible. | **KEEP** | N/A (threshold table) | P1 | KNOWN-TRACKED (OPT-5/C-TW cluster) | — | **SURVIVED** |
| 13 | Knot formation/rupture — ED-912 gauge | Confirmed via direct read of `sim/personal/knots.py`: the module's own header explicitly documents the 2026-07-08 (C-TW-12) rebuild onto the ED-912 bidirectional −5..+5 gauge, with the pre-ED-912 model surviving only as a superseded-comment — REFUTED-finding correction stands. Confirmed the formation pool (`spirit*2 + history_rel`, line 215) really can bottom out near 2D (Spirit 1, no history) — a genuinely low, pivotal, non-aggregated roll gating the entire mechanic. | **KEEP** | Partial non-compliance on `form_knot`'s roll specifically (see NERS Engine Verdict) | P2 | NEW (roll_pool/engine-selection) / KNOWN-TRACKED (C-TW-12, closed) | — | **SURVIVED** |
| 14 | Bounded additive mass-battle drain model | Confirmed the flat model in `threadwork_v30.md:45-53` ("[STRUCK — campaign_architecture_v1 §3.1]... −1 MS/battle standard, −2 cataclysmic...") and independently confirmed a *second*, consistent re-statement in `params/mass_combat.md:239-244` ("×3 multiplier STRUCK (PP-601 struck PP-192/225)... full TTRPG value (×1)"). Two independent canonical sources agree the catastrophic ×3 is dead; genuinely capped, non-dominant replacement. | **KEEP** | N/A (deterministic cost tiers) | P3 | KNOWN-TRACKED (PP-192/225 struck, PP-601; re-confirmed) | — | **SURVIVED** |
| 15 | Substrate Saturation Counter, PP-606 | Confirmed `params/threadwork.md:160-167` in full — a genuinely distinct *congestion/frequency* penalty (≥3 ops/turn escalates Ob/Coherence, hard-capped) layered atop, not duplicating, the flat baseline cost (row 14). Creates a real stagger-vs-mass tactical tradeoff not covered by any sibling tracker. | **KEEP** | N/A (deterministic threshold counter) | P3 | KNOWN-TRACKED (PP-606) | — | **SURVIVED** |
| 16 | Cognitive load framing (≈14 trackers/≈10 decisions) | Confirmed the source findings text (`01_findings_threadwork.md` §4) itself frames the 14-tracker volume as earned richness ("genuinely the richest... action in the corpus") and its *only* actual complaint is the MS/RS translation tax — textually identical to row 1's finding. Attempted to un-merge into an independent "too much complexity" verdict: fails, because each individual tracker (rows 3-6, 8, 11-12, 15) was separately vetted and passed N/Ω/Q on its own merits — there is no separate duplicate/abstractable tracker left over once row 1 is accounted for. MERGE holds. | **MERGE → Row 1** | N/A | P1 (inherits row 1) | KNOWN-TRACKED (ED-WR-0002) | Same as Row 1 | **SURVIVED** |
| 17 | No player-facing UI model for Thread ops | Confirmed cross-corroboration: `integration_proposal_v30.md` line 338/342 verbatim match ("the game knows what Thread operations do mechanically; it doesn't fully specify what they *feel like* to perform"). Independent second source strengthens a single-source finding as the charter intends. | **KEEP** | N/A | P1 | KNOWN-TRACKED (cross-corroborated, confirmed) | — | **SURVIVED** |
| 18 | Hidden Thread-site bonus, +0.5/+1 Accord | Confirmed `threadwork_v30.md:856` verbatim ("+0.5 Accord/season, unlabeled in UI... At Thread revelation..., the bonus becomes visible") and the ED-693 propagation citation at line 865. Minor note: "historical_precedents_analysis §4.3" is cited only as an in-doc source label — no standalone file of that name exists in the repo (likely an archived/external source, or a citation-only reference) — doesn't affect the mechanic's soundness. Considered whether "deliberately hidden" conflicts with Q-robust's "visible traceable change" — resolves because the hiding is itself the point (mirrors the Revelation Curve thesis) and is eventually revealed, i.e., traceable-on-a-delay by design, not an accidental legibility gap. | **KEEP** | N/A | P3 | KNOWN-UNTRACKED (working as intended) | — | **SURVIVED** (flagged: "historical_precedents_analysis" citation is not a resolvable standalone file — administrative note only) |
| 19 | Dense philosophical jargon (Amendment-01/02) | Confirmed `threadwork_v30.md:729` verbatim — the exact passage cited ("substitute layer 3 for failed layer 2... Reality Strain MS cost") is a genuinely dense, multi-conditional (TS-band-gated) paragraph that could be reformatted as a simple table without changing the underlying rule. Confirmed `references/glossary.md` Part Eight (Thread Operation Types, lines 173-184) has zero entries for Amendment-01/02 vocabulary, "Knot Strain," or "Thread Debt" — the right home exists and is empty. The underlying substrate ontology is foundational and correctly not targeted for cutting. | **REFINE** | N/A | P3 | KNOWN-UNTRACKED (confirmed) | Add Part Eight entries; consider reformatting the TS-band cascade as a table | **SURVIVED** |

## NERS Engine Verdict — Thread Operations Resolver (independently re-derived)

Re-derived from source, not trusted from the dossier's framing. Confirmed line-by-line:

- `sim/thread/operations.py` imports **only** `roll_pool` from `dice_engine` (line 40) — never `degree_from_net`, never `continuous_engine_sample`, never `sigma_leverage`. It implements its own **local** `_compute_degree` (line 134-142): `net >= ob + 3` → Overwhelming. `sim/thread/collective.py:166-167` and `sim/personal/knots.py:226` independently reimplement the identical `ob + 3` rule.
- `sim/autoload/dice_engine.py:115` (`degree_from_net`, the canonical function in the very module Thread imports from) defines Overwhelming as `net >= 2*ob and net >= 3` — confirmed different from Thread's local rule.
- `references/glossary.md:141` confirms a *third* documented form: `≥ Ob+1` (BG, provisional). `params/core.md:74` documents a *fourth* variant for "videogame implementation" continuous thresholds (`net ≥ 2·Ob`, no minimum-3 floor). Thread's `ob+3` is a fifth, silently divergent, uncited local reinterpretation — if anything the corpus-wide divergence is worse than the dossier's "three plus Thread's fourth" count suggests.
- Confirmed `sim/personal/contest/*` (wrapper.py, resolver.py, primitives.py, armature.py, etc.) and `designs/scene/combat_engine_v1/core.py` (`from sim.autoload import sigma_leverage as SL`) both import and use `sigma_leverage`/`continuous_engine_sample`; confirmed **zero** hits for either symbol anywhere under `sim/thread/` or `sim/personal/knots.py`. ED-884, ED-1037, ED-1055 all verified real, ratified/resolved ledger entries documenting exactly this migration for combat and contest.
- Confirmed Thread's pool formula (`_actor_pool`, operations.py:145-157: `2*Spirit + min(3, history+3) + TS//10`) ranges 8D (Spirit 1, TS 30, the minimum Leap-eligible TS) to 27D (Spirit 7, TS 100) — a healthy pool with a genuine setup axis, which the Engine-Selection Rule (`skills/valoria-resolution-diagnostic/SKILL.md:148`) routes to **Instance A**. Confirmed Knot formation's pool (`knots.py:215`: `spirit*2 + history_rel`, no TPS, no floor) can be as low as 2D — pivotal (gates the entire mechanic), non-aggregated — which the same Decision Rule (line 149) routes to **Instance B**.
- **Additional context not in the dossier, found during this pass:** `skills/valoria-resolution-diagnostic/SKILL.md:339` itself contains a pre-existing, unverified placeholder — "**Thread operations (Instance A)** — likely compliant... verify in-band leverage + continuity correction" — filed without ever checking whether Thread actually touches `sigma_leverage`. This pass shows that placeholder was wrong: Thread never migrated. This strengthens, rather than undercuts, the finding — it's not just a "new" defect this audit surfaced, it's one the corpus's own governing skill had assumed away without verification.

```
ENGINE: Thread Operations Resolver   INSTANCE: A (should be) — IMPLEMENTED as legacy discrete (Lesson-3 violation)
VERDICT: NON-COMPLIANT — confirmed independently: wrong engine class for a healthy-pool+setup-axis system,
         plus an independently divergent, uncited degree formula shared by all seven operations + collective + knots.
```

This verdict **survives** the critic's independent re-derivation in full, and is now better-evidenced than the dossier presented it (corpus has ≥4 documented Overwhelming variants pre-existing, not 3; the resolution-diagnostic skill's own "likely compliant" assumption for Thread is shown false).

## Per-lane rollup (corrected)

**TW remains the richest system in the corpus, and the cleanest lane on mechanism merit — its debt is naming, homing, and engine plumbing, not existence.** All 15 originally-KEEP elements survive independent re-verification against the working tree with no citation found to be substantively wrong (two minor line-number corrections noted: operations.py 303/312 not 300/309 in row 6; finding_status.md needs the `2026-07-07-unaddressed-areas-audit/` path, not the pessimist-audit directory, in row 9). The two REFINEs (Knot Strain naming collision; dense Amendment-01/02 jargon) and the MERGE (cognitive-load framing folding into the MS/RS finding it actually describes) all hold exactly as scoped — each has a confirmed, currently-empty landing slot already built into `references/glossary.md` (Part Eight, Part Twelve) waiting to receive the fix. The one DISTILL (the orphaned "RS x3" mass-battle-opposing-ops line) not only survives but is **strengthened**: independent digging found the exact struck-PP-226 phrase reused four days after the general ×3 was killed, making "unreconciled fossil" the better-evidenced reading over "deliberate carve-out." The NERS Engine Verdict — Thread's entire resolution path running on the legacy discrete engine with an uncited, divergent Overwhelming formula, while combat and contest have already migrated to sigma-leverage — is independently confirmed from the code itself (not the dossier's framing) and is, if anything, understated: the corpus carries at least four mutually-inconsistent Overwhelming formulas, and the corpus's own resolution-diagnostic skill had assumed Thread was compliant without ever checking. That defect sits underneath the cleanly-KEEP-verdicted mechanisms (rows 9, 13 above and, on inspection, all seven operations) — the mechanisms earn their place; the engine seating them does not yet deliver on it.

**19 elements: 15 KEEP / 2 REFINE / 1 DISTILL / 1 MERGE / 0 PRUNE / 0 CUT (post-critic).**

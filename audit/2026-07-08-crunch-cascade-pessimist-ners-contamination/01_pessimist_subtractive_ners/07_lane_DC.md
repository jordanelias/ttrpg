# Pessimist Subtractive NERS — Board Game + Dice/Resolution Core (DC)

Independent verification complete. I re-derived every citation against the working tree (params/core.md, params/board_game.md, params/bg/*.md, values_master.yaml, descriptor_registry.yaml, social_contest_v30.md, mass_battle_v30.md, combat_engine_v1/core.py, module_contracts.yaml, tools/export_engine_params.py, sim/autoload/sigma_leverage.py, skills/valoria-resolution-diagnostic/SKILL.md, canon/editorial_ledger.jsonl) rather than trusting the dossier's framing. Findings below.

# Pessimist Subtractive NERS — Lane: Board Game + Dice/Resolution Core (Inverted Critic Pass, FINAL)

**Critic method note:** Every non-KEEP verdict was attacked in good faith (steelman for KEEP, or argument the severity was miscalibrated). Every KEEP was independently re-derived, not rubber-stamped. Result: **all 18 verdicts survive** — none overturned to a different disposition-family, none softened. Three items gained materially new corroborating evidence not in the original dossier (marked STRENGTHENED), one severity was recalibrated (item 2, P3→P2), and one factual miscount was corrected (item 1, 17 dead links, not 18). The dossier's own rollup paragraph contains an arithmetic error (claims "8 KEEP" while listing 5 item numbers and the table itself shows 6) — corrected below.

## Per-element table

| # | Element (file:line) | N/Ω/Q reasoning | Subtractive verdict | NERS engine verdict | Severity | Calibration | Downstream retirement | Critic disposition |
|---|---|---|---|---|---|---|---|---|
| 1 | `params/board_game.md` broken index (verified: **17** dead links, not 18 — all `## [...]` headers point to `references/params_bg_*.md`/`references/params_board_game_misc.md`, none of which exist; real content lives at `params/bg/*.md` under un-prefixed names, e.g. `params/bg/core.md`) | N: an index for the CURRENT.md-designated BG canonical head is necessary. Ω as-if-fixed: cheap, non-dominant, pure navigation. Q as-if-fixed: one-read legible; current instantiation fails Q only because every link 404s. | **KEEP** | n/a | P1 | KNOWN-TRACKED — `ED-FA-0003` (ledger: "16... links... zero exist" — my own count is 17, ED-FA-0003's is 16, dossier's is 18; regardless of exact count, the defect and remediation direction are identical) | — (KEEP) | SURVIVED — steelman attempted (is the index itself the wrong mechanism, i.e. should CURRENT.md's own row simply absorb this and the index be PRUNED?); rejected because `params/bg/*` is 18 files of real, differentiated content CURRENT.md cannot inline — an index is still the right tool, just mis-generated. |
| 2 | "Quasi-binomial" (`CURRENT.md:56`, exact hyphenated form: one hit corpus-wide, confirmed) | N-fail as a *live* term: no distinct math rides on it in the canonical head (`core.md`'s Continuous Engine samples `Normal(μ·N,σ·√N)`, never "quasi-binomial"). Ω: pays nothing directly. Q: hurts legibility. **New evidence changes the picture**: the un-hyphenated "quasibinomial" is NOT decorative-only — it names a real, distinct, now-superseded v15 sampling architecture (`designs/audit/2026-05-14-balance-audit/part16_v15_quasibinomial_checkpoint.md`: `Mean=N×p, Variance=φ×N×p×(1−p)`, an overdispersed-binomial model), explicitly superseded by Decision E's Normal model per `combat_balancing_methodology.md` ("the ratified dice distribution is the smooth continuous engine... VFIVE board-game sampling was rejected"). CURRENT.md's phrasing conflates a rejected/superseded architecture with the ratified one. | **DISTILL** into "Continuous Engine (Decision E)"; no live decision lost — but the term is a genuine historical vestige, not pure decoration | n/a | **P2 (raised from dossier's P3)** | NEW (dossier undercharacterized this as "purely decorative"; it's a superseded-architecture vestige surfacing in the repo's single most-trusted navigational doc) | Same as dossier + removes risk of a reader inferring the live engine has an overdispersion parameter φ it does not have | **STRENGTHENED** — survived, but the dossier's own "pays nothing... no lost decision" framing was too soft; re-grounded against `part16_v15_quasibinomial_checkpoint.md` + `combat_balancing_methodology.md` and severity raised because CURRENT.md is the one doc CLAUDE.md §1 says to trust above filenames/version strings. |
| 3 | Combat Pool canonical formula `max(5, Relevant History+6)` (`params/core.md:161`, `combat_engine_v1/core.py:27-32`, `module_contracts.yaml:811`) | N: sizes every personal-combat roll. Ω: sole live formula, non-dominant, cross-scale-traceable via the shared σ-kernel. Q: one-read legible at its site. All three citations verified byte-for-byte identical (`max(5, int(round(history))+6)` in code; `max(5, Relevant History + 6)` in prose; `pool=max(5,History+6)` in the contract). | **KEEP** | see consolidated NERS block | P1 | KNOWN-TRACKED — ED-901/900/904 (ratified), ED-1084 (site cleanup) | — (KEEP) | SURVIVED — spot-checked hardest of the KEEPs; no daylight between the three citations. |
| 4 | `values_master.yaml` stale Combat Pool entries ×2 (struck `(Agi×2)+Hist+3` filed under nonexistent `params/combat.md`, and a second differently-worded stale form filed under `params_core.md`) | N-fail as instantiated: dead output of a dead generator, carries the STRUCK formula. Ω: pays nothing; real bypass risk for a reader who greps/parses the YAML directly and never sees the banner ~200 lines above. Q: fails for that bypass reader. Quarantine banner confirmed verbatim (`⚠️ QUARANTINED — STALE SNAPSHOT, DO NOT CITE AS CANONICAL, 2026-07-01, ED-1084`). | **PRUNE** — delete the two entries; correct value already lives at `core.md:161`/`core.py`/`module_contracts.yaml` | n/a | P2 | KNOWN-TRACKED — quarantine banner + decision-queue item 17 (regenerate-vs-retire) | Resolves decision-queue item 17 toward "retire" for this row | SURVIVED — steelmanned KEEP on the grounds the entries are historical provenance; rejected: the registry's own header declares its purpose as a *live* value snapshot, not an archive, and the struck formula's provenance is already preserved in git history + `editorial_ledger.jsonl` (ED-901/904) + `archives/`, so nothing is lost by deleting just these two rows. |
| 5 | `params/core.md:252` in-file Fieldwork-Pool propagation miss ("matching Combat Pool... construction") | N: Fieldwork Pool is load-bearing. Ω: the false cross-reference teaches a wrong mental model — negative cost, not zero (confirmed: line 252 literally contradicts line 161 in the same file). Q: fails hardest of the cluster. **New evidence**: `ED-1084`'s own ledger text claims Combat Pool was "collapsed to the canonical form... across every live stale site: **params/core.md**, ..." — i.e. ED-1084 explicitly claims to have fixed this file, yet line 252's stale claim demonstrably survives. This isn't just an unlisted site; it's a site ED-1084 claims to have already closed. | **REFINE** — correct/delete the false "matching Combat Pool" claim; Fieldwork Pool's own formula stays | n/a | P1 | NEW | Closes ED-1084's stated goal for real (not just adds a missed site — corrects an overstated completion claim) | **STRENGTHENED** — verified ED-1084's ledger entry names `params/core.md` as an already-fixed site, making this a confirmed gap in a supposedly-closed remediation, not merely an omission. |
| 6 | Argue/Contest Pool formula (`social_contest_v30.md` §8 table, verified: "Argue pool \| (Primary Attribute×2)+History bonus \| Variable \| Combat Pool = max(5,History+6) — Agility-independent (ED-901; **comparison column, not a shared formula**)") | N: legitimate scale-appropriate mechanism. Ω: non-dominant, distinct scale. Q: explicitly self-annotates its own divergence — positive contrast to item 5. | **KEEP** | n/a | — | KNOWN-TRACKED (correctly annotated; non-issue) | — | SURVIVED — exact text confirmed; this is the textbook "self-aware, does it right" counter-example the dossier uses it as. |
| 7 | Mass-Battle Effective Combat Pool `min(Size,Command)+Command` (PP-233; `mass_battle_v30.md` line ~28, rationale at ~384) | N: distinct strategic-scale aggregation, feeds Instance-A via "aggregate, then A." Ω: drives the documented ranged/melee generalship balance rationale — pays its cost. Q: legible in its own doc; explicitly separated from Combat Pool. **Additional grounding found**: the doc itself flags this as the "legacy/OFF-path model" (ED-899/ED-1013 annotation) — the *live* engine (`tests/sim/mass_battle/orchestration.py`) computes a smoother `Command×(1+cohesion)` form that reproduces this value byte-exact only when `COMMAND_SIGMA_ENABLED=0`. This is a third self-flagged, internally-consistent divergence pattern (same genus as item 6), not a silent one. | **KEEP** | see consolidated NERS block | — | KNOWN-TRACKED (non-issue) | — | **STRENGTHENED** — the self-flagged legacy/live-engine split (ED-899/ED-1013) is additional, uncited-by-the-dossier evidence that this row is honestly maintained, not merely undisturbed. |
| 8 | Derived-stat schema: `descriptor_registry.yaml` 9-attribute/3-group IN-FLUX roster vs `core.md`'s 10-attribute/4-group table (confirmed: registry folds `Cognition→Acuity` `[ASSUMPTION]` and `Spirit→Will`) | N: a single attribute roster is necessary — read by nearly every roll. Ω: as instantiated (two disagreeing live rosters) fails to pay for the alias mechanism's own W1.13 purpose. Q: fails one-read legibility. | **REFINE** — roster mechanism (name-indexed alias resolution) is sound; population must reconcile | n/a | P2 | KNOWN-TRACKED — `ED-IN-0008` (open, ledger text confirmed: "reconcile the roster + resonance_style") | Feeds ED-IN-0008 execution | SURVIVED — considered whether this collapses to a non-finding since ED-IN-0008 already owns it; rejected: an open ED doesn't retroactively make the current two-roster state Q-compliant, and the finding correctly scopes what ED-IN-0008 must fix. |
| 9 | Recall-alias-drop inside `descriptor_registry.yaml` (confirmed: zero case-insensitive hits for "recall" anywhere in the file, as key or alias) | N: Recall sets the per-History point cap (`core.md:151`, confirmed) and drives Fieldwork Research/Reconstruct (`core.md` Attribute Roles table, confirmed row present) — load-bearing. Ω: the alias system's entire payoff (drift-proofing) is defeated for this one attribute — worse than item 8's *visible* disagreement, this is silent disappearance. Q: a registry query for "Recall" returns zero hits, no error — worst-case outcome for a name-indexed lookup. | **REFINE** — add the missing `Recall` key+alias to `attr.mind.*` | n/a | P2 | NEW — confirmed `ED-IN-0008`'s ledger text does not name Recall specifically | Pinpoints one concrete addition to ED-IN-0008's scope | SURVIVED — this is the single cleanest, most surgical finding in the lane; verified independently rather than trusted, and it held exactly as claimed. |
| 10 | Continuous σ-resolution engine as shared cross-scale substrate (`core.md` §Continuous Engine) | N passes trivially — without it, none of five scales resolve. Ω: pays its cost by construction (CLT-general). Q: mostly legible. | **KEEP** | Instance A — see consolidated block | — | KNOWN-TRACKED (Decision E, ratified 2026-05-15) | — | SURVIVED. |
| 11 | Faction-scale bare 1–7D pool sanity-check never run (`core.md:78`, confirmed exact text) | N: not a mechanism itself — an engine-selection/verification gap. Ω/Q: per the diagnostic skill's own Decision Rule (verified verbatim in `SKILL.md:149`: "bare stat (1–7D), pivotal/load-bearing outcome, no aggregation available → Instance B"), some faction pools may be mis-assigned to Instance A (unvalidated) when Instance B (proven, ED-874) is the right tool. | **KEEP** (σ-engine's general application); gap is remediation-tracked, not a defect in the mechanism | Instance A/B boundary ambiguity — see consolidated block | P2 | KNOWN-TRACKED for the validation gap (ED-836, confirmed exact ledger text); Instance-A-vs-B mis-assignment angle is NEW | — (KEEP; remediation in NERS block) | SURVIVED — the Decision Rule citation was checked verbatim against the skill file, not paraphrased on trust; it's accurate. |
| 12 | Obstacle Scale exception-band split: Ob 20 (general, `core.md:55`) vs Ob 10 (BG, confirmed `params/bg/core.md:74-75`: "Ob 10 exception... `[FLAGGED FOR REVIEW: ED-142-R]`") | N: some exception-band handling at extreme Ob is necessary. Ω: unjustified split — could be deliberate BG recalibration or drift, unconfirmed by the doc's own admission. Q: fails — two cutoffs to remember. | **REFINE** — Jordan must justify or collapse to one shared cutoff | n/a | P2 | KNOWN-TRACKED — self-flagged, open, distinct from the older *resolved* ED-142 (2026-04-04) — confirmed both exist in the ledger as separate items | Resolving ED-142-R removes one of item 18's two named exception thresholds | SURVIVED. Minor citation-hygiene note: the dossier's file label ("params_bg_core.md") is the *broken index's* naming convention, not the real working-tree path (`params/bg/core.md`) — content is accurate, path label isn't. |
| 13 | `combat_engine_v1`'s fixed `DECISIVE_OB=3` vs general 1–20 Obstacle Scale (`core.py:25`, `module_contracts.yaml:811`, confirmed "base Ob 3 FIXED" text) | N: a fixed leverage-anchor is a deliberate, ratified architecture (ED-1085's relocation comment + ED-900/904's blanket ratification), not scope-creep. Ω: pays its cost — different axis (fixed baseline + all variation via leverage). Q: fails — undocumented from the shared substrate doc; **and this has already cost real audit effort once** (the source finding's own §2 records a prior pass mischaracterizing this as a doctrine violation, since corrected). | **REFINE** — mechanism stays; add a cross-reference note in `core.md`'s Obstacle Scale section | Instance A (parameterization) | P2 | KNOWN-UNTRACKED — design ratified; this doc-gap isn't separately tracked | Prevents a third re-discovery of the same false-positive | SURVIVED — P2 (not P3) is justified by the demonstrated recurring cost (one prior false "doctrine violation" claim already burned effort on this exact gap). |
| 14 | Exporter gap: `tools/export_engine_params.py` `derive()` (confirmed line 58: `import config` only) — no reference to `core.py` anywhere in the file (confirmed by full-file grep); `POOL_FLOOR`/`BASE_POOL`/`resolution_pool()` live in `core.py`, absent from the generated JSON | N: the typed-export pipeline is exactly the CLAUDE.md §5/§6 successor discipline. Ω: fails its own headline claim — cannot emit Combat Pool, the single most multiply-defined value in this lane. Q: catastrophic — a Godot implementer following "regenerate, never hand-transcribe" finds no Pool formula, reintroducing the exact ED-1050 failure mode. | **REFINE** — extend the import to cover `core.py` (or relocate constants into `config.py`) | n/a | P1 | KNOWN-UNTRACKED — `ED-1052` (open) cites this exporter as a template but its text (confirmed) doesn't name this specific coverage hole | Sharpens ED-1052 into one concrete next slice | SURVIVED, fully confirmed line-for-line — the strongest and cleanest finding in the lane. |
| 15 | TN judgment call — Controlled/Standard/Desperate (`core.md` §TN Values, prose "When" column) | N: differentiating pressure states is a real dynamic. Ω: as specified (pure prose: "Prepared, unhurried, favourable" / "Duress, exhaustion, existential threat") it cannot fire autonomously under the no-GM charter. Q: fails "composes without special-casing." **New evidence**: confirmed by grep that `sim/autoload/sigma_leverage.py` and `combat_engine_v1/core.py` hardcode `TN = SL.TN_STANDARD` everywhere — Controlled (TN 6) and Desperate (TN 8) are not selected anywhere in the executable layer today. The three-register distinction is currently inert in code, not just under-specified in prose. | **REFINE** — three-register concept survives; the "When" column must become state-readable trigger conditions (or an explicit Standard-default rule) | n/a | P2 | NEW | Prevents N divergent per-subsystem TN-trigger implementations | **STRENGTHENED** — confirmed at the code level that Controlled/Desperate are currently unreachable in the live engine, which sharpens (not weakens, per the as-if-built cardinal rule) the case that the trigger table must be written before any subsystem invents its own ad hoc TN-selection logic. |
| 16 | Discrete d10 vs continuous Normal-sample dual-mode (`core.md` §Die Rule at line 10 / §Continuous Engine at line 58 / Adoption note) | N: both are legitimate — different deployment targets (TTRPG vs Godot), confirmed by the Adoption note's exact text. Ω: continuous alone pays for itself; discrete's co-equal, first-encountered doc position imposes an authoring tax without changing player-facing behavior. Q: fails at the document level (Die Rule precedes the Adoption note that clarifies which mode is live) even though fine at the player level. | **REFINE** — keep discrete as a labeled legacy-TTRPG appendix; reorder `core.md` | Instance A (discrete = legacy twin) | P3 | NEW | Shrinks item 18's chain by one step | SURVIVED — minor, correctly scoped as cosmetic (P3), no basis found to escalate or dismiss. |
| 17 | Momentum auto-success interaction — flat +1/+2 to net, cancellable by a rolled 1 (`core.md:84-93,172-180`, PP-243, confirmed exact text) | N: rewarding decisive prior success with reliable follow-through is real. Ω: fails — per the diagnostic skill's flat-shift trap (verified verbatim, `SKILL.md:87`: `Δz = X/(0.8·√Pool) ∝ 1/√Pool`, "hot at small pools"), Momentum's spend is textbook unscaled leverage, hottest at the 5D Combat Pool floor — same defect class as ED-875 (confirmed: Command-leverage, OPEN, Gate G8). Q: legible in isolation but silently violates the engine's own uniform-leverage guarantee. **Additional finding**: grepped `combat_engine_v1/` and `sim/` — the personal-combat Momentum resource (0–4, auto-success spend) is not implemented in code anywhere yet (the only "momentum" hits in `sim/` are unrelated: mass-battle physics momentum, social-contest "room momentum," and faction "Institutional Momentum" — three different concepts sharing the word). | **REFINE** — Momentum-as-resource stays; net-conversion must move onto the σ_N-scaled axis (Ob-shift/margin bonus of fixed δ/σ_N) | Instance A — P-ii FAIL, see consolidated block | P2 | NEW | Reuses the ED-875 remediation pattern; closes a latent finding before a dedicated Momentum audit re-discovers it | **STRENGTHENED** — the math citation was checked against the skill file verbatim (exact match) and the mechanic is confirmed not yet coded anywhere, meaning per the charter's own stated purpose (catch it before deployment spends effort) this is the cheapest possible moment to fix it. |
| 18 | The 5–6-step check-resolution cognitive chain (attribute pick → History/Recall cap → TN → Ob assembly → pool arithmetic → Momentum → Degree band) | N: each sub-decision is independently load-bearing (verdicted above) — the chain is a necessary composition, not an extraneous mechanism. Ω: costs nothing extra *if the engine executes it* (no-GM charter) — the finding measures designer/auditor load, not player load. Q: fails hard at the design-doc level (six lookups spread across `core.md` + `descriptor_registry.yaml` + BG tables, as this very verification exercise had to reconstruct) even where fully legible player-side (UI surfaces only the final degree). | **REFINE** — not of any mechanism, but of presentation: author one consolidated "how a check resolves" reference | n/a | P2 | NEW | Doubles as the human-readable companion to CLAUDE.md §5's typed-params recommendation | SURVIVED — this REFINE targets documentation, a slight stretch of "simplify the design," but it's an explicit, defensible adaptation for a legibility-audit lane rather than an overreach. |

## Consolidated NERS engine verdict (rolling-engine components: items 3, 7, 10, 11, 13, 16, 17) — unchanged from dossier, independently re-verified

```
ENGINE: Continuous σ-resolution engine (Decision E) + combat_engine_v1 parameterization + legacy d10 twin
INSTANCE: A (sigma-leverage) — discrete d10 is a statistically-equivalent legacy expression (Phase 5
          validation, core.md:66, confirmed: "max deviation 0.029 in mean, 0.022 in std at pool sizes 5-17");
          faction-scale bare 1-7D pools (item 11) are an unresolved Instance A/B boundary case.

VERDICT: NERS non-compliant — N/S pass, E marginal-pass, R FAILS at two independently stress-surfaced points.

R: FAIL —
   1. Momentum flat leverage (item 17, P-ii): confirmed Δz ∝ 1/√Pool, hottest at the 5D floor — same
      defect class as ED-875 (confirmed OPEN, Gate G8), and confirmed not yet ported to code.
   2. Faction-scale bare-pool ambiguity (item 11, P-v/Decision Rule): core.md:78 admits no validation
      sim was run at this boundary; per the confirmed Decision Rule text, some pools may belong on
      Instance B (ED-874 precedent) rather than unvalidated Instance A.

N: PASS — confirmed no redundant roll; DECISIVE_OB=3 is ratified architecture, not accidental dialect.
S: PASS — confirmed parity-tested to 1e-9 (ED-1085 ledger text verified).
E: MARGINAL PASS — continuity correction (net − (Ob−0.5), confirmed landed 2026-06-22) restores
   discrete/continuous agreement to ~1-3%; doc-ordering (item 16) and fixed-Ob under-documentation
   (item 13) remain live elegance/legibility costs.

REMEDIATION (worst-first, unchanged): P2 Momentum flat-leverage → P-ii fix mirroring ED-875 pattern;
P2 Faction-scale bare-pool ambiguity → resolve Instance A/B assignment before treating as "just needs a sim";
P2 combat_engine_v1 fixed-Ob under-documentation → cross-reference note in core.md;
P3 discrete/continuous doc ordering → reorder core.md.
```

## Per-lane rollup (corrected)

Independent re-verification confirms this lane's central pattern: the *mechanisms* mostly earn their place, while the **surrounding apparatus — registries, exports, cross-references, and one un-scaled leverage lever — is where the lane bleeds.** Every citation checked against the live working tree resolved cleanly, with three exceptions worth flagging on their own: (1) the dossier's own rollup paragraph miscounted its verdicts (claimed "8 KEEP" listing only 5 item numbers; the table itself, now re-verified, shows **6 KEEP**); (2) item 1's "18 dead links" is actually **17** (params/board_game.md has 17 `## [...]` header links, all dead); (3) item 2's "purely decorative" framing undersold a real historical fact — "quasi-binomial"/"quasibinomial" names a genuine superseded v15 architecture, not an empty label, which raised its severity from P3 to P2. Beyond these corrections, no verdict failed the adversarial KEEP-attempt: the PRUNE (item 4) survives because the registry's own stated purpose is current-value extraction, not archive (history is preserved elsewhere); the REFINEs survive because in every case the underlying mechanism is sound and only its current instantiation, documentation, or scaling is broken. Three items gained materially new, independently-discovered evidence: item 5 (ED-1084 explicitly claims to have already fixed `params/core.md`, yet the false Fieldwork-Pool cross-reference at line 252 still stands — a confirmed gap in a supposedly-closed remediation, not merely an unlisted site), item 15 (Controlled/Desperate TN is confirmed dead-in-code today — `TN = SL.TN_STANDARD` is hardcoded everywhere — sharpening the urgency of a state-readable trigger table), and item 17 (the Momentum flat-leverage defect is confirmed to exist purely at the design-prose level, since the mechanic isn't implemented in `combat_engine_v1` or `sim/` yet at all — the cheapest possible moment, per the charter's own stated purpose, to fix it). The standout finding remains: Combat Pool is simultaneously the healthiest mechanism in the lane (KEEP, clean, triple-cross-checked formula) and the worst-served by its surroundings — a broken index that used to obscure it (tracked, ED-FA-0003), a quarantined registry that still misstates it (PRUNE, item 4), a live in-file sentence that still misstates it despite a remediation that claims to have already fixed it (P1, item 5), and an export pipeline confirmed to import only `config.py` and never touch `core.py`, so it cannot emit Combat Pool at all (P1, item 14, confirmed byte-for-byte) — four independent, verified failure surfaces around one sound value.

**18 elements: 6 KEEP / 10 REFINE / 1 DISTILL / 0 MERGE / 1 PRUNE / 0 CUT (post-critic).**

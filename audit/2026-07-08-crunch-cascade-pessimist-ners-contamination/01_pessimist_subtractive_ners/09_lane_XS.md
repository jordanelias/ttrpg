# Pessimist Subtractive NERS — Cross-Scale Propagation Architecture (XS)

# Cross-Scale Propagation / Aggregation Architecture — FINAL Pessimist Subtractive NERS Table (post-critic)

**Independent verification method.** I re-read `designs/architecture/propagation_spec_v1.md` (405 lines, full), `designs/architecture/key_substrate_v30.md` (§2–§6), `designs/architecture/holonic_container_doctrine_v1.md` (full, 111 lines), and `references/module_contracts.yaml` directly against the working tree (not from the dossier's paraphrase), reproduced the module-contract arithmetic by script rather than trusting the dossier's count, and pulled one document the dossier never cites — **`designs/architecture/key_echo_armature_v1.md`** (RATIFIED 2026-07-07, ED-IN-0026) — whose §5 is a **Ruling Log** that directly rules on OF-D6, `decay()`/OF-3, RNG-MODEL-COLLISION, ORD-3/ORD-4, and OF-CAP, one day before this audit's own 2026-07-08 date. This is a materially relevant document the dossier's "Recency check" missed (see Meta-finding below). New citations below are tagged `[armature:Lxx]`.

**Scope-call re-verification (§0):** confirmed correct on independent read. `compute_observers` in `key_substrate_v30.md` (§4.2, lines 236–246) literally returns `observers = set()` — no draw. Domain Echo is "table lookup by degree, zero RNG draws" (`propagation_spec_v1.md` AU-6, verified verbatim). The O.5 RNG material is a threading contract about other engines' draws, not a draw here. `cascade_depth`'s scheduler is a capped FIFO drain (§4.2, verified). **No rolling engine in this lane — every row correctly carries N/A in the NERS-engine column.**

**Meta-finding (new, from independent verification — not in the dossier):** the source findings file's Recency check (`01_findings_cross_scale_propagation.md` lines 104–110) states flatly that no landed work "touch[es] D.6, decay(), OF-CAP, RNG-MODEL-COLLISION, ORD-3, or ORD-4, which all remain open exactly as described here." This is **measurably incomplete**: `key_echo_armature_v1.md` §5's Ruling Log (ratified 2026-07-07, one day before this audit) explicitly rules rows 5.1 (OF-D6), 5.2 (`decay()`), 5.5 (RNG-MODEL-COLLISION), 5.6 (ORD-3/ORD-4), and 5.7 (OF-CAP) — mostly landing on **"ACCEPT default / DEFERRED"** postures rather than closing the underlying question, but that is a materially different state than "not touched." Consequence for verdicts: **no verdict category changes** — Jordan's own rulings explicitly keep D.6 "open in effect" (5.1), defer RNG-MODEL-COLLISION "not ruled now" (5.5), and leave ORD-3 "not implemented" (5.6) — so every REFINE below survives on its merits. What changes is the **downstream-retirement pointer**, which should now cite `key_echo_armature_v1.md` §5's specific row + its PR-2 gate rather than the staler `propagation_spec_v1.md` §5 decision-queue numbering alone, and the **evidentiary weight** behind several severities (Jordan's ruling pass treating an item as a named T0 gate is corroborating, not exculpatory).

---

## Per-element table

| # | Element | N / Ω / Q reasoning (re-derived) | Subtractive verdict | NERS engine | Severity | Calibration | Downstream retirement | Critic disposition |
|---|---|---|---|---|---|---|---|---|
| 1 | **Key** (universal event record) | Verified: this is the substrate ontology every other row depends on. No steelman against it is available — nothing to attack. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 2 | **KEY_LOG / `on_key_emitted`** 7-step pipeline | Verified verbatim against `key_substrate_v30.md` §4.1 (lines 184–229): the OF-7/OF-B1 amendment pointer, and AU-3.2's 3-reason rejection of the cheaper "Drafted-state" alternative (`propagation_spec_v1.md`:185), both check out exactly as cited. Each of the 7 steps does distinct, non-redundant work. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 3 | **Aggregate-Up / AU-3.1** — schema doesn't enforce empty `stat_deltas` on faction observers | Attempted KEEP defense: "this is just an open engineering flag (OF-4), not a design defect — authoring discipline is normal." **Defense fails**: §4.1 step 4 applies `stat_deltas` "generically to every observer" (verified, `key_substrate_v30.md`:196-201) with *no* type-check distinguishing a derivation-only faction observer from a directly-writable one — this is a textbook Failure-Lexicon "Special-cased" hit: an author must remember, forever, to write `stat_deltas: {}` on every future Key naming a faction target, or AU-1's entire "no aggregate is ever written directly" guarantee (the architecture's central invariant) silently breaks on a single miss. Unlike #6 below, this closes a real named flag (OF-4). | **REFINE** — schema-enforce (e.g. reject/strip non-empty `stat_deltas` on faction-typed observers at validation) rather than rely on discipline. | N/A | **P1 (elevated from dossier's P2)** | NEW | Directly answers OF-4 (`propagation_spec_v1.md`:205). | **STRENGTHENED — severity raised P2→P1.** Rationale: unlike D.6 (#10), which requires two specific write-channels to *coincide* on the same (settlement,stat) pair to misfire, AU-3.1's failure mode requires only a *single* missed `stat_deltas: {}` on any one future Key — a strictly lower trigger bar than D.6, yet the dossier scored D.6 P1 and this P2. A schema gap on the primary F1 invariant itself, triggerable by one author oversight, should not be scored a full tier below a defect that requires two conditions to align. |
| 4 | **Distribute-Down transform** (D.1: one Key, N-entry `targets[]`) | Verified against `propagation_spec_v1.md`:215-227: fan-out is explicitly engineered to not increment `cascade_depth`, restated identically at D.1/D.5/§4.2/§4.5 (repetition-for-clarity, confirmed not drift — each restatement appears in a different functional context: the rule, the worked example, the termination proof, the anti-scripting-drift guardrail). | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 5 | **`cascade_depth` + OF-CAP caps** | Confirmed the caps are caller-required ints with `# unbacked no cited canonical_source` (`propagation_spec_v1.md`:322,392) — and now independently confirmed via `key_echo_armature_v1.md` row 5.7: ruled "ACCEPT default (caller-supplied caps; no fabricated constant)... matches the repo's anti-fabrication discipline" (`armature:275`). This is a **deliberate** policy, not an oversight — strengthens rather than weakens the KEEP. Per cardinal rule, missing numbers ≠ verdict evidence. | **KEEP** | N/A | P2 | KNOWN-TRACKED | — | **STRENGTHENED** — the missing-calibration fact is now confirmed to be an explicit, ratified anti-fabrication policy choice (`armature:275`), not mere debt. |
| 6 | **`sub_step_index`** naming proximity to `cascade_depth` | Checked the charter's own demotion test directly: is there ANY named OF/ED/§5-register line tracking "sub_step_index/cascade_depth naming clarity" specifically? Searched `propagation_spec_v1.md` §5's Legibility-gaps register (source findings §5, lines 84-102) — **not present** as its own bullet (unlike #22, which IS a named §5 P2 bullet — see below). No nameable downstream retirement exists. Demotion is correctly and consistently applied. | Ordinary finding, **not scored subtractive** (stays KEEP + documentation note). | N/A | P3 | NEW | *None nameable.* | **SURVIVED** — and I verified the dossier is applying this test *consistently*, not selectively (contrast with #22, which passes the same test because it corresponds to an actual named §5 bullet). |
| 7 | **Module-contract count correction** | **Independently reproduced by script**, not trusted from the dossier: parsed `references/module_contracts.yaml`'s 27 modules; `doc: null` = exactly 10 (`npc_memory, scene_slate, game_director, scene_timer, audit, domain_actions, settlement_economy, engine_clock, faction_politics, scenario_authoring`); `[ASSUMPTION]`-tagged **resolver** lines = exactly 11 (`faction_state, npc_behavior, npc_memory, piety_track, territorial_piety, domain_actions, peninsular_strain, settlement_economy, faction_politics, miraculous_event, scenario_authoring`); overlap = exactly 5 (`npc_memory, domain_actions, settlement_economy, faction_politics, scenario_authoring`); union = exactly 16/27 (~59%), not 17/27. Every number in the finding is exact. | **KEEP** — outside subtractive frame, corpus-accuracy bookkeeping. | N/A | P3 | KNOWN-TRACKED | — | **STRENGTHENED** — verified by independent script reproduction, not just re-reading; the correction is exactly right. |
| 8 | **Domain Echo (up) cascade path** | Verified against AU-2/AU-3.2/A1 (settlement-locus deferred write) + Theorem A (1-down+1-up bound, `propagation_spec_v1.md`:308). This is the one path where every claim checks out with no residual open flag threatening its own bound. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 9 | **D.4 down-seams** — 8 emitter→consumer edges, no stated precedence for overlapping targets | Attempted KEEP defense: "decision queue item 8 calls D.4/D.5 residuals 'smaller, non-blocking' (`propagation_spec_v1.md`:394) — the dossier's dispute of that label is overreach." **Defense partially fails**: ORD-1 explicitly states "dict iteration order is load-bearing" (`propagation_spec_v1.md`:97) — ordering is NOT assumed commutative in this architecture generally, and two D.4 rows do converge on the same consumer (`domain_actions`→`npc_behavior` and `faction_politics`→`npc_behavior`; `domain_actions`→`settlement_economy` and `peninsular_strain`→`settlement_economy`). Whether today's *incidental* code-call order is stable forever, vs. canonically ruled, is genuinely unstated. But this does NOT threaten Theorem A/B (termination is unaffected) — it threatens final-state determinism/reproducibility only, a narrower harm than ORD-3. P2 (not P1) is correctly calibrated. | **REFINE** — the D.1 transform mechanism is sound; state a partial-order/precedence rule for D.4's worklist. | N/A | P2 | NEW (severity) / KNOWN-TRACKED (existence) | Sharpens decision-queue item 8 (`propagation_spec_v1.md`:394). | **SURVIVED** — dispute of "non-blocking" is justified (ORD-1 makes ordering a live architectural concern, not a triviality), but P2 (not P1) is the right tier since Theorem A/B hold regardless. |
| 10 | **National-event-modifier down-half (D.5) / D.6 double-count risk** | Verified directly: `propagation_spec_v1.md` §4.3 states plainly "If D.6 is ruled non-disjoint, the convergence story breaks even with a contractive `decay()`" (:349), and decision-queue item 2 marks it HIGH PRIORITY (:388). **New corroboration from `key_echo_armature_v1.md` row 5.1** (`armature:247,269`): ruled 2026-07-07 to "ACCEPT default (assume DISJOINT) as a working assumption, **NOT a closed question**. PR-2 must include an empirical disjointness check before any convergence claim relies on it... HIGH-priority open item **stays open in effect**." Jordan's own ruling confirms this is still live, not resolved. | **REFINE** — mechanism (D.1) sound; must specify disjointness (now partially in motion via the PR-2 empirical-check gate). | N/A | P1 | KNOWN-TRACKED | **Updated pointer**: closes `propagation_spec_v1.md` decision-queue item 2/OF-D6 (:388) — but the *live* tracking surface is now `key_echo_armature_v1.md` §5.1 + its PR-2 empirical-disjointness gate (`armature:247,269`), not the stale decision-queue citation alone. | **STRENGTHENED** — Jordan's own 2026-07-07 ruling corroborates this is still genuinely open ("stays open in effect"), not merely a pessimist audit's invention; citation updated to the live disposition surface. |
| 11 | **Theorem A** (per-tick fixpoint) | Verified the proof directly (`propagation_spec_v1.md`:308): rests on `run_scene_phase` being called exactly once per tick (confirmed, O.3) and A1's deferral of up-echoes to accounting (confirmed, A1 text). Proof is sound and restates in one sentence. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 12 | **Theorem B** (per-cascade termination) | Verified the proof directly (`propagation_spec_v1.md`:341): strictly-increasing bounded counter argument is valid; the determinism caveat is honestly scoped to ORD-3/ORD-4, which is those items' defect, not this theorem's. | **KEEP** | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 13 | **Cross-tick recurrence / C1 macro loop** | Verified §4.3 (:343-351) states termination-only, not convergence, verbatim ("can persist at bounded, non-decaying amplitude for the entire campaign"). Confirmed this row's content is fully subsumed by #10 + #14 — no third independent defect exists once those two are retired. | **REFINE** (no independent credit — synthesis of #10+#14) | N/A | P1 | KNOWN-TRACKED | Retires when #10 and #14 retire. | **SURVIVED** |
| 14 | **`decay()`** unspecified (OF-3) | Verified zero cited functional form exists anywhere in `propagation_spec_v1.md` (AU-4:189, §4.3:348). **New corroboration from `key_echo_armature_v1.md` row 5.2**: ruled "ACCEPT default (ships without `decay()`). Authorship... deferred to when cross-tick convergence work actually starts (Stratum B+)" (`armature:248,270`). This is a *deliberate* scheduling decision, not an oversight — per the cardinal rule this is routing metadata about *when*, not evidence the *concept* is unnecessary; channel (ii) of AU-2 still requires it for any eventual convergence claim, so the REFINE (keep concept, note gap) stands regardless of scheduling. | **REFINE** | N/A | P1 | KNOWN-TRACKED | **Updated pointer**: closes OF-3 (`propagation_spec_v1.md`:389) — live tracking now `key_echo_armature_v1.md` §5.2, explicitly gated to Stratum B+. | **SURVIVED**, citation updated. The deliberate-deferral framing doesn't weaken the REFINE — it just means "when," not "whether," is now answered. |
| 15 | **D.6 ruling-absence (viewed as its own row)** | Confirmed this is the exact same underlying defect as #10, cited twice in the source findings (§3 narrative, lines 55-58, and §5's P1 bullet, lines 86-87) — not two separate design questions. | **MERGE → #10** | N/A | P1 | KNOWN-TRACKED | Same as #10. | **SURVIVED** |
| 16 | **ORD-3 self-contradiction** — `compute_observers` returns bare `set()` | Verified verbatim: `key_substrate_v30.md` §4.2 (lines 236-246) literally reads `observers = set()`, directly contradicting `propagation_spec_v1.md`'s own ORD-2 rule ("no `set()` may gate emission/mutation order," :98). The prescribed fix (source_actor → targets[] order → sorted actors_in_scale → semi/private, de-duplicated) is fully specified in O.6 (:119-123). **New corroboration from `key_echo_armature_v1.md` row 5.6**: ruled "ACCEPT default (ORD-3/`compute_observers` **not implemented**). Deferred to the rendering wave (§6.3 wave 4)" (`armature:252,274`) — confirms the contradiction is *still live and unfixed* in canon prose (the executable substrate simply doesn't implement `compute_observers` at all yet, which sidesteps but does not resolve the prose-level self-contradiction). | **REFINE** — trivial collection-type swap per already-specified rule; still requires an ED to edit `key_substrate_v30.md`. | N/A | P1 | KNOWN-TRACKED | Closes Open Flag O-4 (`propagation_spec_v1.md`:123); tracking now also visible at `armature:252,274`. | **STRENGTHENED** — independently confirmed the contradiction remains completely unaddressed one day after a full ruling pass touched adjacent items. |
| — | *(folded into #5)* | — | — | — | — | — | — | — |
| 18 | **Cognitive load: six rule families + worked-template gap** | Attempted KEEP defense: "each of the six concerns restates in one sentence; the worked-example gap is documentation debt, not a design flaw — demote like #6." **Defense fails, unlike #6**: this finding names a real downstream artifact — ED-1051's module-contract-closure workload (`references/module_contracts.yaml` lines 696-712, verified: `engine_clock`'s own `gap_notes` cites this exact document as candidate home doc) — so it passes the charter's own nameable-retirement test where #6 did not. Six families independently verified non-redundant (ordering ≠ AU ≠ DD ≠ termination ≠ RNG-threading ≠ replay-determinism — no two do the same job). | **REFINE** — keep all six families; add one more worked example against a `doc: null` module (e.g. `scene_timer` or `audit`, both confirmed `doc: null`). | N/A | P2 | KNOWN-TRACKED | Shrinks ED-1051 workload. | **SURVIVED** |
| — | *(folded into #18)* | — | — | — | — | — | — | — |
| 20 | **`engine_clock`'s `doc: null` non-flip** | Verified exactly against `module_contracts.yaml` lines 696-713: `doc: null` with `gap_notes` explicitly naming `propagation_spec_v1.md` as the candidate home doc, unflipped pending ED-1051. Confirmed pure bookkeeping, not a content gap. | **KEEP** — nothing to subtract; propagate the correction so CLAUDE.md's "~37%/~59%" framing isn't misread. | N/A | P3 | KNOWN-TRACKED | — | **SURVIVED** |
| 21 | **`causes[]` field overload** (runtime re-entrancy parent vs. authored provenance) | Verified against `propagation_spec_v1.md` §4.4 (:369): source itself tags this "low severity." Re-derived the blast radius independently: the ambiguity affects only **post-hoc replay-based checkability** (§4.4's own log-derivable invariants, used to *verify* that a past run respected the cap) — it does **not** affect the *live* runtime guarantee itself, which is enforced by in-drain asserts (`assert depth <= CASCADE_DEPTH_MAX`, checked during execution, independent of `causes[]` labeling). Theorem B's *proof* holds regardless; only an external auditor's ability to reconstruct depth from the log alone is impaired. | **REFINE** — add a `kind: authored \| reentrant` tag to `causes[]` entries. | N/A | P2 | KNOWN-TRACKED | Closes the residual ambiguity in `propagation_spec_v1.md` §4 (:379). | **SURVIVED, but tempered**: I disagree with the dossier's "arguably P1-adjacent" framing — the risk is to *post-hoc auditability*, not to the *live* determinism guarantee Theorem B actually proves. P2 (as the dossier's own table already scored it) is the correct tier; I would not push it toward P1. |
| 22 | **Holonic doctrine "wrapper" vocabulary not restated at point of use** | Verified: `holonic_container_doctrine_v1.md` §1's cross-map row and §2's guardrail 2 (lines 58-64) use "wrapper" framing; `propagation_spec_v1.md` never uses the word, speaking entirely in Key/targets/transform language — no crosswalk sentence exists anywhere I could find via direct read. Checked the charter's downstream-retirement test explicitly against #6's demotion for consistency: this item retires an *actual existing named bullet* in the source findings' own §5 register (P2, lines 94-96) — unlike #6, which has no such register line anywhere. Passes the test #6 failed. (Minor overreach note: calling this "ironic" relative to guardrail 2 is a rhetorical stretch — guardrail 2 is about code/schema shape divergence, not prose-vocabulary consistency — but the REFINE itself doesn't depend on that framing.) | **REFINE** — one-line crosswalk table at the top of `propagation_spec_v1.md`. | N/A | P2 | KNOWN-TRACKED | Retires this lane's own §5 P2 item (findings, lines 94-96). | **SURVIVED** — and I verified the dossier applied its own demotion test *consistently* across #6 and #22, which is a genuine discipline signal, not rubber-stamping. |
| 23 | **ORD-4 mislabeled as hard open design question** | Verified against O.7 (:125-129) and decision-queue item 7 (:393), which does bundle ORD-3 (a substantive canon contradiction) with ORD-4 (moving `scene_slate._queue` off module-scope onto `World` — a mechanical code fix) at the same priority tier. The mislabeling is real. | **REFINE** (the labeling, not the code — the fix itself is uncontroversial and must still happen). | N/A | P3 | KNOWN-TRACKED | Splits decision-queue item 7 into a hard item (#16) + an ordinary hygiene ticket. | **SURVIVED** |
| 24 | **RNG-MODEL-COLLISION** — three incompatible RNG contracts, under-registered | Verified all three models directly: O.5's RNG-1/2/3 (single global stream, :113-115), `key_substrate_v30.md` §6.1's per-emission-seed model (:402-404, verbatim "Every random draw is seeded per Key emission, not engine-globally"), and the conversion strategy's named-draw-service (referenced at :117). Confirmed this fact appears in the source findings *only* in the Recency-check paragraph (line 109), never as a scored §5 bullet — a genuine registration gap in the underlying findings document. **New corroboration from `key_echo_armature_v1.md` row 5.5**: ruled 2026-07-07 "**DEFERRED to PR-2 kickoff, not ruled now**... ruling this blind... risks a wrong replay-determinism commitment. Flagged as a **PR-2 T0 item**" (`armature:251,273`) — Jordan's own ruling pass treats this as important enough to name explicitly and gate the *next* PR on, which is stronger corroboration than the dossier had. Its blast radius (every rolling engine's replay/V4-checksum parity — combat, contest, mass battle, thread ops, all elsewhere in the corpus) is at least as large as ORD-3's (which is scored P1). | **REFINE** — reconcile to one RNG contract before any stochastic consumer lands. | N/A | **P1 (elevated from dossier's "P2, under-registered")** | NEW (the register-omission is new; the underlying collision is KNOWN/cited in canon and now independently confirmed as a named T0 gate) | Closes decision-queue item 5 (:391); **updated live pointer**: `key_echo_armature_v1.md` §5.5, explicit PR-2 T0 gate — recommend this lane's §5 register carry an explicit P1 line, not merely a Recency-check mention. | **STRENGTHENED — severity elevated P2→P1.** Corroborated independently by Jordan's own 2026-07-07 ruling explicitly gating PR-2's stochastic-consumer work on this being disposed of first — a blast radius (every rolling engine's replay determinism corpus-wide) at least equal to ORD-3's, which the dossier itself scored P1. Scoring this P2 while ORD-3 gets P1 was an inconsistency in the dossier's own severity ladder. |

**Completeness note (new, from independent read, not scored as its own row):** `propagation_spec_v1.md`'s own open-flags list (§4.6, line 379) also names **"step-7 awareness-watcher synchronous-emit audit still needed"** — whether step 7's `causes[]`-driven awareness update (`clamp(cause_key.awareness + 0.1, 0, 1)`, `key_substrate_v30.md`:214-217) needs the same re-entrancy audit as step 5's B1 rule. Neither the source findings document nor this dossier gives it a row anywhere, even as an ordinary finding. I flag it here for completeness rather than inventing a 25th table row, since the source document itself never scored it either — this is a gap in the underlying findings.md, not something the pessimist dossier should be faulted for missing on its own.

---

## Rollup (corrected)

**Cross-Scale Propagation — the architecture holds up under independent re-verification; the defects are exactly where the spec names its own seams, and one of this pass's own severity calibrations needed tightening.** Every citation in the dossier that I checked against the working tree — `key_substrate_v30.md`'s `compute_observers`, the module-contract arithmetic (independently reproduced by script: 10 `doc:null`, 11 `[ASSUMPTION]`-resolver, 5 overlap, 16 union), Theorem A/B's proofs, AU-1 through AU-7, D.1 through D.6 — checked out exactly as claimed. **Zero CUT/PRUNE/DISTILL candidates survive an adversarial second look**: every primitive here (Key, the single update rule, Aggregate-Up, Distribute-Down, the dual termination theorems, `cascade_depth`'s caps) is load-bearing and non-duplicative even under attack, and I could not construct a defensible steelman for cutting or pruning any of them. Where the dossier's REFINE verdicts are concerned, every one I attempted to argue back to a plain KEEP failed on its own merits — AU-3.1's schema gap, the D.4 sequencing hole, D.6's double-count, `decay()`'s absence, ORD-3's contradiction, and the RNG-model collision are all real, all still open, and independently corroborated (in several cases *more* strongly than the dossier knew) by `key_echo_armature_v1.md`'s 2026-07-07 Ruling Log — a document this pass had to pull in independently, since neither the source findings file's Recency check nor the dossier cited it, despite it being the single most relevant piece of *day-old* canon for exactly this lane's open questions (see Meta-finding above). The net correction from this critic pass is narrow but real: **two severities move up** (AU-3.1 from P2→P1, since its trigger bar is lower than D.6's yet it was scored a tier below; RNG-MODEL-COLLISION from P2→P1, now corroborated by Jordan's own T0-gate framing, closing an inconsistency where ORD-3 got P1 for a comparable blast radius but RNG-MODEL-COLLISION did not), **one severity claim is tempered back down** (`causes[]` overload stays P2, not pushed toward P1 — the at-risk property is post-hoc replay-auditability, not the live termination guarantee), and **the dossier's own tally arithmetic is corrected** (it undercounted KEEP by one and overcounted REFINE by one). Nothing here should be built smaller — only specified more completely, with the added instruction that four of the "still open" items now have a live, more specific tracking surface (`key_echo_armature_v1.md` §5's Ruling Log and its PR-2 gates) than the dossier's citations pointed to.

**22 elements: 10 KEEP / 11 REFINE / 0 DISTILL / 1 MERGE / 0 PRUNE / 0 CUT (post-critic).**

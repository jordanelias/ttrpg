# Execution ledger — code-shape open-items program (ED-IN-0091)

## Role

This file is the program's **single STATUS surface**. `00_open_items_register.md`,
`01_orchestration_plan_v1.md`, and `02_disposition_map.md` are immutable snapshots taken at
program start (plus the 2026-07-29 adversarial reconciliation baked into the disposition map) —
**they do not change as waves execute.** Every wave (W0–W5) appends its rows here, and W5's
capstone additionally completes the diff against `02_disposition_map.md` (closing the
created-by-W5 / appended-by-every-wave tension the orchestration plan otherwise leaves open —
flagged explicitly in the W0b PR body). If a register/plan/map claim turns out stale or wrong
during execution, the correction lands as a row here, not as an edit to those files.

## Column format

`OI/item · wave · PR · ED · falsifier artifact · outcome`

## Rows

| OI/item | wave | PR | ED | falsifier artifact | outcome |
|---|---|---|---|---|---|
| W0a — 7-lane pre-allocation + `id_reservations.yaml` file freeze (no per-item OI; reservation only) | W0a | #256 | (reservation only, no ED filed) | `references/id_reservations.yaml` lane lines (IN 0092–0111, MB 0046–0060, PC 0041–0055, WR 0009–0012, FA 0036–0039, SE 0049–0052, SC 0017–0020; file frozen until W5) | done |
| §5 Jordan docket authored, HELD FOR JORDAN (rulings not yet made) | W0b | (this PR) | ED-IN-0092 | `05_jordan_docket_v1.md` | open — awaiting Jordan |
| OI-55 — orphan-detector integrity: `__init__` relative-import misresolution, CLI-entry-point noise, `vector_audit.py` known-answer coverage, re-scoped at W0b | W0b | (this PR) | ED-IN-0092 | `skills/valoria-vector-audit/scripts/structure_audit.py` diff + `tests/valoria/test_structure_audit.py` + `tests/valoria/test_vector_audit.py` (new known-answer cases) | landed |
| G12 correction — the `__init__` misresolution half of OI-55 was already fixed pre-W0b; re-verified not re-done | W0b | (this PR) | ED-IN-0092 | `structure_audit.py` lines ~271–278 (existing fix comment), confirmed by re-read, no duplicate change made | verified stale claim, no-op |
| G12 correction — the "no known-answer coverage beyond one total-pin" claim was already stale pre-W0b; re-verified not re-done | W0b | (this PR) | ED-IN-0092 | `tests/valoria/test_vector_audit.py` (pre-existing coverage beyond a single total-pin case), confirmed by re-read, no duplicate change made | verified stale claim, no-op |
| execution ledger created (this file) | W0b | (this PR) | ED-IN-0092 | this file's existence + first two waves' rows | done |
| critic relay (W0b) — 3 lanes upheld, 4 fixable defects folded in (docket OI-41 row, 2 stale citations, cli_entries caption + conservation test) | W0b | (this PR) | ED-IN-0092 | the critic report in the W0b PR body + this file's rows | done |
| duplication logged, not chased (§0.1 #5): __main__-guard predicate now defined in structure_audit.py (AST) AND build_apparatus_registry.py:116 (regex, pre-existing, behaviorally different) — single-owner consolidation routed to Wave 4 item 5 (which owns build_apparatus_registry inconsistencies, OI-15) | W0b→W4 | (this PR) | ED-IN-0092 | Wave 4's dedup mutation check when consolidated | open — routed to W4 |
| stale surface found by critic (OI-53 class): build_apparatus_registry.py:232-234 globs the deleted designs/audit tree, silently degrading its orphan signal — routed to Wave 4 item 3's re-verify list | W0b→W4 | (this PR) | ED-IN-0092 | W4's extended dead-root guard test | open — routed to W4 |
| OI-56 — pipeline-reach acceptance oracle: `engine/tests/test_pipeline_reach.py` (new), asserting per-direction coverage with `assert checked >= N` (CLAUDE.md §0.1 point 2), one `XFAIL_MANIFEST` row per still-unwired direction (dynamic `strict=True` xfail keyed to live introspection, not a hardcoded red) | W1 | (this PR) | ED-IN-0093 | `engine/tests/test_pipeline_reach.py` — POST-FIX-BATCH reality (corrected, see fix-round-2 row below): 21 test functions, 13 passed + 8 xfailed, decorative unconditional counters removed (the pre-fix-batch "29 passed" figure was measured before the fix batch's test restructuring and does not reflect the final tree) | landed |
| OI-01 — combat dispatch bridge, FLAG-OFF: `engine/cross_scale/combat_bridge.py` (new, IN-side only, mirrors `_emergency_council_parties`/ED-SC-0006), wired behind `DISPATCH_COMBAT_BRIDGE` (default off) in `scene_dispatch.py`; `engine/tests/test_combat_bridge_seam.py` (new) is a SHAPE/CONTRACT-LEVEL-ONLY characterization test against `combat_engine_v1/wrapper.py`'s public API (never a value/balance pin). ON-flip deliberately NOT scheduled in this wave — happens only after PC batches E0–E3 merge, as a named single-owner (IN) golden re-record citing both plans | W1 | (this PR) | ED-IN-0093 | `engine/tests/test_combat_bridge_seam.py` (shape-only), `test_pipeline_reach.py`'s `combat-bridge-on` XFAIL_MANIFEST row (id verified against `test_pipeline_reach.py:78`; env `DISPATCH_COMBAT_BRIDGE=1` flips it) | landed, flag OFF |
| OI-02 — field investigation dispatch closure: `fieldwork.py`/`investigation.py` converted from unconditional `NotImplementedError` to self-flagging `stubwire.stub_resolve` calls, reached from `scene_dispatch.py`'s investigation/fieldwork branch (no more silent "not live" deferral) | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py` OI-17 strict-conversion assertions (`systems/fieldwork/sim/{fieldwork,investigation}.py` in the converted set) | landed |
| OI-17 — the Pass-2l armature-stub class converted to `stubwire`: `charter_liberties`, `infrastructure_reclamation`, `home_sanctuary`, `varfell_territorial_acquisition`, `varfell_mandate_action`, `hafenmark_equipment` (factions), `fieldwork`, `investigation` (fieldwork), `rs_track`, `ip_track` (overview), `miraculous_event`, `restoration_movement` (world), `companion` (characters), `rendering` (threadwork), `engine/cross_scale/articulation.py`, `engine/autoload/npc_ai.py` — 16 modules. **Two named exclusions, both accepted cross-session handoffs, neither converted here**: `systems/mass_battle/sim/altonian_reinforcements.py` (MB-owned, routed to MB plan §12 I1, critic F9 — an IN exit criterion may not be hostage to another session's schedule) and `systems/social_contest/sim/contest/{dictionaries,modes,wrapper}.py` (OI-18a/OI-19 own these — self-flag-only scope, see below, not a full OI-17 conversion) | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py` OI-17 strict block (asserts the converted-set membership); `tests/valoria/test_stubwire.py` (mutation-checked: delete the import, audit/telemetry/ratchet all fail) | landed — 16 converted, 2 excluded (see above) |
| OI-18a — contest GAMES router stub rows + mode scaffolds, SELF-FLAG ONLY (plan's explicit scope note, not a full resolver build): `contest/dictionaries.py`, `contest/modes.py`, `contest/wrapper.py` stub rows now self-flag via `stubwire` | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py::test_oi18a_contest_games_router_stub_rows_are_self_flagged`, `::test_oi18a_mode_scaffolds_are_self_flagged` | landed |
| OI-19 — partial `NotImplementedError` branches self-flagged via `stubwire`, live branches left untouched: `tribunal.py:149` (§7 Asymmetric Proceeding), `treaty.py:107`, `contest/dictionaries.py:710`. `resolver.py:51` (abstract base) is **deliberately excluded as recorded-benign** — it is the ABC's own `NotImplementedError`, not a live-path stub | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py::test_oi19_...` partial-branch self-flag assertions, explicit `resolver.py:51` exclusion comment in the same test | landed, resolver.py:51 recorded-benign |
| OI-10a — (folded into the OI-17 conversion pass above; no separate module, no separate row beyond this note — OI-10a's scope was already covered by the OI-17 stub census and converts under the same `stubwire` pass) | W1 | (this PR) | ED-IN-0093 | see OI-17 row's falsifier | landed (subsumed by OI-17) |
| `registers/review_baseline.yaml` `stubs.count` ratchet seeding — measured post-conversion `stub_wired` count (23, via `python3 tools/review_core.py --check` / `tools/structure_audit.py`, 2026-07-29 uncommitted Wave-1 tree) + 1 for the MB-owned `altonian_reinforcements.py` (critic F4, not yet converted) = seed **24**, replacing the Spine-stage placeholder 0 + TODO — **SUPERSEDED same PR cycle by the fix-batch row below (24→25): this row's own 23 measurement predates the fix batch's `_kernel_tests.py` stubwire import** | W1 | (this PR) | ED-IN-0093 | `registers/review_baseline.yaml` `stubs.count` block (arithmetic in the YAML comment); `python3 tools/review_core.py --check` no longer regresses at the new baseline | landed, superseded (see fix-batch row) |
| `registers/review_baseline.yaml` `stubs.count` RE-SEED (the fix-batch row the two rows above and `HANDOFF_IN.md` both point at): the fix batch's contest-kernel guard rewrite added a `from engine.substrate.stubwire import StubResult` import to `systems/social_contest/sim/contest/_kernel_tests.py`, moving that file into the `stub_wired` predicate set (importers of `engine.substrate.stubwire`, per the predicate's own "ceiling, not census" semantics). Re-measured post-fix-batch `stub_wired` count = **24** (was 23) + 1 for the still-unconverted MB-owned `altonian_reinforcements.py` (critic F4) = new baseline **25**, replacing the superseded 24 seed above. `tools/review_core.py`'s `stubs.count` registry comment updated to match (was stale "seeded at baseline 0 with a loud TODO") | W1 | (this PR) | ED-IN-0093 | `registers/review_baseline.yaml` `stubs.count` block (baseline: 25, target: 25, arithmetic + predicate-semantics comment); `tools/review_core.py`'s `stubs.count` entry comment; `python3 tools/review_core.py --check` at the new baseline | landed |
| adjudicator+critic relay (W1): 3 defects + 16 verdicts, all adjudicated by the orchestrator; fixes in this batch | W1 | (this PR) | ED-IN-0093 | this fix-batch diff + the re-critic pass | done |
| re-critic round 2: D1 resolved-before-resolve bug fixed; echo attribution reverted to precedent-degenerate winner-self-echo with the attribution model named a flip precondition; ledger reconciled | W1 | (this PR) | ED-IN-0093 | this row + the diff | done |
| FLIP-BLOCKER (named): OFF/ON context-vocabulary dialect — OFF reads ctx['participants'], ON reads faction aggregates; reconciliation + ctx['echo'] verification + a CI flag-ON job are preconditions of the deliberate DISPATCH_COMBAT_BRIDGE ON-flip (post PC E0–E3). EXTENDED (re-critic round 2, item 2e): a THIRD, separate precondition is the cross-faction echo-attribution design question — `scene_dispatch.py`'s combat branch currently reuses the contest precedent's degenerate self-echo shape verbatim (`actor_faction == target_faction == winner's fid`) because no attacker/defender attribution model is ruled; sign (who is credited on a win, who if anyone is debited on a loss), direction, and magnitudes are all UNRULED. The ON-flip needs (1) the OFF/ON context-vocabulary reconciliation above, (2) a ruled attribution model replacing the degenerate self-echo (or an explicit decision to keep it), and (3) a flag-ON test with `world.echo_scheduler` attached (today's `test_flag_on_is_a_no_op_on_the_currently_reachable_campaign` only proves reachability is a no-op absent a combat-scene trigger — it does not exercise the echo path at all, since nothing queues a `combat` scene_type yet) | W1→flip | (this PR) | ED-IN-0093 | the flip PR's own checklist; `scene_dispatch.py`'s combat-branch echo-attribution docstring | open — held for the flip action |
| seam declaration WIDENED: combat bridge consumes wrapper.fight + Combatant constructor (both PC-owned, as-is); plan §0's 'wrapper public API' under-described | W1 | (this PR) | ED-IN-0093 | combat_bridge.py header comment | done |
| duplication logged, not chased: massbattle.py:1875 performs the identical Mil→power scale-crossing derivation with its own [GAP] note — MB-owned; flagged for the MB session via this ledger + PR body, single-owner resolution deferred | W1 | (this PR) | ED-IN-0093 | MB session's eventual consolidation | open — MB-visible |
| stubs.count predicate refinement candidate: 'imports stubwire' counts telemetry consumers; a call-site predicate (stub_resolve invocation) would be exact — deferred, W5 candidate | W1 | (this PR) | ED-IN-0093 | future predicate's own test | open — routed to W5 |
| _KERNEL_EXPECTED count-pin declared move 385→385 (measured post-conversion: OI-18a/OI-19's raise->StubResult rewrite replaced 5 checks 1-for-1, so the total held) | W1 | (this PR) | ED-IN-0093 | test_contest_kernel.py comment | done |
| ED-IN-0094 intake — fractional triad RULED; routed PC/MB/SC/IN per the ledger entry; IN implementation scheduled as its own PR (dice-core primitive + integer-baking census) | intake | (this PR) | ED-IN-0094 | the IN census PR's per-site declared-golden list | open — IN half scheduled |
| decision item for Jordan: enable repo Settings → Allow auto-merge (autonomous merge loop currently needs a bounded timer + manual merge call) | intake | (this PR) | — | — | open — Jordan |
| CI-trigger anomaly (decision item for Jordan): GitHub Actions stopped firing pull_request runs for this session's PR events (PR #265: no run on open, force-push, empty-commit push, or close/reopen; W0a/W0b PRs did fire) — W1 merged on full local CI parity instead; affects all three concurrent sessions' PRs; Jordan may want to check Actions settings/token behavior + enable repo auto-merge | W1 gate | #265 | — | the push-to-main CI run on 304242f (check it ran) | open — Jordan |
| same-lane coordination note: ED-IN-0103 was allocated from the reserved IN block 0092-0111 by a concurrent session (PR #262, centralization/single-ownership plan) — no collision (this program is at 0094); future program IDs continue sequentially and reconcile-on-merge per precedent | intake | (this PR) | ED-IN-0094 | this row | noted |
| OI-12 census (documentation only, no code edits) — **already-stub-wired(W1), 7 of 14**: `engine/autoload/npc_ai.py`, `systems/characters/sim/companion.py`, `systems/overview/sim/rs_track.py`, `systems/overview/sim/ip_track.py`, `systems/threadwork/sim/rendering.py`, `systems/world/sim/miraculous_event.py`, `systems/world/sim/restoration_movement.py` — re-verified in-tree 2026-07-29: each `from engine.substrate import stubwire` + `stubwire.stub_resolve(...)` body (confirmed by direct read, not the register's summary). Note: `rs_track.apply_rs_delta` DOES have a real caller — `engine/cross_scale/echo_transport.py:219-220` (accord-echo lane, OI-03) — so it is not import-orphan in the structure_audit sense even though its own body is a stub; recorded here as a correction to the flat "stub-wired, nothing to do" framing, not a wiring action by this lane | W2 | (this PR) | ED-IN-0091 | direct re-read of each file's stubwire import/body (2026-07-29); `echo_transport.py:215-220` for the rs_track caller correction | census done, no code touched |
| OI-12 census — **verified-orphan-no-specified-callsite, 7 of 14**: `systems/threadwork/sim/co_movement.py` (159 LOC, real `draw_comovement_card`/deck logic per threadwork_v30.md §4.3, ED-577 — zero real Python `import` from any other file; only a `game_state.py:191` field-comment note "from systems/threadwork/sim/co_movement", not a call), `systems/threadwork/sim/collective.py` (201 LOC, `attempt_collective_operation` per §2.5 — zero importers; threadwork_v30.md never names an invoking module), `systems/threadwork/sim/opposing.py` (258 LOC, `resolve_opposing_operations` per §2.6 — zero importers; same doc silence), `systems/settlements/sim/settlement.py` (204 LOC, `SettlementState` derivation per settlement_layer_v30.md §1.2-1.3 — zero real importers; many hits in a naive grep are docstring "Dependencies:" prose or unrelated same-word matches, not imports — re-verified with `^from ... import` anchoring), `systems/settlements/sim/temperaments.py` (170 LOC, §2-4 typology/drift per territory_temperaments_v30.md — zero real importers, same false-positive pattern as settlement.py), `systems/social_contest/sim/parliamentary_stay.py` (106 LOC, `invoke_stay`/§10.1 ED-631 — zero importers; social_contest_v30.md names no invoker), `engine/autoload/registry.py` (120 LOC, `load_index`/`get_mechanic` mechanics_index.yaml loader — zero real importers despite a large naive substring-grep hit list, all false positives on the word "registry"). For all 7: grepped each module's own docstring canon-source line AND its subsystem design head for a specified call site — **none found**; confirms rather than refutes the preflight's finding. No module force-wired or stub-converted (all are real implemented code, not stubs) | W2 | (this PR) | ED-IN-0091 | precise `^from <module> import`/`^import <module>` anchored greps per module (2026-07-29, superseding an initial naive substring pass that false-positived on `settlement`/`registry`/`temperaments` as common words); doc greps against `threadwork_v30.md`, `settlement_layer_v30.md`, `territory_temperaments_v30.md`, `social_contest_v30.md`, `mechanics_index.yaml` | census done, no code touched — honest recorded miss, W5 census input |
| OI-12 census — provisional flag: `settlement.py`/`temperaments.py` orphan status is per the current tree at census time; if the World lane (OI-07, same wave) adds a settlements-registry call site touching either file, that lane's own `files_touched` supersedes this row at bookkeeping time — mark provisional, not final | W2 | (this PR) | ED-IN-0091 | World lane's `files_touched` list (cross-check at Bookkeeping phase) | provisional, pending Bookkeeping cross-check |
| Bookkeeping cross-check on the provisional flag above: World lane's `systems/settlements/sim/registry.py` diff (`populate_from_geography`) does NOT import or call `settlement.py`/`temperaments.py` (`git diff` shows no new `import`/`from` line naming either module) — the census row's orphan finding for those two modules is CONFIRMED, not superseded | W2 | (this PR) | ED-IN-0091 | `git diff systems/settlements/sim/registry.py` (no import of `settlement`/`temperaments`), re-grep of both modules at bookkeeping time | confirmed, provisional flag resolved — still orphan |
| ED allocation (Bookkeeping, W2, per plan Bookkeeping-phase instruction) — drawn from the W0a-reserved blocks, `id_reservations.yaml` left untouched (file frozen): **ED-IN-0095** (W2 umbrella: seams + oracle + census — OI-03/06/07(settlements)/08/12), **ED-FA-0036** (OI-04 transfer motion path), **ED-WR-0009** (OI-05 NPC generation + NPE season wiring, honest-deferral disposition; no golden re-record needed — see below), **ED-SE-0049** (OI-07 settlements population + serialization). Full entries in `registers/editorial_ledger_in.jsonl`, `_fa.jsonl`, `_wr.jsonl`, `_se.jsonl`, each citing ED-IN-0091 as parent | W2 | (this PR) | ED-IN-0095, ED-FA-0036, ED-WR-0009, ED-SE-0049 | the four lane-ledger entries (schema-matched to each file's existing `id/date/system/status/needs_jordan/confidence/description/citations/source` shape) | done |
| OI-03/OI-06/OI-04/OI-07(settlements)/OI-08 — landed and re-verified live at bookkeeping time (not just per the Oracle-stage claim): `engine/tests/test_pipeline_reach.py`'s XFAIL_MANIFEST no longer carries `accord-echo-leg`/`vertical-up-handoff`/`territory-transfer-resolver`/`world-settlements` rows (4 retired to strict this wave); `engine/tests/test_accord_echo.py`, `test_parliamentary_transfer_bridge.py`, `test_world_population.py`, `tests/valoria/test_articulation_subscriber.py`, `test_handoff_dispatch_validity.py` all pass (67 passed, 4 xfailed on the targeted 6-file run, 2026-07-29) | W2 | (this PR) | ED-IN-0095 | `python -m pytest engine/tests/test_pipeline_reach.py engine/tests/test_accord_echo.py engine/tests/test_parliamentary_transfer_bridge.py engine/tests/test_world_population.py tests/valoria/test_articulation_subscriber.py tests/valoria/test_handoff_dispatch_validity.py -q` → `67 passed, 4 xfailed` | landed, verified |
| OI-05 / world.knots half of OI-07 — CORRECTION to the plan's "golden re-record, named loudly" expectation (plan §3 item 3): re-verified against `investigation_systems_v30.md` SYSTEM 1 and `knots_v30.md` §3.1, neither specifies a world-gen/season-tick trigger — the honest-deferral disposition (permanent, via `stubwire`) means `world.npcs`/`world.knots` STAY empty, so `test_f7_smoke_oracle.py`'s `npcs_generated == 0` pin does NOT move. No golden was re-recorded this wave; the plan's expectation is corrected here rather than executed against the tree's actual (canon-driven) outcome | W2 | (this PR) | ED-WR-0009 | `git diff --stat` confirms `engine/tests/test_f7_smoke_oracle.py` is NOT in the changed-file set; `test_pipeline_reach.py`'s `world-npcs`/`world-knots` rows reclassified `honest-deferral` (permanent xfail) rather than removed | landed — honest deferral, golden untouched |
| Bookkeeping (this stage) — scope respected: no edit made to `engine/tests/test_pipeline_reach.py` (Oracle stage's sole ownership), `systems/combat/**`, `combat_engine_v1/wrapper.py`, `faction_action.py:349`, `references/id_reservations.yaml`, or `registers/review_baseline.yaml`; only this file + the 4 lane JSONL ledgers + `HANDOFF_IN.md` + root `HANDOFF.md` were touched, per this stage's own edit-scope instruction | W2 | (this PR) | ED-IN-0095 | this row + `git status --short` for the actual edit set | done |
| **W2-FIX (orchestrator-adjudicated fix batch, 2026-07-29) — critic 'missing'/defect repairs on top of the W2 rows above.** Scope respected: no edit to `systems/combat/**`, `combat_engine_v1/wrapper.py`, `systems/factions/sim/faction_action.py`, `references/id_reservations.yaml`, `registers/review_baseline.yaml`. Rows below are OI-03 fixes 1-4, OI-04 canon gate, the OI-04 duplication log, the OI-08/G12 re-verify, the three test-honesty repairs, the orphan-count delta, and the deferred OI-04 golden re-record — one row per item, per this batch's own instruction ("rows for every fix above, same format") | W2-fix | (this PR) | — | this row + the rows below | header row, no falsifier of its own |
| OI-03 fix 1 — `echo_transport.classify_scene_outcome`'s `_ACCORD_OUTCOME_BY_SCENE_TYPE = {"combat": "violence"}` fallback DELETED: §5.5 classification now trusts ONLY an explicit caller-declared `echo['scene_outcome']`; scene_type no longer participates. The leg reverts to WIRED-BUT-DORMANT (a real `compute_accord_echo` caller exists; no live producer declares `scene_outcome`) — documented in the function's own docstring rather than left implicit | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py`'s `classify_scene_outcome` (fallback map removed) + `engine/tests/test_accord_echo.py::test_classify_requires_explicit_declared_outcome_even_for_combat` (replaces the retired `test_classify_combat_maps_to_violence`) | landed |
| OI-03 fix 2 — `_apply_accord_echo` RETARGETED to the SETTLEMENT where the scene occurred (AUD-SET-02, `scale_transitions_v30.md:215` + `peninsular_strain_v30.md` §2.5 Category B), never `Territory.accord` directly; `echo_ctx['target_settlement']` replaces `target_territory`. Province Accord stays a read-only aggregate (`systems/settlements/sim/registry.py`'s `province_accord`, `floor(mean(order))`) this function never recomputes | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py::_apply_accord_echo` + `engine/tests/test_accord_echo.py` (governance/violence/territorial_transfer rows re-pinned against `world.settlements[sid].order`, not `world.territories[tid].accord`) | landed |
| OI-03 fix 3 — ONE Accord unit: the write applies in `Settlement.order`'s own canonical-index 0-5 scale (`settlement_layer_v30.md` §1.3 — "Order ... Analogous to province Accord but at settlement scale"), matching `domain_echo.compute_accord_echo`'s already-canonical-index `accord_delta`; no `MULTS`/`ACCORD_MAP` conversion anywhere in the function (that pair is `Territory.accord`'s own, different, continuous 0.5-7.0 scale). Clamped to `registry.STAT_MIN`/`STAT_MAX` | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py::_apply_accord_echo`'s Unit docstring note (cites `settlement_layer_v30.md` §1.3) + `test_settlement_order_never_leaves_the_canonical_0_5_index_bound` | landed |
| OI-03 fix 4 — contract collision: `zoom_in_out.zoom_out` documented `accord_changes` as queued to Accounting while `_apply_accord_echo` actually applied immediately. Genuine queue-parity (routing through the same OF-7 `sched.emit(key, apply=...)` mechanism the sibling §5.2 leg uses) would require registering a NEW `scene.accord_echo` Key type in `key_type_registry_v30.md` — Wave 3 territory, not this lane's, and inventing an ad hoc non-Key deferred queue would grow a scale-local dialect (ED-1083 guardrail). Resolved by RENAMING the key to `accord_applied` and correcting both ends' docstrings to describe immediate application, not queuing — the option the task's own "OR" clause names for exactly this case | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py` (`accord_changes` → `accord_applied`, both return sites) + `engine/cross_scale/zoom_in_out.py::zoom_out`'s docstring/loop (`fires_at: 'already_applied'`) + `engine/tests/test_accord_echo.py` (all `out.get("accord_applied")` assertions) | landed |
| OI-04 — canon gates (`parliamentary_transfer_v30.md` §1.1: Frequency "1 per arc per faction" `:27`, Cost "Action slot + CB consumption (whether transfer succeeds or fails)" `:31`, Crown auto-CB "Refreshes per arc if condition holds" `:71`): added `Faction.parl_transfer_used_this_arc` (game_state.py, mirrors `council_used_this_arc`'s existing per-arc-flag shape + `reset_arc()` hook exactly — `arc` is UNAMBIGUOUS in this corpus, `season_manager.SEASONS_PER_ARC = 4` + `World.arc`, no mapping-ambiguity ledger note needed). `propose_transfer` gates on it at the declaration stage (before any CB consumption or roll) and sets it at the same point CB is consumed (the attempt-commit point, unconditional of the eventual degree). `parliamentary_bridge._derive_transfer` excludes an already-arc-gated initiator from candidate derivation itself, so a gated-out season is BYTE-IDENTICAL to a no-qualifying-CB season (verified: zero extra `world.rng` draws in either case) | W2-fix | (this PR) | ED-FA-0036 | `systems/factions/sim/parliamentary_transfer.py::propose_transfer` (frequency gate + cost-on-attempt) + `engine/cross_scale/parliamentary_bridge.py::_derive_transfer` (exclusion) + `engine/tests/test_parliamentary_transfer_bridge.py`'s 4 new tests (`test_propose_transfer_blocks_a_second_attempt_in_the_same_arc`, `test_reset_arc_clears_the_frequency_gate`, `test_derive_transfer_excludes_an_arc_gated_initiator`, `test_gated_out_season_matches_no_qualifying_cb_season_byte_for_byte` — the last asserts identical `rng.getstate()` between a gated-out and a no-CB season) | landed |
| OI-04 duplication log (item C, CLAUDE.md §0.1 point 5 "log, don't chase") — `parliamentary_transfer.py`'s `terr.owner = initiator` is the THIRD `t.owner = <faction>` owner-assign site in the corpus; the other two (`faction_action.py:366`, `mass_seizure.py:292`) are both seam/lane-frozen for this wave (explicit no-touch list). No single-owner consolidation performed — comment at the site points here, this row is the pointed-to record, not a promise of a future sweep beyond what a later wave schedules | W2-fix | (this PR) | ED-FA-0036 | `systems/factions/sim/parliamentary_transfer.py` (comment above `terr.owner = initiator`, "DUPLICATION LOGGED, NOT CHASED") citing this exact ledger row | logged, not chased |
| OI-08 / G12 re-verify (item D) — the plan premise ("`meta.cascade_cluster_event` UNREGISTERED, subscribe to only 9/10") was already stale before this fix batch: ED-IN-0022 (2026-07-07, C-KEY-8) registered it in `key_type_registry_v30.md` (~line 862). `articulation.subscribe_all` already subscribes all 10/10 with the G12-correction comment in place — RE-VERIFIED here (loaded the live registry through the actual runtime `TypeRegistry` parser, confirmed `'meta.cascade_cluster_event' in registry.types`), no code change needed; `tests/valoria/test_articulation_subscriber.py` already asserts `count == 10` (not just `>= 9`) | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/articulation.py`'s G12 CORRECTION comment (pre-existing) + `python3 -c "from engine.substrate.keys import TypeRegistry; r = TypeRegistry.load('systems/_architecture/key_type_registry_v30.md'); print('meta.cascade_cluster_event' in r.types)"` → `True` (re-run 2026-07-29) | re-verified correct, no code change |
| Test-honesty repair 1 (item E, critic 'missing') — `engine/tests/test_world_population.py::test_npc_and_knot_deferral_stubs_fire_every_season`'s `checked = seasons; assert checked == seasons` tautology (CLAUDE.md §0.1 point 2 — `checked` never depended on anything observed) replaced with a REAL per-season conditional counter: the season loop is now driven directly (`systems.overview.sim.season.run_season(world, action_callback=mc_v18._faction_actions_callback)`, mirroring `run_campaign`'s own composition) so `stubwire.invocations` can be sampled BETWEEN seasons and `assert delta >= 2` runs per season, not just once at the end | W2-fix | (this PR) | ED-WR-0009 | `engine/tests/test_world_population.py::test_npc_and_knot_deferral_stubs_fire_every_season` (rewritten) | landed |
| Test-honesty repair 2 (item E) — `engine/tests/test_pipeline_reach.py::test_oi12_census_is_recorded_in_the_execution_ledger`'s `checked == len(<the same fixed tuple>)` assert was the SAME decorative-counter class the file's own module docstring already corrects elsewhere (CORRECTION 2). Split into two tests: doc-presence (unchanged logic, decorative counter dropped) + a NEW `test_oi12_census_matches_the_real_structure_audit_classification`, which re-derives the real orphan/stub_wired sets from `structure_audit.py`'s own graph functions (`collect_py_modules`/`build_g_code`/`split_orphans_and_cli_entries`/`stub_wired_modules` — reused via the same `importlib.util` load `tests/valoria/test_structure_audit.py` uses, not re-implemented) and compares both pinned census tuples against them — genuinely falsifiable | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_oi12_census_matches_the_real_structure_audit_classification` (new) | landed |
| Test-honesty repair 3 (item E) — the articulation reach-guard row (`test_articulation_subscriber_is_wired_and_stub_flags_fire`) CLAIMED production reach ("verified live at that call site") but only ever exercised a hand-built `_world_with_scheduler`, never `run_campaign` itself — a deleted `_articulation.subscribe_all(...)` call inside `run_campaign` would have left it green. New `test_articulation_subscriber_is_wired_in_a_real_production_campaign_construction` patches `articulation.subscribe_all` (via `unittest.mock.patch.object`, `wraps=` the real function) and drives a REAL `run_campaign(..., params={'ECHO_TRANSPORT': 1})`, asserting the production hook actually calls it with a scheduler that ends up with populated subscriptions | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_articulation_subscriber_is_wired_in_a_real_production_campaign_construction` (new) | landed |
| Test-honesty repair 4 (item E) — the accord-echo-leg direction test (`test_direction2b_bottom_up_accord_echo_leg_is_wired`) was RESOLVED/XPASS(strict) on a bare "caller exists" source-scan; OI-03 fix 1 (above) makes that framing stale again (classification now requires an explicit declared outcome nothing supplies). Renamed to `test_direction2b_bottom_up_accord_echo_leg_is_wired_but_dormant` and now asserts BOTH halves: caller-exists (unchanged scan) AND dormancy (a NEW scan over the two actual producer modules, `scene_dispatch.py` + `parliamentary_bridge.py`, confirming neither declares `echo['scene_outcome']` as an input — `echo_transport.py` itself is deliberately excluded from the dormancy scan since its own `_apply_accord_echo` builds an output/telemetry dict containing that same string, which would otherwise false-positive) | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_direction2b_bottom_up_accord_echo_leg_is_wired_but_dormant` (renamed + extended) | landed |
| Orphan-count delta (item F, required exit criterion) — `python3 skills/valoria-vector-audit/scripts/structure_audit.py --stub-count` → **24 stub-wired**, unchanged (this batch converted no new module to `stubwire`). Full audit (`python3 skills/valoria-vector-audit/scripts/structure_audit.py --repo-root . --output-dir <scratch>`) → **code-orphans: 71 (pristine `git archive HEAD`, i.e. before this session's Wave 1+2 work) → 66 (current fixed tree, Wave 1 + this W2-fix batch)**, net **-5**, **0 newly orphaned**. NOTE on the task's cited "W0b PR's 74": the pristine-HEAD re-measurement here gives 71, not 74 — HEAD already carries W0b's OI-55 detector-integrity fixes (commit 9c89030), so 74 was almost certainly measured with the pre-W0b (buggy) detector on the same code, not a different orphan count; used the freshly-verified 71 as the honest baseline rather than citing an unverified prior number. Resolved (no longer orphan): `engine.cross_scale.articulation` (OI-08), `engine.cross_scale.handoff_rules` (OI-06), `systems.factions.sim.parliamentary_transfer` (OI-04), `systems.overview.sim` + `systems.overview.sim.rs_track` (OI-03's `rs_track.apply_rs_delta` caller) | W2-fix | (this PR) | ED-IN-0095 | commands above, run 2026-07-29; `<scratch>/structure_audit_{pristine_out,out}/data/structure_metrics.json`'s `code.orphans` set-diff (71→66, 5 resolved, 0 newly orphaned) | landed, exit criterion met |
| OI-12 census reconciliation (item G) — `settlement.py`/`temperaments.py`'s provisional flag (row above, "provisional flag" / "Bookkeeping cross-check") was ALREADY resolved to CONFIRMED verified-orphan before this fix batch (the World lane's `registry.py` diff never imports either module) — RE-VERIFIED here against the current (Wave 1 + W2-fix) tree via the same `structure_audit` re-derivation as the orphan-count-delta row above: both remain in `code_orphans` (`systems.settlements.sim.settlement`, `systems.settlements.sim.temperaments`), so the confirmed disposition still holds — no further flip needed, nothing was silently re-opened | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_oi12_census_matches_the_real_structure_audit_classification` (asserts this membership as a passing test, not just a one-off script run) | re-confirmed, unchanged |
| OI-05 row (item G, honest deferral) — canon makes NPC generation scene-specification-driven (`investigation_systems_v30.md` SYSTEM 1's Two-Tier Generation: "Scene specification declares density and composition"); no world-gen density rule or season-tick generation trigger exists to cite. The open-items register's `00_open_items_register.md:71` classification of OI-05 as class **B** (buildable-this-program) was STALE (G12 — the same "premise re-verified against the working tree and found outdated" pattern as the OI-08 row above): the honest disposition is a PERMANENT deferral, not a buildable gap. Routed to the **WR** docket per `01_orchestration_plan_v1.md` §3.5's deferred-to-lane table pattern and §5 row 9 ("NPC lane ownership ... fold/retire per GAP-K2/K3; lane = Jordan's" — pending fork-9). `npcs_generated == 0` pin (`test_f7_smoke_oracle.py`) is UNCHANGED by this whole fix batch (re-verified: 0 across the re-recorded seed-42..49 batch, item H below) | W2-fix | (this PR) | ED-WR-0009 | `investigation_systems_v30.md` SYSTEM 1 citation + `00_open_items_register.md:71`'s "B" column + `01_orchestration_plan_v1.md` §3.5 / §5 row 9 + `test_f7_smoke_oracle.py`'s unchanged `assert npcs == 0` in `test_f7_scenes_live_insurgency_and_npe_still_islands` | honest deferral, routed WR, npcs==0 pin unchanged |
| **Transfer/golden row (item H) — the deliberate OI-04-attributed golden re-record, now landed** (deferred from the original W2 row above until after OI-04's canon gates (item B) landed, per the adjudicator's control-isolation instruction: attribute the whole transfer-motion effect — landing it in W2 AND this batch's frequency-gate refinement — to OI-04 as ONE isolated comparison, not two separate re-records). Method: pristine `git archive HEAD` export vs. this fixed tree (both `python3 -m pytest engine/tests -q` runs); **8 pinned constants + 1 narrative assertion moved, across 3 files (per-test summary below)**, all and only in files whose call graph reaches `parliamentary_bridge.run_parliamentary_scene` (`test_f7_smoke_oracle.py`, `test_mc_v18_regression.py`, `test_parliamentary_bridge.py`) — every OTHER `engine/tests` file's pins (`test_echo_transport.py`'s flag-OFF golden, `test_combat_bridge_seam.py`, etc.) did NOT move, confirming OI-03 (item A) is golden-neutral as expected (its leg is dormant both before and after, for different reasons — see the OI-03 fix 1 row). Before/after table below; each OLD value is preserved in a comment at its site citing ED-FA-0036/OI-04 | W2-fix | (this PR) | ED-FA-0036 | before/after table below + `python3 -m pytest engine/tests/test_f7_smoke_oracle.py engine/tests/test_mc_v18_regression.py engine/tests/test_parliamentary_bridge.py -q` (green after re-record) | landed |
| §1.1 Action-slot half UNENFORCED (residual): the transfer motion runs outside the faction action-slot economy; only the CB-consumption half + arc-frequency gate landed — FA-lane residual on ED-FA-0036 | W2 | (this PR) | ED-FA-0036 | future FA action-economy wiring | open — FA residual |
| province-Accord aggregation gap (re-critic MEDIUM): registry.province_accord has ZERO live callers — settlement Order never aggregates to province Accord while Territory.accord is still written directly (parliamentary_transfer.py:210, mass_seizure.py:295); the OI-03 settlement retarget is canon-correct but currently feeds an unwired aggregate — routed to Wave 3 contract-truth sweep (accounting recompute wiring) | W2→W3 | (this PR) | ED-IN-0095 | W3's aggregation wiring + a campaign assertion that province Accord derives from settlement Order | closed — measurement landed W3 (drift probe); write-model → SE/OI-37 (see W3 section) |
| PLAN-EXIT DEVIATION, LOUD (re-critic MEDIUM — Jordan-visible): 01_orchestration_plan_v1.md:318 requires world-npcs/world-knots reach rows STRICT; both ship as canon-justified honest-deferrals instead (no world-gen density rule exists — G12 register correction; routed WR docket/§3.5/fork 9). The plan snapshot is immutable; THIS ROW is the amendment record and the PR body carries it loudly | W2 | (this PR) | ED-IN-0095/ED-WR-0009 | the WR docket ruling re-pins them | open — deviation recorded |

### Item H — golden before/after table (OI-04-attributed, 2026-07-29)

| File · pin | OLD (pristine `git archive HEAD`) | NEW (fixed tree, Wave 1 + W2-fix) |
|---|---|---|
| `test_f7_smoke_oracle.py::GOLDEN_WIN_SHARE` | `{Crown: 37.5, Church: 12.5, Hafenmark: 12.5, Varfell: 37.5}` | `{Crown: 62.5, Church: 0.0, Hafenmark: 0.0, Varfell: 37.5}` |
| `test_f7_smoke_oracle.py::GOLDEN_WINNERS` (sparse dict — only factions with ≥1 win get a key) | `{Crown: 3, Church: 1, Hafenmark: 1, Varfell: 3}` | `{Crown: 5, Varfell: 3}` |
| `test_f7_smoke_oracle.py::GOLDEN_BATTLES_MEAN` | `30.2` | `35.5` |
| `test_f7_smoke_oracle.py::GOLDEN_SCENES_RESOLVED` | `383` | `463` |
| `test_f7_smoke_oracle.py::test_f7_hafenmark_elimination_lockout` | Hafenmark wins `1/8` | Hafenmark wins `0/8` (verified cause: Crown's OWN restoration CB sometimes selects Hafenmark as the largest-non-Crown-holder target, e.g. seed 49 `('Crown','T10','Hafenmark','transferred')` — Crown reclaiming FROM Hafenmark, not a rescue mechanism for it; ED-FA-0005 remains open) |
| `test_mc_v18_regression.py::GOLDEN_WIN_SHARE` (seed 0, n=2) | `{Crown: 50.0, Church: 0.0, Hafenmark: 0.0, Varfell: 50.0}` | `{Crown: 100.0, Church: 0.0, Hafenmark: 0.0, Varfell: 0.0}` |
| `test_mc_v18_regression.py::GOLDEN_WINNERS` | `{Varfell: 1, Crown: 1}` | `{Crown: 2}` |
| `test_mc_v18_regression.py::GOLDEN_BATTLES_MEAN` | `30.5` | `33.5` |
| `test_parliamentary_bridge.py::_ON_WIN_SHARE` (seed 42, n=8, ECHO_TRANSPORT=1) | `{Crown: 37.5, Church: 12.5, Hafenmark: 12.5, Varfell: 37.5}` | `{Crown: 62.5, Church: 0.0, Hafenmark: 0.0, Varfell: 37.5}` |
| `test_parliamentary_bridge.py::test_flag_on_win_share_golden_and_diverges_from_off`'s narrative assert | `off['Church']==0.0 and on['Church']>0.0` ("the spine brings Church in") | `off['Hafenmark']>0.0 and on['Hafenmark']==0.0` (the still-true weaker claim: the spine still shuts out a survivor, now Hafenmark not Church) |
| **UNCHANGED** (confirmed, not re-recorded) | `test_parliamentary_bridge.py::_OFF_WIN_SHARE`, `_ON_KEYLOG_HASH`, `_ON_SCENES_RESOLVED`, `_ON_KEYS_EMITTED` (single seed-42 campaign — Crown never drops under 6 territories on that one seed); `test_f7_smoke_oracle.py`'s `npcs_generated`/`insurgencies_formed` (both stay 0); `test_echo_transport.py`'s flag-OFF golden; every non-parliamentary-bridge-reaching `engine/tests` file | — |

## Wave 3 (Keys & contract truth) — bookkeeping repair

**BOOKKEEPING REPAIR NOTE (2026-07-29, critic 'MISSING'):** the W3 lane executed real work (below) but
the bookkeeping stage that should have appended these rows in the same PR cycle wrote nothing — this
section is that repair, written after the fact against the actual working-tree diff, not against a
plan-stage description of intended work. Every row below is verified against the diff or a passing
test at the time this section was written, not copied from an earlier stage's claim.

| OI/item | wave | PR | ED | falsifier artifact | outcome |
|---|---|---|---|---|---|
| OI-22a — combat-pair dangling-emit closure for articulation: `scene.combat_resolved`/`scene.combat_felled` (the one declared `consuming_systems` member with runtime) now have a real subscriber — `articulation.py`'s `_TRIGGER_TYPE_IDS` extended, `articulation_layer_v30.md` §3.1 gained trigger rows #11/#12 (landed earlier in W3, re-verified here); `npc_behavior`/`faction_state` `consumes:[]` in `module_contracts.yaml` now also declare both types (declared intent, runtime gated on those modules being built — npc_behavior has zero .py files) | W3 | (this PR) | ED-IN-0004 | `tests/valoria/test_articulation_subscriber.py::test_combat_pair_reaches_the_articulation_subscriber`, `engine/tests/test_pipeline_reach.py::test_combat_pair_key_reaches_articulation_subscriber_under_flag_on` (new, W3 item 7 — xfail while `DISPATCH_COMBAT_BRIDGE` is OFF; strict pass RE-VERIFIED 2026-07-29 re-critic round 2, seed pinned and reproducible: seed=0 (30-seed sweep 0-29, all draw-insensitive-identical), reproduce `DISPATCH_COMBAT_BRIDGE=1 python3 -m pytest engine/tests/test_pipeline_reach.py::test_combat_pair_key_reaches_articulation_subscriber_under_flag_on -q`), `tests/valoria/test_structure_audit.py::test_l2_personal_combat_dead_emits_now_consumed` | landed |
| OI-24 a-d — contract truth sweep: (a) `npc_behavior`'s `doc:` repointed from the Key-silent `npc_behavior_v30.md` to `political_dynamics_keys_migration_v30.md` (C-KEY-2 closed), old doc kept reachable in `sources:`; (b) the four `emits:[]` inline comments (`scene.displacement`/`mechanical.project_advanced`/`state.project_failed`/`state.project_completed`) corrected from stale "NOT in registry" to "IN registry (ED-935)", matching the already-landed SUPERSEDED note; (c) `faction_politics`'s `state: []` populated with 3 top-level items (Standing/Coup posture/Succession status) read directly off `faction_politics_v30.md` §1.0/§1.0a/§1.1-1.4/§6/§10.1 — contract-truth declaration only, OI-20's sim half stays deferred to the FA lane; (d) MS ownership annotation on `victory`'s `g_ms0`/`g_ms5`/`g_msrec` gates corrected from "unowned clock" to "owned by peninsular_strain" (shared with OI-32a below, same commit) | W3 | (this PR) | ED-IN-0096 | `references/module_contracts.yaml` diff (npc_behavior `doc:`/`emits:`/`state:` blocks, victory gates' `reads:`) | landed |
| OI-25 — the five silent-emitter types (`state.settlement_revolt`, `mechanical.settlement_captured`, `mechanical.era_transition`, `mechanical.second_calamity`, `mechanical.theocracy_unification_declared`) stay REGISTERED (unchanged, landed pre-W3) but lose their false `consuming_systems: [articulation]` declaration (adj DEFECT 1) — `consuming_systems: []` now, with an explicit held-disposition note per entry ("consumer decided at the emitting module's build") instead of a declared-but-never-subscribed articulation edge. `mechanical.theocracy_unification_declared` additionally gained a dual-emitter single-owner note (ci_political vs territorial_piety — owner decided at whichever module builds first, residual named not guessed). All four emitting modules' `module_contracts.yaml` `gap_notes` gained one line each: "Key types registered (W3, ED-IN-0096): ... — emits: declaration lands with the module's build so the census never carries a declared-emit-no-consumer row" | W3 | (this PR) | ED-IN-0096 | `systems/_architecture/key_type_registry_v30.md` (5 entries' `consuming_systems`/`notes`), `references/module_contracts.yaml` (4 modules' `gap_notes`), `tests/valoria/test_key_substrate.py::test_registry_loads_oi25_silent_emitter_registrations` (rewritten to assert `consuming_systems == []`, was `== ["articulation"]` — the prior assertion pinned the defect this row fixes) | landed |
| OI-28 LIVE half — the accord Key's `causes[]` field is now genuinely populated at construction (`echo_transport.py::_apply_accord_echo`): `causes=[caused_by_key_id] if caused_by_key_id else []`, satisfying keys.py:325's in-log invariant by construction (verified ordering, unchanged). This is the one executable, non-decorative live `causes[]` instance corpus-wide — executable but DORMANT (no live producer declares `echo['scene_outcome']`, so the branch never runs in a real campaign). The OTHER genuine `causes[]` candidate class named in the plan — `npc_behavior` pseudocode per `political_dynamics_keys_migration_v30.md` §5.4 — stays design-gated (no live `npc_behavior` runtime exists to wire it into, `systems/npcs/` has zero `.py` files) and was NOT touched this wave, consistent with the plan's own scope note ruling it out | W3 | (this PR) | ED-IN-0096 | `engine/tests/test_accord_echo.py`'s two §3 tests (log-lookup falsifier on the real Key, not string-equality; the None-when-not-fired honesty case kept), `engine/tests/test_pipeline_reach.py::test_direction6_diagonal_causes_has_an_instance` (rewritten to a campaign-shaped runtime probe with a live-introspected dormancy-gated xfail condition, mirroring `combat-bridge-on`'s own pattern — was a raw source-text scan that would have silently XPASSed the moment the `causes=[...]` line landed) | landed, dormant |
| OI-30a — Category-B scalars (07-14 unification §3 / ED-IN-0059) registered in `references/descriptor_registry.yaml`: `pc.wounds`, `pc.poise`, `pc.initiative`, `prov.turmoil`, `set.accord`, `clock.season_counter`, all under a new `personal_track` KIND covering the whole registration batch (not a shared runtime shape — the six span `track`/`clock`/`derived_value` buckets in `module_contracts.yaml`) — pointer registrations only, no formula/scale/Godot binding (landed pre-bookkeeping, re-verified here) | W3 | (this PR) | ED-IN-0096 | `references/descriptor_registry.yaml` `category_b_scalars` block + `kind:` roster comment | landed |
| OI-32a — MS (Mending Stability) ownership declared: `peninsular_strain`'s `state:` block gains an MS entry (GAP-F1 mechanically-determinable owner); `victory`'s three MS-reading gates' `reads:` annotations corrected from "unowned clock" to point at the new owner (shared row with OI-24d above). `env.ms_delta` emit deliberately NOT added — no declared consumer exists anywhere in the corpus, so adding it would create a new dangling emit rather than close one; recorded as an open GAP-F1 residual, not fabricated as closed | W3 | (this PR) | ED-IN-0096 | `references/module_contracts.yaml` (peninsular_strain `state:`, victory gates' `reads:`, both modules' `gap_notes`) | landed, env.ms_delta residual open |
| OI-40a — the IN half of the 4-vocabulary scale reconciliation stays HELD, not executed this wave: `module_contracts.yaml`'s header gained a coordination-pointer comment citing ED-IN-0103 §6 fork 1 (the centralization program's Jordan-held fork) as the single owner of that ruling — no vocabulary unification lands here or anywhere else until that fork resolves. G12 note: `00_open_items_register.md`'s OI-40 row cites evidence tag "A8" alongside F6/F7 — grepped exhaustively across `audit/2026-07-29-code-shape-open-items/` and the wider `audit/` tree; no "A8" finding definition exists anywhere in the corpus outside that one citation. Logged as unverifiable per CLAUDE.md §0.1 point 5 (log, don't chase), not silently dropped and not guessed at | W3 | (this PR) | — | `references/module_contracts.yaml` header comment (OI-40a coordination pointer); this row (A8 unverifiable) | held, per §6 fork 1 |
| `mechanical.season_change`'s dangling-consumer status — the 4th pre-wave dangling type (`consuming_systems: [all subscribing systems]`, an unresolvable vague roster, same shape as `env.crisis`'s `[all]`) stays UNRESOLVED this wave, HELD at ED-IN-0103 §6 fork 3 (`OI-43a`: `engine_clock`'s `doc: null` flip, ED-1051) — `mechanical.season_change` is emitted by `engine_clock`, whose home doc does not yet exist to cite a real consumer against. LOUD DEVIATION: this is a 4th pre-wave dangling type the plan's own exit criterion ("dangling-emit count 4 → ≤1") did not separately enumerate; recorded here rather than silently left off the census | W3 | (this PR) | — | `systems/_architecture/key_type_registry_v30.md:299-313` (`mechanical.season_change` entry, `consuming_systems: [all subscribing systems]`), `references/module_contracts.yaml`'s `engine_clock` `gap_notes` (`doc: null` open, ED-1051) | held, per §6 fork 3 — loud deviation |
| `env.crisis`'s dangling-consumer status — DEDICATED ROW (item 7, small/LOW — this type previously only appeared inline in the census-arithmetic row below, not its own row like `mechanical.season_change`'s above): accounts for **2 of the 3** surviving dangling pairs (one type, both its declared emitters — `peninsular_strain` and `scenario_authoring` — contribute a pair each against the same unresolvable `consuming_systems: [all]` roster). Stays UNRESOLVED this wave, HELD at `05_jordan_docket_v1.md` **Fork 4** (`OI-22b`: "env.crisis consumer — or ruled terminal", NO DEFAULT — none named anywhere) per `02_disposition_map.md:59`; `01_orchestration_plan_v1.md:357`'s own exit criterion explicitly holds it out (`dangling-emit count 4 → ≤1 (env.crisis held for §5)`). No code change made, no consumer invented | W3 | (this PR) | — | `systems/_architecture/key_type_registry_v30.md:783-798` (`env.crisis` entry: `emitting_systems: [peninsular_strain, scenario_authoring]`, `consuming_systems: [all]`); `05_jordan_docket_v1.md` Fork 4 (lines 278, 283); `02_disposition_map.md:59` | held, per §5 fork 4 — dedicated row |
| W2 handoff item 1 (accord-echo type + queue parity) — DONE, re-verified: `scene.accord_echo` registered pre-W3 bookkeeping; W3 closed the remaining half — a real §3.1 trigger row (#13) + `articulation.py` subscription, and the `causes[]` population (OI-28 LIVE half, row above). W2 handoff item 2 (province-Accord drift probe) — DONE, report-only, re-verified: `systems/overview/sim/accounting.py::_probe_province_accord_drift` + `engine/mc_v18.py`'s `accord_drift_probe_hits` telemetry field, landed pre-bookkeeping; never writes either compared value. **Closes the W2→W3 routed row at line 76 above accordingly**: the MEASUREMENT half (the drift probe) is DONE this wave; the WRITE-MODEL reconciliation half (making `registry.province_accord` and `Territory.accord` a single write model) is NOT done here and was never this program's to do — it is SE lane territory, routed to OI-37 ("single highest-priority open item" per `HANDOFF_SE.md`'s L/PS workstream), unchanged from the original W2→W3 routing intent, just now correctly attributed to SE rather than left as an open IN residual | W3 | (this PR) | ED-IN-0095, ED-IN-0096 | `systems/overview/sim/accounting.py::_probe_province_accord_drift`, `engine/mc_v18.py` `accord_drift_probe_hits` field, this row (closes line-76 routing) | line-76 row CLOSED — measurement done, write-model routed to SE/OI-37 |
| `references/rendering_dispositions.yaml` §10 precondition — CORRECTION (re-critic MED-HIGH): the authority is `key_type_registry_v30.md` §10's own RATIFIED precondition (2026-07-07, ED-IN-0026, `key_echo_armature_v1.md` §5.16 — "no new Key type may be appended to this registry without a corresponding row in `references/rendering_dispositions.yaml`"), not "a prior wave's planning text" as this row previously (wrongly) framed it. The file does NOT EXIST anywhere in the working tree (`ls references/rendering_dispositions.yaml` — no such file); enforcement is A15 report-only, not yet blocking (§4 of the armature). This session (W2 OI-25 + W3 OI-03) appended SIX types with no corresponding `rendering_dispositions.yaml` row: `state.settlement_revolt`, `mechanical.settlement_captured`, `mechanical.era_transition`, `mechanical.second_calamity`, `mechanical.theocracy_unification_declared` (OI-25, W2) + `scene.accord_echo` (OI-03, W3) — the precondition was not satisfied for any of the six, consistent with report-only (not blocking) enforcement, but a real, growing residual against a RATIFIED gate. Not created here (authoring a datafile of rendering verdicts is out of this wave's scope and not this lane's call to make unilaterally). Filed honestly rather than silently assumed: `rendering_dispositions.yaml` authoring (or the precondition's formal retirement) is a Jordan-visible decision item — routed to the docket alongside fork 11's rendering questions; residual filed against ED-IN-0026 | W3 | (this PR) | ED-IN-0026 | `ls references/rendering_dispositions.yaml` (confirmed absent, 2026-07-29); `key_type_registry_v30.md` §10 (RATIFIED precondition text); the six type entries' own notes (no rendering_dispositions.yaml row cited) | logged, residual filed against ED-IN-0026, routed to Jordan docket |
| OI-26 — `personal_combat`'s internal `_emit()` trace vocabulary → the 4 canonical `scene.combat_*` Key types — STILL OUTSTANDING, correctly not attempted here: this is the PC-owned half (`combat_engine_v1/{wrapper.py,state_graph.py}`), scheduled as PC plan §15 I4's post-E3 batch deliverable (`wrapper_emit_key_map.md`). This wave's IN-side half (registering/consuming the combat-pair types) landed as OI-22a above; the PC mapping table has not landed, so its consumption stays outstanding per the plan's own "item 1 still ships, the registry-side wiring does not block on it" instruction | W3 | (this PR) | — | PC plan §15 I4 (unlanded); `systems/_architecture/key_type_registry_v30.md`'s combat-pair entries (IN side, landed) | outstanding — PC session's |
| Census arithmetic correction (item 8): pre-wave dangling emits were **4 distinct types / 5 pairs** (`scene.combat_resolved`→articulation, `scene.combat_felled`→articulation — the 2 pairs with runtime that OI-22a closes; `env.crisis` held for §5; `mechanical.season_change`'s vague `[all subscribing systems]` roster, held at fork 3 — the other 2 types contributing pairs that stay open), not the smaller figure an earlier PR-body draft cited. Post-wave: **2 distinct types / 3 pairs** remain open (`env.crisis` — held for §5 **fork 4**'s docket, corrected here from this row's own prior "fork 1" mislabel — see the dedicated row above; `mechanical.season_change` — held at ED-IN-0103 §6 fork 3, see row above) — OI-22a's 2 combat-pair pairs are the ones this wave closed | W3 | (this PR) | — | this row + the OI-22a/season_change rows above (arithmetic re-derived directly against the registry, not copied forward) | corrected |
| Small corrections (item 8): `module_contracts.yaml`'s MS row comment corrected — the claim that `accounting.py` "L36-39 also inlines the same PP-255 decay" is FALSE against the current tree (verified: no inline decay logic anywhere in `accounting.py`; the import is at `:43`, the Year-End-gated call at `:117`, full delegation to `ms_track.apply_ms_baseline_decay`) — the false claim was carried over unverified from `ms_track.py`'s own stale docstring (not corrected there, out of this file's scope, logged per §0.1 point 5). `engine/cross_scale/domain_echo.py:167`'s violence row `fires_at="immediate"` corrected to `"accounting_step_4c"` to match every sibling row and `echo_transport._apply_accord_echo`'s own docstring (only the RS sub-component is canon-immediate; the field describes the Accord leg, which queues like every other row) — verified no consumer branches on the field's value (`fires_at` is read nowhere outside `domain_echo.py`'s own dataclass; `zoom_in_out.py`'s same-named dict key is a separate, hardcoded literal) | W3 | (this PR) | ED-IN-0096 | `references/module_contracts.yaml` MS row comment, `engine/cross_scale/domain_echo.py:163-175` | landed |
| `tests/valoria/test_articulation_subscriber.py`'s roster-drift test now genuinely parses §3.1 (item 6, critic HIGH): `test_trigger_roster_matches_articulation_layer_v30_section_3_1` extracts type_ids directly from `articulation_layer_v30.md`'s §3.1 table (backtick-delimited token per numbered row, isolated to the §3.1 section) and compares both directions against `articulation._TRIGGER_TYPE_IDS`, instead of comparing two independently-hardcoded sets that could silently drift together | W3 | (this PR) | ED-IN-0096 | `tests/valoria/test_articulation_subscriber.py::test_trigger_roster_matches_articulation_layer_v30_section_3_1` (new parser + both-directions diff assertion) | landed |
| Stale registry line-range citations (critic-found) corrected: `key_type_registry_v30.md:727-742/781-794` (the combat pair's citation, which had already drifted from the working tree by the time it was written) re-derived to the current `:873-889` (`scene.combat_resolved`) / `:927-940` (`scene.combat_felled`) across `engine/cross_scale/articulation.py` and `tests/valoria/test_articulation_subscriber.py` | W3 | (this PR) | ED-IN-0096 | `engine/cross_scale/articulation.py` comment block, `tests/valoria/test_articulation_subscriber.py` (module docstring + `_PAYLOADS` comment + `test_combat_pair_reaches_the_articulation_subscriber` docstring) | landed |
| ROUND 2 CORRECTION (fix round 2, re-critic — this batch's item 3, the drift root cause named explicitly: "re-deriving mid-growth"): the row above's `:873-889`/`:927-940` re-derivation was ITSELF already stale by the time it landed, because this same session kept appending to `key_type_registry_v30.md` after that re-derivation was written (OI-25's declare-only types, §9/§10 prose corrections, this batch's own §10-precondition and note edits). Re-derived a SECOND time, done LAST in this batch (after every other edit to the file), against the settled tree: `scene.combat_resolved` :899-915, `scene.combat_felled` :953-966, `scene.accord_echo` :969-1006 (was :943-977). Updated across `engine/cross_scale/articulation.py`, `tests/valoria/test_articulation_subscriber.py` (module docstring `_PAYLOADS` comments + `test_combat_pair_reaches_the_articulation_subscriber` docstring), `registers/editorial_ledger_in.jsonl`'s ED-IN-0096 citation list, and `systems/articulation/articulation_layer_v30.md:110`'s own stale first-generation range | W3-fix2 | (this PR) | ED-IN-0096 | `grep -n '^### scene\.' systems/_architecture/key_type_registry_v30.md` (899/953/969, closing fences 915/966/1006, re-verified 2026-07-29 against the settled tree) | landed |
| Registers a new §3.1 trigger row (#13, `scene.accord_echo`) closing the OI-03 half of the same trigger-ruleset gap the combat pair (#11/#12) closed: `articulation_layer_v30.md` §3.1 table + explanatory paragraph + new §6.6 Class-B-extension-confirmation subsection; `articulation.py`'s `_TRIGGER_TYPE_IDS` extended to 13; `subscribe_all`'s docstring/return-count updated; `key_type_registry_v30.md`'s `scene.accord_echo` entry note updated from "subscription wiring OUTSTANDING" to "CLOSED" | W3 | (this PR) | ED-IN-0096 | `systems/articulation/articulation_layer_v30.md` §3.1/§6.6, `engine/cross_scale/articulation.py`, `tests/valoria/test_articulation_subscriber.py` (13-type_id roster, `scene.accord_echo` payload entry, all `== 12` → `== 13`) | landed |
| OBSERVATION (item 7, small/LOW, re-critic fix round 2) — `systems/_architecture/key_type_registry_v30.md` is now **1295 lines** (measured `wc -l` 2026-07-29, post fix-round-2 edits) after this wave's registrations (OI-25's 5 declare-only types + OI-03's `scene.accord_echo`) and the §10/§9-roster prose corrections landed this batch. Flagged as an atomization/size-cap watch, not an action taken here — a `valoria-chunker` split candidate for W5 if it keeps growing (CLAUDE.md §4's sequential-parts convention, `_part2` in reading order, applies once it clears the size-cap threshold) | W3-fix2 | (this PR) | — | `wc -l systems/_architecture/key_type_registry_v30.md` (1295) | observation logged, no split performed |

**Verification note (this bookkeeping pass):** `python3 -m pytest tests/valoria/test_articulation_subscriber.py engine/tests/test_accord_echo.py engine/tests/test_pipeline_reach.py engine/tests/test_key_substrate.py -q` — all green, run 2026-07-29 against the tree this section describes. Full-suite `tests/valoria` + `engine/tests` + campaign-golden run recorded separately at this PR's close (see PR body / commit message for the verbatim tail); no golden moved.

## Wave 4 (mechanical sweep / single-owner consolidation) — bookkeeping, AFTER the critic

Disputes were the orchestrator's to adjudicate, not this pass's — this section records the reconciled
outcome only. Umbrella: **ED-IN-0097** (drawn from the W0a-reserved IN block; `id_reservations.yaml`
itself left untouched, file frozen per the run's own convention — same protocol as W2/W3).

| OI/item | wave | PR | ED | falsifier artifact | outcome |
|---|---|---|---|---|---|
| OI-52a — `engine.autoload.game_state` ↔ `systems.world.sim.npe` import cycle broken: `canonical_accord` extracted to a new no-deps leaf `engine/substrate/canon_buckets.py`; `game_state.py` re-exports it unchanged for every existing call site, `npe.py` imports it top-level instead of the prior lazy in-function import | W4 | (this PR) | ED-IN-0097 | `tests/valoria/test_import_cycle_game_state_npe.py` (new — pins the cycle count 4→3, mutation-checked, enumerates the 3 expected-remaining cycle families) | landed |
| `__main__`-guard single-owner consolidation (W0b-routed row above, OI-52a/OI-53a class) — `structure_audit.py`'s AST predicate and `build_apparatus_registry.py`'s regex predicate collapsed into one owner, `tools/ci_common.py::has_main_guard(tree)` (AST-based, both operand orders); both callers now import it | W4 | (this PR) | ED-IN-0097 | `tests/valoria/test_ci_common.py` (new — plants a comment/string-literal false-positive the old regex would have wrongly matched) | landed — **closes the W0b-routed duplication row above** |
| OI-53a — 4 sites still keyed on the retired `designs/`/`sim/` roots fixed via one new owner, `tools/ci_common.py::sim_reference_prefixes()` (sibling of the pre-existing `sim_reference_roots()`, ED-IN-0087 precedent): `tools/audit_staleness.py`'s `scope_prefixes`, `tools/workplan_status.py`'s `RELEVANT_PREFIXES`, plus the two `__main__`-guard callers consolidated in the row above. `build_apparatus_registry.py`'s `_gcode_imported_modules()` glob against the deleted `designs/audit/` tree (silently empty since `designs/` retired 2026-07-19) turned into an explicit documented no-op — no live stable `g_code.json` home exists to glob instead; `imported_internally` already falls back to the full-text-grep signal, unaffected. Deltas reported, not silently absorbed | W4 | (this PR) | ED-IN-0097 (corrected at the W4 gate from a mis-cited `ED-IN-0104`, which lies inside the CONCURRENT centralization-single-owner program's reserved `0103–0111` partition — §0.1 row 6; site fixed in `ci_common.py:56`) | `tools/ci_common.py::sim_reference_prefixes()` + its docstring's per-site enumeration; `tools/build_apparatus_registry.py`'s explicit-no-op docstring | landed — **closes the W0b-routed stale-surface row above** |
| OI-54 — `module_contracts.yaml` ↔ `mechanics_index.yaml` ↔ live-code join: `structure_audit.py --contracts-join` (composes on the existing `build_l2()`/`l2_contract_code_join()` pass, no second contract parse); 27/27 `module_contracts.yaml` `sim_module:` rows populated and cross-checked against `mechanics_index.yaml`'s 88 existing rows plus direct per-module grep verification (`faction_state`→`game_state.py` Faction dataclass with an honestly-logged resolver-half residual; `npc_behavior`/`npc_memory`→`none`, re-verified zero code; `conviction_track`→`sim/conviction.py`; `territorial_piety`→`overview/sim/ci_track.py` split, `Territory.pt` noted separately) | W4 | (this PR) | ED-IN-0097 | `tests/valoria/test_structure_audit.py::test_module_contracts_sim_module_join_is_exact` (27/27, 0 unresolvable); `tools/review_core.py`'s new report-only `contracts.join` check (`count_re` on the unresolvable count only) | landed |
| `registers/review_baseline.yaml` row for `contracts.join` — deliberately NOT added: the file is CODEOWNERS-gated/frozen this run (STOP-list); `_apply_ratchet` only grades regression on a `fail` verdict, so an absent-baseline ceiling-defaults-to-0 is safe against the signal's current passing state (unresolvable=0); banking a nonzero baseline, if ever needed, is Jordan's own addition | W4 | (this PR) | ED-IN-0097 | `tools/review_core.py`'s `contracts.join` entry comment (rationale inline) | deliberately not done, documented |
| OI-15 — 4 confirmed-orphan tools retired to `deprecated/tools/` (`build_audit_registry_backfill.py`, `geography/jsx_to_canonical.py`, `measure_stamp_false_positives.py`, `observability/npc_audit_report_gen.py`), each grepped repo-wide across `.github/`, `.githooks/`, `.claude/`, `skills/`, `tools/`, `registers/`, `CLAUDE.md` BEFORE moving (ED-1082 precedent) — every hit was a docstring/comment mention or generated-data reference, never an import or CI/hook invocation | W4 | (this PR) | ED-IN-0097 | `deprecated/tools/README.md`'s "Retired 2026-07-29 (Wave 4 mechanical sweep, OI-15/OI-16, ED-1082 precedent)" section (per-file grep record; its OI-16 half is now a separate NOT-retired section — see the OI-16 row) | landed — 4 retirements |
| `tools/wf_harness.js` — re-checked against this wave's own changes per the item's own instruction; no drift found needing a fix | W4 | (this PR) | — | re-check only, no diff produced | verified clean, no-op |
| `references/apparatus_registry.{yaml,md}` regeneration against the post-retirement tree — deferred to the W5 capstone (the plan's designated regenerator wave), not run standalone here to avoid an uncoordinated second regen event before W5's own retirements land | W4 | (this PR) | ED-IN-0097 | W5's regen run | deferred to W5 |
| OI-16 — **HELD, NOT EXECUTED. Retirement performed by the sweep lane, then REVERSED at the W4 gate.** The sweep did retire `tools/registry.py` + `tests/valoria/test_registry.py` on a correct zero-production-consumer grep (ED-1082 precedent). The reversal is the disposition: the concurrent `audit/2026-07-29-centralization-single-owner/` program (ED-IN-0103, merged PR #262) holds a **BINDING** interlock on exactly this file (its §0.1 row 1, "`tools/registry.py` retirement race"), whose W1.3 + §1 predicate 2 *make the facade real* — and which foresaw this precise outcome: "the ED-1082 grep-then-move precedent would find no consumers *precisely because W1 has not run*." The zero-consumer result is therefore evidence OF the race, not evidence FOR retirement. **Why nothing stopped it:** that interlock's declared executable form is a `[CSO]`-prefixed blocking row in THIS ledger that W4 must read before its item 5 — and that row was never written (grep `[CSO]` over this file: 0 hits; the CSO pointer still reads "W0 not yet started"). The interlock was protocol on a shared writable surface, which that plan itself labels "stronger than prose but weaker than a gate". Intent was unambiguous, so W4 yields the file. Both files restored to their `HEAD` state (byte-identical, `git checkout`); both `deprecated/tools/` copies deleted. Retirement-or-consumer call routed to CSO W1.3 — W4 executed neither | W4 | (this PR) | ED-IN-0097 | `deprecated/tools/README.md`'s "OI-16 (Wave 4) — `registry.py` NOT retired: HELD under a cross-program interlock" section; `tools/registry.py` + `tests/valoria/test_registry.py` present and unmodified vs `HEAD`; `test_registry.py` still collected by `pytest tests/valoria` | **HELD — routed to CSO W1.3, deliberately not executed** |
| OI-16 companion ask (`references/head_pointers.yaml` + `docs/REPO_MAP.md`) — recorded **NOT-TO-BE-BUILT**: that role is already served by `CURRENT.md` (CLAUDE.md §1) plus the `PROPOSALS.md`/`DECISIONS.md` observability family; a third pointer surface would duplicate a single-owner role (CLAUDE.md §8) | W4 | (this PR) | ED-IN-0097 | `deprecated/tools/README.md`'s disposition note (rationale inline, no file created) | rationale recorded, not built |
| OI-51 — re-verified ENTIRELY STALE: everything the register cited was already executed pre-program at commit f60b74d; recorded as a no-op, nothing re-done | W4 | (this PR) | — | `HANDOFF_IN.md`'s pre-W4 header-correction note (f60b74d citation); this row | verified stale, no-op |
| OI-57 — 2 currency-layer orphan mechanics indexed in `registers/mechanics_index.yaml`: `franchise` (doc status DRAFT, `sim_module: none`), `faction_succession_split` (doc status CANONICAL, `sim_module: none`) — both entries index the doc as-is, do not ratify it. The register's "insurgency" claim re-verified stale (already resolved pre-W4, not re-done). ED-1054's navigation-surface item re-verified against the current tree: `tools/README.md`'s 4 previously-missing entries now present (CLOSED); `sim/README.md`/`sim/CONVENTIONS.md` regeneration targets are MOOT (`sim/` retired wholesale 2026-07-21); the ~850KB narrative-markdown relocation stays genuinely OPEN, target corrected from the now-retired `designs/audit/` to `audit/` or `deprecated/archives/`. FA-owned `CURRENT.md` rows for the two newly-indexed mechanics courtesy-flagged for the FA session, not edited here (IN does not own FA's `CURRENT.md` rows) | W4 | (this PR) | ED-IN-0097 | `registers/mechanics_index.yaml` diff (2 new entries); `registers/handoffs/HANDOFF_IN.md`'s ED-1054 re-verification note (3/4 sub-items closed-or-moot) | landed — 2 indexed, ED-1054 loop closed, FA rows courtesy-flagged |
| OI-32a — `engine/mc_v18.py`'s dead-param tripwire (`DEFAULT_PARAMS['VICTORY_THRESHOLD'] = 11`) annotated in place, pointing at `engine/tests/test_f7_smoke_oracle.py::test_victory_threshold_dead_param` (C-EMERGE-8/C-FA-9), with a 2026-07-29 re-verification note that it is still unread anywhere in `engine/cross_scale/`, `systems/overview/sim/season.py`, or `engine/autoload/victory.py`. Intel field recorded as a re-verify, not a new wiring action — wiring it would move a golden and both the tripwire test and `victory.py` are STOP-listed | W4 | (this PR) | ED-IN-0097 | `engine/mc_v18.py`'s inline comment (re-verification note); `engine/tests/test_f7_smoke_oracle.py::test_victory_threshold_dead_param` (unchanged, still green) | annotated, re-verified, no wiring change |
| **DECISION ITEM FOR JORDAN, not self-ratified** — `vocab.a17` (review_core's stub/gap-vocabulary tripwire) measures **21** live rows against `registers/review_baseline.yaml`'s pinned ceiling of **29**: an **8-row banked shrink** from this wave's OI-52a/53a/54/15/16 cleanup. `review_baseline.yaml` is STOP-listed (frozen, CODEOWNERS-gated) so the lower ceiling is NOT banked here — flagged as a baseline-lowering candidate requiring its own ED + Jordan's explicit CODEOWNERS-review sign-off before any future PR tightens the ratchet | W4 | (this PR) | — | `HANDOFF_IN.md`'s pre-W4 header note ("vocab.a17 sits at 21 vs baseline 29"), re-measured unchanged at bookkeeping time | open — Jordan decision item, not banked |
| Bookkeeping (this stage) — scope respected: no edit made to `systems/combat/**`, `combat_engine_v1/wrapper.py`, `faction_action.py:349`, `references/id_reservations.yaml`, `registers/review_baseline.yaml`, or `engine/tests/test_pipeline_reach.py`; only this file + `registers/editorial_ledger_in.jsonl` (+ its archive, size-cap repair) + `registers/handoffs/HANDOFF_IN.md` + root `HANDOFF.md` were touched | W4 | (this PR) | ED-IN-0097 | this row + `git status --short` for the actual edit set | done |
| Register-size repair (bookkeeping housekeeping) — `registers/editorial_ledger_in.jsonl` exceeded its 50,000-token cap after the ED-IN-0097 entry landed (51,583 tokens); archived 5 uncited-elsewhere resolved/superseded entries (`ED-IN-0058`, `ED-IN-0063`, `ED-IN-REMEDIATION-0063`, `ED-IN-0012`, `ED-IN-0013`) to `registers/editorial_ledger_in_archive.jsonl` per the established archive-resolved-content procedure. Each candidate was grepped for external citations before moving; `ED-IN-0029`'s duplicate resolved rows were checked out again after an initial move caused 10 `validate_ed_citations.py` OPEN_AS_BASIS violations (multiple docs cite ED-IN-0029 as ratifying basis; archiving both resolved copies left only an `open`-status duplicate as the sole in-file citation target) | W4 | (this PR) | — | `python3 tools/ci_register_size_check.py` (48,216/50,000 after); `python3 tools/validate_ed_citations.py` (0 violations after, re-verified) | done |

**Verification note (Wave 4 bookkeeping):** `python3 -m pytest tests/valoria/test_ci_common.py tests/valoria/test_structure_audit.py tests/valoria/test_stubwire.py tests/valoria/test_retired_tree_apparatus.py tests/valoria/test_import_cycle_game_state_npe.py engine/tests/test_pipeline_reach.py -q` → **92 passed, 5 xfailed**, run 2026-07-29 against the tree this section describes. No golden moved (STOP-list re-verified untouched by this bookkeeping pass, listed in the scope row above).


### Wave 4 — orchestrator gate batch (post-critic, ED-IN-0097)

The Adjudicate stage returned `open-defects` and the read-only critic returned 18 verdicts + 5
items the adjudicator missed. The run's own `stop_reason` was `disagreement_unadjudicated` (8
disputes) — by design: the harness is report-only and the script states disputes are the
orchestrator's. Every row below is a gate fix applied AFTER the critic, by the orchestrator.

| Row | Wave | PR | ED | Falsifier | Status |
|---|---|---|---|---|---|
| **BLOCKING GATE 1 — `compliance_check` size cap.** The OI-54 join took `references/module_contracts.yaml` from 70,069 → 82,280 chars (17,372 → 20,402 tok), over its 18,000 cap, failing a BLOCKING CI job the wave introduced. **Not fixed by pruning:** the added bulk IS the join's disclosure content (per-row `none` reasons, 3 recorded preflight DISCREPANCIES, two-file-split residuals) — condensing it to fit would delete exactly the honesty §0.1 #4/#5 demands. Cap raised 18000 → 24000 in `references/atomization_rules.yaml`, the same disposition that row's own note took at 10000 → 18000 and `canonical_sources.yaml` took three times. ⚠ RATIFIABLE ON MERGE (ED-1094), called out in the PR body | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/compliance_check.py --check-only --repo-state .` exit 1 → **exit 0**; measured 21,088 tok vs 24,000 cap after all gate edits | landed |
| **DEFECT FILED, NOT FIXED — `on_exceed: "warn_only"` is a NO-OP.** `tools/compliance_check.py:179` reads `severity = 'warn' if on_exceed.startswith('flag') else 'error'`, so `warn_only` — and ANY unrecognised token — silently grades as a blocking error. **12 files** in `atomization_rules.yaml` declare `warn_only`; all are mis-graded. Had it worked, gate 1's cap raise would not have been needed. NOT fixed here: `size-cap single-sourcing` is the declared scope of `audit/2026-07-29-centralization-single-owner/`, and unilaterally loosening a blocking gate another program owns is the wrong call for a wave that merely tripped it | W4-gate | (this PR) | ED-IN-0097 | `grep -c 'warn_only' references/atomization_rules.yaml` = 12; `tools/compliance_check.py:179` reads as quoted | **filed → routed to CSO** |
| **BLOCKING GATE 2 — `ci_claude_workflow_paths` 6 DEAD paths, self-inflicted.** The sweep's retirements made `.claude/wf_wave4_central.js`'s own prompt text cite 6 nonexistent files. Fixed via the sanctioned route (`ci_claude_workflow_paths.py:57-61` treats `.claude/` aliases as non-fatal — comments are prose, not executed commands): 4 pointer rows added to `references/restructure_ledger.md`; the other 2 (`registry.py`, `test_registry.py`) resolved by the OI-16 reversal above, not by an alias | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/ci_claude_workflow_paths.py` `6 DEAD / [GATE FAILED]` → **`0 DEAD / [GATE PASSED]`**, 290 referenced / 279 live / 11 aliased | landed |
| **Gate 3 was already green** — `validate_ed_citations` exit 0. The adjudicator's "27 ED-IN-0097 violations" was measured BEFORE the Bookkeeping stage filed the entry; re-run at the gate confirms 0 violations. Recorded because an uncorrected report would read as an outstanding blocker | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/validate_ed_citations.py` → `0 citation-integrity violation(s)`, exit 0 | no action needed |
| **Cross-program ID incursion corrected** — `tools/ci_common.py:56` cited `ED-IN-0104`, which lies inside the partition CSO §0.1 row 6 reserves for itself (`ED-IN-0091` keeps `0093–0102`; CSO takes `0103–0111`). Invisible to `validate_ed_citations` (its `SCAN_SUFFIXES` excludes `.py`). Corrected to `ED-IN-0097` at both sites (the `.py` and this ledger's OI-53a row) | W4-gate | (this PR) | ED-IN-0097 | `grep -rn ED-IN-0104 tools/` → 0 hits | landed |
| **Vacuous assertion replaced + mutation-verified** (§0.1 #2) — `tests/valoria/test_structure_audit.py`'s `assert sa.ci_common.has_main_guard is not None` asserted nothing (a module-level function is never None) while its comment claimed single-owner coverage. Replaced with an identity check against the owner **plus** `assert not hasattr(sa, 'has_main_guard')`. NB the identity check imports plain `ci_common`, NOT `tools.ci_common` — `structure_audit.py:51-57` uses the `sys.path` idiom, so the dotted import would build a second module object and fail spuriously | W4-gate | (this PR) | ED-IN-0097 | **EXECUTED MUTATION:** planted a local `has_main_guard` re-copy in `structure_audit.py` → test FAILED at the new line; restored, sha256 byte-identical, test passes | landed |
| **Two guards de-flaked** — `tests/valoria/test_retired_tree_apparatus.py`'s audit-dir and decisions-digest tests were cwd-relative while every other test in the file anchors on `ROOT`, and the latter asserted `drift > 0`, a TIME-VARYING quantity that goes red whenever the digest is refreshed with no later corpus change. Both anchored on `ROOT`; the drift assertion relaxed to `>= 0` with the real falsifier (scope resolves at all → `_family_status` is not None) stated | W4-gate | (this PR) | ED-IN-0097 | `cd / && pytest tests/valoria/test_retired_tree_apparatus.py` was **2 failed / 21 passed** → **23 passed**; 23 passed from repo root too | landed |
| **Cross-lane test break averted** — `test_structure_audit.py`'s `join['undeclared'] == ['mass_battle']` equality pin would go RED the moment the MB session adds its `sim_module:` row (which `module_contracts.yaml:552` explicitly invites), forcing MB to edit an IN-owned test to ship in-lane work. Relaxed to `set(...) <= {'mass_battle'}` — the invariant that actually matters | W4-gate | (this PR) | ED-IN-0097 | `pytest tests/valoria/test_structure_audit.py` 32 passed; the assertion now tolerates MB's row landing and still fails on any OTHER undeclared module | landed |
| **Dead cross-reference the wave itself shipped** (critic MISSING #1) — `engine/mc_v18.py:47`'s new OI-32a annotation cited `test_f7_smoke_oracle.py::test_victory_threshold_dead_param`, a node id that does not exist; the real test is `test_f7_victory_threshold_is_a_dead_param`. A wave retiring dead pointers must not ship one | W4-gate | (this PR) | ED-IN-0097 | `pytest 'engine/tests/test_f7_smoke_oracle.py::test_f7_victory_threshold_is_a_dead_param'` collects and passes; the old id collected nothing | landed |
| **3 stale prose pointers redirected** — `ci_audit_registry_check.py:71`, `build_apparatus_registry.py:313`, `currency_consistency_check.py:91` each named a now-retired tool at its old `tools/` path; all repointed at `deprecated/tools/` with the retiring ED. (A 4th, `test_descriptor_registry_category_b.py:4-5`, needed no edit — the OI-16 reversal restored the `registry.py`/`test_registry.py` it cites) | W4-gate | (this PR) | ED-IN-0097 | `grep` for each retired filename outside `deprecated/` returns only `deprecated/tools/…`-qualified mentions | landed |
| **Generated-artifact write hazard closed** — `tools/build_apparatus_registry.py` had NO argument parsing: `main()` overwrote `references/apparatus_registry.{yaml,md}` on ANY invocation, `--help` included. The W4 adjudicator tripped it while merely inspecting the tool and had to `git checkout` both files back. That is a forged write against a single-writer table (IN regenerates at W5). Added `argparse` + `--dry-run` | W4-gate | (this PR) | ED-IN-0097 | `--dry-run` and `--help` both leave both artifacts sha256-unchanged (verified); `git status` clean on both | landed |
| **3 `sim_module: none` reasons completed** (adj finding 8) — the join's mandate included cross-checking `mechanics_index.yaml`, and three `none` rows cited only a narrow grep. Each now names its near-miss and says why it is still `none`: `npc_behavior` vs `npc_ai_service → engine/autoload/npc_ai.py` (**declined: that module is a W1 `stubwire` shell — declaring it would report a code home that answers no Key**); `domain_actions` vs `domain_echo → engine/cross_scale/domain_echo.py` (**declined: alias collision — `domain_echo` transports the echo and emits none of the five `da.*` types**); `game_director` vs `engine/cross_scale/scene_dispatch.py` (**declined: not in `mechanics_index` at all, and emits none of the `mechanical.scene_*` Keys**) | W4-gate | (this PR) | ED-IN-0097 | `structure_audit.py --contracts-join` still `13 joined / 13 none / 0 unresolvable / 1 undeclared (of 27)`; each cited index line re-read at the gate | landed |
| **UNFIXED BY DESIGN — the 6th dead root, in a BLOCKING gate.** `tools/validate_ed_citations.py:108`'s `SCAN_PREFIXES` still carries the retired `designs/` root and covers neither `engine/` broadly nor `audit/`. Same defect class as OI-53a's four sites. **NOT swept here:** CSO §0.1 row 8 explicitly assigns `validate_ed_citations.py:91,108,278` repoints + guards to that program's W1/W2. Second instance the adjudicator raised, `compliance_check.py:606`, is inert — the critic verified `find_index_for()` has zero callers repo-wide | W4-gate | (this PR) | ED-IN-0097 | `sed -n '108p' tools/validate_ed_citations.py` reads as quoted; `grep -rn find_index_for tools/` returns only its own `def` | **recorded, routed to CSO** |
| **Harness observation, not a ruling** — the run returned `stop_reason: disagreement_unadjudicated` with 8 disputes whose `finding_id` is `"?"` and `positions` empty. The script calls `run.dispute({layer, target, detail, severity})` but the harness's record shape keys on `finding_id`/`positions`, so every dispute serialised identity-less and no adjudication could bind to one. This is the FIRST live multi-lens run since `tools/wf_harness.js` landed (ED-IN-0087), and its own residual asked exactly this to be checked. The disputes were adjudicated by the orchestrator from the critic's verdict list instead. Owner: `tools/wf_harness.js` (edit the owner, never a copy) | W4-gate | (this PR) | ED-IN-0097 | the run's returned `run.summary()`: `"unadjudicated": ["?","?","?","?","?","?","?","?"]`, every `positions: []` | **filed — harness defect, not a wave defect** |
| **Status-vocabulary gap found by the gate's own re-run** — citing `ED-IN-0097` from `references/atomization_rules.yaml` (a SCANNED prefix) flipped `validate_ed_citations` to `OPEN_AS_BASIS`, because the Bookkeeping stage filed the entry with `status: "landed"` and `_is_resolved()` accepts only `resolved*`/`ratif*`/`struck`/`applied`/`superseded`/`closed`/`confirmed`/`deprecated`. Set to `resolved`, matching **ED-IN-0093** (the Wave-1 umbrella, same class of completed-wave entry). **FILED, NOT FIXED:** `ED-IN-0095` and `ED-IN-0096` are still `landed`, so the same trap re-arms for the next session that cites either from `canon/`, `systems/`, `engine/params/` or `references/`. Widening `_is_resolved` to accept `landed` would be the class fix — deliberately NOT done here, on the same reasoning as the `warn_only` row: this wave tripped a BLOCKING gate and should not also redefine its vocabulary | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/validate_ed_citations.py` `1 violation` → **`0 citation-integrity violation(s)`**, exit 0; `grep '"status": "landed"' registers/editorial_ledger_in.jsonl` still returns ED-IN-0095/0096 | landed (0097) / **filed** (0095, 0096) |
| **The cap was duplicated — third instance of one defect, now single-sourced + guarded.** Raising `atomization_rules.yaml` alone did NOT clear the gate: `tools/ci_register_size_check.py:71` carried its own hardcoded `"references/module_contracts.yaml": 18_000`. That file already single-sources two other caps through `yaml_max_tokens()` and its own comment names the class — "two gates, one file, two limits" — so this is the pattern recurring a third time, having been fixed twice as one-offs with a guard written for only ONE of them. Fixed by the file's own remedy (`MODULE_CONTRACTS_LIMIT = yaml_max_tokens(...)`), and `tests/valoria/test_coverage_matrix_threshold.py` GENERALISED from the single coverage_matrix case to all three single-sourced caps, with a counted-assertion floor (§0.1 #2). This is the §0.1 #5 guard, not another one-off | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/ci_register_size_check.py` exit 1 → **exit 0** (`20,917 / 24,000`); **EXECUTED MUTATION:** restored the hardcoded `18_000` → `test_every_single_sourced_threshold_matches_the_policy_file` FAILED; reverted, sha256 byte-identical, 5 passed | landed |
| **Freshness pin refreshed, and the tree is now fully fresh.** Editing `module_contracts.yaml` staled its `canonical_sha__` pin in `references/canonical_sources.yaml`. `freshness_gate.py --update` touched exactly one line (verified by diff — 1 insertion, 1 deletion). NB this corrects a stale CLAUDE.md §8 claim in passing: that section says "~12 stale `canonical_sha__` pins" remain and freshness is report-only until they are refreshed — the live measurement is **132 FRESH / 0 STALE, [GATE PASSED]**. Recorded, not acted on: flipping freshness from report-only to blocking is a governance call, not this wave's | W4-gate | (this PR) | ED-IN-0097 | `python3 tools/freshness_gate.py` → `FRESH: 132 / STALE: 0 / [GATE PASSED]`; `git diff --stat references/canonical_sources.yaml` = 1 file, 1 insertion, 1 deletion | landed |

### W5 walk-back + the P0-P8 remediation batch (ED-IN-0098, 2026-07-30)

| Row | Wave | PR | ED | Falsifier | Status |
|---|---|---|---|---|---|
| **ID BLOCKS RELEASED — freeze lifted.** 13 ids returned: SC 21→17 (0017-0020), FA 40→37 (0037-0039), WR 13→10 (0010-0012), SE 53→50 (0050-0052). MEASURED max-allocated per lane from live AND archive lane ledgers | W5 | (this PR) | ED-IN-0098 | `tests/valoria/test_id_reservations_walkback.py` — 12 tests; asserts `next_free > max_allocated` for all 9 lanes, pins the 4 walked-back pointers. **EXECUTED MUTATION:** SC→16 (colliding with live ED-SC-0016) fails it | landed |
| **IN NOT walked back — deliberate, and the near-miss is the reason.** IN max-allocated is 0103 but `next_free` STAYS 112. The 0092-0111 block is SUB-PARTITIONED: CSO §0.1 row 6 gives code-shape 0093-0102 and CSO 0103-0111, and CSO's W0 has not started. 5 ids (0098-0102) are genuinely free but a single pointer cannot express a hole, so walking back to 98 would hand out a LIVE reservation five allocations later — the same failure class as W4's `registry.py` near-retirement. Stranding 5 ids is the cheap side of that trade | W5 | (this PR) | ED-IN-0098 | `test_in_lane_gap_is_documented_not_accidental` — requires the gap to be explained IN the file, so an undocumented gap cannot masquerade as this one | landed (5 ids stranded by design) |
| **MB/PC RELEASED ZERO — the reservations were undersized.** Both consumed their block EXACTLY to its top (MB 0060 of 0046-0060; PC 0055 of 0041-0055). The freeze was actively constraining both sessions, and PR #274 has since had to bump MB past its block to file ED-MB-0061. Size the next pre-allocation from observed burn rate | W5 | (this PR) | ED-IN-0098 | the measurement itself: max-allocated == block top for both lanes | recorded |
| **MB's `id_reservations.yaml` line left BYTE-UNTOUCHED by this PR — a collision avoided at the gate.** The first draft rewrote MB's inline comment. Open PR #274 bumps that exact line (61→62, ED-MB-0061), so the edit would have clobbered a concurrent lane's allocation or conflicted. Reverted; MB's line is identical to `origin/main`. This is the same class as the `registry.py` interlock and was caught only by checking OPEN PRs — reading merged ledgers alone is NOT sufficient to know which ids are claimed | W5 | (this PR) | ED-IN-0098 | `git diff --cached HEAD -- references/id_reservations.yaml` contains no `MB: {` line; the walk-back guard would independently fail if MB's pointer ever fell to/below its max-allocated | landed |
| **P2 REDESIGNED ON EVIDENCE — a lane-ownership gate would NOT have caught the incident it was proposed for.** MEASURED: `build_decisions.infer_lane('tools/registry.py')` returns `IN`, and W4 *was* the IN session — the collision was between two concurrent programs INSIDE one lane, which lane inference cannot see. Built `tools/ci_program_claim_check.py` instead: it reads backticked repo paths out of LIVE `workplans/POINTER_*.md` scope lines (existing convention, no new register) and warns when a diff touches a path another live program claims. This is the executable form of the interlock CSO declared and never installed | W5 | (this PR) | ED-IN-0098 | `tests/valoria/test_program_claim_check.py` — 7 tests, primary one **REPLAYS the real incident**: the W4 retirement diff must be flagged with CSO named as claimant. Also pins prose-backtick rejection (`resolve()` is not a file) and LIVE-only claims | landed (report-only) |
| **P1 — `tools/ci_vacuous_assertion_check.py`.** §0.1 #2 was vigilance-only and vigilance measurably failed twice in one wave. AST scanner, two tiers: PROVABLE (V1-V5, unfalsifiable by construction) and SUSPICIOUS (S1/S2, heuristic, never counted as violations). **It found a THIRD instance nobody had reported** — `engine/tests/test_accounting_accord_drift_probe.py:149`, whose own comment conceded it could not fail ("never negative -- an additive-only counter"), leaving that test's stated boundary claim asserted by nothing. Fixed to `> 0` (live value 461) after first drafting a cross-boundary equality check and discovering `CampaignResult` has no `.world` handle — dead-branch code removed rather than shipped | W5 | (this PR) | ED-IN-0098 | `tests/valoria/test_vacuous_assertion_check.py` — 24 tests: both real historical shapes as fixtures, 10 must-NOT-flag precision cases, and a corpus baseline asserting **0 provably-vacuous assertions across 132 files** (so the PROVABLE tier could be promoted to blocking today) | landed (report-only) |
| **The tool's own test found a bug in the tool.** `assert len(x) > -1` was not detected: `-1` is `UnaryOp(USub, Constant(1))`, not `Constant(-1)`, so the comparison silently never matched. Added `_const_num()` and routed all three numeric comparisons through it. Recorded because a detector with a silently-unreachable rule is the §0.1 #2 defect *in the detector* | W5 | (this PR) | ED-IN-0098 | the parametrised `test_provable_rules_fire[assert len(x) > -1-V2]` case, which failed before the fix and passes after | landed |
| **P3+P6 — `tools/ci_gate_coverage.py`.** Derives the gate from the workflow instead of recalling it: parses all 34 CI jobs, prints every pytest root with the jobs that run it, flags roots the diff touches, lists the 21 blocking non-test jobs, and warns when `origin/main` is ahead of HEAD (CI tests the MERGE; a local run does not). **It immediately found a SECOND root the W4 gate missed: `tests/contracts`** (also in `unit-tests`) — 34 tests never run by that gate. So W4 skipped two roots, not one | W5 | (this PR) | ED-IN-0098 | `tests/valoria/test_gate_coverage.py` — 7 tests pinning the root SET to exactly {tests/valoria, tests/contracts, engine/tests}, so a root added to CI and missed here fails rather than passes | landed (report-only) |
| **P4 — `on_exceed` vocabulary, single-owned and closed.** Two tokens had NEVER worked: `warn_only` (12 files) and `chunk_by_quarter` (a split strategy, not a severity), both silently graded as blocking errors by `severity = 'warn' if on_exceed.startswith('flag') else 'error'`. Now `_on_exceed_severity()` with an explicit blocking/advisory split; an unimplemented token raises `UnknownOnExceedToken` naming the token AND the declaring file. **Found while fixing it:** the CI-mode path carried its own divergent copy that never honoured `skip`, so an explicitly-exempted file over cap graded as blocking there while exempt on the other path — both sites now route through the owner | W5 | (this PR) | ED-IN-0098 | `tests/valoria/test_compliance_on_exceed_vocabulary.py` — 11 tests incl. every-declared-token-implemented, unknown-token-raises, blocking-tokens-still-block (no over-correction), and no-inline-copy-survives | landed |
| **P5 — `landed` accepted as terminal.** `_is_resolved()` rejected `landed` though ED-IN-0095/0096 already used it, so the first citation of a `landed` ED from a scanned prefix turned a BLOCKING gate red for a bookkeeping reason. W4 patched the ENTRY instead of the vocabulary — the wrong end, leaving the trap armed. Fixed at the vocabulary end | W5 | (this PR) | ED-IN-0098 | `validate_ed_citations.py` exit 0 with ED-IN-0095/0096 still `landed`; this PR's own `references/` citations of ED-IN-0098 resolve | landed |
| **CONTROL: the 16 `tests/valoria` failures are INHERITED, not this batch's.** `origin/main` @ `3005096` was checked out in an isolated worktree and run clean: **16 failed / 1184 passed** — the same count and overlapping names (MB lane, from #271). ⚠ FURTHER FINDING: the four implicated files **PASS in isolation** (0 failures) and fail only in a full-suite run, so these are ORDER/STATE-dependent, not deterministic per-file failures — consistent with PR #274's "environment-unstable" digest-gate note. Owner: MB lane. Recorded, not fixed (out of lane) | W5 | (this PR) | ED-IN-0098 | the control run itself, in `scratchpad/baseline` at `3005096`; per-file runs of the four implicated modules return 0 failures on BOTH trees | recorded, routed to MB |
| **NOT EXECUTED, deliberately — P7 (PC's F-1/F-2/F-3 slotting).** The recommendation stands: F-1 (half-sword form carrying across engagement boundaries, no test sees it) is the same class as E1a, which the PC plan calls "the most urgent item in the whole plan", so it belongs in the E-track rather than behind a ⚖ gate. NOT done here because it edits a PC-owned plan while the PC session is actively merging (#273 merged, #275 open) — editing another lane's live plan mid-flight is the exact coordination failure this batch built a detector for | W5 | (this PR) | ED-IN-0098 | n/a — routed, not executed | **routed to PC** |
| **NOT EXECUTED, deliberately — P8 (5 chronically stale audit families).** vector-audit is 199 files behind, decisions-digest 71, proposals 49, apparatus 22, graph-lexicon 12. Which families are load-bearing enough to wire into `audit-refresh.yml` versus retire is a judgement about what the corpus needs, not a mechanical fix — Jordan's call. The warnings have become background noise, which is worse than absent | W5 | (this PR) | ED-IN-0098 | n/a — filed for a ruling | **needs Jordan** |

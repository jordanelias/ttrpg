# Execution ledger — code-shape open-items program (ED-IN-0091)

## Role

This file is the program's **single STATUS surface**. `00_open_items_register.md`,
`01_orchestration_plan_v1.md`, and `02_disposition_map.md` are immutable snapshots taken at
program start (plus the 2026-07-29 adversarial reconciliation baked into the disposition map) —
**they do not change as waves execute.** Every wave (W0–W5) appends its rows here, and W5's
capstone additionally completes the diff against `02_disposition_map.md` (closing the
created-by-W5 / appended-by-every-wave tension the orchestration plan otherwise leaves open —
flagged explicitly in the W0b PR body). If a register/plan/map claim turns out stale or wrong
during execution, the correction lands as a row here, not as an edit to those files.

## Column format

`OI/item · wave · PR · ED · falsifier artifact · outcome`

## Rows

| OI/item | wave | PR | ED | falsifier artifact | outcome |
|---|---|---|---|---|---|
| W0a — 7-lane pre-allocation + `id_reservations.yaml` file freeze (no per-item OI; reservation only) | W0a | #256 | (reservation only, no ED filed) | `references/id_reservations.yaml` lane lines (IN 0092–0111, MB 0046–0060, PC 0041–0055, WR 0009–0012, FA 0036–0039, SE 0049–0052, SC 0017–0020; file frozen until W5) | done |
| §5 Jordan docket authored, HELD FOR JORDAN (rulings not yet made) | W0b | (this PR) | ED-IN-0092 | `05_jordan_docket_v1.md` | open — awaiting Jordan |
| OI-55 — orphan-detector integrity: `__init__` relative-import misresolution, CLI-entry-point noise, `vector_audit.py` known-answer coverage, re-scoped at W0b | W0b | (this PR) | ED-IN-0092 | `skills/valoria-vector-audit/scripts/structure_audit.py` diff + `tests/valoria/test_structure_audit.py` + `tests/valoria/test_vector_audit.py` (new known-answer cases) | landed |
| G12 correction — the `__init__` misresolution half of OI-55 was already fixed pre-W0b; re-verified not re-done | W0b | (this PR) | ED-IN-0092 | `structure_audit.py` lines ~271–278 (existing fix comment), confirmed by re-read, no duplicate change made | verified stale claim, no-op |
| G12 correction — the "no known-answer coverage beyond one total-pin" claim was already stale pre-W0b; re-verified not re-done | W0b | (this PR) | ED-IN-0092 | `tests/valoria/test_vector_audit.py` (pre-existing coverage beyond a single total-pin case), confirmed by re-read, no duplicate change made | verified stale claim, no-op |
| execution ledger created (this file) | W0b | (this PR) | ED-IN-0092 | this file's existence + first two waves' rows | done |
| critic relay (W0b) — 3 lanes upheld, 4 fixable defects folded in (docket OI-41 row, 2 stale citations, cli_entries caption + conservation test) | W0b | (this PR) | ED-IN-0092 | the critic report in the W0b PR body + this file's rows | done |
| duplication logged, not chased (§0.1 #5): __main__-guard predicate now defined in structure_audit.py (AST) AND build_apparatus_registry.py:116 (regex, pre-existing, behaviorally different) — single-owner consolidation routed to Wave 4 item 5 (which owns build_apparatus_registry inconsistencies, OI-15) | W0b→W4 | (this PR) | ED-IN-0092 | Wave 4's dedup mutation check when consolidated | open — routed to W4 |
| stale surface found by critic (OI-53 class): build_apparatus_registry.py:232-234 globs the deleted designs/audit tree, silently degrading its orphan signal — routed to Wave 4 item 3's re-verify list | W0b→W4 | (this PR) | ED-IN-0092 | W4's extended dead-root guard test | open — routed to W4 |
| OI-56 — pipeline-reach acceptance oracle: `engine/tests/test_pipeline_reach.py` (new), asserting per-direction coverage with `assert checked >= N` (CLAUDE.md §0.1 point 2), one `XFAIL_MANIFEST` row per still-unwired direction (dynamic `strict=True` xfail keyed to live introspection, not a hardcoded red) | W1 | (this PR) | ED-IN-0093 | `engine/tests/test_pipeline_reach.py` — POST-FIX-BATCH reality (corrected, see fix-round-2 row below): 21 test functions, 13 passed + 8 xfailed, decorative unconditional counters removed (the pre-fix-batch "29 passed" figure was measured before the fix batch's test restructuring and does not reflect the final tree) | landed |
| OI-01 — combat dispatch bridge, FLAG-OFF: `engine/cross_scale/combat_bridge.py` (new, IN-side only, mirrors `_emergency_council_parties`/ED-SC-0006), wired behind `DISPATCH_COMBAT_BRIDGE` (default off) in `scene_dispatch.py`; `engine/tests/test_combat_bridge_seam.py` (new) is a SHAPE/CONTRACT-LEVEL-ONLY characterization test against `combat_engine_v1/wrapper.py`'s public API (never a value/balance pin). ON-flip deliberately NOT scheduled in this wave — happens only after PC batches E0–E3 merge, as a named single-owner (IN) golden re-record citing both plans | W1 | (this PR) | ED-IN-0093 | `engine/tests/test_combat_bridge_seam.py` (shape-only), `test_pipeline_reach.py`'s `combat-bridge-on` XFAIL_MANIFEST row (id verified against `test_pipeline_reach.py:78`; env `DISPATCH_COMBAT_BRIDGE=1` flips it) | landed, flag OFF |
| OI-02 — field investigation dispatch closure: `fieldwork.py`/`investigation.py` converted from unconditional `NotImplementedError` to self-flagging `stubwire.stub_resolve` calls, reached from `scene_dispatch.py`'s investigation/fieldwork branch (no more silent "not live" deferral) | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py` OI-17 strict-conversion assertions (`systems/fieldwork/sim/{fieldwork,investigation}.py` in the converted set) | landed |
| OI-17 — the Pass-2l armature-stub class converted to `stubwire`: `charter_liberties`, `infrastructure_reclamation`, `home_sanctuary`, `varfell_territorial_acquisition`, `varfell_mandate_action`, `hafenmark_equipment` (factions), `fieldwork`, `investigation` (fieldwork), `rs_track`, `ip_track` (overview), `miraculous_event`, `restoration_movement` (world), `companion` (characters), `rendering` (threadwork), `engine/cross_scale/articulation.py`, `engine/autoload/npc_ai.py` — 16 modules. **Two named exclusions, both accepted cross-session handoffs, neither converted here**: `systems/mass_battle/sim/altonian_reinforcements.py` (MB-owned, routed to MB plan §12 I1, critic F9 — an IN exit criterion may not be hostage to another session's schedule) and `systems/social_contest/sim/contest/{dictionaries,modes,wrapper}.py` (OI-18a/OI-19 own these — self-flag-only scope, see below, not a full OI-17 conversion) | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py` OI-17 strict block (asserts the converted-set membership); `tests/valoria/test_stubwire.py` (mutation-checked: delete the import, audit/telemetry/ratchet all fail) | landed — 16 converted, 2 excluded (see above) |
| OI-18a — contest GAMES router stub rows + mode scaffolds, SELF-FLAG ONLY (plan's explicit scope note, not a full resolver build): `contest/dictionaries.py`, `contest/modes.py`, `contest/wrapper.py` stub rows now self-flag via `stubwire` | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py::test_oi18a_contest_games_router_stub_rows_are_self_flagged`, `::test_oi18a_mode_scaffolds_are_self_flagged` | landed |
| OI-19 — partial `NotImplementedError` branches self-flagged via `stubwire`, live branches left untouched: `tribunal.py:149` (§7 Asymmetric Proceeding), `treaty.py:107`, `contest/dictionaries.py:710`. `resolver.py:51` (abstract base) is **deliberately excluded as recorded-benign** — it is the ABC's own `NotImplementedError`, not a live-path stub | W1 | (this PR) | ED-IN-0093 | `test_pipeline_reach.py::test_oi19_...` partial-branch self-flag assertions, explicit `resolver.py:51` exclusion comment in the same test | landed, resolver.py:51 recorded-benign |
| OI-10a — (folded into the OI-17 conversion pass above; no separate module, no separate row beyond this note — OI-10a's scope was already covered by the OI-17 stub census and converts under the same `stubwire` pass) | W1 | (this PR) | ED-IN-0093 | see OI-17 row's falsifier | landed (subsumed by OI-17) |
| `registers/review_baseline.yaml` `stubs.count` ratchet seeding — measured post-conversion `stub_wired` count (23, via `python3 tools/review_core.py --check` / `tools/structure_audit.py`, 2026-07-29 uncommitted Wave-1 tree) + 1 for the MB-owned `altonian_reinforcements.py` (critic F4, not yet converted) = seed **24**, replacing the Spine-stage placeholder 0 + TODO — **SUPERSEDED same PR cycle by the fix-batch row below (24→25): this row's own 23 measurement predates the fix batch's `_kernel_tests.py` stubwire import** | W1 | (this PR) | ED-IN-0093 | `registers/review_baseline.yaml` `stubs.count` block (arithmetic in the YAML comment); `python3 tools/review_core.py --check` no longer regresses at the new baseline | landed, superseded (see fix-batch row) |
| `registers/review_baseline.yaml` `stubs.count` RE-SEED (the fix-batch row the two rows above and `HANDOFF_IN.md` both point at): the fix batch's contest-kernel guard rewrite added a `from engine.substrate.stubwire import StubResult` import to `systems/social_contest/sim/contest/_kernel_tests.py`, moving that file into the `stub_wired` predicate set (importers of `engine.substrate.stubwire`, per the predicate's own "ceiling, not census" semantics). Re-measured post-fix-batch `stub_wired` count = **24** (was 23) + 1 for the still-unconverted MB-owned `altonian_reinforcements.py` (critic F4) = new baseline **25**, replacing the superseded 24 seed above. `tools/review_core.py`'s `stubs.count` registry comment updated to match (was stale "seeded at baseline 0 with a loud TODO") | W1 | (this PR) | ED-IN-0093 | `registers/review_baseline.yaml` `stubs.count` block (baseline: 25, target: 25, arithmetic + predicate-semantics comment); `tools/review_core.py`'s `stubs.count` entry comment; `python3 tools/review_core.py --check` at the new baseline | landed |
| adjudicator+critic relay (W1): 3 defects + 16 verdicts, all adjudicated by the orchestrator; fixes in this batch | W1 | (this PR) | ED-IN-0093 | this fix-batch diff + the re-critic pass | done |
| re-critic round 2: D1 resolved-before-resolve bug fixed; echo attribution reverted to precedent-degenerate winner-self-echo with the attribution model named a flip precondition; ledger reconciled | W1 | (this PR) | ED-IN-0093 | this row + the diff | done |
| FLIP-BLOCKER (named): OFF/ON context-vocabulary dialect — OFF reads ctx['participants'], ON reads faction aggregates; reconciliation + ctx['echo'] verification + a CI flag-ON job are preconditions of the deliberate DISPATCH_COMBAT_BRIDGE ON-flip (post PC E0–E3). EXTENDED (re-critic round 2, item 2e): a THIRD, separate precondition is the cross-faction echo-attribution design question — `scene_dispatch.py`'s combat branch currently reuses the contest precedent's degenerate self-echo shape verbatim (`actor_faction == target_faction == winner's fid`) because no attacker/defender attribution model is ruled; sign (who is credited on a win, who if anyone is debited on a loss), direction, and magnitudes are all UNRULED. The ON-flip needs (1) the OFF/ON context-vocabulary reconciliation above, (2) a ruled attribution model replacing the degenerate self-echo (or an explicit decision to keep it), and (3) a flag-ON test with `world.echo_scheduler` attached (today's `test_flag_on_is_a_no_op_on_the_currently_reachable_campaign` only proves reachability is a no-op absent a combat-scene trigger — it does not exercise the echo path at all, since nothing queues a `combat` scene_type yet) | W1→flip | (this PR) | ED-IN-0093 | the flip PR's own checklist; `scene_dispatch.py`'s combat-branch echo-attribution docstring | open — held for the flip action |
| seam declaration WIDENED: combat bridge consumes wrapper.fight + Combatant constructor (both PC-owned, as-is); plan §0's 'wrapper public API' under-described | W1 | (this PR) | ED-IN-0093 | combat_bridge.py header comment | done |
| duplication logged, not chased: massbattle.py:1875 performs the identical Mil→power scale-crossing derivation with its own [GAP] note — MB-owned; flagged for the MB session via this ledger + PR body, single-owner resolution deferred | W1 | (this PR) | ED-IN-0093 | MB session's eventual consolidation | open — MB-visible |
| stubs.count predicate refinement candidate: 'imports stubwire' counts telemetry consumers; a call-site predicate (stub_resolve invocation) would be exact — deferred, W5 candidate | W1 | (this PR) | ED-IN-0093 | future predicate's own test | open — routed to W5 |
| _KERNEL_EXPECTED count-pin declared move 385→385 (measured post-conversion: OI-18a/OI-19's raise->StubResult rewrite replaced 5 checks 1-for-1, so the total held) | W1 | (this PR) | ED-IN-0093 | test_contest_kernel.py comment | done |
| ED-IN-0094 intake — fractional triad RULED; routed PC/MB/SC/IN per the ledger entry; IN implementation scheduled as its own PR (dice-core primitive + integer-baking census) | intake | (this PR) | ED-IN-0094 | the IN census PR's per-site declared-golden list | open — IN half scheduled |
| decision item for Jordan: enable repo Settings → Allow auto-merge (autonomous merge loop currently needs a bounded timer + manual merge call) | intake | (this PR) | — | — | open — Jordan |
| CI-trigger anomaly (decision item for Jordan): GitHub Actions stopped firing pull_request runs for this session's PR events (PR #265: no run on open, force-push, empty-commit push, or close/reopen; W0a/W0b PRs did fire) — W1 merged on full local CI parity instead; affects all three concurrent sessions' PRs; Jordan may want to check Actions settings/token behavior + enable repo auto-merge | W1 gate | #265 | — | the push-to-main CI run on 304242f (check it ran) | open — Jordan |
| same-lane coordination note: ED-IN-0103 was allocated from the reserved IN block 0092-0111 by a concurrent session (PR #262, centralization/single-ownership plan) — no collision (this program is at 0094); future program IDs continue sequentially and reconcile-on-merge per precedent | intake | (this PR) | ED-IN-0094 | this row | noted |
| OI-12 census (documentation only, no code edits) — **already-stub-wired(W1), 7 of 14**: `engine/autoload/npc_ai.py`, `systems/characters/sim/companion.py`, `systems/overview/sim/rs_track.py`, `systems/overview/sim/ip_track.py`, `systems/threadwork/sim/rendering.py`, `systems/world/sim/miraculous_event.py`, `systems/world/sim/restoration_movement.py` — re-verified in-tree 2026-07-29: each `from engine.substrate import stubwire` + `stubwire.stub_resolve(...)` body (confirmed by direct read, not the register's summary). Note: `rs_track.apply_rs_delta` DOES have a real caller — `engine/cross_scale/echo_transport.py:219-220` (accord-echo lane, OI-03) — so it is not import-orphan in the structure_audit sense even though its own body is a stub; recorded here as a correction to the flat "stub-wired, nothing to do" framing, not a wiring action by this lane | W2 | (this PR) | ED-IN-0091 | direct re-read of each file's stubwire import/body (2026-07-29); `echo_transport.py:215-220` for the rs_track caller correction | census done, no code touched |
| OI-12 census — **verified-orphan-no-specified-callsite, 7 of 14**: `systems/threadwork/sim/co_movement.py` (159 LOC, real `draw_comovement_card`/deck logic per threadwork_v30.md §4.3, ED-577 — zero real Python `import` from any other file; only a `game_state.py:191` field-comment note "from systems/threadwork/sim/co_movement", not a call), `systems/threadwork/sim/collective.py` (201 LOC, `attempt_collective_operation` per §2.5 — zero importers; threadwork_v30.md never names an invoking module), `systems/threadwork/sim/opposing.py` (258 LOC, `resolve_opposing_operations` per §2.6 — zero importers; same doc silence), `systems/settlements/sim/settlement.py` (204 LOC, `SettlementState` derivation per settlement_layer_v30.md §1.2-1.3 — zero real importers; many hits in a naive grep are docstring "Dependencies:" prose or unrelated same-word matches, not imports — re-verified with `^from ... import` anchoring), `systems/settlements/sim/temperaments.py` (170 LOC, §2-4 typology/drift per territory_temperaments_v30.md — zero real importers, same false-positive pattern as settlement.py), `systems/social_contest/sim/parliamentary_stay.py` (106 LOC, `invoke_stay`/§10.1 ED-631 — zero importers; social_contest_v30.md names no invoker), `engine/autoload/registry.py` (120 LOC, `load_index`/`get_mechanic` mechanics_index.yaml loader — zero real importers despite a large naive substring-grep hit list, all false positives on the word "registry"). For all 7: grepped each module's own docstring canon-source line AND its subsystem design head for a specified call site — **none found**; confirms rather than refutes the preflight's finding. No module force-wired or stub-converted (all are real implemented code, not stubs) | W2 | (this PR) | ED-IN-0091 | precise `^from <module> import`/`^import <module>` anchored greps per module (2026-07-29, superseding an initial naive substring pass that false-positived on `settlement`/`registry`/`temperaments` as common words); doc greps against `threadwork_v30.md`, `settlement_layer_v30.md`, `territory_temperaments_v30.md`, `social_contest_v30.md`, `mechanics_index.yaml` | census done, no code touched — honest recorded miss, W5 census input |
| OI-12 census — provisional flag: `settlement.py`/`temperaments.py` orphan status is per the current tree at census time; if the World lane (OI-07, same wave) adds a settlements-registry call site touching either file, that lane's own `files_touched` supersedes this row at bookkeeping time — mark provisional, not final | W2 | (this PR) | ED-IN-0091 | World lane's `files_touched` list (cross-check at Bookkeeping phase) | provisional, pending Bookkeeping cross-check |
| Bookkeeping cross-check on the provisional flag above: World lane's `systems/settlements/sim/registry.py` diff (`populate_from_geography`) does NOT import or call `settlement.py`/`temperaments.py` (`git diff` shows no new `import`/`from` line naming either module) — the census row's orphan finding for those two modules is CONFIRMED, not superseded | W2 | (this PR) | ED-IN-0091 | `git diff systems/settlements/sim/registry.py` (no import of `settlement`/`temperaments`), re-grep of both modules at bookkeeping time | confirmed, provisional flag resolved — still orphan |
| ED allocation (Bookkeeping, W2, per plan Bookkeeping-phase instruction) — drawn from the W0a-reserved blocks, `id_reservations.yaml` left untouched (file frozen): **ED-IN-0095** (W2 umbrella: seams + oracle + census — OI-03/06/07(settlements)/08/12), **ED-FA-0036** (OI-04 transfer motion path), **ED-WR-0009** (OI-05 NPC generation + NPE season wiring, honest-deferral disposition; no golden re-record needed — see below), **ED-SE-0049** (OI-07 settlements population + serialization). Full entries in `registers/editorial_ledger_in.jsonl`, `_fa.jsonl`, `_wr.jsonl`, `_se.jsonl`, each citing ED-IN-0091 as parent | W2 | (this PR) | ED-IN-0095, ED-FA-0036, ED-WR-0009, ED-SE-0049 | the four lane-ledger entries (schema-matched to each file's existing `id/date/system/status/needs_jordan/confidence/description/citations/source` shape) | done |
| OI-03/OI-06/OI-04/OI-07(settlements)/OI-08 — landed and re-verified live at bookkeeping time (not just per the Oracle-stage claim): `engine/tests/test_pipeline_reach.py`'s XFAIL_MANIFEST no longer carries `accord-echo-leg`/`vertical-up-handoff`/`territory-transfer-resolver`/`world-settlements` rows (4 retired to strict this wave); `engine/tests/test_accord_echo.py`, `test_parliamentary_transfer_bridge.py`, `test_world_population.py`, `tests/valoria/test_articulation_subscriber.py`, `test_handoff_dispatch_validity.py` all pass (67 passed, 4 xfailed on the targeted 6-file run, 2026-07-29) | W2 | (this PR) | ED-IN-0095 | `python -m pytest engine/tests/test_pipeline_reach.py engine/tests/test_accord_echo.py engine/tests/test_parliamentary_transfer_bridge.py engine/tests/test_world_population.py tests/valoria/test_articulation_subscriber.py tests/valoria/test_handoff_dispatch_validity.py -q` → `67 passed, 4 xfailed` | landed, verified |
| OI-05 / world.knots half of OI-07 — CORRECTION to the plan's "golden re-record, named loudly" expectation (plan §3 item 3): re-verified against `investigation_systems_v30.md` SYSTEM 1 and `knots_v30.md` §3.1, neither specifies a world-gen/season-tick trigger — the honest-deferral disposition (permanent, via `stubwire`) means `world.npcs`/`world.knots` STAY empty, so `test_f7_smoke_oracle.py`'s `npcs_generated == 0` pin does NOT move. No golden was re-recorded this wave; the plan's expectation is corrected here rather than executed against the tree's actual (canon-driven) outcome | W2 | (this PR) | ED-WR-0009 | `git diff --stat` confirms `engine/tests/test_f7_smoke_oracle.py` is NOT in the changed-file set; `test_pipeline_reach.py`'s `world-npcs`/`world-knots` rows reclassified `honest-deferral` (permanent xfail) rather than removed | landed — honest deferral, golden untouched |
| Bookkeeping (this stage) — scope respected: no edit made to `engine/tests/test_pipeline_reach.py` (Oracle stage's sole ownership), `systems/combat/**`, `combat_engine_v1/wrapper.py`, `faction_action.py:349`, `references/id_reservations.yaml`, or `registers/review_baseline.yaml`; only this file + the 4 lane JSONL ledgers + `HANDOFF_IN.md` + root `HANDOFF.md` were touched, per this stage's own edit-scope instruction | W2 | (this PR) | ED-IN-0095 | this row + `git status --short` for the actual edit set | done |
| **W2-FIX (orchestrator-adjudicated fix batch, 2026-07-29) — critic 'missing'/defect repairs on top of the W2 rows above.** Scope respected: no edit to `systems/combat/**`, `combat_engine_v1/wrapper.py`, `systems/factions/sim/faction_action.py`, `references/id_reservations.yaml`, `registers/review_baseline.yaml`. Rows below are OI-03 fixes 1-4, OI-04 canon gate, the OI-04 duplication log, the OI-08/G12 re-verify, the three test-honesty repairs, the orphan-count delta, and the deferred OI-04 golden re-record — one row per item, per this batch's own instruction ("rows for every fix above, same format") | W2-fix | (this PR) | — | this row + the rows below | header row, no falsifier of its own |
| OI-03 fix 1 — `echo_transport.classify_scene_outcome`'s `_ACCORD_OUTCOME_BY_SCENE_TYPE = {"combat": "violence"}` fallback DELETED: §5.5 classification now trusts ONLY an explicit caller-declared `echo['scene_outcome']`; scene_type no longer participates. The leg reverts to WIRED-BUT-DORMANT (a real `compute_accord_echo` caller exists; no live producer declares `scene_outcome`) — documented in the function's own docstring rather than left implicit | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py`'s `classify_scene_outcome` (fallback map removed) + `engine/tests/test_accord_echo.py::test_classify_requires_explicit_declared_outcome_even_for_combat` (replaces the retired `test_classify_combat_maps_to_violence`) | landed |
| OI-03 fix 2 — `_apply_accord_echo` RETARGETED to the SETTLEMENT where the scene occurred (AUD-SET-02, `scale_transitions_v30.md:215` + `peninsular_strain_v30.md` §2.5 Category B), never `Territory.accord` directly; `echo_ctx['target_settlement']` replaces `target_territory`. Province Accord stays a read-only aggregate (`systems/settlements/sim/registry.py`'s `province_accord`, `floor(mean(order))`) this function never recomputes | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py::_apply_accord_echo` + `engine/tests/test_accord_echo.py` (governance/violence/territorial_transfer rows re-pinned against `world.settlements[sid].order`, not `world.territories[tid].accord`) | landed |
| OI-03 fix 3 — ONE Accord unit: the write applies in `Settlement.order`'s own canonical-index 0-5 scale (`settlement_layer_v30.md` §1.3 — "Order ... Analogous to province Accord but at settlement scale"), matching `domain_echo.compute_accord_echo`'s already-canonical-index `accord_delta`; no `MULTS`/`ACCORD_MAP` conversion anywhere in the function (that pair is `Territory.accord`'s own, different, continuous 0.5-7.0 scale). Clamped to `registry.STAT_MIN`/`STAT_MAX` | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py::_apply_accord_echo`'s Unit docstring note (cites `settlement_layer_v30.md` §1.3) + `test_settlement_order_never_leaves_the_canonical_0_5_index_bound` | landed |
| OI-03 fix 4 — contract collision: `zoom_in_out.zoom_out` documented `accord_changes` as queued to Accounting while `_apply_accord_echo` actually applied immediately. Genuine queue-parity (routing through the same OF-7 `sched.emit(key, apply=...)` mechanism the sibling §5.2 leg uses) would require registering a NEW `scene.accord_echo` Key type in `key_type_registry_v30.md` — Wave 3 territory, not this lane's, and inventing an ad hoc non-Key deferred queue would grow a scale-local dialect (ED-1083 guardrail). Resolved by RENAMING the key to `accord_applied` and correcting both ends' docstrings to describe immediate application, not queuing — the option the task's own "OR" clause names for exactly this case | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/echo_transport.py` (`accord_changes` → `accord_applied`, both return sites) + `engine/cross_scale/zoom_in_out.py::zoom_out`'s docstring/loop (`fires_at: 'already_applied'`) + `engine/tests/test_accord_echo.py` (all `out.get("accord_applied")` assertions) | landed |
| OI-04 — canon gates (`parliamentary_transfer_v30.md` §1.1: Frequency "1 per arc per faction" `:27`, Cost "Action slot + CB consumption (whether transfer succeeds or fails)" `:31`, Crown auto-CB "Refreshes per arc if condition holds" `:71`): added `Faction.parl_transfer_used_this_arc` (game_state.py, mirrors `council_used_this_arc`'s existing per-arc-flag shape + `reset_arc()` hook exactly — `arc` is UNAMBIGUOUS in this corpus, `season_manager.SEASONS_PER_ARC = 4` + `World.arc`, no mapping-ambiguity ledger note needed). `propose_transfer` gates on it at the declaration stage (before any CB consumption or roll) and sets it at the same point CB is consumed (the attempt-commit point, unconditional of the eventual degree). `parliamentary_bridge._derive_transfer` excludes an already-arc-gated initiator from candidate derivation itself, so a gated-out season is BYTE-IDENTICAL to a no-qualifying-CB season (verified: zero extra `world.rng` draws in either case) | W2-fix | (this PR) | ED-FA-0036 | `systems/factions/sim/parliamentary_transfer.py::propose_transfer` (frequency gate + cost-on-attempt) + `engine/cross_scale/parliamentary_bridge.py::_derive_transfer` (exclusion) + `engine/tests/test_parliamentary_transfer_bridge.py`'s 4 new tests (`test_propose_transfer_blocks_a_second_attempt_in_the_same_arc`, `test_reset_arc_clears_the_frequency_gate`, `test_derive_transfer_excludes_an_arc_gated_initiator`, `test_gated_out_season_matches_no_qualifying_cb_season_byte_for_byte` — the last asserts identical `rng.getstate()` between a gated-out and a no-CB season) | landed |
| OI-04 duplication log (item C, CLAUDE.md §0.1 point 5 "log, don't chase") — `parliamentary_transfer.py`'s `terr.owner = initiator` is the THIRD `t.owner = <faction>` owner-assign site in the corpus; the other two (`faction_action.py:366`, `mass_seizure.py:292`) are both seam/lane-frozen for this wave (explicit no-touch list). No single-owner consolidation performed — comment at the site points here, this row is the pointed-to record, not a promise of a future sweep beyond what a later wave schedules | W2-fix | (this PR) | ED-FA-0036 | `systems/factions/sim/parliamentary_transfer.py` (comment above `terr.owner = initiator`, "DUPLICATION LOGGED, NOT CHASED") citing this exact ledger row | logged, not chased |
| OI-08 / G12 re-verify (item D) — the plan premise ("`meta.cascade_cluster_event` UNREGISTERED, subscribe to only 9/10") was already stale before this fix batch: ED-IN-0022 (2026-07-07, C-KEY-8) registered it in `key_type_registry_v30.md` (~line 862). `articulation.subscribe_all` already subscribes all 10/10 with the G12-correction comment in place — RE-VERIFIED here (loaded the live registry through the actual runtime `TypeRegistry` parser, confirmed `'meta.cascade_cluster_event' in registry.types`), no code change needed; `tests/valoria/test_articulation_subscriber.py` already asserts `count == 10` (not just `>= 9`) | W2-fix | (this PR) | ED-IN-0095 | `engine/cross_scale/articulation.py`'s G12 CORRECTION comment (pre-existing) + `python3 -c "from engine.substrate.keys import TypeRegistry; r = TypeRegistry.load('systems/_architecture/key_type_registry_v30.md'); print('meta.cascade_cluster_event' in r.types)"` → `True` (re-run 2026-07-29) | re-verified correct, no code change |
| Test-honesty repair 1 (item E, critic 'missing') — `engine/tests/test_world_population.py::test_npc_and_knot_deferral_stubs_fire_every_season`'s `checked = seasons; assert checked == seasons` tautology (CLAUDE.md §0.1 point 2 — `checked` never depended on anything observed) replaced with a REAL per-season conditional counter: the season loop is now driven directly (`systems.overview.sim.season.run_season(world, action_callback=mc_v18._faction_actions_callback)`, mirroring `run_campaign`'s own composition) so `stubwire.invocations` can be sampled BETWEEN seasons and `assert delta >= 2` runs per season, not just once at the end | W2-fix | (this PR) | ED-WR-0009 | `engine/tests/test_world_population.py::test_npc_and_knot_deferral_stubs_fire_every_season` (rewritten) | landed |
| Test-honesty repair 2 (item E) — `engine/tests/test_pipeline_reach.py::test_oi12_census_is_recorded_in_the_execution_ledger`'s `checked == len(<the same fixed tuple>)` assert was the SAME decorative-counter class the file's own module docstring already corrects elsewhere (CORRECTION 2). Split into two tests: doc-presence (unchanged logic, decorative counter dropped) + a NEW `test_oi12_census_matches_the_real_structure_audit_classification`, which re-derives the real orphan/stub_wired sets from `structure_audit.py`'s own graph functions (`collect_py_modules`/`build_g_code`/`split_orphans_and_cli_entries`/`stub_wired_modules` — reused via the same `importlib.util` load `tests/valoria/test_structure_audit.py` uses, not re-implemented) and compares both pinned census tuples against them — genuinely falsifiable | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_oi12_census_matches_the_real_structure_audit_classification` (new) | landed |
| Test-honesty repair 3 (item E) — the articulation reach-guard row (`test_articulation_subscriber_is_wired_and_stub_flags_fire`) CLAIMED production reach ("verified live at that call site") but only ever exercised a hand-built `_world_with_scheduler`, never `run_campaign` itself — a deleted `_articulation.subscribe_all(...)` call inside `run_campaign` would have left it green. New `test_articulation_subscriber_is_wired_in_a_real_production_campaign_construction` patches `articulation.subscribe_all` (via `unittest.mock.patch.object`, `wraps=` the real function) and drives a REAL `run_campaign(..., params={'ECHO_TRANSPORT': 1})`, asserting the production hook actually calls it with a scheduler that ends up with populated subscriptions | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_articulation_subscriber_is_wired_in_a_real_production_campaign_construction` (new) | landed |
| Test-honesty repair 4 (item E) — the accord-echo-leg direction test (`test_direction2b_bottom_up_accord_echo_leg_is_wired`) was RESOLVED/XPASS(strict) on a bare "caller exists" source-scan; OI-03 fix 1 (above) makes that framing stale again (classification now requires an explicit declared outcome nothing supplies). Renamed to `test_direction2b_bottom_up_accord_echo_leg_is_wired_but_dormant` and now asserts BOTH halves: caller-exists (unchanged scan) AND dormancy (a NEW scan over the two actual producer modules, `scene_dispatch.py` + `parliamentary_bridge.py`, confirming neither declares `echo['scene_outcome']` as an input — `echo_transport.py` itself is deliberately excluded from the dormancy scan since its own `_apply_accord_echo` builds an output/telemetry dict containing that same string, which would otherwise false-positive) | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_direction2b_bottom_up_accord_echo_leg_is_wired_but_dormant` (renamed + extended) | landed |
| Orphan-count delta (item F, required exit criterion) — `python3 skills/valoria-vector-audit/scripts/structure_audit.py --stub-count` → **24 stub-wired**, unchanged (this batch converted no new module to `stubwire`). Full audit (`python3 skills/valoria-vector-audit/scripts/structure_audit.py --repo-root . --output-dir <scratch>`) → **code-orphans: 71 (pristine `git archive HEAD`, i.e. before this session's Wave 1+2 work) → 66 (current fixed tree, Wave 1 + this W2-fix batch)**, net **-5**, **0 newly orphaned**. NOTE on the task's cited "W0b PR's 74": the pristine-HEAD re-measurement here gives 71, not 74 — HEAD already carries W0b's OI-55 detector-integrity fixes (commit 9c89030), so 74 was almost certainly measured with the pre-W0b (buggy) detector on the same code, not a different orphan count; used the freshly-verified 71 as the honest baseline rather than citing an unverified prior number. Resolved (no longer orphan): `engine.cross_scale.articulation` (OI-08), `engine.cross_scale.handoff_rules` (OI-06), `systems.factions.sim.parliamentary_transfer` (OI-04), `systems.overview.sim` + `systems.overview.sim.rs_track` (OI-03's `rs_track.apply_rs_delta` caller) | W2-fix | (this PR) | ED-IN-0095 | commands above, run 2026-07-29; `<scratch>/structure_audit_{pristine_out,out}/data/structure_metrics.json`'s `code.orphans` set-diff (71→66, 5 resolved, 0 newly orphaned) | landed, exit criterion met |
| OI-12 census reconciliation (item G) — `settlement.py`/`temperaments.py`'s provisional flag (row above, "provisional flag" / "Bookkeeping cross-check") was ALREADY resolved to CONFIRMED verified-orphan before this fix batch (the World lane's `registry.py` diff never imports either module) — RE-VERIFIED here against the current (Wave 1 + W2-fix) tree via the same `structure_audit` re-derivation as the orphan-count-delta row above: both remain in `code_orphans` (`systems.settlements.sim.settlement`, `systems.settlements.sim.temperaments`), so the confirmed disposition still holds — no further flip needed, nothing was silently re-opened | W2-fix | (this PR) | ED-IN-0095 | `engine/tests/test_pipeline_reach.py::test_oi12_census_matches_the_real_structure_audit_classification` (asserts this membership as a passing test, not just a one-off script run) | re-confirmed, unchanged |
| OI-05 row (item G, honest deferral) — canon makes NPC generation scene-specification-driven (`investigation_systems_v30.md` SYSTEM 1's Two-Tier Generation: "Scene specification declares density and composition"); no world-gen density rule or season-tick generation trigger exists to cite. The open-items register's `00_open_items_register.md:71` classification of OI-05 as class **B** (buildable-this-program) was STALE (G12 — the same "premise re-verified against the working tree and found outdated" pattern as the OI-08 row above): the honest disposition is a PERMANENT deferral, not a buildable gap. Routed to the **WR** docket per `01_orchestration_plan_v1.md` §3.5's deferred-to-lane table pattern and §5 row 9 ("NPC lane ownership ... fold/retire per GAP-K2/K3; lane = Jordan's" — pending fork-9). `npcs_generated == 0` pin (`test_f7_smoke_oracle.py`) is UNCHANGED by this whole fix batch (re-verified: 0 across the re-recorded seed-42..49 batch, item H below) | W2-fix | (this PR) | ED-WR-0009 | `investigation_systems_v30.md` SYSTEM 1 citation + `00_open_items_register.md:71`'s "B" column + `01_orchestration_plan_v1.md` §3.5 / §5 row 9 + `test_f7_smoke_oracle.py`'s unchanged `assert npcs == 0` in `test_f7_scenes_live_insurgency_and_npe_still_islands` | honest deferral, routed WR, npcs==0 pin unchanged |
| **Transfer/golden row (item H) — the deliberate OI-04-attributed golden re-record, now landed** (deferred from the original W2 row above until after OI-04's canon gates (item B) landed, per the adjudicator's control-isolation instruction: attribute the whole transfer-motion effect — landing it in W2 AND this batch's frequency-gate refinement — to OI-04 as ONE isolated comparison, not two separate re-records). Method: pristine `git archive HEAD` export vs. this fixed tree (both `python3 -m pytest engine/tests -q` runs); **8 pinned constants + 1 narrative assertion moved, across 3 files (per-test summary below)**, all and only in files whose call graph reaches `parliamentary_bridge.run_parliamentary_scene` (`test_f7_smoke_oracle.py`, `test_mc_v18_regression.py`, `test_parliamentary_bridge.py`) — every OTHER `engine/tests` file's pins (`test_echo_transport.py`'s flag-OFF golden, `test_combat_bridge_seam.py`, etc.) did NOT move, confirming OI-03 (item A) is golden-neutral as expected (its leg is dormant both before and after, for different reasons — see the OI-03 fix 1 row). Before/after table below; each OLD value is preserved in a comment at its site citing ED-FA-0036/OI-04 | W2-fix | (this PR) | ED-FA-0036 | before/after table below + `python3 -m pytest engine/tests/test_f7_smoke_oracle.py engine/tests/test_mc_v18_regression.py engine/tests/test_parliamentary_bridge.py -q` (green after re-record) | landed |
| §1.1 Action-slot half UNENFORCED (residual): the transfer motion runs outside the faction action-slot economy; only the CB-consumption half + arc-frequency gate landed — FA-lane residual on ED-FA-0036 | W2 | (this PR) | ED-FA-0036 | future FA action-economy wiring | open — FA residual |
| province-Accord aggregation gap (re-critic MEDIUM): registry.province_accord has ZERO live callers — settlement Order never aggregates to province Accord while Territory.accord is still written directly (parliamentary_transfer.py:210, mass_seizure.py:295); the OI-03 settlement retarget is canon-correct but currently feeds an unwired aggregate — routed to Wave 3 contract-truth sweep (accounting recompute wiring) | W2→W3 | (this PR) | ED-IN-0095 | W3's aggregation wiring + a campaign assertion that province Accord derives from settlement Order | open — routed to W3 |
| PLAN-EXIT DEVIATION, LOUD (re-critic MEDIUM — Jordan-visible): 01_orchestration_plan_v1.md:318 requires world-npcs/world-knots reach rows STRICT; both ship as canon-justified honest-deferrals instead (no world-gen density rule exists — G12 register correction; routed WR docket/§3.5/fork 9). The plan snapshot is immutable; THIS ROW is the amendment record and the PR body carries it loudly | W2 | (this PR) | ED-IN-0095/ED-WR-0009 | the WR docket ruling re-pins them | open — deviation recorded |

### Item H — golden before/after table (OI-04-attributed, 2026-07-29)

| File · pin | OLD (pristine `git archive HEAD`) | NEW (fixed tree, Wave 1 + W2-fix) |
|---|---|---|
| `test_f7_smoke_oracle.py::GOLDEN_WIN_SHARE` | `{Crown: 37.5, Church: 12.5, Hafenmark: 12.5, Varfell: 37.5}` | `{Crown: 62.5, Church: 0.0, Hafenmark: 0.0, Varfell: 37.5}` |
| `test_f7_smoke_oracle.py::GOLDEN_WINNERS` (sparse dict — only factions with ≥1 win get a key) | `{Crown: 3, Church: 1, Hafenmark: 1, Varfell: 3}` | `{Crown: 5, Varfell: 3}` |
| `test_f7_smoke_oracle.py::GOLDEN_BATTLES_MEAN` | `30.2` | `35.5` |
| `test_f7_smoke_oracle.py::GOLDEN_SCENES_RESOLVED` | `383` | `463` |
| `test_f7_smoke_oracle.py::test_f7_hafenmark_elimination_lockout` | Hafenmark wins `1/8` | Hafenmark wins `0/8` (verified cause: Crown's OWN restoration CB sometimes selects Hafenmark as the largest-non-Crown-holder target, e.g. seed 49 `('Crown','T10','Hafenmark','transferred')` — Crown reclaiming FROM Hafenmark, not a rescue mechanism for it; ED-FA-0005 remains open) |
| `test_mc_v18_regression.py::GOLDEN_WIN_SHARE` (seed 0, n=2) | `{Crown: 50.0, Church: 0.0, Hafenmark: 0.0, Varfell: 50.0}` | `{Crown: 100.0, Church: 0.0, Hafenmark: 0.0, Varfell: 0.0}` |
| `test_mc_v18_regression.py::GOLDEN_WINNERS` | `{Varfell: 1, Crown: 1}` | `{Crown: 2}` |
| `test_mc_v18_regression.py::GOLDEN_BATTLES_MEAN` | `30.5` | `33.5` |
| `test_parliamentary_bridge.py::_ON_WIN_SHARE` (seed 42, n=8, ECHO_TRANSPORT=1) | `{Crown: 37.5, Church: 12.5, Hafenmark: 12.5, Varfell: 37.5}` | `{Crown: 62.5, Church: 0.0, Hafenmark: 0.0, Varfell: 37.5}` |
| `test_parliamentary_bridge.py::test_flag_on_win_share_golden_and_diverges_from_off`'s narrative assert | `off['Church']==0.0 and on['Church']>0.0` ("the spine brings Church in") | `off['Hafenmark']>0.0 and on['Hafenmark']==0.0` (the still-true weaker claim: the spine still shuts out a survivor, now Hafenmark not Church) |
| **UNCHANGED** (confirmed, not re-recorded) | `test_parliamentary_bridge.py::_OFF_WIN_SHARE`, `_ON_KEYLOG_HASH`, `_ON_SCENES_RESOLVED`, `_ON_KEYS_EMITTED` (single seed-42 campaign — Crown never drops under 6 territories on that one seed); `test_f7_smoke_oracle.py`'s `npcs_generated`/`insurgencies_formed` (both stay 0); `test_echo_transport.py`'s flag-OFF golden; every non-parliamentary-bridge-reaching `engine/tests` file | — |


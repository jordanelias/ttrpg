# A vs B — Declared contracts (PR #298) vs as-built flow skeletons

**A** = `references/CONTRACT_INDEX.md` (27 modules) + `references/KEY_INDEX.md` (55 key types),
GENERATED by `tools/build_contract_index.py` from `references/module_contracts.yaml` +
`references/key_graph.json` + `references/wiring_manifest.yaml`. The **DECLARED** view.

**B** = the 15 `systems/<x>/<x>_flow_skeleton_v1.md` files, hand-traced from code at `6545067`.
The **AS-BUILT** view.

Everything below was adjudicated against the working tree, not against either artifact's prose.

---

## 0. Headline

**A and B do not contradict each other about the world.** Every apparent conflict I chased
resolved into (ii) — two layers correctly reported. The two artifacts are close to
complementary: A's `wiring_manifest.yaml` `build:` notes and B's §7 "Traced gaps" independently
reach the *same verdicts* on threadwork, piety_track, social_contest, settlement_layer,
scene_slate, peninsular_strain and mass_battle, from opposite directions and with no shared
source. That convergence is the strongest evidence either artifact is right.

The genuine defects are **inside A**, and they are visible only because B exists:

| # | Defect | Class |
|---|---|---|
| D1 | 2 key types (`scene.accord_echo`, `meta.cascade_cluster_event`) fall through **every** A review queue | A blind spot |
| D2 | A's stated `authority` derivation rule is false for `mass_battle` | A doc↔A output |
| D3 | `wiring_manifest.yaml:68` "IP isn't even a clocks key" is false | A source-data error |
| D4 | A renders `build:`/`godot:` but discards `wiring_manifest`'s `note:` — the field that carries the reconciling nuance | A rendering loss |
| D5 | A's §3 framing "the contract side is simply unauthored" is false for `articulation_layer` — it IS authored, as `*` | A framing |

One defect is **inside B**: B's fieldwork skeleton never audits its own contract's declared
`consumes: {type: "*"}` or its 4 declared emits, though B's threadwork/characters/victory
skeletons all audit theirs. Asymmetric rigour, not a wrong claim.

---

## 1. The five cross-checks

### CC1 — `env.crisis`: is it in A's list of 8?

**YES. A and B agree, and both are right.**

A, `KEY_INDEX.md` §1 "8 key type(s) nobody consumes", first row:
`env.crisis` | environmental | producers `peninsular_strain`, `scenario_authoring`.

B, `systems/overview/overview_flow_skeleton_v1.md:180`: "`peninsular_strain`'s contract declares
`env.crisis` among its emits, also emitted by `scenario_authoring`; no `consumes:` row anywhere in
the file carries `env.crisis` … a dangling emit."

A goes further than the question implies — it carries **two additional A4 violations naming the
same key** (`CONTRACT_INDEX.md:118`, `:139`), one per producer, each with the load-bearing
qualifier B does not state: *"universal readers `['articulation_layer', 'fieldwork_knots']` see it
but do not count."* That is the wildcard-consumer exclusion, and it is why `env.crisis` counts as
unconsumed despite two modules declaring `*`.

**Code adjudication.** `grep -rn "env\.crisis"` over the tree:
`references/module_contracts.yaml:649` (peninsular_strain emits),
`references/module_contracts.yaml:952` (scenario_authoring emits), plus `key_graph.json`,
`key_types.json`, `execution_map.json`, glossary and audit data. **No `consumes:` row. No `.py`
file outside two `tests/valoria/` fixtures.** Both artifacts confirmed.

**B's extra, which A structurally cannot reach:** none of `peninsular_strain`'s *four* declared
emits is produced by any production `.py`. Verified independently:

| key | production `.py` hits |
|---|---|
| `env.crisis` | none (2 test files only) |
| `env.disaster` | none (1 test file only) |
| `env.peninsular_strain_shock` | `articulation.py` (subscription roster), `temperaments.py`, `dashboard_data.py` — no emitter |
| `env.population_change` | none (1 test file only) |

So A's "dangling emit" is one instance of a whole module whose emit set is 0/4 built. A is
generated from declarations and cannot see this.

**Verdict: no contradiction. Both right. A is the more precise of the two on this key.**

---

### CC2 — 41-of-42 `articulation_layer` under-declaration vs B's "wildcard `*` in contract, 13 explicit ids in code"

**Different things. No contradiction — but A's framing sentence is wrong.**

They are three distinct facts at three layers:

| layer | fact | source |
|---|---|---|
| registry | `key_type_registry_v30.md` names `articulation_layer` as a consumer of **43** individual key types | `key_graph.json` |
| contract | `module_contracts.yaml` declares **one** consumer edge: `{type: "*", from: engine}` | `module_contracts.yaml:972` |
| code | `articulation.py` subscribes to exactly **13** explicit type ids; no wildcard subscription exists | `articulation.py:116-130` |

A measures registry-vs-contract (43 named, 0 declared per-key → an under-declaration column).
B measures contract-vs-code (`*` declared, 13 subscribed → a divergence). Neither observes the
other's pair. **Not a contradiction.**

**Both verified against the tree.** `module_contracts.yaml:972`:

```yaml
    consumes:
      - {type: "*", from: engine}   # universal reader of the full Key stream (substrate §8.7);
                                    # registry also lists 31 explicit per-type subscriptions —
                                    # kept as the wildcard
```

`articulation.py:116-130` — `_TRIGGER_TYPE_IDS` is a 13-tuple: `state.scar_acquired`,
`state.coup_attempted`, `state.succession`, `mechanical.mission_shift`, `da.covert_betrayal`,
`meta.knot_formed`, `meta.knot_ruptured`, `env.peninsular_strain_shock`,
`meta.cascade_cluster_event`, `state.belief_revised`, `scene.combat_resolved`,
`scene.combat_felled`, `scene.accord_echo`. **B is exactly right.**

**D5 — A's framing is false here.** `KEY_INDEX.md` §3 header reads: *"42 key type(s) where the
registry names systems the contracts have not declared back. No contradiction — the contract side
is simply unauthored."* For `articulation_layer` the contract side is **not** unauthored: it is
authored as a deliberate wildcard, and the YAML comment says so in as many words ("kept as the
wildcard"). A even knows this — its own §5 lists `*` as declared by `articulation_layer`, and its
A4 warnings name articulation as a "universal reader … [that does] not count". The
under-declaration table and the wildcard section are two views of one authored decision, and A
presents the first as an unauthored backlog. This is a wording defect, not a wrong number.

**D1 — the sharper finding, and A's real blind spot.** A reports **41** articulation_layer
under-declared edges. The graph says **43**. The two missing are `meta.cascade_cluster_event` and
`scene.accord_echo`.

Cause, adjudicated at `tools/build_contract_index.py:239-240`:

```python
    under = sorted(k for k, v in real.items()
                   if v['reconciliation']['consumer_status'] == 'registry_superset'
                   or v['reconciliation']['producer_status'] == 'registry_superset')
```

`registry_superset` means *the contracts named some consumers and the registry named more*. Both
missing keys are `consumer_status: registry_only` / `producer_status: registry_only` — the
contracts name **zero**. They are therefore not a superset case and are dropped.

They are also not in "nobody produces" (they have registry producers), not in "nobody consumes"
(they have registry consumers), not in "contradictions", not in "unresolved references", and not
in "not key types". Verified: `grep` for either string in `references/module_contracts.yaml`
returns **nothing**; `grep` for either in the review-queue prefix of `KEY_INDEX.md` returns
**nothing** (they appear only in the per-family listing).

**These are exactly the two keys B singles out.** `scene.accord_echo` is one of only *two* Key
types with any live construction site in the whole engine
(`engine/cross_scale/echo_transport.py:309-311`, `type="scene.accord_echo"`) — B records it in the
articulation skeleton (§6, §7) and the `_architecture` skeleton (S3.2.2.4.7).
`meta.cascade_cluster_event` is one of B's ten subscribed-with-zero-emitter ids
(`articulation_flow_skeleton_v1.md:125`).

So the two key types that are *most* interesting at the code layer are the two A's review queues
cannot see. **This is a genuine, actionable defect in A.**

**Verdict: describe different things; no contradiction on facts; A carries one framing defect (D5) and one queue blind spot (D1).**

---

### CC3 — A says `*` is a wildcard subscription, not a key type. Does B contradict?

**No. B is compatible and adds the code half.**

A, `KEY_INDEX.md` §5: *"`*` is the known and deliberate one — a **wildcard subscription** meaning
'every key', not a malformed name (ED-IN-0149). … declared by `articulation_layer`,
`fieldwork_knots`."*

B never treats `*` as a key type. `articulation_flow_skeleton_v1.md:130` reads: *"`module_contracts.yaml`
declares articulation as a universal wildcard reader of the Key stream (`{type: "*", from: engine}`);
the actual code subscribes to exactly the 13 explicit ids in `_TRIGGER_TYPE_IDS` and nothing else —
**no wildcard subscription exists in this module**."* That is a statement about the *code*, not
about `*`'s status as a name. Fully compatible.

Mechanically, A's classification is also right in the generated graph: `*` is the sole
`well_formed: false` entry (55 well-formed + 1 malformed = 56), which is why A's title says 55 key
types and `*` is handled in its own section rather than counted.

**One asymmetry worth recording.** A names `fieldwork_knots` as the second `*` declarer —
verified at `module_contracts.yaml:383`, `{type: "*", from: engine}  # Memory Query API`. **B's
fieldwork skeleton never mentions this.** It notes only that "module_contracts.yaml declares a
contract only for fieldwork_knots" and never audits that contract's `consumes`/`emits`. Meanwhile
B's threadwork skeleton *does* audit its contract's emits (finding both unbuilt), and B's
characters skeleton audits `piety_track`'s. **Coverage gap in B, not an error.** For the record,
the audit B did not run: `knots.py` contains **zero** Key/emit/subscribe code, and
`meta.knot_formed` / `meta.knot_ruptured` / `scene.gift` appear in no production `.py` except
`articulation.py`'s subscription roster — so `fieldwork_knots`' declared `*` consumer and all
four declared emits are 0/5 built.

**Verdict: no incompatibility. B is silent where A speaks.**

---

### CC4 — A's 8 modules with neither doc nor code: do the sets reconcile with B?

**Yes, with no contradiction. B confirms 2 explicitly, covers 4 implicitly, and is silent on 2.**

A's counts verified directly from `references/key_graph.json`: 27 modules, 14 with no subsystem,
**9 with no design doc**, **8 with neither doc nor code**. Exact.

| # | A's module | B's account | Verdict |
|---|---|---|---|
| 1 | `audit` | **No B skeleton names it.** Not traced by any of the 15. | No account — B gap, not a conflict |
| 2 | `domain_actions` | Not named as a module by B. B's factions skeleton does record that the one built `da.*` Key (`da.public_governance`) is emitted by `systems/factions/sim/parliamentary_transfer.py:176`, i.e. from the FA lane, not from any domain_actions module. Consistent with A's own `sim_module: none` note, which records a verified zero-emitter grep for all five `da.*` literals. | Implicit agreement |
| 3 | `engine_clock` | **Explicit.** `_architecture_flow_skeleton_v1.md:197`: "`engine_clock` — the `module_contracts.yaml`-declared contract this scheduler notionally realizes — carries `sim_module: none`; no code was ever joined to `TickScheduler`/`KeyLog` under that contract name." | Direct agreement |
| 4 | `game_director` | Claimed in `overview_flow_skeleton_v1.md`'s Contracts header, then never traced to code — the overview trace finds `season_manager`/`scene_slate`/`scene_dispatch` doing the work, none contracted as game_director. | Implicit agreement |
| 5 | `npc_memory` | **Explicit.** `npcs_flow_skeleton_v1.md:126`: "The companion `npc_memory` contract … is `doc: null`, `sim_module: none`, with its own gap note stating a grep for `npc_memory*` returns nothing tree-wide — no npc-memory store exists in code at all." | Direct agreement |
| 6 | `scenario_authoring` | **No B skeleton names it.** | No account — B gap |
| 7 | `scene_timer` | **No B skeleton names it.** | No account — B gap |
| 8 | `settlement_economy` | Claimed in `settlements_flow_skeleton_v1.md`'s Contracts header; the trace finds no economy code anywhere (`Settlement.legitimacy`/`.popular_support` inert, `province_effective_prosperity` no production caller). | Implicit agreement |

**Sets reconcile.** Nothing B traced contradicts A's 8. The three unaddressed by B (`audit`,
`scenario_authoring`, `scene_timer`) are all `scene`/`peninsula`-scale declared-only modules with
no folder of their own — B's 15 skeletons are folder-scoped, so a module with no folder and no
code has nowhere to be traced. That is a structural consequence of B's method, not an omission.

One adjacent A row is worth flagging: `scene_slate` is in the 9-with-no-doc set but **not** the
8-with-neither, because `engine/autoload/scene_slate.py` exists. B's overview skeleton
(`:128`) traces it as a module-level `_queue` global. A's contract declares scene_slate emits
**8 key types**. `grep -n "emit\|Key(" engine/autoload/scene_slate.py` → **zero hits**. All 8
declared emits are unbuilt. A cannot see this; the wiring manifest's own note ("built = a
59-line queue, the canonical §4 generator is unbuilt") does, but A does not render notes (D4).

---

### CC5 — A6: 20 cross-scale violations across 9 module pairs. Does B record the seam, and agree on direction?

**B records no counterpart seam for any of the 9 — because none of them is built. No direction disagreement is possible, and the convergence is meaningful.**

A6's 9 pairs (`CONTRACT_INDEX.md:23`), consumer←producer:

| pair | producer scale → consumer scale | keys |
|---|---|---|
| `npc_behavior←domain_actions` | provincial → personal, scene | `da.antinomian_action`, `da.covert_betrayal`, `da.public_governance`, `scene.draft_da` |
| `npc_behavior←faction_politics` | provincial → personal, scene | `scene.investigation_resolved`, `state.coup_attempted`, `state.standing_change`, `state.succession` |
| `npc_behavior←peninsular_strain` | peninsula → personal, scene | `env.peninsular_strain_shock` |
| `piety_track←domain_actions` | provincial → personal | `da.antinomian_action`, `da.covert_betrayal` |
| `piety_track←scene_slate` | scene → personal | `scene.dialogue`, `scene.insult`, `scene.threat`, `scene.witness` |
| `settlement_economy←domain_actions` | provincial → settlement | `da.economic_intervention` |
| `settlement_economy←peninsular_strain` | peninsula → settlement | `env.population_change` |
| `settlement_layer←peninsular_strain` | peninsula → settlement, territory | `env.disaster`, `env.peninsular_strain_shock` |
| `settlement_layer←scenario_authoring` | peninsula → settlement, territory | `env.disaster` |

**Consumer side — B's verdicts, all four:**

- `npc_behavior` — `npcs_flow_skeleton_v1.md:126`: contract declares the full consumes/emits/state/gates flow "against `sim_module: none` … **None of this declared flow exists in code**"; `systems/npcs/` holds zero `.py`.
- `piety_track` — `characters_flow_skeleton_v1.md:145` (gap 3): "`module_contracts.yaml`'s `piety_track` entry declares `conviction.py` as a Key-substrate consumer of 9 Key types and an emitter of `state.scar_acquired`, but `conviction.py`/`beliefs.py`/`companion.py` contain **no Key subscription, `echo_scheduler`/`TickScheduler` reference, or `.emit(` call anywhere**."
- `settlement_economy` — `settlements_flow_skeleton_v1.md`: no economy code found at all.
- `settlement_layer` — `settlements_flow_skeleton_v1.md`: the only settlement write path is the dormant `_apply_accord_echo` leg; no `env.*` consumption anywhere.

**Producer side — B's verdicts:** `domain_actions` has zero emitter code (A's own contract note records the same verified grep); `peninsular_strain`'s four `env.*` emits have zero production emitters (verified above, CC1); `scenario_authoring` has no code; `scene_slate.py` has no emit call; `faction_politics` — `wiring_manifest` "contest/faction.py succession/coalition math exists but unwired", and B's social_contest skeleton §7 independently confirms `contest/faction.py` has zero production callers.

**So all 20 declared cross-scale edges are producer-dead AND consumer-dead.** B cannot record a
direction because there is no runtime edge to direct.

**The convergence is the finding, and it is non-trivial.** The *one* cross-scale seam that IS
built — scene → faction Domain Echo via `echo_transport.emit_scene_echo`
(`_architecture_flow_skeleton_v1.md` S3.2.2.4.3, `combat_flow_skeleton_v1.md` S5.2c,
`social_contest_flow_skeleton_v1.md` S7.4) — is **not** in A6's list, because it *has* a
`scale_transitions` entry: A renders it under `social_contest` and `personal_combat` as
"scale_transitions §3.4 Scene → Faction (Domain Echo)". A6's rule is "crosses scales with no
transitions entry on either side", and it fires on exactly the 20 unbuilt edges and none of the
built one. **A6 is, empirically, a build-status detector that does not know it is one.** Two
independent instruments, one derived from `scale_transitions_v30` declarations and one from
tracing `.py` files, partition the same 21 candidate seams identically.

**Verdict: no B counterpart seam exists; no direction to disagree about; strong indirect corroboration in both directions.**

---

## 2. Direct contradictions — full sweep

I walked every A module-detail block against the B skeleton that claims it. Classifying each
apparent conflict as (i) same fact / different answers, or (ii) different layers.

### 2.1 Type (i) — genuine defects

**D1. Two key types fall through every A review queue.** Adjudicated in CC2. `scene.accord_echo`
and `meta.cascade_cluster_event` have zero declaration in `module_contracts.yaml` on either side;
`build_contract_index.py:239` filters the under-declaration queue to `registry_superset`, which
excludes `registry_only`. Both keys are code-relevant: one is a live Key construction site
(`echo_transport.py:309-311`), the other is a subscribed-with-no-emitter id. **Fix:** widen the
`under` filter to include `registry_only`, or add a fifth review queue "declared only in the
registry, contract silent" — 2 keys today, and the count is a real backlog signal.

**D2. A's stated `authority` derivation rule is false for `mass_battle`.** A's own preamble:

> `authority` is derived, not stored (Jordan's 2026-08-02 precedence rule): **code** if a declared
> `sim_module` resolves on disk, **prose** if only a design doc exists, **none** if neither.

A's "Modules with no home" table then renders `mass_battle` with **sim module `—`** and
**authority `code`** — visibly violating the rule in the same document. Adjudicated at
`tools/build_key_graph.py:99-101`:

```python
CODE_EXISTS_UNDECLARED = {
    'mass_battle': 'systems/mass_battle/sim/  (row MB-owned, OI-54/ED-IN-0097 W4 — IN must not fill it)',
}
```

and `:194-196`: `if undeclared: has_code = True`. The override is defensible on the merits —
`systems/mass_battle/sim/massbattle.py` really is live code, and B confirms it is the campaign's
only battle seam (`mass_battle_flow_skeleton_v1.md` S1-S4, TREE A) — but the *published rule does
not describe the implementation*, and the artifact is the one place a reader would go to learn how
`authority` is computed. **B corroborates the substance** (`mass_battle_flow_skeleton_v1.md:184`:
"`references/module_contracts.yaml`'s `mass_battle` module entry deliberately omits `sim_module`
at all (marked 'undeclared', awaiting an MB-lane add)"). **Fix:** amend A's preamble to state the
hand-maintained override and name it, or render the `code_undeclared_note` (already carried in
`key_graph.json`) in the table.

**D3. `wiring_manifest.yaml:68` asserts a false code fact.** peninsular_strain's note reads:

> "owns Turmoil + IP; env.* emitters + occupation ladder unbuilt **(IP isn't even a clocks key)**."

Adjudicated at `engine/autoload/game_state.py:244`:

```python
clocks={'CI': 30.0, 'MS': 60.0, 'IP': 20.0, 'PI': 0.0, 'Strain': 0.0, 'Turmoil': 0.0},
```

`IP` **is** a clocks key, seeded to 20.0. B has it right and states it twice —
`overview_flow_skeleton_v1.md:114` (`world.clocks['IP']` | W (seed only) | declared writer
`ip_track.apply_ip_delta` is uncalled) and `:171`. The rest of the note is correct and B agrees
with all of it (Turmoil zero-writers, env.* emitters unbuilt). **B is right; A's source is
wrong.** Note the parenthetical was probably true when written — `game_state.py:240-243` carries a
comment describing the IP seed as later "dormant groundwork", so the clock was added after the
manifest note. Classic stale-by-someone-else's-commit. Mitigating: A does not render notes (D4),
so the false claim is not on A's published surface — but `wiring_manifest.yaml` is A's declared
build-status source and is read by tooling.

**D5. A's under-declaration framing.** Adjudicated in CC2. "The contract side is simply
unauthored" is false for the 41 `articulation_layer` rows, which are 97% of the section's volume.

### 2.2 Type (i) — inside B

**B1. Asymmetric contract auditing.** B's threadwork skeleton audits its contract's declared emits
and Thread Fatigue state and finds all unbuilt (`threadwork_flow_skeleton_v1.md:150`, `:158`).
B's characters skeleton audits `piety_track`'s 9 declared consumes and 1 emit (`:145`). B's
victory skeleton audits the 4 declared era gates and the 8-quantity declared state read (`:122`,
`:126`). B's **fieldwork** skeleton audits neither `fieldwork_knots`' `*` consumer nor its 4
declared emits, though A names both. Not a wrong claim — an unrun check. Filling it (done above,
CC3) yields 0/5 built.

### 2.3 Type (ii) — different layers, NOT defects

These are the ones that look like conflicts and are not. Listing them explicitly, because the
instruction is to say so rather than manufacture conflict.

| Apparent conflict | A (declared) | B (built) | Ruling |
|---|---|---|---|
| `settlement_layer` sim_module | `systems/settlements/sim/settlement.py`, authority **code**, build `design` | `settlement.py` has **zero importers** anywhere; the live code is `registry.py`, which has no contract entry (`settlements_flow_skeleton_v1.md:150`) | **No contradiction, different layers.** A's rule is "resolves on disk", and it does. A cannot distinguish exists-from-runs. B's finding — *the contract points at the dead file while the live file goes uncontracted* — is exactly the defect A's method cannot express. |
| `personal_combat` emits 3 Keys / consumes 2 | declared in contract | `combat_engine_v1/` contains no `engine.substrate`, `Key(` or `KeyLog` reference at all; the one `scene.combat_resolved` construction site is in `echo_transport.py`, outside the subsystem (`combat_flow_skeleton_v1.md:144`) | **No contradiction.** A's `build: unwired` already encodes it. B names *where* the Key actually gets built, which A cannot. |
| `threadwork` emits `scene.thread_operation`, `meta.thread_woven`; owns `Thread Fatigue` | declared | zero `.py` constructs either literal; zero `.py` under `systems/threadwork/` mentions "fatigue" (`threadwork_flow_skeleton_v1.md:150`, `:158`) | **No contradiction.** A's `build: unwired` + its own manifest note "13 operations built, zero live callers" agree with B verbatim. |
| `victory` declares 4 era gates + an 8-quantity state read | declared | `victory.py` implements GD-1 only; reads `Turmoil`, territory `accord`/`owner`, faction fields — the other 5 declared reads never happen; grep for "Post-Calamity"/"Second Calamity"/"Anarchy Era"/"Phased Occupation" returns zero `.py` (`victory_flow_skeleton_v1.md:122`, `:126`) | **No contradiction.** A's own gap note on `victory` says the era transitions "are UNKEYED — no mechanical_event/state_transition type exists". A knows; A just cannot say "and here is the inline fallback in `mc_v18.py:276-286` that actually decides most winners", which B does. |
| `faction_state` build `deferred` | manifest | B's factions skeleton: `faction_action` executes in every traced phase; `execution_trace.json` records it in boot, loop.s1, s2.factions, s2.parliament, s3 (`factions_flow_skeleton_v1.md:185`) | **No contradiction, and A already reconciles it** — `wiring_manifest.yaml:57` note: "the in-13 hub; **action code (conquest/muster/govern/uniques) is live**, resolver [ASSUMPTION]". B's own gap 1 says the *resolver* half has no implementation. Same finding. **A just doesn't render the note (D4).** |
| `peninsular_strain` build `deferred`, authority `code` | manifest | ci_track/ms_track run every season (`overview_flow_skeleton_v1.md` S2.2.3.1-.2) | **No contradiction.** Manifest note scopes "deferred" to the env.* emitters + occupation ladder, both of which B confirms unbuilt. Rendering loss again (D4). |
| `scene_slate` emits 8 Keys, authority `code` | declared | `scene_slate.py` has no emit/Key code; it is a queue | **No contradiction.** A's `build: deferred` + manifest note "built = a 59-line queue, the canonical §4 generator is unbuilt" is precisely B's finding. |
| `mass_battle` build `live`, one emit `scene.battle_concluded` | manifest | TREE A (`systems/mass_battle/sim/`) is the live seam and does emit it via `faction_action.py:459`; TREE B (`tests/sim/mass_battle/`) is canon-but-unreachable | **No contradiction.** The manifest note is a full, accurate J2 account that matches B's two-tree finding in detail. Best-agreeing pair in the corpus. |
| `social_contest` build `gated` | manifest | the contest branch is the *only* organically-queued scene type; reached via `emergency_council` only | **No contradiction.** Manifest note: "Agôn kernel built (1 of 4 games); reached live only via emergency_council." Exact match to B's S7.2 + §7 game=-dead-weight row. |
| `piety_track` build `deferred` | manifest | `conviction.apply_conviction_scar`'s only caller is `knots.apply_knot_loss`, which has zero production callers (`characters_flow_skeleton_v1.md:143`) | **No contradiction.** Manifest note: "conviction.py Scar accumulation real; reached only via the unwired knot chain." Exact match. |
| `articulation_layer` build `stub`, IN=1, OUT=0, state=0 | declared | articulation owns no state, emits nothing, returns typed no-ops (`articulation_flow_skeleton_v1.md` §4, §5) | **No contradiction.** Full agreement including the zero-state claim. |
| `ui` absent from A's 27 modules | A never mentions ui | B's ui skeleton independently greps `- module:` and confirms 27 rows, no ui row (`ui_flow_skeleton_v1.md:41`) | **Agreement.** B verified A's roster size from the other side. |

**Zero type-(i) contradictions between A and B about the world.** Every conflict I could
construct dissolved on inspection into declared-vs-built, and in eight of them A's own
`wiring_manifest` note already carried B's answer.

---

## 3. Coverage asymmetry

### 3.1 What A knows that no B skeleton records

1. **The whole `audit`, `scene_timer`, `scenario_authoring` module trio.** Declared, folder-less,
   codeless. B's method is folder-scoped; these have no folder, so no skeleton owns them.
2. **The A1-A12 adjudicator verdicts as a class.** A imports 22 contract violations (20 × A6, 2 ×
   A8) and 67 warnings from `contract_adjudicator.py`. B has no equivalent — no skeleton runs a
   declaration-consistency check.
3. **A8 canonical-sources violations.** `systems/characters/conviction_track_v1.md` and
   `systems/social_contest/social_contest_v30.md` not declared in `canonical_sources.yaml`. No B
   skeleton checks `canonical_sources.yaml` membership.
4. **Scale signatures per module and per key.** `scales: [personal, scene, provincial]` etc.,
   and per-key `scale`/`permanence`/`time_horizon`. B traces call graphs, not scale algebra; no
   skeleton records a key's permanence or horizon.
5. **Formal derivation formulas.** `settlement_layer`'s 7 derivations (Mandate `7T/(T+6)`,
   `Local Economy = Prosperity × 50`, Treasury income, LPS drift) and `personal_combat`'s Health
   formula, each with a doc-section provenance. B records that they are unimplemented but not what
   they are.
6. **Gates as declared objects.** 4 victory era gates, 3 settlement_layer gates, 3 knot gates,
   2 territorial_piety, 4 peninsular_strain, 2 npc_behavior — with `when`/`then`/`on`/`source`.
   B records only the gates that exist in code.
7. **Feedback loops + dampers.** `social_contest ↔ npc_behavior` 2-cycle with its bound analysis
   (Procedure-D cadence, |Δaffect|≥0.5 threshold, Chain-Contest cap 3). Nothing in B.
8. **Scale-transition citations.** `scale_transitions §3.1/§3.5/§3.6` for threadwork,
   `§3.6-§3.8` for mass_battle, etc. B has "seams" but no transition-spec references.
9. **Registry-vocabulary mismatches.** `faction_layer` (registry) vs `faction_state` (module) — a
   naming unification held open for Jordan. Invisible to a code trace.
10. **The `key_graph` reconciliation status vocabulary** — `agreed`/`registry_superset`/
    `contracts_superset`/`registry_only`/`absent_both`, and the fact that there are **zero**
    conflicts (both authored views agree everywhere they both speak). That is a real and
    reassuring finding no code trace can produce.
11. **`meta.legacy_event`** — a payload schema with no producer anywhere. Verified: present in
    `key_graph.json`, `key_types.json`, `execution_map.json` and the `_architecture` identifier
    census; no `.py`. No B skeleton mentions it.
12. **Provenance stamps.** Every module carries `sources:` with `[READ: date]`. B carries
    `path:line symbol` anchors but no doc-read provenance.

### 3.2 What B knows that A structurally cannot

A is generated from declarations. These are the classes it cannot reach *even in principle*:

1. **Reachability.** A's `build:` column is a hand-maintained 6-value enum; B computes actual call
   chains. B's central finding — that `evaluate_triggers` only ever queues `scene_type: "contest"`,
   so the entire combat branch of `_resolve_slot` is dead **regardless of `DISPATCH_COMBAT_BRIDGE`**
   (`combat_flow_skeleton_v1.md:145`, `_architecture_flow_skeleton_v1.md:189`) — is a two-hop
   inference across three files. No declaration expresses it.
2. **Orphans and stubs.** B counts them precisely: 6 of 16 factions modules are pure
   `stubwire.stub_resolve` armature; `temperaments.py` all four entry points zero importers;
   `compute_settlement_state`/`aggregate_to_province` zero importers; `parliamentary_stay`'s whole
   public API zero callers; `contest/faction.py` zero production callers; `agon_harness.py` zero
   callers; `render_protagonist_lens`/`generate_chronicle_entry`/`compute_thread_echo`/
   `check_arc_boundary`/`TickScheduler.schedule_emission`/`drain_tick` zero callers.
3. **Dead constants and dead params.** `DEFAULT_PARAMS['VICTORY_THRESHOLD'] = 11` vs the live
   `victory.py` constant 15 — different owners, no wiring, pinned by a test that sweeps 11→999→1
   and moves nothing. `max_seasons`'s fallback structurally unreachable. `PIETY_HIGH`/`PIETY_LOW`,
   `BREADTH_OB`/`DISTANCE_OB`, `COHERENCE_BAND_STRAIN_PACING`, `RUPTURE_WOUND_DISSOLUTION`,
   `Standing.strip`, `FACTION_TACTIC_CARD_POOL_MODIFIERS`.
4. **Execution order and cadence.** `phase_boundary`'s fixed 7-call order; `accounting.py`'s
   6 steps (against an `__init__.py` docstring claiming 13); the `accounting_boundary()` deferred-
   apply landing point; the fact that `run_season`'s `SeasonResult` is discarded at its sole
   production call site.
5. **Unreachable guards.** Both `TickScheduler` termination guards other than the per-tick
   emissions cap are structurally unreachable: the cascade-depth cap because `emit()` always passes
   depth 0 against a default cap of 0; the B1 re-entry gate because `_in_drain` is set only inside
   `drain_tick`, which has zero production callers. A declares the invariants; only a trace shows
   two of three can never fire.
6. **Latent defects.** `faction_take_action`'s compound `if`-not-`elif` cascade that can surface a
   bare `_NOOP` sentinel as an outcome — currently unreachable only because of a *caller-side*
   gate. `npe.py`'s `hidden_allegiance` branch computing a value never passed to the constructor.
   `knots.py` calling `apply_conviction_scar(conviction='Loyalty')` against a 9-member
   `CONVICTIONS` tuple that has no `Loyalty` — with B correctly refusing to rule which side is
   wrong, because the module comment declares a canonical 13-set the tuple never migrated to.
7. **Code↔docstring divergence.** `treaty.propose_treaty`'s docstring promises "raises"; the body
   is a silent `stub_resolve` no-op — the observed behaviour is the *opposite* of the contract.
   `faction_action.py`'s "GD-2 mandatory threat-response" claim, propagated to four more places,
   contradicted by the single `rng.random()` gate. `attempt_leap`'s docstring claiming a
   `target_state['ts_minimum']` read the body never performs. Stale line-number citations
   (`accounting.py` citing `parliamentary_transfer.py:210`, actual write at `:278`).
8. **Two-store and shadow-field defects.** `Settlement.religious_building` vs
   `InfrastructureState.religious_building` — same name, different stores, one logically dead.
   `World.territories` (from module constants) vs `World.settlements` (from geography YAML),
   populated in the same `create_world()` from two uncoordinated sources.
9. **Import-graph topology.** The `contest/` package's 9-node SCC and the two different-but-both-
   correct answers depending on whether `__init__` edges count. TREE A's test-pinned
   `massbattle ↔ units` cycle. `faction_action` as a cut-vertex.
10. **Disjoint-implementation findings.** The two mass-battle trees, with the measured
    `AttributeError: 'Subunit' object has no attribute 'cells_float'` proving they cannot be
    joined. The four coexisting social-contest resolvers. Church Tribunal implemented twice, once
    in the contest kernel and once in FA, sharing canon prose and no code.
11. **Default-flag semantics.** `ECHO_TRANSPORT` defaults `'1'`; `DISPATCH_COMBAT_BRIDGE`
    defaults `'0'`; `CASCADING_ENABLED` is a hardcoded `True` in both trees so its `else` branch
    is unreachable without a source edit. A has no notion of a runtime flag.
12. **Negative existence proofs with method stated.** "`find systems/ui -type f` returns 10 files,
    all `.md`/`.yaml`"; "repo has zero `.tscn` files"; "grep hits inside those `.gd` files are
    substring false-positives inside 'requires'". B's ui skeleton also does something A cannot:
    it names the one code artifact that self-identifies as UI (`articulation.py`'s "Tier 1 UI
    Lens") and explicitly declines to claim it, stating the scope boundary rather than leaving it
    to inference.

### 3.3 Where the two overlap and reinforce

Eight independent agreements between `wiring_manifest.yaml` notes (A's source) and B's §7 gaps,
listed in §2.3. Neither artifact cites the other. `wiring_manifest.yaml`'s `as_of` is 2026-07-29;
B traced at `6545067` (2026-08). The notes have held for ~10 days of commits with one
exception (D3, the IP clocks claim). That is a usable reliability estimate for the manifest.

---

## 4. Recommended actions

| Action | Target | Class |
|---|---|---|
| Widen `build_contract_index.py:239`'s `under` filter to include `registry_only`, or add a 5th queue | `tools/build_contract_index.py` | D1, blocking-ish — 2 keys currently invisible, one of them the engine's live Accord Echo type |
| Amend A's `authority` preamble to disclose `CODE_EXISTS_UNDECLARED`, or render `code_undeclared_note` in the table | `tools/build_contract_index.py` | D2 |
| Delete "(IP isn't even a clocks key)" from `wiring_manifest.yaml:68` | `references/wiring_manifest.yaml` | D3 — false against `game_state.py:244` |
| Render `wiring_manifest`'s `note:` in the module-detail block | `tools/build_contract_index.py` | D4 — recovers 8 reconciliations A currently drops |
| Rewrite `KEY_INDEX.md` §3's "the contract side is simply unauthored" to distinguish unauthored from wildcard-authored | `tools/build_contract_index.py` | D5 |
| Add the `fieldwork_knots` contract audit (`*` consumer + 4 emits, 0/5 built) to B's fieldwork skeleton | `systems/fieldwork/fieldwork_flow_skeleton_v1.md` | B1 |
| Consider: A6 fires on exactly the unbuilt cross-scale edges and not the built one. Worth recording as an observed property before someone "fixes" the 20 by adding transition entries | ED note | observation |

---

*Adjudicated against the working tree at `b24a1e9`. Every code claim above was re-derived by
hand: `module_contracts.yaml`, `key_graph.json`, `wiring_manifest.yaml`, `build_key_graph.py`,
`build_contract_index.py`, `articulation.py`, `echo_transport.py`, `game_state.py`,
`scene_slate.py`, and targeted greps for each key-type literal.*

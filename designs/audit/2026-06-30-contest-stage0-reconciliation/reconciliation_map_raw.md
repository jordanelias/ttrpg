The two upheld findings verify. Now I confirm the m1 numeric claim precisely and check the Â§2.1a text I need to revise.

At pool=10, ns=0.7: `soft_cap(0.7) = 1.5*tanh(0.7/1.5)` and combat path multiplies by `sigma_n(10) = 0.8*sqrt(10)`. Meanwhile `net_boost(0.7, 10, TN)` = `soft_cap(0.7) * sigma_per_die[TN] * sqrt(10)`. These agree only when `sigma_n(pool) = 0.8*sqrt(pool)` equals `sigma_per_die[TN]*sqrt(pool)`, i.e. when `sigma_per_die[TN] = 0.8` â€” which holds at **TN7 only** (0.800), not TN6 (0.806) or TN8 (0.781). Confirmed: the combat path (`soft_cap*sigma_n`, coefficient 0.8) and `net_boost` (TN-dependent `sigma_per_die`) diverge off TN7. Now returning the full revised artifact with both fixes applied.

---

# STAGE 0 â€” FOUNDATION DESIGN ARTIFACT: Social-Contest Rebuild

**Role:** AGONIST (builder). **Scope:** the four locked decisions (groundup+CR1-CR7 reconciliation; all four deliberative games; sim+canon only; drive to SETTLED). This artifact is the design contract every later stage cites. All evidence is cited to file/section. No files were edited.

---

## 0. SOURCE-OF-TRUTH SUMMARY (what each input actually is)

| Input | What it is | Load-bearing facts |
|---|---|---|
| `designs/audit/2026-06-03-contest-groundup/` | The **kernel** to PROMOTE. `tests.py` is **seeded (L14 `random.seed(20260603)`) and gates (L454 `sys.exit(1 if Fc else 0)`)** â€” both RATIFICATION-finding-#6 defects are ALREADY fixed in the suite. **The suite was RUN this pass and prints `RESULT: 151 passed, 0 failed`** â€” so both the README "36/36" and the RATIFICATION "62/62" self-reports are **STALE** (the suite grew ~147 `ck()` calls past both); the true measured green count is **151** (see SELF_ATTACK Â§5.1). Wrapper-over-modules acyclic DAG: `contract`â†’`engine`â†’`primitives`â†’`resolver`â†’`modes`. **The kernel is NOT five .py modules â€” it is NINE** (`glob`-verified): `contract, engine, primitives, resolver, modes, policy, faction, narrative, tests`. `tests.py` imports `policy` (L10), `faction` (L168), `narrative` (L251) â€” so the promotion set is the full nine, not the prompt's five. | Venue-determined; no privileged "merits"; per-side `adv` clock; 6 win-conditions; `DefeatCatalogue`; Ïƒ-leverage as mu-shift (`net_boost`). **`faction.py` already implements Â§10 BG-Vote (`coalition_vote` L118-140), Â§7.2 Succession (`succession` L85-109), and the committee band (`band_of`/`rate_banded` L68-82)** â€” Consensus is largely PROMOTE-EXISTING, not author-new (Â§1.6/Â§4). Status: "MECHANICAL proposal, diverges from canon" (RATIFICATION.md L4-6). |
| `designs/scene/social_contest_v30.md` | The **canonical surface** to PRESERVE. CANONICAL, approved 2026-04-17. | GM-framed (Â§2 "GM SETUP" â€” **but there is no GM**, hazard). Genres {Memory/Projection}; orientations {Revealing/Obscuring}; Persuasion Track 0-10 banded; 6 faction boosts; Thread co-movement; Obligations/Wager; Conviction Scars; Chain Contests; Domain Echo; 8 proceedings; Composure=ChaÃ—3; Concentration=3F+2S; **Appraise read-the-room loop**; **Doubt Marker authored at TWO sites: the headerless L179-183 OBSCURING-WIN rule (fires in CLASH OR REINFORCE per the infill binding) AND the CROSS L170 rule**. |
| `params/contest.md` | Canonical param tables (prose). | Argue pool by adjudicator; faction-boost table; **Appraise 4-band ladder (Â§Exchange Step 1, PP-614/ED-893)**; interaction types CLASH/REINFORCE/CROSS/TIE; **Obscuring-win Doubt Marker L95**; derived-value formulas incl. **Concentration = (3Ã—Focus)+(2Ã—Spirit) L110 (ED-902, supersedes the struck FocusÃ—3)**; **Persuasion Track "Starting position: GM-set" L101**. |
| `RATIFIED_2026-06-01.md` | **CR1-CR7** to FOLD IN. Provisional canon-of-record. | CR1 wrapper-arch; CR2 Ïƒ-substrate migration; CR3 three trackers (Concentration+Face+Persuasion, Composure retired) â€” **but CR3's literal text L13 reads "Concentration (shared w/ combat; Focus*3)", a formula ED-902 later STRUCK**; CR4 stasisÃ—genre (definitional via the **authored Present-rendering**); CR5 orientation re-grounding; CR6 leverage-as-Î´Ïƒ; CR7 Concentration shared w/ combat. |
| `tests/sim/mass_battle/engine.py` | **Wrapper pattern to MIRROR.** | `MECHANICS` registry + `mechanics_selftest` (L79-82: `missing = [name for name,spec if _resolve(spec["fn"]) is None]` â€” a **hard all-must-resolve** check; every registered fn is WIRED so it passes); `build_unit` adapter + `resolve_battle` router; "wrapper RESOLVES NOTHING" (L85). |
| `designs/scene/combat_engine_v1/{config,core,wrapper}.py` | Combat oracle pattern + the Ïƒ-sibling location. | `config.CFG` = one dict of seeds; `core.resolve()` = mu-shift resolution. **`core.py` L31 uses `m1.soft_cap(net_sigma) * m1.sigma_n(pool)` â€” the TN-independent `sigma_n` path (coeff 0.8), NOT `m1.net_boost`.** `core.py` L3 `sys.path.insert`s `tests/sim/v32-combat-balance` and imports `m1_dice_sigma_core` from there; `wrapper` owns A/B identity, "RESOLVES NOTHING." |
| `sim/autoload/dice_engine.py` | **The ONE canonical dice kernel.** | `roll_pool(pool,tn,ob,rng)â†’RollResult`; `degree_from_net`; `continuous_engine_sample`. CANONICAL (params/core.md). **L8-9 declares "Dependencies: none â€” root primitive"; stdlib `math`/`random` only. Contains NO Ïƒ-leverage functions** (no `soft_cap`/`sigma_n`/`eff_sigma`) â€” see Â§2. |
| `tests/sim/v32-combat-balance/m1_dice_sigma_core.py` | The **only working Ïƒ-core** â€” but it lives in a TEST dir and **`import numpy as np` (L21)**. | Exports `sigma_n` (L65, `0.8Â·âˆšpool`), `soft_cap` (L70), `net_boost` (L92, `soft_capÂ·sigma_per_die[TN]Â·âˆšpool`), `p_success` (L116) via numpy `tanh`/`sqrt`/`random`. **`net_boost` is TN-DEPENDENT (reads `PER_DIE[tn]` = 0.806/0.800/0.781 for TN6/7/8); `soft_capÂ·sigma_n` is TN-INDEPENDENT (coeff 0.8). They coincide ONLY at TN7 (`sigma_per_die`=0.800=coeff) and diverge at TN6/TN8** â€” the boost-magnitude split of Â§2.1a. Reachable today only via combat `core.py`'s `sys.path.insert` hack. |
| `sim/personal/contest.py`, `tests/sim_framework/contest.py` | **Live stubs being superseded.** | sim/personal: success-count, no Ïƒ, no Face/stasis/orientation. sim_framework: feature-fire smoke only. Both to retire. |

---

## 1. RECONCILIATION MAP (the heart of the artifact)

Legend: **CLEAN** = direct rename, no semantics lost. **LOSSY** = mappable but drops/approximates content that must be re-authored. **CONTRADICTORY** = the two surfaces disagree and a decision is required.

### 1.1 Appeal axis â†” v30 adjudicator primary-attribute â€” **[the ethos/pathos/logos relocation is MULTIPLICATIVEâ†’ADDITIVE, NOT a clean carry-across; a resâ†’Î´Ïƒ conversion + parity check is now required]**

| Groundup | v30 surface | CR1-CR7 | Verdict | Carry-across requirement |
|---|---|---|---|---|
| `Appeal{ethos,pathos,logos}` (`primitives.Appeal` L27-29). **In the resolver it enters the gain MULTIPLICATIVELY:** `_advance` (resolver L200-215) computes `res = max(RES_FLOOR, (1-leak)Â·venue_w + leakÂ·adj.character()[appeal])` (L212) where `venue_w = joint_weight(appeal, tense)` carries the Aristotelian cross-term (L211/L128-134), then `gain = MERIT_SCALEÂ·magnitudeÂ·resÂ·rdyÂ·jitterÂ·bias` (L214). Appeal is a **gain-scaler**, on a code path **entirely separate** from the additive Ïƒ-mu-shift `net_boost` (which enters via `_reception` L193). | v30 has **no per-move appeal**. v30's analogous axis is the **primary attribute set by adjudicator type**: Expert judgeâ†’Cognition, Crowdâ†’Charisma, No-adjudicatorâ†’Attunement (`social_contest Â§3`; `params/contest Â§Pools` L19-21) â€” a *pool-construction* choice, not a per-move proof. | CR6 lists "audience boost, Recall, Face, corroboration" as Î´Ïƒ levers â€” i.e. the appeal-fit becomes one Ïƒ-lever among several. | **CONTRADICTORY (axis mismatch) â€” AND the relocation into Ïƒ-space is a SEMANTIC CHANGE, not a rename.** Groundup's ethos/pathos/logos is a **per-move** dimension; v30's Cognition/Charisma/Attunement is a **per-contest** dimension (fixed at setup, Â§2). They are not the same axis and cannot be aliased. **Additionally: moving ethos/pathos/logos into the Î´Ïƒ armature (Â§3.1) converts it from a MULTIPLICATIVE gain-scaler (`res`, resolver L212, on a path separate from `net_boost`) into an ADDITIVE-in-Ïƒ mu-shift lever. A gain-multiplier and a mu-shift are NOT interchangeable, and the `joint_weight` (appealÃ—tense) cross-term (resolver L128-134) has no additive analogue.** This is an authored-surface-fidelity loss, not a clean carry-across. | **DECISION REQUIRED (route to engine, not GM):** keep BOTH. (a) Adjudicator-type â†’ primary attribute sets the **pool** (canonical, `params/contest Â§Pools`). (b) ethos/pathos/logos becomes the **resonance-fit** Ïƒ-lever (`Resonance.leak` blends venue-role vs adjudicator-character). Map ethosâ†’Face-built credibility (CR3), pathosâ†’Room/crowd, logosâ†’evidence/Recall. The Â§2-Step-1 "GM determines adjudicator type" must become an **engine resolver** that reads the venue/scene context (the `Venue` preset already does this in `modes.py`). **âš  AND (the multiplicativeâ†’additive fix): the CR6/armature ED MUST record the resâ†’Î´Ïƒ CONVERSION explicitly â€” the current groundup `res` is a multiplicative gain-scaler (resolver L212-214), the armature target is an additive tanh-capped `net_dsigma` (Â§3.1). These are not equivalent transforms.** The ED must (i) define the conversion (what Î´Ïƒ reproduces a given `res` gain, and over what magnitude/pool regime â€” a multiplicative scaler and an additive mu-shift can only be matched pointwise, not identically), (ii) state explicitly how the `joint_weight` appealÃ—tense cross-term (resolver L128-134) is preserved (or record its loss as a deliberate simplification with a cited rationale), and (iii) ship a **seeded parity check** comparing the old multiplicative-`res` path against the new additive-Î´Ïƒ path across the pool/appeal/tense grid, so the re-housing does not silently shift outcome magnitude. Do NOT present the ethos/pathos/logosâ†’Î´Ïƒ move as a clean rename in the CR6 ledger entry. |

### 1.2 Stasis (6-ground LADDER) â†” v30 genre + CR4

| Groundup | v30 surface | CR4 | Verdict | Carry-across |
|---|---|---|---|---|
| `Stasis.LADDER` = **six grounds** (`primitives.py` L11-14): FACT, DEFINITION, QUALITY, JURISDICTION, CONSEQUENCE, FEASIBILITY. A separate `TENSE` map (L16-17) tags each ground past/present/future (FACT/DEFINITIONâ†’past; QUALITY/JURISDICTIONâ†’present; CONSEQUENCE/FEASIBILITYâ†’future), driving `RhetoricalWeights` and `Venue.tense_weight()`. | v30 **two genres {Memory, Projection}** (`social_contest Â§Genre restructure`; the *three*-genre Past/Present/Future was explicitly "Turfed", Â§12). Memory=Retention(past), Projection=Protention(future). | CR4 (L14): "stasis (conjectural/definitional/qualitative/translative) = terrain; genre (Memory/Projection, authored, preserved) = stance; **stasis sets primary genre**; **definitional = higher-order reframe via the authored Present-rendering**; translative = pre-merits jurisdiction (the Stay)." CR4 carries **four staseis**. | **CONTRADICTORY (axis-mismatch, not a clean 3â†’2 collapse).** The map must route on the **stasis ground, not tense** â€” CR4's intermediary is the stasis ground, not `TENSE`. Two distinct defects if unfixed: **(a)** a `DEFINITIONâ†’pastâ†’Memory` chain **directly contradicts CR4**, which routes definitional through the **authored Present-rendering** (a reframe operator), NOT into Memory. **(b)** Groundup carries **two grounds CR4 never names** â€” CONSEQUENCE and FEASIBILITY â€” that must not be silently dropped. | **Re-map on the stasis ground, NOT on tense (CR4 is the authority):** â‘  Build a **groundâ†’{genre, operator}** table keyed to CR4's four staseis, not the `TENSE` field. **Do not use `TENSE` as the genre intermediary** â€” it is a groundup-internal rhetorical-weight tag with no canonical standing; CR4 supersedes it. â‘¡ **conjectural/FACT** â†’ terrain that sets primary genre by the *question asked* (forensicâ†’Memory). **qualitative/QUALITY** â†’ genre by stance, not auto-"present". **translative/JURISDICTION** â†’ the pre-merits **Stay** (v30 Â§10.1 Parliamentary Stay + groundup `Stasis.shift`), genre-neutral until resolved. â‘¢ **definitional/DEFINITION** â†’ **NOT Memory.** It is a **higher-order reframe operator** ("the authored Present-rendering"), i.e. an in-bout move that re-renders which genre/ground is live â€” host it on the groundup `Stasis.shift` path, flagged as the CR4 reframe, never collapsed to a genre. â‘£ **CONSEQUENCE + FEASIBILITY** (groundup's two deliberative-future grounds) â†’ both map to **Projection** stance (future-oriented), with CONSEQUENCE = "what follows" and FEASIBILITY = "can it be done" as two terrains *under* Projection. **This reconciliation must be explicitly authored** â€” it closes CR4's 4-stasis surface against groundup's 6-ground LADDER, which CR4 does not do. Allocate it to the CR4-reconciliation ED (Â§4). The `RhetoricalWeights` present-row folds into venue proof-weighting, never a player-chosen genre (the turfed-three-genre decision stands). |

### 1.3 v30 orientation {Revealing, Obscuring} â†” CR5 â€” **[both Doubt-Marker sites correctly re-identified; the OBSCURING-WIN rule fires in CLASH *or* REINFORCE; the REINFORCE case + CROSS case both explicitly hosted]**

| Groundup | v30 surface | CR5 | Verdict | Carry-across |
|---|---|---|---|---|
| Groundup has **no orientation axis**. Closest: the `rebut` move (PROTOTYPE, `allow_rebuttal`, attrition on opponent `adv`, `REBUT_CAP=3.0` resolver L23) and the `hard` move (SelfGating-licensed). | v30 orientation {Revealing, Obscuring}, presented as styles {Precedent, Suppression, Vision, Insinuation} = genreÃ—orientation (`params/contest Â§Argument Styles` L37-42). **The Doubt Marker is authored at TWO independent spawn sites:** (i) the **headerless OBSCURING-WIN rule** (`v30 Â§4 L179-183`): the infill binds this block to "winning any exchange with Obscuring orientation in **CLASH or REINFORCE**" (`social_contest_v30_infill.md L30`) â€” i.e. it fires in the **REINFORCE** context as well as CLASH; mirrored `params/contest Â§Interaction Types L95`. (ii) the **CROSS interaction** rule (`v30 Â§4 L170`: "Obscuring in CROSS: if the side with larger movement used Obscuring, place a Doubt Marker on the opponent instead of track movement"). Both spawn the same âˆ’2-next-winning-margin token (`v30 Â§4 L181`). | CR5: "Directâ†’moves Persuasion (merits); Indirectâ†’**attacks opponent Face (ethos), with self-Face backfire on failure**. Supersedes Doubt Marker." F7 self-gating (Quintilian/Nyaya). | **CONTRADICTORY but CR5-resolved â€” with a residual gap.** v30 maps Revealingâ†’Direct, Obscuringâ†’Indirect. CR5 *replaces* the Doubt-Marker mechanic with a Face-attack-with-backfire. That cleanly hosts the **CLASH/Obscuring-win** case (adversarial, same-genre, direct Face contact). **But CR5 does NOT cover TWO other Obscuring-win contexts:** (a) **REINFORCE** â€” same-genre, *same-orientation* coalition push (v30 L160-163: "Both orators push in the same direction"), no opponent Face to attack, yet the L179-183 rule spawns a Doubt Marker here too; and (b) **CROSS** â€” *different-genre*, non-adversarial ("Neither argument attacked the other", v30 Â§4 L171), no-strain, no Face contact, yet L170 still spawns a Doubt Marker. All three contexts (CLASH, REINFORCE, CROSS) must be enumerated so none is orphaned. | Implement CR5 as: orientation=Direct â†’ `_advance` on own `adv` (Persuasion). orientation=Indirect â†’ a new `attack_face` resolver that strips opponent **Face** (CR3 tracker), gated by `SelfGating.licit` (your own Face caps your obstruction â€” F7), with self-Face loss on failed degree. **Retire ALL Doubt-Marker sites** (`v30 Â§4 L179-183` OBSCURING-WIN + `v30 Â§4 L170` CROSS; `params/contest Â§Interaction Types L95`) â€” enumerate **all three contexts** (CLASH, REINFORCE, CROSS) in the retirement ED so none is orphaned. **Map each context explicitly:** â‘  **CLASH/Obscuring-win** â†’ CR5 `attack_face` (direct adversarial Face contact â€” the clean host). â‘¡ **REINFORCE/Obscuring-win** â†’ because REINFORCE has no opponent Face to attack (same-orientation coalition push), it does NOT map to `attack_face` either; host it on the **same bounded-suppression channel as CROSS** (below), i.e. the Obscuring reinforcer applies the one-shot âˆ’2 clamp to the opponent's next winning margin, self-gated by its own Face. â‘¢ **CROSS/Obscuring-win** â†’ because CROSS has no Face contact, author a **CROSS/REINFORCE-shared suppression effect**: the Obscuring side, instead of moving the track, applies a **one-shot âˆ’2 clamp to the opponent's *next winning margin* (CROSS or track movement)** â€” preserving the mechanical outcome (deny the opponent's next winning margin) but re-homing it off the retired token onto a bounded, self-gated suppression channel consistent with CR5's F7 (its size capped by the Obscuring side's own Face). This CROSS/REINFORCE bounded-suppression replacement is a genuinely-missing authored piece â†’ allocate to the CR5/Doubt-Marker-retirement ED (Â§4). The four styles (Precedent/Suppression/Vision/Insinuation) survive as the genreÃ—orientation labels. |

### 1.4 Standing/Reserve/Room/Readiness â†” Composure/Concentration + CR3 three trackers â€” **[CR3's FocusÃ—3 recorded as superseded by ED-902]**

| Groundup | v30 surface | CR3 | Verdict | Carry-across |
|---|---|---|---|---|
| `Standing` (ethos-built authority, gates hard, feeds readiness+leak; `primitives.py`, BUILD/STRIP=0.8, frac normalized to START); `Reserve` (action-economy budget); `Room` (audience receptivity, pathos-built); `Readiness` (built support, floor 0.40). | `Composure = ChaÃ—3` (`params/contest Â§Derived Values L107`); `Concentration = (3Ã—Focus)+(2Ã—Spirit)` (`params/contest Â§Derived Values L110`, **explicitly "(Canonical per derived_stats_v30 Â§14.1, ED-902 2026-06-04; supersedes the struck Focus Ã— 3)"**). **No Standing/Room/Readiness primitives** in v30. | CR3 (L13): **three trackers** â€” Concentration (stamina, "shared w/ combat; **Focus*3**"), **Face** (NEW, contest-local ethos), Persuasion (merits clock). **Composure RETIRED** â†’ split into Concentration(stamina)+Face(standing). "Face â‰  Disposition/Reputation (transient vs persistent)." | **CONTRADICTORY on TWO axes â€” Composure retirement AND the Concentration formula.** (1) **Composure retirement**: CR3 is authoritative â€” `Standing`â†’**Face**; v30 `Composure` deleted. (2) **Concentration formula â€” a live CR3-vs-v30 contradiction.** CR3's own literal text (L13) defines Concentration as **`Focus*3`**, but that exact formula was **struck by ED-902 (2026-06-04)** â€” four days after CR3 was ratified (2026-06-01) â€” in favour of **`(3Ã—Focus)+(2Ã—Spirit)`** (`params L110`). "Fold in CR1-CR7 verbatim" would silently re-import the struck `FocusÃ—3`. The reconciliation MUST record **CR3's Concentration formula as SUPERSEDED by ED-902**; the canonical formula is `3F+2S`. `Room`â†’Persuasion-feeding pathos sub-channel; `Readiness`â†’internal multiplier (not a tracker); `Reserve`â†’action-economy (engine-internal, not one of CR3's three trackers). | **Carry:** (1) Map groundup `Standing` â†’ CR3 **Face** (rename `Standing`â†’`Face` in the contest package; resolves the TERMINOLOGY.md VERIFY flag). **NB (knock-on):** `faction.py` uses `Standing` throughout (`Standing.START` L44-46/L100-101, `import ... Standing` L8) â€” the rename must propagate into the promoted `faction.py` adapter too, or the Consensus module fails at import. (2) **Concentration = `(3Ã—Focus)+(2Ã—Spirit)` per v30/ED-902 â€” NOT CR3's `FocusÃ—3`, which is superseded.** This matches combat `config.CFG CONC_FOCUS=3/CONC_SPIRIT=2` (CR7 â€” Concentration shared), so v30 and combat agree and CR3's bare `FocusÃ—3` is the outlier. **The CR1-CR7 fold-in ED for CR3 must annotate: "Concentration formula `Focus*3` in CR3 L13 is superseded by ED-902; canonical = `3F+2S`."** Do NOT use groundup's `Reserve` for Concentration. (3) **Retire Composure** in `social_contest_v30.md Â§6/Â§8` and `params/contest Â§Derived Values L107`/Â§Composure L113-118 (cite CR3). (4) `Room`/`Readiness` survive as engine-internal pathos accounting, not player-facing trackers. **Hazard:** ComposureÃ—3 strain math (Cha-mod, Focus-defence, Â§CLASH) is deeply wired into v30; retiring it requires re-routing CLASH strain to Face â€” flagged in SELF_ATTACK Â§5.2. |

### 1.5 Per-side advancement clock â†” canonical Persuasion Track

| Groundup | v30 surface | Verdict | Carry-across |
|---|---|---|---|
| `ContestState.adv{A,B}` (resolver L32-36); `PersuasionTrack` (resolver L67-81) implements the 0-10 banded read: `track = clamp(start + scale*(adv[A]-adv[B]))`, **`start` hardcoded 5.0, `scale=1.5`** (L72), bands â‰¥9 A_total / â‰¥7 A_decisive / 4-6 committee / â‰¤3 B_decisive / â‰¤1 B_total. | Persuasion Track 0-10; A wins â‰¥7, B wins â‰¤3, compromise 4-6, Total Victory â‰¥9/â‰¤1 (`params/contest Â§Persuasion Track L99-102`). **Starting position: GM-set (typical 5)** (L101). v30 Tribunal/Excommunication start the track **biased** (Â§7 â€” accused starts at a disadvantaged position); the Hybrid CI clamp (PP-256) pins BG starts to 4-6. | **CLEAN on the band read; CONTRADICTORY on the start.** The band thresholds match canon exactly (groundup `PersuasionTrack` docstring L68 explicitly cites Â§10/Â§7.2). **BUT** groundup hardcodes `start=5.0`, while v30 says the start is **GM-set** and is **non-default in biased proceedings** (Tribunal/Excommunication) and CI-clamped in Hybrid (PP-256). With **no GM**, the start is an un-rerouted decision. **NB:** `faction.py` already parameterizes this correctly â€” `coalition_vote` computes `start=max(4.0,min(6.0,5.0+lobby))` (L134, ED-621 lobby clamp) and passes `PersuasionTrack(scale=..., start=start)` (L138), and `succession` passes an explicit `scale` (L96). So the "start-is-a-parameter, not a hardcode" reroute is **already demonstrated in the promotion set** â€” the venue resolver generalizes it, it does not invent it. | Adopt `PersuasionTrack` band-read directly. **But the `start` is NOT a constant â€” route it to an engine resolver.** â‘  Replace the hardcoded `start=5.0` with a **`Venue`-supplied start** computed by a `starting_position` resolver that reads the proceeding type: neutral proceedings â†’ 5.0; **biased proceedings (Tribunal/Heresy/Excommunication) â†’ a venue-canonical biased start** (the accused's disadvantage, v30 Â§7); Hybrid â†’ the PP-256 4-6 clamp â€” NOT a GM call. **The `faction.coalition_vote` ED-621 lobby clamp (L134) IS the BG-Vote instance of this resolver** â€” generalize it, don't re-author it. This is one of the un-rerouted "GM decides" points the hazard demands be engine-resolved (see Â§5.6). â‘¡ Carry the resistance model: v30 audience resistance = avg faction Stability âˆ’1 (min 0), with ED-864 per-exchange erosion `max(0, R0 âˆ’ âŒŠexchange/2âŒ‹)` (`params/contest Â§P1 Findings Resolved L266`). â‘¢ The `advâ†’track` `scale` param must be calibrated so margins map to the canonical 0-10 movement (SELF_ATTACK Â§5.3 â€” coefficient risk). |

### 1.6 Groundup win-conditions â†” the four games {Agon, Negotiation, Inquiry, Consensus} â€” **[Consensusâ†’committee band, not VoteAtClose; Consensus is PROMOTE-EXISTING via `faction.py`, not author-new/wire-stub]**

This is the **central deliverable of Decision (2)**. The groundup ships **six** win-conditions (`ThresholdRace`, `TallyAtClose`, `ProofBar`, `GraceThreshold`, `PersuasionTrack`, `VoteAtClose` â€” resolver L40-103). The four games are the canonical surface they must be re-skinned to. **All six must be assigned; totality is claimed in Â§1.8, so the map must be total.**

| Game | Canonical anchor | Win-condition (groundup) | Venue/adjudicator | Verdict | What must be authored |
|---|---|---|---|---|---|
| **Agon** (debate/disputation) | Formal/Grand Contest (`social_contest Â§2,Â§5`); the adversarial exchange | `ThresholdRace(T)` or fused `TallyAtClose` read via `PersuasionTrack` bands | `disputation`/`fused_arbiter` (`modes.py`), single adjudicator or crowd Panel | **CLEAN** â€” `ThresholdRace`+`TallyAtClose`+`PersuasionTrack` already cover it; the v30 Formal/Grand 3/5-exchange structure maps to `Venue.budget`. | Wire exchange-count (3 Formal / 5 Grand) â†’ `budget`; wire Recall +2D / Grand once-per-source (`social_contest Â§4`) as a Ïƒ-lever (CR6). |
| **Negotiation** | Private Negotiation, ZOPA (`social_contest Â§2 Step 5`; "Negotiation compromise (ZOPA-style)â€¦ **identified as structurally different from Persuasion Track, not designed**" Â§12). `NegotiationMode` is a **SCAFFOLD** raising NotImplementedError (`modes.py`). | **NONE EXISTS for bilateral overlap.** No groundup win-condition models ZOPA. (`GraceThreshold` is **not** this â€” it is one-sided supplication; see Personal Appeal row below.) | No-adjudicator (Attunement pool, `params/contest Â§Pools L21`); the parties decide. | **CONTRADICTORY / GENUINELY MISSING.** The single biggest authoring gap. v30 flags it un-designed; groundup explicitly scaffolds it. **No GM** to adjudicate, so overlap-resolution MUST be an engine resolver. | **AUTHOR NEW:** a `ZOPAOverlap` win-condition â€” bilateral reservation values, agreement when offers overlap, no-deal = the v30 "stall with consequences" (strain persists, Reads permanent, relationship stressed, `params/contest Â§Post-Contest "Private tie" L137`). Allocate an ED (Â§4). |
| **Inquiry** | Church Tribunal / Heresy Investigation / Excommunication (`social_contest Â§7,Â§7.1,Â§7.3`); inquisitorial asymmetry | `ProofBar(bar, challenger)` (resolver L53-59) â€” asymmetric, burden on challenger; defender prevails on doubt. Groundup inquisition venue exists (`modes.py` `CROSS_CULTURAL_VENUES["inquisition_hearing"]`/`["excommunication_court"]`, tests L386-389). | `court`/`inquisition` (`modes.py`), single inquisitor (hostile, low discipline). | **CLEAN-to-LOSSY.** `ProofBar` + the inquisition venue cover the *mechanics*. LOSSY because v30 Tribunal carries heavy authored state: **biased Persuasion-Track start (Â§1.5)**, CI gates, Evidence Track â‰¥3, the 8-condition closure lifecycle (Â§7.3.2). | Carry the asymmetric-resistance (halved for accused, `params/contest Â§Proceeding Types L147`), the **biased start via the Â§1.5 venue resolver** (not GM), and Evidence-Track-as-dossier. The groundup hidden-weight `Dossier` (`primitives.py`) hosts Evidence Track. The Â§7.3 lifecycle (4-6 season, closure conditions) is **faction-layer state**, not bout logic â€” keep it in the canon doc, out of the engine (mirrors RATIFICATION.md "Succession downstreamâ€¦ is faction-layer state, not engine logic"). |
| **Consensus** | BG Parliamentary Vote (`social_contest Â§10`); Succession Contest (Â§7.2); committee/compromise outcomes | **`PersuasionTrack` committee band (4-6)** â€” read via `TallyAtClose`/`ThresholdRace` into the 0-10 track. **NOT `VoteAtClose`.** | Panel (jury/bench/council) via `contract.Panel`. | **CLEAN + PROMOTE-EXISTING.** `faction.py` **already implements the entire Consensus surface on the Track**, verified on disk: `coalition_vote()` (L118-140) pools each side's Mandate, clashes them via `roll_net`, and reads `PersuasionTrack(start=ED-621-clamped, scale=damped).resolve(...)` into `pass/committee/fail` (Â§10 BG-Vote); `succession()` (L85-109) runs a `PersuasionTrack` Bout returning `unified/decisive/split` with the Â§7.2.1 split ratios `{4:0.60, 5:0.55, 6:0.50}` (L108); `band_of`/`rate_banded` (L68-82) supply the committee outcome the strict-threshold `vote()` lacks. Both BG-Vote and Succession are **pooled Track reads**, confirming the committee-band mapping AND that the module is **built, not a stub**. `VoteAtClose` is a *separate* PROTOTYPE, not this. | **PROMOTE `faction.py`** into the scaffold as the Consensus adapter (Â§3.1) â€” it is not author-new. Work reduces to: (a) re-home `faction.py` (its `import faction` in `tests.py` L168 already depends on it); (b) propagate the `Standing`â†’`Face` rename into it (Â§1.4); (c) generalize its ED-621 `start` clamp into the Â§1.5 `starting_position` resolver; (d) the Â§7.2.1 faction-split ratios and Â§10 downstream stay **faction-layer state / canon prose** (as `faction.py`'s own docstrings already scope â€” L94-95 "NOT modelled here", L118-124). Re-classify the Consensus MECHANICS rows from STUB to **PARTIAL/WIRED** (Â§3.2). **`VoteAtClose` is NOT assigned to Consensus** â€” it is a genuinely distinct mechanic (Athenian dikasteria; the room severed from the verdict) with **no canonical Â§10/Â§7.2 anchor**, so it is **UNMAPPED at Stage 0** (see summary note below). |
| **Personal Appeal / Royal Audience** *(does NOT map to one of the four games â€” see note)* | Royal Audience, Personal Appeal (`social_contest Â§2 Step 5`, two of the 8 proceedings; `params/contest Â§Proceeding Types L146,L151`); one-sided petition/supplication before a grace-granting authority | `GraceThreshold(bar, petitioner)` (resolver L60-65) â€” **one-sided**: petitioner must clear a bar; no bilateral overlap, no two-pole track. Groundup already presets petition venues (`imperial_petition`/`memorial_remonstrance`, tests L390-395). | Single grace-granting adjudicator (sovereign/prelate), petitioner-only pool. | **ORPHANED by the four-game frame â€” REQUIRES A DECISION.** `GraceThreshold` and the petition venues fit **none** of Agon/Negotiation/Inquiry/Consensus cleanly: it is neither adversarial debate, nor bilateral overlap, nor inquisitorial burden, nor a panel vote. | **DECISION REQUIRED:** Petition/supplication is a **sub-mode of Agon** (a degenerate one-sided ThresholdRace-against-a-grace-bar), NOT a fifth game. Bind Royal Audience + Personal Appeal to **Agon** with win-condition `GraceThreshold` and a single grace-adjudicator venue. This keeps Decision-2's four-game frame total **and** preserves all 8 proceedings: the four games each own â‰¥1 win-condition, and `GraceThreshold` is Agon's petition variant. **The grace-bar itself (how high the petitioner must clear) is venue/adjudicator-resolved, not GM-set** (no GM). Flagged in Â§5.4 as a possible under-spec. |

**Summary (win-condition assignment):** Agon owns `ThresholdRace`/`TallyAtClose` **and** `GraceThreshold` (petition sub-mode); Inquiry owns `ProofBar`; **Consensus owns the `PersuasionTrack` committee band â€” and it is PROMOTE-EXISTING: `faction.py` already implements BG-Vote + Succession on the Track (not per-juror ballots, not a stub to author)**; **Negotiation owns nothing yet â†’ `ZOPAOverlap` is GENUINELY MISSING** (one new win-condition to author + ED). **`VoteAtClose` is UNMAPPED** â€” it is a distinct dikasteria mechanic with no canonical Â§10/Â§7.2 home; it is a groundup PROTOTYPE (resolver L85 "Default venues do NOT use this") and stays **out of the canonical four-game surface at Stage 0** (retain in the kernel as an opt-in prototype, flagged; do not present it as the Consensus resolver). All 8 proceedings still route to a game (Â§1.8 item 7 totality holds: the petition venues land in Agon; BG/Succession land in Consensus via the Track).

### 1.7 DefeatCatalogue/SelfGating â†” Quintilian/Nyaya fallacy-as-foul (CR5)

| Groundup | v30 surface | CR5 | Verdict | Carry-across |
|---|---|---|---|---|
| `DefeatCatalogue` (venue-configured fatal faults: barred-device, self-contradiction, evasion-strikes, yield-strikes â€” Nyaya *nigrahasthÄna*); `SelfGating.licit` (only "hard" gated by standing vs audience) â€” `primitives.py` / `resolver.py` L256 | v30 has **Chamber Violence (forced forfeit, Â§9.6)** and the **stalemate Forced Unmask (PP-255)** but **no general fallacy-as-foul catalogue**. | CR5: "F7 self-gating: obstruction is bounded by your own standing (Quintilian/Nyaya)." | **LOSSY** (v30 has no equivalent richness; groundup adds it). Net-positive import â€” new content the rebuild *gains*, consistent with the world-churn principle. | Adopt `DefeatCatalogue` + `SelfGating` wholesale. Map v30 Â§9.6 Chamber Violence â†’ a `barred` fault. Map PP-255 Forced Unmask â†’ the `DefeatCatalogue` 10-exchange terminator. **Carry the no-GM rule:** which faults are fatal is a **`Venue` property** (engine-resolved from proceeding type), not a GM call. |

### 1.8 Authored v30 content that MUST be carried across (the preservation manifest)

Every item below is canonical v30 content with **no groundup equivalent** â€” the rebuild must preserve it, routed through the new substrate:

0. **Appraise read-the-room loop** (`social_contest Â§4 Step 1 L122-131` / `params/contest Â§Exchange Step 1 L71-77`, PP-278/PP-614/ED-893): **per-exchange Appraise roll** = Attunement + Recall, TN 7, Ob = opponent Cha Ã· 2 (min 1), read into a **4-band information ladder** â€” Failureâ†’*misleading signal: wrong boost identified*; Partial(1)â†’boosted **axis type** (genre or orientation) but not which; Success(2)â†’**full boost identified**; Overwhelming(3+)â†’boost + one specific detail (Belief/threshold/emotional state). **Groundup has NO Appraise** â€” `resolver._view` (L178-187) reads static `audience_learned`/`audience_hostile` and **never rolls**. â†’ **AUTHOR an `appraise` resolver** as the first stochastic surface of each exchange (CR1 L11 names {Appraise, ResolveExchange} as the only stochastic surfaces â€” so Appraise is canonically required and currently absent from groundup). It rolls Attunement+Recall via `dice_engine.roll_pool`, returns the 4-band result, and feeds the Ïƒ-lever armature (a successful Appraise informs which Î´Ïƒ levers the actor can target). **The Failure-band "wrong boost identified" must be an engine-generated misleading signal** (a resolver fabricates a plausible-but-wrong boost), **not a GM fabrication** (Â§5.6) â€” there is no GM. This is a distinct MECHANICS entry (`appraise`) added in Â§3.2.
1. **6 faction ethical-mode boosts** (`social_contest Â§2 Step 3` / `params/contest Â§Faction Boosts L52-61`): Churchâ†’Obscuring, Crownâ†’Revealing, Varfellâ†’Projection, Hafenmarkâ†’Memory, Restorationâ†’Revealing, Guildsâ†’engine-picks (was "GM picks" L60 â€” **re-route to engine**), LÃ¶wenritterâ†’Projection. Niflhel STRUCK (ED-899/764). â†’ Implement as a **Î´Ïƒ lever** (CR6), +1D-equivalent in Ïƒ-space when the orator's chosen genre/orientation matches the dominant faction's boosted axis.
2. **Thread co-movement by winning genre** (`social_contest Â§6` / `params/contest L139`): Memory winâ†’retention shift, MS+1; Projection winâ†’actualization anchor / +1D first Domain Action. â†’ Post-bout hook (engine emits `scene.contest_resolved` with genre; Thread system consumes â€” already in `module_contracts.yaml Â§social_contest emits`).
3. **Conviction Scars** (`social_contest Â§6.2`; Resonant-Style targeting): decisive outcome wounds NPC conviction. â†’ Post-bout hook keyed on degree + appeal-fit. Visibility signal (Â§6.2) is presentation, not engine.
4. **Obligations / Wager** (`social_contest Â§6.1`, Â§6.1.1 edge cases): binding cross-season commitments. â†’ Post-bout faction-layer state, NOT bout logic. Engine emits the decisive-win event; faction layer mints the Obligation clock.
5. **Chain Contests** (`social_contest Â§6.3`): compromise â†’ follow-up next season, starts at final track. â†’ `module_contracts.yaml` records the Chain-Contest cap-3 damper. Engine carries the final track out; faction layer schedules the chain.
6. **Domain Echo** (`social_contest Â§6` / `params/contest Â§Post-Contest L133-135`): decisive+Memoryâ†’Mandate+1; decisive+Projectionâ†’DA bonus. â†’ Post-bout emit, faction layer consumes.
7. **The 8 proceedings** (`social_contest Â§2 Step 5` / `params/contest Â§Proceeding Types L142-151`): Formal, Grand, **Royal Audience**, Church Tribunal, Guild Arbitration, Casual Dispute, Private Negotiation, **Personal Appeal**. â†’ Each becomes a **`Venue` preset** in the new `venues.py`. **Totality now holds (per Â§1.6):** Formal/Grand/Casualâ†’Agon (ThresholdRace/TallyAtClose); **Royal Audience + Personal Appealâ†’Agon petition sub-mode (GraceThreshold)**; Church Tribunalâ†’Inquiry (ProofBar); Guild Arbitrationâ†’Inquiry or Consensus per panel shape; Private Negotiationâ†’Negotiation (ZOPAOverlap, to author); **BG Vote + Successionâ†’Consensus via the PersuasionTrack committee band (PROMOTE-EXISTING `faction.py`, not a per-juror ballot, not a stub)**. Map each proceeding to {exchange-countâ†’budget, adjudicator-typeâ†’pool/Panel, **starting-position resolver (Â§1.5)**, resistance-mod, win-condition, DefeatCatalogue}.

---

## 2. SUBSTRATE DECISION â€” **[m1 numpy-into-root defect + duplicate-export overlap + the TN-dependent-vs-independent boost-magnitude split made explicit]**

**Recommendation: the contest kernel calls `sim/autoload/dice_engine.roll_pool` as the resolution root; the CR2/CR6 Ïƒ-leverage layer is added to `dice_engine` as a SIBLING function, NOT forked into the contest package â€” but this is BLOCKED on first re-housing the Ïƒ-core out of `tests/`, AND the re-housing must STDLIB-re-port (de-numpy) the Ïƒ-core, de-duplicate its already-exported symbols, AND resolve which of m1's TWO non-equivalent boost forms is canonical.**

### 2.1 Why â€” the "two sigma kernels" hazard

The repo already has **two** Ïƒ-leverage implementations that must not become three (flagged in CLAUDE.md Â§7 and the distillation investigation in MEMORY):
- `designs/audit/2026-06-03-contest-groundup/engine.py` â€” `net_boost(lev, pool) = eff_sigma(dÏƒ)Â·sigma_N(pool)`, `eff_sigma = M_MAXÂ·tanh(dÏƒ/M_MAX)`.
- `designs/scene/combat_engine_v1/core.py` â€” `resolve()` (L31) does `roll_net(pool) + m1.soft_cap(net_sigma)Â·m1.sigma_n(pool)` via `m1_dice_sigma_core`, which `core.py` L3 imports by `sys.path.insert`-ing **`tests/sim/v32-combat-balance`**.

These are the **same math** (tanh soft-cap, Ïƒ_N mu-shift, ED-884/934 lineage) expressed twice. If Stage 1 copies the groundup `engine.py` into `sim/personal/contest/`, that becomes a **third** kernel. **Do not.**

### 2.1a **PRECONDITION: the Ïƒ-core does NOT yet live in `sim/autoload`, it is numpy-based, it already exports the symbols we would "add," AND it ships TWO non-equivalent boost forms that a naive de-dup would silently pick between.**

**Verified decisively:**
- `sim/autoload/dice_engine.py` contains **only** `roll_pool` / `continuous_engine_sample` / `degree_from_net` â€” it has **NO** `soft_cap`, **NO** `sigma_n`, **NO** `eff_sigma`, and L8-9 declares **"Dependencies: none â€” root primitive"** using stdlib `math`/`random` only.
- The only working Ïƒ-core (`m1_dice_sigma_core`) lives in a **TEST directory** (`tests/sim/v32-combat-balance/`), reachable today only because combat `core.py` does a `sys.path.insert` hack. **It `import numpy as np` (L21)** and implements `sigma_n` (L65), `soft_cap` (L70), `net_boost` (L92), `p_success` (L116) via `np.tanh`/`np.sqrt`/`np.random`.

**Three defects to address:**
1. **numpy-into-the-root.** Folding m1's `soft_cap`/`sigma_n`/`eff_sigma` into `dice_engine` and adding `roll_pool_leveraged = roll_pool().net + soft_cap()Â·sigma_n()` **as a move** would inject **numpy into the declared dependency-free canonical root** â€” a real substrate defect. The re-housing MUST **stdlib-re-port** the Ïƒ-core (the groundup `engine.py` already does exactly this â€” `eff_sigma`/`net_boost` in pure `math`, no numpy), or house it as a **separate `sim/autoload/sigma_core.py` that `dice_engine` need not import** (keeping the root's "Dependencies: none" honest and numpy out of the canonical root).
2. **duplicate-export overlap.** m1 **already exports `net_boost` (L92) and `p_success` (L116)** â€” so "add `roll_pool_leveraged`/the Ïƒ-lever functions" is **not fresh authoring**; it is a **move-and-de-numpy-and-de-duplicate**. The re-housing spec must reconcile the existing m1 symbols against the intended `dice_engine` surface (avoid two `net_boost`s), not blindly add.
3. **âš  TWO NON-EQUIVALENT BOOST FORMS off TN7 (verified this pass â€” a live substrate defect the de-dup must decide, not gloss).** m1 ships the Ïƒ-boost as **two different functions that agree only at TN7**:
   - **`soft_cap(net_sigma)Â·sigma_n(pool)`** (the **combat path**, `core.py` L31): `sigma_n = SIGMA_N_COEFFÂ·âˆšpool` with `SIGMA_N_COEFF=0.8` (m1 L62/L65-67) â€” **TN-INDEPENDENT** (flat 0.8 coefficient).
   - **`net_boost(net_sigma, pool, tn)`** (m1 L92-99): `soft_cap(net_sigma)Â·sigma_per_die[tn]Â·âˆšpool` where `sigma_per_die` = **0.806 / 0.800 / 0.781 at TN6 / TN7 / TN8** (m1 `PER_DIE` L26-30) â€” **TN-DEPENDENT.**
   These coincide **only at TN7** (where `sigma_per_die`=0.800 = `SIGMA_N_COEFF`=0.8) and **diverge at every other TN**. **Concrete repro (pool=10, net_sigma=0.7):** `soft_cap(0.7)=1.5Â·tanh(0.7/1.5)â‰ˆ0.6529`; combat path `= 0.6529Â·(0.8Â·âˆš10)=0.6529Â·2.5298â‰ˆ1.6519`; `net_boost(TN7)=0.6529Â·0.800Â·âˆš10â‰ˆ1.6519` (**agree**); but `net_boost(TN6)=0.6529Â·0.806Â·âˆš10â‰ˆ1.6643` and `net_boost(TN8)=0.6529Â·0.781Â·âˆš10â‰ˆ1.6127` (**disagree by ~Â±0.05 net**). **The combat oracle uses the TN-independent `soft_capÂ·sigma_n` path (`core.py` L31), NOT `net_boost`.** A de-dup that naively keeps `net_boost` as "the" boost would therefore **shift combat resolution magnitude off TN7** â€” a real substrate defect, latent for contest (TN7-only, Â§5.9) but LIVE for the combat re-point.

**The plan, corrected, is a three-decision re-housing:**

1. **STEP 0 (precondition â€” re-house AND stdlib-re-port AND de-duplicate-with-an-explicit-boost-form-decision).** Port `soft_cap`/`sigma_n`/`eff_sigma`/`net_boost`/`p_success` (the `m1_dice_sigma_core` surface) **out of `tests/sim/v32-combat-balance/` and into `sim/autoload/`** as a **pure-stdlib** module (e.g. `sim/autoload/sigma_core.py`, mirroring how groundup `engine.py` already renders the same math without numpy). This (a) **removes the `sys.path.insert` hack** in combat `core.py`, (b) **keeps numpy out of the `dice_engine` root**, (c) **de-duplicates** m1's existing `net_boost`/`p_success` against the new home (one definition, one place), **and (d) EXPLICITLY DECIDES the boost form.** The re-housing ED MUST state which boost form is canonical and reconcile the two:
   - **Preserve the combat oracle's actual behaviour:** combat resolves on the **TN-independent `soft_capÂ·sigma_n` (coeff 0.8)** path (`core.py` L31). Any consolidated `roll_pool_leveraged` used by combat MUST reproduce that exact form, or combat balance shifts off TN7 (the Â§5.9 parity guard catches this).
   - **Record that `net_boost`'s TN-dependence is a DIFFERENT (TN-exact) design intent** â€” its docstring (m1 L92-96) claims "uniform impact at every pool size **and every TN**." At TN7 the two are identical; off TN7 they encode different intents. Do NOT silently alias them. If the consolidated home keeps both, name them distinctly (`sigma_boost_flat` vs `sigma_boost_tn_exact`) with a cited note on which each caller uses; if it keeps one, the ED records the discarded form and re-baselines any caller that used it. **Contest is TN7-only, so either form gives the same contest result â€” but the choice is load-bearing for the combat re-point and must be made in the ED, not by whichever symbol the de-dup happens to keep.**

   **This is its own task with its own provenance** (the Ïƒ-core's ED-884/934 lineage must be re-cited at the new stdlib home; the boost-form decision needs its own citation). Allocate it to the J-36 seam ED and/or a PP (Â§4). Combat `core.py` is then re-pointed to the autoload home (a follow-on combat edit, recorded under CR7-adjacent propagation â€” NOT an in-place oracle correction, just an import-path change to a behaviourally-identical stdlib port; see Â§5.9 for the parity guard, which now MUST cover the TN-independent-vs-dependent boost form).
2. **STEP 1 (then add the sibling).** With the Ïƒ-core stdlib-re-ported in `sim/autoload` and the boost form decided, expose `roll_pool_leveraged(pool, net_dsigma, tn=7, ob=DECISIVE_OB, rng=None)` alongside `dice_engine.roll_pool`, internally `roll_pool(...).net + <chosen boost form>(net_dsigma, pool[, tn])`. `dice_engine` is the CANONICAL root (params/core.md), already exports `continuous_engine_sample`, and is imported by both the live `sim/personal/contest.py` and the mass-battle engine. **The contest package imports it; it does not redefine it.** `sim/personal/contest/core.py` calls `roll_pool_leveraged`. So does combat (re-pointed, on its TN-independent form). One kernel, two callers â€” the invariant CLAUDE.md Â§8 demands.

### 2.2 The numeric disagreement (must be reconciled before promotion)

The die map is **identical** across all three (`1â†’âˆ’1, 2-6â†’0, 7-9â†’+1, 10â†’+2`; verified `engine.roll_net` vs `dice_engine._die_result`). MU=0.40/SD=0.80 at TN7 agree. **The disagreement is in `degree()`:**

| Source | Overwhelming bar |
|---|---|
| `dice_engine.degree_from_net` | `net >= 2*ob AND net >= 3` (flat, PP-232) |
| groundup `engine.degree` (pool-aware path) | `net >= MUÂ·pool + 0.85Â·SDÂ·âˆšpool AND net >= max(3,ob)` (Ïƒ-scaled, `OVERWHELM_SIGMA=0.85`, "de-saturates degree-3 to ~uniform ~21% across pools") |
| combat `core.degree` | `net >= 2*ob âˆ’ 0.5 AND net >= 2.5` (continuity-corrected for the *continuous* sampler, `core.py` L22) |

**These three give different Overwhelming rates for the same pool.** This matters because Overwhelming drives the decisive outcomes (Conviction Scars, Total Victory, Domain Echo). (Note: the groundup suite's own de-saturation tests â€” `tests.py` L422-435 â€” assert the pool-aware bar stays bounded across pools; that behaviour is the groundup path, not the flat canonical one.)

**Reconciliation rule (respecting "no oracle-correction-in-place"):** Do NOT silently pick one in the new code. The discrete TTRPG path is canonical = `dice_engine.degree_from_net` (flat `2*ob`). The groundup Ïƒ-scaled bar and the combat continuity bar are *calibration refinements* of the continuous path. The fix:
- The contest kernel uses `dice_engine.degree_from_net` for the **discrete** read (canonical).
- If a Ïƒ-scaled Overwhelming de-saturation is wanted (the groundup `OVERWHELM_SIGMA` behavior), it must be **authored into canon via the editorial ledger first** (params/core.md Â§Degrees), then exported to `dice_engine` â€” not hand-carried from `engine.py`. Allocate an ED (Â§4, J-36).
- The combat `âˆ’0.5` continuity correction carries an explicit `[AUDIT-FIX]` self-note (combat `core.py` L20) â€” a known in-place oracle edit that CLAUDE.md Â§6 flags as a hazard. **Do not propagate it** into the contest path; let the continuity correction be resolved once, centrally, at the Ïƒ-core's new autoload home, under its own ED.

**Net:** one kernel (`dice_engine` + re-housed **stdlib** Ïƒ-core in `sim/autoload`), one Ïƒ-sibling (on an explicitly-chosen boost form, Â§2.1a), one degree function. The contest package is a pure consumer. The Overwhelming-bar divergence, the Ïƒ-core re-housing/de-numpy/de-dup, **and the TN-independent-vs-dependent boost-form decision** are all canon/substrate-authoring tasks, not code-copies.

---

## 3. PACKAGE SCAFFOLD SPEC â€” `sim/personal/contest/`

Mirrors `tests/sim/mass_battle/engine.py` (adapter+router+registry, "RESOLVES NOTHING") and the combat `config/core/wrapper` split. Replaces the flat `sim/personal/contest.py` stub.

### 3.1 File list + one-line responsibility â€” **[`policy.py`, `narrative.py`, `faction.py` given homes so the ported suite can COLLECT; Consensus module (`faction.py`) is promote-existing; armature.py now carries the resâ†’Î´Ïƒ conversion + parity check]**

**Scaffold-completeness note:** the promoted `tests.py` imports **three modules the prior file list had no home for** â€” `policy` (L10), `faction` (L168), `narrative` (L251) â€” all present in the groundup dir. Without homes, the ported suite raises **`ModuleNotFoundError` at collection**, a strictly stronger failure than "needs pytest migration." All three are now given scaffold homes below. The promotion set is the **full nine groundup modules**, not five.

| File | Responsibility (one line) |
|---|---|
| `sim/personal/contest/__init__.py` | Re-export the public API (`build_contest`, `resolve_contest`, `MECHANICS`, `mechanics_selftest`); package marker. |
| `config.py` | **The oracle.** One dict `CFG` of all `[SEED]`/`[CANONICAL]` coefficients (scale, jitter, leak caps, Face build/strip, resistance, band thresholds, **venue starting-positions incl. biased Tribunal/Excommunication starts + PP-256 Hybrid clamp + the `faction.coalition_vote` ED-621 lobby clamp**), each with an inline `PP-NNN`/`ED-NNN` provenance comment. Class-C calibration knobs separated from canonical constants. Mirrors `combat_engine_v1/config.py`. |
| `core.py` | **The kernel.** Thin wrapper over `dice_engine.roll_pool` + the Ïƒ-sibling (per Â§2 precondition, on the explicitly-chosen boost form); the `Bout` resolution loop (promoted from groundup `resolver.Bout`); exchange resolution + degree banding. Knows nothing of venues â€” only how an exchange resolves. |
| `primitives.py` | The substrate: `Stasis` (CR4 terrain, **6-ground LADDER reconciled to CR4's 4 staseis + the definitional-reframe operator + CONSEQUENCE/FEASIBILITYâ†’Projection mapping, Â§1.2**), `Appeal`, **`Face`** (renamed from groundup `Standing`, CR3 â€” **rename must propagate into `faction.py`, Â§1.4**), `Room`, `Readiness`, `Resonance`, `SelfGating`, `DefeatCatalogue`, `EvidenceItem`/`Dossier`. |
| `policy.py` | **Promoted from groundup `policy.py`.** The strategy/behaviour policies (`logos_spammer`, `demagogue`, `courtier`, `build_then_close`, `exploiter`, `overreacher`, `staller`) that the test suite drives contestants with (`tests.py` L10-11) and that `faction.py` imports (`logos_spammer` L9). Pure move-selection policies, no resolution. Without this home the suite fails at collection. |
| `venues.py` | The 8 proceedings as `Venue` specs (proof-weights, win-condition, DefeatCatalogue, resistance-mod, exchange-count, **starting-position**). Promoted from groundup `modes.py` venue presets (incl. the cross-cultural inquisition/petition presets, tests L381-398). |
| `adjudicators.py` | `Adjudicator` + `Panel` (single/jury/bench/council), the adjudicator-typeâ†’primary-attribute router (Â§1.1), and the **engine resolvers that replace "GM determines adjudicator type" AND "GM-set starting position" (Â§1.5)** â€” both read scene/venue context, no GM. |
| `modes.py` | The four games {Agon, Negotiation, Inquiry, Consensus} as `ContestMode` constructors binding a venue + adjudicator + win-condition. **Consensus binds `faction.py`'s `coalition_vote`/`succession` on the `PersuasionTrack` committee band (PROMOTE-EXISTING, NOT `VoteAtClose`, Â§1.6).** Hosts the **new `ZOPAOverlap`** (Negotiation) and binds **`GraceThreshold` as Agon's petition sub-mode** (Â§1.6). Promoted from groundup `modes.py` (`ContestedMode`, `INSTITUTIONAL_MODES`, `CROSS_CULTURAL_VENUES`). |
| `faction.py` | **Promoted from groundup `faction.py` â€” the Consensus adapter, already built.** Implements Â§10 BG Parliamentary Vote (`coalition_vote` L118-140, ED-621 lobby clamp), Â§7.2 Succession (`succession` L85-109, split ratios Â§7.2.1), and the committee band (`band_of`/`rate_banded` L68-82) on the `PersuasionTrack`. NOT author-new; re-homed with the `Standing`â†’`Face` rename propagated (Â§1.4) and its ED-621 `start` clamp generalized into the Â§1.5 resolver. Its downstream faction-split domain logic stays faction-layer prose (its own docstrings L94-95/L118-124 already scope this). |
| `armature.py` | The CR6 Î´Ïƒ-lever accumulator: genre-stasis affinity, audience(faction) boost, Recall, Face, corroboration, prep, commit-spend â†’ one `net_dsigma`, tanh soft-capped. **Consumes the Appraise result (Â§1.8 item 0)** to gate which levers the actor can target. The single place setup-advantages become Ïƒ. **âš  This is where the ethos/pathos/logos MULTIPLICATIVEâ†’ADDITIVE conversion lands (Â§1.1): the groundup `res` gain-scaler (resolver L212) becomes an additive Î´Ïƒ lever here. The module MUST carry (i) the documented resâ†’Î´Ïƒ conversion, (ii) an explicit statement of how the `joint_weight` appealÃ—tense cross-term (resolver L128-134) is preserved or deliberately dropped, and (iii) a seeded parity harness against the old multiplicative-`res` path â€” this conversion is NOT a rename and cannot be asserted without the parity check (CR6/armature ED, Â§4).** |
| `appraise.py` | **The read-the-room resolver (Â§1.8 item 0).** Rolls Attunement+Recall via `dice_engine.roll_pool` (TN 7, Ob = opp ChaÃ·2 min 1), returns the 4-band ladder; the Failure band synthesizes an **engine-generated** misleading signal (no GM). CR1's first stochastic surface. |
| `narrative.py` | **Promoted from groundup `narrative.py`.** The post-bout chronicle layer (`summarize`, `venue_brief`, `SHAPES` â€” ROUT/REVERSAL/COLLAPSE/DEADLOCK/SPLIT_DECISION classification) the suite exercises heavily (`tests.py` L250-307, L339-355, L372-379, L443-451). Inert-by-default instrumentation (`record=False` leaves no log, tests L254) â€” presentation, not resolution. Without this home the suite fails at collection. |
| `keys.py` | Key INâ†’OUT contract surface (consumes `state.opinion_revised`; emits `scene.contest_resolved`/`scene.dialogue`/`scene.insult`/`scene.threat` per `module_contracts.yaml Â§social_contest`); the post-bout hooks for Thread co-movement, Conviction Scar, Obligation, Domain Echo. |
| `wrapper.py` | **The adapter+router, RESOLVES NOTHING.** `build_contest` (partyâ†’Contestant adapter) + `resolve_contest` (game-type router). Owns A/B identity (mirrors `combat_engine_v1/wrapper.py`). Hosts `MECHANICS` + `mechanics_selftest`. |
| `tests/` | **Seeded pytest suite.** NB: the promoted groundup `tests.py` is **already** seeded (`random.seed(20260603)`, L14) and **already** gates (`sys.exit(1 if Fc else 0)`, L454) â€” RATIFICATION finding #6's two defects are fixed in the suite; **the suite currently RUNS at `151 passed, 0 failed` (measured this pass)**, so the Stage-1 test work is the **pytest migration** (not "seed a currently-unseeded suite," and not "recover a green count" â€” 151 is measured). **AND the suite only collects once `policy`/`faction`/`narrative` are re-homed** (imports at L10/L168/L251) â€” migration is blocked on scaffold-completeness, not the reverse. Add a deterministic seeded smoke assertion per CLAUDE.md Â§7. Ports the groundup tests + new tests for Face, CR5 orientation (incl. **the CROSS/REINFORCE-suppression replacement**), ZOPA, **Appraise 4-band**, the biased-start resolver, **the resâ†’Î´Ïƒ parity check (Â§1.1/armature)**, the four games. |

### 3.2 MECHANICS registry design (mirrors mass_battle `engine.py L41`) â€” **[Consensus rows PROMOTE-EXISTING â†’ WIRED, not STUB; selftest scoped to WIRED]**

```
MECHANICS = {  # name -> {fn, toggle, source, status}
  # resolution core
  "pool_roll":        {"fn":"roll_pool",          "toggle":None,        "source":"params/core.md (dice_engine)",     "status":"WIRED"},
  "sigma_leverage":   {"fn":"roll_pool_leveraged","toggle":"SIGMA_HEAD","source":"CR6 / ED-884/934 (dice_engine sibling; BLOCKED on m1 stdlib re-housing + boost-form decision, Â§2.1a)","status":"BLOCKED"},
  "degree":           {"fn":"degree_from_net",    "toggle":None,        "source":"params/core.md PP-232",            "status":"WIRED"},
  # read-the-room (Â§1.8 item 0)
  "appraise":         {"fn":"appraise",           "toggle":None,        "source":"social_contest Â§4 Step1 / params Â§Exchange Step1 (PP-278/PP-614/ED-893)","status":"STUB"},
  # trackers (CR3)
  "persuasion_track": {"fn":"PersuasionTrack",    "toggle":None,        "source":"social_contest Â§6 / params/contest","status":"PARTIAL"},  # start now venue-resolved, not hardcoded 5.0 (Â§1.5)
  "starting_position":{"fn":"starting_position",  "toggle":None,        "source":"params Â§Persuasion Track 'GM-set'â†’engine; v30 Â§7 biased Tribunal start; PP-256 Hybrid clamp; generalizes faction.coalition_vote ED-621 clamp","status":"STUB"},  # replaces GM-set start (Â§1.5/Â§5.6)
  "face":             {"fn":"Face",               "toggle":None,        "source":"CR3 (ED-TBD)",                      "status":"PARTIAL"},  # renamed Standing (propagate into faction.py); Composure retirement pending
  "concentration":    {"fn":"Concentration",      "toggle":None,        "source":"social_contest Â§4 / params Â§Derived Values L110 / CR7 (3F+2S; CR3 'Focus*3' SUPERSEDED by ED-902, Â§1.4)","status":"WIRED"},
  # stasis x genre (CR4) â€” 6 grounds reconciled to 4 staseis (Â§1.2)
  "stasis_terrain":   {"fn":"Stasis",             "toggle":None,        "source":"CR4 (ED-TBD); 6-ground LADDER â†’ 4 staseis + CONSEQUENCE/FEASIBILITYâ†’Projection","status":"PARTIAL"},
  "definitional_reframe":{"fn":"stasis_shift",    "toggle":None,        "source":"CR4 'authored Present-rendering' reframe operator (NOT Memory, Â§1.2)","status":"STUB"},
  "genre_stance":     {"fn":"genre_pick",         "toggle":None,        "source":"social_contest Â§Genre (Memory/Projection)","status":"WIRED"},
  # appeal-fit as Î´Ïƒ lever â€” MULTIPLICATIVEâ†’ADDITIVE conversion (Â§1.1)
  "appeal_dsigma":    {"fn":"appeal_lever",       "toggle":None,        "source":"CR6 armature: ethos/pathos/logos res-scaler â†’ additive Î´Ïƒ; REQUIRES resâ†’Î´Ïƒ conversion + joint_weight cross-term decision + seeded parity check (Â§1.1, ED-TBD)","status":"STUB"},
  # orientation (CR5)
  "orient_direct":    {"fn":"_advance",           "toggle":None,        "source":"CR5 (ED-TBD)",                      "status":"WIRED"},
  "orient_indirect":  {"fn":"attack_face",        "toggle":None,        "source":"CR5 self-Face backfire (ED-TBD); retires CLASH/Obscuring-win Doubt Marker (v30 Â§4 L179-183)","status":"STUB"},
  "cross_suppression":{"fn":"cross_suppress",     "toggle":None,        "source":"CR5-adjacent; retires CROSS+REINFORCE Doubt Marker (v30 Â§4 L170 + L179-183 REINFORCE ctx) â€” NEW replacement, Â§1.3","status":"STUB"},  # CROSS + REINFORCE site fix
  # faction boosts
  "faction_boost":    {"fn":"faction_dsigma",     "toggle":None,        "source":"social_contest Â§2 Step3 / params/contest (Guilds 'GM picks'â†’engine)","status":"STUB"},
  # defeat
  "defeat_catalogue": {"fn":"DefeatCatalogue",    "toggle":None,        "source":"groundup (Nyaya) / CR5 F7",         "status":"WIRED"},
  "self_gating":      {"fn":"SelfGating",         "toggle":None,        "source":"CR5 F7 (Quintilian/Nyaya)",         "status":"WIRED"},
  # evidence
  "dossier":          {"fn":"Dossier",            "toggle":None,        "source":"groundup hidden-weight evidence",   "status":"WIRED"},
  # win-conditions / four games
  "win_threshold":    {"fn":"ThresholdRace",      "toggle":None,        "source":"Agon (social_contest Â§2,Â§5)",       "status":"WIRED"},
  "win_grace":        {"fn":"GraceThreshold",     "toggle":None,        "source":"Agon petition sub-mode: Royal Audience/Personal Appeal (social_contest Â§2 Step5)","status":"WIRED"},
  "win_proofbar":     {"fn":"ProofBar",           "toggle":None,        "source":"Inquiry (social_contest Â§7)",       "status":"WIRED"},
  "win_committee":    {"fn":"PersuasionTrack",    "toggle":None,        "source":"Consensus: BG Vote Â§10 + Succession Â§7.2 = pooled Track committee band (NOT VoteAtClose, Â§1.6)","status":"WIRED"},
  "consensus_bgvote": {"fn":"coalition_vote",     "toggle":None,        "source":"Consensus Â§10 BG Vote â€” PROMOTE-EXISTING faction.py L118-140 (ED-621 lobby clamp)","status":"WIRED"},
  "consensus_succession":{"fn":"succession",      "toggle":None,        "source":"Consensus Â§7.2 Succession â€” PROMOTE-EXISTING faction.py L85-109 (split ratios Â§7.2.1)","status":"WIRED"},
  "consensus_committee_band":{"fn":"band_of",     "toggle":None,        "source":"Consensus committee outcome â€” PROMOTE-EXISTING faction.py L68-82 (band_of/rate_banded)","status":"WIRED"},
  "win_vote_PROTO":   {"fn":"VoteAtClose",        "toggle":"DIKASTERIA","source":"groundup PROTOTYPE (dikasteria); UNMAPPED to canon â€” no Â§10/Â§7.2 anchor (Â§1.6)","status":"PROTOTYPE-UNWIRED"},
  "win_zopa":         {"fn":"ZOPAOverlap",        "toggle":None,        "source":"Negotiation (social_contest Â§12 â€” NEW, ED-TBD)","status":"STUB"},
  # post-bout hooks (faction-layer emits)
  "thread_comovement":{"fn":"thread_hook",        "toggle":None,        "source":"social_contest Â§6",                 "status":"STUB"},
  "conviction_scar":  {"fn":"scar_hook",          "toggle":None,        "source":"social_contest Â§6.2",               "status":"STUB"},
  "obligation":       {"fn":"obligation_hook",    "toggle":None,        "source":"social_contest Â§6.1",               "status":"STUB"},
  "domain_echo":      {"fn":"echo_hook",          "toggle":None,        "source":"social_contest Â§6",                 "status":"STUB"},
}
```

**`mechanics_selftest()` â€” CANNOT mirror mass_battle's "every registered fn resolves".** The mass_battle registry is all-`WIRED` precisely so its selftest (`engine.py L79-82`: `missing = [name for name,spec if _resolve(spec["fn"]) is None]`) is a legitimate hard all-must-resolve gate. This registry deliberately carries `STUB`/`BLOCKED`/`PROTOTYPE-UNWIRED` entries whose implementing symbols **do not yet exist** (`appraise`, `attack_face`, `cross_suppress`, `starting_position`, `stasis_shift`, `appeal_lever`, `faction_dsigma`, `ZOPAOverlap`, `roll_pool_leveraged`, the four post-bout hooks, etc.). A verbatim all-must-resolve selftest would therefore **fail on day one**. The fix (choose the scoped selftest, not the fabrication of stub symbols):

```python
def mechanics_selftest():
    """DIVERGES from mass_battle.mechanics_selftest by design: this registry is a Stage-0â†’Stage-N
    burndown, so it legitimately carries STUB/BLOCKED/PROTOTYPE-UNWIRED entries whose fn symbols do
    not exist yet. We therefore assert resolution ONLY over WIRED entries (mass_battle can assert over
    ALL because every mass_battle entry is WIRED). Non-WIRED entries are validated by *status*, not by
    symbol-resolution, until their authoring ED lands and they flip to WIRED.
    Returns (ok, missing_wired, unresolved_nonwired)."""
    wired = {n: s for n, s in MECHANICS.items() if s["status"] == "WIRED"}
    missing_wired = [n for n, s in wired.items() if _resolve(s["fn"]) is None]
    # informational only â€” NOT a failure while status != WIRED:
    unresolved_nonwired = [n for n, s in MECHANICS.items()
                           if s["status"] != "WIRED" and _resolve(s["fn"]) is None]
    return (len(missing_wired) == 0, missing_wired, unresolved_nonwired)
```

Status legend: **WIRED** (resolves + tested â€” the ONLY tier the hard selftest gates) / **PARTIAL** (resolves, canon edit pending â€” still WIRED-resolvable, so it passes the hard gate) / **STUB** (symbol not yet authored â€” validated by status, must NOT be registered as WIRED) / **BLOCKED** (depends on the Â§2.1a m1 re-housing + boost-form-decision precondition) / **PROTOTYPE-UNWIRED** (`VoteAtClose` â€” kept in the kernel as an opt-in prototype, deliberately NOT bound to a canonical game, Â§1.6). **Invariant enforced by the selftest itself:** a mechanic may carry `status:"WIRED"` **only if** its `fn` already resolves â€” so the promotion gate for each Stage-N item is "flip STUBâ†’WIRED, and the same selftest that ignored it now hard-checks it." **The Consensus rows (`consensus_bgvote`/`consensus_succession`/`consensus_committee_band`) are WIRED from day one** because their symbols (`coalition_vote`/`succession`/`band_of`) already exist in the promoted `faction.py`. The remaining burndown is the honest count of non-WIRED entries: `appraise`, `starting_position`, `definitional_reframe`, **`appeal_lever`** (the resâ†’Î´Ïƒ conversion), `orient_indirect`/`attack_face`, **`cross_suppression`** (the CROSS+REINFORCE Doubt-Marker replacement), `faction_dsigma`, `win_zopa`, the four post-bout hooks, plus the `BLOCKED` `sigma_leverage` and the `PROTOTYPE-UNWIRED` `VoteAtClose`. **The genuinely-already-present groundup symbols keep `WIRED`** (`roll_pool`/`degree_from_net`/`PersuasionTrack`/`Concentration`/`genre_pick`/`_advance`/`DefeatCatalogue`/`SelfGating`/`Dossier`/`ThresholdRace`/`GraceThreshold`/`ProofBar`/**`coalition_vote`/`succession`/`band_of`**).

### 3.3 The wrapper API (mirrors `build_unit`/`resolve_battle`)

```python
def build_contest(side, *, primary_attribute, history, focus, spirit, charisma,
                  attunement, recall,                      # Appraise pool inputs (Â§1.8 item 0)
                  faculty=None, face_start=Face.START, evidence=None,
                  convictions=(), beliefs=()):
    """Party -> Contestant ADAPTER (mirrors mass_battle.build_unit). Constructs one
    contestant spec from canonical character stats; resolution stays in core/. No outcome logic.
    primary_attribute is resolved by adjudicator-type at resolve time, not here.
    attunement/recall feed the per-exchange Appraise roll.
    Concentration is derived (3*focus)+(2*spirit) per ED-902 (NOT CR3's Focus*3, Â§1.4)."""

def resolve_contest(side_a, side_b, *, game='agon', venue=None, adjudicator=None,
                    stakes=None, rng=None, kind=None):
    """Game-type ROUTER (mirrors mass_battle.resolve_battle). Dispatches to the Bout loop in core;
    computes no outcome itself. game in {'agon','negotiation','inquiry','consensus'} selects the
    {win-condition, default venue, adjudicator shape}; venue/adjudicator override the defaults.
    - 'agon' additionally accepts petition venues (Royal Audience/Personal Appeal) -> GraceThreshold (Â§1.6).
    - 'consensus' (BG Vote / Succession) resolves on the PersuasionTrack committee band via the promoted
      faction.coalition_vote/succession, NOT VoteAtClose (Â§1.6).
    The starting Persuasion-Track position is resolved from venue, NOT passed/GM-set (Â§1.5).
    Args pass through to core.Bout.resolve so routing is byte-exact with calling Bout directly."""
```
Both delegate downward (adapterâ†’`primitives`/`core`, routerâ†’`core.Bout`; `consensus`â†’`faction`). No resolution in `wrapper.py` â€” the P1 seam. The `game` argument is the four-games entry point (Decision 2); `venue` is the 8-proceedings entry point; the **petition proceedings ride Agon** (not a fifth game); **BG Vote/Succession ride Consensus via the promoted `faction.py` on the Track** (not a per-juror ballot, not a stub).

---

## 4. ED-ID RE-BLOCK PLAN â€” **[Consensus wiring re-classified promote-existing â†’ the "Consensus-committee-band-wiring" ED is removed; the appeal resâ†’Î´Ïƒ conversion is folded into the CR6/armature ED, no new ID; count 20; allocate ED 1055-1074 / next_free 1075]**

**State (verified, `references/id_reservations.yaml`):** ED ceiling exhausted at 1042 (round-1 blocks A/B/C overran to 1042). Round-2 block **D** is ACTIVE: `ED: {block:"1050-1099", next_free: 1055}` (1050-1054 filed for ecosystem-review Top-5); `PP: {block:"800-829", next_free: 800}`. Buffer 1043-1049 deliberately left clear.

### 4.1 ID demand (count)

| Need | IDs | Rationale |
|---|---|---|
| **CR1-CR7 ledger entries** | 7 ED | RATIFIED_2026-06-01.md "ED entries for CR1-CR7 (ID assignment pending)" â€” one ED each. **The CR3 ED must annotate: Concentration formula `Focus*3` (CR3 L13) SUPERSEDED by ED-902; canonical = `3F+2S` (Â§1.4)** â€” no separate ED, folded into the CR3 entry. |
| **The armature** (CR6 Î´Ïƒ-lever layer as authored canon) | 1 ED | `armature.py`'s lever table needs a canon home + provenance. **âš  This ED ALSO owns the ethos/pathos/logos MULTIPLICATIVEâ†’ADDITIVE conversion (Â§1.1): it MUST record the resâ†’Î´Ïƒ conversion, the `joint_weight` appealÃ—tense cross-term decision (preserved or deliberately dropped, with rationale), and a seeded parity check old-`res` vs new-Î´Ïƒ. This is not a rename â€” it is a semantic change and is scoped into the CR6/armature ED, no extra ID.** |
| **Â§1.2 stasis reconciliation** (6-ground LADDER â†’ CR4's 4 staseis; definitional-reframe operator; CONSEQUENCE/FEASIBILITYâ†’Projection) | 1 ED | CR4 leaves 2 of groundup's 6 grounds unreconciled and mis-locates definitional; this authors the closure. |
| **Â§1.3 Doubt-Marker retirement + CROSS/REINFORCE replacement** (retire ALL THREE Doubt-Marker contexts: CLASH L179-183, REINFORCE L179-183, CROSS L170; author the CROSS/REINFORCE-shared bounded-suppression channel) | 1 ED | The CROSS **and REINFORCE** contexts have no clean CR5 `attack_face` host; needs an explicit replacement + enumerated retirement of all three contexts. NEW. |
| **Appraise resolver** (read-the-room loop as an engine roll + 4-band ladder + engine-generated Failure signal) | 1 ED | Â§1.8 item 0; groundup has no Appraise, CR1 requires it as a stochastic surface. |
| **Starting-position resolver** (GM-set Persuasion start + biased Tribunal/Excommunication starts + PP-256 Hybrid clamp + generalize `faction.coalition_vote` ED-621 clamp â†’ engine) | 1 ED | Â§1.5/Â§5.6 un-rerouted GM decision. |
| **Two new/wired games** | 2 ED | Negotiation/ZOPA (genuinely missing) + Inquiry-asymmetry-wiring. **Consensus is NO LONGER a separate ED: `faction.py` already implements BG-Vote + Succession on the committee band (PROMOTE-EXISTING, Â§1.6) â€” its promotion is a re-home + `Standing`â†’`Face` rename + `start`-clamp generalization, all covered by the CR3 rename ED and the starting-position ED, not a fresh game-authoring ED.** (Agon CLEAN; the GraceThreshold petition-sub-mode binding rides the Agon wiring, no separate ED.) |
| **Seam-closure (J-36)** â€” TWO substrate seams | 2 ED | (a) the `degree()` Overwhelming-bar reconciliation across `dice_engine`/groundup/combat (Â§2.2); **(b) the Ïƒ-core re-housing + stdlib-re-port (de-numpy) + de-dup + the TN-independent-vs-dependent boost-form decision out of `tests/sim/v32-combat-balance` into `sim/autoload` (Â§2.1a), with combat `core.py` import re-point + parity guard.** |
| **Settlement** (terminal T-25 throughlines pass + seeded sim-validation â†’ non-provisional) | 1 ED | RATIFIED_2026-06-01 L3. |
| **Subtotal** | **17 ED** | (7+1+1+1+1+1+2+2+1 = 17) |
| Contingency (Composure-retirement edit, faction-boost-as-Ïƒ, Guilds "GM picks"â†’engine â€” v30 edits each wanting a citation) | +3 ED | The Â§1.4/Â§1.8 v30 surface edits. |
| **TOTAL** | **20 ED** | (17 + 3 = 20) Within block D's remaining capacity (1055-1099 = 45 free). |

PP demand: the Ïƒ-sibling in `dice_engine` (`roll_pool_leveraged`), **the Ïƒ-core stdlib re-housing + de-dup + boost-form consolidation + combat `core.py` import re-point (Â§2.1a)**, and the discrete-degree reconciliation may warrant PPs (mechanical patches to existing code). Reserve **3 PP** from block D (`800-802`). Most work is *new authoring* (ED), so ED-heavy is correct. **Note: promoting `faction.py`/`policy.py`/`narrative.py` into the scaffold is a code re-home, not new authoring â€” it consumes no ED beyond the CR-fold-in/rename EDs already counted.**

### 4.2 Block choice

**Block D** (round-2 ACTIVE). No re-block of a *new* round is needed â€” D has 45 free ED (1055-1099) and 30 free PP (800-829), more than enough for the 20 ED + 3 PP. The CLAUDE.md Â§4 warning ("ED ceiling exhausted at 1042 â€” re-block before allocating") refers to *round-1* exhaustion that round-2 block D already resolved. **No version bump, no new disjoint block.** Allocate sequentially from `next_free`.

### 4.3 Precise edit to `references/id_reservations.yaml` (PLAN â€” not applied)

Allocate **ED 1055-1074 (20 IDs, next_free 1075)** and PP 800-802 (3 IDs). Edit the block-D entry:

```yaml
  D:
    ED: { block: "1050-1099", next_free: 1075 }   # 1050-1054 ecosystem-review; 1055-1074 social-contest rebuild Stage-0 (20 IDs)
                                                   # (CR1-CR7 [CR3 annotates ED-902 Concentration supersession] + armature
                                                   #  [armature ED owns the appeal resâ†’Î´Ïƒ multiplicativeâ†’additive conversion + parity check, Â§1.1]
                                                   #  + Â§1.2 stasis reconciliation + Â§1.3 Doubt-Marker retirement/CROSS+REINFORCE replacement
                                                   #  + Appraise + starting-position + 2 games [Negotiation/ZOPA + Inquiry;
                                                   #  Consensus is PROMOTE-EXISTING faction.py, no game-ED] + J-36 degree seam
                                                   #  + sigma-core stdlib re-housing/boost-form seam + settlement + 3 v30-edit contingency)
    PP: { block: "800-829", next_free: 803 }       # 800-802 social-contest rebuild
                                                   # (roll_pool_leveraged sibling + sigma-core stdlib re-housing/de-dup/boost-form/combat import re-point
                                                   #  + discrete-degree reconciliation)
```

And bump the header `updated:`/`source:` to note the round-2 social-contest allocation. The actual ledger entries (`canon/editorial_ledger.jsonl`) are written in the Stage that does the work, citing these IDs; Stage 0 only **reserves** by bumping `next_free` (per the ALLOCATION PROTOCOL, this file co-commits with the entries â€” so the bump and the JSONL writes land together in a later stage, not now). **Lane ownership:** block D is the integration lane; per the protocol, note the bump in the handoff for Lane B or bump within-range and let CollisionError-retry serialize. **Off-by-one guard:** the itemized demand is exactly 17 line-item IDs + 3 contingency = **20**; the allocation is 1055-1074 inclusive = **20 IDs**; `next_free` advances to **1075**.

---

## 5. SELF_ATTACK â€” the weakest seams in my own map

Honest list, worst-first. These are where Stage 1 will bleed if not watched. (Items previously upheld as MUST-FIX are folded into the body above; the residual risks below are what remains live.)

**5.1 The test COUNT is now MEASURED at 151 â€” the two self-reports (README 36, RATIFICATION 62) are both STALE.** I **ran `python tests.py` this pass**; it prints `RESULT: 151 passed, 0 failed`. So the earlier "62 is the more plausible live count" was a *plausibility-ranked guess presented as a conclusion*, and any claim that the count was "verified directly" was false for the count â€” the suite is fast (sub-second) and simply had not been executed. **Corrected: the true green count is 151** (~147 `ck()` calls confirm the magnitude â€” the fork/cross-cultural/de-saturation/faction blocks push it far past both self-reports). **Both README.md (36/36) and RATIFICATION.md (62/62) are therefore STALE self-reports and must be re-baselined to 151** in the Stage-1 migration commit. The residual risks that *remain* real: (a) the suite is a hand-rolled `ck()` harness with a `RESULT: P passed, Fc failed` line, **not pytest** â€” CLAUDE.md's only executable suite is `tests/valoria` under pytest, so a **pytest migration** is still owed (and the 151 assertions must survive that migration unchanged, else balance drift hides in the port); (b) **the suite cannot even *collect* until `policy`/`faction`/`narrative` are re-homed** (imports L10/L168/L251), so "run it green" in the *new package* is gated on the scaffold being complete first â€” the 151 I measured is against the groundup dir where all nine modules co-reside, not against the not-yet-built `sim/personal/contest/`. (RATIFICATION finding #6's two defects â€” unseeded, exit-0 â€” are separately already fixed in the suite: L14 seed, L454 gate.) Net: the *count* is no longer a risk (it is 151, measured), but the *migration-preserves-151* and *collect-only-after-scaffold-complete* dependencies are.

**5.2 Composure retirement (Â§1.4) is the deepest-wired edit and the most likely to break canon.** CR3 retires Composure, but v30 wires ComposureÃ—3 into CLASH/REINFORCE strain math (`params/contest Â§Interaction Types L90-91`: strain = margin + Cha-mod âˆ’ Foc-defence, Ã—3-scaled; ED-891). Retiring Composure means re-deriving *all* strain routing onto Face â€” and Face (groundup `Standing`, BUILD/STRIP=0.8, frac-to-START) has *different* dynamics. **The strainâ†’Face remap is a re-derivation, not a rename, and I have not specified the conversion.** High risk of silently changing balance. **Compounded twice:** (a) the same CR3 that retires Composure *also* carries the struck `Focus*3` Concentration formula â€” so the CR3 fold-in touches two independently-superseded surfaces (Composure and the Concentration formula), both annotated in one ED, raising the chance of a transcription slip; and (b) **the `Standing`â†’`Face` rename must now propagate into the promoted `faction.py`** (`Standing.START` at L44-46/L100-101, `import` L8) â€” a rename that spans the Consensus adapter, not just `primitives.py`; miss it and `faction.py` fails at import (compounding the Â§3.1 collection risk).

**5.2a The ethos/pathos/logos MULTIPLICATIVEâ†’ADDITIVE conversion (Â§1.1) is a second silent-balance-shift risk, on par with Composure retirement.** The upheld finding is now in the body (Â§1.1/armature.py/Â§4), but the residual danger persists: groundup `res` is a **multiplicative gain-scaler** on `MERIT_SCALEÂ·magnitude` (resolver L212-214), on a code path **entirely separate** from the additive `net_boost` mu-shift (L193); the armature (Â§3.1) folds appeal-fit into a **single additive tanh-capped `net_dsigma`**. A multiplicative scaler and an additive mu-shift **cannot be made identical** â€” only matched pointwise over a chosen pool/appeal/tense grid â€” and the `joint_weight` appealÃ—tense cross-term (L128-134) has **no additive analogue at all**. So even with the ED's conversion + parity check, some behaviour (the cross-term's shape, and the res/mu-shift interaction at the tails) **will** change; the honest posture is "bounded, measured, and cited," not "preserved." Risk: if the parity check is written to a loose tolerance it will greenlight a real magnitude drift; if the cross-term is dropped without a churn check it may inert a live rhetorical dimension (ties to Â§5.5). This conversion must be treated as a *design change requiring sim-validation*, not a mechanical carry-across.

**5.3 The `advâ†’track` scale coefficient is unvalidated and gates every outcome.** `PersuasionTrack.scale=1.5` (resolver L72) maps per-side `adv` deltas onto the 0-10 band, but `adv` accumulates `MERIT_SCALE=2.6 Ã— deg Ã— res Ã— rdy Ã— jitter Ã— bias` per advance (resolver L214) â€” a *different magnitude regime* than v30's margin-vs-resistance integer movement. The band-read is CLEAN (Â§1.5) but the **scale calibration is a [SEED]** (RATIFICATION CONFIDENCE: "medium on lever/Face/Concentration coefficients"). CR6's validated "+0.191 uniform across pools" is the *lever*, not the `advâ†’track` mapping. **Plus (Â§1.5 fix): the per-venue biased starting positions are themselves uncalibrated** â€” how far the Tribunal start is from neutral, and where exactly inside the PP-256 4-6 Hybrid clamp a BG contest starts, are numbers I have not pinned to a cited ED. **And (knock-on):** `faction.coalition_vote` uses its own `RESIST_DAMP=0.15` `[SEED]` (faction.py L28) and a `scale*damp` compression (L138) â€” so promoting `faction.py` imports a *second* uncalibrated scale regime into Consensus; the Â§1.5 resolver must reconcile the BG-Vote `start`/`scale`/`damp` with the general Track calibration, not just adopt `faction.py`'s seeds unaudited. **Now a third uncalibrated regime lands here too (from Â§5.2a): the resâ†’Î´Ïƒ conversion coefficient is itself a new [SEED] gating every advance's magnitude â€” so `scale`, `damp`, AND the appeal-Î´Ïƒ gain are three interacting uncalibrated coefficients feeding one `adv` clock.**

**5.4 Negotiation/ZOPA is a genuine void I am hand-waving â€” and Personal Appeal/Grace may be a second one.** v30 says ZOPA is "structurally different from Persuasion Track, **not designed**" (Â§12); I have *no* mechanical spec for bilateral reservation values or how Ïƒ applies to a non-adversarial overlap-search. RATIFICATION.md also flags "Non-adversarial motions (Subsidy, Treaty Ratification) not modelled" â€” note `faction.MOTIONS` (L23-25) *lists* `subsidy`/`embargo`/`blockade` but with `reb_ob=None`, i.e. they route through the strict vote, not a designed non-adversarial path, so the void persists even in the promoted Consensus module. **From the Â§1.6 fix:** I bound Personal Appeal/Royal Audience to Agon-via-`GraceThreshold`, but a one-sided supplication-against-a-grace-bar is arguably *also* structurally distinct from adversarial debate â€” calling it an "Agon sub-mode" may be a frame-of-convenience that hides a second under-designed surface (how is the grace-bar set? what does the petitioner spend? is there genuine churn, or is it a degenerate pass/fail?). Both ZOPA and Grace may need their own ground-up research passes, not win-condition slots.

**5.5 The ethos/pathos/logos vs Cognition/Charisma/Attunement axis-mismatch (Â§1.1) â€” my "keep both" resolution doubles the dimensionality.** A move now has BOTH a per-contest pool axis AND a per-move appeal axis, where v30 had only the former. I have not checked the appeal axis against the canon-guard / world-churn axiom (does it generate reachable churn, or is it inert?). If ethos/pathos/logos doesn't change outcomes enough to matter, it is a zero-Key inert mechanic â€” exactly the debt MEMORY flags as top-priority. I asserted "keep both" without proving both earn their keep. **This risk is now doubly live: the multiplicativeâ†’additive conversion (Â§5.2a) is precisely the moment the appeal axis could be flattened** â€” if the resâ†’Î´Ïƒ conversion collapses ethos/pathos/logos into a scalar with the cross-term dropped, the axis loses its differentiating shape and becomes inert. So the CR6/armature parity check must assert not just magnitude parity but that the appeal axis still *moves outcomes differently by appeal type* (a churn check), or "keep both" is unearned. (Weakly mitigated: `faction.py`'s voter `disposition=(ethos,pathos,logos)` L17 and `discipline`-gated leak L18 *do* make the appeal axis outcome-bearing in the Consensus context â€” so the axis is at least non-inert there; whether it earns its keep in adversarial Agon, and survives the additive conversion, is still unproven.)

**5.6 The "no GM" reroute is broader than I first caught.** Load-bearing un-rerouted GM points now surfaced: **(a)** the **GM-set Persuasion-Track starting position** (incl. biased Tribunal/Excommunication starts + the PP-256 Hybrid clamp, `params L101` + v30 Â§7) â€” routed to a `starting_position` venue-resolver (Â§1.5), whose BG-Vote instance already exists as `faction.coalition_vote`'s ED-621 clamp; **(b)** the **Appraise-Failure "wrong boost identified" signal** (`params L74`) â€” GM fabrication with no engine analogue, routed to an engine-generated misleading signal in `appraise.py` (Â§1.8 item 0); **(c)** the **Guilds "GM picks" boost axis** (`params L60`) â€” routed to engine; **(d)** the adjudicator-type determination (Â§1.1); **(e)** the Negotiation overlap adjudication (Â§1.6). That is **five** rerouted points â€” but the v30 doc is *pervasively* GM-framed ("GM records" Â§Exchange Step 7 `params L85`; "GM narrates" Â§Post-Contest `params L136`; "hidden GM ledger," "GM sets" throughout). **A full Â§-by-Â§ GM-audit of `social_contest_v30.md` + `params/contest.md` is still needed and is not in this artifact** â€” I have caught the load-bearing ones, not all of them. Each resolver I named is *specified by intent, not by implementation*; Stage 1 must actually design them.

**5.7 The degree() reconciliation (Â§2.2) defers to "author an ED" but the three implementations may not be reconcilable to a single function.** dice_engine wants discrete `2*ob`; combat wants continuous `2*obâˆ’0.5` (`core.py` L22); groundup wants Ïƒ-scaled. These encode *different design intents*. A single `degree()` may not satisfy all three callers. The honest possibility: `dice_engine` needs `degree_from_net` AND `degree_from_net_continuous`, and "one kernel" is satisfied by co-location, not by one function. **Compounded by the Â§2.1a fix:** the Ïƒ-core stdlib re-housing means the continuous-path functions move homes AND lose numpy, so this reconciliation and the re-housing must be sequenced together (re-house + de-numpy first, then reconcile at the new home), or the continuity term gets resolved twice. Note also the groundup suite *asserts* its pool-aware de-saturation behaviour (`tests.py` L422-435) â€” so a "reconcile to the flat canonical bar" decision will break promoted groundup tests unless those assertions are explicitly re-baselined under the degree-seam ED (and re-baselining those assertions will change the 151 count â€” see Â§5.1).

**5.8 Faction-boost-as-Î´Ïƒ changes the boost's magnitude semantics.** v30 boosts are flat +1D (PP-234 explicitly chose integer dice over fractional multipliers; `params Â§Genre and Orientation Bonus Dice L45-50`). CR6 converts setup advantages to Î´Ïƒ (tanh soft-capped). **A +1D flat boost and a Î´Ïƒ-lever boost are not numerically equal** â€” the conversion (what dÏƒ equals "+1D"?) is unspecified, and PP-234 deliberately *removed* fractional multipliers that CR6 reintroduces in Ïƒ-space. Could re-litigate a settled decision. Needs explicit +1Dâ†”Î´Ïƒ calibration with a cited anchor.

**5.9 The Ïƒ-core re-housing (Â§2.1a) touches combat â€” a "frozen" oracle path â€” and is now a bigger change than "an import path," with a BOOST-FORM landmine.** Moving `m1_dice_sigma_core` out of `tests/sim/v32-combat-balance`, **stripping numpy**, **de-duplicating its existing `net_boost`/`p_success`**, **deciding between its TWO non-equivalent boost forms (`soft_capÂ·sigma_n` TN-independent vs `net_boost` TN-dependent â€” verified to diverge off TN7, Â§2.1a)**, and re-pointing combat `core.py`'s import is a change to the combat engine's substrate. **Combat resolves on the TN-independent `soft_cap(net_sigma)Â·sigma_n(pool)` path (`core.py` L31), NOT `net_boost`** â€” so a de-dup that keeps `net_boost` as "the" boost would shift combat's resolution magnitude at TN6/TN8 (combat is not TN7-only the way contest is). Combat is an oracle the GDScript port validates against (CLAUDE.md Â§6/Â§7); any edit to its resolution path risks Key-log parity drift. **The numpyâ†’stdlib re-port is NOT mechanically inert by default** â€” `np.tanh`/`np.sqrt`/`np.random` vs `math.tanh`/`math.sqrt`/`random` can differ at the last ULP and, more dangerously, the RNG stream will differ unless the port preserves the exact draw sequence. This must be done as a **numerically-audited port with a seeded parity re-run** against the current combat Key-log â€” the parity assertion MUST hold combat on its **TN-independent `soft_capÂ·sigma_n` form** (not silently swapped to `net_boost`) â€” **not** bundled with the degree() reconciliation, or combat balance could shift under cover of a "refactor." This is the highest-risk *substrate* task in the plan and belongs to its own PP with a mandatory before/after parity assertion that covers the boost-form choice.

**5.10 `VoteAtClose` is now orphaned in the kernel â€” a latent trap.** By the Â§1.6 fix I un-assigned `VoteAtClose` from Consensus (it has no canonical Â§10/Â§7.2 anchor) and left it as a `PROTOTYPE-UNWIRED` opt-in. **Risk:** a later stage, seeing an unused win-condition in the promoted kernel, may re-wire it to Consensus by default (it *looks* like the natural "vote" resolver), silently reintroducing the room-crossing dikasteria semantics that canon does not have. **This risk is sharpened** by `faction.coalition_vote`/`succession` sitting right beside `VoteAtClose` in the promoted kernel â€” the *correct* Consensus resolvers on the Track â€” so a maintainer could easily conflate "the vote thing" and wire the wrong one. The MECHANICS registry flags it (`DIKASTERIA` toggle, `PROTOTYPE-UNWIRED`, "UNMAPPED to canon") and names `coalition_vote`/`succession` as the WIRED Consensus resolvers, but that is a soft guard. If BG Vote/Succession are ever found to *need* per-juror crossing (they currently do not â€” both are pooled Track reads, confirmed in `faction.py`), that would be a **new canon decision with its own ED**, not a default wiring. **Note also (tests.py L392-393):** the groundup `secret_council` cross-cultural venue *does* bind `VoteAtClose` (ballotta) â€” so the prototype is reachable via a preset; Stage 1 must decide whether `secret_council` is in or out of the canonical proceeding set (it is NOT one of the v30 eight), and if out, not let its test assertions keep the prototype alive as de-facto canon (those assertions are part of the 151 â€” see Â§5.1; dropping the venue drops some of the count).

**5.11 The MECHANICS selftest DIVERGES from mass_battle by design â€” the divergence itself is a risk.** To avoid the day-one failure of an all-must-resolve gate over a registry that legitimately holds STUB/BLOCKED fns, Â§3.2's `mechanics_selftest` scopes the hard check to `status:"WIRED"` entries only. **This is a deliberate weakening of the mass_battle invariant** ("every registered fn resolves"), and it has a failure mode: the gate is only as honest as the `status` field. If a later stage mislabels a still-missing symbol as `WIRED` (or forgets to flip STUBâ†’WIRED when it authors the symbol), the scoped selftest will either (a) pass while a `WIRED` entry is actually broken â€” caught, because I made `WIRED`â‡’must-resolve an enforced invariant â€” or (b) silently leave a finished mechanic at STUB, so it never gets hard-checked. **This risk shrank slightly:** the three Consensus rows are legitimately WIRED on day one (their `faction.py` symbols resolve immediately), so there are *fewer* STUBs to mis-track â€” but it also means the selftest now hard-gates `coalition_vote`/`succession`/`band_of` from day one, so the `Standing`â†’`Face` rename (Â§5.2) MUST land cleanly in `faction.py` or the selftest goes red immediately (a good early tripwire, but a tripwire nonetheless). Mitigation baked in: the promotion ritual is "author symbol â†’ flip STUBâ†’WIRED â†’ the same selftest now gates it," and `unresolved_nonwired` is reported (not asserted) so drift is visible. Stage 1 should add a CI assertion that **no WIRED entry is unresolved** (already the `missing_wired` return) AND periodically audit that STUB entries whose symbols now resolve get promoted.

---

### Key file paths (absolute) this artifact is built from

- Kernel to promote (the **full nine** modules, `glob`-verified): `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\designs\audit\2026-06-03-contest-groundup\{contract,engine,primitives,resolver,modes,policy,faction,narrative}.py`, `RATIFICATION.md`, `TERMINOLOGY.md`, `README.md`, **`tests.py` (VERIFIED: L14 `random.seed(20260603)` seeded; L454 `sys.exit(1 if Fc else 0)` gates â€” finding-#6 defects already fixed; imports `policy` L10 / `faction` L168 / `narrative` L251 â†’ all three MUST have scaffold homes or the suite fails at collection, Â§3.1; MEASURED THIS PASS: `python tests.py` â†’ `RESULT: 151 passed, 0 failed` â†’ README 36/36 and RATIFICATION 62/62 are BOTH STALE self-reports, re-baseline to 151; open item = pytest migration that preserves 151, Â§5.1)**; resolver.py verified: `PersuasionTrack` L67-81, `VoteAtClose` L84-103, `GraceThreshold` L60-65, `ProofBar` L53-59; **`_advance` L200-215 â€” appeal enters MULTIPLICATIVELY via `res` L212 (`gain=MERIT_SCALEÂ·magnitudeÂ·resÂ·rdyÂ·jitterÂ·bias` L214), on a path SEPARATE from the additive `net_boost` mu-shift (`_reception` L193); `joint_weight` appealÃ—tense cross-term L128-134 â€” the multiplicativeâ†’additive conversion the armature ED must record + parity-check, Â§1.1/Â§5.2a**
- **Consensus adapter (PROMOTE-EXISTING):** `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\designs\audit\2026-06-03-contest-groundup\faction.py` (VERIFIED: `coalition_vote` Â§10 BG-Vote L118-140 w/ ED-621 lobby clamp L134; `succession` Â§7.2 L85-109 w/ split ratios Â§7.2.1 L108; `band_of`/`rate_banded` committee band L68-82; uses `Standing` L8/L44-46/L100-101 â†’ rename to `Face` on promotion; `RESIST_DAMP=0.15` [SEED] L28; voter `disposition=(ethos,pathos,logos)` L17 / `discipline`-gated leak L18 â†’ appeal axis is outcome-bearing in Consensus, Â§5.5)
- Canonical surface: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\designs\scene\social_contest_v30.md` (Doubt Marker TWO sites: OBSCURING-WIN headerless block Â§4 L179-183 firing in CLASH **or REINFORCE** per infill L30 + CROSS Â§4 L170; REINFORCE block L160-163; CROSS block L165-172); `...\params\contest.md` (Appraise Â§Exchange Step 1 L71-77; Persuasion-Track GM-set start L101; Obscuring-win Doubt Marker L95; **Concentration=(3Ã—Focus)+(2Ã—Spirit) L110 / ED-902**; faction boosts L52-61)
- CR1-CR7: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\designs\audit\2026-06-01-contest-redesign\RATIFIED_2026-06-01.md` (CR3 "Focus*3" L13 â€” SUPERSEDED by ED-902; CR4 definitional-via-Present-rendering L14; CR5 supersedes Doubt Marker L15; **CR6 leverage-as-Î´Ïƒ â€” receives the appeal resâ†’Î´Ïƒ multiplicativeâ†’additive conversion + parity check, Â§1.1**)
- Wrapper pattern: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\tests\sim\mass_battle\engine.py` (**`MECHANICS` L41-77 all-WIRED; `mechanics_selftest` L79-82 = hard all-must-resolve â€” the contest registry legitimately carries STUB/BLOCKED fns, so its selftest must scope to WIRED, Â§3.2**); `...\designs\scene\combat_engine_v1\{config,core,wrapper}.py` (**core.py L3 `sys.path.insert` of `tests/sim/v32-combat-balance` â†’ `m1_dice_sigma_core`; L31 resolves on `m1.soft_cap(net_sigma)Â·m1.sigma_n(pool)` â€” the TN-INDEPENDENT boost form, NOT `net_boost`, Â§2.1a/Â§5.9; L22 degree continuity `2*obâˆ’0.5` [AUDIT-FIX] L20**)
- Dice root: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\sim\autoload\dice_engine.py` (L8-9 "Dependencies: none â€” root primitive"; NO Ïƒ-functions present â€” re-housing precondition, Â§2.1a)
- Ïƒ-core (test dir, numpy): `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\tests\sim\v32-combat-balance\m1_dice_sigma_core.py` (L21 `import numpy as np`; exports `sigma_n` L65 [`0.8Â·âˆšpool`, TN-independent] / `soft_cap` L70 / `net_boost` L92 [`soft_capÂ·sigma_per_die[tn]Â·âˆšpool`, TN-DEPENDENT via `PER_DIE` L26-30 = 0.806/0.800/0.781] / `p_success` L116 â€” **`soft_capÂ·sigma_n` and `net_boost` COINCIDE ONLY AT TN7 and diverge off it (repro pool=10/ns=0.7: TN7 both â‰ˆ1.652, TN6 net_boostâ‰ˆ1.664, TN8 net_boostâ‰ˆ1.613); must be stdlib-re-ported + de-duplicated + boost-form-decided on re-housing, Â§2.1a/Â§5.9**)
- Stubs to retire: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\sim\personal\contest.py`; `...\tests\sim_framework\contest.py`
- ID reservations: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\references\id_reservations.yaml` (block D ED next_free 1055 â†’ **allocate 1055-1074, next_free 1075** â€” 20 IDs, Consensus-wiring ED removed as PROMOTE-EXISTING, appeal resâ†’Î´Ïƒ conversion folded into the CR6/armature ED, Â§4.3)
- Module contract: `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\references\module_contracts.yaml` (social_contest block L425-447)
- New package to build (Stage 1): `C:\Github\ttrpg\.claude\worktrees\happy-shaw-da0f1d\sim\personal\contest\` (directory) â€” homes for all **nine** promoted modules incl. `policy.py`, `faction.py`, `narrative.py`

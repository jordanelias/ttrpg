# Attribute / Value Coherence Audit (2026-07-08)

**ED-IN-0029 · Lane IN · READ-ONLY · status: PROPOSED (docket UNRULED — Jordan picks).**

The **quantity-layer extension of the Key & Echo Armature** (ED-IN-0018): the armature unified the
seams; this audit covers the quantities those seams read and write — every named attribute,
aggregate, derived value, track, clock, pool, faction/settlement stat, and load-bearing constant,
across registry / glossary / params prose / design heads / module contracts / sim oracle / Godot
export. Jordan's mandate: *"we desperately lack unity, consistency and coherence with our explosion
of attributes and derived scores... developed in silos."* Silo-grown stat vocabularies are shape
divergence at the stat layer (armature §1; holonic doctrine ED-1083 §2).

**Nothing here ratifies on merge.** Per the ED-1094 exception clause, stated loudly: the docket
(`ed_options.md`) and the extension (`proposed_quantity_armature_extension.md`) are **held back**
from merge-ratification and wait for explicit rulings. Merging the audit PR lands evidence, not
decisions.

## Read in this order

1. `attribute_value_coherence_v1.md` — the verdict-first corpus report: headline stats, per-cluster
   coherence picture, the collision/synonym/stale/export-drift registers, the A17/A18 measured
   backlogs, and what each KNOWN-TRACKED open item gains.
2. `ed_options.md` — the **Jordan-rulable docket** (OPT-AV-1..18, each with a recommended default;
   lane-owned rows marked "feeds \<LANE\>, not ruled here").
3. `proposed_quantity_armature_extension.md` — the PROPOSED armature companion: the
   quantity-contract row (§1), 16 registry deltas (§2), conformance checks A17/A18 + the unbuilt
   prose-drift detector spec (§3), and the "Quantity/descriptor unification wave (IN)" slot (§4).
4. `finding_status.md` — tallies before/after the critic funnel, kind/calibration breakdowns,
   census stats, coverage notes.
5. `02_census/quantity_census.{yaml,md}` — the 88-row quantity census (the seeded §1 table).
6. `01_workings/` — 7 finder inventories (F-GODOT backfilled same-day after a workflow finder
   failure; its 10 findings are an uncritiqued addendum — see `finding_status.md` §5), 7 dossiers,
   7 critic passes.

## Headline

**88 quantities censused · 82 findings survive the critic funnel (18 P1 · 39 P2 · 25 P3) · 54
upheld · 17 softened · 3 sharpened · 1 overturned · 8 critic-sourced additions · 6 census verdicts
corrected · 18-item docket.** The per-quantity layer is mostly sound (46/88 COHERENT as filed,
~50 after corrections); the incoherence lives in the **carriers and the enforcement**: 4
incompatible attribute rosters, a 4-formula Knot Pool, an already-drifted Godot RESIST port,
stale-canonical clock registry, and a naming gate where **everything is warn-tier, the registry
test is dead, and `stat_deltas` accepts any string**. Measured backlogs: A17 stat-vocabulary
closure ~39%, A18 formula single-source ~28–31% (seeds; the checks re-derive). The recurring
disease is not bad rulings but **rulings whose sweeps never finished** (ED-PC-0005, PP-663,
ED-731, ED-830, ED-1050, LPS-2e...).

**The audit proposes; Jordan rules the docket.** Genuine design forks (roster, Piety-family name,
Standing range, values_master disposition, wave sequencing) are docket rows with defaults — none
are decided here, and lane-owned items (FI/FA/SC/SE/PC/MB/GO) are recorded as feeds per the
armature §5.8/5.9 precedent.

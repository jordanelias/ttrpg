# Attribute/Value Coherence Audit — Jordan-rulable docket

## Status: PROPOSED (docket UNRULED) · Lane: IN · Anchor: ED-IN-0029 · 2026-07-08

**Nothing below ratifies on merge (ED-1094 exception, called out loudly): every row is held back
for explicit ruling.** Each row states a recommended default; lane-owned rows are marked
"feeds \<LANE\>, not ruled here" per the armature §5.8/5.9 precedent. Severity = the finding(s)
the option closes.

| id | what | recommended default | sev | feeds | lane |
|---|---|---|---|---|---|
| **OPT-AV-1** | **Attribute roster ratification** — which roster is canonical: glossary 7 / registry 9 / either 10-table (C1-F1); Recall registration (C1-F2); the two fold directions (C1-F3/F4/M1). | Adopt the registry's **9-key structure with the naming corrections applied**: primary names flip to **Cognition** (ED-899 already live, named engine constants) and **Spirit** (zero live "Will" usage; both fold-target names Acuity/Will have zero consumers), **plus `attr.mind.recall` as a 10th key**; retire glossary Part-One formula tables; supersede canonical_registry.md's competing table. *(This reconciles the C1 dossier's self-contradicting U1-vs-U2/U3 set per the critic.)* | P1 | workplan v6 T1 **queue-13**; **ED-IN-0008** | Jordan (design call) |
| **OPT-AV-2** | **values_master.yaml disposition** — retire vs regenerate (quarantined ED-1084; generator dead; carries struck Combat Pool ×2, old Stamina, RS-era labels). | **Retire**: mark historical snapshot, keep for citation archaeology; do not regenerate until a typed extraction pipeline exists (regeneration today re-couples to dead tooling, C7-F5). Bundle the six secondary indices' disposition (OPT-AV-14). | P2 | 2026-07-01 decision-queue **item 17**; ED-1052 | IN |
| **OPT-AV-3** | **Adopt the descriptor_registry quantity extension** (`proposed_quantity_armature_extension.md`): quantity-contract row format (§1) + registry deltas (§2) — Recall, TS/TPS, Fort Level, WI/MW, FacilityTier/W_s, Face, Coherence, Political Pool, WR, the 8 missing derived_values, alias scope tags. | Adopt as PROPOSED companion to the armature; file deltas via the armature §10 extension process with Class-B PP-674 vetting; registry filing itself is bookkeeping, key *names* that embed design calls (roster, Political Pool) wait on their own rows. | P1–P2 | armature §3 analog; ED-IN-0018 lineage | IN |
| **OPT-AV-4** | **A17 stat-vocabulary closure check** — build `tools/` check that every stat named in Key payloads / contract state / params tables resolves to a registry key. | Build **report-only first** (armature §4 lifecycle); seeded backlog ~34/88 + 23/34 contract declarations; flip-on-zero-backlog only after OPT-AV-1 lands (the vocabulary must be ratified before absence is an error). | P1 (closes C7-F6 class) | armature §4 analog; CLAUDE.md §8 single-source invariant | IN |
| **OPT-AV-5** | **A18 formula single-source check + prose-drift detector** — one defining surface per formula, all other carriers cite it; requires the (unbuilt) `params/*.md` prose-formula parser (CLAUDE.md §5 gap). | Approve the spec (extension §3.2); build detector before check; report-only; seeded backlog ~25–27/88. Sequence **after** A17 (A18 requires citation-judgment; A17 is deterministic). | P1 (closes the C1-F5/F6, C3-F1 class) | ED-1052; extension §3 | IN |
| **OPT-AV-6** | **warn→block tier promotion** for attr/agg/fac/set keys in names_index.yaml + resurrect/replace the dead `tests/registry/test_descriptor_registry.py` + extend the drift lint to report (not block) on `aliases:` disagreements (C7-F3/U3). | Promote **only after** OPT-AV-1 ratifies and the A17 report-only backlog is swept to zero on the affected keys; alias-report channel can land immediately (report-only, no policy change). | P2 | ED-IN-0008; C1-F10; C7-F3/F4 | IN |
| **OPT-AV-7** | **The hygiene batch** — pure propagation of already-made rulings, no design calls: `params/core.md:4` stale PP-247 header; broken `params/board_game.md`/`params/factions.md` auto-indexes; dead registry test (with OPT-AV-6); `derived_stats_v30.md:515` struck-form-as-current + :589 missing ED-899 caveat; `threadwork_v30.md:968,1008` struck Mending pool; `params/core.md:158-165` regenerate Health/Stamina rows from derived_stats §4 + add Concentration row; glossary Health/Stamina rows; `campaign_modes.md:42-47` MS 72→60 + fix 3 nonexistent citations; TC strike + infill rename + ED-782→correct citation; clock_registry Torben 3→7; `name_collision_database.yaml:469` stale residual → mechanical_terms_index.md:196,1167; `settlement_layer_v30.md:43` citation fix; ED-869 footnote; combat_v30.md:307 + mass_battle_v30.md:313-314 wound-penalty sweep completion (co-signed PC/MB); factions_personal Intel backfill (co-signed FA). | Execute as one `[cleanup]` batch citing this audit's ED; each item cites the ruling it propagates; lane-owned file touches co-signed by owning lane per §4 convention. | P1–P3 | closes ~20 stale-register rows | IN + co-signs |
| **OPT-AV-8** | **Shaping-wave slot** — schedule "Quantity/descriptor unification wave (IN)" against armature waves 1–5 (extension §4). | Insert **after wave-1 (echo transport) and before any params-consuming wave**; worklist = OPT-AV-7 batch + registry filing (OPT-AV-3) + tier promotion (OPT-AV-6). | — | armature §6.3 | Jordan (sequencing) |
| **OPT-AV-9** | **Knot Pool formula ratification** — four live formulas (C3-F1); oracle runs Spirit×2+History(Rel). | Default: (Spirit×2)+History(Rel)+3, Bonds stays eligibility-gate only; **note ED-920's ledger scope covers only the fieldwork variant — widening it needs explicit acknowledgment**. Add pool regression test after (C3-F2). | P1 | ED-920 | **feeds FI, not ruled here** |
| **OPT-AV-10** | **Mandate dual-form marking** — linear personal view vs saturating canonical aggregate (C5-F1/C2-F1/C7-F8; ED-784 already blessed the dual view). | No fork to rule: **mark** `params/factions_personal.md:14` explicitly as `personal_mandate_view` (ED-784) citing the saturating canonical form; sweep params/bg/* carriers. | P1→propagation | ED-FA-0004 (params half) | **feeds FA, not ruled here** |
| **OPT-AV-11** | **CI starting-value provenance** — four non-agreeing assertions; no supported TTRPG value exists (C4-F3/F4). | Adopt MS's TTRPG-vs-BG split discipline: either author a cited TTRPG start or declare CI BG/Hybrid-only; fix campaign_modes=15 (unsupported). | P1 | FIX-03; ED-793-adjacent | **feeds FA, not ruled here** |
| **OPT-AV-12** | **Standing** — same-name range collision 0-10 vs 0-5 on the BG faction track + cross-scale homonym with the contest kernel (C4-F9 sharpened). | Rule the BG range (default: 0-10 per glossary, correct clock_registry) and rename or scope-tag one homonym half; add as 4th entry to name_collision_database (note existing :139 partial exemption). | P1 | name_collision_database | **feeds SC + FA, not ruled here** |
| **OPT-AV-13** | **Piety/Conviction/CT/CV/PT family** — 4 structurally different referents share the name; abbreviation ruled 3 ways (C7-F1, census C4 row). | Present-options-only per critic: ED-644 explicitly defers the rename pending cost-benefit; this audit adds the consolidated referent map + the names_index absence. **No default asserted beyond: add the missing registry key(s) regardless of which name wins.** | P1 | **ED-644, ED-1006, ED-1009, ED-919** | Jordan (held open by ledger) |
| **OPT-AV-14** | **Secondary-index disposition** — six indices with severed regeneration + seven more registries with zero tool consumers (C7-F5/M1). | Mark all point-in-time historical snapshots (values_master precedent); re-platform only those wanted as live A17/A18 signal (mechanical_terms_index is the best candidate). | P2 | decision-queue item 17 (bundle) | IN |
| **OPT-AV-15** | **RESIST re-export** — combat_config.gd carries pre-2026-06-30 values, 4/12 cells drifted (C6-M1); GAP_EXPOSURE/GAP_PREC_REF never carried (C6-M2). | Re-export from core.py now (the ED-1050 rule: fix canon, re-export — already satisfied on the oracle side); annotate the .gd until the port slice lands. | P1 | **ED-1050 residual** | **feeds PC/GO, not ruled here** |
| **OPT-AV-16** | **stat_deltas vocabulary validation** — Key substrate accepts any string as a stat name (C6-F1/C7-F6). | Add warn-tier check in `KeyLog._validate` mirroring the impact_vector AXES check (candidate invariant 9, key_substrate §2.3), vocabulary = registry union; promote with OPT-AV-6. | P1 | armature §4; OPT-AV-4 | IN |
| **OPT-AV-17** | **Exporter scope fence** — widen `tools/export_engine_params.py` to core.py's ~15 canonical constants, or document them as accepted hand-transcription risk; prune/mark the 7 dead CFG keys (C6-F2/F3). | Widen (small count, co-equal canon, docstring already claims the whole typed oracle); prune dead keys at next config.py pass matching the DAMAGE_SCALE precedent. | P2 | **ED-1052** | IN (PC co-sign) |
| **OPT-AV-18** | **Residual design forks surfaced, one line each**: Renown cap 10 vs uncapped + Shadow Renown relation (C4-F5); Fort Level province→settlement inheritance rule (C5-F6, default: settlement = province value); Garrison/Local-Economy/Public-Order §9 ratify-vs-[ASSUMPTION]-mark (C5-F2 — critic: the ratify default leans SE-owned); Discipline rename (C5-F4, default: display-scope "Faction Discipline"); Faction Political Pool name (C3-F4/M1 — default: **"Political Pool" per existing prior art**, not a third name); registry per-stat floors incl. Intel floor (C5-F3). | Rule individually; defaults as stated; none executed by this audit. | P1–P2 | ED-IN-0008; SE/MB/FA registries | **feeds SE / MB / FA / SC as marked, not ruled here** |

**Reading note:** OPT-AV-1, -8, -13 and the OPT-AV-18 sub-items are genuine design calls (rule
no forks — cardinal rule e). Everything else is propagation, bookkeeping, or tooling that only
*executes* rulings already made; it still waits for this docket because the audit is read-only.

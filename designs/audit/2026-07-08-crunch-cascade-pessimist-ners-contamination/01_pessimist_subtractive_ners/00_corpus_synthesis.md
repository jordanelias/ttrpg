> ⚠️ **CURRENCY NOTE — read `../00_workplan.md`'s "CURRENCY DISCLOSURE" section first.** Same-day
> commits landed on `main` after this synthesis was gathered directly invalidate two of its
> headline items: **ED-PC-0007 (PC action consolidation) was deferred by Jordan** (`464fa89`,
> "target is stale" — the PC lane's citation of live consolidation work is wrong), and **the MB
> `subunit_combat_pool` full-Command-per-subunit defect (§3 item #3) is already FIXED** (ED-MB-0004,
> `e6d8bd1`, partition-invariance renormalization). Re-verify every P1 below against current `main`
> before acting on it.

All highest-severity anchors verified against the working tree: TW's resolver imports only `roll_pool` (no sigma_leverage anywhere in `sim/thread`) and its local `_compute_degree` uses `net >= ob+3` versus the canonical `net >= 2*ob and net >= 3` (`dice_engine.py:115`); combat DID migrate (`combat_engine_v1/core.py:19: from sim.autoload import sigma_leverage`); the exporter imports only `config`, not `core.py`; SE's Develop pool is the bare `Cognition + Wealth-relevant History` (`settlement_layer_v30.md:427`); MB's dual stamina constants both exist. The lane reports are accurate. Here is the synthesis.

---

# Pessimist Subtractive NERS — Corpus Synthesis (crunch-cascade / cognitive-load / legibility pass, verdict-first)

## Status: READ-ONLY SYNTHESIS (2026-07-08) · Lane: IN · aggregates 9 lane critic-passes (PC/MB/SC/FA/SE/TW/DC/AN/XS); not a ratifiable design doc. [reclassified 2026-07-15, proposal-reconciliation pass]
Judged **as-if-built** throughout. No verdict rests on stub/unwired/unbuilt/unported status; build state is captured only as routing metadata. Unlike the sibling *action* audit, this pass ranges over **all flagged elements** — trackers, mechanics, rolling engines, and legibility gaps — not just player verbs.

---

## §0 · Verdict summary (lanes × subtractive verdict)

| Lane | KEEP | REFINE | DISTILL | MERGE | PRUNE | CUT | Total |
|---|---:|---:|---:|---:|---:|---:|---:|
| **PC** personal / scene combat | 4 | 10 | 1 | 2 | 1 | 0 | 18 |
| **MB** mass battle | 12 | 8 | 2 | 0 | 1 | 0 | 23 |
| **SC** social contest | 15 | 5 | 0 | 0 | 2 | 0 | 22 |
| **FA** faction / political | 17 | 7 | 0 | 0 | 0 | 0 | 24 |
| **SE** settlement / territory | 9 | 11 | 1 | 2 | 2 | 0 | 25 |
| **TW** threadwork | 15 | 2 | 1 | 1 | 0 | 0 | 19 |
| **DC** board game + dice/resolution core | 6 | 10 | 1 | 0 | 1 | 0 | 18 |
| **AN** articulation + NPC behaviour | 13 | 3 | 0 | 1 | 0 | 0 | 17 |
| **XS** cross-scale propagation | 10 | 11 | 0 | 1 | 0 | 0 | 22 |
| **Corpus** | **101** | **67** | **6** | **7** | **7** | **0** | **188** |

*MB evaluated 24 elements; the 189th (its #22, a doc-currency claim independently disproven via commit `9adf13c`) is a non-mechanism, dropped as N/A — so MB's six verdict counts sum to 23 scored. Every other lane's counts sum exactly to its element count. Corpus: 188 scored + 1 N/A = 189 elements.*

**Read of the shape:** 54% KEEP, 46% subtractive — but the subtractive near-half is **overwhelmingly legibility/spec repair, not deletion.** 67 of 87 subtractions (77%) are **REFINE** (the mechanism stays; a name, formula-currency, display contract, or engine-seating gets fixed); 13 are consolidation (6 DISTILL + 7 MERGE); only **7 are hard PRUNE and 0 are CUT** anywhere in the corpus. Every one of the 7 PRUNEs is dead-code removal (PC `seize`, DC's quarantined `values_master` rows), duplicate-coverage retirement (MB `LETHALITY_SCALE`, SC `contest_legacy_stub`, SE `Trade`), or lane-re-homing (SE `Grant/Revoke` → FA) — **not** "a mechanism that shouldn't exist." Run to exhaustion across the whole corpus, the pessimist lens found essentially **no fantasy-imposition to excise.** The dominant defect is not junk and — unlike the action audit's over-articulation finding — not even mainly duplicate packaging: it is **legibility-contract, currency, and engine-seating debt** sitting on top of mechanisms that already pass their own N/Ω/Q bar.

---

## §1 · The distilled core

Once every subtractive verdict is applied, Valoria's corpus loses almost no *substance* and gains one *invariant per axis*. Mechanically, what survives is the entire cross-scale leadership sim intact: the sigma-leverage combat substrate (Combat Pool, Health, Initiative/Poise, Contact) and its intents; the untouched mass-battle tracker lattice (Size, Power, Discipline, Morale, Army Morale, Command, Cohesion, sub-unit count — Army Morale standing as the corpus's model of legibility-through-abstraction); the full social-contest stack (Persuasion Track, Face/Standing, Concentration, the four-axis Adjudicator Armature, Doubt Marker, CR5, the four deliberative games); the constructive faction core (Mandate, Legitimacy/Popular Support, the capacity cluster, Stability with its bounded collapse loops, Cascade Fidelity, the grounded multi-root cascade for Crown/Church/Varfell); the settlement triad + governance verbs + victory-gate chain; the richest system in the corpus, threadwork (MS/RS, Coherence, Thread Sensitivity, Fatigue, three-axis Ob, the seven operations + Leap + Knots), the continuous σ-resolution engine that all five scales share, the articulation/NPC spine (Conviction, Resonant Style, Disposition, Scar, `significance()`, the Tier-2 trigger table, the universal Key subscription), and the entire cross-scale propagation architecture (Key, the single update rule, Aggregate-Up, Distribute-Down, both termination theorems). What is *removed or folded* is packaging and cruft: one dead ability channel, one duplicate damage-scale constant, one 256-line poorer contest stub, one bare income roll, one mis-homed provincial action, and a handful of quarantined/struck formula fossils. What is *repaired* is a set of one-per-axis invariants the corpus does not yet hold: **one canonical name per concept** (no `cohesion`/Coherence, `disp`/Disposition, Turmoil/Strain, Piety/Persuasion, MS/RS collisions), **one live formula per value** (no three-generation pool text, no `values_master` stragglers, no reversed wound rules), **one status line per doc**, **one rolling engine per resolution path** (the sigma-leverage substrate, with its migration and Instance-selection debts closed), and **a display contract for every number the engine consumes**. The single systematic finding, generalized across trackers, mechanics, engines and legibility gaps alike: the corpus **already meets its own N/Ω/Q bar on mechanism everywhere** — its debt is that those sound mechanisms are inconsistently *named, specified, seated, and surfaced*, never that they should not exist.

---

## §2 · Per-lane rollup

- **PC — sigma-leverage substrate sound, display contracts missing.** The lane is REFINE-dominant (10/18), not bloated: every mechanism that has been built (Combat Pool, Health, Initiative/Poise — the last confirmed clean at every mutation site) cleanly KEEPs, and the rolling engine is compliant with two residuals. The whole subtractive weight is *legibility-contract debt* — the wound-Ob shift is applied but never shown pre-commit; ~15 continuous fields are engine-only with no partition; Momentum has no σ_N-scaled engine translation (a naive port reimports the flat-shift trap); `disp` collides uncaught with NPC Disposition; nine jargon terms lack a player-facing gloss. The lone PRUNE (`seize`) is *more* dead than the dossier said (zero `lever='seize'` entries in the live `ABILITIES` dict), so its cleanup target correctly retargets from the already-clean code to the stale "live"-marked doc rows. Concentration was corrected from a robustness risk to a bounded design-intent question. No CUT.
- **MB — cleanest tracker set, dirtiest calibration.** All eight load-bearing trackers KEEP; the subtractive weight is 8 REFINE + 2 DISTILL (formation dice-table folds into the already-existing `ROLE_SPEC["ShieldWall"]` geometry/stance key; three coexisting pool-formula generations collapse to the one live ED-1013 form) against a single PRUNE. But the **rolling engine is NON-COMPLIANT on Robustness**: 15/20 gauge rows out of band, two stamina-penalty channels live simultaneously by default (a *known, deliberately-deferred* divergence per ED-1017), and `subunit_combat_pool` applies full Command to every sub-unit with no multi-front cap (PP-504). This pass raised those two to P1 for consistency with the engine block, and overturned the false "no UI spec exists" premise (Part 8 exists and is self-audited under F-38/F-39).
- **SC — near-whole, one severe already-accepted wiring gap.** 15/22 KEEP; the Adjudicator Armature routes correctly through the σ_N-scaled channel (Instance A PASS). The one genuinely severe item is the P1 PRUNE of `contest_legacy_stub.py` — campaign scenes dispatch through a 256-line stub instead of the 13-module kernel — but this is *already Jordan-accepted* (ED-SC-0006/0011), so its "NEW" tag was corrected to KNOWN-TRACKED. A recurring flat-"+1D" leverage idiom (CR4, Parliamentary Vote, KU-1's +9D stack) is real but modest (~1.84×, not 2.4×) and one proposed remedy was contraindicated. One clean overturn: Face/Standing REFINE→KEEP (its basis was a stale in-code comment; ED-1056 resolved it).
- **FA — over-articulated in documentation, not mechanism.** 17/24 KEEP after the critic overturned the Multi-root Cascade PRUNE→KEEP (its basis — "defaults to single-root, deferred to Stage 10" — was exactly the build-state reasoning the charter forbids; the working tree names three grounded multi-root factions). Zero PRUNE/CUT survive. The 7 REFINEs cluster into resolver-migration debt (Suppress still runs "Mandate pool vs flat Ob"; Domain Action has no home doc), citation/currency drift (Turmoil/Strain broken xref; two CANONICAL-headed docs carry materially conflicting Mass Seizure formulas with zero adjudicating ED and no CURRENT.md victory row), and two overstated Ω-d tensions walked back P2→P3. The faction count remains unreconciled (P1, ED-FA-0001).
- **SE — sound trackers, one wrong-seated engine, a self-contradicting doc pair.** 9 KEEP; 5 verdicts merely re-affirm already-executed ED-SE-0005 work (1 DISTILL, 2 MERGE, 2 PRUNE — Trade folded, Grant/Revoke re-homed to FA). The 11 REFINEs are all definition/citation repairs. The one structural finding is Element 13: the ratified §3.2 Governance check sits on **Instance A with a bare `Cognition + Wealth-relevant History` pool** (no ×2, no floor — barer than every personal sibling), inside the victory-gate chain, importing the ED-865 degeneracy Instance B exists to prevent; its tag was corrected NEW→KNOWN-UNTRACKED (a previously-orphaned ER-6 hypothesis + an unledgered 2026-05-28 finding both flagged it). Two doc-pair contradictions (stacking rule; Accord cap) sit in the *same sentence* of `peninsular_strain_v30.md:196` — one ruling closes both (P1, ED-SE-0002).
- **TW — richest system, cleanest on mechanism merit, worst-seated engine.** 15/19 KEEP; every individual tracker (MS/RS, Coherence, TS, Fatigue, three-axis Ob, Saturation) survives, and the cognitive-load "complaint" MERGEs into the MS/RS naming finding it actually is. Two REFINEs (Knot Strain collision; Amendment jargon) and one strengthened DISTILL (the "RS x3" fossil, now shown to be a copy-paste from struck PP-226, four days after the ×3 was killed). But the **Thread Operations Resolver is NON-COMPLIANT**: verified against the working tree, it imports only `roll_pool` (never `sigma_leverage`, which combat and contest *did* migrate to) and reimplements a divergent `net ≥ ob+3` Overwhelming rule — one of ≥4 mutually-inconsistent corpus variants — across all seven ops + collective + knots.
- **DC — mechanisms earn their place; the apparatus around them bleeds.** Only 6 KEEP against 10 REFINE + 1 DISTILL + 1 PRUNE. The core σ-resolution engine is **NON-COMPLIANT on Robustness** (Momentum's flat +1/+2 net conversion is the flat-shift trap, Δz ∝ 1/√Pool, hottest at the 5D floor, same class as the open ED-875; faction-scale bare pools sit on unvalidated Instance A). Combat Pool is simultaneously the healthiest mechanism (triple-cross-checked KEEP) and the worst-served by its surroundings: a broken index, a quarantined registry that still misstates it (PRUNE), a live in-file sentence that still misstates it despite a remediation *claiming* to have fixed it (P1), and an export pipeline confirmed to import only `config`, never `core.py`, so it cannot emit Combat Pool at all (P1, ED-1050 relapse risk for Godot).
- **AN — five orthogonal trackers, one real design gap.** 13/17 KEEP; three KEEPs came back *strengthened* (the Companion panel shows five continuously-visible fields, not two; "Thread/Calamity silence" is narrower once trigger #8 is counted; "RS never surfaced pre-contest" is directly contradicted by the Appraise-reveal text). The one genuine design-merit defect is `unarticulated_weight`'s broken overflow promise — re-traced through the actual pseudocode, its starvation-prevention guarantee is undeliverable for an NPC whose Key stream never matches the 10-row trigger table (REFINE: add an overflow branch; MERGE its control-flow twin; cite ED-IN-0019 alone, as ED-IN-0012 is superseded). No engine (articulation is non-rolling).
- **XS — the architecture holds; the defects are exactly at its named seams.** 10 KEEP, 11 REFINE, 1 MERGE; every primitive (Key, single update rule, Aggregate-Up, Distribute-Down, both termination theorems, the caps) is load-bearing and non-duplicative under attack — zero CUT/PRUNE/DISTILL survive. It is non-rolling (`compute_observers` returns a bare `set()`, no draw), so it carries no NERS-engine verdict, but it holds the corpus's densest P1 cluster: D.6 double-count (the convergence claim depends on unproven disjointness), the three-way RNG-model collision (gates every rolling engine's replay determinism), and the ORD-3 self-contradiction. This pass raised two severities (AU-3.1 and RNG-collision to P1) and pulled in the day-old `key_echo_armature_v1.md` §5 Ruling Log that both the source findings and the dossier had missed — Jordan's own rulings confirm these items are "open in effect," not closed.

---

## §3 · Ranked docket highlights (worst-first; engine-compliance failures lead because they cascade)

Unlike the sibling action audit, this corpus pass has real P1s, and the worst of them are **rolling-engine Robustness failures** — each cascades across every mechanism the engine seats, so they outrank any single subtractive verdict (see §5).

1. **TW Thread Operations Resolver — NON-COMPLIANT (R + S, P1).** The entire thread path runs on the legacy discrete engine (verified: imports only `roll_pool`) while combat and contest migrated to sigma-leverage, and reimplements a divergent `net ≥ ob+3` degree rule across all seven ops + collective + knots — one of ≥4 inconsistent corpus Overwhelming formulas. **Highest downstream value in the corpus:** one migration + one single-sourced degree function retires the whole legacy path and reconciles every variant.
2. **DC Continuous σ-engine — R FAIL + exporter gap (P1).** Momentum's flat net-conversion reimports the flat-shift trap (Δz ∝ 1/√Pool, same class as the open ED-875), and `export_engine_params.py` imports only `config` (never `core.py`), so the typed pipeline cannot emit Combat Pool — reopening the exact ED-1050 hand-transcription failure mode for Godot. Fixes reuse the ED-875 σ_N pattern and sharpen ED-1052 into one slice.
3. **MB Mass Battle Engagement Pool — R FAIL (P1).** 15/20 gauge rows out of band; two stamina-penalty channels live at once by default (ED-1017 known-and-punted); `subunit_combat_pool` applies full Command per sub-unit uncapped across simultaneous fronts (PP-504). Retires the open `HANDOFF_MB.md:118-121` question and a fatigue-pathway reconciliation.
4. **FA resolver-migration + canon conflict (R + S, P1/P2).** Suppress still runs the pre-ED-865 "stat pool vs flat Ob" degeneracy; Domain Action is `doc: null` (nothing to check consistency against); two still-CANONICAL-headed docs carry materially different Mass Seizure formulas with **zero adjudicating ED and no CURRENT.md victory-conditions row at all.** Promotes `domain_action_resolver_spec.md` to canonical and forces one Seizure spec.
5. **SE §3.2 Governance check on the wrong Instance (P1) + the twin one-sentence doc contradictions (P1).** A bare-stat pool inside the victory-gate chain imports the ED-865 degeneracy (finally gives orphaned ER-6 an ED number); separately, the stacking rule and the Accord cap contradict their sibling docs *within the same sentence* — one ruling closes both (ED-SE-0002, open).
6. **XS propagation P1 cluster.** D.6 non-disjointness would break the convergence story even with a contractive `decay()`; the three incompatible RNG contracts gate every rolling engine's replay/checksum parity corpus-wide; `compute_observers` returns a bare `set()`, directly contradicting the spec's own ORD-2. All now tracked through `key_echo_armature_v1.md` §5's PR-2 T0 gates.
7. **SC `contest_legacy_stub` reachability — PRUNE (P1, already Jordan-accepted).** Campaign contest scenes resolve through a strictly-poorer 256-line stub instead of the 13-module kernel. The single largest duplicate-coverage retirement in the corpus; ED-SC-0006 already scopes it.
8. **Corpus-wide cognitive-load ceiling breach (P1).** SC alone demands ~13 per-decision consults, TW ~14, FA 9–10 trackers, against a ratified 3–4 immersion ceiling — with a human-playtest already funded (SC P3-lite). The shared remediation is tiered/progressive disclosure, not mechanism removal.
9. **FA faction count unreconciled (P1, ED-FA-0001, open).** 4–8 factions across four docs, no default — blocks all faction-scope binding; resolving it *unblocks* rather than retires.
10. **Corpus naming-collision + status-line + currency sweep (P1/P2).** MS/RS (ED-WR-0002), `cohesion`/Coherence, `disp`/Disposition, Turmoil/Strain, Piety/Persuasion, plus status-line self-contradictions (FA, SE, AN) and struck-formula fossils (DC `values_master`, PC) — one editorial pass across `names_index.yaml`/`glossary.md` + `tools/currency_consistency_check.py` coverage.

---

## §4 · Complementarity ledger (what each subtractive verdict retires downstream)

Every subtractive row names downstream work it removes or shrinks — the charter's non-negotiable for a scope cut vs an ordinary finding. Rolled up by downstream target:

| Downstream target | Retired / shrunk by | Effect |
|---|---|---|
| **Thread resolution path (7 ops + collective + knots) + ≥4 corpus Overwhelming variants** | TW engine (NON-COMPLIANT), TW "RS x3" fossil (D) | one sigma-leverage migration + one single-sourced degree function retires the legacy path and reconciles all variants |
| **ED-SC-0006 — `contest_legacy_stub` retirement** | SC #13 (P) | retires a 256-line duplicate stub; routes campaign dispatch to the 13-module kernel |
| **module_contracts `settlement_economy` retirement flag** | SE Trade (P) | closes the standing retirement recommendation; folds into Guild-contract Port/City |
| **C-FA-12 / `domain_actions` doc:null closure** | SE Grant/Revoke (P), FA Domain Action (R) | single-owner dedup; promotes `domain_action_resolver_spec.md` to canonical doc |
| **KU-1 global stacking-cap docket (P0 spec-reconciliation)** | SC Recall/Prep (R) | caps the +9D stack; 4× cross-audit corroborated |
| **ED-SE-0002 — one stacking + cap rule** | SE #14 + #25 (R) | one Jordan ruling closes two contradictions living in the same sentence |
| **OPT-16 governance staging + ER-6 gets an ED number** | SE Governance check (R), SE dashboard (R) | reclassifies §3.2 onto Instance B; formalizes the orphaned ER-6 hypothesis |
| **ED-875 σ_N-scaled leverage remediation pattern** | DC Momentum (R), MB gauge overshoot (R) | retires the flat-shift-trap defect class wherever a flat +X bonus scales as 1/√Pool |
| **ED-1052 exporter slice (`core.py` coverage)** | DC `export_engine_params.py` (R) | the typed pipeline can finally emit Combat Pool — guards against an ED-1050 relapse |
| **ED-1084 struck-formula sweep / decision-queue item 17** | DC `values_master` rows (P), PC struck formulas ×2 (M), DC `core.md:252` (R) | one relocation/CI-fence task; deletes the last stale Combat Pool statements |
| **ED-WR-0002 — MS/RS execution** | TW MS/RS (R-adjacent), TW cognitive-load (M), MB `cohesion`/Coherence (R) | one canonical name (Mending Stability / Formation Integrity) corpus-wide |
| **`names_index.yaml` / `glossary.md` collision registry** | PC `disp` (R), TW Knot Strain (R), SC Piety/Persuasion (R), FA Turmoil/Strain (R) | one naming-collision sweep populates the disambiguation table |
| **Accord §2.8 presentation-template extension (the display-contract deliverable)** | FA formula-visibility ×4 (KEEP-folds), SE dashboard (R), PC wound-Ob display (R), MB Command readout (R) | extends the one lane with real UI coverage across every hidden resolver input |
| **`key_echo_armature_v1.md` §5 PR-2 T0 gates** | XS D.6 (R), `decay()` (R), ORD-3 (R), RNG-collision (R) | the propagation open-flags close through the ratified Ruling Log rather than stale decision-queue numbering |
| **ED-IN-0019 overflow clause** | AN `unarticulated_weight` (R) + its control-flow twin (M) | one repair closes the starvation-prevention gap; drops the superseded ED-IN-0012 citation |
| **ED-IN-0004 / ED-IN-0014 Key hooks** | AN battle-conclusion accuracy bug, era-transition UNKEYED (KEEP, P1) | articulation gains the missing `scene.battle_concluded` + era-transition Keys |
| **`tools/currency_consistency_check.py` + CURRENT.md status-line coverage** | AN #17 (R), SE #24 (R), FA #22 (KEEP), DC index/quasi-binomial (D) | one editorial pass across status-line self-contradictions + stale banners + the broken BG index |

**Discipline audit:** all 7 PRUNEs (and the 0 CUTs) name concrete downstream retirements; no subtractive row degrades to an ordinary finding. Critic overturns correctly *withdrew* their scope-cut credit: FA Multi-root Cascade (PRUNE→KEEP — no scope-cut survives; the remedy is to *schedule* Crown/Church/Varfell activation, not delete the mechanism), SC Face/Standing (REFINE→KEEP — only a stale-comment fix remains), and PC `seize` (cleanup retargeted from already-clean code to the stale doc rows). MB #22 (a doc-currency claim) was disproven outright via `git` and dropped from the tally.

---

## §5 · Rolling-engine NERS rollup (N / R / S / E) — engine-compliance failures ranked above single-mechanism verdicts

Six lanes seat a rolling resolution engine (PC, MB, SC, FA, TW, DC). This section rolls up each engine's N/R/S/E verdict and flags any that **fail R (Robust) or E (Elegant) as the section's highest-severity items**, because an engine-compliance failure cascades across every mechanism the engine resolves — strictly further than one subtractive verdict on one mechanism. Ordered worst-first.

| Lane | Rolling engine (Instance) | N | R | S | E | Overall |
|---|---|:--:|:--:|:--:|:--:|---|
| **TW** | Thread Operations Resolver (A by profile; runs on **legacy discrete**) | PASS | **FAIL** | **FAIL** | degraded | **NON-COMPLIANT** |
| **FA** | Faction resolvers (B primary; C declaration-gate) | PASS | **FAIL** | **FAIL** | mixed | **NON-COMPLIANT** |
| **MB** | Mass Battle Engagement Pool (A) | PASS | **FAIL** | PASS · legib. flag | PASS | **NON-COMPLIANT** |
| **DC** | Continuous σ-engine / Decision E (A) + legacy d10 twin | PASS | **FAIL** | PASS (1e-9 parity) | MARGINAL | **NON-COMPLIANT** |
| **SC** | Social Contest / Adjudicator Armature (A) | PASS | PASS · flagged idiom | PASS | PASS · P-i UI-gap | COMPLIANT (flagged) |
| **PC** | Combat sigma-leverage (A) | PASS | COND-PASS | PASS | COND-PASS | COMPLIANT (2 residuals) |
| *(cross-listed)* | **SE** §3.2 Governance check (A; belongs on **B**) | PASS | **FAIL** | — | — | **NON-COMPLIANT** |

*SE is outside the six named rolling-engine lanes, but its §3.2 Governance check is a genuine NON-COMPLIANT rolling-engine verdict (verified: bare `Cognition + Wealth-relevant History` pool, `settlement_layer_v30.md:427`) and shares the R-failure genus below, so it is cross-listed rather than omitted.*

**Highest-severity — the four (five with SE) engines that fail R (TW and FA also fail S):**

- **TW — R FAIL + S FAIL.** The whole thread resolution path was never migrated to sigma-leverage (verified: `operations.py` imports only `roll_pool`; nothing in `sim/thread/` touches `sigma_leverage`), so a healthy-pool, setup-axis system (8D–27D) runs on the legacy discrete engine — a Lesson-3 violation the corpus's own resolution-diagnostic skill had assumed away as "likely compliant" without checking. Its local `net ≥ ob+3` Overwhelming rule (reimplemented identically across all seven ops + collective + knots) diverges from the canonical `net ≥ 2·Ob ∧ net ≥ 3` (`dice_engine.py:115`) — an S/consistency failure and one of ≥4 mutually-inconsistent corpus variants. `form_knot`'s 2D pool separately belongs on Instance B.
- **FA — R FAIL + S FAIL.** Suppress still resolves "Mandate pool vs `floor(Mandate/2)+1` flat Ob" — the pre-ED-865 bare-stat degeneracy Instance B exists to kill (R). Domain Action is `doc: null`, so there is no governing spec to check consistency against (S). (Stability §1.4 and the Stability-Triggers ladder Instance-B PASS; the CI Mass Seizure *declaration* gate is a deliberate Instance-C PASS — but its seizure pool sits at 5D on the ER-2 continuity boundary with no source confirming the correction is applied to that one-shot irreversible action.)
- **MB — R FAIL.** 15/20 balance-gauge rows out of band (ED-MB-0003); two stamina-penalty channels live simultaneously by default (both constants verified in `config.py`; ED-1017 shows the divergence was known and deliberately deferred on 2026-06-17); `subunit_combat_pool` applies full Command to every sub-unit across simultaneous fronts with no cap (PP-504). S passes with a legibility flag (three coexisting formula generations in prose); E passes.
- **DC — R FAIL, E MARGINAL.** Momentum's flat +1/+2 net conversion is the textbook flat-shift trap (Δz ∝ 1/√Pool, hottest at the 5D pool floor — same class as the open ED-875, Gate G8); faction-scale bare 1–7D pools sit on unvalidated Instance A when the bare-stat/pivotal profile calls for the ED-874-proven Instance B. S passes (discrete/continuous parity to 1e-9); E is a marginal pass — the continuity correction landed, but `core.md`'s discrete-first doc ordering and `combat_engine_v1`'s under-documented fixed `DECISIVE_OB=3` remain elegance/legibility costs.
- **SE (cross-listed) — R FAIL.** The §3.2 Governance check is misclassified onto Instance A with a pool barer than every personal-scale sibling (no ×2, no floor), importing the ED-865 degeneracy — and it sits directly inside the universal victory-gate chain.

**Clear of R and E (the two correctly-instantiated engines):**

- **SC — COMPLIANT (flagged).** The Adjudicator Armature routes through the σ_N-scaled `net_boost` channel (M_MAX=1.5 `tanh` soft-cap), passing R and S. Two flags, neither a hard failure: a recurring flat-"+1D" leverage idiom (CR4, Parliamentary Vote, KU-1) that is real but modest (~1.84×) and in one case has a contraindicated remedy, and a P-i UI-gap (genre/stasis is a live but unshown resolver input).
- **PC — COMPLIANT (two residuals).** Leverage uniformity holds by construction (Δσ before σ_N-scaling), the continuity correction is present, and every Initiative/Poise mutation site was verified to route through its clamp. Two residuals keep R and E at conditional: the `grip_position` fix is a Phase-5 "true finding" (a deliberate fix with no stated general invariant), and the wound-Ob channels lack a pre-commit display contract (cheaply fixable via the already-existing, currently-unused `SL.eff_ob` primitive).

**Corpus-level engine read.** The sigma-leverage architecture itself is **not** the defect — it passes cleanly everywhere it is correctly instantiated (PC combat, SC's armature, DC's and MB's core math where actually used). Every R failure is one of three recurring *seating* errors: **(1) un-migrated engine** — TW's entire path still on legacy discrete, FA's Suppress still pre-ED-865; **(2) flat-leverage reimport** — DC Momentum, MB's overshoot, and SC/FA's flagged +1D bonuses, all reintroducing the exact `1/√Pool` non-uniformity the engine exists to kill, hottest at the 5D floor; **(3) wrong Instance** — SE's governance check and DC's faction bare-pools on unvalidated Instance A where the ED-874 Instance-B precedent applies. That makes the corpus-wide engine finding a **migration-and-seating discipline gap, not an architecture defect** — and because it recurs across four (five) engines and cascades to every mechanism each seats, it is the single most consequential class of finding in this audit, above any individual subtractive verdict in §§3–4.

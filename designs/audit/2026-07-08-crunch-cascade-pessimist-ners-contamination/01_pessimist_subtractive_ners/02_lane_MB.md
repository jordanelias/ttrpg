# Pessimist Subtractive NERS — Mass Battle (MB)

> ⚠️ **CURRENCY NOTE:** `e6d8bd1` (#102, same day) **shipped ED-MB-0004**, a partition-invariance
> fix directly resolving this report's `subunit_combat_pool` full-Command-per-subunit finding
> (renormalization via `core/exchange.py:_pair_engaged_troops` + `orchestration.py:
> _convergence_scale`). Wherever this report cites that as an open P1, it is now FIXED on
> current `main` — do not re-file it. See `../00_workplan.md`'s disclosure for detail (also
> ED-MB-0005's DG-2 yield mechanic, not reflected below).

I performed independent verification against the working tree (design docs, `tests/sim/mass_battle/*.py`, `canon/editorial_ledger.jsonl`, `handoffs/HANDOFF_MB.md`, `CURRENT.md`, `designs/ui/*`, git log) before rendering a verdict on each row. Two corrections of substance surfaced; details are in the disposition column and footnotes.

# Pessimist Subtractive NERS — Mass Battle Lane (post-critic, FINAL)

Judged **as-if-built** throughout. Verified independently against the working tree at `/home/user/ttrpg` — every citation below was re-read from disk, not trusted from the dossier or from memory.

## Per-element table

| # | Element | N/Ω/Q reasoning | Subtractive verdict | NERS engine verdict | Severity | Calibration tag | Downstream retirement | Critic disposition |
|---|---|---|---|---|---|---|---|---|
| 1 | **Size** (`floor(TroopCount/block_size)`, dual display "4,428/5,000 (Size 4)") | N: attrition is the single most load-bearing Renaissance-battle dynamic. Ω-a: feeds territory/faction outcome. Q: verified verbatim at `mass_battle_v30.md:88` ("Player sees: 'Heavy Infantry — 4,428 / 5,000 (Size 4)'") — dual display is genuinely legible without hiding precision. | **KEEP** | N/A (health pool, roll input) | — | KNOWN-TRACKED (ED-694) | — | **SURVIVED** — citation confirmed exact. |
| 2 | **Power** (1–7 tier-set, Levy→Peerless) | N: equipment/training quality is real and load-bearing. Ω-a: Military stat caps Power ceiling (verified `mass_battle_v30.md:153-165`, table Military 1-7 → Max Power). Q: tier names one-read legible. | **KEEP** | N/A (roll input) | — | KNOWN-TRACKED | — | **SURVIVED** — cross-scale cap table confirmed. |
| 3 | **Discipline** (1–7 per-subunit, deterministic degrade) | N: cohesion-under-stress is central to rout/hold dynamics. Verified `mass_battle_v30.md:172-183`: "DETERMINISTIC" two-condition degrade, tiered penalty table (5-7 none, 3-4 -1D, 1-2 -2D, 0 broken) — genuinely one read. Ω-a/c: feeds pool + formation-hold + shape-drift, one concept three consequences, not conflation. | **KEEP** | N/A (deterministic, no draw) | — | KNOWN-TRACKED | — | **SURVIVED.** |
| 4 | **Morale** (1–7 per-subunit, continuous, ED-1024) | N: rout is the defining historical battle event. Verified `mass_battle_v30.md:193-216`: 8 named degradation triggers, several genuinely autonomous (ally routs, general down) — Ω-c holds. Continuous (not rounded) representation avoids premature cliffs. | **KEEP** | N/A (threshold tracker; the Shaken-triggered Command Ob-2 check is a separate roll, confirmed at `mass_battle_v30.md:869`) | — | KNOWN-TRACKED | — | **SURVIVED.** |
| 5 | **Army Morale** (0–7 derived composite) | N: necessary abstraction given #4's granularity. Verified `mass_battle_v30.md:867-869` verbatim: named-threshold table (6+ Resolute…0 Routed) exactly as dossier states. Ω-d: a summary stat, can't be dominant. | **KEEP** — model example other trackers should imitate | N/A | — | KNOWN-TRACKED | — | **SURVIVED**, citation exact. |
| 6 | **Command** (`clamp(round((2·Cha+Cog)/3),1,7)`) | N: "generalship dominates" is an explicit named design axiom, verified verbatim at `mass_battle_v30.md:36-38`. Ω-d: re-derived independently — Command is fixed for the duration of a battle (only a Stage-1/2 incapacitation *halves* it, `mass_battle_v30.md:288`; nothing raises it mid-battle), so the "near-certain outcome" is a chargen-time build cost (Cha/Cog opportunity cost against every *other* scale's stats), not an in-the-moment free lever. This is a legitimate Ω-d pass, not a rationalization. | **KEEP** (tracker itself); fan-out at #10 | Leverage input — see engine block | — | KNOWN-TRACKED | — | **SURVIVED** — re-derived the Ω-d argument myself rather than accepting it on faith; it holds. |
| 7 | **Cohesion** (`hp/hp_max`, `hierarchy/units.py:1572`, ED-1013) | N: smooth 2×Command→Command degradation beats a discrete Size-tier cliff. Verified the formula at `units.py:1572` and confirmed it **is** design-doc-named at `mass_battle_v30.md:30` ("cohesion = current Size ÷ max Size (hp/hp_max)") — the producer's "engine-internal only" claim was wrong; dossier's correction holds. | **KEEP** | N/A (feeds the roll) | — | KNOWN-TRACKED (critic-corrected as doc-named) | — | **SURVIVED**, correction re-confirmed. |
| 8 | **Stamina** — two unreconciled parallel channels: atom-level scalar (`atom.stamina`, drained by `orchestration.py:1323` `STAMINA_DRAIN_PER_CONTACT_CELL=1`, feeding `_stamina_pool_penalty` in `core/exchange.py:15-22` — confirmed **effectively binary** since `STAMINA_POOL_THRESHOLDS=[(1,0)]`, i.e. only stamina==0 penalizes) **and** per-column grid stamina (`_ColBlock.stamina`, `percell.py:18`, drained by `update_stamina`/`PC_STAMINA_DRAIN=12` at `orchestration.py:1410-1411`, feeding `_fatigue_sigma` at `orchestration.py:819-821`, `PC_STAM_SIGMA=1.5`) | N: fatigue-under-engagement is real and load-bearing (du Picq cited, `percell.py:135`). Q-smooth **fails**: I confirmed both channels are **simultaneously live by default** — `PER_CELL` defaults ON (`config.py:94`) *and* the atom-level drain at `orchestration.py:1303-1323` is **unconditional**, not PER_CELL-gated — so in the shipped default configuration every tick pays a stamina penalty twice, through two independently-initialized, independently-scaled, never-synced variables. **New evidence beyond the dossier:** `canon/editorial_ledger.jsonl` ED-1017 shows the divergence was *known*, not accidental — its own text reads "Deliberately did NOT change `_fatigue_sigma`: that per-column fatigue channel is already sub-unit-scoped… so an atom parameter would be unused apparatus" — i.e. the authors considered unifying the two channels and punted, on 2026-06-17, three weeks before this audit. That makes reconciliation overdue, not merely desirable. | **REFINE** — reconcile to one fatigue-penalty pathway (dossier's own remediation block already treats this as the #1 priority) | Instance-A input, P-iii-adjacent | **P1 (raised from P2)** | **NEW** — confirmed, and strengthened | Retires/merges into a single fatigue pathway; a strong root-cause candidate for #19's overshoot | **SURVIVED, STRENGTHENED, severity corrected P2→P1.** The dossier's own engine-verdict remediation list already ranks this as the #1 P1 fix — the per-row severity tag (P2) contradicted the dossier's own engine block. Fixed for internal consistency, and backed with the ED-1017 "known-and-punted" evidence the dossier didn't have. |
| 9 | **Sub-unit count** (1–11 videogame / TTRPG cap 3) | N: videogame-scale armies legitimately need more sub-formations. Verified `engine.py:99`: `SUBUNIT_CAP = 11 # ED-1090, superseding TTRPG hard cap 3`. Ω-a: cross-scale. | **KEEP** | N/A (composition limit) | — | KNOWN-TRACKED (ED-1090) | — | **SURVIVED.** |
| 10 | **Command's 4+ multiplicative downstream fan-out** | N: unified command authority cascading through pool/Discipline/Morale/tactics/span-of-control is exactly the named axiom (`mass_battle_v30.md:36`, `:268-272`). Re-derived: is 5 effects on 1 stat *inherently* less legible than 5 effects on 5 stats, or is it actually *more* elegant (one stat = one coherent story, "the general is the battle") with the cost paid entirely in presentation? I side with the dossier: the coupling is intentional and arguably elegant; only the player-facing readout is missing. | **REFINE** — consolidate the fan-out into one legible causal readout, not a mechanism change | See engine block | P2 | KNOWN-TRACKED (source itself calls this a "legibility risk," not a bug) | UI-consolidation task (GO/WR lane) | **SURVIVED** — attempted to argue this is a hidden Ω-d violation needing a mechanical fix, not just UI; the "generalship dominates" axiom is explicit, named, and paid for at chargen (see #6), so REFINE (presentation-only) is correctly scoped, not too lenient. |
| 11 | **`subunit_combat_pool` applies FULL Command to every sub-unit independently** (PP-504, `core/exchange.py:71,73`) | N: "the general's attention doesn't literally divide" is a defensible unified-command model. Verified PP-504 verbatim at `mass_battle_v30.md:285`. Verified the live exposure question is genuinely open: `HANDOFF_MB.md:118-121` (ED-MB-0003, 2026-07-05, three days before this audit) calls it "a new, deliberately undecided finding," explicitly not silently tuned. The base rule is ratified; the multi-front spatial-separation exposure is not. | **REFINE** — ratify PP-504 as-is; force a Jordan ruling + likely per-front decay/cap on the exposure case | Instance-A exposure — leverage applied N times per turn, N=simultaneous fronts, uncapped | **P1 (raised from P2)** | KNOWN-UNTRACKED (flagged in handoff, no ED yet) | Directly retires the open `HANDOFF_MB.md:118-121` question into a scoped ruling+repair task | **SURVIVED, severity corrected P2→P1.** Same internal-consistency fix as #8 — the dossier's own engine block already lists this as a P1 remediation item, and the HANDOFF text frames it as a live, undecided blocker on further gauge calibration, not a P2 backlog item. |
| 12 | **Three pool-formula generations coexist textually** (`mass_battle_v30.md:28-30, 443` — legacy PP-233, ED-899, ED-1013) | N: only ED-1013's form is live; the other two are legacy prose, confirmed verbatim at the cited lines. Q-elegant **fails**: a reader must trace three layered notes to find the live rule even though the doc demotes the first two. This is exactly the failure mode CLAUDE.md itself names corpus-wide ("Versioning ≠ currency… A filename or in-file version cannot tell you what is current") — this finding isn't a one-off nitpick, it's an instance of a documented, recognized repo-wide hazard. | **DISTILL** — collapse to the single live ED-1013 statement | Instance-A; P-i doc-fidelity defect, not live-math | P2 | KNOWN-TRACKED | One-section edit to `mass_battle_v30.md` §A.1/§A.7 | **SURVIVED, STRENGTHENED** — tied directly to CLAUDE.md §4's own explicit "Versioning ≠ currency" warning, which independently corroborates this is a real, recognized-pattern risk, not audit-manufactured. |
| 13 | **Formation dice-modifier table** (Shield Wall/Wedge/Skirmish/Column) — no live consumer | N: formation-matchup tactics are well-grounded. Verified `config.py:60-62`: SHAPE_OFF_MOD/SHAPE_DEF_MOD retired 2026-06-02 verbatim ("formations grant NO flat per-shape bonuses"). Verified the replacement mechanism is *even more direct* than the dossier claimed: `config.py:182` — `ROLE_SPEC["ShieldWall"] = {"shape": "Line", "instructions": ("brace","hold")}` — there is a **literal `ShieldWall` key** already mapped to geometry+instruction, plus `"Skirmish": {"shape":"GappedLine","instructions":("loose","harass")}` at line 186, matching the dossier's claim exactly. ED-909 (`editorial_ledger.jsonl:165`) confirms the mapping was explicitly parked, not silently dropped. | **DISTILL** — fold into stance/geometry, no lost decision | N/A (dice-mod table itself was never a live modifier) | P1 | KNOWN-TRACKED (ED-909 parked) | Closes the ED-909 follow-up outright | **SURVIVED, STRENGTHENED** — found stronger evidence than the dossier cited: `ROLE_SPEC` literally contains a `"ShieldWall"` key already performing this exact job. |
| 14 | **`threadwork_check` phase-boundary hook** (stub) | N: Thread↔mass-battle cross-scale handoff is core substrate ontology. Verified `mass_battle_integration_v30.md:265` verbatim. Stub-ness is explicitly not a verdict input per the charter's cardinal rule. | **KEEP** — pure wiring gap | N/A | — | KNOWN-TRACKED | — | **SURVIVED.** Minor aside (not a verdict change): `mass_battle_integration_v30.md:255` stubs `rally_check`/`reform_check` alongside `threadwork_check` as the same "never got mechanics" trio, but only `threadwork_check` is in scope here — the source audit's own coverage looks incomplete, not the dossier's; noted for completeness, not actioned. |
| 15 | **PP-201/PP-204 literal GM-address prose** (`params/mass_combat.md:248,251`) | N: pre-Dissolution stakes disclosure is real and load-bearing. Verified both clauses verbatim ("the GM should confirm…", "the GM must inform…") — and confirmed both are still tagged **[PROVISIONAL]** in the source, not ratified canon. Q-smooth fails hard: no engine-executable analog exists in a no-GM engine. | **REFINE** — rewrite as an engine-executable UI warning/confirm gate; underlying disclosure dynamic stays | N/A (pre-roll disclosure gate) | P1 | KNOWN-UNTRACKED (recurring since 2026-04-04, no ED) | Scoped UI-warning-dialog task (WR/GO) | **SURVIVED** — additional context found: both clauses are still [PROVISIONAL], meaning this is *unratified* prose, which if anything argues for fixing the enforcement mechanism now, before ratification, rather than after. Verdict and severity unchanged. |
| 16 | **Per-cell density vs. tracked-variable scaling mismatch** (`CELL_FLOOR=40`/`CELL_CAP=200`/`MAX_TROOPS_PER_UNIT=10000`) | N: per-cell density is legitimate simulation substrate. Verified all three constants verbatim at `config.py:25-28`. Ω-d: 100% engine-internal today, costs the player no decisions — can't be dominant. | **KEEP** (cell substrate); gap is a missing display/summarization rule | N/A | P2 | KNOWN-UNTRACKED | — | **SURVIVED.** |
| 17 | **Command clamp (1-7) vs. sub-unit count (up to 11) span-of-control gap** | N: a future subordinate-officer mechanism is historically well-grounded (Roman legates, Napoleonic corps) but genuinely undrafted. Verified verbatim at `mass_battle_v30.md:274-280`: "Open reconciliation (flagged, not resolved) … future ED" — matches PP-504's own text ("Sub-unit limit (max = Command, TTRPG cap: 3)," `mass_battle_v30.md:285`) confirming Command-governs-count was the original intent before ED-1090 decoupled the videogame cap. | **REFINE** — extend the existing Command-span rule with the missing mechanism | N/A | P2 | KNOWN-UNTRACKED | Scopes `HANDOFF_MB.md:38-41`'s "future ED" placeholder | **SURVIVED.** |
| 18 | **"No player-interaction/UI model exists for mass battle at all"** | N: some UI model is unambiguously necessary — undisputed. **But the underlying factual claim is materially wrong, and this is my own independent catch this pass, on par with #22.** I searched all 9 `designs/ui/` files and found **4 of them discuss mass battle substantively**, not zero: `valoria_ui_ux_v4.md` Part 8 (hex-scale unit tokens, 7-phase per-phase UI list, an ASCII General-Duel-screen mockup, T15 Askeheim drag-drop TS-check rules — lines 1379-1492); refined again in `valoria_ui_ux_v4_1.md` (lines 707-733); independently *audited* by `valoria_ui_ux_v4_1_max_audit.md` **Finding F-39 [P2]**, which explicitly says Part 8 is "under-specified relative to Part 7," not absent, and flags a named repair (per-phase sub-sections 8.2.1-8.2.7); and tracked with assigned priorities in `valoria_ui_ux_v4_2_workplan.md` §7.7/7.9/3.5 ("F-38 mass battle Phase 4… P1"; "F-39 per-phase UI… P2"). | **KEEP** (necessity) — **but strike "no spec… among its 9 files" as false; restate as "a skeleton UI spec exists (Part 8) and is already self-audited as under-specified, with F-38/F-39 tracked and prioritized in the existing UI workplan"** | N/A | P2 (down-graded from the source's P1, since remediation is already scoped and prioritized, not a from-scratch gap) | **KNOWN-TRACKED** (re-tagged from KNOWN-UNTRACKED — F-38/F-39 are already live workplan items) | Nothing new to retire — this *is* already the additive resolution plan's job (F-38/F-39), just mis-scoped as absent here | **OVERTURNED (factual premise), disposition unchanged.** Same failure the dossier itself caught in #22: a claim carried forward uninspected. The KEEP-for-necessity holds, but "no spec at all" is false and should not survive to publication; severity and calibration tag corrected accordingly. |
| 19 | **Balance known-broken** (15/20 gauge rows fail, ED-MB-0003) | N/Ω: calibration state is orthogonal to design merit per the charter. Verified the 6-vs-9 split precisely: `HANDOFF_MB.md` states "Full 20-row gauge: 4/20 → 5/20 (C1 newly passes; RC-5's 9 rows untouched)" and "every row now OVERSHOOTS its band decisively" (except C7) — 20−5=15 failing, 9 of those are RC-5 (separate, untriaged), leaving 6 as the attacker-favor overshoot. Matches dossier exactly. | **KEEP** (tuning process is sound) | Instance-A Robustness FAIL — see engine block | P1 | KNOWN-TRACKED (ED-MB-0003) | — | **SURVIVED**, arithmetic independently re-verified against `HANDOFF_MB.md`. |
| 20 | **Jargon collision:** engine "cohesion" (`hp/hp_max`) vs. player-facing "Coherence" (Thread stat) | N/A — naming problem atop #7. Verified "Coherence" is a real, load-bearing 0-10 Thread track at `params/threadwork.md:14` ("Ontological rendering frame. Practitioners only."), one edit-distance from "cohesion." Considered raising severity: this collision sits inside the exact Thread-during-mass-battle zone the audit's own §4 calls the highest-cognitive-load combination in the corpus (item 15's PP-201/204 clauses live in the same section) — a case for P1. I did not force that upgrade; P2 is still defensible read as a pure renaming task independent of when a reader encounters it. | **REFINE** — rename the engine-internal term (e.g. "Formation Integrity") | N/A | P2 (aggravating context noted, not auto-upgraded) | KNOWN-TRACKED | Closes a naming-registry gap in `references/names_index.yaml` | **SURVIVED**, with a flagged-but-not-forced severity consideration (see reasoning column). |
| 21 | **MS/RS naming split reaching mass battle's Thread section** | N/A — naming-consistency, owned by TW lane. | **REFINE** (MB-lane citation only) | N/A | P2 | KNOWN-TRACKED (TW owns) | Closes for free once TW's fix lands | **SURVIVED**, low-stakes, no dispute. |
| 22 | **"Orphaned unbannered superseded doc"** claim re: `references/mass_battle_redesign_workplan_v1.md` | The claim is false. Independently re-verified via `git log`: the `⚠️ STALE / SUPERSEDED` banner was added in commit `9adf13c` ("[editorial] Mass-battle LC-8 canon reconciliation (ED-1088)"), and `git log` confirms this commit predates the repo's mass-battle work through PR #84 and the crunch-cascade audit itself by six days. Not a mechanism — a documentation-currency claim, and an incorrect one. | **N/A** — strike from the live findings | N/A | P3→resolved, drop | **NEW** | Retires the crunch-cascade report's own P3 line item | **SURVIVED fully** — independently reproduced with the exact commit hash; this is a legitimate, well-evidenced catch and the strongest piece of "audit the auditor" discipline in the dossier. |
| 23 | **`LETHALITY_SCALE` restoration** vs. **`CASUALTY_SCALE`** (live) | N: judged against *today's* engine, restoring `LETHALITY_SCALE` as literally specified (`mass_battle_integration_v30.md:176,184-194,324-330`) targets a damage formula and HP model (`Size×BLOCK_SIZE`) that no longer exists. Verified `CASUALTY_SCALE = float(...,'4')` at `config.py:41` — "D-B: per-tick lethality… TUNING" — already serves the identical calibration purpose. Classic duplicate-coverage. | **PRUNE** — strike the restoration TODO; `CASUALTY_SCALE` is the live knob | N/A (damage-multiplier constant, not a roll) | P3 | KNOWN-TRACKED (disclosed-deferred, not silent rename) | Closes `mass_battle_integration_v30.md` §4.2 "Step 2" outright | **SURVIVED** — attempted a KEEP argument (maybe LETHALITY_SCALE names a distinct, still-needed concept); it doesn't hold, `CASUALTY_SCALE` genuinely already does this job under the current HP model. |
| 24 | **CURRENT.md canonical-head pairing friction** | N/A — pure navigation legibility. Verified `CURRENT.md:42` pairs `mass_battle_v30.md` + `mass_battle_integration_v30.md`; verified the latter's own banner (`mass_battle_integration_v30.md:9`) literally says "config.py is leading canon" — self-disclaiming half of its own pairing. | **REFINE** — repoint CURRENT.md's mass-battle row to name `config.py` directly | N/A | P3 | KNOWN-TRACKED | One-line CURRENT.md edit | **SURVIVED**, citation confirmed exact. |

## NERS engine verdict — Mass Battle Engagement Pool (Instance A, aggregated sigma-leverage) — corrected

```
ENGINE: Mass Battle Engagement Pool (Phase 5, §A.1/§A.7)   INSTANCE: A (aggregated sigma-leverage)
VERDICT: NON-COMPLIANT — Robustness fails at the system-calibration level; Necessity/Elegance hold;
         Smoothness carries a legibility (not live-math) defect from unpruned legacy formula text.

N: PASS — confirmed, no redundant roll.
R: FAIL —
   (a) 15/20 gauge rows out of band (ED-MB-0003): 6/20 attacker-favor overshoot, 9/20 untriaged RC-5
       — independently re-verified against HANDOFF_MB.md's own arithmetic.
   (b) Two unreconciled stamina-penalty channels — CONFIRMED both live simultaneously by default
       (PER_CELL=1 default, atom-level drain unconditional) — strengthened this pass with the ED-1017
       ledger evidence that the divergence was known and deliberately left unresolved on 2026-06-17.
   (c) ED-875 (low-Command leverage spike) is RESOLVED 2026-06-20 (Jordan: deliberate, magnitude
       defensible) — but skills/valoria-resolution-diagnostic/SKILL.md still asserts "OPEN, Gate G8"
       at THREE spots, not two: lines 98, 341, and 352 (dossier only caught 98/352; line 341 —
       "watch ED-875 (low-Command leverage too hot, OPEN Gate G8)" — was missed). Skill-currency
       defect, not an engine defect.
   (d) subunit_combat_pool's full-Command-per-subunit multi-front exposure (PP-504) — confirmed live
       and confirmed explicitly "deliberately undecided" per HANDOFF_MB.md, three days pre-audit.
S: PASS w/ legibility flag — confirmed 3 textually-coexisting formula generations, one line rule.
E: PASS — confirmed one-line live formula; doc's legacy text fails Elegance for the reader only.

REMEDIATION (worst-first, severities now consistent with per-row table):
  P1  Diagnose/reconcile the dual stamina-penalty channels (item 8).
  P1  Jordan-rule the full-Command-per-subunit multi-front exposure question (item 11).
  P2  Prune mass_battle_v30.md's two superseded pool-formula generations (item 12).
  P3  Update SKILL.md's ED-875 status citations at lines 98, 341, AND 352 (item 19c, corrected count).

RE-TEST: not run this pass (read-only audit), consistent with prior pass.
```

## Per-lane rollup (corrected)

**MB — clean of true CUTs, but not clean of debt.** Every load-bearing tracker (Size, Power, Discipline, Morale, Army Morale, Command, Cohesion, sub-unit count) survives independent re-verification against the working tree, and Army Morale (#5) remains the corpus's own best-practice example of solving legibility through abstraction rather than deletion. The subtractive weight sits almost entirely in **DISTILL/REFINE, not PRUNE/CUT** — 2 DISTILLs (formation dice-table folding into a geometry/stance system that, on closer reading, already carries a literal `ROLE_SPEC["ShieldWall"]` key; the three-generations pool formula collapsing to its one live ED-1013 form) and 8 REFINEs, against exactly 1 PRUNE (`LETHALITY_SCALE`'s restoration order). This pass raises two severities for internal consistency — the dual-stamina-channel defect (#8) and the multi-front Command-exposure question (#11) were tagged P2 at the row level while the dossier's own engine-verdict block already ranked both P1; both are now P1 throughout, and #8 gained hard evidence (ED-1017) that the stamina divergence was a *known, deliberately deferred* decision from 2026-06-17, not an accidental oversight — making it a more, not less, overdue fix. This pass also overturns one factual premise: item 18's claim that "no player-interaction/UI model exists for mass battle at all" is false — a skeleton UI spec (Part 8 of `valoria_ui_ux_v4.md`/`v4_1.md`) exists, is independently self-audited as under-specified (F-39, P2), and already has scoped, prioritized remediation tasks (F-38 P1, F-39 P2) sitting in `valoria_ui_ux_v4_2_workplan.md` — the necessity-based KEEP survives, but the "absent" framing does not, exactly the kind of stale claim the dossier's own item 22 (independently re-confirmed here via `git log` commit `9adf13c`) warns against carrying forward uninspected. No verdict category changed hands in either direction — every REFINE/DISTILL/PRUNE that was attacked survived as its own category, just with corrected severities and, in two cases, stronger evidence than the dossier had.

**24 elements: 12 KEEP / 8 REFINE / 2 DISTILL / 0 MERGE / 1 PRUNE / 0 CUT (post-critic)** — plus 1 non-mechanism (#22, doc-currency claim, dropped from the tally as N/A, both before and after critique.

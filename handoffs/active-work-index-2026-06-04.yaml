id: active-work-index-2026-06-04
scope: general
task:
  skill: valoria-orchestrator
  description: 'CONSOLIDATED master resumption index (2026-06-04). Folds 5 prior active
    handoffs into one sectioned master across 4 work-streams; sources archived to
    archives/handoffs/. Reconcile: #1 cell-cap merged into mass-battle (#2). NO design
    decisions resolved — every Jordan-blocked call carried as OPEN.'
last_commit: 'build C committed 40fe9845 (2026-06-05). Chain: bf8168aa (audit) ->
  cb8b5844 (A) -> c2134e16 (B) -> 40fe98458f4db25967afe0dc2f34e553a53da341 (C). A+B+C
  VALIDATED + full test pass GREEN (post-reset 2026-06-05): all 5 validators PASS;
  counters 5/8,8/8,8/8 byte-exact; fuzz(120) 0 engfail/0 degen (wins A39/B38/draw37);
  mirror skew 6/48. D and E found SUBSUMED/COSMETIC (see next).'
owns:
- designs/scene/combat_engine_v1/**
- designs/audit/2026-06-02-combat-engine/**
- designs/audit/2026-05-29-combat-armature/martial_traditions_synthesis.md
- designs/proposals/pc_formation_system.md
- designs/provincial/mass_battle_v30.md
- designs/audit/2026-06-01-massbattle-stub-wiring/**
- tests/sim/mass_battle/**
- tests/sim/gauge_mb.py
- tests/sim/sim_mb_sigma.py
- tests/sim/mass_battle_stress.py
- skills/valoria-orchestrator/scripts/**
- references/**
- tests/hooks/**
- tests/index/**
- tools/**
- archives/**
- session_logs/**
- skills/valoria-orchestrator/references/skill_registry.yaml
- sim/**
- tests/sim/**
- tests/coverage_matrix.md
- valoria-game/**
working_state:
  next:
  - 'BUILD C DONE + COMMITTED 40fe9845 (envelop maneuver: a detachment with the ''envelop''
    instruction paths around the flank into the rear; PC_ENVELOP_PATH default ON;
    two-phase wide-pass-then-turn-in reusing the 2D cell_target steering; byte-exact;
    V-ENVELOP: rear-reach mean row-offset ~0 / 7-of-20 behind vs +3.9 / 0 behind straight).
    FULL TEST PASS GREEN post-reset: 5 validators PASS, counters 5/8,8/8,8/8, fuzz(120)
    ROBUST (0/0), mirror skew 6/48.'
  - 'BUILD D (pin/Anvil) -- HONEST FINDING: SUBSUMED, not shipped. The ''pin'' instruction''s
    intended function (fix the enemy so it cannot reface/disengage while the hammer
    strikes) is ALREADY delivered by the existing cell-pin mechanic (orchestration.py
    ~L1466: a cell with an attacker in its front arc within PC_PIN_REACH cannot reface
    -> takes the flank penalty) + build A''s fixed_by_other. Empirically every wiring
    of ''pin'' (deny-disengage; extended pin reach PC_PIN_REACH*2 for an anvil) produced
    delta 0.0000 in every scenario (held D, balanced D, anvil at contact, anvil held
    at dist 2): deny-disengage duplicates halt-at-contact (contact cells already frozen);
    extended reach is a no-op (fixers close to contact -> base reach already pins;
    held defenders never reface). Per no-manufacture, did NOT ship inert code or a
    sham validator -- reverted to clean A+B+C. A MEANINGFUL D would need a NON-redundant
    pin mechanic (e.g. a held enemy suffers a distinct cohesion/mobility penalty WITHOUT
    double-counting A/B) -- a separate scoped build, Jordan to decide if wanted.'
  - 'BUILD E (atomize archer volley/flanker targeting + sweep) -- HONEST FINDING:
    targeting-atomization is COSMETIC under the current model, not shipped. volley_phase
    (orchestration.py ~L1753) accumulates each shot''s dmg into a FACTION-LEVEL total
    (loss_a/loss_b); the returned target subunit (tgt) is used ONLY as a None/in-range
    gate, and the density mult keys on the whole target_unit -- so WHICH subunit an
    archer targets changes neither the damage amount nor its distribution (spread
    across the whole enemy unit downstream). Implemented PC_VOLLEY_TARGETING (order_target_idx/weakest
    in fire, byte-exact) but it changed no outcome (probe [225,225] either way; weakest
    hit the stronger). A MEANINGFUL E needs a VOLLEY-DAMAGE DISTRIBUTION rework (apply
    dmg to the SELECTED target subunit''s cells, not a faction total) -- substantial,
    calibration-sensitive, byte-exactness-risky; separate scoped build. The ''sweep''/horizontal-path
    half is untouched (would mirror the kite/envelop instruction pattern as a gated
    movement mode). E reverted; clean A+B+C.'
  - 'SEQUENCE VERDICT: A+B+C achieve the CORE envelopment goal -- decisive envelopment
    of a vulnerable line (front pinned + flank, with B''s shock) is validated; the
    detachment can reach flank (wheel) and rear (C). D+E as originally scoped are
    subsumed/cosmetic given the existing engine model (cell-pin+A/B/C for D; faction-level
    volley for E). RECOMMEND to Jordan: either accept A+B+C as the envelopment deliverable
    and rescope D/E as the deeper reworks above (non-redundant pin; per-target volley
    damage; sweep movement mode), or deprioritize them. Do NOT ship the cosmetic D/E
    wirings.'
  - 'BUILD C bottom-up read DONE (2026-06-05) -- scope REFINED by an empirical probe.
    Read: find_contacts (orchestration.py:1177) is PURELY POSITIONAL (Chebyshev<=1
    any cell pair, facing-agnostic) -> the rear WOULD register as contact if reached,
    so NO contact-detection fix needed. assign_targets (:1028) = nearest-centroid
    (or order_target_idx/target_condition). MOVEMENT (Subunit advance, ~:695-790)
    already does 2D steering toward a cell_target (row+col split), and column-local
    targeting holds each cell''s file UNLESS it is an OVERHANG cell (column beyond
    the enemy frontage emin..emax), in which case the ENVELOPMENT WHEEL (PC_WHEEL,
    default ON, Jordan 2026-05-31) steers it to the nearest enemy cell (the flank)
    and rotates its facing vector inward so the octagon reads the flank/rear. PROBE
    (M frontal at (20,25) dir-1 fixing D''s front + detachment F deployed WIDE at
    (15,40) past D''s frontage, PC_WHEEL on): F WHEELED in and engaged D''s right
    flank (F centroid (16.0,30.5) vs D (16.5,27.5); 2 contact pairs M->D and F->D;
    decisive: D hp 0.541, A hp 0.822). => FLANK ENVELOPMENT ALREADY WORKS via the
    existing wheel + A + B when the detachment is deployed WIDE (overhang). The earlier
    ''no contact pair'' finding was for a detachment BESIDE (within frontage, no overhang)
    or directly behind -- NOT a wide deployment. NEXT-SESSION caveat: verify A''s
    fixed_by_other + B''s shock fire precisely in this flank case (the probe shows
    the decisive outcome + RED/flank zones, consistent with A+B, but confirm via the
    per-pair a_fixed_other / angle_mod, not just hp).'
  - 'BUILD C REFINED DESIGN (implement next session): the FLANK is handled; C''s remaining
    gap is REAR-REACH (deep envelopment) -- a detachment getting BEHIND the enemy
    (the rows past it) for the full Cannae (front pinned + flank + REAR) and the RED
    rear shock (1.6x vs flank 1.0x). The wheel steers overhang cells to the NEAREST
    enemy cell (the flank), never the rear. PROPOSED: an ''envelop'' INSTRUCTION (Subunit.instructions,
    like the existing ''brace''/''kite'') that, when set, gives the detachment a maneuver
    target = the enemy''s REAR cells (centroid + a depth offset past the far edge),
    so the 2D cell_target steering paths it AROUND the flank into the rear rows. Gate
    PC_ENVELOP_PATH (default ON but fires ONLY for units carrying the ''envelop''
    instruction -> the gauge/counter scenarios have none -> byte-exact; verify 5/8,8/8,8/8).
    Reuse the existing 2D steering + wheel; do NOT rebuild movement. Validator V-ENVELOP:
    a detachment ordered to envelop REACHES and ENGAGES the rear (RED zone on the
    enemy), and the enveloped unit then takes A''s penalty + B''s RED shock from behind
    -> strictly more decisive than the flank-only case. [canonical: Cannae 216 BC
    double-envelopment; Khalid at Walaja; A.8 Envelopment.] SEQUENCE NOTE for Jordan:
    the CORE envelopment goal (front pinned + flank, DECISIVE) is now ACHIEVED by
    A+B+wheel; C (rear) is the maximal-effect refinement, D (pin/Anvil role) and E
    (sweep + archer-targeting) are separate features. Re-confirm priority before C
    if desired.'
  - 'BUILD B DONE + COMMITTED c2134e16 (2026-06-05). Envelopment moral-shock: a subunit
    FIXED frontally by a separate body and struck on its flank/rear cannot face the
    new threat, so the du Picq moral shock fires even WITHOUT a momentum charge (the
    charge path''s gap that left A modest). B REUSES the calibrated resolution._charge_shock_sigma
    on the SAME fixed_by_other + flank/rear condition A detects, fired as an elif
    to the charge-shock site in orchestration.py (mutually exclusive -> no double-count).
    Flag PC_ENVELOP_SHOCK (orchestration.py module global, default ON, env-overridable,
    toggled in-process by validators). B DEPENDS ON A (its gate b_angle_mod<=-0.5
    needs A''s term to have fired). Byte-exact: counters 5/8,8/8,8/8 with B on / B
    off / A+B off (fires only on fixed_by_other -> inert single-subunit); fuzz(120)
    0 engfail/0 degen; mirror skew 6/48 (stochastic). One shock mechanic, two triggers
    (charge, envelopment) -- Lesson 1; REJECTED flat-temp-disc-penalty + node-scatter
    (latter would force PC_NODE_COHESION on, breaking byte-exactness). Validators
    tests/sim/mass_battle/validators.py now 4: V-CANNAE/V-FIXING isolate A (shock=False);
    V-SHOCK (B decisive vs a balanced disc4 line: A+B retains +0.030 over A-only,
    0 seeds worse) + V-BRACE (a braced disc5 unit RESISTS: B-marginal braced 0.004
    < line 0.030 -- guards against B as a blanket flank insta-kill). Magnitudes: A
    angle penalty -0.4sigma (RED); B shock ungated -0.55sigma (flank) to -0.88sigma
    (rear), brace floor 0.05 -> -0.044sigma.'
  - 'EMERGENT picture after A+B: envelopment is now DECISIVE against a vulnerable
    line, but ONLY via frontal-mass SPILLOVER (held/balanced D fixed by main M frontal
    + detachment F joining behind M and spilling onto D''s flank/rear). A braced+deep+disciplined
    unit still resists (correct -- Waterloo square; needs multi-direction envelopment,
    which awaits C). Full Cannae annihilation (front pinned + both flanks + rear)
    is NOT yet reachable because a detachment cannot ENGAGE the rear directly.'
  - 'NEXT: build C -- fix the front-biased contact/targeting so a detachment ordered
    to envelop can REACH and ENGAGE the flank/rear directly (the no-rear-contact-pair
    finding: direct rear/flank approaches form NO contact pair today; envelopment
    emerges only by spillover). Bottom-up read FIRST: find_contacts (orchestration.py
    ~L1216) + the maneuver-pathing system (_maneuver_target / envelop maneuver, A.8
    Envelopment) + assign_targets. Likely a Phase-2 maneuver hook routing the detachment
    to FLANK/REAR cells (not nearest) + possibly a contact-detection fix so a body
    bearing on the rear forms a pair. Validator V-ENVELOP (a detachment ordered to
    envelop reaches and engages the rear; the enveloped unit then takes A''s penalty
    + B''s shock from the new direction). Then D (wire a fixing/pin role -- ''pin''/Anvil
    currently inert in TROOP_TYPE_ROLES/ROLE_SPEC) and E (''sweep''/horizontal path
    + atomize volley/flanker targeting of archers; V-ARCHER). CALIBRATION GUARD unchanged:
    byte-exact-verify every change vs A-absent (match 5/8,8/8,8/8 OR PC_FIXING_FLANK=0/PC_ENVELOP_SHOCK=0)
    before commit; flag/known-values method, never swap-and-restore.'
  - 'BUILD A DONE + COMMITTED cb8b5844 (2026-06-05). Atomized the flank-bonus gate
    from the unit-WIDTH gate to SUBUNIT-SCALE fixing-force: a subunit fixed frontally
    by a SEPARATE body (orchestration.py _front_fixers, centroid-GREEN per contacting
    pair) cannot wheel as a body, so a detachment bearing on its flank/rear lands
    the zone angle penalty regardless of frontage width. Provably inert single-subunit
    -> byte-exact counters (deepColumn>thinLine 5/8, ShieldWall>Wedge 8/8, command-dominates
    8/8). Toggle PC_FIXING_FLANK (orchestration.py module global, default ON; term
    reads it at call time so validators toggle in-process). Validators tests/sim/mass_battle/validators.py:
    V-CANNAE (envelopment advantage -- no seed worse, delta>0) + V-FIXING (pin-conditional
    -- exactly 0 effect without a front-fixer). fuzz(120) 0 engine-fail/0 degenerate;
    mirror skew 2/48. Ledger (tests/sim_verification_ledger.json) +9 validator scenario-fixture
    entries. Good-A backup at /tmp/orch_A_GOOD.py.'
  - 'FINDINGS feeding next builds: (1) A is the ANGLE DISADVANTAGE only and is MODEST
    -- cuts the fixed unit''s offence (+0.006..0.012 attacker-hp delta over a decisive
    battle, compounding with length, one extra rout 7-vs-6); the DECISIVE cohesion/morale
    collapse of an enveloped line is BUILD B (shock). (2) A detachment approaching
    DIRECTLY into the rear/flank forms NO contact pair today -- envelopment currently
    emerges ONLY via frontal-mass spillover around a fixed defender (config: held
    D + main M frontal + detachment behind M -> F spills 20 RED + 16 YELLOW onto D);
    reliable rear-reach (maneuver to flank/rear + contact-detection fix) is BUILD
    C. (3) centroid front-fixer is CONSERVATIVE: a SPLIT frontal attack (two off-centre
    subunits straddling the front) is NOT detected as fixing (under-fire, no false
    positive) -- refine later if needed. (4) the angle mod reduces the FLANKED unit''s
    OFFENSE, not its incoming damage -> validator metric is ATTACKER-retained-hp,
    not D-hp.'
  - 'NEXT: build B -> C -> D -> E in order, cautious/granular bottom-up, each with
    its validator. B (shock on new-front contact): temp discipline penalty + N-tick
    node-cohesion cutoff (cell scatter) local to the struck subunit; validator V-SHOCK.
    THIS makes envelopment DECISIVE (A alone is modest). Bottom-up read first: resolution.py
    _charge_shock_sigma (DEFENDER moral-shock on charge impact, zone-gated) + the
    discipline/node-cohesion system (PC_NODE_COHESION). Jordan OPEN design Qs for
    B: flat temp disc -1 for N ticks vs node-scatter vs both; fire on ANY sudden new-front
    contact (incl. infantry-flank) or charge-only; pairs with morale->SUBUNIT atomization.
    Decide bottom-up + propose, vetoable. CALIBRATION GUARD: combat core is calibrated
    -- byte-exact-verify EVERY change vs A-absent (match 5/8,8/8,8/8 OR PC_FIXING_FLANK=0)
    before commit; prefer the flag/known-values method over swap-and-restore (which
    silently lost A once this session).'
  - 'Resume by stream (A-D). Mass-battle (B): RATIFIED + CANONIZED (ED-905) -- discipline
    decoupled from speed (PP-232), reform_check wired with PP-241 command gate, directed
    maneuver pathing (envelop implements A.8 Envelopment / surge = breakthrough gap-fill),
    targeting granularity unit->subunit->cell confirmed. NOW IN: maneuver-constant
    tuning + stress testing of the new mechanics. Lane-B(C)+Lane-C stubs(D) autonomous;
    A awaits Jordan.'
  - '(B) Recent chain: PC_NODE_COHESION continuous-path crash FIXED (545af082, setdefault->anchor);
    the long-standing GappedLine/Horseshoe ''non-close'' diagnosed as NOT a shape
    defect but the discipline-speed flooring cliff, then discipline DECOUPLED from
    movement speed (afdabadf; canon PP-232 discipline=cohesion, params/mass_combat.md
    has zero speed coupling) - disc_mult retained only for node cohesion factor +
    wheel rate; ledger auto-bootstrap + relaxed fabrication-gate pattern (88479e58).
    THIS session (8ec085c0): reform_check WIRED (unengaged unit restores +1 discipline
    toward discipline_start each phase boundary per mass_battle_v30 L180; engaged
    = Chebyshev<=1); directed maneuver pathing added (Subunit.maneuver: envelop=go-wide-then-hook-flank/rear,
    surge=break-line-and-drive-deep; also fired by instructions tokens so the Flanker
    envelop template is now live; _maneuver_target stateless per-tick; advance_cells
    branches, direct column-local+wheel is byte-exact default); targeting granularity
    CONFIRMED unit(matchup+stance/disc/cmd)->subunit(target selection + maneuver)->cell(each
    cell computes own target+moves). Validation: counters 5/8 8/8 8/8 unchanged, fuzz(120)
    0 engfail/0 degen, reform unengaged 3->4 capped/engaged unchanged, envelop hooks
    flank (row 17), surge breaks weak line (row 10 vs direct 16). OPEN (B): cell-support-fluidity
    (discipline role #4) only PARTIAL + threadwork_check empty (G-9); config.py 10
    pre-existing uncited constants (hygiene - blocks future config.py commits; maneuver
    constants live in orchestration.py to avoid it); multi-subunit lateral spacing
    convention (Jordan sign-off - harness SPREADs are my invention); in-combat tactical
    withdrawal (enhancement). Jordan-blocked: gauge band realign, per-cell CAP value,
    density magnitude/role table.'
  - '(A) Await Jordan: ratify combat_engine_v1 (3 commits) + martial-traditions D-alpha/beta/gamma/delta.'
  - '(C) Autonomous: index-refresh+generator-fix, check_handoff_conflicts glob-bug,
    propagation_map owns-narrow, 7.2-followup, 1.20.'
  - (D) Continue Lane-C buildable stubs (6.3->6.5->8.1->1.16); CB-economy + resolver-dispute
    wait on Jordan.
  consolidation: 'Collated 5 active handoffs (collate->reconcile->consolidate). #1
    valoria-troops-in-cell-cap-decision merged into mass-battle stream (#2): identical
    last_commit 613a3e87, overlapping owns, #1=latest step. Other three are distinct
    streams kept as sections (write-disjoint lane model preserved). Sources archived;
    git keeps originals.'
  sections:
    A_personal_combat_engine:
      status: BUILT+validated; ratification STAGED — awaiting Jordan
      last_commit: 6e9b6aa6 (engine); fe8d9dfc (martial-traditions, this session)
      done: 'combat_engine_v1 (6 modules/655 lines) matrix-validated A0-A3; ratification_and_deprecation_proposal.md
        staged; wound-count tiebreak removed (undecided=UNRESOLVED, 9e7ce85a); P1/P2
        cleanup. THIS SESSION: martial_traditions_synthesis.md (fe8d9dfc) — 6 Valoria-native
        traditions engine-legible (vector+armature abilities+familiarity), Constraint
        fully specced, grade=''V'' proposed.'
      next: 'On ratification: 3 safe_commits (canonize+canonical_sources / combat_v30
        PARTIALLY SUPERSEDED / deprecate params/combat.md) + magnitude tuning. Martial-traditions:
        spec 5 remaining + run resolution-diagnostic once D-alpha/beta/gamma set.'
      jordan_blocks:
      - ratify combat_engine_v1 (3-commit canonization)
      - martial-traditions D-alpha replace/coexist, D-beta count(6), D-gamma grade=V,
        D-delta naming
    B_mass_battle_pc_formation:
      status: 'Engine validated, targeting-extended, stress-tested, discipline canon-aligned.
        Discipline DECOUPLED from movement speed (PP-232: discipline=Cohesion; canon
        has no disc-x-speed coupling). disc_mult retained for cohesion factor + wheel
        rate only. PC_NODE_COHESION crash fixed. Historical counters preserved. NO
        GappedLine/Horseshoe shape defect (it was the speed cliff).'
      last_commit: '19298bb (Column). Cell-up chain: 19298bb <- 6d6db27(2c) <- 2bd7d28(2b)
        <- 3a47d15(2a) <- da7b7b6(step1).'
      done: 'THIS SESSION cell-up build: (1) cell-primary state da7b7b6 (Subunit.cell_troops
        dict; hp=sum cells). (2) node-relational cohesion behind PC_NODE_COHESION
        (default OFF->byte-exact): 2a 3a47d15 (live _node_pos + relational offsets
        + soft cohesion + translation), 2b 2bd7d28 (formation wheel), 2c 6d6db27 (node-aware
        contention + momentum + per-cell collision/no-pass-through). (3-REDIRECT)
        deep-narrow Column shape 19298bb = the DEPLOY PRIMITIVE. Measurement redirected
        step 3: engine is ALREADY frontage-capped (_lanchester_strength = contact
        cols x per-cell density) + has depth machinery (rotation/refill, fatigue-damp,
        charge-absorption), and a deep formation wins frontally ONCE DEPLOYABLE (monkeypatch
        deep-Column-vs-wide-Line 8/12). So the gap was a deploy primitive, NOT a damage
        rewrite. Column = Line-block transposed (tier pattern) + width=s,depth=round(LINE_ASPECT^2
        *s) ~2:1 (continuous); purely additive (existing shapes byte-exact); validated
        deploys 4x8, holds/wins vs wide Line (7/12). CANON VALIDATION (vs precedents_warfare
        §1, resolving depth max_turns=180): command-dominates EMERGES 20/20 (small
        high-Cmd annihilates large low-Cmd); ShieldWall>Wedge EMERGES 19/20; Wedge>Line
        FRAGILE 8/20 continuous (tier3 ok). REFRAME: Cannae is COMMAND-enabled maneuver
        in canon (§1.1), NOT a Horseshoe shape -> gauge H3/H4 Horseshoe bands are
        NON-CANONICAL; the earlier ''envelopment broken'' was a bad test. gauge_mb
        FIXED LOCALLY (exec(sim_v22)->modular imports + matchup max_turns 18->180
        + canonical-status docstring) but COMMIT BLOCKED: sim_fabrication_check whole-file-scans
        and flags gauge_mb''s PRE-EXISTING constants (ANCHOR_MAP, bands) — the container-local
        citation ledger is non-durable and lost them. || 2026-06-05 (THIS SESSION)
        BREAKTHROUGH VALIDATION (1f9361fb): radial-concentration exchange-sigma channel
        tried -> FAILED (attrition-frontage limit). Reframed bottom-up-primitives
        / top-down-history: _lanchester_strength frontage-only = CORRECT (Goldsworthy:
        rear ranks inflict no significant damage; depth = cohesion/endurance/sustainment,
        not lethality). Continuous N=18x2: Wedge>Line 33%, deepColumn>thinLine 50%,
        command 100%, ShieldWall>Wedge 97%, cav-vs-braced 0%; winner 84-94% vs loser
        45-63%, all decisive (Sabin). VERDICT: wedge-frontal-loss is HISTORICALLY
        CORRECT; primitives already correspond; prototype reverted to pristine. PC_BREAKTHROUGH
        next-primitive RETIRED. || 2026-06-05 (THIS SESSION): Flanking/cav assessed
        (facing works, rear-charge devastating); stance=retreat pre-contact lure mechanic
        confirmed; all 6 shapes vary size/density/location confirmed; targeting extensions:
        target_delay_ticks + target_condition (weakest/in_range:N) added to Subunit
        + assign_targets (3faa2b4bc8); stress harness stress_random.py committed (3778d6e6);
        PC_NODE_COHESION crash in continuous path found (KeyError _node_advance, off
        by default). || 2026-06-05 (cont.): Self-healing fabrication gate (88479e58:
        pattern accepts bare [canonical:], bootstrap auto-fetches ledger); PC_NODE_COHESION
        continuous-path crash fixed (545af082: _node_advance setdefault); discipline-speed
        flooring fixed (d869461c: floor->round in advance_cells + _node_advance —
        base_speed=1 + disc_mult=0.7 floored to 0, freezing disc-3-4 units; both-low-disc
        battles drew at 100%); NULL on GappedLine/Horseshoe shape defect (single-subunit
        shapes close 20/20 at every parameter; the non-close was the speed cliff,
        shape-independent, affecting plain Lines too). || 2026-06-05 (cont2): Discipline-speed
        DECOUPLING (afdabadf, supersedes d869461c). Canon evidence: mass_battle_v30
        L7 PP-232 renames Cohesion to Discipline; params/mass_combat zero disc-x-movement.
        Removed disc_mult from actual_speed and step; kept it for node cohesion factor
        (k) and wheel rate (kw). Counters byte-identical, fuzz and mirror clean. Mapped
        discipline actual wiring vs the 4 intended roles.'
      next: 'DISCIPLINE 4-ROLE WIRING (design direction needed): (1) morale-drop WIRED
        (erosion=dmg/(disc*cmd), L2265); (2) cohesion-loss WIRED (resolve_internal_collisions
        d10 vs disc L828-873; node k=disc_mult L632); (3) cell-regroup EMPTY HOOK
        (reform_check unimplemented L348; canon has Reform +1 disc restoration mass_battle_v30
        L180; engine future cycle G-8) -- THE REAL GAP; (4) cell-support-fluidity
        PARTIAL (cohesion factor governs hold; discipline-gating of support unclear).
        Plus prior blocked items (gauge band, per-cell CAP, density magnitude) and
        multi-subunit spacing convention.'
      jordan_blocks:
      - per-cell CAP value — the OLDER density-into-_lanchester_strength refinement
        (strength=SUM min(troops-per-cell,CAP)); still blocked; reconcile with validated
        density-dominates when wiring.
      - '[ANSWERED by ''proceed with wiring''] cell-vs-cell damage necessity -> SUBSUMED
        by the breakthrough primitive; NOT a separate build.'
      - gauge canonical band realignment — H3/H4 Horseshoe bands non-canonical (Cannae=command);
        realign to canonical counters (historical targets).
      - PC_NODE_COHESION default on/off (more-decisive 91/30 calibration).
    C_lane_b_infra_governance:
      status: much DONE; autonomous + Jordan-verify + non-parallel-gated remain
      last_commit: a166d52c
      done: 5.5 freshness; 5.6 compliance; 7.2 hook tests(33); 5.2 six subsystem docs;
        5.8 scope-vocab single-source+drift-guard; 1.10b CI fix; 1.18 retire atoms_pending(381);
        P1 citation-rot diagnosed+prevention (validate_ed_citations.py 8/8); INDEX
        de-accretion (647 curated rows preserved).
      next: 'Autonomous: index-refresh+generator-fix, check_handoff_conflicts glob-bug,
        propagation_map owns-narrow, 7.2-followup, 1.20.'
      jordan_blocks:
      - 1.17 superseded-before-deletion verify (2 flagged branches)
      - 'non-parallel-gated: 5.3 canonical_sources SHA-split + 5.8-reduction (single-lane
        only)'
    D_lane_c_sim_stubs:
      status: queue in progress; mostly self-unblocked
      last_commit: 8feb7526
      done: '6.1 DONE (20 sim/ stubs remain); 6.4 DONE; 6.3 IN PROGRESS 3/11: parliamentary_vote(d0f3c0bd)/stay(251c5528)/transfer(8feb7526),
        sweep-verified; articulation reclassified (not a sim stub; consumes PP-687
        Key substrate).'
      next: Continue 6.3 buildable stubs -> 6.5 -> 8.1 -> 1.16. parliamentary_transfer
        gated on CB-economy gap (no Casus Belli ledger).
      jordan_blocks:
      - 7 hard-gated stubs + Varfell need Lane-A/Jordan canon (the resolver dispute
        below)
  reconciliation_notes:
  - '#1 cell-cap folded into Section B — same stream/commit, latest step; no conflict,
    pure redundancy.'
  - 'coverage_matrix.md claimed by B,C,D — generated/derivative, regenerated not hand-merged
    (conflict-res #9); shared-ownership noted.'
  - propagation_map.md double-claim (Lane A vs B) — Lane A finding; owns-narrow is
    a Lane-B item + coordination flag.
  - No design/canon decision resolved during consolidation; all carried to blockers
    as OPEN (Jordan authority).
blockers:
- '[OPEN - Jordan] (A) Ratify combat_engine_v1 -> 3 commits (canonize+canonical_sources
  / combat_v30 PARTIALLY SUPERSEDED / deprecate params/combat.md).'
- '[OPEN - Jordan] (A) martial_traditions_synthesis: D-alpha replace-vs-coexist, D-beta
  count(6), D-gamma grade=''V'' ratify, D-delta in-world naming.'
- '[OPEN - Jordan] (B) gauge canonical band realignment — H3/H4 Horseshoe targets
  non-canonical (Cannae=command-enabled maneuver, §1.1); realign bands to the canonical
  counters before trusting gauge verdicts.'
- '[OPEN - Jordan] (B) mass-battle per-cell CAP value — blocks density-into-exchange
  wiring (THE input).'
- '[OPEN - Jordan] (B) density magnitude + troop-type->role table (pending research)
  + MIN_DISCIPLINE/UX opens + canon A.6/A.5 reconciliation.'
- '[OPEN - Jordan] (C/D) ED-883/865/866 P1 resolver dispute — canon ID-conflict, faction/Jordan
  adjudication (matches ED-865/866/867 backlog); gates 7 Lane-C stubs + Varfell.'
- '[OPEN - Jordan] (C) lane-B 1.17 superseded-before-deletion verify (2 flagged branches).'
- '[COORDINATION] coverage_matrix.md shared B/C/D (regenerate, don''t hand-merge);
  propagation_map.md owns-narrow (Lane A/B).'
key_values:
- 'Reserved ID maxima — ED: lane-B max 884 (<890); lane-C next 970 (970-999). PP:
  lane-B 726 (<727); lane-C next 780 (780-799). DO NOT collide.'
- 'valoria_index.sql is SELECTIVE: committed = schema + curated (meta 8/concepts 82/aliases
  334/concept_files 231); files+entities runtime-only, NEVER committed. Naive full-regen-then-commit
  DESTROYS the 647 curated rows.'
- Mass-battle engine byte-exact at HEAD (7 modules). COMMAND_SIGMA_ENABLED ON; OFF==canonical
  Size pool byte-exact. Command=round((2*Cha+Cog)/3) (ED-899).
- 'combat_engine_v1: 6 modules/655 lines, validated 6e9b6aa; invariants hold (mirror
  50, mastery dominant, traditions ~48, no one-shot, 95% cap).'
- 'Lane model (workplan v2 2026-05-31): write-disjoint A(canon/designs)/B(infra/tooling)/C(sim+tests+valoria-game).
  Preserved here as sections.'
- 'CELL-UP (this session): steps 1-2 committed behind PC_NODE_COHESION (env, default
  ''0'' -> byte-exact); Column shape committed 19298bb (additive). RUN: cd mass_battle
  && PER_CELL=1 PYTHONHASHSEED=0 PYTHONPATH=/home/claude python3 -m mass_battle_stress.
  Toggles set BEFORE python (config reads env at import): PER_CELL=1, PC_NODE_COHESION=1.'
- 'CANON VALIDATION vs precedents_warfare §1: command-dominates ✓20/20, ShieldWall>Wedge
  ✓19/20, Wedge>Line FRAGILE 8/20. Canonical counters §1.2: Wedge>Line, ShieldWall>Wedge,
  Skirmish-immune-flanking + the command-dominates axiom. Cannae = command-enabled
  maneuver (NOT Horseshoe). [2026-06-05: Wedge>Line "fragility" is HISTORICALLY CORRECT,
  not a defect — wedge wins by gap/penetration (Gaugamela), not frontal grind; 1f9361fb.]'
- '[DRIFT] sim_fabrication_check whole-file-scans + flags PRE-EXISTING comment-cited
  constants the non-durable container ledger lost -> re-committing ANY touched legacy
  sim file (e.g. gauge_mb) is BLOCKED until its constants are re-registered.'
- '[RESOLVED 2026-06-05, 1f9361fb] PC_BREAKTHROUGH RETIRED — validated a misframed
  target (NULL). Wedge wins via gap/penetration not frontal grind; pristine engine
  already matches precedent. NOT built.'
- 'Mass-battle commit workflow (engine=tests/sim/mass_battle/*.py + tests/sim/mass_battle_stress.py):
  bootstrap -> h.sim_fabrication_check(adds) -> g.read_files_graphql(sim-set+coverage)
  -> h.task_gate(''simulation'') -> h.safe_commit(adds,[],msg). ANY tests/sim/** commit
  REQUIRES co-file tests/coverage_matrix.md in adds (append dated HTML comment). Reuse
  cited constants. Commit set this session: da7b7b6,3a47d15,2bd7d28,6d6db27,19298bb.'
- 'TARGETING: target_delay_ticks (delay N ticks before engaging), target_condition="weakest"
  (focus-fire fewest-troops), target_condition="in_range:N" (hold until within N cells),
  order_target_idx (direct index). All committed 3faa2b4bc8.'
- 'LEDGER: tests/sim_verification_ledger.json committed (3778d6e6) — bootstrap must
  fetch to /home/claude/sim_verification_ledger.json for future orchestration.py commits.'
- 'NULL: no GappedLine/Horseshoe shape defect. Single-subunit shapes close at every
  param. Prior "non-closing" was the discipline-speed cliff, not shape geometry.'
- 'DISCIPLINE=COHESION (PP-232, mass_battle_v30 L7). Decoupled from speed (afdabadf):
  actual_speed=base_speed+stance_mod, step=base+stance_mod. disc_mult kept ONLY for
  node cohesion factor (L632) and wheel rate (L624). Speed is discipline-independent.
  Counters preserved.'
- 'DISCIPLINE 4-ROLE STATUS: morale-drop WIRED, cohesion-loss WIRED, cell-regroup
  EMPTY HOOK (reform_check), cell-support PARTIAL. Regroup is the open design item.'
context_files:
- path: designs/audit/2026-06-02-combat-engine/ratification_and_deprecation_proposal.md
  depth: full
  reason: 'A: ratification checklist+deprecation+validation'
- path: designs/scene/combat_engine_v1/
  depth: full
  reason: 'A: the engine'
- path: designs/audit/2026-05-29-combat-armature/martial_traditions_synthesis.md
  depth: full
  reason: 'A: this-session synthesis (fe8d9dfc); D-alpha..delta open'
- path: designs/proposals/pc_formation_system.md
  depth: full
  reason: 'B: P-C capture, S3.5 roles/instructions, §11 counter-cycle log'
- path: tests/sim/mass_battle/orchestration.py
  depth: full
  reason: 'B: _lanchester_strength(1105-1118)+call sites(1374-1375)'
- path: tests/sim/mass_battle/percell.py
  depth: full
  reason: 'B: build_column_grid (no cap) — cap-derived footprint lands here'
- path: references/lane_assignments.yaml
  depth: full
  reason: 'C/D: lane owns/range/protocol'
- path: references/id_reservations.yaml
  depth: full
  reason: 'C/D: reserved ID blocks'
- path: references/historical/precedents_warfare.md
  depth: full
  reason: 'B: §1 canonical counters + command-dominates + Cannae=command (the validation
    targets)'
- path: tests/sim/mass_battle/geometry.py
  depth: skeleton
  reason: 'B: Column shape (_SHAPE_BUILD/CELL_PATTERN_FN/column_cells), cell_speed,
    footprint_for'
- path: tests/sim/mass_battle/orchestration.py
  depth: skeleton
  reason: 'B: resolve_engagements (sigma head = breakthrough-primitive site), _lanchester_strength,
    node-cohesion _node_advance'
created: '2026-06-04T04:45:50.955649+00:00'
updated: '2026-06-06T02:05:56.167673+00:00'
status: active

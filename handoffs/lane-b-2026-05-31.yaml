id: lane-b-2026-05-31
scope: infrastructure
task:
  skill: valoria-orchestrator
  description: 'Lane B (Infra/Tooling/Governance). Done: enablers, 5.5, 5.6, 7.2 hook
    tests, 5.2 (6 docs), 5.8 scope-vocab safe core, 1.10b CI fix + roadmap note, 1.18
    retire atoms_pending (381 files). Remaining autonomous: INDEX-REFRESH+generator-fix
    (2 defects, focused), check_handoff_conflicts glob-bug fix, propagation_map owns-narrow,
    7.2-followup, 1.20. HELD: 5.8-reduction (3 lanes active), 1.3. Jordan-gated: 1.17,
    CI arch corrections, roadmap reconcile. See working_state.'
owns:
- skills/valoria-orchestrator/scripts/**
- references/**
- tests/hooks/**
- tests/index/**
- session_logs/**
- tools/**
- archives/**
- skills/valoria-orchestrator/references/skill_registry.yaml
context_files:
- path: references/lane_assignments.yaml
  depth: full
  reason: Lane B owns; needs propagation_map owns-narrow (Lane A finding).
- path: references/scope_vocabulary.md
  depth: full
  reason: 5.8 canonical vocab + reduction proposal.
- path: references/subsystems/ci_subsystem.md
  depth: full
  reason: 5.2; CI drift findings for Jordan-applied arch.
- path: references/id_reservations.yaml
  depth: full
  reason: Reserved ID blocks; parked 5.10 spec.
- path: skills/valoria-orchestrator/scripts/valoria_hooks.py
  depth: skeleton
  reason: Gate chain; COMMIT_FORMAT L73 / TASK_REQUIRED_FILES L84; FORBIDDEN_TOKEN_EXEMPT_PATHS.
- path: skills/valoria-orchestrator/scripts/github_ops.py
  depth: skeleton
  reason: SESSION_SCOPES L936; check_handoff_conflicts (glob bug); append_to_register
    L827 (5.10); bootstrap session_paths.
working_state:
  done:
  - 5.11+5.10 enablers; 5.5 freshness; 5.6 compliance. b5d5f5fb2c736047724bede59287318c8e519af8/ad98144d258f4597dec8c83eee5e12af2f657ed6/c6465ea16f3b4bd5c67f4d52ada317e6d930578d.
  - 7.2 hook tests (33). 772dfbb4b2c851c6dad4c9efd50065246c77ee98. | 5.2 all 6 subsystem
    docs. 5d34727dd0d38657572f1373cebb08fdee69f6be.
  - 5.8 scope-vocab single-source + drift-guard (safe core). ecef73676665b231520a2dd217bf0e2b3ffbd1f5.
  - 1.10b CI active-wiring fix + roadmap Phase-2 progress_note. 665b8f7b6c44dee381169511a79ab201612a8042.
  - 1.18 RETIRE references/atoms_pending/2026-04-25 — 381 files deleted, 0 remaining,
    recoverable via git. fb024547ebff95af176a3cd64ce8f8ea8df3474c.
  - '1.17 (PARTIAL): cleanup/remove-deprecated DELETED; 2 branches FLAGGED not-deleted
    (investigations).'
  next:
  - 'INDEX REFRESH + GENERATOR FIX [focused task — do NOT ship piecemeal]: regenerate_file_index.py
    has TWO defects. (1) __main__ unreachable: the argparse/run/print entrypoint sits
    as DEAD CODE after run_fast()''s return (no `if __name__==main` guard) -> `python3
    tools/regenerate_file_index.py ...` is a NO-OP. (2) CURATED-DATA LOSS: the entrypoint
    builds schema_sql = existing-index lines minus ALL ''INSERT'' lines -> strips
    the curated tables too; a full run() then dumps EMPTY concepts(82)/aliases(334)/concept_files(231)
    = destroys 647 authored rows. Confirmed: committed valoria_index.sql = schema
    + curated tables ONLY (files/entities are runtime-rebuilt, NEVER committed — dump_to_sql
    include_generated=False by design). CORRECT FIX: hoist entrypoint to `if __name__==main`,
    AND make schema/seed load CREATE-TABLEs PLUS curated INSERTs (concepts/aliases/concept_files/meta)
    into the DB before run(), so the dump preserves them. Ship WITH a test (assert
    curated rowcounts preserved). Also drops the 1 dangling concept_files row (atoms_pending)
    as a side effect.'
  - 'check_handoff_conflicts GLOB BUG [Lane B, github_ops — from Lane A]: fnmatch(proposed,
    pattern) returns false-empty when `proposed` is itself a glob (missed the faction
    overlap). Fix: prefix-component region-overlap + strip annotations + check BOTH
    directions. Add tests/hooks (folds into 7.2). NOTE: my session''s checks used
    CONCRETE paths -> were valid; the bug bites glob-vs-glob.'
  - 'propagation_map OWNS-NARROW [Lane B, lane_assignments.yaml — from Lane A]: references/propagation_map.md
    is double-claimed (Lane A explicit via co-file rule vs lane-b references/**).
    Move it to lane-b shared_not_exclusive; co-file rule -> Lane A owns it.'
  - '7.2-FOLLOWUP [big]: network-gated gate tests (safe_commit, task_gate, assert_bootstrap,
    context_gate, co-file). Mock github_ops or throwaway branch.'
  - '1.20 [scope first]: inline-marker sweep in Lane B owns.'
  - '5.10-WIRING [PARKED]: allocate_id + co-commit + test; read append_to_register
    (github_ops L827) first.'
  - 'COMPLIANCE: 1 non-blocking bootstrap warning persists — run compliance_check
    verbose to find the file/rule.'
  - 'ROADMAP structural reconcile [integration/close]: items_total 11->6 (1.19b),
    current_phase advance.'
  held:
  - '5.8-REDUCTION [HOLD: A+C active]: removes live COMMIT_FORMAT scopes (shared gate)
    -> breaks other lanes. THREE lanes now active -> hold firmly. Spec in scope_vocabulary.md.'
  - '1.3 [HOLD: out of owns]: 06_B6_fix.md under designs/audit/ -> Lane A.'
  - '1.15-ACTION [HOLD: partly canon-side]: editorial-archive consolidation touches
    deprecated/canon/.'
  investigations:
  - '1.17-DISPOSITION (Jordan): claude/refresh-state-3m7nL = 17 SUBSTANTIVE commits
    (ED-717/721/722/723-728 closures w/ editorial permission, engine_v4 proposal,
    ED-721/722 sim re-run, index regens). Confirm captured/superseded in main''s editorial_ledger.jsonl
    before delete. mb-sim-foundation = 1 commit (sim/** = Lane C) -> Lane C/Jordan.'
  - '1.15-FINDING: editorial archives across archives/editorial/ + archives/editorials/
    (typo-twin, both exist) + deprecated/canon/. editorials->editorial merge is Lane
    B; deprecated/canon/ canon-side.'
  - 'CONTEXT-GATE LESSON: a full index regen (run(full=True), 1476-file fetch + multi-MB
    valoria.db) inflates the on-disk fetch estimate to ~7M -> context_gate hard-stops.
    FIX: rm /home/claude/valoria.db + scratch idxrepo + .valoria_cache.json + .freshness_cache.json,
    re-bootstrap (estimate returns ~13k). NOT real conversation context (1M model).'
key_values:
- 'Reserved-block ID maxima: ED 884 (<890), PP 726 (<727).'
- 'valoria_index.sql is SELECTIVE by design: committed = schema + curated tables (meta
  8, concepts 82, aliases 334, concept_files 231). files/entities = runtime-only,
  rebuilt each session, NEVER committed (dump_to_sql include_generated=False). A naive
  full-regen-then-commit DESTROYS the 647 curated rows.'
- 'regenerate_file_index.py: run() is self-contained (fetches tree + all files + proper_noun_registry.yaml
  via GraphQL); writes via write_sql_to for caller to safe_commit; runtime DB /home/claude/valoria.db
  never committed; ~78 batches/154s for full. CLI entrypoint currently a no-op (defect
  above).'
- 5.8 canonical vocab = 17-member union; test_scope_vocabulary.py is the drift guard.
- '1.10b: CI THRESHOLDS fixed. Most other deprecated-name hits are correct historical
  mentions; github_ops bootstrap session_paths still lists deprecated summaries (shared-bootstrap,
  non-parallel-window).'
- 'tests/hooks: 3 files (placeholder_names, correctness_gates 33, scope_vocabulary
  6). pip install pytest --break-system-packages.'
- THREE lanes now active (A reduced-scope canon/designs, B infra, C sim). Collisions
  on commit are routine -> use re-fetch+retry; shared-enforcement edits (5.8-reduction,
  5.3) held firmly.
blockers:
- 'JORDAN-VERIFY (1.17): 17 substantive commits + 1 sim commit. Confirm superseded
  before deletion.'
- 'CROSS-LANE from Lane A: (1) check_handoff_conflicts glob bug (Lane B fix, github_ops
  + 7.2 test). (2) propagation_map.md double-claim -> narrow lane-b owns. (3) Lane
  A P1 resolver-ratification dispute (ED-883/865/866) — faction/Jordan, not Lane B.'
- 'NON-PARALLEL-WINDOW gated: 5.3 (canonical_sources SHA-split) + 5.8-reduction (COMMIT_FORMAT).
  Three lanes active -> do not run.'
- 'JORDAN-APPLIED: PI/arch text — CI drift corrections, M3/M4, 5.8 merge calls, arch
  atoms_pending count.'
last_commit: fb024547ebff95af176a3cd64ce8f8ea8df3474c
created: '2026-05-31T23:56:26.070814+00:00'
updated: '2026-05-31T23:56:26.070814+00:00'
status: active

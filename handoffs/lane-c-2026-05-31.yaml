id: lane-c-2026-05-31
scope: simulation
task:
  skill: valoria-simulator
  description: Lane C (Simulation/Implementation), 3-session write-disjoint model
    (workplan v2 2026-05-31). Queue 6.1->6.3->6.4->6.5->8.1->1.16. 6.1+6.4 DONE; 6.3
    IN PROGRESS — 3 of 11 implementable stubs shipped (parliamentary_vote, parliamentary_stay,
    parliamentary_transfer). Lane C writes sim/**+tests/** only -> write-disjoint
    from Lane A (canon/designs) and Lane B (infra). All 3 lanes active.
owns:
- sim/**
- tests/sim/**
- tests/coverage_matrix.md
- valoria-game/**
- tests/sim/HANDOFF_*
context_files:
- path: references/lane_assignments.yaml
  depth: full
  reason: Lane C owns/range/entry-items + protocol.
- path: references/id_reservations.yaml
  depth: full
  reason: Lane C reserved ED 970-999/PP 780-799 (none allocated; the 3 parliamentary
    stubs used no ED/PP).
- path: designs/proposals/stub_infill_plan.md
  depth: full
  reason: 'Tiering authority. designs/proposals now Lane-A-owned -> READ-ONLY. NOTE:
    plan dep-list is imprecise on canon location (verify per-stub against the stub''s
    own docstring ''Canon source:'').'
- path: designs/scene/social_contest_v30.md
  depth: full
  reason: §10 vote + §10.1 stay (used by all 3 parliamentary stubs). designs/scene
    NOT Lane-A reduced scope -> safe.
- path: skills/valoria-simulator/SKILL.md
  depth: skeleton
  reason: Mode-G; sim_gate run-grounding NOT a commit gate.
working_state:
  completed:
  - '6.1 DONE — true stub state: 20 sim/ stubs remain (was 22 @4ee54f28; my 2 personal
    stubs shipped). NOTE: stub list re-verified against real tree (the plan''s ''parliamentary_transfer''
    is sim/PROVINCIAL/ not personal/; treaty.py is an intentional guard not a stub).'
  - 6.4 DONE — tribunal.py generic run_tribunal RAISES LOUDLY; mc_v18 uses §7.1; gated
    on Phase-5 Contest not canon.
  - 6.3 parliamentary_vote §10 COMMITTED d0f3c0bd — faction-scale BG Mandate-pool
    vote. 11 ledger entries. Confirmed bug-free (60-seed sweep).
  - 6.3 parliamentary_stay §10.1 COMMITTED 251c5528 — Senator Inward Tribunal-suspension
    via §10 vote; CI<55 gate. 3 entries. Verified (sweep + season-gating).
  - '6.3 parliamentary_transfer §§1-4 COMMITTED 8feb7526 — CB-gated territorial transfer
    wrapped in §10 vote (parliamentary_transfer_v30, v12c N=1000). Pool=Influence(±1D
    vote)/Ob=Legitimacy+2; Continuous-Engine degree -> §1.2 (transfer+Accord / Partial-CB
    / Failure Sta-1+L+1, mode-adjusted). 4 modes, 8 CB sources, §1.3 protections.
    Applies mutations + returns result (crown_initiative convention). NOT P1-gated.
    7 entries (125 total). Verified: 4 blocks + 80-seed sweep (24/41/15) + transfer
    mutation + mode effects.'
  - 6.3 articulation RECLASSIFIED — NOT buildable as a sim stub. articulation_layer_v30
    (PP-688) consumes the PP-687 Key substrate (Key log, typed Keys w/ salience+cascade_fidelity_history+symbolic_dimensions+scale_signature,
    CAUSAL_GRAPH, Concern queue, Memory API) — ALL absent from sim (0 files each).
    Also a UI/narrative layer (Tier 1 Lens / Tier 2 cut scenes / Tier 3 Chronicle
    — 'Phase 5a Godot scope'), not a balance mechanic. The stub's declared deps (knots+beliefs)
    drastically understate the canon's real deps. BLOCKED on PP-687 substrate + belongs
    to valoria-game/Godot UI. Do NOT build hollow.
  - '6.3 investigation/fieldwork RECLASSIFY-PENDING — prior ''verified buildable''
    was a WRONG substrate check (grepped articulation''s Key-log, not investigation''s
    Ontological Ledger). investigation_systems_v30 (457L read full) is substantially
    a PLAYER-FACING dialogue/narrative UI system: 7-gate Dialogue Lattice (gates on
    PC Certainty/TS/Histories — only PARTIALLY in sim) + 5-filter Response Matrix
    + Case Board + scene graphs (Godot/UI scope) + 5 EDITORIAL content gaps (utterances/NPC-stances/scene-content
    = Jordan creative layer, not authorable by Claude). A balance-sim build of the
    gates/filters = hollow skeleton. fieldwork_v30 (899L) UNREAD — the run_fieldwork_scene/advance_evidence/advance_disposition
    MECHANICAL core (Evidence Track clock + Disposition + dice scene resolution) MIGHT
    be a buildable subset, but the investigation<->fieldwork cycle + editorial gaps
    + UI scope make the pair questionable.'
  next:
  - 'PATTERN FINDING (2 of 2 remaining ''ready'' stubs reclassified): the 3 shipped
    (parliamentary family) were the genuinely-mechanical FACTION-scale stubs. The
    remaining unimplemented sim stubs appear to be predominantly PERSONAL-scale narrative/dialogue/UI/threadwork
    systems (articulation, investigation, dialogue, companion arcs, rendering/threadwork)
    that the FACTION-BALANCE sim (mc_v18) does not model + does not need for balance
    — they are Godot/UX-layer + editorial-content. The ''11 implementable'' (6.1)
    was over-optimistic; the genuinely-balance-relevant mechanical stubs are largely
    done.'
  - 'RECOMMENDATION (reaffirmed, stronger): PIVOT to the determinism+migration batch
    (Path B) — the higher-value remaining SIM work. massbattle.py L630/L1053 random.randint
    -> world.rng + 3 canonical-pipeline subs (accounting._ci_generation->ci_track,
    ._ms_decay->ms_track, mc_v18 season-loop->season.run_season). Behavior-shifting,
    one re-baseline. Unblocks 6.5 trustworthy balance. Needs Jordan go (moves balance
    numbers).'
  - 'ALT if continuing stub-infill: read fieldwork_v30 (899L) full to determine if
    its MECHANICAL core (Evidence Track + Disposition + dice scene resolution) is
    buildable as a subset independent of the investigation dialogue-UI — uncertain
    given the cycle + editorial gaps. OR Jordan SCOPE CALL: confirm the personal/narrative/dialogue/UI
    stubs are valoria-game/Godot scope (not balance-sim), and close them out of the
    Lane-C sim queue.'
  - 'Other remaining stubs unchanged: rendering+rs_track (threadwork, likely same
    UI/narrative pattern — verify), miraculous_event (dep rendering), 7 hard-gated
    (Lane-A/Jordan canon), Varfell (P1-resolver-gated).'
  cross_lane:
  - All 3 lanes active. Lane C write-disjoint (sim/+tests/ only). Lane B notes non-parallel-window
    items held while 3 lanes active (not Lane C's concern).
  - 'Lane A P1 CANON-INTEGRITY (ED-883/865/866): Domain Action resolver ratification
    disputed. Gates CANON-GATED stubs (7 hard-gated + Varfell) + faction Domain Action
    layer. Does NOT gate the 3 parliamentary stubs shipped (Mandate-pool/Influence-vs-Legitimacy
    via core engine, independent of disputed resolver). Jordan adjudication + Track
    1.7 needed.'
  canon_2_reeval:
  - Varfell EXISTS (varfell_path_b_v30) -> varfell_mandate/territorial_acquisition
    candidate-unblocked but ALSO P1-resolver-gated; verify path_b first.
  - 'treaty.propose_treaty = INTENTIONAL GUARD not a stub. 7 STILL HARD-GATED (Lane-A/Jordan
    canon): altonian_reinforcements, charter_liberties, hafenmark_equipment, home_sanctuary,
    infrastructure_reclamation, restoration_movement, npc_ai.'
  cross_cutting_defects:
  - 'PRIORITY: mc_v18 NON-DETERMINISM (UNFIXED) — massbattle.py L630/L1053 random.randint
    bypass world.rng -> balance untrustworthy.'
  - 'NEW: Territory.owner not synced on territorial transfer — BOTH faction_action.py
    (reference) and parliamentary_transfer track ownership via Faction.territories
    only; Territory.owner left stale. Flag whether Territory.owner is authoritative/read
    anywhere -> codebase-wide fix if so.'
  - Deferred Migration Batch (4 subs); per-record serializers for 14 World registries;
    19b canonical_pt/accord bucketing for new stubs.
  notes:
  - sim_gate=RUN-GROUNDING (preset+full-depth+LOCAL ledger), NOT a module-commit gate
    -> skipped for stub-infill.
  - 'Verification discipline: SWEEP SEEDS (single seed misled on parliamentary_stay
    S1). CB economy NOT in game_state -> duck-typed world.casus_belli + auto Crown-restoration.
    BOUNDARY: do NOT write tests/sim/v32-combat-balance/.'
key_values:
- 'Reserved IDs: ED 970-999 next 970; PP 780-799 next 780 (none allocated).'
- Mandate==Faction.L; Influence pool==int(Faction.I); GD-3==parliamentary False; CI==world.clocks['CI'];
  territory ownership==Faction.territories (list); Accord set via ACCORD_MAP[level]
  (1->2.5, 2->4.0); Faction.adjust(stat, n*MULTS[stat]). Clone /home/claude/ttrpg_clone.
- 'Commits this session: parliamentary_vote d0f3c0bd; parliamentary_stay 251c5528;
  parliamentary_transfer 8feb7526. Ledger 125 entries.'
blockers:
- NONE blocking ungated work (articulation + cyclic pairs ready). Canon-gated stubs
  + Varfell gated by Lane-A P1 resolver dispute. 7 hard-gated need Lane-A/Jordan canon.
last_commit: 8feb752627b2586cdcc46eb6c8429aaa98e1d329
created: '2026-06-01T00:14:00.830306+00:00'
updated: '2026-06-01T00:49:07.293766+00:00'
status: active

id: massbattle-engine-resume
scope: simulation
last_commit: 7e878ade3c45ae171631a1c533a883b0a49e6fed
task:
  skill: valoria-simulator
  description: 'Mass-battle engine. P-A modularize / P-B capture / P-L Lanchester
    DONE. COMMAND-ONLY sigma base DONE (2cf3feb6) + canon record ED-899 (c8ba5b7f)
    + strengthened signature law-exponent guard (7e878ade). NEXT: re-tune gauge bands
    to the new model, then canon-doc reconciliation, then P-C FM formations.'
owns:
- tests/sim/mass_battle/**
- tests/sim/sim_mb_sigma.py
- tests/sim/gauge_mb.py
- tests/coverage_matrix.md
- designs/audit/2026-06-01-massbattle-stub-wiring/**
- designs/provincial/mass_battle_v30.md
context_files:
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_engine_workplan.md
  depth: full
  reason: 'Workplan v2 (943687d3). Master sequence: P-A done, P-B done, P-L next,
    then P-C/P-D/P-E-stub/P-F/P-G. Primary resume doc.'
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_lanchester_design.md
  depth: full
  reason: P-L design spec (81ea569d). Exact term, linear/square split, composition
    with sigma+per-cell, validation+re-tune. Implement directly from this.
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_engine_completeness_audit.md
  depth: full
  reason: Architecture+completeness audit (1a525127). 22 WIRED/8 PARTIAL/6 GAP; MECHANICS
    registry mirrors it.
- path: tests/sim/mass_battle/engine.py
  depth: full
  reason: Package wrapper + MECHANICS registry (27) + mechanics_selftest(). engine.py
    __all__ = UNION of all module __all__ (critical for re-export through shim).
- path: tests/sim/mass_battle/orchestration.py
  depth: skeleton
  reason: 'MELEE casualty term to change for P-L: resolve_engagements dmg_a += CASUALTY_SCALE*max(0,
    DAMAGE_BY_DEGREE[b_deg](unit_b.power)-unit_a.dr); volley_phase = square-law seam;
    hp application sites.'
- path: tests/sim/mass_battle/percell.py
  depth: skeleton
  reason: 'Frontage cap (linear-law mechanism): _engaged_cols + distribute_casualties
    give engaged-column strength. P-L melee term reads engaged enemy strength here.'
- path: tests/sim/mass_battle/config.py
  depth: skeleton
  reason: Constants + DAMAGE_BY_DEGREE + CASUALTY_SCALE(->k_linear). Every module
    has explicit __all__ (import* skips _names).
- path: tests/coverage_matrix.md
  depth: full
  reason: Co-file REQUIRED in every tests/sim/ commit. Rows incl P-A stages 1-5 +
    P-B.
- path: designs/provincial/mass_battle_v30_index.md
  depth: skeleton
  reason: Canon index (skeleton). Full doc designs/provincial/mass_battle_v30.md;
    ED-898 death->capture reframe landed @00eedf9d at A.5/A.4/D.2. Lanchester is an
    engine substrate, not canon.
- path: tests/sim/mass_battle/lanchester_signature.py
  depth: full
  reason: P-L signature regression tests (linear/square/no-annihilation); run PYTHONPATH=tests/sim
    python3 -m mass_battle.lanchester_signature. All PASS PER_CELL 0 and 1.
working_state:
  done:
  - 'COMMAND-ONLY SIGMA BASE COMMITTED 2cf3feb6 (config.py + orchestration.py + coverage).
    Per Jordan canon-structure directive: canonical Size-based pool (min(Size,Command)+Command,
    PP-233/derived_stats L276) SET ASIDE. base_combat_pool = COMMAND_POOL_MULT(2)*command
    + pen + stam_pen when COMMAND_SIGMA_ENABLED (default ON; OFF = canonical pool,
    BYTE-EXACT to committed verified via ISOLATED run — note: cwd /home/claude shadows
    mass_battle, so run committed refs with cwd INSIDE the package dir). Size enters
    outcomes ONLY via the Lanchester frontage term.'
  - 'Command DERIVED: derive_command(cha,cog)=clamp(round((CMD_CHA_WEIGHT*cha+CMD_COG_WEIGHT*cog)/(sum)),1,7)
    = clamp(round((2*Cha+Cog)/3),1,7); Cha primary, Cog secondary. Unit gains optional
    charisma/cognition fields (default None); __post_init__ derives command when both
    supplied (else explicit command stat). Gauge units pass command directly -> derivation
    inert there.'
  - 'Constants (config.py, class-B sim-tunable, Jordan-confirmed 2026-06-02): COMMAND_SIGMA_ENABLED(env,default
    ON), COMMAND_POOL_MULT=2, CMD_CHA_WEIGHT=2, CMD_COG_WEIGHT=1.'
  - 'ED-899 recorded (c8ba5b7f, editorial_ledger.jsonl): canon-structure decision.
    NEXT free ED id now 900.'
  - 'Signature test STRENGTHENED 7e878ade: added check_law_exponents() (no-rout conserved-quantity
    fit) — melee must conserve A-B (p<=1.4 LINEAR), volley A^2-B^2 (p>=1.6 SQUARE).
    PASSES under Command-only (melee 1.25/volley 2.00); FAILS under Size-based pool
    (melee 1.65) — catches the contamination the old win%/cas-diff checks missed.'
  - 'VALIDATION COMPLETE (the coefficient-confirmation Jordan asked for): conserved-quantity
    exponent (no-rout primitive, OR-literature method): Size-based pool contaminated
    melee to p~1.7; Command-only base restores melee to TRUE LINEAR p~1.0 (CV@1 0.01),
    volley stays SQUARE p~2.0 (CV@2 0.002). Coefficient-INVARIANT (melee~1/volley
    2 across K_LINEAR in {2,4,8}) -> K sets timescale NOT law. Magnitude historical:
    melee 2:1 big-win 77%/cas-diff +39 (du Picq winner<5%); no-annihilation rout-decided
    (mirror ~30%, loser ~70% hp); square concentration 17.8x (Cannae/Pharsalus). Coefficients
    K_LINEAR=4/K_SQUARE=0.25/LANCHESTER_STRENGTH_REF=4 CONFIRMED, contingent on the
    Command-only base.'
  - 'NO-REGRESSION on historically-validated behaviors (new-model PER_CELL=1 battery):
    charge C1(58)/C2 braced(18)/C3(55)/C4 envelopment(80) + H4 Cannae(40/55) ALL IN
    BAND; H1 mirror now 45/52 BALANCED (old ~57-60% A-side bias FIXED). Multi 12/20
    (vs old 14/20) = band-calibration artifact (bands fit to old pool), NOT physics
    regression.'
  next:
  - 'RE-TUNE gauge bands to the Command-only model (class-B/M, were calibrated to
    the old Size-based pool). OUT under new model: H5/H6/H7/H8/H11 (formation), R1/R3
    (ranged), C5 (cav-vs-shaken 98% — band 58-90 likely TOO LOW; top-down a shaken
    line charged by cavalry routs -> widen). Re-characterize each band top-down (du
    Picq/precedent), do NOT overfit to noise; PER_CELL=1 battery ~200s (timeout 285,
    ONE slow run/block). gauge_mb.py bands are Class-B/M, Jordan-vetoable.'
  - 'CANON-DOC RECONCILIATION (ED-899 follow-up): update params/mass_combat.md (PP-233
    pool) + derived_stats_v30.md (Command derivation) to match the engine (Command-only
    base; Command=f(Cha-primary,Cog-secondary)). Engine leads, docs lag. Needs Jordan
    structural sign-off on doc wording (canon edit -> [editorial] scope + canonical_sources
    co-file + the ED-899 link).'
  - THEN P-C FM compositional formation system (original workplan next; XL; design-capture
    prereq ED-814 allocation grid / FM instructions into a canon design doc first).
  - 'OPEN: melee 4v2 exponent dips to ~0.85 (discreteness at tiny pools; unambiguously
    linear, CV@1<<CV@2; cosmetic). Magnitude grounding partly inherited from the ~30%-rout
    calibration (gameplay threshold runs hotter than the historical ~10-15% stand)
    — acceptable but noted.'
status: active
key_values: []
blockers: []
created: '2026-06-02T20:41:40.600485+00:00'
updated: '2026-06-02T20:41:40.600485+00:00'

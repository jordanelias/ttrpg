id: massbattle-modularize-lanchester-resume
scope: simulation
last_commit: 81ea569d88bb6fe676d36e99cf761ec5ed47eb05
task:
  skill: valoria-simulator
  description: 'Mass-battle engine: P-A modularization COMPLETE + P-B general-death->capture
    COMPLETE; P-L Lanchester attrition substrate SPECCED (next to implement). Resume
    from workplan v2 (943687d3) and the Lanchester design spec (81ea569d).'
owns:
- tests/sim/mass_battle/**
- tests/sim/sim_mb_sigma.py
- tests/sim/gauge_mb.py
- tests/coverage_matrix.md
- designs/audit/2026-06-01-massbattle-stub-wiring/**
- designs/provincial/mass_battle_v30.md
context_files:
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_engine_workplan.md
  depth: full
  reason: 'Workplan v2 (943687d3). Master sequence: P-A done, P-B done, P-L next,
    then P-C/P-D/P-E-stub/P-F/P-G. Primary resume doc.'
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_lanchester_design.md
  depth: full
  reason: P-L design spec (81ea569d). Exact term, linear/square split, composition
    with sigma+per-cell, validation+re-tune. Implement directly from this.
- path: designs/audit/2026-06-01-massbattle-stub-wiring/mb_engine_completeness_audit.md
  depth: full
  reason: Architecture+completeness audit (1a525127). 22 WIRED/8 PARTIAL/6 GAP; MECHANICS
    registry mirrors it.
- path: tests/sim/mass_battle/engine.py
  depth: full
  reason: Package wrapper + MECHANICS registry (27) + mechanics_selftest(). engine.py
    __all__ = UNION of all module __all__ (critical for re-export through shim).
- path: tests/sim/mass_battle/orchestration.py
  depth: skeleton
  reason: 'MELEE casualty term to change for P-L: resolve_engagements dmg_a += CASUALTY_SCALE*max(0,
    DAMAGE_BY_DEGREE[b_deg](unit_b.power)-unit_a.dr); volley_phase = square-law seam;
    hp application sites.'
- path: tests/sim/mass_battle/percell.py
  depth: skeleton
  reason: 'Frontage cap (linear-law mechanism): _engaged_cols + distribute_casualties
    give engaged-column strength. P-L melee term reads engaged enemy strength here.'
- path: tests/sim/mass_battle/config.py
  depth: skeleton
  reason: Constants + DAMAGE_BY_DEGREE + CASUALTY_SCALE(->k_linear). Every module
    has explicit __all__ (import* skips _names).
- path: tests/coverage_matrix.md
  depth: full
  reason: Co-file REQUIRED in every tests/sim/ commit. Rows incl P-A stages 1-5 +
    P-B.
- path: designs/provincial/mass_battle_v30.md
  depth: skeleton
  reason: Canon. ED-898 reframe (death->capture) landed @00eedf9d at A.5/A.4/D.2.
    Lanchester is an engine substrate, not canon.
working_state:
  done:
  - 'ENV: repo jordanelias/ttrpg. Bootstrap each bash subprocess: sys.path.insert(0,''/home/claude'');
    from github_ops import quick_bootstrap; g,h,files,token=quick_bootstrap(). PAT
    /mnt/project/VALORIA_PAT; scripts cached /home/claude/. No other active sessions.'
  - 'P-A MODULARIZATION COMPLETE (5 commits): 4d073403 config -> 720fdbb2 geometry
    -> c5fc0903 percell -> 6e26dd76 resolution -> e60fd184 orchestration+engine.py+MECHANICS+shim.
    Engine = package tests/sim/mass_battle/{config,geometry,percell,resolution,orchestration,engine}.py;
    sim_mb_sigma.py = 5-line shim `from mass_battle.engine import *`; gauge_mb.py
    exec()s shim unchanged. engine.py: MECHANICS registry (27) + mechanics_selftest()
    PASS. EVERY stage PER_CELL=0 BYTE-EXACT to 7cfa44d5; PER_CELL=1 clean, C1-C4 in
    band. Behaviour frozen.'
  - 'P-A GOTCHAS (reuse): (1) `from X import *` SKIPS underscore names -> every module
    needs explicit __all__ (incl _helpers); engine.py __all__ = UNION of all modules''
    __all__ or config consts (SIDE_A_START_ROW) won''t re-export via shim. (2) Re-grep
    def/class line numbers after EACH extraction (they shift). (3) Verify: PER_CELL=0
    python3 gauge_mb.py <engine>, diff vs /tmp baseline, normalize echoed filename
    `sed ''s/_x.py/sim_mb_sigma.py/g''`.'
  - 'P-B GENERAL-DEATH->CAPTURE COMPLETE (2 commits): 00eedf9d canon reframe (ED-898)
    + d35f3468 engine comment relabel. mass_battle_v30.md A.5 two-stage death->incapacitation
    (Stage2 captured if field lost, general SURVIVES; -2 morale/Command=0/uncommanded
    UNCHANGED), A.4 triggers, D.2 officer death->incapacitation/capture (+ companion
    combat-death->battlefield capture), A.7/A.8 incidental phrasings. Canonical ''No
    character death from mass battle'' clause added: engine emits ''incapacitated/captured''+side-holding-field;
    consequence (ransom/imprison/escape) WORLD-LAYER, OUT of container (Jordan r2
    -- NOT designed). Engine relabel comment-only (Command<=0->rout already matched).
    Magnitudes/timing unchanged.'
  - 'P-L LANCHESTER SPECCED (81ea569d), NOT implemented. Bottom-up: melee = degree
    x Power - DR, NO force-on-force term (orchestration resolve_engagements ~L1258-59);
    numbers enter only via pool; volley separate (square seam). Top-down: linear law=ancient
    melee/frontage-capped (per-cell _engaged_cols provides the cap), square law=ranged/volley.
    JORDAN CONFIRMED SPLIT (linear melee / square volley; NOT blended 1.5).'
  - 'CONFIRMED: P-A split done. Capture consequence OUT of container. Terrain (P-E)=STUB
    only. Phase-3 cavalry-shock 9 PC_SHOCK_* magnitudes RATIFIED. FM formation system
    (P-C) GROUNDED in prior conversation (ED-814 ''adjustable allocation grid, FM
    analogy exact'' + SI/FM architecture pivot + team-instruction layer) via conversation_search;
    NOT in canon/ledger yet -> design-capture is P-C prerequisite.'
  - 'COMMIT MECHANICS: safe_commit(additions:list[(path,content)], deletions, message)
    in ONE bash block. Scopes = editorial|patch|simulation|compilation|infrastructure|skill|cleanup|godot|phase|fix|bugfix
    (''audit'' is a task_gate type, NOT a commit scope -> [editorial] for audit/design
    docs). Co-files: ANY tests/sim/** commit REQUIRES tests/coverage_matrix.md; ANY
    designs/*_v30.md edit REQUIRES references/canonical_sources.yaml (UPDATE git-blob
    SHA = sha1(b''blob ''+len+b''\x00''+content)). sim_fabrication_check scans tests/sim/**/*.py
    for uncited numeric literals -> need `# [canonical: ...]` comment OR value in
    LOCAL /home/claude/sim_verification_ledger.json (45 entries, value-based; exempt
    0,1,2,10,100; leading-zero decimals mismatch int ledger -> use comment). CollisionError
    = NORMAL (own commits move HEAD) -> re-fetch touched paths, confirm orthogonal,
    retry. Next free editorial id = ED-899 (898 used).'
  - 'GAUGE: PER_CELL=0 python3 gauge_mb.py sim_mb_sigma.py = fast full battery (baseline
    single 0/13 multi 5/13). PER_CELL=1 slow ~185-250s -> use `timeout 280`. C-rows
    C1-C7 (cavalry/charge) are PER_CELL=1-only. Baseline /tmp/refbase_pc0.txt regenerable
    from committed shim (byte-exact to 7cfa44d5).'
  next:
  - 'P-L IMPLEMENT (next; behaviour CHANGE, gauge-gated) from 81ea569d: (a) melee
    LINEAR term in orchestration resolve_engagements -- scale casualties by enemy
    strength IN CONTACT (engaged-column strength from percell), keep DAMAGE_BY_DEGREE
    as per-soldier quality; CASUALTY_SCALE->k_linear. (b) volley SQUARE term in volley_phase
    -- effectiveness ~ shooter count (cap lifted); add k_square. (c) gate behind LANCHESTER_ENABLED
    (OFF reproduces flat-scale for A/B). (d) PER_CELL=0 fallback (frontage proxy from
    effective_size). VALIDATE: re-tune k_linear/k_square to HOLD all bands (H1-R3
    multi + C1-C7); add linear-signature row (equal quality, unequal size -> survivors
    ~ size diff) + square-signature row (volley concentration advantage); no-annihilation
    invariant (rout ~30%, not Size->0); PER_CELL=0 AND =1. Ledger k_linear/k_square
    (Class-B). Commit [simulation] + coverage co-file.'
  - 'P-C FM FORMATION SYSTEM (after P-L; XL). PREREQ: design-capture ED-814/FM design
    from conversation into canon (conversation_search ''ED-814 allocation grid'' +
    ''football manager formation instructions''). Three levels: army doctrine (Aggression/Cohesion-priority)
    -> unit formation (default template OR custom % allocation, auto-distribute +
    manual override) -> subunit role/subformation + standing instructions. Subsumes
    A.6/A.8 formation/tactic GAPs (templates carry canonical dice rules). Build incrementally
    in new formations.py/tactics.py; gauge-validate each increment vs A.6/A.8 counter-logic
    + precedent.'
  - P-D two-stage general EVENT (capture-only, on existing command/morale machinery;
    consequence out-of-container). P-E terrain STUB (reserve seam, build nothing).
    P-F speed-into-combat S1 + vectorized penetration S4 (_defender_depth->_support_along_vector).
    P-G cleanup/retire S7/S8/S9 + deep formula-consistency audit (valoria-mechanic-audit
    over modular engine; line-check 8 PARTIAL rows vs canon).
  - 'OPEN CONFIRMS: P-L coefficient VALUES (Class-B, sim-tuned at impl, surfaced for
    veto). P-C template/instruction catalog + sigma-magnitudes.'
created: '2026-06-02T05:47:51.240961+00:00'
updated: '2026-06-02T05:47:51.240961+00:00'
status: active
key_values: []
blockers: []

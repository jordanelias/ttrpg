id: massbattle-troop-types-roles
task:
  skill: valoria-simulator
  description: 'Develop historical troop types + their roles, wired into the P-C compositional
    formation system. Turn the troop-type/role research into: (1) revised config.TROOP_TYPE_ROLES
    (FM position->role gating); (2) ROLE_SPEC role->(shape+instructions); (3) the
    instruction->primitive modulation (brace->+density, skirmish->-density, charge->concentration),
    wired so each role effect EMERGES from primitives (NO flat bonuses), gated by
    Discipline, conditioned by troop type. Validate every role effect on the gauge
    before committing; keep toggle-OFF byte-exact.'
scope: simulation
context_files:
- path: designs/proposals/pc_formation_system.md
  depth: full
  reason: 'the P-C design-capture: 3-level FM surface, Discipline/Command/Morale split,
    S3.5 roles/instructions/troop-type gating + the A/B/C(=C+B) resolution, shapes,
    engine mapping, open decisions'
- path: tests/sim/mass_battle/config.py
  depth: full
  reason: TROOP_TYPE_ROLES scaffold to populate + COMMAND_*/LANCHESTER_* constants
- path: tests/sim/mass_battle/orchestration.py
  depth: full
  reason: Subunit.role/instructions + roles_for/role_allowed gating + command_base_pool
    + base_combat_pool + per-cell primitives instructions will modulate (_defender_depth,
    build_column_grid)
- path: tests/sim/mass_battle/geometry.py
  depth: full
  reason: CELL_PATTERN_FN shapes, cell_speed, _support_along_vector -- geometry roles
    map onto
- path: tests/sim/mass_battle/resolution.py
  depth: skeleton
  reason: _charge_shock_sigma + sigma head -- where charge/brace effects land
- path: tests/sim/mass_battle/engine.py
  depth: skeleton
  reason: MECHANICS registry -- register density/instruction mechanics when wired
- path: designs/provincial/mass_battle_v30.md
  depth: skeleton
  reason: A.5 Command / A.6 formations / A.8 tactics / A.12 rout + canonical troop
    types -- VERIFY (recovered from conversation, not fresh-read)
- path: canon/editorial_ledger.jsonl
  depth: skeleton
  reason: ED-814 allocation surface, ED-815 MIN_DISCIPLINE, ED-899 Command-only base,
    ED-136 stratagem-degrades-Discipline (open)
working_state:
  done:
  - P-C role scaffold + command_base_pool refactor + report-grounded taxonomy (TROOP_TYPE_ROLES
    + ROLE_SPEC + mounted_archers) committed; design doc + handoff in repo
  - 'BRACE MECHANISM committed (7691eb6a): brace instruction engages the charge-shock
    gate w/o hold-stance penalty + reciprocal charge-recoil (PC_CHARGE_RECOIL=6, prep=disc
    x depth). Emergent pike-beats-cavalry, gated by the ''brace'' instruction -> instruction-less
    byte-exact (proven brace ON==OFF, PER_CELL 0+1)'
  - 'Counter-cycle validated top-down (gauge, 40 seeds): pike-beats-cavalry braced+deep+disc
    ~75% / shallow-green ~12% / braced-vs-inf neutral; cavalry-rides-shaken 75%->28%;
    cavalry-catches-archers def 55%->5%'
  - 'Missile-density coupling committed (a6ae38ad): _volley_density_mult scales volley
    casualties by target col_grid density (Carrhae/Agincourt). Correct direction (dense
    bleeds more), small in single-engagement, compounds at standoff scale. Ranged-only
    -> melee gauge byte-exact; volley-exponent signature preserved (p=2.00).'
  - Counter-cycle validation log + measured baseline folded into designs/proposals/pc_formation_system.md
    (§11) and committed — durable record (compaction-safe).
  - 'Arch review #1 committed (051a8d4c): unified discipline/depth preparedness into
    shared _disc_prep/_depth_prep (gate independent, recoil conjunctive). Gate byte-exact
    (proven); 4 edges hold; no re-calibration.'
  next:
  - 'KITING (research #1 troop type + item-3 amplifier): ranged unit faster than target
    maintains range + volleys each turn (vs single volley-then-close); needs run_multi_turn_battle;
    unlocks mounted_archers/Kite. Measure missile-attrition-over-distance; validate
    vs report (Carrhae/Hattin/Mohi horse-archer attrition).'
  - 'Arch-debt (fix when composites/instructions arrive): #2 apply_instructions(subunit)
    dispatch (ROLE_SPEC/role/instructions still INERT); #3 per-atom (not unit-mean)
    brace/density for composites; #4 derive PC_VOLLEY_DENSITY_REF=80 from a canonical
    formation.'
  - 'Dynamic-movement off-grid: guard covers INITIAL placement only; cells moving
    past battlefield edge during resolution is a separate latent concern.'
  - 'Optional record hygiene: restore 0.806/0.800/0.781 EV entries to /home/claude/sim_verification_ledger.json
    (gate already passes via # comment).'
  architecture: 'Three-level FM surface: L1 army doctrine (Aggression/Cohesion-priority)
    | L2 unit formation = SHAPE (arrangement only, NO bonus) | L3 subunit ROLE = troop-type-gated
    INSTRUCTION package. Governance triangle: Command orchestrates (adapt strategy
    / interpret events / reinforce Discipline / rally Morale / stratagems / coordination-cap;
    Command = derived Cha-primary + Cog-secondary) -> Discipline holds shape+tactics
    (MIN_DISCIPLINE adopt-gate; formation breaks at 0) -> Morale sustains (rout resource,
    rallied by Command). EMERGENCE PRINCIPLE (load-bearing): formations grant NO flat
    bonuses; all effects emerge from primitives (frontage/Lanchester, depth-damping,
    support, facing, charge-shock, density). SHAPE_OFF/DEF_MOD retired to make this
    structural.'
  methodology: 'Build geometry/instruction -> MEASURE on the gauge -> iterate until
    the historical intent emerges. SHIELD-WALL LESSON this session: a narrow+deep+braced
    SHAPE did NOT make defense emerge (-1pp vs melee; +3pp vs charge only) -> shield-wall
    must be a brace-INSTRUCTION raising density, not a shape. Do not repeat the shape
    approach.'
  calibration_target: (proposed; Jordan to ratify) A braced heavy-infantry shield
    wall should ~hold its ground vs an equal melee line (parity, not -1pp) AND meaningfully
    blunt a cavalry charge (well above the +3pp brace-only). Tune the density primitive
    to this.
  bootstrap: Standard. Engine = tests/sim/mass_battle/ package + shim sim_mb_sigma.py
    + gauge_mb.py. Run committed/isolated with cwd INSIDE the engine dir (sys.path
    shadowing makes import mass_battle resolve to cwd). PER_CELL=1 enables the per-cell
    layer; PYTHONHASHSEED=0 deterministic.
  completed:
  - 'ARCH-FIX #1 (051a8d4c): unified wall-prep _disc_prep/_depth_prep; gate byte-exact,
    recoil disc-curve unified'
  - 'Pattern-matching audit performed + folded into design doc §12 (5e64c129): provenance
    rule + 5-finding table'
  - 'Audit item 1 FIXED (4248cdd0): §11 anchors Waterloo/Albuera -> report in-period
    Courtrai/Swiss/Hussite, Hastings/Adrianople'
  - 'Audit item 2 VERIFIED: _SIG EV 0.806/0.800/0.781 present in params/core.md at
    TN 6/7/8'
  - 'Audit item 4 VERIFIED: _charge_shock_sigma fires 0x in melee gauge (probe); a_pen=0;
    not load-bearing'
  - 'Subunit construction guard (1e4e6436): __post_init__ validates shape/tier/bounds
    -> clear ValueError (was KeyError/off-grid); byte-exact, selftest 30 + signatures
    4/4 + gauge ON==OFF'
  in_progress:
  - 'Kiting: bottom-up grounding done + code-level build plan folded into design §13.
    Ready to implement (3 coupled changes specced + measurement protocol).'
last_commit: 1e4e643629b6a4d067a11eea120cc99047cea9f2 (Subunit construction guard)
owns:
- tests/sim/mass_battle/*.py
- tests/coverage_matrix.md
- designs/proposals/pc_formation_system.md
session_token: 5697f2ca3775a8ff
key_values:
- Command = round((2*Cha + Cog)/3), Charisma primary (ED-899); COMMAND_SIGMA_ENABLED
  default ON, OFF == canonical Size pool (byte-exact)
- 'Engine grid is UNBOUNDED: BATTLEFIELD_SIZE/UNIT_GRID_SIZE/BUFFER_CELLS nominal/unused;
  only start-rows + cell layouts matter. Wider/shallower grid = allocation surface
  + template proportions, not an engine box.'
- 'Shapes in CELL_PATTERN_FN: Line/Arrowhead/Horseshoe/GappedLine/RefusedFlank. Canonical
  Shield-wall=role (NOT shape); Skirmish/Column/Wedge/Reserve TBD as shapes or roles.'
- role_at_contact (Subunit) is ORPHANED (callers removed with SHAPE retirement) --
  positional-role helper; decide retain-vs-retire when L3 positional roles are designed.
- 'orchestration uses an explicit __all__ at end-of-file: any new public symbol MUST
  be added to it (caught by validation this session).'
- 'Session commits: a5fc9d0e P-L | 2cf3feb6 Command-only | c8ba5b7f ED-899 | 7e878ade
  signature-guard | c13269c4 registry | e677a11f SHAPE-retire | 6d8eb3b7 role-scaffold
  | 7d8e262f command_base_pool.'
- Gauge bands PENDING re-tune to the Command-only model (formation/ranged/cav-vs-shaken
  bands OUT == calibration artifacts, not physics regressions).
- 'COLLISION mechanism: github_ops._fetch_head is PER-PATH (repo_key->HEAD-at-fetch)
  in /home/claude/.valoria_cache.json. write_handoff/atomic_commit advance HEAD but
  do NOT refresh other paths per-path fetch_head, and skip_cache reads do not either
  -> a later safe_commit on an untouched path falsely collides. RESOLVE: verify the
  commit paths are unchanged at live HEAD (refs API), set G._fetch_head["ttrpg:"+path]=HEAD
  for each, then safe_commit. Safe when no other session + no real conflict.'
- designs/proposals/pc_formation_system.md now ~23756 chars (§11 corrected + §12 provenance/audit).
blockers:
- DENSITY PRIMITIVE (B) MAGNITUDE -- how much brace raises density + density damps
  penetration (calibration target in working_state). Behaviour-cascading; Jordan to
  ratify the target.
- CONFIRM troop-type->role gating table (S3.5) + instruction vocabulary (brace/hold/advance/charge/skirmish/refuse/lure/harass)
  -- pending the historical research.
- MIN_DISCIPLINE hard-gate vs smooth emergence; stratagems-degrade-Discipline (ED-136);
  intervention cadence; custom-allocation UX; subunit coordination cap (design open
  decisions).
- 'CANON-DOC reconciliation: strip A.6 flat dice rules + update A.5 Command to Cha-primary
  (ED-899 follow-up); VERIFY recovered A.5/A.6/A.8/A.12 + troop-type canon against
  mass_battle_v30.md.'
created: '2026-06-03T00:52:10.734036+00:00'
updated: '2026-06-03T05:10:11.165055+00:00'
status: active

id: massbattle-troop-types-roles
task:
  skill: valoria-simulator
  description: 'Develop historical troop types + their roles, wired into the P-C compositional
    formation system. Turn the troop-type/role research into: (1) revised config.TROOP_TYPE_ROLES
    (FM position->role gating); (2) ROLE_SPEC role->(shape+instructions); (3) the
    instruction->primitive modulation (brace->+density, skirmish->-density, charge->concentration),
    wired so each role effect EMERGES from primitives (NO flat bonuses), gated by
    Discipline, conditioned by troop type. Validate every role effect on the gauge
    before committing; keep toggle-OFF byte-exact.'
scope: simulation
context_files:
- path: designs/proposals/pc_formation_system.md
  depth: full
  reason: 'the P-C design-capture: 3-level FM surface, Discipline/Command/Morale split,
    S3.5 roles/instructions/troop-type gating + the A/B/C(=C+B) resolution, shapes,
    engine mapping, open decisions'
- path: tests/sim/mass_battle/config.py
  depth: full
  reason: TROOP_TYPE_ROLES scaffold to populate + COMMAND_*/LANCHESTER_* constants
- path: tests/sim/mass_battle/orchestration.py
  depth: full
  reason: Subunit.role/instructions + roles_for/role_allowed gating + command_base_pool
    + base_combat_pool + per-cell primitives instructions will modulate (_defender_depth,
    build_column_grid)
- path: tests/sim/mass_battle/geometry.py
  depth: full
  reason: CELL_PATTERN_FN shapes, cell_speed, _support_along_vector -- geometry roles
    map onto
- path: tests/sim/mass_battle/resolution.py
  depth: skeleton
  reason: _charge_shock_sigma + sigma head -- where charge/brace effects land
- path: tests/sim/mass_battle/engine.py
  depth: skeleton
  reason: MECHANICS registry -- register density/instruction mechanics when wired
- path: designs/provincial/mass_battle_v30.md
  depth: skeleton
  reason: A.5 Command / A.6 formations / A.8 tactics / A.12 rout + canonical troop
    types -- VERIFY (recovered from conversation, not fresh-read)
- path: canon/editorial_ledger.jsonl
  depth: skeleton
  reason: ED-814 allocation surface, ED-815 MIN_DISCIPLINE, ED-899 Command-only base,
    ED-136 stratagem-degrades-Discipline (open)
working_state:
  done:
  - P-C role scaffold + command_base_pool refactor + report-grounded taxonomy (TROOP_TYPE_ROLES
    + ROLE_SPEC + mounted_archers) committed; design doc + handoff in repo
  - 'BRACE MECHANISM committed (7691eb6a): brace instruction engages the charge-shock
    gate w/o hold-stance penalty + reciprocal charge-recoil (PC_CHARGE_RECOIL=6, prep=disc
    x depth). Emergent pike-beats-cavalry, gated by the ''brace'' instruction -> instruction-less
    byte-exact (proven brace ON==OFF, PER_CELL 0+1)'
  - 'Counter-cycle validated top-down (gauge, 40 seeds): pike-beats-cavalry braced+deep+disc
    ~75% / shallow-green ~12% / braced-vs-inf neutral; cavalry-rides-shaken 75%->28%;
    cavalry-catches-archers def 55%->5%'
  - 'Missile-density coupling committed (a6ae38ad): _volley_density_mult scales volley
    casualties by target col_grid density (Carrhae/Agincourt). Correct direction (dense
    bleeds more), small in single-engagement, compounds at standoff scale. Ranged-only
    -> melee gauge byte-exact; volley-exponent signature preserved (p=2.00).'
  - Counter-cycle validation log + measured baseline folded into designs/proposals/pc_formation_system.md
    (§11) and committed — durable record (compaction-safe).
  - 'Arch review #1 committed (051a8d4c): unified discipline/depth preparedness into
    shared _disc_prep/_depth_prep (gate independent, recoil conjunctive). Gate byte-exact
    (proven); 4 edges hold; no re-calibration.'
  - '[CONSOLIDATION 2026-06-03] Single mass-battle MASTER handoff. Subsumes archived
    massbattle-engine-resume (gauge re-tune folded) + massbattle-speed-facing-resume
    (Phase-3 Jordan gate folded).'
  - 'STRESS-TEST + MECHANICAL-LOGGING harness committed (aea3a264): NON-INVASIVE tick-by-tick
    driver (run_battle max_turns=1; byte-exact run_battle(N)==N x run_battle(1)) ->
    trace_battle() per-tick mechanical trace (hp/morale/stamina/distance/rout) + mirror_sweep()
    symmetry/variance stress. Engine UNTOUCHED (observes state only).'
  - 'SIDE-SKEW found by harness + FULLY DIAGNOSED: mirror match favours the 2nd-PROCESSED
    unit ~70% (tier4 raw single-order A5/B14/D1 skew9; grows with battle length, biggest
    at tier4). Emergent from sequential RNG-stream consumption x the multi-tick morale-feedback
    loop -- roll_pool consecutive draws iid (9848:9908), deterministic-roll mirror
    = 20/20 draws, resolve_engagements symmetric under A<->B relabel, but arg-order
    swap FLIPS the winner. Sub-dominant to real force gaps (disc6-vs-disc4 wins BOTH
    arg-orders 14/20 & 18/20 -- shifts margin ~20pts not winner). Root: resolve_engagements
    draws a_net before b_net (~orchestration 1349-1350).'
  - 'DECISION C (Jordan) on side-skew: (B) DONE (a1142887) -- mirror_sweep(cancel_order=True,
    default) runs each seed BOTH arg-orders + tallies by config -> order-bias CANCELS
    in measurement (cancelled-skew=0 ALL tiers, 40 trials); engine byte-exact. Coverage
    figures corrected (the aea3a264 note had 24-seed A5/B18 mislabeled 20-seed).'
  - 'PER-MECHANIC TRACE LOGGING committed (454adef4): resolution.py passive observe-only
    collector (start_trace/trace_event/get_trace; _trace_on default False -> ENGINE
    BYTE-EXACT, never feeds back). orchestration instrumented at melee (resolve_engagements:
    pools/sigma ns_a,ns_b/nets/degrees), volley (volley_phase fire: shooter/range/pool/net/DR/density/loss),
    per-tick marker (run_battle). Harness trace_battle(mechanical=True) captures per-tick
    events; format_trace renders a full per-tick + per-mechanic AUDIT LOG. Byte-exact
    PROVEN: trace ON==OFF, mechanics_selftest (True,[]), signatures 4/4 (volley p=2.00),
    stress_suite cancelled-skew=0 unchanged.'
  - 'NERS STRESS DIMENSIONS probed + committed (613a3e87): config_sweep generalization
    (configA vs configB, order-cancelled; make_unit stat-overrides) + ners_dimensions().
    FINDINGS: NO threshold cliff (discipline response smooth/monotonic, steepest at
    parity = competitive S-curve); NO runaway loop (edges scale ~proportionally then
    saturate, power +1/+2/+3 -> +20/+50/+55pp); modifiers express above t1 (power
    ~+20pp uniform t2-t4); NON-UNIFORM at smallest pool (t1 = +0pp, swamped by the
    double-rout). Engine mostly NERS-healthy.'
  - 'TIER-1 DOUBLE-ROUT ROOT-CAUSED (via the new mech trace): NOT morale -- it is
    SIZE-depletion. orchestration recalc_size (line 816) = max(0, math.floor(effective_size));
    size==0 -> routed (line 817). tier-1 = Size 1 (TROOPS_PER_SIZE; 100 troops); first
    ~10% casualty -> effective_size 0.9 -> floor 0 -> instant rout at 90% strength.
    ANY casualty destroys a Size-1 unit. Size>=2 has headroom (rout ~50%+ loss). Defect
    specific to the Size-1 floor.'
  - 'RESOLVED 2026-06-03 (Jordan): (1) size < 1 -> rout is INTENDED (''no point keeping
    them around'') -- recalc_size 816-817 is CORRECT; the tier-1 ''double-rout'' I
    flagged is NOT a defect, just a unit starting AT the Size-1 floor. No fix. (2)
    Lanchester-LINEAR (size -> linear combat edge via numbers) confirmed correct,
    and should EMERGE from per-cell density rather than be read off the top-down _lanchester_strength
    frontage term.'
  - INVESTIGATED the per-cell melee path end-to-end (percell.py + resolve_engagements
    + geometry footprint) to locate where size-granularity does/doesn't enter combat.
    Conclusion in key_values.
  next:
  - '[Jordan design decision + build] Make per-cell troops-in-cell DENSITY grade the
    melee EXCHANGE continuously so 2.9 > 2.1 emerges under equal conditions. Today
    melee magnitude = K_LINEAR * enemy LIVE-COLUMN frontage * max(0, DAMAGE_BY_DEGREE[deg](power)
    - dr); density only touches survival/sigma/volley. DESIGN (bottom-up): (a) a density-ratio
    -> per-cell exchange-edge function (denser cell wins, equal conditions); (b) the
    max-cap-per-cell value (Jordan named it; is there a canonical source? build_column_grid
    currently spreads tpc=total/ncells UNIFORMLY with NO explicit cap); (c) decide
    whether density grades the EXCHANGE (net/damage) or stays survival-only. VALIDATE:
    re-run the 4 Lanchester signatures + mechanics_selftest after; expect re-calibration.
    Jordan-vetoable.'
  - '[deferred (A), decision C] engine order-independent contest -- root: resolve_engagements
    a_net-before-b_net (orchestration ~1349-1350); removes the side-skew in the engine
    but CHANGES RNG consumption -> re-validate selftest + RE-CALIBRATE the 4 signatures.
    Side-skew is currently cancelled in MEASUREMENT (config_sweep cancel_order=True),
    engine byte-exact.'
  - '[ready] broaden the sweep -- shapes / stances / troop-types via config_sweep
    + ners_dimensions (both generalize). Mass-battle stress harness = tests/sim/mass_battle_stress.py
    (HEAD).'
  - '[set aside] terrain layer (Jordan''s later direction; field size is a proven
    proxy: 25=constrained, 45+long=open).'
  - '[arch debt] #2 generalize apply_instructions dispatch; #3 per-atom brace/density
    for composites; #4 derive PC_VOLLEY_DENSITY_REF from a canonical formation.'
  architecture: 'Three-level FM surface: L1 army doctrine (Aggression/Cohesion-priority)
    | L2 unit formation = SHAPE (arrangement only, NO bonus) | L3 subunit ROLE = troop-type-gated
    INSTRUCTION package. Governance triangle: Command orchestrates (adapt strategy
    / interpret events / reinforce Discipline / rally Morale / stratagems / coordination-cap;
    Command = derived Cha-primary + Cog-secondary) -> Discipline holds shape+tactics
    (MIN_DISCIPLINE adopt-gate; formation breaks at 0) -> Morale sustains (rout resource,
    rallied by Command). EMERGENCE PRINCIPLE (load-bearing): formations grant NO flat
    bonuses; all effects emerge from primitives (frontage/Lanchester, depth-damping,
    support, facing, charge-shock, density). SHAPE_OFF/DEF_MOD retired to make this
    structural.'
  methodology: 'Build geometry/instruction -> MEASURE on the gauge -> iterate until
    the historical intent emerges. SHIELD-WALL LESSON this session: a narrow+deep+braced
    SHAPE did NOT make defense emerge (-1pp vs melee; +3pp vs charge only) -> shield-wall
    must be a brace-INSTRUCTION raising density, not a shape. Do not repeat the shape
    approach.'
  calibration_target: (proposed; Jordan to ratify) A braced heavy-infantry shield
    wall should ~hold its ground vs an equal melee line (parity, not -1pp) AND meaningfully
    blunt a cavalry charge (well above the +3pp brace-only). Tune the density primitive
    to this.
  bootstrap: Standard. Engine = tests/sim/mass_battle/ package + shim sim_mb_sigma.py
    + gauge_mb.py. Run committed/isolated with cwd INSIDE the engine dir (sys.path
    shadowing makes import mass_battle resolve to cwd). PER_CELL=1 enables the per-cell
    layer; PYTHONHASHSEED=0 deterministic.
  completed:
  - 'ARCH-FIX #1 (051a8d4c): unified wall-prep _disc_prep/_depth_prep; gate byte-exact,
    recoil disc-curve unified'
  - 'Pattern-matching audit performed + folded into design doc §12 (5e64c129): provenance
    rule + 5-finding table'
  - 'Audit item 1 FIXED (4248cdd0): §11 anchors Waterloo/Albuera -> report in-period
    Courtrai/Swiss/Hussite, Hastings/Adrianople'
  - 'Audit item 2 VERIFIED: _SIG EV 0.806/0.800/0.781 present in params/core.md at
    TN 6/7/8'
  - 'Audit item 4 VERIFIED: _charge_shock_sigma fires 0x in melee gauge (probe); a_pen=0;
    not load-bearing'
  - 'Subunit construction guard (1e4e6436): __post_init__ validates shape/tier/bounds
    -> clear ValueError (was KeyError/off-grid); byte-exact, selftest 30 + signatures
    4/4 + gauge ON==OFF'
  - Kiting plan folded into design §13 (161421ac)
  - 'Kiting primitive WIRED + committed (9159c22c): config PC_KITE_ENABLED/PC_KITE_STANDOFF=5[class-B]
    + advance_cells maintain-range hook (gated on the "kite" instruction, Chebyshev
    to match volley) + mounted-speed for mounted_archers. Byte-exact (selftest 30,
    signatures 4/4, gauge ON==OFF).'
  - 'Kiting MEASURED (PER_CELL=1, Line t4, 20 seeds): kiter vs infantry = kiter 100%
    HP / inf 88% / DRAW; no-kite ranged control closes + loses 16/20 at 81% (proves
    the hook works); kiter vs cavalry == vs infantry (no cornering counter).'
  - 'Edge-cornering committed (adf9fb9e): advance_cells clamps cell abs to [0,BATTLEFIELD_SIZE),
    back-solves offset (no hysteresis). Closes the dynamic-bounds item. Byte-exact
    (selftest 30, signatures 4/4, gauge ON==OFF).'
  - 'KITING RPS VALIDATED + §13.1 as-built folded into design: vs cavalry kiter LOSES
    13/20 (cornered+caught = Patay/Arsuf); vs infantry contested on 25-field (87/87,
    cornered) but DOMINATES on open field+time (45 cells/40 turns -> 14/20, kiter
    97% / inf 80% = Carrhae).'
  - 'Item-3 compounding CONFIRMED by measurement: sustained volley over a long open-field
    kiting fight compounds the small per-turn chip into a decisive win (field 45 /
    40 turns -> infantry to 80%).'
  in_progress:
  - 'PER-CELL DENSITY-COMBAT GRANULARITY (Jordan design direction 2026-06-03). Intent:
    the size granularity (effective_size 2.9 > 2.1 under 100% equal conditions) must
    EMERGE from per-cell troops-in-cell DENSITY combat -- ''more troops in one cell
    betters fewer troops in one cell'' -- decomposed via subunits distributed through
    the formation grid, each cell holding a troop count up to a max-cap-per-cell before
    overflow to a new cell. FINDING (this session): the engine does NOT do this yet
    -- melee magnitude is frontage-discrete, not density-continuous (see key_values
    GAP). NEXT: design the density-graded per-cell exchange + define the max-cap-per-cell,
    then validate against the 4 signatures + selftest. No code changed this session
    (investigation only).'
last_commit: 613a3e87 (config_sweep + NERS dimensions) -- no new commits this thread
  (per-cell investigation only). Engine byte-exact at HEAD.
owns:
- designs/audit/2026-06-01-massbattle-stub-wiring/**
- designs/proposals/pc_formation_system.md
- designs/provincial/mass_battle_v30.md
- tests/coverage_matrix.md
- tests/sim/gauge_mb.py
- tests/sim/mass_battle/*.py
- tests/sim/sim_mb_sigma.py
session_token: 0517c078f9073244
key_values:
- Command = round((2*Cha + Cog)/3), Charisma primary (ED-899); COMMAND_SIGMA_ENABLED
  default ON, OFF == canonical Size pool (byte-exact)
- 'Engine grid is UNBOUNDED: BATTLEFIELD_SIZE/UNIT_GRID_SIZE/BUFFER_CELLS nominal/unused;
  only start-rows + cell layouts matter. Wider/shallower grid = allocation surface
  + template proportions, not an engine box.'
- 'Shapes in CELL_PATTERN_FN: Line/Arrowhead/Horseshoe/GappedLine/RefusedFlank. Canonical
  Shield-wall=role (NOT shape); Skirmish/Column/Wedge/Reserve TBD as shapes or roles.'
- role_at_contact (Subunit) is ORPHANED (callers removed with SHAPE retirement) --
  positional-role helper; decide retain-vs-retire when L3 positional roles are designed.
- 'orchestration uses an explicit __all__ at end-of-file: any new public symbol MUST
  be added to it (caught by validation this session).'
- 'Session commits: a5fc9d0e P-L | 2cf3feb6 Command-only | c8ba5b7f ED-899 | 7e878ade
  signature-guard | c13269c4 registry | e677a11f SHAPE-retire | 6d8eb3b7 role-scaffold
  | 7d8e262f command_base_pool.'
- Gauge bands PENDING re-tune to the Command-only model (formation/ranged/cav-vs-shaken
  bands OUT == calibration artifacts, not physics regressions).
- 'COLLISION mechanism: github_ops._fetch_head is PER-PATH (repo_key->HEAD-at-fetch)
  in /home/claude/.valoria_cache.json. write_handoff/atomic_commit advance HEAD but
  do NOT refresh other paths per-path fetch_head, and skip_cache reads do not either
  -> a later safe_commit on an untouched path falsely collides. RESOLVE: verify the
  commit paths are unchanged at live HEAD (refs API), set G._fetch_head["ttrpg:"+path]=HEAD
  for each, then safe_commit. Safe when no other session + no real conflict.'
- designs/proposals/pc_formation_system.md now ~23756 chars (§11 corrected + §12 provenance/audit).
- 'KITE measured numbers (PER_CELL=1, Line t4, 20 seeds): kiter-vs-inf A_win=0 B_win=0
  draw=20 (A 100% / B 88%); no-kite-ranged-vs-inf A_win=3 B_win=16 (A 81% / B 90%);
  kiter-vs-cav == kiter-vs-inf.'
- 'advance_cells offset semantics: abs = starting_position + oriented + offset*advance_dir;
  the offset itself accumulates r_step*advance_dir, so advance_dir appears twice and
  cancels (abs ~ sp + or + Sum(r_step)). r_step already carries the toward/away sign.
  Care needed for any movement-clamping (edge-cornering).'
- PC_KITE_STANDOFF=5 (band [2,8]); kiter holds+volleys in [5,8], retreats when d<5,
  closes when d>8.
- 'STRESS HARNESS tests/sim/mass_battle_stress.py: make_unit / trace_battle(ticks)->per-tick
  snapshot / format_trace / mirror_sweep(tier,cancel_order=True default; both arg-orders,
  wins-by-config) / stress_suite. _SEEDS=20 (fabrication gate: 24 FLAGGED, <=~18-20
  exempt; [class-B] does NOT exempt in scanned non-config sim files -- only [canonical:]/ledger).
  Run: PER_CELL=1 PYTHONHASHSEED=0 PYTHONPATH=<dir> python3 -m mass_battle_stress.'
- 'NON-INVASIVE tick-driver VERIFIED byte-exact: run_battle(N)==N x run_battle(max_turns=1).
  Harness drives the engine turn-by-turn + snapshots STATE; sees no mechanism internals
  (that needs the deferred passive trace collector).'
- 'SIDE-SKEW: 2nd-drawn unit favored ~70% raw; CANCELLED in measurement via cancel_order
  both-orders (cancelled-skew=0); engine byte-exact; deferred fix (C-A) = order-independent
  contest + signature re-calibration.'
- 'PER-MECHANIC TRACE: resolution.py _trace_on/start_trace/trace_event/get_trace (in
  __all__; orchestration gets them via import *). trace_event(''melee''|''volley''|''tick'',
  ...) is observe-only -> byte-exact when off (default) AND ON==OFF (proven). harness
  trace_battle(mechanical=True) wraps each max_turns=1 tick with start_trace/get_trace;
  snap[''mech''] = that tick''s melee/volley events; format_trace prints them under
  each tick.'
- 'BYTE-EXACT RUNNERS: from mass_battle.engine import mechanics_selftest -> (ok, missing)
  registry check (NOT 30 behavioural checks); python3 -m mass_battle.lanchester_signature
  -> 4 signatures (LINEAR/SQUARE/NO-ANNIHILATION/EXPONENTS, volley p=2.00). Both +
  gauge ON==OFF + trace ON==OFF are the byte-exact battery.'
- 'SIZE/ROUT: orchestration Subunit/Unit -- size init max(1, total//TROOPS_PER_SIZE)
  (line 795); recalc_size = max(0, math.floor(effective_size)) + size==0 -> self.routed=True
  (816-817). So rout has TWO causes: morale<=0 (run_battle 1742-44) AND size->0 (recalc_size).
  tier-1 Size-1 floor-routs on any casualty.'
- 'config_sweep(tier, a_kw, b_kw, n_seeds, cancel_order) in harness: configA vs configB
  by config, order-cancelled. mirror_sweep = a_kw==b_kw case. make_unit now takes
  power/command/discipline/morale/dr overrides (default None -> module fixtures).
  ners_dimensions() = the cliff/runaway/uniformity probe.'
- 'MELEE MAGNITUDE (current, COMMAND_SIGMA_ENABLED): casualties to X = K_LINEAR *
  enemy LIVE-COLUMN frontage (_lanchester_strength = count of distinct contact columns)
  * max(0, DAMAGE_BY_DEGREE[deg](power) - dr). Pool = base_combat_pool (COMMAND-ONLY
  per Jordan 2026-06-02) * troop_frac * engage_frac. NO density in the pool or the
  damage. orchestration: pool 1128-1145, damage 1370-1379.'
- 'PER-CELL DENSITY consumers (current): _ColBlock.density (troops/column) feeds ONLY
  -> _fatigue_sigma (1343-44), _envelopment_sigma (1346-47, depth/overhang), _volley_density_mult
  (1482-94, RANGED only), distribute_casualties (1723-24, WHERE casualties land).
  NONE feed the melee magnitude. percell.py: _ColBlock(density/depth/stamina); column
  ''live'' while density > 0.5 (line 23); build_column_grid tpc=total_troops/ncells
  (uniform, no explicit cap).'
- 'FOOTPRINT is DISCRETE: CELL_PATTERN_FN[shape](tier) -> width,depth = sizes.get(tier)
  (geometry.py 21/57/277). Shape+tier based, NOT continuous effective_size. So same
  tier+shape => same footprint; effective_size 2.9 vs 2.1 = different DENSITY in the
  SAME cells, NOT different frontage.'
- 'THE GAP (Jordan 2026-06-03 intent vs engine): melee magnitude tracks the COUNT
  of live columns (frontage), which STEPS down only when a column empties past density
  0.5 -- it is NOT continuous in troops-in-cell density. So 2.9 and 2.1 deal the SAME
  melee damage at equal live-column count; the density gap shows up only in survival
  (hp/depth) and as a stepped frontage drop at column death. Jordan wants CONTINUOUS
  density-graded per-cell combat. [CONFIDENCE: high -- code reading; an effective_size-degradation
  trace would confirm end-to-end].'
blockers:
- DENSITY PRIMITIVE (B) MAGNITUDE -- how much brace raises density + density damps
  penetration (calibration target in working_state). Behaviour-cascading; Jordan to
  ratify the target.
- CONFIRM troop-type->role gating table (S3.5) + instruction vocabulary (brace/hold/advance/charge/skirmish/refuse/lure/harass)
  -- pending the historical research.
- MIN_DISCIPLINE hard-gate vs smooth emergence; stratagems-degrade-Discipline (ED-136);
  intervention cadence; custom-allocation UX; subunit coordination cap (design open
  decisions).
- 'CANON-DOC reconciliation: strip A.6 flat dice rules + update A.5 Command to Cha-primary
  (ED-899 follow-up); VERIFY recovered A.5/A.6/A.8/A.12 + troop-type canon against
  mass_battle_v30.md.'
- 'GAUGE-BAND RE-TUNE to the Command-only sigma model (folded from massbattle-engine-resume):
  re-tune gauge bands (tests/sim/gauge_mb.py) to the new model BEFORE canon-doc reconciliation.'
- 'JORDAN RATIFY/VETO Phase-3 cavalry-shock magnitudes (folded from massbattle-speed-facing-resume,
  commit 7cfa44d5): 9 PC_SHOCK_*/PC_CHARGE_SIGMA Class-B sim-tunable values calibrated
  to C1-C7 historical bands; mechanism validated, numbers tunable.'
created: '2026-06-03T00:52:10.734036+00:00'
updated: '2026-06-03T16:32:12.766079+00:00'
status: active

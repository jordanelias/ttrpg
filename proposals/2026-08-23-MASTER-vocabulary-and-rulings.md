# MASTER — Competing Vocabularies, Three Rulings, and What Is Actually Waiting On You

## Status: FINDINGS + RULING EXECUTION NOTES. Nothing here rules anything new. No `.py` touched, no registry edited, nothing deleted.

**Date:** 2026-08-23 · **Supersedes, for reading purposes:** `2026-08-23-competing-vocabularies-index.md` and `2026-08-23-rulings-and-adversarial-review.md`. Both remain on disk for their citations; **this document is the one to read.** Every claim below has survived an adversarial pass or is marked as not having had one.

**Method.** Six producer sweeps, three read-only antagonists, and independent re-verification by parse or execution of everything load-bearing. Grep located lines; reading and parsing concluded. Corrections are **absorbed into the findings**, not appended as commentary — the only place I narrate my own errors is §10, and only where the error itself is instructive.

---

## §1 MASTER INDEX

**State column:** ✅ verified by me · ⚔ survived an antagonist · ✖ overturned, corrected form shown · 📋 already filed as open work · ⏳ no antagonist yet.

### The three rulings

| | Ruling | What executing it actually requires | State |
|---|---|---|---|
| **R1** | **Tree A removed** | adapter → repoint one import → delete 5 modules. **One guard blocks it, not two** | ✅⚔ |
| **R2** | **TN always 7** | two constant sites, not nineteen. **Answers a question already docketed and HELD** | ✅⚔ |
| **R3** | **One pool document** | seven formulas + two design picks + **one rounding chokepoint nobody had found** | ✅⚔ |

### Your actual queue (§3)

| | Finding | State |
|---|---|---|
| **Q1** | **283 open items — but 155 of them do not need you at all** | ✅ |
| **Q2** | 128 flagged for you; **20 are pre-July** | ✅ |
| **Q3** | **Nothing anywhere counts ratified-but-unpropagated** — I verified 17 | ✅ |
| **Q4** | The registers are **12 days stale** (last refresh 2026-08-11) | ✅ |

### Person texture (§4)

| | Finding | State |
|---|---|---|
| **P1** | **Five conviction rosters** — 13/9/8/7/7, union 21, intersection {Faith, Order} | ✅⚔ |
| **P2** | **7 of 13 canonical Convictions cannot be Scarred; 12 of 43 named NPCs are inert** | ✅⚔ 📋 T0-1 |
| **P3** | **Resonant Style is NOT a four-way** — the armature axis is a ratified distinct concept | ✖⚔ |
| **P4** | The prescribed fix for the `Authority` collision **manufactures a new collision** | ⚔ |
| **P5** | `alias_registry` marks **5 canonical Convictions as deprecated legacy** — an inverted table | ✅ |
| **P6** | **PP-684 mis-superseded Ethical Framework**, an orthogonal axis | ⏳ |
| **P7** | `resonance_style` retired on a premise the same entry records as **FALSE**; NEEDS-JORDAN since 2026-07-07 | ⚔ 📋 |

### Place and polity (§5)

| | Finding | State |
|---|---|---|
| **L1** | **"Territory" is one ratified model + one bannered supersession + unexecuted propagation** — not three rivals | ✖⚔ 📋 |
| **L2** | **The ratified `Territory` tier has zero code representation**; the dataclass named `Territory` holds Provinces | ⚔ **sharpened** |
| **L3** | **T16 Schoenland is authored, adjacency-linked, and absent from the world** — S-037 orphaned | ✅⚔ |
| **L4** | Accord: 4 ranges; **T9 yields 5, outside the map's keyspace** | ✅⚔ 📋 B9/OI-37 |
| **L5** | **43% of settlements have no type** — Village (14), Fortress-City, Cathedral-City | ✅⚔ 📋 E6 |
| **L6** | **The two highest-value non-capitals have no Weight in the ratified Mandate formula** | ⚔ **new** |
| **L7** | Ledger families: five lists, **D3 RATIFIED and unpropagated** | ⚔ 📋 |
| **L8** | **The authored per-settlement L/PS seed table has no loader** | ⚔ **new, inverts the remedy** |
| **L9** | `religious_building` stored twice, never synced — an **inert field**, not a broken arc | ✖⚔ 📋 |
| **L10** | Goldenfurt's authored numbers are not loadable — its own vise runs at 2 AP, not 3 | ✅⚔ |
| **L11** | Niflhel struck by CANONICAL ruling — **and it WAS propagated**; residue is narrow | ✖⚔ |
| **L12** | `settlement_layer` self-contradiction is an **unmigrated SID reference**, not a control conflict | ✖⚔ |
| **L13** | `faction_canon` PART B has **2 sheets of 6** (not 8) | ⚔ |

### Outcomes, events, scale (§6)

| | Finding | State |
|---|---|---|
| **O1** | **37 distinct outcome/degree vocabularies** | ⏳ |
| **O2** | One CANONICAL file carries **nine** of them, incl. `graze` in a Failure slot | ⏳ |
| **O3** | **`graze` has four meanings — and the Godot skeleton contains the collision internally** | ⏳ |
| **O4** | **A Foundational-depth Weaving cannot emit a Key** — scale rosters 4 (enforced) / 5 / 6 | ⏳ |
| **O5** | **`alias_registry` ships phantom vocabulary** that is player-facing screen labels | ✅ |
| **O6** | Three canonical Knot-Formation tables disagree; 3 of 4 rows encode the ruled-out ladder | ⏳ |
| **O7** | The Church-dominance threshold is **40 / 60 / 65 / 100** | ⏳ |

### Voice (§7)

| | Finding | State |
|---|---|---|
| **V1** | The CANONICAL voice doc **lists 8 authors while claiming 12** | ⏳ |
| **V2** | **The RATIFIED narrative engine cites the PROVISIONAL and the skill, not the canon** | ⏳ |
| **V3** | **Truth-0 has no voice register** — §18 runs 1-2/3/4/5/6+ on a 0–5 track | ⏳ |

### Machinery (§8)

| | Finding | State |
|---|---|---|
| **M1** | **The fractional-pool ruling is defeated by one `round()`** at `sigma_leverage.py:284` | ✅⚔ **new** |
| **M2** | A planned guard would **permanently exempt the three constants R2 outlaws** | ✅⚔ **new, catchable now** |
| **M3** | ED-874's Domain Action resolver: ratified, **zero implementations** — and its ED is flagged **NONEXISTENT** | ⚔ **sharpened** |
| **M4** | **Suppress is unreachable** — its caller passes no kwargs, so the two-doc Ob conflict is moot | ⚔ **sharpened** |
| **M5** | A fabricated path under `deprecated/` resolves **FORKED** and is tolerated | ✅⚔ |
| **M6** | **PP is FROZEN and three live surfaces say allocate** | ✅ |
| **M7** | `ci_checks_registry.yaml` **contradicts itself** and its `role:` line has rotted | ⚔ **new** |

### Structural causes (§9)

| | Cause | State |
|---|---|---|
| **S1** | **No roster primitive exists** — no registry can express a set | ✅ |
| **S2** | **The naming gate enforces exactly one rule tree-wide** (112 of 113 entries are `warn`) | ✅ |
| **S3** | **`CURRENT.md` has no row for Convictions, Characters, or `systems/world`** — a supersession has nowhere to land | ✅ |

---

## §2 THE THREE RULINGS

### R1 · Remove Tree A

**Why it keeps coming back.** Three rulings, none reconciled:

| Date | Surface | Says |
|---|---|---|
| 2026-08-03 | **J2, Jordan** | canon is `tests/sim/mass_battle/`; the 5-module tree *"is retired, not kept alongside"* |
| 2026-08-04 | **evacuation keep-set** (one day later) | pins `massbattle.py` as **`'keep'`** |
| 2026-08-06 | vector audit ED-IN-0148 | finds it still present and load-bearing |
| **2026-08-23** | **Jordan** | remove it |

`CURRENT.md` recorded J2 as **resolved**. It was never executed, and the keep-set contradicted it the next day without either surface seeing the other.

**The coupling is one line:** `systems/factions/sim/faction_action.py:452`. Everything else is tooling registries and tests.

**The blocker is real — I reproduced it:**

```
Tree A adapter built unit: Unit
BLOCKER (measured): 'Subunit' object has no attribute 'cells_float'
```

Tree A is **faction-scale** (`resolve_mass_battle(faction_a, faction_b, terrain, world)`); Tree B is **unit/cell-scale** (`run_battle(unit_a, unit_b)`) and has **no faction-scale entry point**. `_faction_to_unit`'s own docstring concedes the mapping was never specified: `[GAP: no canonical spec for faction.Mil -> Unit construction]`.

**The execution path — and it is cheaper than I first said:**

1. **Write the strategic → cell-based-Unit adapter.** This is the whole job.
2. **Repoint `faction_action.py:452`.**
3. **Delete the five modules.**

> **Correction absorbed:** I previously listed "flip the keep-set pin or CI fails." **That is wrong.** `test_known_files_land_correctly` calls `ep.classify(rel)` on a **string**, and its own docstring says *"Paths need not all exist."* Deleting the file does not change the classifier's verdict. **Exactly one guard blocks removal** — the seam test's state-B requirement — and it was written as a disjunction precisely so it would not punish this migration. Flipping the pin is tidying, not a gate.

**What removal resolves by deletion:** Tree A lacks the σ head, the fractional pool, and PP-241 Reform (`reform_check` is an empty `pass`). Your ED-MB-0032 "pool must be fractional" is implemented **only in Tree B**. And 40 of 51 mass-battle tests already target Tree B — the suite has been ready longer than the code.

### R2 · TN is always 7

**The ruling is nearly already true.** I resolved every TN constant by AST and read each definition. All are **already 7** except:

| Site | Constant | Value |
|---|---|---|
| `threadwork/sim/operations.py:48-50` | `TN_BINDING` / `TN_POP` / `TN_POP_BINDING` | **8 / 8 / 9** |
| `combat/sim/combat.py:57,136-146` | `WEAPON_TN_BASE` + `_weapon_tn` mods | **5–8** |

**Two sites, not nineteen.**

**⚠ You are answering a question the tree already docketed and held.** `audit/2026-08-11-divergence-audit/02_remediation_plan.md:608-634` is `A5 · HELD — make roll_pool honour its TN`, offering three options, of which **(b) is *"re-declare the threadwork TNs to 7"*.** Your ruling selects (b). The finding itself is D1 of that same audit, twelve days old.

**⚠ And there is a live item that now contradicts you — catchable today.** The same plan (`:606`) specifies a guard, `tests/valoria/test_tn_single_owner.py`, and states:

> *"Threadwork's `TN_BINDING`/`TN_POP`/`TN_POP_BINDING` are **excluded** — they are non-standard **on purpose**."*

**If that guard is built as written, it permanently exempts the exact three constants your ruling outlaws.** I confirmed the file does not exist yet.

**What to do:** set the three constants to 7 (or delete them and use `TN_STANDARD`); retire `_weapon_tn`; amend A4's exclusion before the guard is written. Behaviour does not change — the owner already discarded the values. **The declarations stop lying.**

**One scope correction:** `systems/combat/sim/combat.py` is not inert. It is on the **default** dispatch branch (`scene_dispatch.py:268-274`, the `DISPATCH_COMBAT_BRIDGE`-OFF path). It is reachable-but-context-starved, gated on `ctx['participants']`.

### R3 · One pool document

Seven formulas, read at source:

| # | Subsystem | Formula | Floor |
|---|---|---|---|
| 1 | **Personal combat (canonical)** | `max(5, round(History) + 6)` — Agility-independent by ratification | 5 |
| 2 | Personal combat (superseded, still executable) | `max(5, (Agi×2) + History + 3)` + wound/breath mods | 5→1 |
| 3 | Social contest (legacy stub) | `max(1, (Primary×2) + History + fatigue)` | 1 |
| 4 | Social contest (live kernel) | `max(5, faculty×2 + 3)` — no History, `BASE=3 [SEED]` | 5 |
| 5 | Threadwork | `(Spirit×2) + min(3, History+3) + TS//10` — History capped | none |
| 6 | Knots | `(Spirit×2) + history_relationships` — no constant | none |
| 7 | Mass battle | `max(1, floor(min(size, command) + command + pens))` | 1 |

**What the document must decide:** which attribute and whether doubled · the constant (+6/+3/+3/+0/+0) · the floor (two formulas can produce a non-positive pool) · whether History is capped · **whether #7 belongs at all** (it is a unit-scale aggregate, not an attribute pool — a different quantity sharing the word).

> **⚠ The finding that makes this ruling load-bearing, and nobody had it.** `engine/autoload/sigma_leverage.py:284` does:
>
> ```python
> effective_pool = max(1, int(round(pool)))
> return dice_engine.continuous_engine_sample(pool=float(effective_pool), …)
> ```
>
> **Every strategic call goes through this wrapper, and it rounds the fraction away before the fractional engine ever sees it** — even though `dice_engine:87-93` documents the engine as fractional-capable. `faction_action.py:107-112` records this against your fractional ruling and says it plainly: ED-IN-0187 *"was written into the ledger without being applied here — which is worse than an unimplemented feature, because the call site asserted the opposite."*
>
> **A pool document that specifies formulas but not this chokepoint will produce integer pools regardless of what it says.**

---

## §3 YOUR QUEUE — the 283, measured

You asked why 283 things are awaiting your approval. **Most of them are not.**

| | Count |
|---|---|
| Total open work items | **283** |
| Explicitly labelled *"actionable — no ruling needed"* | **128** |
| Flagged `needs_jordan` | **128** |
| Neither / other kinds | 27 |

**So 155 of the 283 do not need you** — they are sitting in a register whose headline reads *"128 need your decision"* while listing them all together.

**By age:**

| | Total | needs_jordan |
|---|---|---|
| pre-July | **75** | **20** |
| July | 165 | 81 |
| August | 35 | 23 |
| undated | 8 | 4 |

**Your real queue is at most 104** (July + August), and **20 of the 128 flagged for you are pre-July** — the ones you say are likely irrelevant.

**Two caveats.** The registers were last refreshed **2026-08-11**; the banner reports 55–102 in-scope files changed since. And the doc-status count is unreliable because several files carry two `## Status:` lines and every reader takes the first — `faction_canon_v30.md` reads as CANONICAL while its second line says PROVISIONAL.

### §3.1 The category nothing counts

Every register counts **unratified** work. `PROPOSALS.md` says so in its own header: *"whole work awaiting sign-off."* **There is no bucket anywhere for ratified-and-never-landed.**

I verified **seventeen**. Four are inside the last two weeks:

| Ruled | What | State |
|---|---|---|
| 2026-08-14 | degree ladder single-owned, margin bands | two HOLDs **ruled to migrate 2026-08-15** — neither executed |
| 2026-08-14 | PP frozen at 726 | three live surfaces still say *"Next PP number: 727"* |
| 2026-08-14 | *"it will be 10 attributes"* | registry ships nine; the tenth is unnamed |
| 2026-08-15 | port block deleted | the gap it blocked on is unchanged |

The older thirteen have had longer: **J2** (the one you just re-ruled), **D3** and **D4** (both RATIFIED 2026-07-13, neither propagated), **`scale_hierarchy_v1`** (RATIFIED direct ruling, whose own §6.1 says *"tracked, unexecuted"*), **the Niflhel strike**, **ED-874**, **PP-684**, **ED-912**, **ED-731**, **ED-644**, **ED-IN-0075**.

**The register that would catch this is a smaller job than any ruling it would track.** `build_proposals.py` already walks the ledgers and already reads `status`. It never asks *"ruled — and did anything change?"*

---

## §4 PERSON TEXTURE

### §4.1 Five conviction rosters

| Roster | n | Where it lives |
|---|---|---|
| **13 — canonical** | 13 | `conviction_taxonomy_v30.md:29-41`, the 13×4 matrix, **all 46 registry entries** |
| **9 — legacy** | 9 | `conviction_track_v1.md:20-28` — **and `conviction.py:46-49`, the live runtime gate** |
| **7 — legacy** | 7 | `conviction_track_v1.md:70` — **and `names_index.yaml`, the live name registry** → `definitions.yaml` → `lexicon.json` → the dashboard |
| **8 — NPE** | 8 | **`npe.py:80`, the live generator** + `investigation_systems_v30.md:84`, which asserts these *are* "the existing conviction taxonomy" |
| **7 — "Seven Convictions"** | 7 | `complete_systems_reference.md:23` — its supersession banner is **scoped to PART 2 combat only**, so §1.1 stands as live canon |

**Union 21. Present in all five: `Faith` and `Order`.**

### §4.2 The cost, executed

```
SCARRABLE   : Faith, Order, Equity, Precedent, Community, Warden
SILENT NO-OP: Authority, Scholastic, Utility, Liberty, Identity, Virtue, Honor  (7 of 13)
named NPCs with authored primaries: 43
CANNOT be scarred on ANY primary : 12
```

Conviction Scars are the **only** mechanism by which an NPC's value-frame destabilises, shifts weight, activates Resonant Style, or enters crisis. **Ehrenwall, Cesare and Maret Uln are inert** — nothing that happens to them can change them.

The one production write path, `knots.py:349-353`, passes `'Loyalty'` — a member of no roster. It returns magnitude 0. The test asserts an intent flag set three lines earlier and never inspects the record, so it is green. **Already `HANDOFF_IN.md` Tier-0, next action #1.**

### §4.3 Resonant Style — not a four-way

The armature's fourth axis (`Insinuation`) is a **ratified, explicitly distinct concept**: `social_contest_v30.md:174` — *"what the ADJUDICATOR is moved by, **distinct from** … the opponent-aimed Resonant Style targeting."* Solidarity is Knot-gated and relational; a third-party judge is not Knot-bound. **Merging them would destroy a ratified distinction.**

**What survives is a two-name conflict on one member:** `Solidarity` vs `Loyalty`, where a 2026-07-21 registry addition re-canonised both names a 2026-05-08 rename had retired, with `legacy: []`.

**And the prescribed fix manufactures a new collision.** `name_collision_database.yaml:421-425` renames "Authority (pressure point)" → **Sanction**, against a live five-tier **Parliamentary Sanction** ladder at `faction_layer_v30.md:455-461`.

### §4.4 Two more

**`alias_registry.yaml:648-663` lists Virtue, Faith, Scholastic, Equity, Honor — five of the canonical thirteen — as deprecated legacy**, in a record whose own `note` enumerates all thirteen as canonical. The mechanism is one line: the source is a two-column map, and **the registry transcribed the target column into the legacy slot.** It is regenerated by `vocab_store`, so `--check` republishes it faithfully, and it ships to the dashboard.

**PP-684 mis-superseded an orthogonal axis.** `npc_behavior_system_v1.md:19-23`: *Conviction* = what the NPC **wants to do**; *Ethical Framework* = inherited from the faction, what the NPC is **rewarded for doing**. Folding them destroys the corpus's most legible dramatic engine — a devout man serving a cynical institution — and every institutional-betrayal arc loses its mechanism. `character_canon` D2 has been quietly ignoring the supersession for 3.5 months, and that instinct was right.

---

## §5 PLACE AND POLITY

### §5.1 "Territory" — one ratified model, not three rivals

`valoria_political_hierarchy_v30.md:10-18` carries an **eight-line supersession banner** naming `scale_hierarchy_v1.md` (RATIFIED, 2026-07-13) and ending *"Do not treat §1/§2.3 as current."* So this is **one ratified model + one bannered supersession + unexecuted code propagation.** The remedy is authoring, not a ruling.

**And T1–T17 do have a name** — `valoria_political_hierarchy_v30.md:48` deprecates "territory" for that tier in favour of **province**, and `settlement_layer_v30.md:151` reconciles the geography file's usage explicitly. `registry.py:262`'s `province_id=e['territory']` is the **correct PP-726 rename**, documented in the docstring above it.

> **The real defect runs the other way, and it is sharper.** The `Territory` dataclass (keyed T1–T17) holds what the ratified model calls **Provinces**. The ratified model's actual *Territory* tier — between settlement and province — has **zero representation anywhere in code.** The collapse is Territory↔Province, not Territory↔Settlement.

### §5.2 The world the engine builds

```
territories: 16 | T16 present: False
geography provinces: 17 | settlements: 37
type histogram: {Town 15, Village 14, Seat 2, Fortress 2, City 2, Fortress-City 1, Cathedral-City 1}
settlements whose province is NOT in world.territories: ['S-037']
```

- **T16 Schoenland** is authored, temperament-assigned, radiation-banded, **adjacency-linked** (`{from: T1, to: T16, type: coastal}`) — and absent from `STARTING_OWNER`. S-037 is permanently orphaned. `game_state.py` carries **two territory sets eight lines apart** (`ALL_PLAYABLE_15` = 15, `STARTING_OWNER` = 16).
- **43% of settlements have no type.** Village (14), Fortress-City, Cathedral-City are absent from §1.2, §1.4.1 and §4.5. Port/Cathedral/Mine/Outpost have **zero instances**. Already filed as **E6**, cross-confirmed by a baseline audit and a 500-seed run, with the fix authored.
- **Accord**: `Territory.accord` bucketed and `province_accord()` diverge at **T2/T6/T8/T9**, and **T9 yields 5 — outside `ACCORD_MAP`'s keyspace.** Already filed as **B9** with a one-line patch (`return min(4, ...)`), instrumented by a live probe, tracked as OI-37. **D4 already picked the winner.**

> **The consequence nobody had: `settlement_layer_v30.md:160` defines `base(Type)` — the Mandate Weight term — including Village while citing two tables that lack it, and omitting Fortress-City and Cathedral-City entirely.** So **S-014 Ehrenfeld and S-036 Himmelenger, the two highest-value non-capital settlements, have no defined Weight in the ratified Mandate formula.** That is an arithmetic hole, not a taxonomy tidiness issue.

### §5.3 The authored table with no loader

> **`faction_state_authoring_v30.md §8:323-338` authors the per-settlement L/PS seeds** — Crown 5/5, Church 5/5, Hafenmark 4/4, Varfell 4/4, Guilds 3/3, Restoration 0/0, Löwenritter 0/0 — stating they *"seed each controlled settlement's L/PS at game start."*
>
> **Nothing reads it.** `populate_from_geography` reads only the geography YAML, which carries no L/PS keys. All 37 settlements load at 0/0.
>
> **This inverts the remedy.** The defect is not "L/PS is unauthored and needs design"; it is **"an authored canonical table has no loader"** — roughly ten lines, not a design item. The honest headline: *the authored starting legitimacy of the entire world is silently discarded at world-gen.* And that table's provenance chain points at the evacuated `engine/params/` tree.

**On Mandate:** `Faction.L` **is** Mandate by explicit convention, cited five times across the tree (`parliamentary_bridge.py:72` — *"Mandate==Faction.L pre-LPS-1"*). It evaluates to Church 5.0, Crown 5.0, Hafenmark 4.0, Varfell 4.0 — **nonzero**. What has no implementation is the **ratified LPS-1 aggregate** (`clamp(round(7T/(T+6)))`), which is a counterfactual about unbuilt code.

### §5.4 Three things that are less broken than they looked

- **Ledger Reputation does not "silently erase."** Replacement is specified behaviour, documented three times in `ledger.py`. And the ledger **has no production writer at all** — the only call sites are a prototype harness.
- **`religious_building`'s double store is an inert field, not a broken arc.** The Goldenfurt gate lives in the zero-caller prototype cluster and is fed by its own fixture, where it does fire. Production never reads the field.
- **Niflhel was propagated.** The strike is real and CANONICAL (2026-04-19), it is registered in `deprecated_terms_registry.yaml`, and `settlement_layer_v30.md` §4.7/§4.8 are both headed *"(Niflhel Dissolution)"*. The string matches 136 files, not 9; genuine unreframed organisational residue is narrow.
- **`settlement_layer`'s S-003 / S-023-25 contradiction is an unmigrated SID reference** — those sentences were correct under the pre-PP-726 36-settlement scheme. The remedy is an ID remap, and it is residue of a 2026-07-13 sweep that migrated the YAML and missed the prose that motivated it.

---

## §6 OUTCOMES, EVENTS, SCALE ⏳

**No antagonist has checked this section.** On the two sections that did get one, roughly a fifth of headline claims were overturned. **Read these as producer-grade with an unmeasured error rate.**

- **37 distinct outcome/degree vocabularies.** The degree ladder is the one axis with a purpose-built guard and is correspondingly clean; everything the guard cannot see (it matches four spellings, scans only `.py`) has forked.
- **`key_type_registry_v30.md` — one CANONICAL Class-A file — carries nine outcome vocabularies**, including `:942` which puts **`graze` in the Failure slot** under a field named `degree`. `rout` and `withdrawal` — the two combat endings a chronicle most wants — are registered and **unwritable**.
- **`graze` has four incompatible meanings, and the Godot skeleton contains the collision internally**: `strike_module.gd:74` says `graze = partial`; `combat_config.gd:27` lists them as distinct. The sharpest port-time defect found.
- **Scale rosters: 4 (hard-enforced, `keys.py:415` raises) / 5 (doc) / 6 (threadwork).** Only `Personal` appears in all three. **A Foundational-depth Weaving — Ob 13, TS 90 — cannot emit a Key**, so the largest act in the world cannot be witnessed, cascade, or reach a chronicle. *This is the claim I most want attacked, because it may be two different concepts forced into one class.*
- **`alias_registry` ships phantom vocabulary** — `Past|Present|Future` genres (the explicitly forbidden form), `AMPLIFY|DIVERGE` (nowhere in the tree), `Regular`, a 4-member Gap scale — drift-locked by `--check`, and these are **player-facing screen labels**.
- **Three canonical Knot-Formation tables disagree**, and §5.6a's net annotations encode the ruled-out ladder on 3 of 4 rows.
- **The Church-dominance threshold is 40 / 60 / 65 / 100** across four surfaces.

---

## §7 VOICE ⏳

Also unchecked by an antagonist.

| | `narrative_voice_canon_v30.md` | Solmund corpus | `prose-writer/SKILL.md` |
|---|---|---|---|
| Status | **CANONICAL** | PROVISIONAL/DRAFT | a skill |
| Claims | *"the single source"* | — | — |
| Authors | **8 listed, 12 claimed** | **21 registers** | **12** |
| Cited by code | **nothing** | nothing | **the RATIFIED narrative engine** |

The two frameworks share **not one author name**. The four the canon claims but does not list are exactly the four the skill adds.

**The precedence runs backwards from status.** `narrative_engine_design_v2_churn.md` is RATIFIED and is `CURRENT.md`'s narrative-engine head; its bake directives name the **skill's** table and the **PROVISIONAL** Solmund registers, under the axis name *Certainty* — renamed thirteen days after that doc was ratified.

**Truth-0 has no register.** §18's rows run 1-2/3/4/5/6+ on a **0–5** track, so the Edeyja pole — the character who has fully accepted Thread-truth — falls off the bottom of every table. Two Jordan-REJECTED sources (ibn Gabirol, Abulafia) are still assigned. And **`CURRENT.md` has no `systems/world/` row**, so a session establishing currency by the book never sees any of it.

---

## §8 MACHINERY

- **M1 — the fractional-pool chokepoint.** See §2 R3. One `round()` defeats a ruling.
- **M2 — the planned TN guard's exclusion.** See §2 R2. Catchable before it is written.
- **M3 — ED-874.** The resolver text is real and says *"It supersedes the bare-stat-pool-vs-Ob dice approach."* It has **zero implementations**. Two sharpenings: **`validate_ed_citations.py` names ED-874 as NONEXISTENT** and uses it as the fixture for bad citations; and its only home on `main` is the byte-faithful capture of the evacuated `engine/params/`, which CLAUDE.md §5 rules *"history, not canon: where it disagrees with code, the code wins."* So *implement it* is not obviously the remedy. The `M = Mandate − difficulty` / `Ob = floor(Prosperity/2)+1` spec is at `ci_political_v30.md:221`, a **second** canonical surface — and the code implements neither.
- **M4 — Suppress is unreachable.** `apply_seasonal_ci(world)` is called with no kwargs, and the gate is `if suppress_attempted and suppress_success:`. **Step 4 Suppress cannot fire in any campaign season.** The two-doc obstacle conflict (Ob = M vs ⌊M/2⌋+1, diverging 3 points) is therefore moot until a caller is wired.
- **M5 — the anti-fabrication hole.** `pathres` resolves a fabricated `deprecated/…` path as **FORKED** (tolerated) where the same fabrication under `systems/` is **DEAD** (blocked), because `restructure_ledger.md:1195` asserts the whole `deprecated/` tree left `main` — false; it is present and load-bearing. The blocking gate is `broken_dependency_checker.py:219-228`, which routes FORKED to non-blocking `infos`. **And the control cannot see it**: `test_forked_status.py:51-53` blesses prefix-tolerance *"because the PREFIX genuinely evacuated"* — true for `designs/audit/`, false for `deprecated/`. The guard passes while its own stated premise is false for one of its rows.
- **M6 — PP.** `patch_register_active.yaml:9-12` says *"Nothing allocates a new PP"*; `:15-16` of the same file says *"Next PP number: 727 … allocate from reserved blocks"*; `id_reservations.yaml` carries six live PP pointers. Following CLAUDE.md §4's protocol turns a blocking gate red.
- **M7 — the rot-proof surface has rotted.** `ci_checks_registry.yaml:101-105` records `freshness_gate`'s issue as live and non-blocking; `:369-376` of the **same file** records it resolved 2026-07-11. Its `role:` line scopes the gate to `engine/params/`, evacuated 2026-08-05. CLAUDE.md §4 designates this file's `role:` lines as the one definition surface that *"cannot silently rot away from CI."*

---

## §9 WHY THIS KEEPS HAPPENING

Three causes generate most of the index. Fixing them is cheaper than fixing rows.

**S1 — No roster primitive exists.** Every registry maps *name → canonical string*. **None can express "these thirteen and no others."** That is why five conviction rosters coexisted invisibly and why `ci_names_consistency` skips `conviction` and `pressure_point` by design.

**S2 — The naming gate enforces exactly one rule tree-wide.**

```
enforce tiers across 113 entries: {'warn': 112, 'block': 1}
```

`ci_naming_check` builds its forbidden set only from `block`. **No conviction, pressure point, roster conflict or phantom vocabulary could ever trip it.** *(Confirmed the hard way: the pre-commit guard rejected an earlier draft of this document on that one rule, in the sentence describing that it is the only rule.)*

**S3 — There is nowhere to record that a ruling happened.** `CURRENT.md` has 22 rows and none is Convictions, Characters, or `systems/world`. CLAUDE.md §3 already knows these are *"doc homes, not yet formalized 1:1 subsystems."*

> **S3 is the mechanism behind §3.1 and behind R1.** J2 was marked resolved in `CURRENT.md`, was never executed, and was contradicted the next day — because there was no surface on which "this was ruled and here is what changed" could be written and read.

---

## §10 WHAT I GOT WRONG

Four errors, all the same shape, recorded because the shape is the lesson.

| Claim | Corrected |
|---|---|
| The TN-blind roller is the defect | **The roller is correct; the two non-7 declarations are the defect.** I read a discarded parameter as a broken mechanism without asking whether the mechanism was authorised — and the ruling that would have told me was cited in my own footnote |
| Resonant Style is a four-way | **The armature axis is a ratified distinct concept.** Merging would have destroyed it |
| An `accord` read/write asymmetry | **Assembled by matching a token across a registry key and a method name.** Two scales, two quantities |
| "Mandate computes to 0 for every faction" | **`Faction.L` *is* Mandate by explicit convention.** I cited the convention as evidence against itself |

**The shape: matching a name across two surfaces and inferring a defect, without establishing that the two surfaces are talking about the same thing.** It is the exact error class this repo's §0 names as costliest, and I committed it four times inside a sweep commissioned to hunt it.

**The second failure is procedural.** Findings I sold as new were already filed — the TN divergence twelve days old with a held remediation option; the Accord range with a one-line patch; the settlement types with a fix authored and cross-confirmed by two instruments; the conviction Scar no-op as Tier-0 next action. **The rule against this was written in this session's own documents, four days before I broke it.** The fix is one line and I am stating it as a standing instruction to myself: *read `audit/` and the handoffs before calling anything new.*

---

## §11 COVERAGE

**Verified by me personally:** every TN constant (AST + read at each definition); the TN Monte Carlo; every production importer of Tree A; the `cells_float` blocker (executed); the keep-set pin's actual behaviour; all seven pool formulas; the `sigma_leverage` rounding; the planned guard's exclusion; the conviction scar gate and the 12-of-43 count; `names_index`'s enforce tiers; the `alias_registry` inverted table; `SCALES` and `Key`'s fields; the world load (territories, S-037, type histogram); the `deprecated/` FORKED probe; the PP freeze; the 283 triage.

**Antagonist-checked:** §4 (person texture), §5 (place and polity), §8 (machinery).

**NOT antagonist-checked — read as a lower bound:** **§6 (outcomes, events, scale) and §7 (voice).** One antagonist is still running on exactly these. On prior evidence it will overturn something; the claims I flagged for attack are the scale-roster conclusion and the nine-vocabulary count.

**Declared unread:** `settlement_adjacency_v30.md`, `march_layer_v30.md`, `faction_behavior_v30.md`, `factions_personal_v30.md`, `worldbuilding_v30 §9.1`, most of the grounded card deck, `systems/ui/`, most of `audit/`.

**The standing qualifier:** every count of the form "N rosters" is a **lower bound**. The fifth conviction roster surfaced only because an antagonist checked a supersession banner's *scope*. **No instrument in this repo can tell us a count is complete, because — S1 — no registry can express a set.**

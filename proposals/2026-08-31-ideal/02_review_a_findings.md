# 02 — FABLE READ-ONLY REVIEW: body (a)'s individual findings
## Status: REVIEW (2026-08-31). Read-only, top tier, no write tools.
## Every ruling re-verified against the tree by direct read, not by trusting any stage's summary —
## including Part 3's.

---

## THE NULL RESULT — the contamination sweep, which was the highest-risk open question

**Question:** D-1 is struck, but body (a)'s roster verdicts were computed *using* it. If the instrument
scored seats assuming an unpracticed person has no verbs, how many verdicts invert?

> **ANSWER: NO VERDICT INVERTS.** The false reading lived in **lane rhetoric, not in the verdicting
> machinery.**

`07_seasons_edges_and_ground.md:220-222` does say *"she has no reach terms at all … It is an empty verb
set."* **But the same document's own option table computes EIGHT live acts** (`:305-318`), and the
matrix verdict row gives the actual THIN grounds: *"Eight acts, one dominates — foster out, whose cost
column is unpriced … three of four needs return zero."* **The machinery used the correct "adds no
verbs" reading plus dominance plus needs-zero.** SPECTATOR Torben rests on needs-zero and a hash-chosen
act; the four BLOCKED rest on missing objects; the rank-1 correlate rests on the `gap ≥ 3` supplication
rule, not on practice rank.

**What DOES fall:** convergence #19's *"from below"* half — *"being ordinary is being inaudible"* — is
false; an unmarked act at a market with four witnesses computes `1.0 × 2.0 × 1.0 = 2.0`, a **high**
band. The *"from above"* half stands. And `01_the_floor.md` §1 and §6, including its re-run table, are
**void**.

**This is a negative result and it is worth as much as a positive one:** the matrix survives, and I was
wrong to suspect it wholesale.

---

## THREE OVERTURNS

**D-3 — "the need formulas are specified nowhere" is FALSE.** `02_the_person.md:485-492` **does** carry
COMMITMENT (`urgency = round(d × unmet × stance[p].weight / 5)`) and EXPOSURE. The fix's own text
concedes it (*"nearly right, and the residue matters"*). **What survives is narrower and real:**
`unsatisfied` has **no predicate anywhere**; EXPOSURE's urgency is **unbounded with no normaliser**;
and the four-site range collision stands.

**D-6 — "`sovereign_fraction` is undefined over every Church office" is FALSE.** `14:477` defines it as
a **reachability query**, and reachability is **total on cyclic graphs.** Nothing is undefined. What
the cycle actually produces is **mutual revocability with no fixed point** — which body (b)'s reviewer
independently verified as *"genuinely elegant"* politics.

**D-9 — the brief's claim that its repair was never reviewed is wrong.** I-9 **was** reviewed and
**struck** (S-G). The finding also narrows: *"empty at every rung"* overstates, since `14:254-256`'s
revocation-in-fact already approximates absence-vacancy at office rungs.

---

## TWO SHARPENINGS, BOTH LOAD-BEARING

### S19 — a containerless cluster vacancy has NO CLOCK AND NO CONVENER

A conferral standing date opens *"at the horizon **the container** carries"* (`14:195`) — and a cluster
root **has no container**. So a vacant Cardinalate has **no date**, and nothing in the design can open
one. Compose with `14:239`'s **three-of-four** conclave and two seats already vacant: **a Confessorate
vacancy is unfillable — a full-cycle deadlock one death away.** Either the Consecration Crisis as
intended content, or a soft-lock. **Nobody has ruled which.**

### ⚠ AND THE ORDERING DEPENDENCY NOBODY STATED: **E1's repair is downstream of S19**

D-7 (office clusters unpetitionable) is **UPHELD on a type-level read** — `Petition(petitioner,
proposition, respondent_container, backing)`, and `carry` requires standing at
`respondent_container(P)`, so **a cluster has nothing legal to file at**, and *"being refused"* is
indistinguishable from *"having nowhere to ask."* B-11 accepted *"you address it to a person, and that
person can drop it"* as the price — **and the type cannot pay it.**

The live repair is `respondent ∈ ContainmentNode | Office` — which **queues at the vacancy date when
the office is vacant… except a cluster office's vacancy has no date.** So **D-7 is blocked on S19**,
and no document in the relay noticed.

### D-10 — SHARPENED, and it is a live contradiction between two shipped documents

The report said the holdingless have no material mode. **It missed its own strongest evidence.**
`13:285` **refuses a currency** (*"What dies: the flavour of coin"*) — while **doc 12 spends coin as
live mechanism throughout**: retinues cost *"coin per season, at a standing date"* (`12:149-151`);
*"A retinue's contract has a standing date each season at which coin is due"* (`12:510`); *"his needs
are computed from a larder that the coin was filling"* (`12:516-517`); *"Paying outsiders requires
coin"* (`12:663`). And `12:640`'s levy/hire R-check **passes only because the hire arm exists.**

> **The coercion document's central fork rests on an object the material document refuses to model —
> with no producer, no transfer act, and no unit.** `12` §6.4 asserts wage-fed hearths; `13` §1's
> `draw(h)` has no term for them.

Both repairs fell: `convey` was struck twice over (the fabricated quotation, and for repurposing
`settle_in_full` — whose **defining property** is that the judging set never fires on it — into a
deposit-carrying act); `found` was dropped.

---

## THE REPAIR DIRECTION NOBODY FILED — D-9 by presence

> **Presence is already a substrate roll-up.** So an absence predicate can be written over *presence at
> the container across the seat's existing horizon table* — **without `exercise`, and without new
> standing dates.** The world already knows where bodies are.

**This composes exactly where I-9 did not**, and it is the direct answer to the S-G defect: `exercise`
is identically zero at a hearth because a hearth has no remit; **presence is defined at every rung
because every rung has bodies in it.** Falsifiers: the deliberate-absence case (a head wintering
elsewhere), and the hostage repricing — **which fires on the King's household first.**

---

## THE BLOCKED-CORE CONSTRUCT — uphold as a shape, overturn as run

Against its own disposition document, **13 of 19 flagged cores were the design working, canon
artefacts, or rulings already on the books.** The disposition also **double-counts** (one character
appears in two categories, another is simultaneously SUPPLY and "needed no edit"). **An instrument with
a ~2/3 false-positive rate against its own follow-up is not bankable per seat.**

**But the shape is sound, and it is already a suite formula run by hand:** *enumerate the acts whose
object is the seat's want* **is `shortfall(p, prop)`**. With the narrowed needs formulas it becomes
**computable**: *a seat is blocked iff a computed need emits a proposition no act on the menu can
advance.* **The genuine residue is FOUR seats, not nineteen — and all four repairs have fallen.**

---

## D-2, THE ACT ECONOMY — the largest open ruling in body (a)

The contradiction is verified and **worse than reported**: `14:562`'s header says *ten acts*, the
paragraph narrates **seven**, against *"exactly one act per season."* **§8 is wrong under every
reading, including its own.** Every proposed resolution has fallen. And **Reading B keeps a live
foothold** in the text used against it: `14:91-92`'s `seat_items` *"consume the holder's own hours"* is
**already a fourth per-holder capacity quantity** against `14` §1's *"three quantities and nothing
else."*

**Gameplay stake:** whether a Duke's season is one pick among six shapes or a ten-act sweep is the
difference between **a personnel game and a decree game** at every rung above Settlement — and it
decides whether a player-Duke experiences the top of the ladder as **a demotion.**

Reading C is also the only reading **identical at every rung** — the fisher's `requisition` and the
Duke's `dispatch` are literally one call — but it carries an **unpriced cohort exploit**: individuate
your own watch, get eleven acts instead of one.

---

## D-5 — a silent relay leak

**Never reviewed, never integrated, and dropped between rounds with no ruling.** The finding splits in
a way nobody drew: the **Grauwald territory court** is a *suite object* with a convener, seventeen
seatholders and an eleven-item capacity that appears in **neither venue table** — a suite-internal gap.
The **Jarl and Inner Councils** are canon bodies with no rows — an *authoring* gap, and the venue tuple
already expresses them.

**One new defect against the orphaned fix:** its Varfell row sets *"item order runs by seniority of
deed"* — **an authored decision function with no actor**, against `05:190-194`, which forswears by name
*"the engine deciding which grievances matter with no person in the loop."*

---

## CONSEQUENCES FOR THE SUPERSEDING SUITE

| finding | consequence |
|---|---|
| **D-1** the floor | **DROP as a defect.** Fix three collisions with one ruled owner. Keep I-3 only. `01_the_floor` §1/§6 are void |
| **D-2** act economy | **MUST FIX — the largest open ruling.** Rewrite `14` §8 (wrong under every reading); rule the economy as a LIVE CHOICE with the `seat_items` foothold acknowledged |
| **D-3** needs | **FIX narrowed** — the `unsatisfied` predicate, the scale, the range collision. **Re-run the needs-zero verdicts**: SPECTATOR Torben is **stale** under the surviving fix |
| **D-4** relational | **FIX via the deferred `04` §6 read** — one afternoon, repeatedly postponed. Drop the settlement-scale gate |
| **D-5** councils | **small FIX + record the relay leak.** Respecify the Varfell convener |
| **D-6** conferral cycle | **STATE AS LIMIT; FIX S19; FLAG as the suite's top LIVE CHOICE** |
| **D-7** clusters unpetitionable | **FIX or STATE — never silently. Ordering: after S19** |
| **D-8** dominance | **STATE AS LIMITS minimum**; re-verify the Vossen component first |
| **D-9** vacancy-by-absence | **FIX with the presence predicate**, or state the limit. Cost the hostage repricing |
| **D-10** material | **MUST FIX — second-largest open item**, and the only one where two shipped documents contradict each other with both arms load-bearing |
| §2 correlates | **delete most**; state the TS non-monotonicity or rule it intended |

**Null-result note:** *every* strike re-checked held on direct read. **No Part 1–3 strike was itself
wrong.** The new yield of this pass is the contamination sweep's negative result, D-3's overturned
headline, D-5's silent drop, D-6's overturned clause, the **S19→E1 ordering dependency**, D-10's doc-12
sharpening, the stale SPECTATOR verdict, and the presence-based D-9 direction.

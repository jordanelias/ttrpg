# 12 — ANTAGONIST CHALLENGES, ROUND 2
## Status: CHALLENGES (2026-08-31). Cold read of the revised `10_SUPERSEDING.md` (1,898 lines).
## **1 FATAL · 8 MAJOR · 4 MINOR · 1 observation. NINE OF SIXTEEN ARE REGRESSIONS FROM ROUND 1's OWN FIXES.**

---

## A. THE LOOP'S FALSIFIER PASSED — the disposition audit

The protocol's falsifier is that a claimed disposition with no corresponding edit means the loop did
not run. **Audited all 24:**

> **20 fully real · 3 real-but-incomplete (C-3, C-7, C-9) · 1 real-as-edit-only (C-23).
> NO FABRICATED FIX.**

**And the C-2 REBUT is sound on direct read, verified by a second independent reader.** It looked for
any line making burial free, and for a later line qualifying the drop-deposit rule. **There is none.**
`05:206`, `05:207-208`, `05:211` and `05:218-224` all read exactly as quoted. **A finding four stages
believed is dead** — and the design already priced what everyone thought it didn't.

It also nearly banked a stale finding and caught itself: attacking §4.5's `sovereign_fraction` claim
with the digest's *"undefined over every Church office"*, **the attack failed** — review (a) had
already overturned that, since `14:477` is a reachability query, total on cyclic graphs. *The draft is
on the corrected side of the fact base.*

---

## B. D-1 · FATAL — the units error was RELOCATED, not removed

Round 1 fixed the sign. **The corrected formula moves the units defect into the other limb:**

`condition(site) += nature's term, via season_factor` — but `04:59` and `:62-63` make `season_factor`
**a dimensionless multiplier on a yield quantity, centred on 1.0.** So the formula **adds ~1.0 per
season to a quantity bounded in `[0,1]`.** The acts limb is clean; the nature limb is not — **and the
sentence asserting consistency covers only the limb that was already fine.**

Three compounding faults: it **inverts a shipped semantic** (`13:71-72` assigns permanence to `base`
and impermanence to `season_factor`; routing weather into a persistent accumulator makes weather
permanent and lets it enter yield twice, compounding); **`depletion` is still not free of a hidden
constant**, because condition now accumulates nature's term, so *"the seam runs out because people
worked it"* is false on its own formula; and **`[0,1]` is asserted, not enforced** — no clamp.

## C. D-2 · MAJOR — the shipped stochastic yield term is silently deleted

The document's only definition of `yield` drops `04:59`'s `(3 + d10)/8.5`, which `04:62` makes
load-bearing: *"A bad season is `d10 ≤ 3` — a 30% event costing between a quarter and a half of a
holding's contribution."* **Without it `stores` is deterministic and the larder bands stop moving.**
**This is the exact failure C-2 named as the worst available** — a shipped rule deleted without record
from a document claiming a reader can read it alone.

## D. D-3 · MAJOR — the payoff table under the rebuttal argues the opposite of the rebuttal

§8.4's own pseudocode carries both limbs of the regard rule. **Its payoff table two paragraphs later
omits `regard_gain` entirely** — so as printed, omitting is cheaper on cost, cheaper on risk, and
spends no capacity: **weakly dominant, which is precisely what the section spends forty lines
refuting.** *The term that actually breaks the dominance is shipped at `05:207-208`, the document
quotes the line, and then drops it from the analysis.*

## E. D-4 · MAJOR — the single-document property broke in the section that fixed it

**Three duplications, all in round-1 text**, against the document's own rule that *"if a rule appears
twice, that is a defect in this document"*: the Office-row justification stated **twice near-verbatim
in one subsection**, same two citations and same conclusion; the `exercise`-vs-presence argument
verbatim in §7.5 and §8.6 and alluded to a third time; and the "people starve inside a system working
exactly as written" case in §8.6 and again in §13.1.

## F. THE OTHER MAJORS

**D-5** — `carry`'s cost is re-denominated against `05:176` (*"one item of the **container's**
standing-date capacity"*) **with no ground given, unrecorded anywhere** — and **ruled against §3.4's
own conflict rule**, since the document itself says doc 05's declared subject is carriage.
**A fifth collision, ruled silently.**
**D-6** — `convene` and `compose_agenda` are **one operation under two names**, and the ambiguity lands
on a price: *"attaching costs the setter one of his own acts, exactly as `compose_agenda` does — it is
the same remit act."* Read literally, both cost one act together. **In the section rewritten to fix
price conflation.**
**D-7** — §4.5 rules conferral office-rooted **citing none of the three surfaces that call it a live
choice**, including the review this document is built on (*"FLAG as the suite's top LIVE CHOICE"*) and
`00_INDEX:109`'s *"Not an audit's call."* ⚠ **The direction is not what is attacked** — the evidence
does point office-rooted — **but the sweep behind it is marked incomplete by the fact base, and §4.5
says neither.**
**D-8** — §10.6 cites *"§14's own words"* for a threshold-fires-an-outcome row **§14 does not contain**;
recounted, all fourteen match source and none is that row. **And it drops the one review-(b) MUST FIX
the document loses entirely** — the variable-not-threshold ban, whose enforceable form (*"`force` and
`hold` never appear in a precondition"*) is carried nowhere.
**D-9** — §15.18 understates the coverage limit **by the document carrying §11's evidence**: the ledger
says doc 06 **and doc 12** are uncovered, and doc 12 is the load-bearing arm of the coin hole, the
naming ruling and §15.17.
**D-10** — §3.4's rule **does not resolve the `exposure` row** (one word over five objects, each owned
by its own document, so the rule selects nothing and the draft scopes by fiat instead), and the
practice-rank ruling **arguably inverts it**: rank is a field of doc 02's tuple, doc 10 *asserts* 0–7
in passing while doc 02 *uses* 0–5 structurally — and adopting 0–7 **orphans the rank-3/rank-5 verb
ladder.**

## G. MINORS

**D-11** §8.7's *"identical behaviour"* drops `05` §8.2's clearing rule, so nothing removes a dormant
row · **D-12** the C-23 fix reworded the bullet and left the falsifying sentence twenty lines above it
· **D-13** the Costed band's floor is unreproducible (Pool 12 recomputes to ≈17%, not 14%; the 28%
upper end checks exactly) · **D-14** three residues inside the C-3 fix — a cross-reference to the wrong
section, an office-held date's capacity owned by no row, and the retired phrase *"sitting capacity"*
surviving twice · **D-15** `draw(h)` defined at two sites with the first unmarked, and the conferral
horizon's carrier named three ways · **D-16** (observation, out of lane) the cohort exploit —
*individuate your own watch, get eleven acts* — is unrecorded in §16's D-2 row.

---

## H. TWELVE THINGS SURVIVED, each with the attack that failed

**The C-2 rebuttal** (searched for a qualifying line; none exists) · **§4.5's `sovereign_fraction`
totality** (attacked with a stale finding; **the attack failed and the draft is on the corrected
side**) · **the fourteen refusal rows**, recounted line by line, order and content matching one-for-one
· **§10.3's anti-leverage bound**, where Δ ∝ 1/N genuinely falls with N and the `share = 1` concession
is correct · **the doc-12/doc-13 hole**, re-read cold, both arms load-bearing, *not* a filing artifact
· **F2/F5/F10 severities**, all accurate as printed · **the dormancy ruling**, now resting on §3.4
rather than the refuted spine ground · **the `burn` inheritance**, which does ship · **no apparatus
anywhere** · **the refuted `convener`-as-Person typing not smuggled** · **E1 not treated as solved, the
low end not repaired, the arc tally not used as an estimate** · **every R-line conditional on L-4, the
up-stroke's additionally on D-2**, stated in three places.

---

## I. CONVERGENCE VERDICT

> **"The loop is running and it is working."** Every disposition corresponds to a real edit, the one
> REBUT is sound, and the most valuable thing in the document is still round 1's **deletion** of a
> mechanism built on a false premise. **Defect density is falling** — round 1 found five FATALs
> including a refuted premise under an invented mechanism; round 2 found one, and it is arithmetic in
> a boxed formula rather than a structural error.
>
> **It has not converged.** Nine of sixteen findings are regressions in text written last round — the
> expected signature of 325 lines of unreviewed prose, and exactly why the protocol re-runs.

**Cheapest high-value repairs:** D-1 (define nature's term as a signed function on `[0,1]`, or delete
the limb and state the fuse as act-only) · D-2 (restore `04:59`) · D-3 (put `regard_gain` in the table
and re-read the conclusion) · D-4 (delete one of two identical paragraphs). **D-7 needs a decision,
not a repair.**

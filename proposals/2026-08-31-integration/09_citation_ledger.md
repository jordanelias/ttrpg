# 09 — THE CITATION LEDGER (Part 3's verified fact base)
## Status: VERIFIED FACT BASE (2026-08-31). Built by direct read of the working tree, not from any
## earlier document's quotation of it. **Where this ledger and any earlier document in this suite
## disagree, THIS LEDGER WINS** — it is the only document here produced by opening every cited line.

**Why it exists.** This session's dominant defect is a confident claim that direct read refutes. The
ledger is the upstream fix: the Part 3 writer gets verbatim text with verdicts, so it never has to
trust a quotation made by an earlier stage.

**It immediately found five errors in my own prior work, including one that reassigns blame I had
already assigned.** Those are §Z below, and they are the most important part of this document.

---

## A. SIGNATURES AND RULES

**1. The three signatures — VERIFIED, but THREE SPELLINGS COEXIST.**
`11_code_shape.md:57-59`:
```
choose  : (Person, View)   -> Act
resolve : (Act,    World)  -> [Event]
witness : (Person, Event)  -> [Claim]
```
`01_substrate.md:212-216` gives singular `act`/`event`/`claim`. `09_churning_world.md:819-821` amends:
*"The spine writes `resolve(act, world) -> event`. … I propose `resolve(acts, world) -> events`."*
`00_INDEX.md:67-69` prints the amended plural. **Three forms on disk; the writer must not treat any
one as "the" signature without saying which.**

**2. The ownership table — VERIFIED.** `11:97`:
`| **Container (a rung)** | its stake, its judging set, its standing dates. **Nothing else.** |`
Four owner rows (94–99): Person, Container, Faction, and **Nobody** (`:99` — *"aggregates, norms,
densities, needs, openings, scale, reputation"*).

**3. §7 forbidden list — FOURTEEN ROWS, not twelve.** ⚠ **My briefs said twelve. That was wrong.**
Lines 209–222. The two most relevant here: `:212` *a deposit into a cohort carrying a VALUE rather
than a DISTRIBUTION* — whose own note reads *"This row exists because the defect passed every other
row in this table"*; and `:214` *a stored aggregate, norm, density, unrest or reputation field*.
Also `:213` (a pushed aggregate **or a field one is stored in**), `:219` (anti-leverage), `:221`
(per-entity branch), `:222` (authored per-person opportunity).

**4. §8 what must NOT be built — VERIFIED.** `11:243-245`: *"validators over the design documents,
freshness checkers, guards on the guards, or any apparatus whose subject is the repository's own
process rather than the game. A guard earns its existence only when the thing it protects is
load-bearing on the game."* §8's four tests are `:231-241`.

**5. R-1 / R-2 — VERIFIED, and R-1 CARRIES AN INLINE RETRACTION.** `11:113-116` R-1 ends
*"**Compute-on-demand, never push, never store.**"*; `:118-125` retracts its original "it receives
aggregates" wording per adjudication C-1. **Scope, per the preamble at `:111`: RUNG MODULES** — not
persons, not factions. *That scope is what makes `stores(h)` a precedent rather than a violation.*

**6. `01_substrate.md:490-491` — VERIFIED verbatim:**
*"**Container-level memory, ledgers, or councils that think.** Containers hold stakes, judging sets
and dates. Persons hold everything else."* §6 heading `:463`.

**7. `00_INDEX.md:28` — VERIFIED:** doc 01 is *"**the spine — binding on everything else.**"*

---

## B. THE WORLD-SUBSTRATE OBJECT (lane b's centrepiece) — the reuse claims hold

**8. `09:100` VERIFIED:** *"Every act declares `touches: {(object, mode)}`, mode ∈ `{read, alter,
exclude}`."* Exactly three modes. **`alter` exists unextended.**
**9. `09:102-103` VERIFIED:** *"Conflicts route to the substrate's `contest(container, prize,
claimants)`, unchanged."* Primitive owned at `01:363`.
**10. `03:531` VERIFIED:** `**Thread-Read**(person|place|object)` — **the `place` argument is already
in the signature**, already yielding *"prior configurations at a place"*.
**11. The dangling pointer — VERIFIED, both sites.** `02:4` *"Defers to: doc 10 (resolver), doc 07
(argument), doc 04 (Thread)"*; `02:435` *"Thread ops per doc 04"*. `00_INDEX.md:31` assigns **04** to
*The Hearth and the Community*. **These are the only two `doc 04` occurrences in the file, and there
is no Thread owner anywhere in the index (01–16).**
**12. `09` §12 refusal list — VERIFIED verbatim (803–813).** It **does** name *"a morale bar, unrest
meter, or **cohesion field stored on a container**"*. **The string "Thread" does not appear in §12.**
So: a **shape** refusal exists; a refusal **naming the Thread** does not.
**13. P1 SETTLE — VERIFIED at `09:55-59`** (not 56–58). Five items exactly: larders consume against
mouths · production resolves against Prosperity · wounds close or fester · bodies age and die ·
travellers advance a leg. **Ore depletion and silt accrual are in none of them.**
**14. `13` §5 — VERIFIED, both say "every season".** `:166-169` `base(H_mine) −= depletion(H_mine)`;
`:178-181` `transport_cost(Hafenmark ↔ sea) += silt_accrual`. ⚠ **`depletion` has NO independent
definition anywhere in the file** — it appears only as the subtrahend and once in prose. And
`transport_cost` is defined at `:132` as a function of a **route `r`**, not of a container.
**15. `04` §1.1 state table (29–36) — VERIFIED. EXACTLY TWO items are declared stakes:** `holdings`
(`:32`, *"this is the hearth's **stake**"*) and `seat` (`:33`, *"likewise a stake"*). **`stores`,
`pointer` and `banked_claims` are NOT** — and `banked_claims` is *"a standing date with a watch
predicate"*. `:37`: *"That is the entire hearth."*
**16. `09` §8.3 — VERIFIED verbatim (562–564):** *"**The only clock-driven quantities in the game are
matter, bodies, and the confidence of a memory.**"* Three classes admitted; five excluded by name
(standing, regard, grievance, cohesion, commitment).

---

## C. LANE (a) ITEMS

**17. Referent kinds — VERIFIED, at `02:227` (my reconciliation cited `:226`, which is the stance
tuple).** `referent ∈ Person | Faction | Proposition | Place`. Corroborated `01:148`. **No "object"
referent kind exists.**
**18. Practice-rank collision — CONFIRMED.** `02:153` *"rank 0–5"*, reinforced `02:199`; `10:33`
*"Practice ranges 0–7 … Realistic Pool therefore runs **1–14.**"* Do not confuse with `07:568`'s
`standing ∈ 0..7`.
**19. The untrained clause — VERIFIED, `10:33`**, verbatim as quoted by lane (a).
⚠ **But `10:27` does NOT say "option set".** It reads: *"Document 02 supplies **Capability** … This
document owns **the arithmetic that turns that into a die count**."* It disclaims Capability's
*composition* and claims the *arithmetic*. **See §Z-2.**
**20. `10:30` Pool formula — VERIFIED. NO Thread term:** `Pool = Attribute[relevant] + Practice`.
Thread is a **second pool through the same `roll`** (`10:192`), while `02:197-200` puts a conditional
`+ thread_pool` **in the pool expression**. The two documents disagree on placement.
**21. Pre-roll exposure — VERIFIED.** `10:132` *"Before any die is drawn, the resolver exposes …
both pool sizes, the obstacle interpretation, nothing else"*; `10:143` *"At the point of declaration
— before `roll` is called"*.
**22. The act-count contradiction — VERIFIED, and WORSE THAN REPORTED.** `14:562` header claims
*"one turn, **ten acts**"*; **the paragraph narrates SEVEN verbs**, all Duke Vaynard's. Against
`09:33` *"Every person and every cohort commits **exactly one act per season**."* So §8 contradicts
both doc 09 **and its own enumeration**.
**23. `convener` — VERIFIED, `05:197-200`.** *"= **the office** named by the container's charter …
It is an ordinary office (doc 14 §1): conferred, revocable, **vacant-able**."* All five examples are
role names. **C-3's Person typing is refuted on the text.**
**24. The nine convener rows — ENUMERATED. SIX ROLE, THREE PERSON.**
Person: Hafenmark Court Parliament → `Duchess Inge Baralta` (`:415`); Goldenfurt settlement court →
`Praefect Aldwin Storr` (`:416`); the Crown's council → `King Almud Almqvist` (`:423`).
Role: four Dicasteries → `its Cardinal` (`:417-420`); Masterpiece Examination → `the guild warden`
(`:421`); Löwenritter chapter sitting → `the chapter master` (`:422`).
**25. `seat_items` — VERIFIED, `14:91-92`:** *"An office's standing dates consume **the holder's own
hours** … Holding two offices does not double a day."* It is item 1 of three costs.
**26. The salience formula — VERIFIED. FLAT PRODUCT, NO `max`.** `03:335-349`; the clamp is on
`stanceweight` only, `[0.05, 2.0]`. The proposed floor lives **only** in `09:834`, doc 09's challenge
section — **not in the owning document.**
**27. A-6b / B-4 / B-1 — ALL VERIFIED.** A-6b `15:294-296`: *"**Carrying it into 03 is outstanding
work, not a settled fact.**"* B-4 `15:135`: *"if it has no claimants, it is not a standing date."*
B-1 `15:87-89`: three limbs — **contested · allocated at standing dates · factions fight over it.**

---

## D. THE TWO ENUMERATIONS THAT MATTER

**28. `exposure` has FIVE distinct definitions, not four.**

| # | what it is | where |
|---|---|---|
| 1 | a **need term** — urgency over dispensation terms | `01:174`, `02:490-492` |
| 2 | the **price of Passing** — explicitly *not* a counter | `02:62`, `02:78-81` (*"accrues no hidden counter on a clock … There is no timer"*) — **but `02:741` lists it as a carrier** |
| 3 | a **STORED, MUTATED hidden counter** per (actor, operation) | `03:574-579` (`exposure += extraction_weight`), `03:586`, `03:622`, applied `03:914` |
| 4 | a **DERIVED, NEVER-STORED** sum over an alignment edge | `07:149-157` (*"Exposure is derived, never stored … It cannot rise on a clock."*) |
| 5 | **contagion** exposure — a sum over co-presence loci | `13:215` |

**Senses 3 and 4 are the SAME CONCEPT implemented incompatibly.** And `07:556` lists, in its §8
refusal table, *"a 'known %' on a secret, **a stored exposure counter**"* — **doc 07 refuses by name
the object doc 03 §7 defines and mutates.** Sense 2 agrees with 4 and disagrees with 3 while `02:741`
lists it as state. **This is a three-document contradiction, live, on the surface lane (a)'s I-4 edits.**

**29. The view budget K collides.** `03:325-329`: `K = 7 + Focus` (→ 8..14), ±Knot, −Coherence;
applied in doc 03's own trace as `K = 11`. `09` asserts a constant **12 four times** (`:63`, `:133`,
`:151`, `:490`), plus `:689`'s *"top-12"*. Doc 09 also carries `K = 3` per cohort, which doc 03 never
mentions.

**30. Schoenland IS a containment node.** `09:684`: *"3 duchies · 14 provinces · 35 settlements
**+ Himmelenger + Schoenland** · districts · ~800 community nodes … hearths on demand"*.
⚠ But `00_INDEX.md:112-113` lists as genuinely open *"An off-board polity acting without a person to
carry it. **Altonia and Schoenland** exert real pressure from off the map."* **Two documents, and
neither settles the other.**

---

## Z. ⚠ FIVE ERRORS THIS LEDGER FOUND IN MY OWN PRIOR WORK

**Z-1 · THE FABRICATED QUOTATION IS MINE, NOT LANE (a)'s. I MISASSIGNED IT.**
`08_part2_reconciliation.md` §0 said the agonist *"appears to have compressed a doc-14 sentence into
a doc-07 quotation."* **False.** Direct grep:
- `proposals/2026-08-30-fixes/05_the_blocked_cores.md:418` — *"And `07 §4` gives the purchased basis
  **the cheapest named cut in the game**"*
- `proposals/2026-08-30-fixes/05_the_blocked_cores.md:471` — *"the purchased basis has the cheapest
  cut in the game"*

**I wrote that, in my own fixes document, days before lane (a) existed.** Lane (a) inherited it
faithfully from a source it was told to build on. The antagonist's strike stands — the quotation is
still fabricated, `07:288`'s purchased row still says *"money — the only basis whose cut is
symmetrically available to any rich rival"* — but **the author is me.**
**Revised count: FIVE fabricated or unverifiable objects this session, THREE of them mine.** And this
one demonstrates the mechanism the others only suggested: **a fabrication in an upstream document is
inherited by every downstream producer that trusts it**, exactly as C-3 was.

**Z-2 · My I-1 ruling rests on inference, not quotation.** I ruled for the antagonist that *"Doc 10
disclaims the option set (`10:27`)"*. **`10:27` contains no such phrase** — it disclaims Capability's
composition and claims the arithmetic. The nearest texts on the option set are `02:204` and `10:166`.
**The ruling's direction survives** (doc 10 does not claim the option set) **but I overstated it as a
quotation, which is the defect I was ruling against.**

**Z-3 · §7 has fourteen rows. My briefs said twelve, twice.**

**Z-4 · Three citation slips in `08_part2_reconciliation.md`:** `02:226`→**`02:227`**;
`09:56-58`→**`09:55-59`**; `09:689` says *"top-12"*, not *"K = 12"*. **None changes a verdict.**

**Z-5 · I asked for four `exposure` senses. There are five.**

---

## COVERAGE — what this ledger does NOT cover
Read in full: `00_INDEX`, `01`, `02`, `09`, `10`, `11`, and `08_part2_reconciliation.md`. Read in
cited regions with context: `03`, `04`, `05`, `07`, `08`, `13`, `14`, `15`, `16`. **Not covered:**
`06_down_stroke.md` and `12_coercion_and_force.md` beyond grep hits; the other assertions in
`06_`/`07_` antagonist documents (their claims were checked **against the tree** instead, which is
the stronger check); and `proposals/2026-08-30-fixes/` and `2026-08-30-play-space-coverage/` except
where item 31's grep reached them — **which is how Z-1 was found, and is a reason to sweep them.**

# 12 — PART 3 RECONCILIATION: the relay's close, and the finding that falls
## Status: ORCHESTRATOR'S ADJUDICATION (2026-08-31). Closes Part 3 and the three-part task.
## Inputs: `11_INTEGRATED.md` (agonist) · the cold antagonist's findings · `09_citation_ledger.md`
## Verdict on the integrated document: **UNSOUND** — and the reason is worth more than the document was.

---

## 0. The ruling, before the detail

**D-1 — "the floor" — is STRUCK. I verified it myself, three ways, and the antagonist is right.**

The floor has been in this session's survivor list since Part 1. `03_corrected_findings.md` §B listed
it as surviving *"on FORMULA evidence, not on the control"*. It was body (a)'s headline, the entry
term of Part 3's entry/exit framing, and the thing I have cited most often as the coverage
instrument's justification. **It does not survive direct read.**

| D-1 claimed | the tree says | verified at |
|---|---|---|
| an untrained person has **an empty verb set**, not a smaller pool | *"an untrained attempt is always legal (T1: a person with no office, and no training, can still try), **it is just a small pool**"* | `10_resolution_surface.md:33` |
| the rank-3 clause **gates** the option set | *"At `rank ≥ 3` a practice **adds** verbs to the actor's option list"* — **adds to a list, does not constitute it.** Only the rank-5 clause is a true gate | `02_the_person.md:204` |
| — | the unpracticed case is a **first-class term in the pool formula**: `practice[…].rank 0..5 (absent → 0, **unpracticed**)` | `02_the_person.md:199` |
| `mark_salience` = 1.0 makes an unmarked person **inaudible in both directions** | `mark_salience = 1 + 0.2 × (…)`. **1.0 is the identity element of a product**, not a floor and not a cutoff. And the suite states the consequence in the opposite direction: *"Maret Uln's transgression reaches **twice as far** as an identical act by an unmarked neighbour"* — the unmarked act **reaches**, at half the distance | `04_hearth_and_community.md:415-424` |
| the ordinary person's season is empty | **the suite already contains that worked season.** *"Torvald Aske, hamlet fisher, **no post**"* — menu *"fish; sell the boat; requisition a brother"*, reach 0.31, and **shortfall 0.47, IDENTICAL to Duke Vaynard's.** Closing line: *"**Nothing consults post; post shows up as more entries in `own_acts`.**"* | `05_up_stroke.md:52-62` |

**The integrated document quoted `10:33` and stopped before the clause that refutes it.** It then
read the same line a second time, in a different section, to extract the practice-range collision —
and did not notice. That is term-matching over concept-matching on a single line, and it is the
session's dominant defect committed by the document whose §7 counts the session's instances of it.

**What survives of the floor:** (i) a **3→5 advancement gap** for rank-5-only verbs — which is exactly
what lane (a)'s own antagonist narrowed I-1 to, and I upheld that narrowing in Part 2 without noticing
it also destroyed the finding I-1 was serving; and (ii) a **marked/unmarked publicity gradient of
about 2×** — which `04:425` declares deliberately as *"a structural caste effect produced by nothing
but visibility."* **A declared design property is not a defect.**

---

## 1. What falls with it

**§1.4's entry/exit framing is DROPPED, not narrowed.** With no entry term there is no symmetry. And
the antagonist's second objection stands independently: the framing paired body (b)'s headline against
a **non-headline** item of body (a), while body (a)'s actual headline — the blocked core — is itself
an **exit** test. *"Best finding"* was doing the selection, and the selection produced the symmetry.

**The honest narrative answer is simpler and I prefer it:** *both instruments found the same class of
failure — the design is good at starting things and bad at ending them.* Blocked cores are an exit
finding. The closure axis is an exit finding. The world-substrate hole is a prerequisite for a kind of
ending. **The design's problem is termination.**

**⚠ And that weakens the non-redundancy claim, which I must state rather than let stand.**
`10_comparative_judgment.md` §Q1 argued the two bodies are non-redundant because *"each body's best
finding sits in the other's structurally blind region"* — with the floor as (a)'s side of that pair.
**The floor is gone, and both headlines are now on the same side of the axis.** Non-redundancy still
holds, but on **weaker and different** evidence: (b) found the world-substrate hole, for which the
coverage matrix genuinely has no cell; (a) enumerates per-seat, which no arc corpus can do. **That is
a real but smaller claim than the one Part 3 was built on, and the document overstated it.**

**Also struck: §0.1's "body (b) is DOUBLY BLIND to the floor."** `03_arcs_41_55_and_emergent.md:297-302`
**restates D-1 verbatim** — *"Per the play-space report, verbs gate on a practice at rank 3+…"* — and
builds an arc route on it. **Body (b) was not blind to it; it read it and inherited it.** *Blind* and
*contaminated* are different states, and I used the first where only the second was true. **This is
also the third verified instance of the session's propagation path**: an upstream error inherited
downstream by a producer that trusted it. C-3, the fabricated quotation, and now D-1.

---

## 2. §3.3 is MANUFACTURED — struck

The document claimed the `10` §4.2 compliance frontier was *"reached independently by each lane's own
antagonist"*, citing `06` §E-1 and `07` §S-b. **`06` §E-1 is about a stored `exposure` counter and
says nothing about `10` §4.2, obstacles, or declaration.** Only lane (b)'s antagonist reached that
surface. The document's own §7.3 cites `06` §E.1 correctly for the `exposure` collision — **two
sections of one document contradicting each other about the contents of one citation.**

**And it is the exact sentence the document declared absent.** §3.4 says: *"Any sentence of the form
'found by both…' is presumptively this session's thrice-committed P-2 failure unless it demonstrates
structurally disjoint routes. **This document contains no such sentence, and that is the check to run
against it.**"* The antagonist ran that check. **The document contains one, and it is §3.3.**

The frontier itself is real and survives as a **one-lane** finding.

---

## 3. `11_code_shape.md:219` — the unaccounted row, and it lands on the only surviving EXTENDS

The integrated document said one of §7's fourteen rows lacks a clearance argument and it could not
say which. **The antagonist named it: `:219`, *"a personal effect on a group that is not a fraction of
that group."***

Why it is the right candidate, and why this matters more than a bookkeeping gap:
- `11:254-257` glosses what §7 does about leverage: *"fractions rather than flat amounts, option-set
  changes rather than modifiers."* `thread_condition(n)` is **a flat amount and a modifier** — wrong
  side of both.
- `02_the_person.md:211-213` names the design's own discharge of that row, and I verified it verbatim:
  *"**That split is also the anti-leverage rule (precedent)**: a person's contribution to a
  container-scale outcome is a *fraction of the container's own capacity*, because what they
  contribute is a verb the container can now attempt, sized to the container."*
- One person's `alter` act moves a place-scoped scalar that then enters **every subsequent actor's
  `resistance_pool` at that site** — a person-scale act with a container-scale effect **by neither
  licensed route.**

**The evidence was inside the relay and went uncomposed.** Lane (a)'s antagonist had already
identified `:219` as this suite's live anti-leverage row (its F-5). Nobody carried a lane-(a)
antagonist finding across to lane (b)'s object. **That is a defect in my orchestration, not in either
lane** — the relay produced the datum and I never crossed it over.

**RULING: not a violation, an unexamined row.** Whether `:219` bites turns on whether *"a group"*
covers *"every subsequent actor at a site"*, which the row does not define. **`thread_condition(n)`
may not be treated as §7-clear until all fourteen rows are walked explicitly, starting at `:219`.**
Since that object is **the entire surviving EXTENDS of both lanes combined**, this is now the single
most consequential open item in the integration.

**Mechanical origin found, and it is mine:** `00_REVIEW_BRIEF.md:44-47` — my own brief — paraphrases
§7 in prose as **eleven items**, silently dropping `:212`, `:213` and `:219`. Both lanes' antagonists
worked from that paraphrase. **The row nobody checked is the row my brief deleted.**

---

## 4. §3.4's headline survives — on a better argument, and after two untabulated candidates

**Upheld: no finding is independently corroborated by both instruments.** But:

**The structural argument is stronger than the one the document made.** All three arc-lane documents
carry `## Cross-read: …09_GAP_REPORT.md` **in their headers** (`01:7`, `02:7`, `03:7`). **No arc-side
finding can independently corroborate a gap-report finding by construction.** That is a fact about the
apparatus, not a verdict on four candidates.

**Two candidates the sources themselves nominate as strongest were not tabulated.** `04_SYNTHESIS.md`
calls Feldhaus's undeliverable revelation *"the strongest single convergence across the two
exercises"*; `02_arcs_19_40.md` calls Torsvald *"the strongest signal either process produces"*. **A
synthesis whose headline is "every candidate failed" must test the candidates its own sources
nominate.** Both fail — but for a reason the document never states: **the contamination is one-way,
(a)→(b)**, so the arc lane's *recognition* is contaminated even where the play-space verdict was
produced blind. **Torsvald most nearly survives**, because its arc-side datum is arc 37's premise at
ref `v30-snapshot-2026-06-28`, which predates both exercises and cannot have been seeded. The
blanket "seeded" reasoning does not reach it, and no better argument was supplied.

---

## 5. §2.1's "the code shape survives almost entirely" is partly an ARTEFACT — narrowed

The antagonist's sharpest structural point. The classification runs over a set already filtered by
**P-5: *"a fix that adds a system has failed."*** A rule that penalises additions, applied upstream,
**guarantees** a low-EXTENDS survivor set downstream. Reporting that count as *a result about the code
shape* — and writing *"the narrowness is the result"* — treats the filter's output as the filter's
discovery.

**Narrow to:** *the surviving set adds one object, which is what P-5 was applied to produce.* It is
**not** independent evidence that the code shape withstands the two instruments. The genuinely
independent evidence is narrower and still real: **lane (b)'s N-line survived a hostile sweep wider
than its proposer's**, and **no lane-(a) survivor places state anywhere** — which the antagonist
re-ran independently and confirmed.

**Also narrowed:** *"exactly ONE EXTENDS"* holds only by switching definitions mid-document — I-A-4
requires a mint path with **no demographic envelope to draw from**, which the design does not hold
today, and lane (b) admitted as much. **Restate as: one new stored field, plus one new generation
path admitted as authored.**

---

## 6. The fabrication count, again — and the sixth is authored by the counting document

The integrated document worked the count independently and got **five objects, all five authoring to
the orchestrator role, zero to lane (b)**. That was right as far as it went.

**There is a sixth, and it is finding 1 above: D-1's truncated `10:33`.** Authored in the document
whose §7 counts the instances. **So "five" is a floor, not a count** — as is "six".

**The distribution is the finding, and it has now survived three independent attempts to attack it.**
Lane (b) checked ~45 claims and fabricated nothing. Every other instance sits in a **synthesis or
adjudication document** — an audit, a gap-report headline, a binding corrections file, a
reconciliation, an integrated synthesis. **The rate tracks LAYER, not producer count.**

And the antagonist found the recurrence one level deeper than I did: my Part 2 reconciliation's §1,
*the section indicting the session's attribution failures*, **misattributes which cold reader caught
C-3** — it was lane (a)'s antagonist, and my own table two pages later files it correctly.

**What does NOT follow.** No guard, no validator, no checker. `11` §8 forbids it and P-5 forbids it
from the other side. The licensed repair is a writing discipline — every claim resolvable to a cited
unit — and the honest report on that discipline is that **the document declaring itself the first
artifact written under it violated it in its own headline.** A discipline is not a mechanism, which is
§0.05 of this repository's own doctrine, arrived at here from the other direction.

---

## 7. What Part 3 concludes, after the relay

**Emergent narrative.** Both instruments found the same class of failure: **the design starts things
well and ends them badly.** Blocked cores, the closure axis, the closure limit and the world-substrate
hole are all findings about **termination**. The entry claim was false and is withdrawn — the suite's
own worked example gives a postless hamlet fisher a non-empty menu and **the same shortfall arithmetic
as a Duke.**

**Code shape.** It survives both instruments, on narrower evidence than claimed. The residue is **one
stored field plus one authored generation path**, and the field is **not yet §7-clear** — `:219` is
unwalked, on the object that is the whole of the surviving addition.

**Working together.** Complementary coverage, never mutual confirmation. **No cross-body corroboration
exists, and by construction none could**: the arc lanes declare the gap report as a handed input in
their headers. The three concrete yields stand, minus §3.3, which was manufactured.

**Carried to Jordan, unsettled:** L-4 the playable-seat list (highest leverage) · L-1 clock-decay on
the Thread · E1's unsolved repair · the `exposure` store-or-derive contradiction · the `14` §8 rewrite
branch · convener cost · **and now `:219` against `thread_condition(n)`.**

---

## 8. The result I would keep if I could keep only one

Not the integration. **The relay.**

Six producers, four cold readers, and a verified citation ledger. Every substantive error in this
session — five and now six fabricated or unverifiable objects, C-3's false binding correction, D-1's
false floor — **was caught by a reader that had not seen the producer's reasoning, and not one was
caught by its producer.** The ledger caught three of mine in a single pass. The last critic caught the
document that was written *from* the ledger, using a clause that document had quoted and truncated.

**A finding I have carried and cited since Part 1 was wrong, and the only thing that found it was a
reader with no memory of why I believed it.**

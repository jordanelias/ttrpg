All primaries verified. I have what I need to rule.

---

## FABLE LANE 2 — NPCs, PERSONS, WORLD CHURN & EMERGENT NARRATIVE

### VERDICT IN THREE SENTENCES

PR #350's Person out-represents the archive NPC on epistemics (claims, false belief, forgetting) and on statics (marks, stance, ties, obligations), and its refusals of the archive's stored-aggregate machinery are almost all argued and correct. But its **moral layer is a facade**: it carries the 13 Conviction names and a motion law ("by scar and crisis") while containing **no mechanism that consumes a conviction weight and no mechanism that moves one** — both holes confirmed by the suite's own adversarial register — and the archive holds the complete, ratified, partially-sim-tested machinery (13×4 matrix as a Law-3-compatible Query, PP-718 per-Conviction scarring, Self-Other) that closes them with near-zero shape violence. Secondary but real: the two-kind tie graph plus universal memory-decay actively erases multigenerational enmity the archive names as load-bearing, and the suite inherits lane C's "acyclic provenance is not loop-safety" defect intact — its five structural tests all test signatures and write classes, never dynamics.

---

### PART 1 — MISSING, ranked by value

**M-2-1 — The Conviction roster is carried; everything that makes a Conviction DO anything is not**
- WHAT THE ARCHIVE HAS: The 13×4 axis matrix with per-cell calibration rationale (`snapshot/designs/personal/conviction_axis_matrix_v30.md:24-38`, verified by hand), the composition rule `armature_position[axis] = Σ convictions[c] × MATRIX[c][axis]` **recomputed at read, never stored** (`:209-236` — explicitly "no manual migration required; engine reads matrix at compute time"), the orthogonal Self-Other scalar `[-1,+1]` with outcome-driven drift κ=0.03 (`conviction_taxonomy_v30.md:69-105`, verified) so Cesare Borgia and a public magistrate share convictions but not beneficiaries, and 8 cultural-background templates with a measured authoring-cost reduction (lane E F-E-1/2/3; C-10 corroborated by four lanes).
- WHERE PR #350 STANDS: **MISSING-SILENT.** `02_ONTOLOGY.md:803-813` carries the 13 names, "1–3 primary + distributed", and registry-at-load — the archive's structured-concentration *labels* without its *numbers or consumers*. The only statement of what convictions do is one unformalized clause: "Convictions weight the option ranking" (`02:877`, `15_ADJUDICATIONS.md:163`). No formula in `07` §3, `08`, or anywhere else takes `convictions` as input. Not one of the six documents mentions the matrix, the armature, Self-Other, or the templates.
- WHY IT STILL HAS VALUE: Kind 2 (closes an open — `choose`'s conviction term is currently unimplementable, so every implementer will invent one, which is exactly how the archive got three Disposition formulas, lane D F-D-1) and kind 1 (the matrix passed a 12/14 Stage-10 sim battery per its header — thin, but more measurement than the suite's zero). Two bonus facts: the matrix is *already Query-shaped* (computed at read — the archive independently arrived at Law 3 here, C-3), and the 4-axis substrate is **live in the executing engine** — `TRACE_REGISTER.md:97,140` shows `AXES = (hierarchical, sacred, instrumental, traditional)` enforced with RAISE on every Key's `symbolic_dimensions`. The suite silently dropped an executing substrate's interpretive layer, replacing armature-based witnessing with stance-based `salience` (`02` §5.4) without recording the substitution as a departure.
- ITS N-LINE: Two persons with identical ledgers and identical stances must be able to choose differently *in a way that reads as character*. Without a conviction consumer the 13 axes are dead weights — the archive's own open question F11 ("are 9–10 of the 13 permanent landscape?", lane I F-I-13) becomes true of all 13 by construction, and the §5.5 moral layer's careful Conviction/Belief/Claim separation separates three things of which one does nothing.
- COST TO ADOPT: One 13×4 registry table + one person-side Query (`armature(person)`), + Self-Other as either a mark-like scalar adjusted at RESOLVE per outcome (act-driven, so it clears "nothing social moves on a clock") or derived at read. Zero new objects by the suite's own accounting standard.
- CONFIDENCE: HIGH. FALSIFIER: any formula in the suite consuming `convictions` — I grepped all 17 documents; there is none.

**M-2-2 — Per-Conviction Scarring: the moral layer's motion law is invoked by name and specified nowhere**
- WHAT THE ARCHIVE HAS: PP-718 (verified: `conviction_track_v1_pp718_vetting.md:3-29`) — each Conviction scars independently; crisis fires when **any single** Conviction hits 3+, not the aggregate; a multi-primary NPC is *more* resilient (the Charles V argument); the crisis d6 rolls **on the wounded axis**; a per-event × per-Conviction witnessing trigger matrix capped 1 Scar/season/NPC (lane E F-E-4/5); and the vetting doc's argument that aggregate scarring "collapsed the vector to a scalar at exactly the moment it became most interpretively significant" (`:81`). Lane I F-I-4 independently names Scars "the sole mechanism by which tactical outcomes become permanent narrative-arc change," plus its one live calibration defect (crisis table ignores which convictions the character holds weight on).
- WHERE PR #350 STANDS: **MISSING-SILENT, self-confessed.** `02_ONTOLOGY.md:792` states Convictions move "slowly, by scar and crisis"; `06:351` row 15 depends on "scars ratchet the arming threshold." The suite's own `ADVERSARIAL.md:596` rules the suppression scar "named as a mechanism, no object, no owner, no N-line," and `:599` finds `convictions`, `beliefs` and `Duty` have "**no rows in the write matrix at all**" where §4 says an unmarked cell is a write-class violation.
- WHY IT STILL HAS VALUE: Kind 2, direct hit — it is the specified content of a mechanism the ontology already asserts, and narrative-ledger row 15's only candidate producer. It is also the character-transformation engine (see Part 4).
- ITS N-LINE: Cut it and no outcome ever changes what a person is — Beliefs revise (an act), claims decay, but the axes beneath never tremor, so "revising a Belief is a tremor in the axis beneath it" (`02:856`) is a sentence about nothing; and suppression's return-at-magnitude has no producer.
- COST TO ADOPT: A per-Conviction counter is interior primary state exactly like ledger rows — written at WITNESS (the archive's trigger matrix is literally a `witness` post-step), which fills write-matrix rows the suite owes anyway. Adopt with the archive's own known defect fixed (crisis rolls only on held primaries).
- CONFIDENCE: HIGH. FALSIFIER: a scar/crisis specification anywhere in the 17 documents; grep finds only the two invocations above.

**M-2-3 — Loop-safety as a discipline: PR #350 inherits lane C's defect whole**
- WHAT THE ARCHIVE HAS: The principle — the `causes[]` DAG is acyclic by construction while every turn of a behavioural spiral emits a new legitimately-caused entry, so "a collapse spiral is, to the substrate, a perfectly valid, perfectly acyclic chain" and no cycle check will ever fire (lane C F-C-1, stated independently in two artifacts). Plus the empirical record of what un-damped loops cost: the canonical victory fired **0/120** with a self-defeating mechanism (F-C-7), a rich-get-richer Standing exclusion loop worth ~8 points of Order-share (C-6), and closures shipped **with termination arguments attached** — deterministic floors, saturating aggregates, hop-attenuated cascades (F-E-8, C-6).
- WHERE PR #350 STANDS: **MISSING-SILENT.** Its bounds are per-tick and per-row: `TerminationBreach` emission caps, `cooldown`, `excludes[]`, caller-supplied `max_depth` (`05` §4.3, `09` §4). All five structural tests (`12_TESTS.md:32-135`) test signatures, witnessing, order-independence and the write matrix — **none tests a trajectory.** `06` §6.3 concedes no convergence check exists; divergence (does a spiral terminate?) is the same unmeasured claim's other tail, and the suite ships declared positive-feedback shapes through primary state (row 15's ratchet, grievance→commit→force cycles) that no per-tick cap bounds across seasons.
- WHY IT STILL HAS VALUE: Kind 3 — failure modes found by running. The suite's stored-aggregate refusal deletes the *specific* archive spirals (Mandate runaway cannot exist when Mandate is a Query), which is genuine progress; it does not delete the class.
- ITS N-LINE: Not an emergent possibility lost but its inverse — an emergent *impossibility* unguarded: nothing prevents a campaign whose only story is one spiral. Per the brief's own test I note this is the weakest N-line form in my list and rank the finding on its kind-3 evidence, not its N-line.
- COST TO ADOPT: Zero objects. One discipline row (in `14_NERS.md` or `12` §4: *any mechanism whose output raises the probability of its own trigger ships with a damping argument, on the model of the archive's three patterns*) plus campaign runs on the instrument that already exists (`tools/balance_oracle.py`, per CLAUDE.md §7 — it is a campaign instrument, which is exactly what this question is). This also respects `06` §6.3's correct refusal to build process apparatus: the answer is running campaigns with win-path attribution (F-C-7's method), not a checker.
- CONFIDENCE: HIGH on the gap; MEDIUM on how much it costs in practice. FALSIFIER: a dynamics-level test or damping requirement anywhere in `12`/`14` — there is none.

**M-2-4 — Feud transmission on death: the two-kind edge does not produce multigenerational enmity, and memory-decay actively erases it**
- WHAT THE ARCHIVE HAS: Six typed edges; kinship and feud **cannot break by strain** — they transition (`npc_relational_graph_v30.md:93,115,129-132`, verified); on death, feud **auto-transmits along all strength-2+ kinship edges** (`:196-207`, verified), named in the primary itself as "the key load-bearing mechanic for ROTK-style emergent multigenerational narrative" (`:207`); rivalry→feud escalation gated on a Conviction Scar firing (`:192` — note it depends on M-2-2's mechanism).
- WHERE PR #350 STANDS: **MISSING-SILENT.** Its person-person kinds are `tie` (decays), `knot` (ruptures) and `oblige` — and `oblige` is destroyed by **death** (`02_ONTOLOGY.md:447`). Enmity's carriers are stance rows and `Grudge` ledger tags (`02:1092`), i.e. claims — and claim confidence decays under the universal rule the suite celebrates as "a governor loses the town by being forgotten" (`05:277,323`). The same rule means **a feud is lost by being forgotten**, which is the opposite of the Renaissance texture the archive grounded. Nothing at death writes anything into an heir's ledger or stance table; churn row 3's N-line ("succession never fires") covers offices, not grievances.
- WHY IT STILL HAS VALUE: Kind 5, with the N-line pre-named by the primary. Most of the six edges genuinely dissolve into PR #350's shape (liege-vassal → hold+oblige; patronage → upkeep/Debt; rivalry → emergent via ambition-obstruction, `06:350` row 14 — a *better* mechanism); this one residue does not.
- ITS N-LINE: The Capulet/Montague campaign. Without transmission, every enmity is one generation deep and every death is a reset button — removal-from-play collapses to conflict resolution, inverting `06`'s own row 3 logic.
- COST TO ADOPT: No new object. One WITNESS/CENSUS rule: a death Event deposits the deceased's high-weight negative stance rows (or Grudge-tagged claims) at inherited confidence into ledgers of kin reachable by `succeed`/`oblige` — inheritance as *witnessing with a channel*, which even preserves the suite's per-person arrival discipline (an heir abroad inherits the feud when the news reaches him, which is better than the archive's instant transmission).
- CONFIDENCE: MEDIUM-HIGH. FALSIFIER: any rule in the suite writing interior state to survivors on a death event beyond `until` on tenures; I found none (`02` §5.1's death seam handles tenures only).

**M-2-5 — The articulation layer: a genuine hole, with a scope caveat stated**
- WHAT THE ARCHIVE HAS: (a) A prior-art **law**, mechanism-surveyed across seven acclaimed titles: *no acclaimed narrative game generates text at runtime* — author a finite tagged fragment library offline; runtime only selects→substitutes→splices (lane K F-K-9). (b) An anti-starvation accumulator: un-articulated significance accrues per actor and resets when featured (`articulation_layer_v30.md:141-153`, verified). (c) A four-factor deterministic NLG whose discourse grammar is **derived from `causes[]`** — "cause→because, reversal→but" (F-K-11) — i.e. a renderer shaped precisely for PR #350's provenance chains. (d) The three-tier PP-688 pipeline with a numeric significance function (`:127-137`).
- WHERE PR #350 STANDS: **MISSING-SILENT.** The suite renders candidates at a `depth_score` and calls `informational` rows "rendered, never resolved" (`06:125,224`) — rendering is a named operation with no owner. Its declared exclusions are combat, social contest and mass battle only (`01:294-297`, `09` header); presentation is not on the list, so silence is not a decision — and this suite read 138 of 162 archive documents never. The executing layer is fully stubbed: `render_protagonist_lens`/`generate_chronicle_entry` are no-ops and all 13 articulation subscriptions are dead (`TRACE_REGISTER.md:707,952`).
- WHY IT STILL HAS VALUE: (a) is kind-5 evidence that forecloses the "call an LLM at runtime" proposal someone will otherwise make, and it converts into a **sequencing fact for `13_EXECUTION.md`**: fragment authoring is a large design-time cost with no line item. (b) closes a real mechanism gap: the Slate's rank concentrates attention (`tie_proximity`, `inertia` both compound toward the already-cast) and nothing guarantees circulation; the accumulator is adoptable as a barrier-built Query over un-rendered Keys — Law-3 compatible, no store. (c) is the only worked bridge from `causes[]` to readable story.
- ITS N-LINE: The funnel's "6 reach the slate" arrive as typed records; a provenance chain you can walk is not yet a story anyone reads. If Jordan scopes presentation to a later suite this downgrades to one sequencing row in `13` — I state that downgrade path per the brief.
- COST TO ADOPT: (b) is one Query. (a)/(c) are a future document plus a real authoring budget; do **not** adopt PP-688's tiers wholesale — its stored trackers and protagonist terms need Law-3 rework, and its own status header carries five contradictory markers (lane K DEAD ENDS).
- CONFIDENCE: HIGH that the hole exists; MEDIUM that it is this suite's to fill.

**M-2-6 — Hysteretic band edges on `condition`**
- WHAT THE ARCHIVE HAS: Asymmetric band edges — recovery re-crosses at a higher value than decline (+8 gap), grounded in regime-shift ecology (per brief; lane B's tree). What I can verify from my own lanes: the archive's trajectory numbers were *contested*, not calibrated — three canonical sources disagreed on MS's baseline direction and the −10/season cap was disputed (lane C OPEN QUESTIONS), so **the archive does NOT supply the wear:restoration measurement** `02` §10 item 1 asks for; the model is also global-scalar-shaped, which `05` §3's falsifier explicitly names as the wrong object. The *shape* transplants; the numbers do not.
- WHERE PR #350 STANDS: **MISSING-SILENT.** Band edges are single thresholds; a crossing is an Event (`05:291,322`). A site hovering at an edge strobes its verb set and emits crossing Events in alternation every season, and recovery to the exact edge value is as cheap as the last point of wear — the flux model's "neglect has a price" asymmetry vanishes at precisely the margin where play concentrates.
- ITS N-LINE: "We let the harbour go and getting it back cost double" — the political economy of neglect having a *ratchet*, which is the flux ruling's own dramatic content. Weak-to-moderate as an N-line; the strobing defect is the harder half of the case.
- COST TO ADOPT: Two columns per band edge in the exported params table; zero objects. Fold into the §10 item-1 measurement — measure per edge-*pair*, not per edge.
- CONFIDENCE: MEDIUM-HIGH.

---

### PART 2 — COVERED / SUPERSEDED

- **Per-territory Exposure bands (F-D-4)** — COVERED by a better mechanism: awareness is per-person claims via witnessing (`examine` is witnessed, `surveil` accrues exposure to you, `interview` leaks the question — `08:157-176`), concealment is the `cover` act, season-reset is confidence decay, and "the dominant faction may respond" is persons acting on claims. The band thresholds survive only as authoring reference. *(Ruled from lane D's citation, not re-read in the primary — flagged honestly.)*
- **False belief indistinguishable from true (F-D-2)** — COVERED, better: `reconstruct` "deposits at real confidence and is acted on" (`08:163`), forgery "true for every purpose that reads claims" until discovered (`02` §7.4). The archive's audited design intent survives with a cleaner mechanism.
- **Global 7-level priority tree (F-E-10, BG half)** — SUPERSEDED: it reads global stats with no perception model; Law 2 refuses exactly that, and `choose(Person, View, Sensation)` is the strictly better specification.
- **Ethical Framework Ob modifiers (F-I-6)** — SUPERSEDED twice: retired in the archive itself (PP-686 triadic calc), and PR #350's no-flat-modifier rule (`01` §4) finishes the job. Do not resurrect; lane I's dead-end note agrees.
- **"Disposition derived, never stored" (F-E-7, C-3)** — COVERED: it *is* Law 3, reached independently by the archive five ways; stance rows are primary state, aggregates are Queries.
- **Faction-as-weighted-people + institutional inertia (C-4, F-I-2)** — COVERED generatively: the reformist leader's court resisting him is nine establishment members' own `choose` (`07` §4), which is a better mechanism than an α-blended cascade constant; the NPCs-survive-collapse-as-poachable-free-agents property falls out of commit edges surviving nothing but themselves.
- **`role_acting` (F-I-3)** — COVERED adequately by remit acts + pool substitution + venue doors; note its full force returns only once M-2-1 gives convictions a consumer.
- **Insurgency from neglect + RAND dissolution (C-7)** — COVERED generatively (wear → needs → grievance stances → an uttered OUGHT + commits; churn row 13 mints the person the situation requires); RAND's taxonomy remains authoring reference for event rows. Whether it *fires* is `06` §6.3's admitted unmeasured claim — see M-2-3.
- **MS-critical → Mandate → Fracture (F-D-9)** — SUPERSEDED: a world clock writing a faction stat is what Laws 3+4 exist to forbid; condition bands gating verbs, then people acting, is the same story told legally.
- **MS naming fracture and off-bus writes (F-C-13, F-K-7)** — SUPERSEDED by construction: single-owner substrate, witnessed events; the archive's "central clock invisible to narration" defect is unrepresentable in this shape. Credit where due.
- **Arc-as-recognizer (F-K-6, F-K-10)** — COVERED: `causes[]` projection *is* the recognizer model; Datalog-style sifting is an implementation note that composes on it.
- **Roster tiering Active/Passive/Background (F-E-13, F-K-12)** — COVERED, better: `weight`/individuation is demand-driven in both directions with a named-by count (`02` §2.1.2); no authored soft cap needed.
- **Obligation-violation network propagation (F-I-15)** — SUPERSEDED: the archive's instant "all NPCs at Disposition ≥+1 take −1" is consensus broadcast, the exact forbidden shape; per-witness claim arrival produces the same ripple at telling speed.
- **Five-filter response chain (F-D-3)** — OBSERVATION, not a ruling: scene-scale response internals are SC/FI-lane territory by `09`'s declared scope. What I may report: the chain's Conviction-wound filter lands on M-2-2's missing mechanism, and PR #350's `interview` rolls vs obstinacy (`08:161`) where the archive's audited model kept dice off the disclosure decision entirely — the SC lane should rule whether that reintroduction is deliberate.
- **Facility-slot scarcity as crisis generator (C-8, F-K-13)** — OBSERVATION for the settlement/governance lane: `stake[]`, `capacity(date)` and venue doors look shape-sufficient, but that is not my lane's call.
- Dead-end hygiene: nothing above resurrects the 7/9-Conviction sets, Niflhel content, the Coup Counter, Momentum, or the tier-drift models — checked against every lane's DEAD ENDS.

---

### PART 3 — WHERE PR #350 IS AFFIRMATIVELY WRONG

1. **`02` §5.5 presents the moral layer as closed while its dynamics are absent — and the suite's own register proves it.** The motion column asserts Convictions move "by scar and crisis" (`02_ONTOLOGY.md:792`); `ADVERSARIAL.md:596` rules the scar "no object, no owner, no N-line" and `:599` finds convictions/beliefs/Duty have no write-matrix rows under a matrix that calls any unmarked cell a violation. A reader of `02` alone believes a mechanism exists; the suite elsewhere records that it does not. The archive supplies the missing content (M-2-1/M-2-2).

2. **R-11's ground for the Momentum cut is type-confused by the suite's own ontology.** `15_ADJUDICATIONS.md:157` (repeated at `02:853`): "a Conviction is already a stance row and keeps full resolver consequence without it." A Conviction is not a stance row — `02:55-56` defines them as different fields with different domains, and §5.5's whole table exists to keep them apart; and the resolver never reads either (`resolve` takes no Person, `08` §1). The cut itself survives on its other two grounds (flat pool bonus; fidelity breach) — the ruling stands, its first ground is false, and a wrong mechanism attached to a right conclusion is exactly what `02:171` warns "survives review by being agreeable."

3. **`06` §7's arc refusal argues against the wrong object.** The refusal row ("an arc is a projection; a projection with a store goes stale," `06:318`) answers the *narration* sense of arc. The archive's canonical arc object — the per-NPC A/B/C machine with eight typed conditioner classes, `npc_behavior_v30` §5 / `arc_expansion_v30` Part I, **CANONICAL, Jordan-approved 2026-04-17** (lane K F-K-4) — is a *behaviour-transformation* engine, and lane K F-K-16 records the corpus never reconciled the two senses. PR #350 scored the 55-arc illustrative corpus, picked the recognizer sense, and never engaged the ratified formalism by name (zero mentions of `npc_behavior`, A/B/C, or its conditioners across all 17 documents). The refusal is right about narration; it never met the object it needed to refuse, whose function it silently reassigns to the unspecified mechanism of item 1.

---

### PART 4 — THE SHARPEST THING I FOUND

**PR #350 answers my lane's question in two halves, and only tells you about one.** *What can this Person represent that the archive NPC could?* — statics and epistemics: everything, usually better. Marks, stances, ties, obligations, the ledger's confident wrongness, the per-person arrival of every consequence — on all of these the suite's shape is a genuine improvement on the archive, and its refusals (stored Disposition, the priority tree, the Ob-modifier frameworks, broadcast propagation) are argued and correct. *What can it not represent?* — **change of character.** And on that half the suite is not merely silent; it is silently circular.

Trace the circle. How does a person become someone else in this shape? Beliefs revise — but revision is "its own act, taken by the holder" under pressure that is "derived at read over recent challenging outcomes" (`02` §5.5.2), so revision reduces to the holder choosing it, and `choose` weighs options by... Convictions (`02:877`). How do Convictions move? "Slowly, by scar and crisis" (`02:792`). What is a scar? "No object, no owner, no N-line" (`ADVERSARIAL.md:596`). Every road out of the moral layer's statics runs through a mechanism the suite names and does not contain, and the write matrix confirms it structurally: there is no legal write anywhere in the loop that could move a conviction (`ADVERSARIAL.md:599`). Meanwhile the layer that *did* do this job in the archive — the CANONICAL A/B/C arc machine, whose branch conditions fired Conviction shifts and behavioural consequences off measurable state — was refused under a description that fits a different object (Part 3, item 3), with its function implicitly reassigned to the same unspecified scar-and-crisis mechanism. The circle closes on a hole.

What makes this the sharpest finding rather than merely the largest is that **the archive filled exactly this hole, in the suite's own architectural idiom, and the suite gives no sign it knew.** The 13×4 matrix is computed at read with no stored armature — the archive independently applied Law 3 to it in 2026-05 (`conviction_axis_matrix_v30.md:230-236`). PP-718's per-Conviction scarring is interior state written at witness-time — precisely a WITNESS-class row, the shape the write matrix is missing. The witnessing trigger matrix (F-E-5) is a `witness` post-step. Self-Other drift is outcome-driven, not clocked — it clears "nothing social moves except by an act" as written. Four axes of this machinery are *live in the executing engine today* (`TRACE_REGISTER.md:97,140` — `AXES` enforced with RAISE on every Key). This is not a rich legacy mechanic pleading for resurrection against the meta-rule; it is the specified content of mechanisms the ontology already asserts, pre-fitted to the ontology's own laws, cut by the 138-of-162 unread gap rather than by any argument. The adoption adds no system: one registry table, one Query, one interior counter family, and the write-matrix rows the suite owes regardless.

The falsifier for the whole finding is a single sentence Jordan could rule: *"Convictions are flavor; character change enters this game only through Belief revision, and Beliefs revise on social outcomes alone."* That would make the moral layer's statics sufficient, M-2-1/M-2-2 mostly moot, and `02:792`'s motion column a defect to delete rather than a promise to keep. Until that ruling exists, the honest description of PR #350's person is: **a better witness than the archive ever designed, wired to a moral engine that is currently a nameplate.**

---

**Coverage note** (what I checked, so silence is legible): all six assigned primaries in full plus `09_THE_SEAM`, `12_TESTS` (headers+§3), `15_ADJUDICATIONS` R-10–R-13, targeted greps of all 17 suite documents for scar/crisis, the six edge types, self-other/templates/matrix, hysteresis, articulation/render, spiral/termination, and npc_behavior/Resonant/Disposition; hand-verified four archive primaries (`conviction_axis_matrix_v30.md` in full, `conviction_taxonomy_v30.md` §3, `conviction_track_v1_pp718_vetting.md`, `npc_relational_graph_v30.md` §2–§4) against the lanes' citations — all four citations accurate. Not verified by hand: lane D's Exposure state-graph citation and lane B's +8 hysteresis figure (outside my input set; both used only for rulings that do not turn on their exact values). I produced no files; this message is the deliverable.
All verification complete. Assembling the report.

---

## FABLE LANE 3 — THE ADVERSARIAL CROSS-CUT

**Paths.** PR #350 = `/home/user/ttrpg/proposals/2026-08-31-unified-code-shape/` (cited below as `NN:line`). Archive snapshot = `/tmp/claude-0/-home-user-ttrpg/78360267-ece4-57b1-8568-be13abd76bad/scratchpad/snapshot/` (cited as `snapshot/...`). Lane files = `/tmp/claude-0/-home-user-ttrpg/78360267-ece4-57b1-8568-be13abd76bad/scratchpad/lanes/LANE_*.md`.

**Coverage.** I read all 12 lane files in full; `ADVERSARIAL.md` in full; `15`, `07`, `06`, `12`, `13`, `16` in full; `05` in full; `02` §§5.5, 7–10 and targeted greps; `01` §4; `14` §5–§6; and I verified the load-bearing archive citations against snapshot primaries myself: `snapshot/designs/architecture/player_agency_v30.md:55, 297–319, 361–374, 436–458` and `snapshot/designs/audit/2026-04-28-political-dynamics-session/15_stress_tests_batch3.md:164–181, 414–438`. I did not open `03`, `04`, `08`–`11` beyond greps, and I rule on nothing engine-side (`10`'s `[engine]` claims are observations only, out of my lane).

### VERDICT IN THREE SENTENCES

The architecture survives this cross-cut too, but its two highest-confidence *dispositions* do not: the act economy was closed as "answered by precedent" when the corpus's only CANONICAL precedent (unread, among the admitted 138) says 3–5 scene-actions and Jordan has now ruled ~5 — and the suite's own attention layer (4–9 slate, 4 acted-on/season) had silently inherited that precedent's numbers while the act economy declared 1. Second, the suite deleted, as Law-3/Law-4 violations, every damper class the archive ever *verified by execution* — the saturating aggregate, the deterministic floor, the mean-reverting drift — while shipping at least two designed ratchets, licensing no bounded-loop assertion among its four guards, and refusing the only instrument that would notice, so the one defect class the archive found by running 120 campaigns is the one this shape cannot currently see. Third, the suite reproduces at its own HEAD the archive's characteristic failure (a ruling and a landed edit are different events): the `opening_set → Candidate[]` overturn is folded into `08` and `15` and still un-landed in `07:137` and `12` T3, in the same fold pass that landed the Momentum and conflation fixes.

---

### PART 1 — MISSING (the deliverable), ranked by value

**M-3-1 — A termination argument (and one owed measurement) for cross-season feedback: the shape deleted every damper the archive verified and put nothing in the slot**
- WHAT THE ARCHIVE HAS: Lane C F-C-1 (`archives/audit/2026-06-04-loop-safety-ledger/key_cascade_scope.md` §6): *acyclic provenance is not loop-safety* — every turn of a behavioural spiral emits a new, legitimately-caused event, so the DAG grows forward and never trips cycle detection; there is no engine-level bounded-loop assertion anywhere. Plus the closed-loop catalogue C-6: FSS-LOOP-1 deterministic floor, FSS-LOOP-2 conditional re-muster, LPS-2e saturating + mean-reverting Mandate (**sim-verified bounded/convergent over 30 seasons**, lanes B & F, `snapshot/designs/territory/settlement_layer_v30.md:147–179`), the H-10 rich-get-richer Standing loop (~8pp of Order-share over 3 years, fixed by redefining the counted event, `12_development_specification.md:1603–1636` via LANE_H), and F-C-7's 120-campaign battery proving a self-defeating victory condition fired 0/120.
- WHERE PR #350 STANDS: MISSING-ARGUED, but the argument covers the wrong object. `12:167–176` refuses "a convergence checker" (correctly, as process-apparatus); `12:180–196` owes six measurements, none of which is spiral boundedness; `13:173–179` licenses four guards, none a bounded-loop assertion. Full argument in PART 4.
- WHY IT STILL HAS VALUE: kind 3 (failure mode found by running) and kind 1 (the archive has the only executed dynamics evidence in the corpus).
- N-LINE: without it, the first campaign that exhibits a grievance→commit→revolt→suppression-scar spiral (`06:340`, `06:351` — the ratchet is *designed*) is indistinguishable from the design working, until a player reports it.
- COST TO ADOPT: zero objects. One added row to `12` §6 (a sustained-shock two-arm campaign battery, the F-C-7 method, on the already-licensed n≥100 control discipline of `12:148–156`), plus a per-mechanism termination argument as part of `14`'s existing N-line discipline — the archive's own defection cascade shipped with one attached (LANE_E F-E-8, `npc_relational_graph_v30.md` §7: "net per-cycle gain < 1 ⇒ damped").
- CONFIDENCE: HIGH that the gap exists; MEDIUM that any specific spiral is live (none has been run — which is the point). FALSIFIER: a written argument that ranked-not-summed needs, worth-normalised exposure and salience clamps jointly bound every act-caused feedback path; or a battery showing shocks damp.

**M-3-2 — Band-edge hysteresis (ED-882), the one piece of the archive's world-trajectory model that survives the global-scalar refusal**
- WHAT THE ARCHIVE HAS: LANE_F F-F-7, `snapshot/designs/world/ms_trajectory_v1.md:89–107` — **RATIFIED (ED-882, 2026-05-29)**: band crossings are hysteretic, the recovery edge sits +8 above the collapse edge, grounded in regime-shift ecology (Scheffer/Holling), with a 12-point leading-warning window.
- WHERE PR #350 STANDS: MISSING-SILENT. `05:289–299` makes a band crossing "an Event people witness" and a verb-set change; `15` R-8 prices ordering *at* a band edge to the last bit — and no band has a re-entry edge distinct from its exit edge. `ms_trajectory_v1.md` is not cited anywhere in the suite; it is one of the 138.
- WHY IT STILL HAS VALUE: kind 3. A site held at wear≈tending equilibrium — precisely the "some tend" row `05:28` calls *the game* — oscillates across the band floor and fires a witnessed crossing-event stream both ways, strobing the verb gate and the slate. `06` §4.4 already treats anti-strobing (inertia) as a design value for attention; the world layer has no counterpart.
- N-LINE: without it, the most contested sites in a campaign generate crossing-spam and verb flicker at exactly the equilibrium the political layer is designed to produce; recoveries are also as cheap as collapses, which the archive's ecology grounding says is false of substrates.
- COST TO ADOPT: composes. Bands on `COND_SCALE` become `(fall_edge, rise_edge)` pairs in the exported scale — one exporter change, no new object, no fourth clock (the pair is static schema, not state; the "current band" is derivable from condition history already in the log).
- CONFIDENCE: HIGH on the mechanism's value; MEDIUM-HIGH on cost (the band-is-pure-function-of-condition property is lost, and `T4`'s bit-identity test must then hash the band term too). FALSIFIER: a ruling that crossing-spam is acceptable churn, or a demonstration that `wear`-vs-acts equilibria don't sit at edges in practice.

**M-3-3 — The cohort construal spread already has a representation: the archive's cultural-background templates**
- WHAT THE ARCHIVE HAS: LANE_E F-E-3, `snapshot/designs/personal/conviction_taxonomy_v30.md` §4–§5 (CANONICAL): 1–3 primaries at 0.6–0.8 plus one of **8 named cultural-background templates** at 0.2–0.4, with a *measured* claim: structured-concentration NPCs "produce smoother aggregate vectors and more interpretable armatures" and reduce authoring load.
- WHERE PR #350 STANDS: half-carried, and the missing half is exactly the open item. `02:803–806` ships the identical 13-name roster **and** "one to three PRIMARY convictions plus distributed weight" — F-E-1 and half of F-E-3, uncredited. `02:1214–1215` (§10 item 4) then carries "the cohort's construal spread — where it lives, what produces it, what an individuating member draws from" as OPEN. The archive's template is the answer-shape: the spread is `(primaries, weights, template_id)`; an individuating member *draws* from the template distribution (`01:252` already demands a distribution-draw); the template registry is the producer.
- WHY IT STILL HAS VALUE: kind 2 — a mechanism closing a carried-open item, with an authoring-cost result attached.
- N-LINE: without a named representation, every cohort author re-invents a spread ad hoc, which is the pre-F-E-3 authoring cost the archive measured its way out of.
- COST TO ADOPT: one registry (the 8 templates as world-authoring content), zero new types.
- CONFIDENCE: HIGH. FALSIFIER: the templates encode NPC *psychology* and a ruling holds cohort construal to be a different kind of distribution (e.g. over claim-construals rather than conviction weights) — then only the pattern transfers, not the content.

**M-3-4 — `leaders()` should read `succeed` edges before its comparator; the archive litigated this exact two-tier shape**
- WHAT THE ARCHIVE HAS: LANE_H F-H-9 (`12_development_specification.md:1026–1065` + `25_integration_npc_behavior.md:108–134`): algorithmic succession was found insufficient for named heirs; the fix is an explicit-designation check *before* the algorithm. LANE_A F-A-11 / LANE_G F-G-6 (ratified FSS-1): "who leads" and "does it fracture" are decoupled, because a comparator alone mis-decides near-peer cases.
- WHERE PR #350 STANDS: MISSING-SILENT. `02:1210–1211` (§10 item 2) proposes `commitment degree × backing raisable`, "adopt and record" — no mention of the `succeed` Tenure the shape itself ships (`02` §4.1), which is the designation carrier sitting one section away.
- WHY IT STILL HAS VALUE: kind 5, small but precise — a comparator that ignores standing designations makes every named-heir situation a computed surprise, and `06:349` (row 13: deposition = `leaders()` returning somebody else) makes the comparator load-bearing on the whole deposition story.
- N-LINE: the withheld-death-notice and hostage-politics rows (`06:338–339`) both turn on succession being *contestable*; a pure comparator with no designation input deletes the "disputed will" shape entirely.
- COST TO ADOPT: one clause: `leaders() = holder of an un-lapsed succeed edge, else comparator`. Zero objects.
- CONFIDENCE: MEDIUM-HIGH. FALSIFIER: a ruling that `succeed` edges are scoped to holds only and faction leadership is deliberately designation-free.

**M-3-5 (downgraded to observation)** — the archive's cascade-attenuation pair (×0.7 per scale boundary, salience floor 2, LANE_H F-H-7) and the K=6 saturation shape are prior calibration art for `12` §6 row 4's owed `K`/`L` measurement and `11`'s assumption-grade rows. Designed-and-sim-poked rather than measured, so it closes nothing — but it is a starting range where the suite currently has none. No N-line of its own; recorded as reference, not a finding.

---

### PART 2 — COVERED / SUPERSEDED (considered and dismissed)

- **C-1/C-3 "derive, don't store" (8 lanes)** — COVERED: it *is* Law 3; the archive's six independent convergences are corroboration for the suite, not a gap. (The one thing Law 3 does not inherit is LPS-2e's *feedback* half — that is M-3-1/PART 4, not this row.)
- **C-2 Domain Echo (throttled, queued, single-owned)** — COVERED-BETTER: RESOLVE-at-boundary plus the fractional-effect rule (`01:259`) does the anti-manipulation job without a scalar teleporting into a faction integer, which is the defect all seven lanes circled.
- **F-J-16 multiplayer Echo cap-stacking exploit** — COVERED structurally: no per-character cap exists to coordinate around; act scarcity is the cap.
- **Lane B open question 1 (intra-Accounting firing order unspecified, "surrenders or riots first")** — COVERED and strengthened: `12` T4 order-independence with bit-identity is the strongest answer in either corpus.
- **F-A-2's decaying national-event ledger** — COVERED: claim-confidence decay at WITNESS reproduces "events fade rather than ratcheting" per-knower, which is strictly richer.
- **F-I-15 Obligation timers + violation network-propagation** — COVERED: `05:326` row 11b (act-term ripening) for expiry; witnessed breach + each third party's own stance re-evaluation for propagation — emergent rather than ruled, unpriced but plausible.
- **`annex`/`secede` vs F-G-7 fractional province ownership** — COVERED: mixed holds *derive* fractional control by construction; Consolidation is an act; the per-Accounting Fragmentation *Check* is an actorless threshold and is refused with the cost priced at `06` §6.1. Not a gap.
- **C-7 insurgency-from-neglect / RAND dissolution** — COVERED in shape by `06:340/342` (revolt with no meter; vacancy politics) with the promotion *threshold* refused as a threshold; the RAND taxonomy (ED-881) stays valuable as calibration reference only. Do not restore the GD-3 trigger as a mechanism.
- **C-8 seat scarcity (3 Wings)** — GENERATED-IF-AUTHORED: cardinality on `contain`/`hold` plus `capacity(date)` can express it, but nothing produces the crisis without authoring the cap; one registry row, content not architecture.
- **C-9 Church capture-by-helpfulness** — GENERATED: a Chapel is a Site with drawers and an office whose establishment acts; Pastoral Assumption is a person's act on a vacancy the shape already surfaces. Archive remains the content source; no mechanism owed.
- **Goldenfurt Π-homeostat + 28-card pressure deck (F-F-4/F-F-5)** — SUPERSEDED: trigger purity (`05:167–182`) indicts the deck's social-pressure gating by name; the CG-1 mis-signed-decay lesson is already institutionalised as `12` §1's falsifier discipline.
- **F-D-3/F-H-2 five-filter chain / computed armature** — COVERED by `choose` over View/stance/claims: same job (no per-event authored reactions), different and smaller mechanism.
- **F-L-7 Witness Mode** — SUPERSEDED-BETTER: "100% resolves" at `auto` fidelity generalises it and drops the player-only 0-cost Read (which violates P-A on its face).
- **F-L-10 one between-scene currency** — COVERED, and it is the archive's own argument *for* the suite's act-as-only-currency shape; see PART 3 §A on what it implies for scene/act.
- **F-K-9 no-runtime-text-generation law; F-K-2 anti-starvation weight; F-J-19 Accord≥2 victory brake** — out of the suite's declared scope (rendering, chronicle, victory); nothing contradicts them; carry as reference. *Observation, not a ruling: whoever builds the articulation layer should start from F-K-9's convergent survey.*

---

### PART 3 — WHERE PR #350 IS AFFIRMATIVELY WRONG

**A · The act economy — wrong in bold, twice, against its own funnel, against the corpus's canonical precedent, and now against Jordan.**
Verified: `02:1096–1098` and `07:151` declare one act universally; `07:18` (the table), `07:325` and `07:342` repeat it; `06:195` states the funnel — 6 surface, **4 acted on, per season** (~200 played over 50) — which is arithmetically incompatible with a 1-act player; I checked `ADVERSARIAL.md` §§5.6–5.7, which brush the funnel from the throughput side and never record the collision, so the ruling doc's claim that the adversarial pass missed it is confirmed. The archive precedent is not merely "a 5 exists": `snapshot/designs/architecture/player_agency_v30.md:55` (CANONICAL 2026-04-17, five-document convergence per LANE_L F-L-1) specifies 3/4/5 scene-actions against a 4–9 slate with Standing +1/+2 (`:369, :371, :450–451`), and the suite's *own attention layer wears those numbers*: `07:320` ships a "4–9 candidates" slate — the union of the archive's three difficulty rungs — and the funnel's 6-surfaced/4-acted matches the archive's Normal rung (5–7 slate / 4 actions, `:300`). The attention layer inherited the archive economy end-to-end while the act economy contradicted it, and no document reconciles them.

What this voids beyond the ruling doc's list: **(1)** `14:139` — the petition-spray dominance defect "closed — PROVISIONALLY by one act per person" is now un-closed; at ~5 acts a person can spray five petitions, and `14`'s own "NOT RE-RUN" row now covers the budget itself. **(2)** `ADVERSARIAL.md` §5.6's S-UP break (a convener's `compose_agenda` costing his whole season, "the common case") largely dissolves at 5 — the adversarial pass is itself stale on the new budget, and its §5.7 news-transport arithmetic (37 person-seasons) divides by ~5. **(3)** `05:74–79` prices `wear` in person-seasons "drawn from the one-act budget" — off 5×, though the *sign* of the argument survives. **(4)** `02:1127–1129`'s deletion of `seat_items` rests on "one allowance: the act"; at five allowances the one-quantity-two-sides identity must be re-argued (the double-count evidence survives; the identity claim does not follow from it).

**Can "scene" and "act" be the same unit without a player-only mechanism?** Yes, on exactly one mapping, and the archive supplies it: the archive's season-level currency is the *scene-action* (a scene costs 1–2 of them, and contains 1–3 subsystem interactions — `player_agency_v30.md:458`, F-L-2), and its one-currency rule (F-L-10) says the between-scene resource is the only season-level currency. Map PR-350's **act ≡ scene-action** (~5/season, universal); a **scene** is then the *rendered resolution of an act at `played` fidelity* — a camera artifact, not a budget unit — and the archive's within-scene interactions live inside `resolver_ref`, behind the `09` seam, where fidelity invariance already governs them. What may *not* be imported: the archive's player-only scoping (F-L-4: NPCs deliberately had no such budget) and its Standing +1/+2 — reading 3 of the ruling memo, the one the archive actually took. On the coordinator's question: **the symmetry claim survives** — the archive is not "wrong," it made the asymmetric choice a GM-shaped design can make and PR #350's Law 1 refuses for argued reasons (`07:23–26`, `06` §4.1's elite-politics back door) — but symmetric-~5 is then a **departure from the only ratified precedent** and under the suite's own §0 rule (quoted at `15:309–310`) it must be declared and priced: ~5× NPC-side act volume (and therefore candidate volume — the funnel's 190–200 is not scale-free), and the deletion of the archive's difficulty axis (3/4/5 *was* the difficulty dial; a flat 5 answers LANE_L's open question about single-difficulty collapse by choosing the Narrative rung, which someone should say out loud). The archive's Standing bonus, notably, is *covered better* by the suite's own establishment/dispatch mechanism — `player_agency_v30.md:369` even says "from faction resources," which is the establishment intuition stored in the wrong place — so the suite should claim that synthesis rather than ignore the precedent. R-39 (`snapshot/.../15_stress_tests_batch3.md:164–181`, Design-Fail at `:414`): the literal failure cannot recur — PR #350's mandatory rows force *attention*, never *acts* (`06:150–152`), and dispatch/oblige demands are refusable — but the transposed hazard (a mid-campaign season in which obligations convert all ~5 acts, "NPCs always have the initiative") is real, unmeasured, and belongs as an added row beside `12` §6's owed measurements rather than as a mechanism.

**B · "Which seats a campaign offers at start" does not survive all five tests — test 3 answers it, from inside the 138.**
`15:277–282` escalates it as surviving all five; `player_agency_v30` §1.4 (M&B: "The player is never told 'become king'… accumulate capability until political agency becomes available", `snapshot/...:47–49`) and §5.1–§5.3 (`:361–398`) are a CANONICAL answer: campaigns offer low-rung starts; every seat is *reachable* (Standing 7 succession, seizure at 4+, an explicit independent path). The correct escalation is the narrow veto-check — "does the canonical start-low model survive into the greenfield shape?" — not an open question. And the answer has a consequence the suite will like less: since seats are reachable mid-campaign, "offered at start" does not bound R's domain, so `06` §6.4's defect-or-portrait exemption does no work — combined with the suite's own "every seat is occupiable" (`14:121–124`), the two dominance defects that reproduce the flagship arc are *defects*. This extends `ADVERSARIAL.md` §5.5, which I confirm independently and take one step further.

**C · The suite's convergence discipline is applied asymmetrically to its own load-bearing rulings.**
`16:109` discounts the 0.671 two-lane convergence ("one comparison, not two derivations") — correctly. `16:111` then banks "one act per person | 3 routes | coverage data, fiction, establishment argument" — all three routes downstream of the same 2026-08 design line, none of them the corpus's ratified precedent, which points the other way. Same defect class, opposite treatment, and this is the row `15:271` cites as "test 4 — reached independently by three routes." A convergence among documents that share a root is correlated error; the suite wrote that rule and exempted its central economic claim from it.

**D · R-15 / `05:355`'s global-scalar refusal was graded MEDIUM-HIGH confidence without reading the document its falsifier names.**
`15:211–212`: "wrong if the metaphysics needs one global scalar with no site identity." The corpus's CANONICAL substrate model is exactly that — `snapshot/designs/world/ms_trajectory_v1.md` (MS 0–100, peninsula-scale, three forces, RATIFIED hysteresis) with spatial modulation via `calamity_radiation_v30` (periphery-first, CANONICAL) and named social consequences hanging off the global bands (faction Stability checks at Critical, LANE_D F-D-9). Most of it translates (per-site conditions + a Query aggregate reproduce the bands; radiation becomes per-site `wear` rows; the MS-band social checks are independently refused under Law 4; Force 1's clock-driven improvement dies under the flux ruling, rightly) — so I am *not* saying R-15 is wrong. I am saying its falsifier condition is instantiated in ratified canon the suite never opened, its confidence grade is unearned until someone checks the surviving Thread-mechanics against per-place-only, and the one piece that does not translate (hysteresis) is M-3-2. The cheap fix: R-15's falsifier row should name `ms_trajectory_v1.md` as the document to clear against.

**E · The suite reproduces C-5 (ruling ≠ landed edit) at its own HEAD, inside its own correction pass.**
Five archive lanes documented "a migration only touched the load-bearing half" on five subjects (CONVERGENCE C-5). The suite's post-adversarial fold landed the Momentum cut everywhere (`02:844`, `07:322`, `15:154–163`), the §6 conflation rewrite (`06:231–276`), `revision_pressure`-as-derived (`02:821–838`), and the 8-person-side-rows fix (`02:651`, `08:84`) — and did **not** land the `opening_set` overturn: `07:137` still ships `-> Act[]` and `12:94–96` still asserts act-typed results, against `08` row 20, `15:250` and `16:76`. `ADVERSARIAL.md` §5.7 named this with its smallest fix; I confirm independently that at current HEAD the fix is applied in zero of the two documents it named. `13` confronts the *code* half of C-5 admirably (build-beside, dual-agreement, per-faction cut-over at the 31 sites); it has no discipline at all for its own 10,619 lines of prose, and this is the measured consequence. A one-line rule in `13` §6 would close it: *an overturn in `15` is not folded until every document `15` names carries it; the fold list is part of the ruling row.*

**F · The 138-unread cost, priced rather than gestured at.**
`16:123–125` admits 138 of 162 unread (and `ADVERSARIAL.md` §2.5 showed even that figure counted nine of the suite's own documents in its denominator). Measured against what twelve lanes just extracted: **one unread document (`player_agency_v30.md`) has already cost a same-day Jordan overturn of the suite's most-repeated economic claim**, voided one banked R closure (`14:139`), staled two adversarial findings (§5.6, §5.7), and mispriced four load-bearing passages (`05` §2.1, `06` row 19, `02` §8.1, `07` §4). A second (`ms_trajectory_v1.md`) instantiates R-15's own falsifier condition. A third cluster (loop-safety ledger + LPS-2e sim verification) holds the only executed dynamics evidence bearing on Law 3's cost (PART 4). Of `15` §3's twelve dispositions, at least two (act economy; seats-at-start) fail against unread canon — a ~17% disposition-failure rate from the unread set *on the escalation table alone*. The scope limit was honestly declared; the error was allowing "answered by precedent" and "survives all five tests" verdicts to issue from inside it. Dispositions that invoke tests 3 or 4 should be marked provisional-pending-sweep until the named subject area of the 138 is checked — that is one sentence in `15` §3's preamble.

---

### PART 4 — THE SHARPEST THING: the shape is blind, by its own two best rules, to the only defect class the archive ever found by running a campaign

**The claim.** PR #350's Law 3 ("every aggregate is a function, never a field") and Law 4/§5 ("exactly three quantities are clock-driven") jointly delete every stability mechanism the archive *verified* — and the suite's third rule, the anti-apparatus predicate, then stops it from owing anything in their place. The result is not a wrong mechanism but a wrong *epistemic posture*: the shape ships designed ratchets, has deleted the corpus's damper vocabulary, licenses no assertion and owes no measurement that could observe a runaway, and argues — in its only treatment of the subject — against building the instrument that would. Nothing else I found would change the proposal as much as fixing this, because everything else is a row edit; this is a hole in what the suite can *know about itself*.

**1 · What the archive actually established, and how well.** The corpus's dynamics record is thin but it is the only part of either corpus that was ever *executed*. Four results matter. (i) `integration_survey_stress.md` Stage 4 (LANE_C F-C-7): a 120-campaign instrumented battery found the headline victory condition fired **0/120** because two halves of the design were never reconciled — conquest lowered the very Accord the victory demanded. Found only by running; invisible to every read-only audit before it. (ii) FSS-LOOP-1/2 (LANE_A F-A-10, ratified 2026-05-30/31): passive dice decay alone could spiral a faction to collapse; the fix was a *deterministic floor* (at Stability ≤2 the accounting check cannot reduce it further) after the probability-floor version was measured mathematically inert (LANE_G F-G-5: a 2-die pool vs Ob 4 succeeds ~1%). (iii) LPS-2e (CONVERGENCE C-1, eight lanes): Mandate as a saturating aggregate `7T/(T+6)` **plus mean-reverting drift** of the substrate toward the aggregate, capped ±1/settlement/season — the whole assembly **sim-verified bounded and convergent over 30 seasons** (`snapshot/designs/territory/settlement_layer_v30.md:147–179`; independently verified convergent in `lps_mandate_comprehensive_ners_audit.md`, LANE_A F-A-10). (iv) H-10 (LANE_H, three sim passes converging): counting a *lost competition* as a failure produced a rich-get-richer exclusion loop worth ~8 points of Order-share over three years; fixed by redefining the counted event. And over all four sits lane C's principle (F-C-1), stated twice in one session from two artifacts: **acyclic provenance is not loop-safety** — the `causes[]` DAG grows forward through a spiral and never trips cycle detection, and loop-safety is therefore entirely the consuming design's job. No lane retracts any of this; none of it is in a DEAD ENDS section.

**2 · What the suite does with each of these, traced.** The saturating aggregate: refused — Mandate may not be stored, so there is nothing to saturate; legitimacy is a Query (`05:351`). The deterministic floor: refused — it is a special-cased outcome on a stored social stat, both forbidden shapes. The mean-reverting drift: doubly refused — it writes social primary state (settlement L/PS) with **no act as driver** (Law 4) on a seasonal schedule (`05:265–268`: "no fourth may be added"), and its target is a stored aggregate that no longer exists. The H-10 fix survives only as a warning nobody has read, because the suite's standing-analog (`Sensation.standing`, publicity/salience at `07:243–252`) has never been checked for the same self-reinforcement — `act_salience` amplifies acts touching contested referents, contested referents are made by amplified acts, and no one has asked whether that gain is above or below 1. Meanwhile the suite *ships ratchets of its own, on purpose*: suppression scars "ratchet the arming threshold" and dormant rows clear only by terms (`06:351`); grievance cheapens `commit` while revolt generates grievance (`06:340`); and until the fold pass caught it, `revision_pressure` was a stored monotone ratchet lifted unexamined from running code (`02:825–838` — note the suite fixed the *storage*, making it derived-at-read; the derived quantity is still monotone in its inputs over any window in which the holder does not revise). I am not asserting any of these diverges. I am asserting that **nobody can currently assert they don't**: the archive's dampers are gone, and the attenuation terms that replaced them (ranked-not-summed needs `07:110–114`, worth-normalised exposure, hop-free salience clamps, per-tick emission caps that bound fan-out within a season but not across seasons) have no termination argument attached anywhere in ten thousand lines. The archive held itself to a higher standard here than the suite does: even its *designed-only* cascade (F-E-8) shipped with "net per-cycle gain < 1 ⇒ damped; reach and depth finite ⇒ bounded" written next to the mechanism, flagged honestly as argument-not-simulation.

**3 · Why the suite's own defences don't cover it, taken one at a time.** *"The termination caps bound it"*: `keys.py`'s caps are per-emission/per-tick and raise loudly — they bound a cascade inside a season, which is precisely the distinction lane C drew: every season of a behavioural spiral is individually legal and individually under-cap. *"Law 3 prevents runaway aggregates"*: it prevents a stored aggregate from *being* the runaway state, and does nothing about primary state (stances, commits, scars, condition) spiralling while every Query truthfully reports the spiral. *"The adversarial pass would have caught it"*: `ADVERSARIAL.md` ran everything runnable and this is not runnable — §7 item 4 says the structural tests cannot be run because nothing they test exists; a dynamics defect is two stages further away than that. *"`06` §6.3 already addresses it"*: it addresses *convergence* — whether confrontations arrive — and argues, correctly, that a convergence *checker* would be process-apparatus. But boundedness is not convergence, and the conflation matters: convergence is about whether the interesting thing happens; boundedness is about whether the world remains playable. The suite's own text supplies the resolution and then doesn't apply it: "if convergence is a game property, it is a property of `wear`, the act budget and the date calendar — and it is settled by running campaigns, not by building a checker" (`06:290–292`). Boundedness is a game property by the same sentence, and *running campaigns* is exactly the archive's F-C-7 method — instrument win-path/state-trajectory attribution, run N≥100, check the claim. The control discipline for that already exists in the suite (`12:148–156` mandates n≥100 two-arm comparisons for golden-moving changes), and the repository already owns the instrument pattern (`tools/balance_oracle.py`, 240 campaigns, per CLAUDE.md §7). Nothing new has to be built — which disarms the anti-apparatus predicate that would otherwise correctly kill this proposal: the measurement is load-bearing on the game by the suite's own definition (`12:24–26`), not on process.

**4 · What adopting it looks like, priced.** Three edits, zero objects, no checker. **(a)** A seventh row in `12` §6: *"whether act-caused feedback is bounded under sustained shock"* — cannot be argued; settled by a two-arm seeded battery in the F-C-7 pattern (shock arm: sustained grievance/suppression injection at one rung for k seasons; control arm: none; compare the distribution of commit-mass, scar counts and site outcomes at k+m), run when steps 9–11 of `13` §4 make it reachable. **(b)** One sentence in `14` §2's N-line discipline: *a mechanism whose output feeds its own input carries a termination argument in its row* — the standard the archive's F-E-8 already met and the suite's rows 4/15 (`06:340,351`) currently don't. **(c)** One falsifier row in `15`: *Law 3/Law 4 are wrong-as-scoped if a bounded political layer requires either a saturating stored aggregate or a driver-less restoring drift* — i.e., if the archive's LPS-2e assembly turns out to be the only shape that converges. That is a real falsifier with real archive evidence on the other side, which is exactly what `15`'s own standard ("a ruling without one is an opinion") demands and what Laws 3 and 4 — alone among the four laws — currently lack in the dynamic regime.

**5 · Why this is the one thing.** The act economy is already being fixed over the suite's head; the ontology survived two adversarial passes; the escalations are one-sentence rulings. This is the only place where the proposal's *method* — refuse stored state, refuse clocks, refuse apparatus — composes into a blind spot none of its parts has individually, on the one subject where the archive's evidence is executed rather than written. The suite's whole epistemology is "name the falsifier, and prove the scanner can fail" (`12:20`). For the dynamics of its political layer it currently has no falsifier, no scanner, and a documented history — in the corpus it descends from — of that exact silence costing 0-of-120.

**CONFIDENCE:** HIGH that the gap is real and unpriced; MEDIUM on how much it changes once run (the attenuation terms may well suffice — the point is that "may well" is currently the entire state of knowledge). **FALSIFIER for this whole part:** a document or passage I missed that carries a termination argument for the grievance/scar/publicity paths, or a demonstration that every social quantity's feedback path routes through a clamped, non-compounding term. I searched `01` §4, `02` §§5.5/8–10, `05`, `06`, `07`, `12`, `13`, `14` §5–6 and `ADVERSARIAL.md` for one and found none; `14`'s own "NOT RE-RUN" row (`14:141`) concedes the adjacent point for dominance.

---

**Null-result note for the record:** I attempted to overturn the ruling memo's central claim (that the Slate funnel contradicts the one-act economy and the adversarial pass missed it) and could not — `06:195` vs `07:151` is arithmetic, and `ADVERSARIAL.md` §§5.6–5.7 demonstrably graze the funnel without recording the collision. I also attempted to break LANE_L's headline citations by reading the snapshot primaries directly; all figures verified exactly (`player_agency_v30.md:55, 297–304, 369–371, 440–458`; `15_stress_tests_batch3.md:164–181, 414`).
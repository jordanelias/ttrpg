# 17 · PLAYABILITY — the agonist–antagonist relay, adjudicated

## Status: **PROPOSED (2026-09-06). HELD BACK IN FULL. NOTHING RATIFIES ON MERGE.**
## Evidence: `relay/A_FABLE_INTERROGATION.md` · `relay/B_AGONIST_CONVERSIONS.md` ·
## `relay/C_ANTAGONIST_LANE_A.md` · `relay/D_ANTAGONIST_LANE_B.md` — all four verbatim, none patched.

---

> # THE RESULT, BEFORE THE METHOD
>
> **Thirteen positions were put up. Seven were broken, six were wounded, and none came through
> clean.** The two things that survived a named attack are both **prose retractions that cost
> nothing to make** — and one of them is a retraction of a sentence in *this directory*, not in the
> agonist's document.
>
> ⚠ **The relay's most valuable finding is not about the conversions at all. It is that
> `14_THE_WORLD_IN_THE_ROOM.md`'s headline measurement — the belief→decision edge is
> MEASURED-SEVERED, 0 of 4,800 claims can fire — IS STALE, and the code it describes was replaced
> two days before this directory quoted it.** That claim is load-bearing on four surfaces here and
> on the ceiling this directory placed on what can be built at all. **It is retracted in §H.1, and
> the retraction is the single best outcome of the exercise.**
>
> **A relay that kills seven of thirteen has not failed.** Producing and checking are different
> jobs (`CLAUDE.md` §0), and the kills are what the second job is for. What would have failed is a
> relay that returned thirteen survivors.

---

# PART A · WHAT RAN, AT WHAT TIER, AND WHY

| stage | model | tools | what it was answerable for |
|---|---|---|---|
| **A · the interrogation** | `fable` | read-only | the binary census, ~25 precedent games, the option surface, the refusals with the axiom behind each wall, and **its own least-sure list, handed forward** |
| **B · the agonist** | `opus` | full | thirteen positions, each with a mechanism in live schema, a cost count, a falsifier, and its own strongest objection |
| **C · antagonist, lane A** | `opus` | `Read, Grep, Glob` | the two theses and positions 1–7 |
| **D · antagonist, lane B** | `opus` | `Read, Grep, Glob` | positions 8–13, the refusals, and the two counts |
| **E · adjudication** | this session | full | reconcile; verify what neither lane was pointed at; fix what the relay found in the committed files |

**Two tiering decisions, both Jordan's, both recorded because they changed the shape of the run.**

1. *"fable 5.1 isn't supposed to be writing output as an agonist, only informing an agonist."*
   Stage A therefore **takes no positions and authors no mechanism.** It is an option surface. Every
   position in stage B is the agonist's to defend — which matters, because §G.1 finds that stage B
   is far more derivative of stage A than it says it is.
2. *"isn't antagonist being fable very expensive? I thought Opus was fine here, and fable would be
   reserved for larger adversarial critiques."* **Conceded, and it was the right call on the
   arithmetic**: `fable` is 2× `opus` (`CLAUDE.md` §10), and §10's own rule is *promote only on
   evidence a cheaper tier failed the node*. No such evidence existed. **Two opus critics on
   disjoint lanes cost less than one fable critic on the whole document and produced two
   independent readings instead of one** — and where they converged (§G.2), the convergence is
   evidence in a way a single verdict is not.

**Independence is structural, not declared.** Both critics ran as `valoria-critic`, whose agent
definition grants `Read, Grep, Glob` and no write tool — so neither could have edited what it was
auditing whatever its prompt said. Neither saw the agonist's reasoning, only its output. Neither saw
the other. Until 2026-07-28 every critic in this repository was read-only *by a sentence inside its
prompt*, which restricts nothing (`CLAUDE.md` §10); this is the mechanism that replaced it.

⚠ **What the relay CANNOT give you.** Four stages of prose about a subsystem that does not execute is
still four stages of prose (`§0.2`). **Nothing below is done in the sense this repository means it.**
The falsifiers are the part that could become execution artifacts, and §I says which three could run
first.

---

# PART B · THE VERDICT TABLE

**`BROKEN`** — the position does not stand as written. **`WOUNDED`** — the mechanism survives, a
named part of it does not. **`SURVIVED`** — a specific attack was run and failed.

| | position | verdict | what did it |
|---|---|---|---|
| **T-A** | the obstacle is the one lawful sink | ⚠ **WOUNDED** | over-reads the ruling it stands on (§C.1); and **6 of its own 13 positions add zero obstacle terms** |
| **T-B** | the provocation half is measured-severed | ⛔ **BROKEN — fatal** | ⭐ **the measurement is stale.** `belief_contradicts` was rebuilt; a firing is recorded (§C.2) |
| **P1** | aptness is how the room takes it | ⛔ **BROKEN — fatal** | four independent ways (§D.1). **Ranked 1st by the agonist and anchor of its first commit** |
| **P2** | the finding carries a strength | ⛔ **BROKEN — material** | the hole is real, the filler reads the wrong field, the falsifier is circular |
| **P3** | Partial is the band that costs | ⚠ **WOUNDED — material** | lawful; **falsifier unsatisfiable**; the write half is inert |
| **P4** | advantage is bought | ⛔ **BROKEN — material** | **there is no price**; and the permission is cited to a source that lacks it |
| **P5** | the ladder is two-way | ⚠ **WOUNDED** | ⭐ **factual core SURVIVED and sharpened**; the game claim collapses; the P1 dependency is false |
| **P6** | the terms are a burnable document | ⛔ **BROKEN — material** | `carry` is the wrong verb; `forge` cannot write it; **the debt it pays is circular** |
| **P7** | the advocate's rope | ⚠ **WOUNDED — material** | gradient survives; the seizure — *"the point"* — fails three ways |
| **P8** | a proof apt to what it contradicts | ⛔ **BROKEN — fatal** | **no mechanism mints a `Claim` whose subject is a `Proposition`.** The term is inert |
| **P9** | silence is priced by the room | ⚠ **WOUNDED** | ⭐ **THE ONE MECHANISM THAT SURVIVED ITS ATTACK.** Scope and cost both wrong |
| **P10** | the licence veto as four terms | ⛔ **BROKEN — fatal as written** | spends **two STRUCTURAL grades** and books it as a credit. The finding underneath is real |
| **P11** | a closed floor defers the price | ⛔ **BROKEN — fatal** | **all four other channels fail for a `speak`.** `P-41` stands |
| **P12** | reasons as a channel setting | ⛔ **BROKEN — fatal** | `Event.observed` carries **precondition reads**, not reasons |
| **P13** | two keys deleted | ⚠ **WOUNDED — material** | the `registers` half is sound; the census supporting it is miscounted |
| **REF** | the seven refusals | ⚠ **WOUNDED** | **two of seven are forced.** Two rest on prose or on a construction the tree already supplies |
| **#1** | zero new primitives | ⛔ **BROKEN** | true on the five axes it names, **false on the axis it does not — the seam contract** |
| **#2** | ten magnitudes, five terms → twelve | ⚠ **WOUNDED** | arithmetic correct, **accounting incomplete**; and **two** terms are hidden, not one |
| **#3** | eleven of twelve are estimable | ⛔ **BROKEN** | ⭐ **magnitude is estimable for none.** Twelve unknowns from a four-valued observable |

**Score: 7 broken · 6 wounded · 0 clean. Two survivals, both prose retractions, both free.**

---

# PART C · THE TWO THESES

## C.1 · Thesis A — **WOUNDED.** The warrant fails; the argument survives without it

**Claimed.** *"The obstacle is composed and floored by ruling. That makes it the one lawful sink for
every 'priced, not precluded' conversion. Thirteen of twenty binaries reach it at zero or negative
schema cost."*

**The attack, which I ran myself and neither lane was pointed at.** What was ruled is narrower than
what is claimed. `06_RESOLUTION.md:149` records the two messages verbatim — *"I'm fine with Ob being
modified by things"* and *"your presentation of obstacle makes sense to me."* That is **approval of a
presentation of five named terms, each sourced to a figure.** It is not a licence to add seven. And
`06_RESOLUTION.md:290-293` says so in the same file: **the derivation is a ruling AWAITING EXECUTION,
not an accomplished fact** — *"every call site in the tree still passes a hand-set Ob."*

**The second attack, from lane A, is arithmetic and it is worse.** *"Thirteen of twenty binaries
reach it"* is contradicted by the agonist's own cost lines: **six of thirteen positions add zero
obstacle terms** — P4, P6, P7, P11, P12, P13. The obstacle is the sink for **seven of thirteen**, not
for everything.

> ### ⚖ **ADJUDICATION.** The thesis stands as a **design argument** — the obstacle *is* the cheapest
> lawful sink, and that is worth saying. It does **not** stand as **authority**. Restate it as
> *"the obstacle is where a price lands most cheaply, and every term added is a claim on a derivation
> that does not yet run"* — which is both true and a reason for restraint rather than a licence.

## C.2 · Thesis B — ⛔ **BROKEN, fatal, and the breakage is in THIS directory**

**Claimed.** *"`belief_contradicts` fires only on `predicate ∈ PERSON_PREDICATES ∧ value is False`,
the vocabularies are disjoint, **0 of 4,800 claims can fire.** A deposited claim changes nothing
about what forms for them."* — cited to `14_THE_WORLD_IN_THE_ROOM.md:182-198`.

**Lane A refused to take that on trust and read the code. I then verified it myself, because a
retraction of a measured claim is not something to delegate.**

`shape.py:3834-3843`, the docstring of `belief_contradicts`, **naming its own previous version**:

> ⚠ `W-A`: IT ASKS THE VERB'S OWN TYPED CELL, NOT A ROSTER. **The previous version filtered on
> `predicate in PERSON_PREDICATES and value is False`** … `H-116` then measured the consequence: over
> 4,800 deposited claims the two vocabularies were DISJOINT … **The predicate is DERIVED from the
> form now** … so there is one namespace and **the write side has a name to aim at.**

and the body, `shape.py:3860-3861`:

```python
return evaluate(row.requires_typed, LedgerReader(p.ledger),
                binding_of(p.id, operands)).value is False
```

> ### **THE SENTENCE THIS DIRECTORY QUOTES IS A DESCRIPTION OF DELETED CODE.**
> `14_THE_WORLD_IN_THE_ROOM.md` reproduces the `H-116` measurement *of the implementation `W-A`
> replaced*, and presents it as the current state. **That is `06_RESOLUTION.md:185-189`'s own named
> defect — a fact about the tree stated as timeless — committed in the directory that names it.**

**And there is a recorded firing.** `shape.py:1265-1274`, seed 0, NPC-088, mode `actor`, 2026-09-04:
a ledger claim made a person **decline `tell` for a season** on a belief the fold would have admitted.
A deposited claim changed what that person did.

**⚠ BUT LANE A OVER-CLAIMS IN THE OTHER DIRECTION, AND THE HONEST STATE IS NARROWER THAN EITHER.**
Two qualifications the lane did not make, both in the same sources:

1. The same docstring says **`H-116`'s other half — WITNESS depositing claims in that namespace — is
   not this item.** The **reader** was rebuilt and now has a namespace to aim at. Whether the
   **writer** deposits into it is explicitly declared a separate, open question.
2. The one recorded firing is on `claim.held` — a self-referential predicate — and the surrounding
   comment is **in the act of excluding it** at the deposit site, as *"the THIRD principled
   exclusion."* It is evidence the loop can close, not evidence that it is closed.

> ### ⚖ **ADJUDICATION — and this is the finding to carry out of the whole exercise.**
> **RETRACT the severance.** The vocabulary mismatch that made provocation unbuildable **is gone**.
> What replaces it is not *"the loop is live"* but:
>
> **The reader is live and aimed at a named namespace. The write side into that namespace is
> unbuilt and is declared as its own item. So provocation is still not runnable today — but it is
> blocked on a WRITE that a rostered arm can supply (`observation_deposit_modes` : `none / actor /
> total`), not on a vocabulary mismatch no arm could fix.**
>
> ⭐ **That is a materially better state than this directory claims**, and it moves the ceiling.
> `B_AGONIST_CONVERSIONS.md`'s residue — *"the sixth route, through another character's decision, is
> measured-severed"* — is **withdrawn**, and with it the claim that five of six create-then-seize
> levers had to avoid NPC decisions. §H.1 makes the correction in `14`.

---

# PART D · THE THIRTEEN, ONE AT A TIME

**Format.** Each carries the agonist's five declarations, the antagonist's attack with the line that
did the damage, and an adjudication that says what — if anything — is left to build.

---

## D.1 · P1 · Aptness is how the room takes it · ⛔ **BROKEN — fatal**

| | |
|---|---|
| **claim** | delete `speak`'s genre conjunct from `requires`; an inapt speech forms, draws, and is priced by a new obstacle term |
| **mechanism** | `base_Ob += aptness(speech_kind, genre_set(bench.remit), rung(run))`; a `speech_kinds` roster with `apt_genre` / `apt_rung`; the reading rides in `Event.observed` |
| **cost claimed** | 0 primitives · **+1 obstacle term · +2 magnitudes**, minus one `requires` conjunct |
| **precedent** | Ace Attorney's *present* — a wrong present is charged, never refused |
| **falsifier** | a trace: a Candidate forms, one sample, a band emitted, the mismatch in the speaker's ledger and **not** the hearer's |

**THE ATTACK — four probes, all of which landed.**

1. **The premise is false, and it is refuted in this directory's own survival table.**
   `13_ADVERSARIAL.md:85`: *"Candidates form from the interior and `requires` is checked at RESOLVE,
   so a player who believes there is an occasion forms it and gets a **witnessed refusal**. Only the
   sentence was wrong; the design was right."* Confirmed in code — `shape.py:5863-5866` returns
   `ev(row.emits_on_refusal …)`; a refusal **emits**. So *"the Candidate never forms and the turn
   buys nothing"* is wrong twice over.
2. **The delivery channel is destroyed by the position's own edit.** I verified this line myself.
   `shape.py:5843-5861`: `verdict = Verdict(UNKNOWN, ())`, rebound **only** by the `requires` block,
   and `Event(..., observed=verdict.observed)`. **`Event.observed` carries precondition reads and
   nothing else.** P1's whole move is to *delete the conjunct from `requires`* — which removes the
   read from `observed`. To deliver it anyway needs a new channel, which the `+0 fields` line does
   not count.
3. **The conjunct does not exist.** `04_VERBS.md:62-66` — verified: `speak`'s `requires_typed` is a
   **single clause** (`form: existence · of: subject · kind: DocketItem`), no `all:`. The genre rule
   lives only in prose at `:157`, and under `CLAUDE.md` §0.05 **prose is not a mechanism.** So
   *"ONE CONJUNCT REMOVED"* removes nothing, and the position is a net **+1 term, +2 magnitudes,
   nothing subtracted.**
4. **A miscited roster.** `# rung_kinds is ORDERED` points at `rosters.yaml:89-92`, which is the
   **territorial containment ladder** (`person, hearth, community, settlement…`) and carries no
   `ordered:` key. The stasis ladder is `03_PARAMETERS.md:228`. ⚠ **And the agonist's own input had
   the right cite** — `A_FABLE_INTERROGATION.md:229` says *"the rung roster is ordered (`03:228-229`)"*.
   **The agonist downgraded a correct citation into an incorrect one by matching on the word "rung".**

> ### ⚖ **ADJUDICATION. Delete it.** It buys strictly less than the design already has.
> Today a genre-mismatched speech is a **witnessed refusal fanned out to every ledger in the room** —
> which `04_VERBS.md:167` argues *is* the mechanism (*"the player is witnessed committing a category
> error … and it is how they learn"*), and which P1 never mentions. Under P1, deposit mode `actor`
> delivers the reading **to the speaker alone**. **P1 replaces a public reading with a private one
> and charges three magnitudes for the downgrade.**
>
> ⭐ **What survives is one sentence worth keeping:** the `speech_kinds` roster with `apt_genre` /
> `apt_rung` **is** promised at `04_VERBS.md:144-148` and does not exist in data. Authoring it is
> content, costs nothing, and is needed by the design as it already stands.

---

## D.2 · P2 · The finding carries a strength · ⛔ **BROKEN — material**

| | |
|---|---|
| **claim** | `determine` writes `Tenure.degree` and nothing says what value; fill it from the determiner's own strongest live claim; the appeal's obstacle reads it |
| **mechanism** | `finding.degree := band(max{c.confidence : c ∈ actor.ledger, c.subject == matter})` |
| **cost claimed** | 0 fields · **+1 obstacle term (nested appeal only) · +1 magnitude** · one data roster · three `ID-13` closures |
| **precedent** | Duel of Wits' compromise; Ars Magica's Certamen |
| **falsifier** | two determinations differing only in the determiner's ledger → different degrees, then different appeal obstacles |

**THE ATTACK.**

- **The fiction is refuted by the design's own channel table.** P2's story is *"a bench told at third
  hand through `chronicle` finds weakly."* `14_THE_WORLD_IN_THE_ROOM.md:166`: `chronicle → told_by,
  **at the teller's confidence**`. The channel does not attenuate confidence. ⭐ **And the agonist
  quotes that line correctly in its own P11** and contradicts it here. The discriminator the design
  actually supplies is `Claim.source`, which P2 does not read.
- **The falsifier is circular.** It *plants* "one `firsthand` at high confidence, one `told_by` at
  low" — hand-installing the correlation the mechanism was supposed to produce. It tests that
  `band(max(·))` is a function.
- **The claimed `ID-13` closure is not one.** `shape.py:1303-1309`: `if best is None or (c.when,
  c.confidence) > (best.when, best.confidence)` — `confidence` already has a live reader in the
  ledger's tiebreak.
- **`max` over an eviction-ranked ledger is near-invariant.** Eviction is on `(confidence, recency)`,
  so it evicts the *low* claims: a bench holding one claim and a bench holding fifty return the same
  band.

> ### ⚖ **ADJUDICATION. The diagnosis is the keeper; the filler is not.**
> **`determine` declares no `contests:` and therefore writes a degree it cannot compute** — that is a
> real, well-found hole (`04_VERBS.md:186-198`, and `13_ADVERSARIAL.md:80` confirms `contests:` appears
> on one row in the whole table). **Rebuild the reader on `Claim.source`, not `Claim.confidence`** —
> `[firsthand, told_by, inferred, firsthand_via_knot]` is an *ordinal of remove*, which is what the
> fiction wanted and what the ledger actually carries. That version is separable, is not a second
> banding, and does not trip `T-k`.
>
> ⚠ **And the agonist's own strongest objection stands and is now sharper.** *A bench that finds
> strongly because it was told firsthand means the winning move is to reach the bench before the
> sitting* — which the design calls corrupt and cannot distinguish from diligence. On `Claim.source`
> that gets worse, not better. **This is a live design question, not a defect** — see §J.

---

## D.3 · P3 · Partial is the band that costs · ⚠ **WOUNDED — material**

| | |
|---|---|
| **claim** | `Partial: []` inverts the most reliable playability precedent; Partial must write the actor's own stance and raise their next obstacle from this run's own log |
| **mechanism** | `base_Ob += HELD_STEP × |{matter.held emitted by this actor in this run}|` — the intra-run fold, third instance |
| **cost claimed** | 0 primitives · **+1 term · +1 magnitude** · and it closes an `AX-4` ambiguity free |
| **precedent** | Apocalypse World's 7–9 |
| **falsifier** | third press's obstacle exceeds the first **by exactly `2 × HELD_STEP`**; only the actor's stance in `changes[]`; rung unchanged |

**THE ATTACK.**

- ⭐ **The falsifier cannot be satisfied in any run.** `06_RESOLUTION.md:214-217` composes `reception`
  from the hearers' claims about the speaker, and `:368-373` says every speech deposits **regardless
  of band**. After two Partials every hearer holds two more claims about the speaker, so the third
  obstacle differs by `2×HELD_STEP + Δreception`. **The equality can never hold wherever WITNESS
  fires — i.e. everywhere.** Nothing could show the position wrong.
- **Half the payload is inert.** `write_matrix.yaml:58-60`: *"NO VERB IN THE 32-ROW TABLE WRITES ANY
  `Person` INTERIOR FIELD. That is tier-0 `H-62`, and it means every interior consequence is inert."*
  So *the band that costs* costs nothing at the write site. The working half is the obstacle term.
- **The free `AX-4` closure was pre-registered in the agonist's own input**, in the same words
  (`A_FABLE_INTERROGATION.md:239`).

> ### ⚖ **ADJUDICATION. Keep the construction, throw away the instrument.**
> The fold is lawful, the `T-d` caveat is accurate, and **binding `Person.stance` to the actor's own
> is correct and should be made in `04_VERBS.md` regardless of whether P3 is built** — it closes an
> `AX-4` breach that is live in the row as written.
>
> ⭐ **The falsifier must be rewritten as an A/B on the TERM, not on the TOTAL** — the same defect
> lane A found in P9, and the general rule is in §G.3. Until then the position has no instrument, and
> `§0.1` point 3 says a result claim without a falsifier that could show it wrong is not a result.

---

## D.4 · P4 · Advantage is bought · ⛔ **BROKEN — material**

| | |
|---|---|
| **claim** | spend a live `oblige` you opened this run in the engine's unused σ-channel; the only number the player may see |
| **mechanism** | `net += net_boost(levels_to_net_sigma(aggressor=[levels]), pool)` — `sigma_leverage.py:190-203`, uniform `Δz`, soft cap `M_MAX = 1.5` |
| **cost claimed** | **0 of everything**, including 0 obstacle terms; **+1 VISIBLE magnitude**; *"the real cost is a turn in the order"* |
| **precedent** | Blades' devil's bargain |
| **falsifier** | arm B's `net` exceeds arm A's by exactly `net_boost`; obstacle byte-identical; the `Tenure` persists into next season; five obliges show the cap biting |

**THE ATTACK — the engine half survived; the game half did not.**

- **The stated cost is false.** `05_PROCEDURE.md:67-70`: *"⚠ **THERE IS NO TURN LIMIT AND THERE MUST
  NOT BE.** A round cap would be a clock nobody wound (`AX-5`)."* Turns inside a proceeding are
  **unbounded**. With `M_MAX = 1.5` saturating at about two majors, **the dominant line is: open two
  obliges, then speak, every time.** The collapsed line is the position's own worked example, run
  twice.
- **The debt is not binding.** `release` is `eligibility: ["own"]` with `requires: "a live Tenure …
  whose subject is the actor"` — **the debtor discharges himself, unilaterally, next season.**
  `13_ADVERSARIAL.md:65` already grades this: *"`release` is `own`, so it is the obligor discharging
  himself — repudiation, not clemency."*
- ⭐ **The permission is cited to a source that does not contain it — found by lane A and, separately,
  by me before either lane reported.** `07_THE_GAME.md:176-178` owes the player ledger claims with
  source/confidence/when, which are firsthand, and their own convictions. **Nothing about advantage
  levels.** Two lines on, `:181` forbids **"anything from a resolver-side Query"** — and a σ-level
  assigned to an oblige is exactly that. `grep -rn "R-18" *.md` in this directory returns **nothing**:
  R-18 is a **relay artifact**, and it strengthened at each hop — stage A wrote *"⚠ permitted"*,
  stage B wrote *"`R-18` permits it by name."*

> ### ⚖ **ADJUDICATION. The engine plumbing is correct and I could not break it** — `net_boost`,
> `levels_to_net_sigma`, `LEVEL_SIGMA` are cited accurately and R-5/R-6 are genuinely avoided. **Two
> things must change for the position to mean anything.**
>
> 1. **A bargain with no price is a free action.** Either the debt must be closable only by the
>    creditor — which is `P-36`, a ruling, and the refusals table admits it — or the σ-channel must
>    not be spendable this way at all. **As written it is a free lunch on the one lever the player
>    can see.**
> 2. ⭐ **SHOW THE FACTS, NEVER THE LEVEL.** *"You promised Aldric the mill, in front of the bench,
>    three turns ago, and he is present"* is owed under `07:176-178` — it is arithmetic over what the
>    character holds. The σ-translation is not. **The player estimates; the engine never names it.**
>    This fits `07`'s table, fits *legible hand, illegible room* better than the level does, and
>    **takes the visible-magnitude count to zero** — after which the thirteen positions show the
>    player no number at all.

---

## D.5 · P5 · The ladder is two-way · ⚠ **WOUNDED — and its core is one of the two survivals**

| | |
|---|---|
| **claim** | the rung fold already admits any emitted `matter.*`; the prose making descent self-only is what is wrong |
| **mechanism** | none. A prose retraction |
| **cost claimed** | **zero of everything** |
| **precedent** | Dogs in the Vineyard's raise / see / give |
| **falsifier** | the fold reports the shared rung for the opponent's next turn; no Event attributes a descent to them |

**THE ATTACK — the position's own falsifier, run by hand, and it FAILED.**

Lane A looked for a per-person filter on the fold and found none: `00_DERIVATION.md:299-301` reads
*"the lowest rung **any** emitted `matter.*` Event in THIS run has named"*, with no filter anywhere,
and no competing definition in the directory. ⭐ **And the claim is not merely upheld but sharpened:**
`05_PROCEDURE.md:76-77` cites `00_DERIVATION.md` §B.2 **as its authority** while asserting the
`Tenure.degree`-on-a-`commit`-edge model **that §B.2 retracted on five grounds.** That is a live
internal contradiction in this directory, worth fixing on its own.

**Two attacks that did land.**

- **The fold is band-blind, so the story is decorative.** All four `speak` bands emit `matter.*`.
  **A failing at conjecture moves the shared rung exactly as much as A landing Overwhelming.** The
  position's *"your opponent paid — their turn, their proof, their draw"* requires the pusher to have
  **won**, and the mechanism cannot see who won. Its falsifier plants an Overwhelming and would pass
  identically on a planted Failure.
- **The dependency on P1 is false.** `04_VERBS.md:155-159` types the three aptness inputs separately:
  only the **genre** row *"fails `requires`"*; the **rung** row's effect is *"apt at conjecture and
  **empty** at quality"* — empty, not refused. And genre derives from the bench's remit, which moving
  down the ladder cannot change. **No rung movement can produce a refusal. The stun-lock does not
  exist** — which unfounds the *"P5 must ship with P1"* coupling that drove the whole build order.

> ### ⚖ **ADJUDICATION. Make the prose retraction now; it is free and it is correct.** Both the
> two-way reading and the `05:76-77` contradiction are fixed by the same edit (§H.3).
> **Drop the game claim** until the fold can see a band — which would need the fold to read
> `Event.degree`, and `Event.degree` **is** assigned (`shape.py:5854`, `W-E`), so that version is
> available and is a different, honest position: *the rung moves to the lowest rung named by a
> **winning** emission.* **That is worth putting up on its own. It is not what was put up.**

---

## D.6 · P6 · The terms are a burnable document · ⛔ **BROKEN — material, verging fatal**

| | |
|---|---|
| **claim** | give `Record.stages` a reader for everything except the order; the opener's terms become an object `carry`, `forge` and `destroy_record` already act on |
| **mechanism** | `proceedings.run` reads `max_depth`, the named arbiter and the return day off the case Record |
| **cost claimed** | **0 of everything**; one `ID-13` closure; ⭐ *"it pays a debt rather than adding one"* — it sources `H-87`'s no-default `max_depth` |
| **precedent** | Republic of Rome's presiding magistrate; Suzerain's amendable constitution |
| **falsifier** | burn the Record → the appeal returns `Refusal(depth_cap)` and emits; forge it to `max_depth: 5` → five appeals reachable |

**THE ATTACK — I opened the two verb rows myself, because a word-match is the failure mode this
corpus is most prone to.**

- ⛔ **`carry` is the wrong verb.** `verb_table.yaml:83-93`, verified: `requires_typed: {form:
  existence, of: subject, kind: **Petition**}`, `writes: ["DocketItem.matter"]`. **`carry` carries a
  Petition onto a docket.** It does not move a `Record` and cannot — `Record` has no holder and no
  verb in the 32-row table relocates one. This is a match on the English word.
- ⛔ **The forge arm cannot execute.** `forge` writes `["Record.exists", "Record.forgery_quality"]` —
  **not `Record.stages`.** There is nothing to forge.
- ⛔ **The debt it claims to pay is circular, and this is the kill.** `shape.py:1848-1853`:
  *"`(Record, stages)` is a Part D row **unreachable from the person's own decision** … these are the
  instrument's declared stand-in, swept … `record_stages_default=3`."* **`Record.stages` is itself a
  fabricated constant.** P6 relocates `H-87`'s invented `max_depth` into `H-80`'s invented `stages`.
  **The debt is moved, not paid** — and the "pays a debt" line is the position's single strongest claim.
- **Two homes for one value.** `08_SEAM.md:13`/`:21` make `max_depth` a **parameter**;
  `00_DERIVATION.md:248` says *"the cap is the CALLER'S, always."* A value `proceedings.run` also
  reads off a Record is `ID-2` — the defect P13 invokes against `registers`.
- **Coverage.** `00_DERIVATION.md:286`: **five of twelve games have no `open_case` at all.** No case
  Record, no cap, no arbiter, no return day. **That is objection 3 of the five that killed the
  `Tenure.degree` rung proposal, arriving unchanged.**

> ### ⚖ **ADJUDICATION. The mechanism table is wrong on two of four verbs and the headline is
> circular. Do not build it.** ⭐ **One idea inside it is worth rescuing and is not what the position
> argued:** `petition` (`requires: "—"`, no dedup, no cap) genuinely is an unpulled lever for
> demanding a seat docket something. **That is a route into a room, it needs no Record, and it works
> in all twelve games.** It should be put up on its own terms.

---

## D.7 · P7 · The advocate's rope · ⚠ **WOUNDED — material**

| | |
|---|---|
| **claim** | GO/SEND/NEITHER is three options because nobody wrote the instructions down; make them an immutable `Proposition` the advocate commits to |
| **mechanism** | `utter` the rope → advocate `commit`s → advocate `oblige`s the principal. Four degrees of presence |
| **cost claimed** | **0 of everything**; declines `Tenure.payload` on the record |
| **precedent** | Diplomacy's press and the stab; Blood on the Clocktower's immutable public claim |
| **falsifier** | the act forms and resolves (the rope is not a `requires`); a later `tell` produces a claim whose subject is that Proposition |

**THE ATTACK — one probe failed, three landed.**

**The failed attack, stated so this is a verdict and not an absence:** lane A tried to show the rope
must be a `requires` and therefore smuggles a gate. **It does not** — `commit`'s cell is existence
over a Proposition, the rope is a separate Proposition, no cell reads it, and `Proposition` is
genuinely `frozen=True`. The position's claim that the act forms and resolves is correct.

- **It does not match the advocate it cites.** `03_PARAMETERS.md:317` defines the advocate as holding
  *"a `commit` to **another's disposition**"* — of the matter. P7 substitutes a commit to a
  representation-Proposition. Either that is not an advocate under `03:317`, or **a fourth act is
  required — and P7's cost table counts eight things and never counts acts.** (P4 counts its turn
  honestly; P7 does not.)
- **The seizable object references a value the design deliberately does not store.** The rope names a
  rung; the rung *"dies with the run"* and a hearing resumed next season *"starts at the top of the
  ladder again"* (`P-22`). **A permanent frozen Proposition pointing at an ephemeral quantity.**
- **The seizure works for a human and for nobody else.** Nothing compares an emitted rung against a
  Proposition's value; the comparison is the player's reasoning. ⭐ **P11 makes exactly this
  concession for `knot` and P7 omits it** while claiming *"Blood on the Clocktower's mechanism …
  with no deception subsystem."*

> ### ⚖ **ADJUDICATION. Keep the gradient; drop "the point".** Presence-as-four-degrees is a real
> gradient built only from live rows and it costs nothing — **that half stands.** The seizure is
> weaker in three named ways than claimed, and the position's own (vi) already concedes it may be
> flavour. ⭐ **The `Tenure.payload` declination is correct, correctly cited, and is the single best
> piece of engineering judgment in the agonist's document** (`write_matrix.yaml:52-54` — it is
> replaced by `term?` under `#358` rev.2, so a reader built on it dies at the migration).

---

## D.8 · P8 · A proof apt to what it contradicts · ⛔ **BROKEN — fatal**

| | |
|---|---|
| **claim** | term 5 counts proofs; it must weigh them by whether the claim's subject is a `Proposition` the opposing party committed to in this run |
| **mechanism** | `DIRECT if ∃ commit by the opposing party to a Proposition p with t.claim.subject == p.id, else INDIRECT` |
| **cost claimed** | 0 primitives · **0 obstacle terms** (term 5's weight changes) · +1 magnitude |
| **precedent** | Ace Attorney's *press vs present* |
| **falsifier** | three arms — against a committed Proposition, against none, and after a `repudiate` |

**THE ATTACK — lane B traced the DIRECT branch to a producer and found none.**

`Claim.subject` and `Proposition.id` both exist. But **no mechanism in this design mints a `Claim`
whose `subject` is a Proposition id.** `01_AXIOMS.md:324-330`: *"`Event` … has `subject`, **which the
fold sets to the actor**, and WITNESS deposits every claim with `subject = event.subject`."* Every
deposited claim's subject is a **person**. **The DIRECT branch cannot match for any claim the loop
can produce, so term 5's weight is `INDIRECT` identically and the term is inert.**

⭐ **This is the same defect class `04_VERBS.md:207` already caught and corrected once** — *"no act in
this design writes a `heard` relation, so the conjunct had no producer — `ID-13` committed inside a
`requires` cell"* — **re-committed inside an obstacle term.** And `W24`, which will make `subject`
the changed object, does not fix it: the changed object is still not a Proposition id.

**Aggravating.** Thesis B banks the *seizure* half as *"runs today … that is `08`"*. It does not. And
P9 enumerates which positions rest on the `T-d` violation — *"unlike Positions 3 and 5"* — **omitting
8, which rests on it harder than either.**

> ### ⚖ **ADJUDICATION. Delete the mechanism. Keep the question, which is good.**
> *What you produce a proof AGAINST should matter* is the right instinct and the precedent is apt.
> **The lawful version reads `Claim.subject` against the PERSON who committed**, not against the
> Proposition — a claim about a person who is on record is a shape the loop already mints. That is a
> different and weaker lever, and it should be put up as one rather than smuggled in as this one.

---

## D.9 · P9 · Silence is priced by the room · ⚠ **WOUNDED — and its mechanism is the second survival**

| | |
|---|---|
| **claim** | under a calling order, being reached and returning nothing raises your own obstacle — with no act, Event, write, verb or field |
| **mechanism** | `base_Ob += PASS_STEP × passes(actor, run)`, counted by the provider's own loop |
| **cost claimed** | 0 of everything · **+1 term · +1 magnitude** · gives `order` a second job |
| **precedent** | Dogs in the Vineyard's *give*; Liudprand at Constantinople |
| **falsifier** | turn 3's obstacle exceeds a control by exactly `2 × PASS_STEP`; **no Event for the passes**; `order: free` charges nothing |

**THE ATTACK THAT FAILED — stated first, because it is what makes this a verdict.**

Lane B expected P9 to die on availability: under `AX-1` candidates form inside each person, so it
expected the fold to iterate **acts that exist**, in which case a silent attendee never appears and
there is nothing to observe. **It is not so.** `05_PROCEDURE.md:39-44` iterates **attendees** — *"ORDER
the attendees, per `arrangement.order` / for each, in that order: the person forms candidates"* — and
`:62` names *"every attendee forms no candidate, or takes none"* as an observed state of that loop.
**The provider genuinely knows whom it called and what came back, with no Event, no write, and
without touching `Event.subject`.** ⭐ **It is the only position in either lane that clears the `T-d`
defect cleanly.**

**Three attacks that landed.**

- **Its falsifier is written on `order: rank`, the one value its own directory grades unbuildable.**
  `10_LOOPS_AND_GAPS.md:69`, `P-08`, **absent**: *"`order: rank` requires the fold to sort by rank
  within a stratum. That is a change to a shared mechanism, not a parameter of this one."* Never
  mentioned.
- **The `free` / `written_only` carve-out deepens a registered defect.** The loop iterates all
  attendees under every order value, so making `free` charge nothing needs a membership test whose
  only content is to switch the mechanic off for two of five values — which is `P-34` exactly, and
  the position claims *"⭐ IT STANDS ON NOTHING BROKEN."*
- ⭐ **A pass is not a decision.** `05_PROCEDURE.md:62` makes *"formed no candidate"* and *"took none"*
  the **same** observation. **The provider cannot distinguish choosing silence from having no lawful
  act** — so `PASS_STEP` charges a player for having no options, which is `03_PARAMETERS.md:91`'s
  refused shape arriving from the other end: priced instead of gated, and **unavoidable** rather than
  merely closed.
- **An undeclared cost.** `passes` is the design's **first outcome-moving quantity with no
  representation anywhere.** `rung(run)` and `proofs told` fold over emitted Events and are
  reconstructible by any witness; `passes` is reconstructible by nobody. Calling it *"the intra-run
  fold, third instance"* flattens a difference in kind.

> ### ⚖ **ADJUDICATION. The mechanism is real and is the best thing in the document. It cannot be
> built as scoped.** Three fixes, all cheap: **(1)** write the falsifier on `order: scripted`, which
> is buildable, not `rank`, which is not; **(2)** the carve-out must come from the arrangement row as
> data, not from a membership test in the provider; **(3)** ⭐ **charge only where a lawful act
> existed and was not taken** — which the loop can know, since it just formed the candidate set. That
> third fix converts the position from *punishing having no options* into *pricing a choice*, which
> is what it claimed to be.

---

## D.10 · P10 · The licence veto as four terms · ⛔ **BROKEN — fatal as written; the finding underneath is real**

| | |
|---|---|
| **claim** | Fig. 26's four conjuncts must be four prices and one structural ceiling, not one boolean |
| **mechanism** | four obstacle terms + `UnlicensedFrankness(BandExtension)` demoting 3→2; ⛔ **delete `veto: bool` from the seam** |
| **cost claimed** | **+4 terms · +4 magnitudes** · one `BandExtension` subclass — *"and against that, the seam signature **loses** `veto: bool`"* |
| **precedent** | Blades' position × effect |
| **falsifier** | four runs at 0/1/2/4 failures → four monotone obstacles; only the 0-run reaches Overwhelming |

**THE ATTACK — lane B asked what `veto: bool` is load-bearing on OUTSIDE this directory.**

- ⛔ **The one thing booked as a credit is a debit.** `09_IMPOSSIBILITIES.md:20` — impossibility 5,
  *"the subsystem widens an outcome"*, carrier `veto : bool`, **STRUCTURAL by signature**. `:31` —
  impossibility 16, *"a second degree ladder"*, same carrier. **Deleting it spends two STRUCTURAL
  grades** and converts them into the CONVENTION `dice_engine.py:83-87` grades *itself* as — *"an
  auditable **convention** … not a sandbox, and this module should not be read as promising one."*
  ⭐ **And `04_CODE_ARCHITECTURE.md:1134` uses this exact artifact as its worked example of why not:**
  *"a constraint carried by a return type — `veto : bool` — cannot be argued around; one carried by
  a rule can."*
- ⛔ **The seam gets LONGER, not shorter.** `04_CODE_ARCHITECTURE.md:163` fixes `seam/ladder` at
  `degree(margin, veto?) -> Degree`. `may_overwhelm(self, net, ob, licence_failures=0)` needs two
  parameters **plus a context key** crossing that boundary — or the provider calls the ladder itself,
  which breaks `08_SEAM.md:16` (*"a MARGIN. Never a winner"*).
- ⛔ **`T-k` is misapplied.** There is nothing to "get back": `veto: bool` is not a second ladder, it
  is a **parameter to the one ladder**, exactly as `extension` is. Two spellings of one input are not
  `ID-2`.
- ⛔ **Fig. 26 ends up priced twice.** The ruled model has the licence conditions as **veto only**.
  P10 keeps the demotion **and** adds four terms for the same four facts: failing a conjunct both
  raises Ob and forbids `margin ≥ 3`. **`ID-2` committed in the position whose stated purpose is
  removing an `ID-2`.**

> ### ⚖ **ADJUDICATION. The duplication P10 spotted is real. Its prescription inverts the design's
> own structural/convention ordering, and lane B supplied the lawful conversion:**
>
> ⭐ **Implement `veto` AS a `BandExtension` at the seam, once, for every provider — keeping the
> structural `bool` in the signature and giving the engine's own owner the implementation.** That
> resolves the duplication without spending a single STRUCTURAL grade.
>
> **On the four terms:** the position's own objection is the right one — *four terms for one figure
> is a third of the magnitude budget spent on one speech kind among twelve.* **Compose one `licence`
> term from a count of failures**, and demand the sweep before adding more.

---

## D.11 · P11 · A closed floor defers the price · ⛔ **BROKEN — fatal, and the worst outcome in the relay**

| | |
|---|---|
| **claim** | `P-41` is right about the timing and wrong about the amount: `floor:` restricts `co_located` and touches none of the other four channels |
| **mechanism** | nothing built. `tie / knot` the clerk the season before |
| **cost claimed** | **zero of everything** |
| **precedent** | CK3's *secret* |
| **falsifier** | X holds a knot to bench member Y, does not travel; after the descent X's ledger holds a claim, a control Z holds nothing |

**THE ATTACK — lane B took the central factual claim and tested each of the four channels for a
`speak`, which is what a descent is. All four fail, each for its own checkable reason.**

| channel | why it cannot deliver a closed-room descent |
|---|---|
| `chronicle` | `rosters.yaml:410` — the kind is emitted by a `binding_decision` verb; `speak` is `social`. And dead outright: `shape.py:4398-4403` — ***"`chronicle` matches NOBODY."*** |
| `post_remit` | `shape.py:4378-4381` derives remits from verbs whose eligibility starts `remit:`; `speak`'s is `["own"]`, so remits is empty and it returns `False` before any tenure scan |
| `document_key` | `shape.py:4357` matches `t.object == e.subject`, and **`e.subject` is the actor** — it fires for whoever holds a `hold` over *the descending person*, not over the case Record. The position's gloss is false at that line |
| `witness_key` | `shape.py:4361-4365` requires `e.subject ∈ (t.subject, t.object)`. **"Knot the clerk" delivers acts the clerk TOOK, not acts done in the clerk's presence.** The headline play delivers nothing |

**And the falsifier would fail for a reason its own "shows it wrong" list does not contain** — which
is `§0.1` point 2's defect exactly: *an assertion that cannot observe the failure it excludes.* The
position **states** the `T-d` dependency for `witness_key` and then writes an arm that dies on it.

> ### ⚖ **ADJUDICATION. This is the one I would delete first, and the reason is not that it is the
> weakest.** P12 is weaker. **P11 is the only position whose failure leaves things worse than doing
> nothing.** The others fail by being inert or overspending — a reader who acts on them builds
> something that does not work and finds out. **P11 produces no artifact at all: its deliverable is
> the ARGUMENT that `P-41` — the sharpest playability finding either earlier pass produced — is
> wrong.** That argument is false on four independently checkable lines, and if believed it licenses
> shipping the six `floor: closed` rows unchanged. **A wrong fix gets caught by its own falsifier. A
> wrong reassurance closes the ticket.**
>
> ⭐ **`P-41` STANDS, and `10_LOOPS_AND_GAPS.md:99`'s own first option is the live one:** either a
> closed room's concessions reach the world some other way — which today they **do not** — or the
> catalogue's floors are wrong. **That is a real design decision and it is now unavoidable.**

---

## D.12 · P12 · Reasons as a channel setting · ⛔ **BROKEN — fatal to the position, nit to the design**

| | |
|---|---|
| **claim** | `verdict_reasons` cannot gate learning; it sets whether the determination's reads fan out without anyone choosing to |
| **mechanism** | `given → total` fan-out, `withheld → actor`; a bench member can always `tell` regardless |
| **cost claimed** | **0 of everything**, including keys |
| **precedent** | Tyranny's per-faction Edicts |
| **falsifier** | two determinations differing only in the key; under both, a later `tell` still deposits |

**THE ATTACK — the position's own objection was availability; lane B attacked semantics instead, and
that holds even if `observed` is non-empty.**

`shape.py:2118-2120` defines `observed` as *"WHAT THE FOLD READ TO REACH THIS EVENT."* `determine`'s
reads are its `requires_typed` conjuncts — **a `DocketItem` exists, and a basis lookup on the seat.**
**Those are eligibility facts, not verdict reasons.** What actually moves a determination is
`reception`, composed resolver-side, and **obstacle composition is not a `requires`, so it never
enters `observed` under any typing.** The position maps *"can anyone learn why?"* onto a field
carrying *what the precondition check read* — pattern-matching on the phrase *"what the fold read."*

**And a row it does not handle:** `03_PARAMETERS.md:611` still carries `verdict_reasons: n/a` on the
public-debate row. Under P12, `n/a` maps to nothing in `{none, actor, total}`, and the nearest value
is `none` — **the control arm.** The key's **domain** also changes from `{given, withheld}` to
`{total, actor}`, booked as `keys 0`.

> ### ⚖ **ADJUDICATION. Delete it.** The channel carries the wrong content, and the design's own
> rank-13 item is correctly ranked. ⭐ **The observation that survives is worth more than the
> position:** *a bench member can always `tell` what they hold, so no key can gate learning* — that is
> true, follows from `eligibility: own`, and means **`verdict_reasons` as a boolean on learning is
> incoherent regardless of what replaces it.** Fig. 13's three columns still need a construction;
> this was not it.

---

## D.13 · P13 · Two keys deleted · ⚠ **WOUNDED — material**

| | |
|---|---|
| **claim** | `registers[]` and `stakes_grade` convert by removal; 14 keys → 12, and the design gets *more* graded |
| **mechanism** | `registers` is a second home for obstacle term 4; `stakes_grade` is a summary of what `disposes` writes |
| **cost claimed** | **negative** — keys 14 → 12, zero magnitudes |
| **precedent** | Fallen London, inverted |
| **falsifier** | all twelve rows load without the keys; a thirteenth loads; a fifteenth key fails |

**THE ATTACK — one probe confirmed the position, three damaged it.**

- ✅ **Upheld, and credit given.** `grep -n "registers" rosters.yaml` returns exactly one hit —
  line 238, the English verb inside an `H-76` note. **The roster does not exist.** There is nothing to
  delete but a promise.
- ⛔ **The census is miscounted.** *"Five of twelve rows carry `[restricted]` and three carry `[all
  seven]` — eight of eleven live rows."* Actual: `[restricted]` at four rows; the fifth instance is
  the **thirteenth** game, which the directory holds outside the twelve. **Only seven of twelve state
  the key at all.** Neither "eight" nor "eleven" is derivable from the file.
- ⚠ **Both halves are already registered, and one is already this directory's stated next move.**
  `03_PARAMETERS.md:411`: *"**If so, delete the key.** Registered as `P-09`, and it is the one most
  likely to be deleted."* **A conversion that lands on the directory's own next move is not a
  conversion.**
- ⛔ **The replacement inherits an unresolved dilemma.** P13 replaces `registers[]` with Fig. 8's map
  *"applied as A RISK THE MANNER RUNS."* `06_RESOLUTION.md:310-323` states as its headline: **"EXACTLY
  ONE THING IN THIS SUBSYSTEM IS STOCHASTIC, AND IT IS THE DRAW."** A *risk* is either a second draw —
  breaking that claim — or an expectation, which is the deterministic lookup `P-39` says is wrong.
  **Both branches are defects and the position picks neither.**
- **An arithmetic slip in the falsifier of the position whose whole content is arithmetic:** after
  deleting two keys the loader's refusal boundary is the **thirteenth**, not the fifteenth.

**⭐ AND AN ASYMMETRY NEITHER LANE'S SUBJECT OWNS, WHICH LANE B FOUND ANYWAY.** P13 deletes
`registers[]` because term 4 already reads Fig. 8 — *"a second home, `ID-2`"*. **P8 KEEPS `proofs[]`
on the identical shape** — a set-valued admissibility key beside an obstacle term reading the same
figure family — calling it *"an admissibility set doing its real job."* The agonist's own declared
input scores them **symmetrically**, at −1 key each. **The document breaks that symmetry in opposite
directions in two positions and argues for neither.**

> ### ⚖ **ADJUDICATION. Delete `stakes_grade`** — that is `P-09`, already this directory's
> recommendation, and P13 supplies no new reason but no bad one either. **Hold `registers[]`**: the
> grep is right and the key is empty, but the replacement is a live contradiction with the design's
> one-stochastic-thing claim, and **deleting a key whose replacement is undecided is deleting the
> question.** ⚠ **And resolve the `proofs[]` asymmetry either way before touching either** — two
> identical shapes cannot be a second home in one position and doing its real job in another.

---

# PART E · THE TWO COUNTS, RECOUNTED

**The counts are the one part of the agonist's document that is arithmetically checkable, so lane B
was pointed at them and checked them by hand.**

## E.1 · "New primitives: ZERO" — ⛔ **BROKEN on the axis it does not name**

| axis | claimed | recount |
|---|---|---|
| carriers | 0 | ✅ **confirmed 0** |
| edge kinds | 0 | ✅ **confirmed 0** |
| verb names | 0 | ✅ **confirmed 0** — `speech_kinds` are kinds of one verb (`ID-7`); every emitted kind is declared |
| fields | 0 | ✅ **confirmed 0** on `Person`/`Tenure`/`Record`/`Claim`/`Event` |
| Event kinds | 0 | ✅ **confirmed 0** |
| ⛔ **the seam contract** | **not counted** | **P10 amends `seam/ladder`, whose signature is fixed at `degree(margin, veto?) -> Degree` in a directory this one does not own** |

> ### **THE COUNT IS TRUE ON ITS FIVE NAMED AXES AND FALSE ON THE SIXTH — AND THE SIXTH IS THE ONLY
> PLACE A "ZERO PRIMITIVES" CLAIM COULD HAVE HIDDEN ANYTHING.**
> The document prices this class correctly elsewhere — CK3's hook is refused as *"an amendment to a
> theorem, not a row."* **The same amendment, made to a contract instead of a theorem, is booked as a
> NEGATIVE cost**, and two STRUCTURAL impossibility grades are spent without appearing in any cost line.

**Two further undeclared additions:** P1's `speech_kinds` roster adds two typed **columns**
(`apt_genre`, `apt_rung`) — a schema addition, not `ID-12` membership; and P12 changes
`verdict_reasons`' **domain** while booking `keys 0`.

## E.2 · "Ten magnitudes, five terms → twelve" — ⚠ **arithmetic correct, accounting incomplete**

The internal arithmetic checks: 2+1+1+4+1+1 = 10; 5+1+1+1+4 = 12, +1 nested = 13. **Three families
are missing.**

| undeclared | where | why it is a magnitude |
|---|---|---|
| **P2's band cut points** | `band(max{c.confidence})` | only the band→ordinal coefficient is counted. **Where the bands cut is a family**, booked as *"one data roster"* — and the position's own (vi) calls it *"a shadow ladder"* |
| **P10's veto threshold** | `return licence_failures == 0` | **`0` is a chosen cut.** `< 2` is equally defensible, never declared, never swept |
| **Fig. 8 as a risk family** | P13 | booked as *"hidden magnitudes 0."* Under the `P-39` reframing the position adopts, **a 7×7 map of risks is up to 49 parameters** — and `P-39` is graded *pending edit*, so the position banks an unexecuted edit as pre-existing cost |

**And there are TWO hidden terms, not one.** The section asserts *"only `reception` is genuinely
hidden."* Term 13 is read off the **determiner's own ledger**, and `07_THE_GAME.md:181` forbids
*"anything from a resolver-side Query"* — **an appellant cannot read it.** Stated in the same section
that counts term 13.

## E.3 · ⭐ "Eleven of twelve are estimable" — ⛔ **BROKEN. It is noise, and the honest defence was available and not made**

**The document's own defence names the condition under which it fails and does not test it:**
*"If `reception` is ever weak, the whole obstacle is legible and `15` PART A's solver comes back."*

> ### **`reception` IS WEAK TODAY, BY THIS DIRECTORY'S OWN REGISTER.**
> `15_WHY_IT_IS_A_GAME.md:55-66` makes `reception` the **sole** answer to the solver, composed from
> the hearers' claims **and their convictions**. `10_LOOPS_AND_GAPS.md:100` (`P-21`), graded
> **absent**: *"**NOTHING IN THIS DESIGN MOVES A CONVICTION** … **the most important thing this
> proposal did not build.**"* And `15:190`: *"a bench whose convictions never move is a bench that
> never learns."*
>
> So **the one term carrying the whole anti-solver argument is half-static.** Adding seven terms
> sourced to the **visible** arrangement and to the player's own prior acts makes the sum **more**
> legible in direction, not less.

**On estimability, which is the question that was asked.** **Direction is estimable for eleven terms.
Magnitude is estimable for none** — every coefficient is a resolver-side injected constant, `07:181`
forbids showing anything resolver-side, and to recover them a player must invert a **twelve-unknown
sum from a four-valued observable**, one draw per interaction, six draws per hearing, with no re-roll
and no repeat of the same room. **That is unidentifiable, not estimable.** What the player *can* know
— *a genre mismatch is worse; a repeat charge is worse; a stake is worse* — **is exactly what the
binary told them.**

> ### ⚖ **ADJUDICATION. At the player's interface the conversion delivers the binary's information
> plus unrecoverable magnitude: a decorative gradient over a real one.**
>
> ⭐ **The defence that IS available, and that the document reaches past:** the twelve terms are a
> genuine gradient **in the world model**, where NPC-vs-NPC proceedings diverge with nobody watching
> — which is `CLAUDE.md` §0.06's **R-half with no player in it**: *"emergent hooks and scenarios
> WITHOUT player involvement."* **That defence needs no player legibility at all and it is sound.**
> The document instead claims eleven-of-twelve estimability, **an uninstrumented number with no
> control (`§0.1` point 4) defending the most expensive claim in the file.**

---

# PART F · THE REFUSALS — **two of seven are forced**

A section headed *"where the forbidding is the game"* must show that the axiom cited **entails** the
wall. Lane B tested each row.

| row | verdict |
|---|---|
| **B-18** you cannot make anyone a party | ✅ **FORCED.** `AX-1`. Writing another's `commit` is a non-owner write; `03_PARAMETERS.md:314` makes party-hood their own `commit` |
| **B-19** the creditor has no verb | ✅ **FORCED**, narrowly. `T-m` makes closure ownership. But it restates `P-36`, already registered as a ruling |
| ⛔ **B-7** `appeal_basis: none` | ❌ **NOT FORCED.** Both authorities cited are **design prose** — and `CLAUDE.md` §0.05 says a design document **may not be cited as the reason a behaviour is correct.** ⭐ **And the agonist's own declared input scores it convertible at zero cost** (`A_FABLE_INTERROGATION.md:235`), which the document adopts "as established" and then silently overrides |
| ⛔ **B-6** multilateral disposal | ❌ **NOT FORCED.** The refusal cites `T-a`. But `05_PROCEDURE.md:94-98` supplies the `T-a`-compliant construction **in this directory's own words**: *"the count is a **Query** over the bench's live determinations, evaluated **by a later act** … That keeps the map a map and puts the threshold where `T-b` allows it."* The rebuttal is an assertion against a construction the directory endorses |
| ⚠ **B-9** `term_required: false` | **MISFILED.** The operative reason is *"`Tenure` has no `term`"* — an **unbuilt field**, not a wall. Filing it under *"where the forbidding is the game"* and praising it as *"authored indefiniteness"* **dresses a build blocker as a design virtue** |
| **B-11** whose `Person.stance` | **not a refusal** — the row says so itself |
| **R-18** never show a number | **not a refusal** — the row says so itself |

> ### ⚖ **ADJUDICATION.** *"A design that converts every binary has stopped having a shape"* is right,
> and this table does not establish that this design has one. **Two walls stand.** **B-7 and B-6 are
> the load-bearing rows and both are answerable** — and both now need a decision rather than a
> refusal. ⚠ **B-9 must be re-filed as a gap** (`P-04`), because a build blocker recorded as a virtue
> is how a gap stops being worked.

---

# PART G · CROSS-CUTTING

## G.1 · ⭐ The document is largely an elaboration of a read input, and one sentence says otherwise

Lane A mapped stage A's §3.1 census onto the thirteen positions **with the same costs**: B-1→P1,
B-13→P3, B-14→P5 (*including its false "needs B-1" coupling*), B-7→P2/P6, B-10→P6, B-11→P3's `AX-4`
closure, B-2→P10, B-4→P8, B-5→P7, B-16→P9, B-3/B-15→P13, B-8→P12, B-20→P11. Stage A's §4.2 is P2's
compromise; its §4.3 is *"the three levers an agonist should reach for first"*; its §4.5 is *"the
count"*.

> **Therefore the agonist's *"the audit predicted 'up to 12' independently … I reached twelve from
> the other end, which is corroboration"* is FALSE.** Stage A states `Ob terms | 5 | up to 12 | +7`;
> the agonist read it and reached 12 and +7. **That is agreement with a read source presented as
> independent rediscovery** — `§0.1` point 4 applied to the document's own headline defence.

**This is a finding about the RELAY, not only about the agonist**, and it is the one I am least
comfortable with, because **I designed the relay.** Handing a producer a complete option surface and
then asking what it independently concludes is a badly-posed question. §J.2 says what to do differently.

## G.2 · Where the two lanes converged without seeing each other

**Three convergences, and convergence between structurally independent readers is the strongest
evidence this exercise produces.**

1. **`Event.observed` carries precondition reads only.** Lane A found it against P1; lane B found it
   against P12. **Two positions, two lanes, one line of code** (`shape.py:5843-5861`), which I then
   verified myself.
2. **Falsifiers specifying an exact obstacle delta cannot fire.** Lane A against P3's
   *"exactly 2 × HELD_STEP"*; lane B against P9's *"exactly 2 × PASS_STEP"*. Same defect, different
   positions, neither lane saw the other.
3. **The `T-d` / `Event.subject` violation is load-bearing far more widely than declared.** Lane A on
   P3 and P5; lane B on P8 and on both broken channels of P11. **P9 enumerates the affected positions
   as "3 and 5" and is wrong by at least three.**

## G.3 · The rule that follows from convergence 2

> ### **ANY POSITION ADDING A TERM TO A COMPOSED OBSTACLE NEEDS AN A/B ON THE TERM, NOT ON THE TOTAL.**
> `reception` moves on every witnessed act, so a falsifier holding the total constant except for its
> own term is unsatisfiable in every run in which WITNESS fires — which is every run. **This
> invalidates the falsifier of every one of the ten magnitudes as written**, and it is the single
> most reusable thing the relay produced.

## G.4 · Terms 7 and 8 write into the same accumulator and neither position knows the other exists

A player choosing between **pressing** (risking a Partial → `HELD_STEP`) and **passing**
(`PASS_STEP`) pays in the same currency. If `PASS_STEP ≥ HELD_STEP`, silence is strictly dominated and
P9's decision does not exist; if `PASS_STEP` is small, P9 is a shrug. **The existence of the choice is
decided by the ratio of two independently-swept constants that no falsifier compares.** ⭐ **This
directly refutes the count section's defence that *"each term is separately sweepable"* — separately
sweepable and jointly meaningless are compatible.**

## G.5 · Citation hygiene — nit-grade, recorded because the pattern is the point

`shape.py` line numbers run **systematically one to two low** across the agonist's document
(`Claim.subject` 2151→2152, `Proposition.id` 2426→2428, `frozen=True` 2425→2424, `_ch_witness_key`
4360-4364→4361-4365) — including in the one place flagged *"verified by hand, not cited."*
`01_AXIOMS.md` is cited bare three times while every other bare `NN_` citation resolves inside this
directory, where `01_` is `01_THE_STUDY.md`. And the declared input is named `FABLE_PLAYABILITY.md`,
a file that does not exist under that name in the tree.

---

# PART H · WHAT THE RELAY FOUND IN **THIS DIRECTORY**, AND WHAT WAS FIXED

**Four defects in the committed proposal. All four are fixed in this commit rather than registered** —
`CLAUDE.md` §0's adversarial-pass bound says a finding that needs no ruling is fixed here or dropped,
and none of these needs a ruling.

| | file | the defect | fix |
|---|---|---|---|
| **H.1** | `14_THE_WORLD_IN_THE_ROOM.md:185` | ⭐ **the belief→decision severance is STALE.** It reproduces the `H-116` measurement of an implementation `W-A` replaced, and presents it as current | **retracted in place**, with the corrected state stated narrowly: *the reader is live and aimed at a namespace; the write into it is unbuilt and declared a separate item* |
| **H.2** | `13_ADVERSARIAL.md:62` | **a ✅ that the file contradicts.** Finding #12 is marked *applied*; three of the five values are still in `03_PARAMETERS.md:608, 611` | mark **struck and left visible**, not quietly flipped — *a ✅ in an adversarial record the file contradicts is `ID-11`'s corpse in the document written to prevent it* |
| **H.3** | `05_PROCEDURE.md:76-77` | **cites `00_DERIVATION.md` §B.2 as its authority while asserting the exact model §B.2 retracted on five grounds** | corrected; the fold's `any` is stated, with the band-blindness flagged as an open question rather than settled |
| **H.4** | `06_RESOLUTION.md:114` | ⛔ **the file states three answers to one question.** `latitude` is in the pool **and** in the obstacle — that is option **B**, which the same section calls a double-count — while the text says it implements **C** and recommends **A** | corrected in place, **and the consequence stated: if A is adopted the obstacle has FOUR room terms, so every "five terms" figure here is off by one at the base** |

> ### ⭐ **H.1 IS THE MOST VALUABLE THING THE EXERCISE PRODUCED, AND IT IS NOT A CONVERSION.**
> It moves the ceiling. Under the old reading, five of six create-then-seize levers had to avoid NPC
> decisions entirely, and the residue said the honest first act was closing an edge in another lane.
> **Under the corrected reading the edge is half-closed already and the remaining half is a rostered
> arm.** That changes what is worth building here — see §I.

---

# PART I · WHAT TO DO, IN ORDER

**Nothing below is done until it runs (`§0.2`). These are ordered by what unblocks what, and the
first two are rulings rather than builds because the arithmetic is wrong without them.**

| # | do this | why first |
|---|---|---|
| **1** | ⚖ **Rule `P-29` — latitude in the pool, the obstacle, or both** | **H.4.** Until it is ruled, the base term count is unknown and every magnitude claim is off by one |
| **2** | ⚖ **Rule `P-41` — either the closed rows' floors are wrong, or a closed room's concessions reach the world some other way** | **D.11.** P11's reassurance is false on four lines, so this is now unavoidable rather than deferrable |
| **3** | ✏ **Make the three free prose retractions** | **H.3 / D.5** (the rung is a shared, unfiltered fold), **D.3** (`Person.stance` is the actor's own — closing a live `AX-4` breach in the row as written), and **§C.2**'s ceiling correction. Zero cost, three real defects |
| **4** | ✏ **Author the `speech_kinds` roster** | promised at `04_VERBS.md:144-148`, absent from data, needed by the design **as it already stands** — independent of whether P1 is ever built |
| **5** | 🔨 **Rebuild P9 with the three fixes** | **D.9.** The only mechanism that survived its attack. Charge only where a lawful act existed and was not taken; falsify on `order: scripted`; carve out from the row, not from a membership test |
| **6** | 🔨 **`veto` AS a `BandExtension` at the seam, once** | **D.10.** Resolves the real duplication P10 found without spending two STRUCTURAL grades |
| **7** | 🔨 **Rebuild P2's reader on `Claim.source`, not `Claim.confidence`** | **D.2.** `[firsthand, told_by, inferred, firsthand_via_knot]` is the ordinal of remove the fiction wanted and the ledger already carries |
| **8** | 📐 **Rewrite every magnitude falsifier as an A/B on the TERM** | **§G.3.** As written, all ten are unsatisfiable in any run in which WITNESS fires |

**Deleted outright: P1, P6, P8, P11, P12.** Kept in reduced form: P3 (the fold, not the write), P5
(the retraction, not the game claim), P7 (the gradient, not the seizure), P13 (`stakes_grade` only).

---

# PART J · WHAT THE RELAY ITSELF GOT WRONG

**`[SELF-AUTHORED — bias risk]` — I designed this relay, so the section below is the one to read
sceptically.**

## J.1 · The independence was real; the ORIGINALITY was not

**Structural independence held.** Neither critic could write; neither saw the agonist's reasoning;
neither saw the other. The three convergences in §G.2 are worth what convergence is worth.

**But §G.1 is a defect in my design of the run, not in the agonist's execution of it.** I handed the
producer a complete option surface — a census with costs already attached — and then asked what it
independently concluded. **It concluded the census.** The *"I reached twelve from the other end,
which is corroboration"* line is the predictable output of that setup, and I set it up.

**What to do differently:** give the producer the *constraints and the tree* and withhold the option
surface, or give it the surface and **do not ask for independent corroboration of anything in it.**
Not both.

## J.2 · What the critics did NOT do, said plainly

- **Neither lane found H.1, H.3 or H.4 as its assignment.** Lane A found H.1 and H.3 while attacking
  positions aimed at something else; lane B found H.4 and flagged it explicitly as **out of lane, an
  observation, not a ruling** — which is the correct behaviour and worth recording as such.
- **Neither was asked to check ruling attribution**, which is why §C.1 is mine.
- **Neither lane audited the other's subject**, so a defect spanning P7 and P8 would have been missed
  by construction. §G.4 was found only because lane B chose to look outside its lane at the counts.

## J.3 · The null results, and why they count

**Lane B ran an availability attack on P9 and it failed.** Lane A ran the rope-as-`requires` attack
on P7 and it failed. Lane A ran P5's own falsifier by hand and it failed. **Three named attacks that
did not land, reported as failures rather than converted into findings** — which is the property that
makes the seven kills worth believing. A relay that returns no survivals is manufacturing.

## J.4 · The honest bottom line

> **This exercise did not make the subsystem playable. It established that eleven of the thirteen
> proposed ways of doing so do not work as written, that the two survivals are free prose
> retractions, and that the directory's most-cited measurement about what is buildable at all was
> two days out of date.**
>
> **That is a smaller result than the deliverable's shape implies and a larger one than the
> conversions would have been if believed.** `§0.2` is the standard: **none of it runs.** The eight
> items in §I are what would make some of it run, and items 1–4 need no code at all.

> ## RELAY STAGE D · ANTAGONIST, LANE B — `opus`, structurally read-only
> ## Status: **PROPOSED (2026-09-06). NOTHING HERE RATIFIES ON MERGE.**
>
> **Lane: Positions 8–13, the refusals table, and the two counts.** Dispatched with the agonist's
> OUTPUT and not its reasoning, against the working tree. Ran as `valoria-critic` — `Read, Grep,
> Glob`, no write tool. It did not see stage C and stage C did not see it.
>
> **The counts were named as its highest-value target** because a count is the one claim in a design
> document that is arithmetically checkable. It recounted both by hand and found the primitive count
> **true on its five named axes and false on the sixth, which the count does not name.**
>
> ⭐ **Its answer on the twelve-term obstacle is the one to read** — direction is estimable for eleven
> terms, **magnitude for none**, because recovering the coefficients means inverting a twelve-unknown
> sum from a four-valued observable. And it supplies the defence the agonist reached past: the
> gradient is real **in the world model**, which needs no player legibility at all (`CLAUDE.md` §0.06's
> R-half with no player in it).
>
> **One attack FAILED and is reported as a failure** — the availability attack on P9, which expected
> the fold to iterate acts rather than attendees. P9's mechanism is the only one in either lane that
> survived. Its closing observation on `latitude` is flagged **out of lane, an observation and not a
> ruling**, which is the correct behaviour; it was confirmed in adjudication and is `17`'s §H.4.

---

# ANTAGONIST RETURN — lane: Positions 8–13, the refusals table, and the two counts

Ground truth read by hand: `/home/user/ttrpg/proposals/2026-09-05-proceedings-subsystem/{00,03,04,05,06,07,08,09,10,13,15}*.md`, `/home/user/ttrpg/proposals/2026-09-05-proceedings-subsystem/relay/A_FABLE_INTERROGATION.md`, `/home/user/ttrpg/proposals/2026-09-03-meta-architecture/{01_AXIOMS,04_CODE_ARCHITECTURE}.md`, `/home/user/ttrpg/proposals/2026-09-02-executable-architecture/{verb_table,rosters}.yaml`, `/home/user/ttrpg/proposals/2026-09-01-season-loop-tests/tracer/shape.py`, `/home/user/ttrpg/engine/autoload/dice_engine.py`.

---

## POSITION 8 · a proof apt in proportion to what it contradicts — **BROKEN**

**THE ATTACK I RAN.** I traced the DIRECT branch's input to a producer. The test is `t.claim.subject == p.id` where `p` is a `Proposition`. `Proposition.id` is `/home/user/ttrpg/proposals/2026-09-01-season-loop-tests/tracer/shape.py:2428`. `Claim.subject` is `shape.py:2152`. I then asked what subjects claims in a ledger actually carry: `/home/user/ttrpg/proposals/2026-09-03-meta-architecture/01_AXIOMS.md:324-330` — *"`Event` has no `actor` field — but it has `subject`, **which the fold sets to the actor**, and WITNESS deposits every claim with `subject = event.subject`."* And the only other minting vocabulary is `rosters.yaml:311-313` (`stores:<kind>` / `condition` / `contain.path:<to>` / `claim.held` / `exists:<kind>` / a relation stem).

**WHAT IT SHOWED.** No mechanism in the design produces a `Claim` whose `subject` is a `Proposition` id. Every deposited claim's subject is a **person**. So the DIRECT branch cannot match for any claim the loop can currently produce — term 5's weight is `INDIRECT` identically, and the term is inert. This is the same class of defect `04_VERBS.md:207` already caught and corrected in `determine` (*"no act in this design writes a `heard` relation, so the conjunct had no producer — `ID-13` committed inside a `requires` cell"*), re-committed inside an obstacle term.

The aggravating fact: Thesis B (agonist lines 34-39) breaks the provocation half on `H-72` and banks the **seizure** half as *"runs today ... that is `08`"*. It does not. And Position 9 explicitly enumerates which positions rest on the `T-d` violation — *"unlike Positions 3 and 5"* — omitting 8, which rests on it harder than either, because 3 and 5 need `Event.subject` for **attribution** while 8 needs `Claim.subject` to be **a non-person**, which `W24` will not fix either (`W24` makes `subject` the changed object, still not a Proposition id).

Secondary: the two load-bearing citations are both off. `shape.py:2151` is `holder`, not `subject` (`subject` is 2152); `shape.py:2426` is a docstring line, `Proposition.id` is 2428.

**SEVERITY: fatal to the position.** The seizure half is the half the document says survives its own break, and it is the anchor of Thesis B. A position whose weight function cannot fire is a `P-05`-shaped hole in a new place.

---

## POSITION 9 · silence priced by the room — **WOUNDED**

**THE ATTACK I RAN.** Four probes.

1. *Does the provider have a loop that can observe a pass?* — `05_PROCEDURE.md:39-45` and `:62`: `repeat until nobody acts … for each, in that order: the person forms candidates` / *"nobody acts | every attendee forms no candidate, or takes none."* **This attack failed. The mechanism is available.**
2. *Does `order: free` produce `passes ≡ 0`?* — `05_PROCEDURE.md:40-41` orders the attendees per `arrangement.order` and then iterates **all of them, under every value**. The order value sets the sequence, not whether an attendee is reached. To make `free` charge nothing you must add a membership test on the roster's members whose only content is to switch the mechanic off for 2 of 5 values — which is precisely what `10_LOOPS_AND_GAPS.md:92` (`P-34`) already registers as invisible to the closure falsifier (*"`order` and `disposal` are enums the provider must switch on, and a scan for `arrangement.id ==` is green on that"*). The position deepens an already-registered defect while claiming *"⭐ AND IT STANDS ON NOTHING BROKEN, WHICH IS RARE HERE."*
3. *Is `order` buildable?* — `10_LOOPS_AND_GAPS.md:69` grades `P-08` **absent**: *"`order: rank` requires the fold to sort by rank within a stratum. **That is a change to a shared mechanism, not a parameter of this one.**"* The position's falsifier is written on `order: rank` — the one value its own directory grades unbuildable. Never mentioned.
4. *Is a pass a decision?* — `05_PROCEDURE.md:62` makes "formed no candidate" and "took none" the **same** observation. The provider cannot distinguish *chose silence* from *had no lawful act*. So `PASS_STEP` charges a player for having no options, which is `03_PARAMETERS.md:91`'s refused shape (*"a capacity that gated an option would be `if skill < N: return []` … deletes the best thing about the option set"*) arriving from the other end — priced instead of gated, and unavoidable rather than merely closed.

**WHAT IT SHOWED.** The mechanism exists; the framing does not. Also undeclared in the cost line (*"Carriers 0 · edge kinds 0 · verbs 0 · fields 0 · Event kinds 0 · keys 0"*): `passes` is the design's **first outcome-moving quantity with no representation anywhere** — the position requires *"the log contains no Event for the two passes; and no ledger anywhere holds a claim about them."* `rung(run)` and `proofs told` fold over emitted Events and are reconstructible by any witness (`00_DERIVATION.md:298-302`, `06_RESOLUTION.md:221`); `passes` is reconstructible by nobody. Calling it "the intra-run fold, third instance" flattens a real difference in kind.

**SEVERITY: material.** The core mechanic survives; three of its five order values and its own falsifier do not.

---

## POSITION 10 · the licence veto as four terms + a `BandExtension`, deleting `veto: bool` — **BROKEN**

**THE ATTACK I RAN.** I looked up what `veto: bool` is load-bearing on, outside this directory.

- `/home/user/ttrpg/proposals/2026-09-05-proceedings-subsystem/09_IMPOSSIBILITIES.md:20` — **impossibility 5, "the subsystem widens an outcome" | `veto : bool`, and the ladder takes the minimum | STRUCTURAL by signature.**
- `09_IMPOSSIBILITIES.md:31` — impossibility 16, "a second degree ladder | one `ladder.degree(margin, veto)`, in the seam".
- `/home/user/ttrpg/proposals/2026-09-03-meta-architecture/04_CODE_ARCHITECTURE.md:684, 696, 841` — the seam contract; row 23 of the impossibility table, STRUCTURAL by signature.
- `04_CODE_ARCHITECTURE.md:1134` — §G.1.7, which uses this exact artifact as its **worked example**: *"a constraint carried by a return type — `veto : bool` — cannot be argued around; one carried by a rule can."*
- `/home/user/ttrpg/engine/autoload/dice_engine.py:83-87`, on the replacement: *"an auditable **convention** with a structurally-bounded return channel — not a sandbox, and this module should not be read as promising one."*

**WHAT IT SHOWED.** Four separate breaks.

1. **The one thing booked as a credit is a debit.** *"The seam signature loses `veto: bool`, so the seam gets shorter and `T-k` gets its single owner back."* Deleting it deletes the carrier of impossibility 5 **and** impossibility 16 and converts a **STRUCTURAL by signature** grade into the CONVENTION `dice_engine.py:86` grades itself as — in the exact case §G.1.7 chose as its illustration of why not to.
2. **The seam gets LONGER, not shorter.** `04_CODE_ARCHITECTURE.md:163` fixes `seam/ladder` at `degree(margin, veto?) -> Degree`, and `:164` fixes `seam/wrappers/*` at *"nothing, ever | the projection | a `Margin` | none"*. `may_overwhelm(self, net, ob, licence_failures=0)` needs `(net, ob)` **and** a `licence_failures` context key crossing that boundary. Two parameters become three plus a context dict — or the provider calls the ladder itself, which breaks `08_SEAM.md:16` (*"a MARGIN. Never a winner"*).
3. **`T-k` is misapplied.** `01_AXIOMS.md:404-409`: *"this is the one the chain says is enforced by a person noticing. It has no mechanism and no cheap test."* There is nothing to "get back". And `veto: bool` is not a second ladder — it is a parameter **to** the one ladder, exactly as `extension` is. Two spellings of one input are not `ID-2`.
4. **Fig. 26 is now priced twice.** The ruled model (`06_RESOLUTION.md:224`) has the licence conditions as **veto only**. Position 10 keeps the demotion *and* adds four obstacle terms for the same four facts. Failing a conjunct both raises Ob (making `margin ≥ 3` less reachable) and forbids `margin ≥ 3`. That is `ID-2` — two homes for one fact — committed in the position whose stated purpose is removing one.

Also: this is a near-verbatim restatement of the census row the agonist declared as an established input and said it would not re-derive — `/home/user/ttrpg/proposals/2026-09-05-proceedings-subsystem/relay/A_FABLE_INTERROGATION.md:230`: *"four conjuncts → four Ob terms; the structural demotion is `BandExtension.may_overwhelm` (`dice_engine.py:109-111`) … **Cost: up to four injected terms, one declared `BandExtension` subclass; the seam's `veto:bool` is deleted**."* The only additions are `context_keys` and the `interposed: [office]` waiver (which `03_PARAMETERS.md:150-156` already argues).

**The lawful conversion it did not find:** implement `veto` **as** a `BandExtension` **at the seam, once, for every provider** — keeping the structural bool in the signature and giving the engine's own owner the implementation. That resolves the real duplication the position correctly spotted without spending two STRUCTURAL grades.

**SEVERITY: fatal to the position as written, material to the finding underneath.** The duplication is real; the prescribed fix inverts the design's own structural/convention ordering.

---

## POSITION 11 · a closed floor defers the price; `knot` collects — **BROKEN**

**THE ATTACK I RAN.** I took the position's central factual claim — *"`floor:` restricts `co_located` and touches none of the other four witness channels"* — and asked, for a **descent** (which is a `speak`, `04_VERBS.md:59-60`, `stratum: "social"`), whether each of the other four can fire. All four fail, each for its own checkable reason:

| channel | why it cannot deliver a closed-room descent |
|---|---|
| `chronicle` | `rosters.yaml:410` — *"the kind is emitted by a `binding_decision` verb."* `speak` is `social` (`04_VERBS.md:60`). And measured dead outright: `shape.py:4398-4403` — *"**`chronicle` matches NOBODY** … The `any(...)` is over an empty generator for every Event the fold can currently produce. **So the whole of `all_five - presence_only` is `document_key`.**"* |
| `post_remit` | `shape.py:4378-4381` derives `remits` from verbs whose `eligibility` starts `remit:`. `speak`'s eligibility is `["own"]` (`04_VERBS.md:61`), so `remits` is empty and the function returns `False` before any tenure scan. Cannot fire for `matter.held` / `matter.carried`. |
| `document_key` | `shape.py:4357` — `t.object == e.subject`, and `e.subject` **is the actor** (`01_AXIOMS.md:325`). It fires for whoever holds a `hold` Tenure over *the descending person*, not over the case Record. The position's gloss (*"whoever holds the case Record learns THAT it changed"*) is false at that line — and it is broken by the **same** `T-d` defect the position declares for `witness_key` alone. |
| `witness_key` | `shape.py:4361-4365` requires `e.subject ∈ (t.subject, t.object)`. Since `e.subject` is the actor, "knot the clerk" delivers **acts the clerk took**, not acts done in the clerk's presence. The headline play — *"⭐ SO THE PLAY IS: `tie / knot` THE CLERK, THE SEASON BEFORE"* — delivers nothing about someone else's descent, today. |

The position's falsifier (*"X holds a live `knot` to bench member Y. Require: after the descent, X's ledger holds a claim about it"*) would fail for a reason its own "shows it wrong" list does not contain — `§0.1` point 2's defect: an assertion that cannot observe the failure it excludes. The position states the `T-d` dependency and then writes an arm that dies on it.

Two further factual errors: `chronicle` is glossed as *"whoever is later told, at the TELLER'S confidence, possibly false"* — that is `told_by`, a `claim_sources` value (`rosters.yaml:119`), not the `chronicle` witness channel, which `rosters.yaml:393-398` warns *"IS DELIBERATELY NOT `everyone`"*. And `_ch_witness_key` is `shape.py:4361-4365`, not `4360-4364`, in the one claim flagged *"verified by hand, not cited."*

**WHAT IT SHOWED.** The position argues that `P-41` — the directory's own **sharpest playability finding** (`10_LOOPS_AND_GAPS.md:99`; `13_ADVERSARIAL.md:70`) — is *"right about the timing and wrong about the amount."* It is not. On the tree as it stands, the price does not arrive later through the other channels; it does not arrive at all. The position's own §(vi) confesses the shape of this (*"that is the move an author makes when they do not want to change a catalogue"*) and then makes it anyway.

**SEVERITY: fatal.** This is the worst outcome in my lane, because acting on it means leaving the six `floor: closed` rows unchanged on a false rebuttal of a true finding.

---

## POSITION 12 · `verdict_reasons` as a channel setting — **BROKEN**

**THE ATTACK I RAN.** The position's own objection is *availability* — `Event.observed` may be empty for `determine`. I attacked **semantics** instead, which holds even if `observed` is non-empty.

`shape.py:2118-2120` defines `observed` as *"WHAT THE FOLD READ TO REACH THIS EVENT — a tuple of `Observation`."* `determine`'s reads are its `requires_typed` conjuncts (`04_VERBS.md:191-194`): a `DocketItem` exists, and a form-7 basis lookup on the seat. **Those are eligibility facts, not verdict reasons.** What actually moves a determination is `reception` — composed resolver-side from the hearers' claims and convictions (`06_RESOLUTION.md:214-218`, `15_WHY_IT_IS_A_GAME.md:65-66`) — and obstacle composition is not a `requires`, so it never enters `observed` under any typing. The position maps *"can anyone learn why?"* onto a field that carries *what the precondition check read*. That is pattern-matching on "what the fold read" as if it meant "the reasons".

Second, undeclared: `Event.__post_init__` (`shape.py:2138-2144`) checks only `causes`. The design's Events are not gate-verified (`08_SEAM.md:145-150`, `P-02`), so "deposits at TOTAL fan-out" has no enforcement either way.

Third, a row the position does not handle: `03_PARAMETERS.md:611` still carries `verdict_reasons: n/a` (and `:608` `records_dissent: n/a`, `:611` `disposes: standing`) on the public-debate row — while `13_ADVERSARIAL.md:62` claims all five out-of-domain values were **✅ retyped**. They are not. Under Position 12, `n/a` maps to nothing in `{none, actor, total}` (`rosters.yaml:337`), and the nearest value is `none` — the **control arm**, i.e. no deposit at all.

**WHAT IT SHOWED.** Even granting the position everything it asks for on availability, both arms deposit the wrong content. Its stated cost (*"Carriers 0 · fields 0 · verbs 0 · Event kinds 0 · keys 0"*) also understates: `verdict_reasons`' **domain** changes from `{given, withheld}` to `{total, actor}`, and a value falling outside a declared domain is the exact class `13_ADVERSARIAL.md:62` graded fatal-adjacent.

**SEVERITY: fatal to the position, nit-grade to the design.** It is the document's own rank-13 item and the author flagged the neighbourhood correctly.

---

## POSITION 13 · deleting `registers` and `stakes_grade` — **WOUNDED**

**THE ATTACK I RAN.** Four probes; one confirms the position, three damage it.

1. **Upheld.** *"`grep -n "registers" rosters.yaml` returns ONE hit — line 238, the English verb inside a note about `H-76` — and no roster key."* Verified exactly: `/home/user/ttrpg/proposals/2026-09-02-executable-architecture/rosters.yaml:238` is the sole hit and it is `H-76` *registers* how many interactions. The roster does not exist. Credit where due.
2. **The census is miscounted.** *"Five of the twelve rows carry `registers: [restricted]` and three carry `[all seven]` … a set-valued key used, in eight of eleven live rows, as a removal."* Actual, by grep on `/home/user/ttrpg/proposals/2026-09-05-proceedings-subsystem/03_PARAMETERS.md`: `[restricted]` at **:487, :512, :527, :584 — four** of the twelve. The fifth instance is `:648`, the **thirteenth** game (the examination, §F.1), which the directory explicitly holds outside the twelve. `[all seven]` at `:451, :558, :610` — three, correct. Only **seven of twelve rows state the key at all**; the other five show differing keys only. Neither "eight" nor "eleven" is derivable from the file.
3. **Both halves are already registered, and one is already the directory's own recommendation.** `10_LOOPS_AND_GAPS.md:68` (`P-07`, `registers[]`) and `:70` (`P-09`, `stakes_grade` — *"**try deleting the key.** ⚠ **The most likely deletion in the schema**"*), plus `03_PARAMETERS.md:411`: *"**If so, delete the key.** Registered as `P-09`, and it is the one most likely to be deleted."* The position cites `P-09`, so this is not concealment — but a conversion that lands on the directory's own stated next move is not a conversion.
4. **The `P-39` adoption creates a dilemma the position does not resolve.** It replaces `registers[]` with *"Fig. 8's 7→7 misreading map … applied as A RISK THE MANNER RUNS rather than a lookup (P-39)."* `06_RESOLUTION.md:310-323` states, as its headline: *"**EXACTLY ONE THING IN THIS SUBSYSTEM IS STOCHASTIC, AND IT IS THE DRAW.**"* A *risk* is either a second draw — which breaks that claim — or an expectation, which is the deterministic lookup `P-39` (`10_LOOPS_AND_GAPS.md:97`) says is wrong. Both branches are defects, and the position picks neither.

Minor: the falsifier requires *"a **fifteenth** key fails the load naming the row."* After deleting two keys the loader's refusal boundary is the thirteenth, not the fifteenth — the number is copied unchanged from `03_PARAMETERS.md:721`, which was written at 14 keys + `id`. An arithmetic slip in the falsifier of the position whose entire content is arithmetic.

**Asymmetry with Position 8, and with its own input.** Position 13 deletes `registers[]` because obstacle term 4 already reads Fig. 8 (*"a second home, `ID-2`"*). Position 8 **keeps** `proofs[]` on the identical shape — a set-valued admissibility key alongside an obstacle term that reads the same figure family — calling it *"an admissibility set doing its real job."* The census the agonist declared as established and non-re-derived scores them symmetrically: `A_FABLE_INTERROGATION.md:231` (B-3) *"Cost: −1 key"* and `:232` (B-4) *"**Cost: −1 key**, one weight family."* The document breaks that symmetry in opposite directions in two positions and argues for neither.

**SEVERITY: material.** The `registers` half is sound and well-evidenced; the census supporting it is wrong, the arithmetic in its falsifier is wrong, both halves re-propose registered rows, and the replacement inherits an unresolved dilemma.

---

## THE BINARIES I DO **NOT** CONVERT — **WOUNDED. Two of seven rows are genuinely forced.**

I tested each row for whether the cited axiom entails the wall.

| row | verdict |
|---|---|
| **B-18** you cannot make anyone a party | ✅ **FORCED.** `AX-1`. Writing another's `commit` is a non-owner write; `03_PARAMETERS.md:314` makes party-hood *their own `commit`*. The weaker lawful shape (provocation) is correctly named and correctly measured-severed. |
| **B-19** the creditor has no verb | ✅ **FORCED**, narrowly. `T-m` (`01_AXIOMS.md:1127`) makes closure ownership; a holder-side closer is a non-owner write. But the row re-states `P-36` (`10_LOOPS_AND_GAPS.md:94`), already registered as a ruling, and the survivors it names (`tell`, a counter-`oblige`) are `A_FABLE_INTERROGATION.md:247` verbatim. |
| **B-7** `appeal_basis: none` | ❌ **NOT FORCED — a refusal of convenience.** The two authorities cited are `03_PARAMETERS.md:479` and `07_THE_GAME.md:256-257` — both **design prose**. Under `CLAUDE.md` §0.05 a design document may not be cited as the reason a behaviour is correct. And the agonist's own declared input scores it convertible at zero cost: `A_FABLE_INTERROGATION.md:235` — *"Finality can be a function of degree and of the declared cap. **Cost: 0 — a reader for a dead field; the key becomes derivable.**"* The document adopts that census "as established and not re-derived" and then silently overrides it. |
| **B-6** multilateral disposal | ❌ **NOT FORCED.** The refusal cites `T-a` (a tally has no owner). But `05_PROCEDURE.md:94-98` supplies the `T-a`-compliant construction in the design's own words: *"the count is a **Query** over the bench's live determinations, evaluated **by a later act** … That keeps the map a map and puts the threshold where `T-b` allows it."* The rebuttal — *"a different game in which one person decides"* — is an assertion against a construction the directory itself endorses, and it re-refuses `P-15` (`10_LOOPS_AND_GAPS.md:82`), already registered as *"a ruling on whether majorities are wanted."* |
| **B-9** `term_required: false` | ⚠ **MISFILED.** The operative reason is *"`Tenure` has no `term` (`P-04`)"* — `shape.py:2066-2091` confirms the field's absence. That is an **unbuilt field**, not a wall. Filing it under *"where the forbidding is the game"* and praising it as *"authored indefiniteness, lawful under `AX-6`"* dresses a build blocker as a design virtue. |
| **B-11** whose `Person.stance` | ⚠ **NOT A REFUSAL.** The row says so itself: *"Not a binary to convert — an `AX-4` ambiguity to close."* |
| **R-18** never show a number | ⚠ **NOT A REFUSAL.** The row says so itself: *"Not a binary."* |

**WHAT IT SHOWED.** A section headed *"where the forbidding is the game"* contains two forced walls, two refusals resting on prose or contradicted by the directory's own lawful shape, one unbuilt field, and two rows that concede they are not refusals. `A design that converts every binary has stopped having a shape` is right; this table does not establish that this design has one.

**SEVERITY: material.** B-7 and B-6 are the load-bearing rows and both are answerable.

---

## THE COUNTS · my independent recount

### "New primitives: ZERO" — **BROKEN, on one item, and it is a signature.**

Recount of `0 carriers · 0 edge kinds · 0 verb names · 0 fields · 0 Event kinds`:

- **Carriers, edge kinds, verb names, Event kinds: I confirm ZERO.** I checked every position for a smuggled kind. `speech_kinds` are kinds of one verb (`ID-7`, `04_VERBS.md:141-148`); `matter.*`, `speech.unheard`, `tenure.graded` are all declared (`04_VERBS.md:73-78`, `08_SEAM.md:118-124`); no position names a new verb. Position 7's decline of `Tenure.payload` on the `#358` rev.2 migration is a genuine, correctly-reasoned avoidance.
- **Fields: I confirm ZERO on `Person`/`Tenure`/`Record`/`Claim`/`Event`.** Position 3's `Partial: ["Person.stance"]` reuses an existing matrix row (`08_SEAM.md:122`) and keeps loader invariant 12's 4-and-4 (`08_SEAM.md:143`).
- **⛔ THE SMUGGLE: the seam signature.** Position 10 removes `veto: bool` and requires `extension` plus a `licence_failures` context key to cross `seam/ladder`, whose contract is fixed at `degree(margin, veto?) -> Degree` (`04_CODE_ARCHITECTURE.md:163`) and whose wrapper contract is *"nothing, ever … a `Margin`"* (`:164`). That is **an amendment to a contract owned by a different directory**, of exactly the kind the document itself prices elsewhere as *"an amendment to a theorem, not a row"* (its own refusals table, CK3's hook). It is booked as a **negative** cost. Two STRUCTURAL impossibility grades (`09_IMPOSSIBILITIES.md:20` and `:31`) are spent to pay for it and neither appears in any cost line.
- Two more not-primitives that are not declared: Position 1's `speech_kinds` roster adds two typed **columns** (`apt_genre`, `apt_rung`), which is a schema addition, not `ID-12` membership; Position 12 changes `verdict_reasons`' **domain** from `{given, withheld}` to `{total, actor}` while booking `keys 0`.

**Verdict: the primitive count is right on its five named axes and false on the axis it does not name — the seam contract.** That is the one place a "zero primitives" claim could hide something, and it is where it hid.

### "New hidden magnitudes: TEN, obstacle five → twelve" — **arithmetic correct, accounting BROKEN.**

The table's internal arithmetic checks: 2+1+1+4+1+1 = 10; 5+1+1+1+4 = 12, +1 nested = 13. What it omits:

| undeclared | where | why it is a magnitude |
|---|---|---|
| **Position 2's band cut points** | agonist's `band( max{c.confidence} )` | only the *band→ordinal coefficient* is counted. Where the bands cut on `Claim.confidence` (`shape.py:2157`) is a family of magnitudes, booked as *"one data roster."* The position's own §(vi) calls it *"a shadow ladder — which is `T-k`'s exact smell."* |
| **Position 10's veto threshold** | `may_overwhelm(... ) return licence_failures == 0` | `0` is a chosen cut. `< 2` is equally defensible and never declared or swept. |
| **Fig. 8 as a risk family** | Position 13 | booked as *"hidden magnitudes 0."* Under the `P-39` reframing the position adopts, a 7×7 map of *risks* is up to 49 parameters. Defensible only if term 4 already needed them — but `P-39` is graded **pending edit** (`10_LOOPS_AND_GAPS.md:97`), so the position banks an unexecuted edit as pre-existing cost. |

**And the second hidden term.** The section asserts *"Only `reception` is genuinely hidden."* Term 13 — the prior finding's strength — is `band(max{c.confidence : c ∈ **actor.ledger**})`, i.e. read off the **determiner's own ledger**. `Claim` *"Lives in the HOLDER'S OWN ledger"* (`shape.py:2149`), and `07_THE_GAME.md:181` lists as **⛔ forbidden** *"anything from a resolver-side Query."* An appellant cannot read it. That is two hidden terms, not one, stated in the same section that counts term 13.

### ⭐ THE TWELVE-TERM OBSTACLE: **it is noise at the player interface, and the document's own defence names the condition under which that is true and does not test it.**

The section says: *"If `reception` is ever weak, the whole obstacle is legible and `15` PART A's solver comes back."* **`reception` is weak today, by the directory's own register**, and this was checkable in two files:

- `15_WHY_IT_IS_A_GAME.md:55-66` makes `reception` the *sole* answer to the solver, composed from the hearers' claims **and their convictions**.
- `06_RESOLUTION.md:35-40` — `convictions` is **four of thirteen**, `H-46` open by ruling.
- `10_LOOPS_AND_GAPS.md:100` (`P-21`), graded **absent**: *"**NOTHING IN THIS DESIGN MOVES A CONVICTION** … `AX-3`'s *argument and consequence move what is held right* has **no producer** … **The most important thing this proposal did not build.**"*
- `15_WHY_IT_IS_A_GAME.md:190` says it in terms: *"a bench whose convictions never move is a bench that never learns. The loop `L-6` stays severed and now matters."*

So the one term carrying the whole anti-solver argument is half-static and half-unenumerated. Adding seven terms sourced to the **visible** arrangement (`07_THE_GAME.md:179` — *"the **arrangement** … it is visible"*) and to the player's own prior acts makes the sum **more** legible in direction, not less.

**On estimability specifically — the answer to the question I was asked.** Direction is estimable for eleven terms; **magnitude is estimable for none**, because every coefficient is a resolver-side injected constant and `07_THE_GAME.md:181` forbids showing anything resolver-side, while `R-18` forbids the obstacle, the margin, a band preview and a percentage. To recover magnitudes a player must invert a twelve-unknown sum from a **four-valued** observable, one draw per interaction, six draws per hearing (`06_RESOLUTION.md:325`), with no re-roll and no repeat of the same room. That is unidentifiable, not estimable. What the player can know — *"a genre mismatch is worse; a repeat charge is worse; a stake is worse"* — is exactly what the binary told them. **So at the player's interface the conversion delivers the binary's information plus unrecoverable magnitude: a decorative gradient over a real one.**

The defence that *is* available and that the document does not make: the twelve terms are a genuine gradient **in the world model**, where NPC-vs-NPC proceedings diverge without a player watching — `§0.06`'s R-half with no player in it. That defence needs no player legibility at all. The document reaches instead for *"eleven of twelve are estimable"*, which is an **uninstrumented number with no control** (`CLAUDE.md` §0.1 pt 4) defending the single most expensive claim in the file.

---

## CROSS-CUTTING FINDINGS

1. **Terms 7 and 8 write into the same accumulator and neither position knows the other exists.** A player choosing between pressing (risking a Partial → `HELD_STEP`, P3) and passing (`PASS_STEP`, P9) pays in the same currency. If `PASS_STEP ≥ HELD_STEP`, silence is strictly dominated by pressing and P9's decision does not exist; if `PASS_STEP` is small, P9 is a shrug. **The existence of the choice is decided by the ratio of two independently-swept constants that no falsifier compares.** This directly refutes the count section's defence that *"each term is separately sweepable"* — separately sweepable and jointly meaningless are compatible.
2. **The `T-d` / `Event.subject` defect (`01_AXIOMS.md:324-330`) is load-bearing on Positions 8, 11 and both broken channels of 11, and is declared for only some of them.** Position 9 enumerates the affected positions as "3 and 5" and omits 8.
3. **An unread surface neither the producer nor the directory's own adversarial record checked.** `13_ADVERSARIAL.md:62` marks finding #12 (five out-of-domain row values) **✅ applied**. Three are still in the file: `03_PARAMETERS.md:608` `records_dissent: n/a`, `:611` `verdict_reasons: n/a`, `:611` `disposes: standing`. A ✅ in an adversarial record that the file contradicts is `ID-11`'s corpse committed in the file written to prevent it.
4. **Citation hygiene, consolidated (nit).** `01_AXIOMS.md` is cited bare three times (Positions 3, 9, 11) while every other bare `NN_` citation in the document resolves inside `2026-09-05-proceedings-subsystem/`, where `01_` is `01_THE_STUDY.md`; the file is actually `/home/user/ttrpg/proposals/2026-09-03-meta-architecture/01_AXIOMS.md`. `shape.py` line numbers run systematically one-to-two low: `Claim.subject` 2151→2152, `Proposition.id` 2426→2428, `frozen=True` 2425→2424, `_ch_witness_key` 4360-4364→4361-4365. The declared input is named `FABLE_PLAYABILITY.md`; the file is `relay/A_FABLE_INTERROGATION.md`.

**OBSERVATION (out of lane, not a ruling).** `06_RESOLUTION.md:83-87` computes `pool = brought + conduct × latitude(game)` while `:114` states *"This directory implements C"* — option C being *"latitude in the OBSTACLE only"* (`:112`). If `06`'s §B.3 governs, the obstacle has **four** room terms and not five, and every "five terms to twelve" statement in the document is off by one at the base. I record what I saw; the pool/obstacle allocation is `06`'s lane, not mine.

---

## THE ONE POSITION I WOULD DELETE

**Position 11.** Not because it is the weakest — Position 12 is — but because it is the only one whose failure causes a *worse* outcome than doing nothing. Positions 8, 10 and 12 fail by being inert or by overspending; a reader who acts on them builds something that does not work and finds out. Position 11 produces no artifact at all: its deliverable is the argument that `P-41` — `13_ADVERSARIAL.md:70`, *"the sharpest playability finding either pass produced"* — is *"right about the timing and wrong about the amount."* That argument is false on four independently checkable lines (`shape.py:4357`, `:4378-4381`, `:4398-4403`, `:4361-4365` against `04_VERBS.md:60-61`), and if believed it licenses shipping the six `floor: closed` rows unchanged. A wrong fix gets caught by its own falsifier. A wrong *reassurance* closes the ticket.

---

## THE ONE POSITION I COULD NOT BREAK

**Position 9's core mechanism, and the specific attack that failed.**

I expected Position 9 to die on availability: under `AX-1` the engine cannot make anyone act, candidates form inside each person from what they hold (`AX-2`), and I expected the fold to iterate the **acts that exist**, in which case a silent attendee would never appear in the loop at all and there would be nothing for the provider to observe — no pass, no counter, no term. That would have made Position 9 prose, exactly as Position 12 is.

It is not. `05_PROCEDURE.md:39-44` iterates **attendees**, not acts — `ORDER the attendees, per arrangement.order / for each, in that order: the person forms candidates from WHAT THEY HOLD` — and `:62` names *"every attendee forms no candidate, or takes none"* as an observed state of that loop. The provider genuinely knows whom it called and what came back, without an Event, without a write, without touching `Event.subject`, and without a second resolver: `08_SEAM.md:88-90` licenses ordering sub-steps from the arrangement row and the observation is of the provider's own return values. The claim *"it needs no `Event.subject` attribution"* is true, and it is the only position in my lane that clears the `T-d` defect cleanly.

The position is wounded above on its scope (`P-08`, the `free`/`written_only` carve-out, charging players who had no lawful act) and on an undeclared cost (an unwitnessable outcome-moving quantity). **The mechanism itself survived, and this is the attack that failed.**
<!-- Permitted by CLAUDE.md §0's narrow exception (RULED by Jordan, 2026-09-17): a TERMINAL pass whose
     VERDICT IS THE DELIVERABLE may record it where the thing it judges lives, as a sibling inside the
     target's own directory. Never a standing corpus, never a new tree, never `audit/`.

     THE TEST, applied honestly before this file was written — DOES THIS CREATE WORK FOR A FUTURE
     SESSION? No. Every limit below is a judgment about a document that already exists in this
     directory, and the work those limits imply is owned by 04_BUILD_ORDER.md, which is where a
     session should look for what to do. Nothing here is a queue. If a later reader finds this file
     being mined for work items rather than read for reasoning, that is the exception failing and the
     repair is to delete it. This record dies when the suite it judges dies. -->

# The pessimistic steelman NERS verdict — 2026-09-17
Relayed verbatim from the read-only pass over `proposals/2026-09-17-governance-and-holdings/`.
Instruments: A only (no draw). Grade: `paper`. Prepended `[SELF-AUTHORED — bias risk]` by the auditor.

## MUST BE STATED AS A LIMIT
1. **The policy instrument's MATTER half is a DEPARTURE, not an EXTENSION.** True of the carrier and
   the four RESOLVE-gating clauses; FALSE of `draw:`/`spend:`/`levy:` read at MATTER and of
   `reach: all`. `holonic_ARCHITECTURE.md` §37.3 forbids, by name: *broadcasting a dispensation to all
   descendants* ("everyone would receive identical, undistorted terms"), *applying a dispensation as a
   state write* ("the compliance contest, and therefore all political friction"), *a `scope` that
   enumerates places*, and *assuming delivery* ("the distinction between never received and received
   and refused, which is the whole of enforcement drama"). `01` honoured row 3 only. `ARCH` does not
   supersede it: F8 keeps `comply` reading the actor's OWN claim of the terms; F.15 files the downward
   mechanism as an OPEN gap.
2. **The headline path does not construct (C5).** `build_realm` builds `province 0` and no `contain`
   chain names one; a Count's seat is a cluster seat whose clause reaches by `binds` — the diagonal the
   suite itself marks UNBUILT. Shipped objects on the path: `contain`, `presence`, the larder,
   `transfer`, `claim.decayed`. **Absent: `issue`, `in_force`, the clause, `demanded`, and any hearth
   store (0 of 211).**
3. **"The share leaves `r.stores` at MATTER" contradicts `02`'s own RULED line** — `transfer` binds
   `from` to the actor's own rung. The hearth is thinner only after somebody's `transfer`, one season
   upstream.
4. **`sit:` cannot create a date.** `(Date, due_at)` is `[RES]` and written ONLY by `convene`; channel 4
   fires a condition, never an occasion; and `convene` is unformable (`H-71`).
5. **`draw: None` starves.** A rung with no `draw:` ancestor feeds nobody, so the founding-policy
   inventory is MANDATORY — the suite's own "no authored inventory" boast is false as filed. And a
   King's death lapses every `reach: all` `draw:` at once: 211 hearths, ~5 re-issues/season.
6. **The uniformity rule has no checker.** Nothing validates a runtime Record at load. There are NO
   free predicates: a clause is one of seven fixed cells plus operand values, so "only the sworn" is as
   unspellable as "except Björn" unless a fixed cell tests a `commit` edge. Grade: NOTHING today.
7. **`ceiling = matured/declared`** reads a per-stage maturation that `Record.matured: bool` cannot carry.
8. **The conferral claim's deposit is a NEW deposit rule**, not a ride on the existing channel (the
   deposit's predicate is `e.kind`).
9. **Same-`(rung, clause)` collision is an unwritten branch** (arrival order decides). The gate-5 fix —
   one live dispensation per `(rung, clause)` — needs a `cardinality` requirement form, which has no
   `@requirement_form`.

## MAY BE CLAIMED, HAVING SURVIVED
- **The seat model is `ARCH §B.7` entire.** Every clause opened and verified. The purview defect is
  real and re-measured: 3 of 19 posts on the ladder, 16 of 19 `rung None`, conferral and revocation
  empty on 19 of 19.
- **The measurement block reproduces**, re-run: the census; 38 verb rows / 18 resolvable / 28 formable /
  13 both; 35 holds = 19 person + 16 faction; 0 records, dates, docket, crossings; 81 `need` questions;
  10 producerless RES rows; `R-04 not_met`; `m1` NOT MET on its doc-derived row; 0 ED violations;
  `"inferred"` 0 occurrences in `.py`; `.upkeep`/`.binds` 0 readers; `verbs` 5 probe callers; no `port/`;
  wear 10 against floor 800 gives the first crossing at pass 21 by arithmetic.
- **`governance_modes`/`power_bases` are NOT re-added and the cut is named.** `fort_level` and
  `facility_tier` withdrawals verified reader by reader; the export check is blocking.
- **Q5 has a producer**: 110 hearth-subject claims after one season.

## THE AXIS TABLE
| axis | verdict | what would overturn it |
|---|---|---|
| N (six directions) | **FINDINGS** | `reach` top-down: its loss survives the cut — nearness plus dispatch presence already carry a superior. `regrowth` rows: no reader |
| E-OVERHEAD | **FINDINGS** | code in ~27 against code out ~10, plus 16 content rows. "Net deletion" holds only against a counterfactual that never existed in code (14 design-only verbs, a siege subsystem), not against the tree |
| E-LEGIBILITY | **FINDINGS** | the wear x restore x yield equilibrium; a hearth cannot intuit why it reads the Duke's clause under `reach: all`. The seven fixed cells ARE legible |
| R-COMPLETE | **FINDINGS** | `draw:None` starvation · same-rung tie · `sit:`→Date · ceiling/matured · 16 of 19 cluster seats unreachable by the walk |
| R-VARIETY | PASS, narrowed | variety is operand values plus where reach and dispatch are spent |
| R-WORLD | **PASS in shape / DEAD in tree** | unwatched today: wear, yield, claim decay, 723 refused `work`s. Every drama row chains through an unformable office act or an unbuilt body |
| R-CHOICE | **FINDINGS** | `reach: all` + no `sit:` + absence STRICTLY DOMINATES at every issuing seat: same act, wider reach, no named cost |
| S-UP | **PASS in shape / FAIL in tree** | `petition` is `own` with no body; docket 0; the convener who pays cannot be formed |
| S-DOWN | **FINDINGS** | `in_force` publishes rather than addresses (good) — but at MATTER under `reach: all` it is undistorted broadcast, §37.3 row 1 |
| S-PAUSE | **FINDINGS** | channel 4 hands off to a sitting where `judging_set` raises `Unspecified` — a pause to nowhere |
| S-METHOD | FINDINGS | `restore` normalized against fixed-point; capacity floor+sum against the VESSEL bound is consistent |
| the design's own falsifiers (§8) | **HITS** | a family that "asserts on a carrier that does not exist": one cannot observe, one is a universal absence, one is falsified today |

## FALSE N-LINES FOUND
- **`regrowth: 0` rows** — no table, no reader. Deleted.
- **`reach: all`** — narrowed: nearness carries subsidiarity, dispatch carries reach; the operand
  carried only the broadcast §37.3 forbids.
- Null on the other nine candidates (`dispensation`, `in_force`, the seven clauses, Q5, `works`, the
  site families, `capacity`, `character`, the read licences) — each walked, producer named or gap declared.

## THE GRADE
`paper`. "The whole difference is one artifact" — `@effect_for("commit")`, or build items 1a+1b.
Nothing in the suite has run.

# 02 · DISPOSITIONS — the SETTLEMENTS lane

## Status: **MEASURED (2026-09-18)** for every `file:line`, `## Status:` line and count below that is attributed to the working tree — each was opened at the line quoted (`CLAUDE.md` §0.1 pt 3 row three). **`paper`** for every verdict, which is a judgment. **§2 is the exception and it is `RUNS`:** its numbers came out of two executions recorded in §2.0.
## Scope: `proposals/2026-09-10-settlements-factions-populations/` (7 files), `proposals/2026-09-17-governance-and-holdings/02_THE_BUILT_WORLD.md` (`ED-SE-0052`) and `proposals/2026-09-17-governance-and-holdings-r2/04_MATTER_AND_WORKS.md` (`ED-SE-0053`). **No other tree, and the ORDER is not this file's business.**
## Counts (files, lines, inbound refs) are `00_THE_CENSUS.md`'s, handed to this pass and **not re-measured** (`CLAUDE.md` §10 pt 1): the 09-10 tree is **7 files, 2,842 lines, 5 inbound refs from `proposals/` and 3 from the rest of the repo**.
## Closed verdict set: `LIVE` · `SUPERSEDED` · `ABSORBED` · `SPENT` · `ORPHAN` · `NEEDS-STATUS`. Nothing outside it appears below.
## ⚠ **`ED-SE-0051` IS NOT ANSWERED HERE AND MAY NOT BE — AND IT DOES NOT NEED TO BE, BECAUSE JORDAN ALREADY RULED IT.** §3 records the ruling, verified by opening it, and the **one surface that disagrees**: the ledger row is still `open` / `needs_jordan: true`. Reporting a ruling is not making one; **§3 chooses nothing.**

---

## §1 · The table

| # | file(s) | verdict | why, with its citation | `engine_season:` |
|---|---|---|---|---|
| **1** | `2026-09-10…/02_PROPOSALS_SUBSTRATE.md` **P1 · DEARTH REACHES THE BODY** (`:16`) | **ABSORBED** | Its whole chain runs. `(Person, body)` is written at MATTER — `loop/matter.py:309-313`, `w.write("body", WriteClass.MATTER, … emits="body.changed", subject=pid)`; the band crossing is `matter.py:315` through the shared `_crossings` (`matter.py:30`); death at zero is `matter.py:323`, `lambda pid=pid: w.remove_person(pid)`. Falsifiers `LB-3a`/`LB-3b`, `engine/season/tests/test_governance_build.py:160` and `:343`, **25 passed** (§2.0 run A). ⚠ P1's *arithmetic* did not survive — see row 2 | **BUILT, not pending.** `loop/matter.py`, `queries/world_q.py`, `state/world.py`, `loop/effects.py`. What is **NOT** built is the value: `data/fixtures.py:494` ships `body_step=0`, the declared control arm, so the chain executes and moves nothing (§2.2) |
| **2** | the same file's P1 **draw arithmetic** — `wt × Σ weight(eaters at r)` against the rung's OWN stores, deficit shared as `short × weight(p) / mouths(r)` (`:16`, the `mouths` bullet) | **SUPERSEDED** by `2026-09-17-governance-and-holdings-r2/04_MATTER_AND_WORKS.md` §A.3 (`:414`) | The rung-local draw counted eaters where there was nothing to eat. `matter.py:207` strikes it in place — *"~~PER RUNG, AGAINST THAT RUNG'S OWN STORES~~ -> PER EATER, AT `nearest_store`"* — and records why: **0 rungs with both eaters and stores** on `build_realm(0)`. The shipped shape is per-eater up the `contain` ladder (`matter.py:249`), which needs **no `mouths(r)` denominator at all**, because the shortfall is person-keyed at source (`matter.py:250-251`, `short_by_person.setdefault(pid, {})[k] = want`) | **NONE — already spent.** The successor is the built code; nothing further is implied |
| **3** | `2026-09-10…/02_PROPOSALS_SUBSTRATE.md` **P2 · THE BODIES CLOCK** (`:142`) | **LIVE** | Ageing, births and deaths on `Rung.envelope`. **Nothing of it is built**: `envelope` is kept (r2 §A.5, `:862`) but no MATTER pass moves it, and P2's own `envelope_bands` roster, `band_seasons`, `fertility` and `mortality[band]` fixtures do not exist. It is also **P1's consumer** — P1's `mouths(r)` second term was P2's, and row 2 removed the denominator, so P2 must be re-read against the shipped per-eater draw before it is built | **PENDING, and the ruling of §3 changes its shape.** A MATTER pass in `engine/season/loop/matter.py` before the larder, writing `(Rung, envelope)`; roster + fixture rows in `engine/season/data/fixtures.py`; **plus the ruled second conjunct on the births term** (§3). ⚠ Its `fed_ratio` term now has no rung-level denominator to read — see row 2 |
| **4** | `2026-09-10…/02_PROPOSALS_SUBSTRATE.md` **P3 · INDIVIDUATION IS A REFUSAL** (`:255`) | **LIVE** | Untouched by round two and by the 3a/3b build. It is the named seam between the envelope's counts and the named cast, so it is **downstream of P2**, not of P1 | **PENDING**, and it cannot be sized before P2's envelope pass exists |
| **5** | `2026-09-10…/02_PROPOSALS_SUBSTRATE.md` **P4 · FOUNDING AND BUILDING** (`:347`) | **SUPERSEDED** by `…-r2/04_MATTER_AND_WORKS.md` §A.6–A.7 (`:914`, `:1187`) | Round two specifies the same subject completely and more narrowly: a `works` **is a `Record`** with five moments, `found` is **the one new verb**, and `restore` climbs to a ceiling counted off `term.matured` emissions rather than a per-stage field. P4 is kept as the statement of the problem; the specification to build from is r2's | **NONE from P4 as written, and P4's SUBJECT is now load-bearing:** §3's ruling makes *"found and build"* the demographic throttle, so r2's `found` is what the ruled Query throttles against. The pending work is r2's — row 9 |
| **6** | `2026-09-10…/03_PROPOSALS_POLITICS.md` **P5 · RISK OF REVOLT IS A QUERY** (`:11`), **P6 · FORSWEARING COSTS** (`:197`), **P7 · A DISPENSATION IS A DOCUMENT** (`:274`) | **LIVE** | All three stand and none is settlement-substrate: the lane's position in the ratified order is **24, SE-BUILD (settlements P1–P4)** (`workplans/2026-09-18-governance-settlement-behaviour-plan.md` §3, via `registers/handoffs/HANDOFF_SE.md`'s header), so P5–P7 are outside that block by the order's own wording. P7 additionally requires overturning a **written refusal** — `verb_table.yaml:117`'s `{form: existence, of: subject, kind: Record}` for `comply` — named in `00_INDEX.md`'s held-back list, not slipped in | **NONE at SE-BUILD.** P5 is a Query over existing state; P6 and P7 touch `references/verb_table.yaml` and a refusal, which is a ruling before it is a code change |
| **7** | `2026-09-10…/01_PRIMITIVE_BASE.md`, `04_EVALUATION.md`, `04_EVALUATION_part2.md`, `05_COLLISIONS_AND_RESIDUE.md` | **LIVE** as reference, **SPENT** as a work surface | These four are the read of the tree, the evaluation and the residue — `05`'s §4 (`:182`) is the filing of **E-1 / `ED-SE-0051`**, which is still the lane's one open escalation, so the set cannot be called spent while that row is open. But **`01_PRIMITIVE_BASE.md`'s §1 measurement is now stale in the one place that matters**: its *"`(Person, body)` has no producer at MATTER"* reading is false against `matter.py:301-311`. Read `01` for the primitives, never for the counts | **NONE.** Documents about code, not designs for it |
| **8** | `2026-09-17-governance-and-holdings/02_THE_BUILT_WORLD.md` (`ED-SE-0052`) | **SUPERSEDED IN PART** by `…-r2/04_MATTER_AND_WORKS.md`, **and it says so itself** | Its own banner, `:4-11`: hearth larders, the delivery move and every `in_force`-gated reading of a built thing are **overturned**; the fabric/address ontology, the `hold`-guard argument, `Rung.envelope` and the `fort_level`/`facility_tier`/`governance_modes`/`power_bases` withdrawals are **kept by pointer**. ⚠ **A split verdict is unavoidable and the file already carries the split** — do not re-disposition it whole. Its `ED-SE-0051` recommendation (§A.7, `:791`) is superseded in its GROUND, not its conclusion — §3 | **NONE directly.** Everything it proposes that survives arrives through r2 |
| **9** | `2026-09-17-governance-and-holdings-r2/04_MATTER_AND_WORKS.md` (`ED-SE-0053`) | **ABSORBED IN PART · LIVE IN PART** | **Absorbed:** §A.3's `nearest_store` (`:414`) is `queries/world_q.py:172` and `matter.py:249`; §A.4's *"the shortfall crosses the gate"* (`:573`) is `matter.py:301-323`. **Live:** §A.6's `works` lifecycle (`:914`), §A.7's `found`/`restore` (`:1187`), §A.8's five site families (`:1272`), §A.9's fortification-as-condition (`:1318`), §A.10's holdings cuts (`:1354`). Status `:3` is still **PROPOSED, HELD BACK IN FULL**, which is now false of the two absorbed sections and true of the rest | **PENDING, and it is the lane's real queue.** `found` as a verb in `references/verb_table.yaml` + `engine/season/loop/effects.py`'s `_apply_write` map; a `works`-kind `Record` term ripening at MATTER; site-family rows in `engine/season/data/fixtures.py`. §A.6 records that `create_record` with `kind: works` **already fires 69 times a season** |

| **10** | the `hearth_capacity` **FIXTURE TABLE per `site_kind`** as the capacity arm was originally shaped — `05_COLLISIONS_AND_RESIDUE.md:202`, `04_EVALUATION_part2.md:433`, and the ledger row itself (`registers/editorial_ledger_se.jsonl:51`, *"a hearth_capacity fixture table per site_kind, adding no new primitive"*) | **SUPERSEDED** by the ruling recorded at `proposals/2026-09-17-governance-and-behaviour/RULINGS.yaml:1838-1844` — ⚠ **read §3.2 before spending this verdict:** that file disclaims its own authority and the ledger row still reads `open` | Jordan took the capacity **arm** and **rejected this shape by name**: *"A `capacity(w, rung)` QUERY over the rung's dwelling Sites, with a FLOOR — never a fixture table"*, on the ground given at `:1852-1857` — a table *"would be a second authored fact competing with the sites themselves"*. The surviving shape is round one `02` §A.7's and r2 §A.11's, quoted identically in both (`02_THE_BUILT_WORLD.md:798-806`, `04_MATTER_AND_WORKS.md:1417-1424`). ⚠ `04_EVALUATION_part2.md:433`'s objection was correct and is now a cost: *"`site_kinds` is closed at three"* — **MEASURED, still three** (§2.4) | **PENDING and it is ALL of it: `engine/season/queries/world_q.py` has no `capacity`** and `engine/season/` has no `dwelling`, no `houses` count and no `shelters` band floor — §2.4 |

---

## §2 · WHAT IS ALREADY BUILT — items 3a and 3b, read in the code and then RUN

### 2.0 · The two executions these numbers came from (`CLAUDE.md` §0.1 pt 3 row four — *check the run happened*)

| run | command | result |
|---|---|---|
| **A** | `python -m pytest engine/season/tests/test_governance_build.py -q` | **25 passed in 6.76s** |
| **B** | `python -c "from engine.season.harness.populated import build_realm, run; w = build_realm(0); run(N, w=w)"`, then read `w._subsistence_shortfall`, `w.crossings` and `w.log` — for **N = 1** and **N = 2** | §2.1–§2.2 |

⚠ **Run B is the instrument for everything below, and it was run because reading the code cannot answer it.** The code shows a chain; only the run shows whether it moves anything on the world that ships.

### 2.1 · Item 3a — `nearest_store` and the per-eater draw: **BUILT, AND IT MEETS THE DEMAND**

- The owner is `engine/season/queries/world_q.py:172`, `def nearest_store(w, rung_id, kind, ...)`; the one caller is `engine/season/loop/matter.py:249`, `src = world_q.nearest_store(w, homes[pid], k, available=left)`, inside a loop over `world_q.home_of(w)` (`world_q.py:150`). The old rung-local draw is **struck in place** at `matter.py:207`, with its own reason: **0 rungs with both eaters and stores**.
- **MEASURED, run B.** Season **1**: `_subsistence_shortfall` (built at `matter.py:250-251`) holds **46 eaters, 138 units** — the whole cast, because the draw runs before the first yield, which is `matter.py:193-197`'s defended order (*"the reverse order would let a rung eat what it had not yet produced, and no rung could ever run short"*). Season **2**: **0 eaters, 0 units.** Cumulative `stores.changed` = **87** = 37 (s1 yield) + 37 (s2 yield) + **13 draws**.
- ⚠ **13 is r2 §A.4's predicted counterfactual, arriving as an observation.** Round two wrote *"the walk meets 138 of 138, 13 rungs' stores fall"* as arithmetic about an unbuilt engine. The built engine falls **13** rungs' stores and meets **138 of 138**. That is the strongest verification in this lane and it is why §1 row 9 says ABSORBED rather than LIVE.
- **The steady state is surplus, and it is measurable.** After two seasons the 37 store-holding rungs hold `{grain: 2831, salt: 2137, ore: 3663, timber: 703}` — rising, against a demand of 138 a season.

### 2.2 · Item 3b — the body write, the shared `_crossings`, `remove_person`: **BUILT AND INERT AT THE SHIPPED VALUE**

- `(Person, body)` is written at MATTER — `matter.py:309-313` (`w.write("body", WriteClass.MATTER, … record_kind="Person", fieldname="body", emits="body.changed", subject=pid)`), against `w.persons[pid]` rather than a local, which the comment at `matter.py:292-297` ties to `CLAUDE.md` §0.1 pt 1 **by name**: *"a write landing on a copy … leaves every reader seeing the full body forever, with every test still green."*
- The crossing is **one owner for two carriers**: `_crossings` at `matter.py:30`, called at `:315` for persons and `:410` for sites, both reading `band_floors` — and it appends the 5-tuple at `matter.py:58`. No `band_floors.person` second scheme was minted (`fixtures.py:476` holds the `body` row it reads instead).
- Death at zero routes through **one** owner: `matter.py:323` → `state/world.py:320`, `def remove_person(self, who)`, which is also what `loop/effects.py:515` returns from `_eff_kill`. `test_governance_build.py:486` asserts that routing by reading the source.
- ⚠ **AND IT MOVES NOTHING TODAY.** `engine/season/data/fixtures.py:494` ships `body_step=0` — annotated *"swept 0 (control, SHIPPED) / 10 / 67"*. **MEASURED, run B, season 1 with 46 eaters short 138 units: `body.changed` = 0, `person.died` = 0, `len(w.crossings)` = 0, and all 46 bodies at 1000.** The chain executes; the fixture is the control arm. That is `ED-IN-0247` — `registers/editorial_ledger_in.jsonl:22`, and **read the pair of fields, because they are not the same claim**: `"status": "resolved"` (the code landed) with `"needs_jordan": true` (the value did not), under its own heading *"ITEM 3b LANDED AND IT SHIPS AT ITS CONTROL ARM."* **The corpus cannot answer for the number, so the code shipped at the arm where nothing happens.**

### 2.3 · The half of P1 that is NOT built, and it is not the half anyone names

P1 asked for two things from the crossing: the person notices, **and** *"candidates about the hearth"* — the others present notice too. **Only the first is live.** `world_q.py:602-605` reads `for who, what, *_rest in w.crossings:`, then `site = w.sites.get(who)` and `at = getattr(site, "rung", None) if site is not None else None`; for a **person-keyed** crossing `who` is a person id, `w.sites.get(<person id>)` is `None`, so `at` is `None` and the `p.id in presence(w, at)` half of `:605` cannot fire. A body falling is a Question **for its owner alone**. ⚠ The presence branch is not a bug — its own comment (`:594-601`) shows it was added for **site** crossings, where the defect repaired was a site id compared against a person id. P1's hearth-wide reading simply never arrived, and **nothing in the tree records it as missing.**

### 2.4 · What the ruled mechanism has in code today: **nothing**

Measured over `engine/season/` (source and YAML; `__pycache__` excluded): **`capacity` appears only inside `cases/*.yaml` narrative prose** — never as a Query, and not in `queries/` at all. **`dwelling` / `dwell`**: no site kind, no field (the four `dwell` hits are the words *"hearth-dweller(s)"* in `harness/probes.py:1090` and `tests/test_governance_build.py:235`, `:260`). **`houses`: 0 occurrences. `shelters`: 0 occurrences.** And `engine/season/rosters.yaml:836-847` fixes `site_kinds: [harbour, seam, body]` — **three, and one of them is not a site** (*"⚠⚠ `body` IS NOT A SITE"*, `:845`). So the ruled Query has no site family to sum over, no per-kind `houses` count to sum, and no `shelters` floor to test. r2 §A.8's five site families (`04_MATTER_AND_WORKS.md:1272`) is the roster extension it would read.

---

## §3 · `ED-SE-0051` — **RULED, AND ONE SURFACE DOES NOT KNOW IT**

**This section chooses nothing.** It records a ruling that already exists, because a disposition pass that reported this row as open would be reporting a dead question — `CLAUDE.md` §0 test 1, *"a later ruling, commit or head decided it. Cite the successor."*

### 3.1 · The ruling, opened at the line

`proposals/2026-09-17-governance-and-behaviour/RULINGS.yaml`, row `id: "RR-2"`, `disposition: ruled`, `ruled.by: "Jordan, 2026-09-17 (in session)"`, `:1838-1844`:

> **MATTER PLUS HEARTH CAPACITY.** A `capacity(w, rung)` QUERY over the rung's dwelling Sites, with a FLOOR — never a fixture table. `found` (P4, "found and build") is the throttle. SCOPED TO POPULATIONS, per the row's own discharge of `ED-WR-0011`'s pairing rider for NPC generation.

Its own `citation:` block names `registers/editorial_ledger_se.jsonl:51 (ED-SE-0051 …)`, so the row and the ruling are the same question. Three riders travel with it, all in `:1845-1871`: the **floor** exists so *"a population [is not] throttled to zero by a rung with no dwelling sites at all"*, on the same shape as `W5`'s floor-of-1; the **scope** is populations, the NPC-generation half having been settled separately as option A (`registers/editorial_ledger_wr.jsonl:12`); and that option-A settlement is **PROVISIONAL BY ITS OWN TERMS** — *"for now"*, *"we will return much later"* are Jordan's words — *"a qualification that travels with this ruling too."*

### 3.2 · The disagreement, stated rather than resolved

| surface | says | opened at |
|---|---|---|
| `RULINGS.yaml` RR-2 | **ruled** — capacity arm, Query shape, populations scope | `:1838-1845` |
| `registers/editorial_ledger_se.jsonl:51` | `"status": "open"`, `"needs_jordan": true` | read and printed, run B's sibling command |
| `registers/handoffs/HANDOFF_SE.md` | *"`ED-SE-0051` (RR-2) STAYS OPEN, and its ground has MOVED"* | heading *"2026-09-17 · ROUND TWO"* |
| both recommendations | *"RECOMMENDED — **NOT RULED, AND NOT CLOSED HERE**"* | `02_THE_BUILT_WORLD.md:798`, `04_MATTER_AND_WORKS.md:1417` |

⚠ **`RULINGS.yaml` disclaims its own authority, and that is the whole difficulty.** Its header, `:5-6`: *"## Status: PROPOSED — held back. NOTHING HERE RATIFIES ANYTHING, and this file flips no `## Status:` line anywhere in the tree"*, and `:34-36` adds that a `ruled` row — *"`step:` stays `null`"* — *"is not a gate-step closure, and it is not `RATIFIED` canon either."* So the ruling is **recorded** in a file that declines to be the mechanism, while the ledger — which **is** the owner of a row's status (`CLAUDE.md` §0.05 cl.3) — still reads open. **This lane does not flip it.** Whether Jordan's in-session ruling reaches `editorial_ledger_se.jsonl:51` is a decision about the ledger, and the orchestrator owns it.

### 3.3 · What each arm costs, since the row is still priced as a live fork

Reported because the ledger row a reader will meet still says *"PRICED: matter-only costs nothing new; capacity costs one fixture table and one conjunct."* **Both halves of that pricing are now stale**, and that is a fact about the tree, not an argument for an arm.

- **MATTER-ONLY no longer costs nothing new — it costs nothing AT ALL, because it is built.** §2.1 and §2.2: the draw, the shortfall, the body write, the band crossing and the death all run. What is unset is `body_step` (`fixtures.py:494`), which is `ED-IN-0247`. ⚠ **And §2.1's measurement is the honest qualification:** on the shipped corpus the shortfall is **138 units in season 1 and zero from season 2**, against a surplus that grows every season — so a matter-only bound **bites only in the first season** on this world, and it cannot begin to bite again until something makes the population grow, which is P2 and P2 is unbuilt (§1 row 3).
- **CAPACITY no longer costs "one fixture table"** — the ruling rejected the table. It costs a **Query** (`engine/season/queries/world_q.py`), a **dwelling site family** and a **`houses` count** on `engine/season/rosters.yaml:836`'s three-value `site_kinds`, a **`shelters` band floor**, and the **births conjunct** in P2's unbuilt MATTER pass. §2.4 measures all of it at zero today. `04_EVALUATION_part2.md:433` raised exactly this and was right.
- **`found` is the throttle the ruling names, and `found` does not exist.** No `found` row in `engine/season/verb_table.yaml`; r2 §A.7 (`:1187`) is its specification and r2 calls it *"the one new verb."*

---

## §4 · WHAT THIS FILE DOES NOT CLAIM

1. **It does not answer `ED-SE-0051`, and §3 is not an answer.** §3 quotes a ruling and names a surface that disagrees with it. If the ruling is not Jordan's, §3 is wrong and the falsifier is him saying so — nothing in this lane can settle that.
2. **It schedules nothing.** Every `engine_season:` cell is *the change a row implies*, per this gather's own column contract. The ORDER is `workplans/2026-09-18-governance-settlement-behaviour-plan.md` §3's and a later file's; **position 24 SE-BUILD is quoted, never revised** — though §2 is the evidence a reader would need to revise it, since part of P1 is built and the rest of the block grew a ruled dependency.
3. **It re-measured no count handed to it.** The 7 files / 2,842 lines / 5 + 3 inbound refs are `00_THE_CENSUS.md`'s.
4. **Grade `RUNS` for §2, `paper` for §1 and §3.** §2's claims came out of two executions. §1's verdicts and §3's reading of a disagreement are judgments, and a judgment does not execute.
5. **One thing was measured as ABSENT and absence is the cheapest claim to make** (`CLAUDE.md` §0.1 pt 3 row one). §2.4's zeros were taken by grepping `engine/season/` for the *presence* of `capacity`, `dwelling`, `houses` and `shelters` and reading every hit, not by asserting they are missing — the `cases/*.yaml` and `hearth-dweller` hits are reported for that reason rather than filtered away.


### engine/autoload/dice_engine.py:27   [logs: R1_engine]
  CLAIM: | `Degree` (Enum) | `autoload/dice_engine.py:27` | OVERWHELMING, SUCCESS, PARTIAL, FAILURE | n/a | n/a |
    26: 
  >>27: class Degree(Enum):
    28:     OVERWHELMING = "overwhelming"

### engine/autoload/dice_engine.py:95   [logs: PR339]
  CLAIM: | A339-129 | claim-about-repo | "`dice_engine.BandExtension` declares a named policy whose only power is to veto an Overwhelming" | 05§7 | cites existing engine class | — | VERIFIED TRUE — `class BandExtension` at dice_engine.py:95 |
    94: # is a design decision with a ledger entry, not a convenience.
  >>95: class BandExtension:
    96:     """What a subsystem may inject into the ladder. Subclass and override `may_overwhelm`.

### engine/autoload/dice_engine.py:112   [logs: R1_engine]
  CLAIM: | `BandExtension` | `autoload/dice_engine.py:112` | `name`, `context_keys` (class attrs) | subsystem wrapper subclasses it | `may_overwhelm()`/`validate_context()` overridden by subclass |
    111:         return True
  >>112: 
    113:     def validate_context(self, context) -> None:

### engine/autoload/dice_engine.py:163   [logs: R1_engine]
  CLAIM: | `RollResult` | `autoload/dice_engine.py:163` | pool_size, tn, rolls, net, degree, ob | `roll_pool()` | none |
    162: 
  >>163: 
    164: # Per-die EV at TN 7 — the only TN there is.

### engine/autoload/dice_engine.py:171-172   [logs: R1_engine]
  CLAIM: - `dice_engine.py:171-172` — `_MU_PER_DIE = 0.40`, `_SIGMA_PER_DIE = 0.800` (cite `params/core.md Section Expected Value, TN 7`).
    170: #
  >>171: # Note the TN 6 row was always in tension with `_die_result` below, which has never read `tn`:
    172: # μ=0.50 is only reachable if faces 6-9 score, and no face rule here has ever done that. The
    173: # ruling resolves that tension in favour of the die rule.

### engine/autoload/dice_engine.py:180-182   [logs: R1_engine]
  CLAIM: 4. **`degree_from_net`'s ladder is fixed at TN 7, ruled immutable ("TN7 always. Never change TN anywhere ever." -- dice_engine.py:180-182, 2026-08-25).** Any design mechanism (e.g. a Query-derived obstacle) that implicitly assumed a variable TN would conflict with a hard, recent ruling -- must resolve difficulty entirely through Ob, never TN.
    179:               'ever." A varying difficulty is an Ob, not a TN. (ED-IN-0196)')
  >>180: 
    181: 
    182: def _require_tn7(tn: int) -> None:
    183:     """The owner refuses any TN but 7.

### engine/autoload/dice_engine.py:182   [logs: PR337,PR338,PR340]
  CLAIM: | A337-005 | resolution kernel | d10 pool / TN7-always / margin-degree ladder | `cross_scale_action_catalogue_v1.md` §0 [pre-existing, PR #336] | One resolution kernel for every scale: die d10, TN always 7 (`_require_tn7` refuses other values, ED-IN-0196), μ=0.40/σ=0.800 per die, continuous Normal roll, margin=net−Ob, four degree bands. | canon (owner-enforced) | yes — VERIFIED-TRUE, `dice_engine.
  CLAIM: | `dice_engine._require_tn7` refuses any TN but 7 | A337-005/191 | **VERIFIED-TRUE** — `engine/autoload/dice_engine.py:182,196,199,216` |
  CLAIM: | A338-057 | determinism | d10 pool, TN 7 always, enforced by refusal | `engine/autoload/dice_engine.py:182` `_require_tn7`; `:196-216` `roll_pool` | Jordan ruling 2026-08-25 ED-IN-0196; `roll_pool` calls `_require_tn7(tn)` and raises on any value but 7 | BUILT, ratified | VERIFIED-TRUE |
  CLAIM: | 1 | `_require_tn7` refuses any TN≠7 in `dice_engine.py:182,196,216` | **VERIFIED-TRUE** |
  CLAIM: | 5 | `_require_tn7` raises on non-TN7; `degree_from_net` exists; margin ladder Overwhelming≥3/Success≥1/Partial[0,1)/Failure<0 (05 §4.1, throughout) | **VERIFIED-TRUE** — `engine/autoload/dice_engine.py:182` (`_require_tn7`), `:227` (`degree_from_net`) |
  CLAIM: | A337-191 | resolution kernel | d10 pool, TN7 always — `_require_tn7` refuses non-7 | `_v1_part2.md` §"Resolution Kernel" | Before the 2026-08-25 ruling non-7 was accepted, recorded, silently ignored — 19 production sites had always been no-ops. | BUILT | VERIFIED-TRUE, `dice_engine.py:182,196-216` |
    181: 
  >>182: def _require_tn7(tn: int) -> None:
    183:     """The owner refuses any TN but 7.

### engine/autoload/dice_engine.py:227-293   [logs: PR337,PR338]
  CLAIM: | 2 | `degree_from_net` margin ladder (≥3/≥1/[0,1)/<0) is single owner, `dice_engine.py:227-293` | **VERIFIED-TRUE** |
  CLAIM: | A337-192 | formula | Margin-based degree ladder — single owner for every scale | `_v1_part2.md` §"Resolution Kernel" | `margin=net−ob`; ≥3 Overwhelming, ≥1 Success, [0,1) Partial, <0 Failure; both operands may be fractional. | BUILT | yes, `dice_engine.py:227-293` |
  CLAIM: | A338-058 | determinism | Margin-based degree ladder (single owner) | `dice_engine.py:227-293` `degree_from_net` | `margin = net-ob`; ≥3 Overwhelming, ≥1 Success, [0,1) Partial, <0 Failure. Single owner for every scale (Jordan ruling 2026-08-14) | BUILT, ratified | VERIFIED-TRUE |
    226: 
  >>227: def degree_from_net(net: int | float, ob: int | float,
    228:                     extension: "BandExtension | None" = None, **context) -> Degree:
    229:     """THE degree ladder. Single owner for every scale of the game (Jordan ruling, 2026-08-14).
    230: 
    231:     The ladder reads the MARGIN — how far the dice cleared the obstacle — never the obstacle's
    232:     own size:
    233: 
    234:         margin = net - ob

### engine/autoload/dice_engine.py:242-296   [logs: R1_engine]
  CLAIM: 3. **A single degree-of-success ladder, ruled and enforced as the sole owner, already exists.** `dice_engine.degree_from_net` (dice_engine.py:242-296) is explicitly "THE degree ladder... single owner for every scale of the game" -- any design object that needs an obstacle/margin resolution should call this, not define a new one. It even has a formal extension seam (`BandExtension`) for per-subsyst
    241:     Both operands may be fractional. `net` already is — the continuous engine returns fractional
  >>242:     nets. `ob` is RULED to become fractional (Jordan, 2026-08-14: an obstacle rolled against a
    243:     character or faction is "their corresponding score/2 plus whatever specific modifiers exist for
    244:     them in that instance") but ⚠ THAT DERIVATION IS IMPLEMENTED NOWHERE — every call site in the
    245:     tree still passes a hand-set Ob. An earlier draft of this paragraph stated it as an accomplished
    246:     fact; it is a ruling awaiting execution, and the distinction matters here more than anywhere
    247:     else, because this is the function a reader consults to learn what an obstacle IS.
    248:     The Partial band is a whole-success-wide window rather
    249:     than the single point `margin == 0`, which is what the same rule reduces to on integers

### engine/autoload/dice_engine.py:264-270   [logs: R1_engine]
  CLAIM: `_generated` header (params_tables.yaml:1): "GENERATED by tools/export_params_constants.py from engine/params/**/*.md. NEVER hand-edit... Where a value here disagrees with code, THE CODE WINS." `file_count: 37`, `table_count: 258`, `row_count: 1367`. This is reference-only — `dice_engine.degree_from_net`'s own docstring (dice_engine.py:264-270) explicitly disclaims it: the capture still shows the 
    263:     RULED OUT, explicitly, by the same ruling (all three were live here until 2026-08-14):
  >>264:       * Ob-scaled Overwhelming (`net >= 2*Ob`) — Overwhelming no longer depends on difficulty.
    265:       * The separate PP-232 `net >= 3` floor — subsumed; the margin bar IS 3.
    266:       * The Ob-20 exception (Overwhelming unavailable, Partial needing net >= 10) — "always"
    267:         admits no ceiling case.
    268: 
    269:     `extension` is the ED-SC-0032 injection seam: a subsystem's declared `BandExtension`, which
    270:     may VETO an Overwhelming and can do nothing else. It is passed BY THE SUBSYSTEM'S WRAPPER,
    271:     never resolved here — the engine does not know which subsystems exist, and a default of None

### engine/autoload/engine_clock.py:16-22   [logs: R1_engine]
  CLAIM: 7. **The campaign loop currently has 3 phases (SEASON_TICK/ACTION/ACCOUNTING_BOUNDARY), not the design's 6 steps (CALENDAR/MATTER/DELIBERATE/RESOLVE/WITNESS/CENSUS).** `engine_clock.run_tick`'s docstring is explicit that it implements only the phase *ordering*, not the full drain topology (`autoload/engine_clock.py:16-22`) -- WITNESS and CENSUS in particular have no phase-boundary home to attach t
    15: phase's, and its own note says an "earlier draft called `run_accounting(state)` RAW, outside
  >>16: the drain. That was unbounded." Line 103 below calls it raw. That is deliberate and bounded
    17: for now — accounting emits nothing, so there is nothing to drain — but it means the shape
    18: here is the one §4.1 rejects, and closing that is Phase E work, not a footnote to this one.
    19: 
    20: THE DEFECT THE RE-SITING FIXES, stated precisely because it is currently INERT and a reader
    21: should not be misled into thinking a live bug was fixed. Before this module, the two clock
    22: calls lived at the tail of `mc_v18._faction_actions_callback` (mc_v18.py:162-169 at 3d04568):
    23: 

### engine/autoload/engine_clock.py:47   [logs: PR341]
  CLAIM: | 5 | `engine_clock.py:47` states the tree emits "164-229 per campaign" | `00_INDEX.md §5.1`, `05_independent_verification.md §2.1/§3 C-1` | **VERIFIED-TRUE** — `engine/autoload/engine_clock.py` docstring contains the exact phrase "`keys_emitted` is 164-229 per campaign" |
    46: including `key_log_hash`, must be byte-identical — recorded in the commit message. `keys_emitted`
  >>47: is 164-229 per campaign, so the KeyLog hash is a live signal here, not a vacuous one.
    48: The IN-TREE, re-runnable form of that control is `engine/tests/test_parliamentary_bridge.py`,

### engine/autoload/engine_clock.py:90   [logs: R2_systems]
  CLAIM: | **Six loop steps (CALENDAR.MATTER.DELIBERATE.RESOLVE.WITNESS.CENSUS)** | three-phase tick: `SEASON_TICK -> ACTION -> ACCOUNTING_BOUNDARY` | `engine/autoload/engine_clock.py:90` (`run_tick`); documented `systems/overview/sim/season.py:59-72` | **coarser, not a match** | engine_clock owns composition (ED-IN-0199, code-side ruling); no MATTER/DELIBERATE/WITNESS/CENSUS split exists -- see 8 GAPS and
    89: 
  >>90: def run_tick(world, action_callback=None):
    91:     """Run one season tick: SEASON_TICK -> ACTION -> ACCOUNTING_BOUNDARY.

### engine/autoload/engine_clock.py:96-127   [logs: R1_engine]
  CLAIM: - `engine_clock.run_tick(world, action_callback)` (`autoload/engine_clock.py:96-127`) composes, IN THIS ORDER:
    95:     dispatch policy differs between the batch sim, an interactive Godot session, and a
  >>96:     test injecting deterministic actions.
    97: 
    98:     The ordering below is §O.1's, and each of the three scheduler-adjacent lines is placed
    99:     where the spec puts it rather than where it happened to be:
    100: 
    101:       * `accounting_boundary()` runs AFTER the action phase closes and BEFORE accounting's
    102:         body, because it IS the boundary — it drains the OF-7 deferred applies the action
    103:         phase queued, and flips the scheduler to `_PHASE_ACCOUNTING` so that anything

### engine/autoload/engine_clock.py:113-116   [logs: R1_engine]
  CLAIM: - Barriers/writes: `accounting_boundary()` is the barrier that separates "deferred settlement-locus writes queued during ACTION" from "writes applied"; `next_tick()` is the barrier that resets the emission-cap window. **The per-tick emission cap spans BOTH phases** (engine_clock.py:113-116 comment) — resetting it before accounting (the pre-2026-08-27 bug) would have let a tick's total emissions ex
    112:         Resetting it before accounting (the pre-move behaviour) would have let a tick's total
  >>113:         emissions exceed `emissions_per_tick_max` without tripping the guard.
    114:     """
    115:     result = advance_season(world)                       # --- SEASON_TICK ---
    116: 
    117:     if action_callback is not None:

### engine/autoload/engine_clock.py:122   [logs: R1_engine]
  CLAIM: b'. **barrier**: `sched.accounting_boundary()` (engine_clock.py:122) if scheduler present — flips scheduler phase to ACCOUNTING and runs OF-7 deferred applies.
    121:     if sched is not None:
  >>122:         sched.accounting_boundary()                      # --- ACCOUNTING_BOUNDARY opens ---
    123:     composition.require('accounting')(world)

### engine/autoload/engine_clock.py:123   [logs: R1_engine]
  CLAIM: | **MATTER** (loop step 2, event barrier) | **NO clean match; scattered across ACCOUNTING** | `composition.require('accounting')(world)` -> `systems.overview.sim.accounting:run_accounting` (outside this lane) is the closest "world moves without a person choosing" phase, but it is not separated into its own barrier distinct from ACTION | `autoload/engine_clock.py:123` | partial | `engine/`'s loop h
  CLAIM: c. **ACCOUNTING** — `composition.require('accounting')(world)` (engine_clock.py:123) -> `systems.overview.sim.accounting:run_accounting`.
    122:         sched.accounting_boundary()                      # --- ACCOUNTING_BOUNDARY opens ---
  >>123:     composition.require('accounting')(world)
    124:     if sched is not None:

### engine/autoload/engine_clock.py:125   [logs: R1_engine]
  CLAIM: b''. **tick close**: `sched.next_tick()` (engine_clock.py:125) resets per-tick emission counter, returns scheduler to ACTION phase.
    124:     if sched is not None:
  >>125:         sched.next_tick()                                # --- tick closes ---
    126: 

### engine/autoload/game_state.py:74   [logs: R1_engine]
  CLAIM: - `engine/autoload/game_state.py:74` — `MULTS = {'L': 20, 'Sta': 10, 'W': 100, 'I': 15, 'Mil': 10, 'accord': 10, 'pt': 10}` — deliberately NOT moved to the registry yet (docstring at 57-73 explains why: spans two registry blocks, and 3 of 7 numbers have no cited provenance).
  CLAIM: 3. **Confirms** the `engine/` Section 0.05 claim about `descriptors.faction_bounds()` closing the 5-vs-6 gap and Legitimacy's floor-0/ceiling-7 ruling -- `Faction.adjust()`'s docstring (game_state.py:153-196) and `descriptors.py:98-172`'s docstrings are internally consistent with each other and with CLAUDE.md's account, including the still-unreachable `fac.intel` case (game_state.py:164-171, `MULT
    73: # a file whose name lies is worse than a literal that is honest.
  >>74: MULTS = {'L': 20, 'Sta': 10, 'W': 100, 'I': 15, 'Mil': 10, 'accord': 10, 'pt': 10}
    75: 

### engine/autoload/game_state.py:76-77   [logs: R1_engine]
  CLAIM: - `game_state.py:76-77` — `ACCORD_MAP`, `PT_MAP` (5-6 entry float tables).
    75: 
  >>76: ACCORD_MAP = {0: 1.0, 1: 2.5, 2: 4.0, 3: 5.5, 4: 7.0}
    77: PT_MAP = {0: 1.0, 1: 2.5, 2: 4.0, 3: 5.5, 4: 6.5, 5: 7.0}
    78: 

### engine/autoload/game_state.py:105-140   [logs: PR337]
  CLAIM: | `Faction` dataclass has exactly six stats + `territories` list + booleans, no leader/person field | A337-049 | **VERIFIED-TRUE** — `engine/autoload/game_state.py:105-140` matches field-for-field, including the `intel` comment explaining it was added post-hoc and is unwired |
    104: STARTING_ACCORD = world_initial_state.STARTING_ACCORD
  >>105: STARTING_PT = world_initial_state.STARTING_PT
    106: STARTING_GARRISON = world_initial_state.STARTING_GARRISON
    107: 
    108: 
    109: @dataclass
    110: class Faction:
    111:     name: str
    112:     parliamentary: bool = True

### engine/autoload/game_state.py:109-140   [logs: PR337,PR338]
  CLAIM: | A338-075 | class/type | `Faction` — six-scalar dataclass | `game_state.py:109-140` | L/Sta/W/I/Mil/intel (0-7 clamped via descriptors.faction_bounds), territories list, 4 turn-flags, standing (unbounded int) | BUILT | yes |
  CLAIM: | 3 | `Faction` dataclass has no leader/ruler/head field | **VERIFIED-TRUE** (`game_state.py:109-140`) |
  CLAIM: | A337-049 | class/type | `Faction` dataclass (six stats, territories list, turn-tracking booleans) | `_v1.md` §3 faction-strategy primitives | Whole faction model: `name, parliamentary`, six stats, `territories` list, 4 booleans, nothing else. | BUILT | yes — VERIFIED-TRUE, `game_state.py:109-140` |
  CLAIM: | A338-005 | claim-about-repo | Faction has no leader field | `game_state.py:109-140` (verified) | `Faction` dataclass: name, parliamentary, L/Sta/W/I/Mil/intel, territories, standing, 4 turn-flags — no leader/ruler/head field | measured | VERIFIED-TRUE |
    108: 
  >>109: @dataclass
    110: class Faction:
    111:     name: str
    112:     parliamentary: bool = True
    113:     L: float = 2.0
    114:     Sta: float = 3.0
    115:     W: float = 2.0
    116:     I: float = 2.0

### engine/autoload/game_state.py:109   [logs: R1_engine]
  CLAIM: | `Faction` | `autoload/game_state.py:109` | name, parliamentary, L, Sta, W, I, Mil, intel, territories, senator_inward_used, consul_used, peaceful, standing, excommunicated, council_used_this_arc, parl_transfer_used_this_arc | `create_world` (game_state.py:309), `restore_world` (441) | `.adjust()` (153), `.reset_seasonal()` (198), `.reset_arc()` (202); ~31 non-test call sites across `systems/fact
    108: 
  >>109: @dataclass
    110: class Faction:

### engine/autoload/game_state.py:111   [logs: PR337]
  CLAIM: | A337-093 | class/type | `Faction.parliamentary` boolean — entire franchise model | `_v1.md` §3 | Enforces GD-3 at both proposal and ballot. | BUILT | yes, `game_state.py:111`, `parliamentary_vote.py:139-144` |
    110: class Faction:
  >>111:     name: str
    112:     parliamentary: bool = True

### engine/autoload/game_state.py:112   [logs: PR338,R1_engine]
  CLAIM: | **Office** | **NO** | -- | -- | no | No concept of a role/seat distinct from the Faction holding it. `Faction.parliamentary: bool` (game_state.py:112) and the various `*_used_this_arc` flags gate *actions a faction may take*, not an office a Person occupies. Parliamentary machinery (`parliamentary_bridge.py`) operates directly on aggregate Faction stats, never through an Office intermediary. |
  CLAIM: | A338-076 | primitive | `Faction.L` (Legitimacy/Mandate) doing two jobs | `game_state.py:112`; `MULTS['L']=20` | 20/31 adjust() calls target it; ratified as base descriptor 2026-08-23 while LPS-1 ratifies Mandate as a DERIVED aggregate over settlement L/PS — contradictory | BUILT (defect/gap) | yes |
    111:     name: str
  >>112:     parliamentary: bool = True
    113:     L: float = 2.0

### engine/autoload/game_state.py:114   [logs: PR337]
  CLAIM: | A337-107 | class/type | `Faction.W` (Wealth) — only economic state | `_v1.md` §3 economy-accounting | Registry-clamped 0-7. | BUILT | yes, `game_state.py:114` |
    113:     L: float = 2.0
  >>114:     Sta: float = 3.0
    115:     W: float = 2.0

### engine/autoload/game_state.py:118-124   [logs: PR338]
  CLAIM: | A338-077 | primitive | `Faction.intel` — KeyError trap | `game_state.py:118-124` | Declared at 0.0; no `MULTS['intel']` row → `adjust('intel',...)` raises before any bound is consulted | BUILT (unreachable) | INERT |
    117:     Mil: float = 3.0
  >>118:     # fac.intel — ratified 2026-07-08 (descriptor_registry.yaml, OPT-AV-18 / ED-IN-0029) with
    119:     # floor 0, scale 0-7, but the Faction dataclass never carried the field (audit ED-IN-0074 D4).
    120:     # Added at the ratified floor. Currently unread/unwritten by live code (adjust() is not wired
    121:     # for it — no MULTS['intel'] entry until its mechanic is designed); this only closes the
    122:     # canon-vs-code gap so a future reader of `faction.intel` doesn't AttributeError.
    123:     intel: float = 0.0
    124:     territories: list = field(default_factory=list)  # [hash-seed fix 2026-05-20] was set; set iteration depends on PYTHONHASHSEED for str keys, producing cross-process variance in mc_v18 batches. list preserve
    125:     # Seasonal resets

### engine/autoload/game_state.py:124   [logs: R1_engine]
  CLAIM: | -- `hold` | NO | `Faction.territories` list membership | game_state.py:124 | no | as above |
  CLAIM: | **Tenure** (7 kinds incl. `hold`, `oblige`, `commit`) | **NO** | -- | -- | no | No edge type exists anywhere in `engine/`. Relationships are expressed as: (a) scalar dict membership (`Faction.territories: list[str]` — game_state.py:124 — a faction "holds" a territory only via list membership, no edge object, no kind, no provenance); (b) `Key.causes: list[str]` (keys.py:135) which is a **causal D
    123:     intel: float = 0.0
  >>124:     territories: list = field(default_factory=list)  # [hash-seed fix 2026-05-20] was set; set iteration depends on PYTHONHASHSEED for str keys, producing cross-process variance in mc_v18 batches. list preserve
    125:     # Seasonal resets

### engine/autoload/game_state.py:126   [logs: PR337]
  CLAIM: | A337-054 | class/type | `Faction.territories` list | `_v1.md` §3 | Only field representing faction power beyond six stats; no people, no holdings model, no tier. | BUILT | yes, `game_state.py:126` |
    125:     # Seasonal resets
  >>126:     senator_inward_used: bool = False
    127:     consul_used: bool = False

### engine/autoload/game_state.py:129   [logs: PR337,PR338]
  CLAIM: | A338-078 | primitive | `Faction.standing` — 2 things, 1 name | `game_state.py:129`; write sites `crown_initiative.py:81,98,116,119,167,177,254,267,270`; `absolution.py:86`; `parliamentary_transfer.py:379` | Bare unbounded int mutated at 11 sites via +=/-=, never through adjust() (escapes registry clamps); feeds 2 dice pools whose outcomes write it back — 2 undamped positive-feedback loops; DIFFE
  CLAIM: | A337-051 | class/type | `Faction.standing` — bare unbounded int, escapes registry clamps | `_v1.md` §3 | Mutated `+=`/`-=` at 11 sites, never through `adjust()`; feeds two dice pools that write it back — two undamped positive-feedback loops. | BUILT (defect) | yes, `game_state.py:129`; sites listed in `crown_initiative.py`, `absolution.py`, `parliamentary_transfer.py` |
    128:     peaceful: bool = True
  >>129:     standing: int = 0
    130:     # NEW Phase 5/9 faction-unique action flags

### engine/autoload/game_state.py:148-185   [logs: PR340]
  CLAIM: | 11 | All six declared faction stats now floor at 0, ceiling 7, per two 2026-08-23 Jordan rulings; `UNDECLARED_FLOOR=0.5` survives only as the undeclared-stat fallback (13 §2) | **VERIFIED-TRUE** — `engine/autoload/game_state.py:148-185` docstring and constants match exactly |
    147:     #: before ED-IN-0029 was wired at plan S5d.
  >>148:     UNDECLARED_FLOOR = 0.5
    149:     # Same ceiling the registry declares for every stat it DOES declare, so the undeclared case is
    150:     # not quietly more permissive than the declared ones.
    151:     UNDECLARED_CEILING = 7.0  # [canonical: references/descriptor_registry.yaml faction_stats — scale "0-7"/"1-7"]
    152: 
    153:     def adjust(self, stat: str, granular_delta: float,
    154:                floor: float | None = None, ceiling: float | None = None):
    155:         """Apply a granular delta to a faction stat, clamped to the bounds the REGISTRY declares.

### engine/autoload/game_state.py:153-180   [logs: PR340]
  CLAIM: | A340-200 | claim-about-repo | Ordering-hazard fact partially stale, conclusion survives (§2) | 13 §2 | the cited audit's supporting fact ("Faction.adjust floors at 0.5") is now FALSE — game_state.py:153-180 now reads per-stat bounds, all 6 declared stats floor at 0 per 2 Jordan rulings dated 2026-08-23; but the hazard itself survives via a DIFFERENT fact: muster pool = Mil+floor(W/2), so W=0 sti
    152: 
  >>153:     def adjust(self, stat: str, granular_delta: float,
    154:                floor: float | None = None, ceiling: float | None = None):
    155:         """Apply a granular delta to a faction stat, clamped to the bounds the REGISTRY declares.
    156: 
    157:         ED-IN-0029 (ratified 2026-07-08, OPT-AV-14/D14 + OPT-AV-18) set per-stat floors: Influence
    158:         floors at 1 — an institution's influence never fully vanishes — and Wealth, Military,
    159:         Stability and Intel float at 0. This method applied a blanket 0.5/7.0 to every stat for the
    160:         six weeks after that ratification, and none of its 31 call sites overrode it, so a ratified

### engine/autoload/game_state.py:153-196   [logs: R1_engine]
  CLAIM: 3. **Confirms** the `engine/` Section 0.05 claim about `descriptors.faction_bounds()` closing the 5-vs-6 gap and Legitimacy's floor-0/ceiling-7 ruling -- `Faction.adjust()`'s docstring (game_state.py:153-196) and `descriptors.py:98-172`'s docstrings are internally consistent with each other and with CLAUDE.md's account, including the still-unreachable `fac.intel` case (game_state.py:164-171, `MULT
  CLAIM: 6. **A registry-driven bound/clamp mechanism for stat mutation already exists** (`descriptors.faction_bounds()`, game_state.py:153-196) -- floor/ceiling per stat pulled from a cooked registry rather than hardcoded, with a load-time completeness check (`assert_faction_roster_is_covered`, descriptors.py:64-120) that halts import if the registry and the dataclass disagree. This is a working instance 
  CLAIM: | -- `alter` | **closest analogue exists** | `Faction.adjust()`, `Territory.adjust_accord/adjust_pt()` — direct mutation, clamped by registry bounds | game_state.py:153-196, 248-252 | partial (same *effect*, no *representation* as a typed StateChange) | These ARE the game's alter-events today, just not reified as objects — imperative method calls. A `StateChange(subject, mode='alter', driver=...)`
    152: 
  >>153:     def adjust(self, stat: str, granular_delta: float,
    154:                floor: float | None = None, ceiling: float | None = None):
    155:         """Apply a granular delta to a faction stat, clamped to the bounds the REGISTRY declares.
    156: 
    157:         ED-IN-0029 (ratified 2026-07-08, OPT-AV-14/D14 + OPT-AV-18) set per-stat floors: Influence
    158:         floors at 1 — an institution's influence never fully vanishes — and Wealth, Military,
    159:         Stability and Intel float at 0. This method applied a blanket 0.5/7.0 to every stat for the
    160:         six weeks after that ratification, and none of its 31 call sites overrode it, so a ratified

### engine/autoload/game_state.py:164-171   [logs: R1_engine]
  CLAIM: 3. **Confirms** the `engine/` Section 0.05 claim about `descriptors.faction_bounds()` closing the 5-vs-6 gap and Legitimacy's floor-0/ceiling-7 ruling -- `Faction.adjust()`'s docstring (game_state.py:153-196) and `descriptors.py:98-172`'s docstrings are internally consistent with each other and with CLAUDE.md's account, including the still-unreachable `fac.intel` case (game_state.py:164-171, `MULT
    163: 
  >>164:         ⚠ FIVE OF THE SIX FLOORS REACH THE CODE. `intel` does not, and saying "the roster is
    165:         wired" without this qualification would be the false-claim class §1(a) of the plan exists to
    166:         stop. `MULTS` has no `intel` key, so `adjust('intel', …)` raises `KeyError` on the line
    167:         below before any bound is consulted — `faction_bounds('intel')` returns (0, 7) that no code
    168:         path can reach. That is consistent with the field's own history: `fac.intel` was added to
    169:         the dataclass at its ratified floor and is documented as unread and unwritten by live code.
    170:         Wiring it needs a multiplier, which is a canon value nobody has stated, so it is recorded
    171:         here rather than invented.

### engine/autoload/game_state.py:234-253   [logs: PR337,PR338]
  CLAIM: | A338-099 | primitive | `Territory` (T1-T17) — actually a "Province" per ratified hierarchy | `game_state.py:234-253` | tid/owner/accord/pt/garrison/prosperity/fort_level/templar/uncontrolled_since; the ruled scale hierarchy calls this tier "Province", not "Territory" | BUILT (naming collision w/ ruling) | yes |
  CLAIM: | A337-127 | class/type | `Territory` (T1-T17) — what the ruled hierarchy calls a Province | `_v1.md` §3 territory-world | `tid, owner, accord, pt, garrison, prosperity, fort_level, templar, uncontrolled_since`. | BUILT | yes, `game_state.py:234-253` |
    233: @dataclass
  >>234: class Territory:
    235:     tid: str
    236:     owner: str | None
    237:     accord: float
    238:     pt: float
    239:     garrison: bool
    240:     prosperity: int
    241:     fort_level: int

### engine/autoload/game_state.py:234   [logs: R1_engine]
  CLAIM: | **Site** | **NO** as a carrier, **YES** as an aggregate location | `Territory` (16 instances), `Settlement` (defined in `systems/settlements/sim/registry.py`, referenced via `World.settlements` registry, game_state.py:301) | `autoload/game_state.py:234` (`Territory`) | partial | `Territory` has 9 scalar fields (owner, accord, pt, garrison, prosperity, fort_level, templar, uncontrolled_since) — a
  CLAIM: | `Territory` | `autoload/game_state.py:234` | tid, owner, accord, pt, garrison, prosperity, fort_level, templar, uncontrolled_since | `create_world` (316) | `.adjust_accord()`, `.adjust_pt()` (248-252) |
    233: @dataclass
  >>234: class Territory:
    235:     tid: str

### engine/autoload/game_state.py:236   [logs: PR337,PR338]
  CLAIM: | A338-100 | primitive | `Territory.accord` (0.5-7.0 continuous) | `game_state.py:236,248`; `ACCORD_MAP {0:1.0,1:2.5,2:4.0,3:5.5,4:7.0}` | Written by conquest, Govern, Mass Seizure, Parliamentary Transfer, Crown Initiative | BUILT | yes |
  CLAIM: | A337-128 | class/type | `Territory.accord` (0.5-7.0 continuous), bucketed via `ACCORD_MAP` | `_v1.md` §3 | Written by conquest, Govern, Mass Seizure, Parliamentary Transfer, Crown Initiative. | BUILT | yes, `game_state.py:236,248` |
    235:     tid: str
  >>236:     owner: str | None
    237:     accord: float

### engine/autoload/game_state.py:245-246   [logs: R1_engine]
  CLAIM: | -- `efface` | NO | nearest: `Territory.owner = None` (`is_uncontrolled()`, game_state.py:245-246) represents a territory losing its controller, but no generic "cease to exist" primitive exists for any carrier. | -- | no | -- |
    244: 
  >>245:     def is_uncontrolled(self) -> bool:
    246:         return self.owner is None
    247: 

### engine/autoload/game_state.py:256   [logs: R1_engine]
  CLAIM: | `World` | `autoload/game_state.py:256` | factions, territories, clocks, season, arc, winner, battle_count, scenes_resolved, rng, **14 schema-migration registries** (practitioners, insurgencies, uncontrolled_streaks, npcs, npc_counter, treaties, convictions, beliefs, knots, knot_id_counter, territory_infrastructure, npc_drift_state, threadcut_beings, comovement_deck) + **settlements** | `create_w
    255: @dataclass
  >>256: class World:
    257:     factions: dict[str, Faction] = field(default_factory=dict)

### engine/autoload/game_state.py:256-301   [logs: R1_engine]
  CLAIM: | **World** (resolve's second arg) | **YES, but shaped very differently** | `World` dataclass | `autoload/game_state.py:256-301` | partial | `engine/`'s `World` IS a single global mutable aggregate (factions/territories/clocks + 14 heterogeneous "Any-typed" registries, game_state.py:266-301 — explicitly documented as an accreted schema-migration pile, not a designed carrier index). If the design's
    255: @dataclass
  >>256: class World:
    257:     factions: dict[str, Faction] = field(default_factory=dict)
    258:     territories: dict[str, Territory] = field(default_factory=dict)
    259:     clocks: dict[str, float] = field(default_factory=dict)
    260:     season: int = 0
    261:     arc: int = 0
    262:     winner: str | None = None
    263:     battle_count: int = 0

### engine/autoload/game_state.py:265   [logs: R1_engine]
  CLAIM: - **Single seeded stream**: `World.rng: random.Random` (`game_state.py:265`), constructed as `random.Random(seed)` in `create_world` (game_state.py:306) and stored on `World` (339). This is **the** campaign RNG; every subsystem receives it (or a value derived from it) rather than drawing from the global `random` module directly — with one documented, deliberate exception (`scene_dispatch.py:299`, 
    264:     scenes_resolved: int = 0        # F7 telemetry (ED-IN-0021): personal-scale scenes actually resolved (not deferred) — additive counter, no behaviour effect
  >>265:     rng: random.Random = field(default_factory=random.Random)
    266:     # ─── Schema migration 2026-05-19 ──────────────────────────────────────

### engine/autoload/game_state.py:266-301   [logs: R1_engine]
  CLAIM: | **World** (resolve's second arg) | **YES, but shaped very differently** | `World` dataclass | `autoload/game_state.py:256-301` | partial | `engine/`'s `World` IS a single global mutable aggregate (factions/territories/clocks + 14 heterogeneous "Any-typed" registries, game_state.py:266-301 — explicitly documented as an accreted schema-migration pile, not a designed carrier index). If the design's
    265:     rng: random.Random = field(default_factory=random.Random)
  >>266:     # ─── Schema migration 2026-05-19 ──────────────────────────────────────
    267:     # Registries for Tier 0 modules that previously held module-level state.
    268:     # Values use Any-typing because the owning module defines its own
    269:     # dataclass (CoherenceState, InsurgencyRecord, NPC, TreatyRecord) and
    270:     # bidirectional typing would create import cycles. Consumer modules
    271:     # check type at runtime.
    272:     # [canonical: proposals/stub_infill_plan.md Amendment 2026-05-19
    273:     #  "schema-migration commit that adds the missing registries"]

### engine/autoload/game_state.py:274-301   [logs: R1_engine]
  CLAIM: 1. **No individual-actor representation at all.** `World` holds only aggregate Faction/Territory stats plus loosely-typed dict registries (`npcs`, `convictions`, `knots`, etc.) whose *element types* live entirely in `systems/`, not `engine/`. A `Person` carrier (and cohort-as-weighted-Person) requires a net-new first-class type at the `engine/autoload` or `engine/substrate` tier -- nothing to buil
    273:     #  "schema-migration commit that adds the missing registries"]
  >>274:     practitioners: dict = field(default_factory=dict)            # actor_id → CoherenceState (from systems/threadwork/sim/coherence)
    275:     insurgencies: dict = field(default_factory=dict)             # insurgency_id → InsurgencyRecord (from systems/world/sim/insurgency_pipeline)
    276:     uncontrolled_streaks: dict = field(default_factory=dict)     # frozenset[tid] → consecutive seasons (from systems/world/sim/insurgency_pipeline)
    277:     npcs: dict = field(default_factory=dict)                     # territory_id → list[NPC] (from systems/world/sim/npe)
    278:     npc_counter: int = 0                                          # incrementing id source for NPC generation
    279:     treaties: dict = field(default_factory=dict)                 # frozenset[parties] → TreatyRecord (from systems/factions/sim/treaty)
    280:     # ─── Schema migration #2 — 2026-05-19 ─────────────────────────────────
    281:     # Tier 1/2 registries. Same Any-typing rationale + _store(world) router

### engine/autoload/game_state.py:277   [logs: PR337,R1_engine]
  CLAIM: | **Person** (incl. cohort = Person at weight>1) | **NO** | -- | -- | no | Nothing in `engine/` models an individual human actor with weight/identity. `Faction` is an aggregate (5-7 float stats), not a Person. `World.npcs: dict[territory_id -> list[NPC]]` (game_state.py:277) is the closest *slot*, but `NPC` itself lives in `systems/world/sim/npe.py` (outside this lane) and is late-imported by stri
  CLAIM: | A337-145 | class/type | `world.npcs` — territory-keyed store, empty in every seeded campaign | `_v1_part2.md` §"People" | Serialised, round-trips, but empty. | BUILT (empty) | yes, `game_state.py:277` |
    276:     uncontrolled_streaks: dict = field(default_factory=dict)     # frozenset[tid] → consecutive seasons (from systems/world/sim/insurgency_pipeline)
  >>277:     npcs: dict = field(default_factory=dict)                     # territory_id → list[NPC] (from systems/world/sim/npe)
    278:     npc_counter: int = 0                                          # incrementing id source for NPC generation

### engine/autoload/game_state.py:301   [logs: R1_engine]
  CLAIM: | **Site** | **NO** as a carrier, **YES** as an aggregate location | `Territory` (16 instances), `Settlement` (defined in `systems/settlements/sim/registry.py`, referenced via `World.settlements` registry, game_state.py:301) | `autoload/game_state.py:234` (`Territory`) | partial | `Territory` has 9 scalar fields (owner, accord, pt, garrison, prosperity, fort_level, templar, uncontrolled_since) — a
    300:     # [canonical: systems/settlements/goldenfurt_slice/sim_build_spec.md §1]
  >>301:     settlements: dict = field(default_factory=dict)             # sid → Settlement (from systems/settlements/sim/registry)
    302: 

### engine/autoload/game_state.py:304-350   [logs: PR337]
  CLAIM: | A337-142 | process | `create_world` — two settlement-scale families from two sources, cross-validated by nothing | `_v1.md` §3 processes | Builds factions/territories/settlements in one call. | BUILT | yes, `game_state.py:304-350` |
    303: 
  >>304: def create_world(seed: int | None = None) -> World:
    305:     """Build canonical starting world state."""
    306:     rng = random.Random(seed)
    307:     factions = {}
    308:     for name, stats in STARTING_STATS.items():
    309:         f = Faction(name=name, **stats)
    310:         # [hash-seed fix 2026-05-20] set-comp → list-comp; ordered by STARTING_OWNER dict-insertion order (deterministic)
    311:         f.territories = [tid for tid, o in STARTING_OWNER.items() if o == name]

### engine/autoload/game_state.py:306   [logs: R1_engine]
  CLAIM: - **Single seeded stream**: `World.rng: random.Random` (`game_state.py:265`), constructed as `random.Random(seed)` in `create_world` (game_state.py:306) and stored on `World` (339). This is **the** campaign RNG; every subsystem receives it (or a value derived from it) rather than drawing from the global `random` module directly — with one documented, deliberate exception (`scene_dispatch.py:299`, 
    305:     """Build canonical starting world state."""
  >>306:     rng = random.Random(seed)
    307:     factions = {}

### engine/autoload/game_state.py:309   [logs: R1_engine]
  CLAIM: | `Faction` | `autoload/game_state.py:109` | name, parliamentary, L, Sta, W, I, Mil, intel, territories, senator_inward_used, consul_used, peaceful, standing, excommunicated, council_used_this_arc, parl_transfer_used_this_arc | `create_world` (game_state.py:309), `restore_world` (441) | `.adjust()` (153), `.reset_seasonal()` (198), `.reset_arc()` (202); ~31 non-test call sites across `systems/fact
    308:     for name, stats in STARTING_STATS.items():
  >>309:         f = Faction(name=name, **stats)
    310:         # [hash-seed fix 2026-05-20] set-comp → list-comp; ordered by STARTING_OWNER dict-insertion order (deterministic)

### engine/autoload/game_state.py:338   [logs: PR338,PR340,PR341]
  CLAIM: | A341-087 | module/subsystem | `game_state.py` | `game_state.py:338,368,439` | Initialises/serialises/restores `world.clocks` dict `{'CI','MS','IP','PI','Strain','Turmoil'}` | claimed by synthesis §2.1; not independently re-opened | UNVERIFIED (not directly opened in this pass) |
  CLAIM: | 12 | `victory.py:73` reads `world.clocks.get('Turmoil', 0.0)`; `game_state.py:338` initializes Turmoil to 0.0; nothing else writes it (13 §4, P0-7) | **VERIFIED-TRUE** — both lines confirmed; grep for all non-test `Turmoil` occurrences finds exactly these two plus one unrelated pattern-matcher (`skills/valoria-vector-audit/scripts/vector_audit.py:217`, not a writer) |
  CLAIM: opened and read (not just counted): `game_state.py:338,368,439`; `victory.py:73`; `ms_track.py:69,90`;
  CLAIM: | A338-104 | primitive | `world.clocks` — 2/6 written | `game_state.py:338` | CI, MS driven every season; IP, PI, Strain, Turmoil initialised at world-gen and never written again | BUILT (partial) | 4/6 inert |
    337:         # [canonical: clock_registry_v30.md — IP start 20]
  >>338:         clocks={'CI': 30.0, 'MS': 60.0, 'IP': 20.0, 'PI': 0.0, 'Strain': 0.0, 'Turmoil': 0.0},
    339:         rng=rng,

### engine/autoload/game_state.py:469   [logs: R1_engine]
  CLAIM: | **Person** (incl. cohort = Person at weight>1) | **NO** | -- | -- | no | Nothing in `engine/` models an individual human actor with weight/identity. `Faction` is an aggregate (5-7 float stats), not a Person. `World.npcs: dict[territory_id -> list[NPC]]` (game_state.py:277) is the closest *slot*, but `NPC` itself lives in `systems/world/sim/npe.py` (outside this lane) and is late-imported by stri
    468:     if 'npcs' in snapshot:
  >>469:         NPC = composition.require('snapshot_state.npcs')
    470:         w.npcs = {tid: [NPC.from_dict(n) for n in npc_list]

### engine/autoload/game_state.py:474   [logs: PR337]
  CLAIM: | A337-242 | claim-about-repo | Proposal 3 "The Disposal" — cut-list checked against `module_contracts.yaml` as well as grep | `_v1_part4.md` §Proposal 3 | `treaty.py`/`beliefs.py`'s dead SURFACES only (not the modules — `TreatyRecord`/`Belief` are KEEP, reached by string via `composition.require` in `restore_world`, `game_state.py:474,484`). Six other modules confirmed dead by both grep AND regis
  CLAIM: | A337-036 | ownership | Two instrument rules for future sessions | `registers/handoffs/HANDOFF_IN.md:3743-3763` (added, PR #337) | (1) Reachability in this tree is not an import graph — deps resolved by string via `references/`, e.g. `treaty.py`/`beliefs.py` reached via `composition.require` in `restore_world` (`game_state.py:474,484`). (2) Agreement across surfaces is evidence only if independen
    473:     if 'treaties' in snapshot:
  >>474:         TreatyRecord = composition.require('snapshot_state.treaties')
    475:         w.treaties = {frozenset(entry['parties_key']): TreatyRecord.from_dict(entry['record'])

### engine/autoload/npc_ai.py:12   [logs: R1_engine]
  CLAIM: | **Act** (`choose : (Person, View, Sensation) -> Act`) | **NO** | closest: `Action` type referenced only in unimplemented docstrings (`npc_ai.py` entry-point comments: "select_action(actor_id, world) -> Action") — `Action` is never actually defined anywhere in `engine/`. | `autoload/npc_ai.py:12` (docstring only) | no | `npc_ai.select_action`/`evaluate_priority_stack` are **both stub-wired** (npc
    11: 
  >>12: Entry points:
    13:   - select_action(actor_id: str, world: GameState) -> Action

### engine/autoload/npc_ai.py:31-46   [logs: R1_engine]
  CLAIM: | **Act** (`choose : (Person, View, Sensation) -> Act`) | **NO** | closest: `Action` type referenced only in unimplemented docstrings (`npc_ai.py` entry-point comments: "select_action(actor_id, world) -> Action") — `Action` is never actually defined anywhere in `engine/`. | `autoload/npc_ai.py:12` (docstring only) | no | `npc_ai.select_action`/`evaluate_priority_stack` are **both stub-wired** (npc
    30: # Jordan diagnosis 2026-05-17 — audit pending before content authoring.
  >>31: 
    32: 
    33: def select_action(actor_id: str, world: GameState):
    34:     return stubwire.stub_resolve(
    35:         'engine.autoload.npc_ai',
    36:         'select_action(actor_id: str, world: GameState) -> Action',
    37:         reason='Pass 2l armature stub, implementation pending against canonical source '
    38:                '(designs/architecture/complete_systems_reference.md Part 1); OI-17, ED-IN-0091 plan §2.2')

### engine/autoload/npc_ai.py:31   [logs: R1_engine]
  CLAIM: | **choose** | **NO** | stub only (`npc_ai.select_action`) | `autoload/npc_ai.py:31` | no | see Act row |
    30: # Jordan diagnosis 2026-05-17 — audit pending before content authoring.
  >>31: 
    32: 

### engine/autoload/scene_slate.py:25   [logs: R1_engine]
  CLAIM: | `SceneSlot` | `autoload/scene_slate.py:25` | scene_type, context, priority | `queue_scene()` | none |
    24: @dataclass
  >>25: class SceneSlot:
    26:     scene_type: str   # e.g. "combat", "contest", "fieldwork", "companion", "thread"

### engine/autoload/scene_slate.py:34   [logs: PR338]
  CLAIM: | A338-029 | module/subsystem | Scene Slate / `queue_scene` | `engine/autoload/scene_slate.py:34` | The queue of pending scenes with priorities | BUILT | yes |
    33: 
  >>34: def queue_scene(scene_type: str, context: dict | None = None,
    35:                 priority: int = 0) -> SceneSlot:

### engine/autoload/season_manager.py:15   [logs: R1_engine]
  CLAIM: - `season_manager.py:15` — `SEASONS_PER_ARC=4`.
    14: Entry points:
  >>15:   - advance_season(world) -> SeasonResult
    16:   - check_arc_boundary(season: int) -> bool

### engine/autoload/season_manager.py:22   [logs: PR340]
  CLAIM: **When it ticks:** at the accounting boundary, gated purely on current state — no cross-season carry. `season` itself is read (not invented): `engine/autoload/season_manager.py:22` declares `SEASONS_PER_ARC=4`, and `world.season % 4` gives a real quarter index the crop-failure row's growing-season gate reads.
    21: 
  >>22: 
    23: SEASONS_PER_ARC = 4

### engine/autoload/season_manager.py:23   [logs: R1_engine]
  CLAIM: | `SeasonResult` | `autoload/season_manager.py:23` | season, arc, new_arc | `advance_season()` | none |
    22: 
  >>23: SEASONS_PER_ARC = 4
    24: 

### engine/autoload/season_manager.py:29-38   [logs: R1_engine]
  CLAIM: a. **SEASON_TICK** — `advance_season(world)` (season_manager.py:29-38): `world.season += 1`; if `season % 4 == 1` it's a new arc (`world.arc += 1`, `reset_arc()` on every faction); `reset_seasonal()` on every faction always.
  CLAIM: | **CALENDAR** (loop step 1) | **partial** | `season_manager.advance_season()` — arc/season counters and per-arc/per-season faction flag resets | `autoload/season_manager.py:29-38` | partial | Only resets faction flags; does not evaluate "what fires vs merely becomes reachable" per the design's step distinction — no convening-conditions concept exists. |
    28:     season: int
  >>29:     arc: int
    30:     new_arc: bool
    31: 
    32: 
    33: def advance_season(world) -> SeasonResult:
    34:     """Advance the world by one season. Returns season metadata."""
    35:     world.season += 1
    36:     new_arc = (world.season % SEASONS_PER_ARC == 1)

### engine/autoload/sigma_leverage.py:81-99   [logs: R1_engine]
  CLAIM: - `sigma_leverage.py:81-99` — `PER_DIE`, `TN_STANDARD=7`, `LEVEL_SIGMA` (4-entry dict), `M_MAX=1.5`, `SIGMA_N_COEFF=0.8`, `OB_MIN=1`.
    80: 
  >>81: # TN 7 MEANS "A FACE OF 7 OR HIGHER SUCCEEDS" — equivalently, "roll above 6". Both readings are
    82: # the same rule and both are in circulation; RULED 2026-08-15 (Jordan: "TN7, roll of 7 or higher is
    83: # success") so the ambiguity stops here rather than being re-derived. The die rule it indexes:
    84: # face 1 = -1 success, 2-6 = 0, 7-9 = +1, 10 = +2.
    85: # ⚠ TN IS 7. ALWAYS. THERE IS NO OTHER TN.
    86: # [Jordan, 2026-08-25, verbatim: "TN7 always. Never change TN anywhere ever."]
    87: # This SUPERSEDES the 2026-08-15 situational scale that stood here — "Controlled 6 / Standard 7 /
    88: # Desperate 8, selected by SITUATION" — which is now dead canon. The earlier half of that same

### engine/autoload/sigma_leverage.py:108   [logs: PR340]
  CLAIM: | 4 | `OB_MIN = 1`, pinned, imported not restated (01 §6, 05 §4.1) | **VERIFIED-TRUE** — `engine/autoload/sigma_leverage.py:108` |
    107: # Contest engine surface constants (audit/2026-06-03-contest-groundup/engine.py)
  >>108: OB_MIN = 1              # [canonical: params/core.md §Obstacle Scale]
    109: 

### engine/autoload/sigma_leverage.py:284   [logs: PR337,PR338]
  CLAIM: | A338-063 | claim-about-repo | Pool rounded before continuous engine | `engine/autoload/sigma_leverage.py:284` (ED-IN-0187) | `effective_pool = max(1, int(round(pool)))` applied BEFORE the fractional-capable continuous engine — defeats fractional pools at point of use | measured (defect), ledger-recorded | BUILT (defect) |
  CLAIM: | A337-197 | formula | Pool rounded before continuous engine — defeats fractional pools | `_v1_part2.md` §"Resolution Kernel" | `effective_pool=max(1,int(round(pool)))` applied before the fractional-capable engine sees it. | BUILT (defect) | yes, `sigma_leverage.py:284` (ED-IN-0187) |
    283:     """Continuous engine sample, delegating to dice_engine.continuous_engine_sample.
  >>284: 
    285:     Canonical for Godot; statistically equivalent to the discrete engine.

### engine/autoload/victory.py:23-26   [logs: R1_engine]
  CLAIM: - `victory.py:23-26` — `VICTORY_THRESHOLD=15`, `ACCORD_MIN=2.0`, `PS_MAX=6.0`, `SUSTAIN_SEASONS=2`.
    22: from __future__ import annotations
  >>23: 
    24: from dataclasses import dataclass, field
    25: 
    26: 
    27: VICTORY_THRESHOLD = 15   # all 15 of 15 territories (Jordan 2026-06-19, J-22)

### engine/autoload/victory.py:26   [logs: R1_engine]
  CLAIM: | `VictoryResult` | `autoload/victory.py:26` | faction_id, won, held, qualifies_this_season, consecutive_qualifying, reason | `check_peninsular_sovereignty()` | none |
    25: 
  >>26: 
    27: VICTORY_THRESHOLD = 15   # all 15 of 15 territories (Jordan 2026-06-19, J-22)

### engine/autoload/victory.py:52-80   [logs: PR337,PR338]
  CLAIM: | A338-105 | formula | Peninsular Sovereignty (GD-1, sole victory condition) | `engine/autoload/victory.py:52-80` | `held≥15 ∧ all(accord≥2) ∧ Turmoil≤6`, sustained 2 Accountings; Turmoil clause structurally always-true (nothing writes Turmoil) | BUILT (with inert clause) | yes, defective |
  CLAIM: | A337-134 | formula | Peninsular Sovereignty (GD-1) — the sole victory condition, one clause structurally always-true | `_v1.md` §3 | `held≥15 ∧ all(accord≥2) ∧ Turmoil≤6`, sustained 2 Accountings; Turmoil clause always-true because Turmoil never writes. | BUILT (defect) | yes, `victory.py:52-80` |
    51: 
  >>52: def check_peninsular_sovereignty(faction_id: str, world) -> VictoryResult:
    53:     """Check if faction meets GD-1 peninsular sovereignty victory.
    54: 
    55:     Canon: all 15 territories held, Accord >= 2 in all, PS <= 6, sustained 2 seasons.
    56:     """
    57:     from engine.autoload.game_state import ALL_PLAYABLE_15
    58: 
    59:     faction = world.factions.get(faction_id)

### engine/autoload/victory.py:73   [logs: PR340,PR341]
  CLAIM: | A341-083 | module/subsystem | `victory.py` | `victory.py:73` | Reads `Turmoil` as political stability: `ps_ok = ps <= PS_MAX` | one of the registry's two `build: live` modules — but reads a dead clock (T8-2) | yes — live, but reads dead data |
  CLAIM: | 12 | `victory.py:73` reads `world.clocks.get('Turmoil', 0.0)`; `game_state.py:338` initializes Turmoil to 0.0; nothing else writes it (13 §4, P0-7) | **VERIFIED-TRUE** — both lines confirmed; grep for all non-test `Turmoil` occurrences finds exactly these two plus one unrelated pattern-matcher (`skills/valoria-vector-audit/scripts/vector_audit.py:217`, not a writer) |
  CLAIM: opened and read (not just counted): `game_state.py:338,368,439`; `victory.py:73`; `ms_track.py:69,90`;
    72:     # Political Stability is a world clock
  >>73:     ps = world.clocks.get('Turmoil', 0.0)  # PS mapped to Turmoil clock
    74:     ps_ok = ps <= PS_MAX

### engine/cross_scale/articulation.py:35   [logs: R1_engine]
  CLAIM: | **Claim** (`witness : (Person, Event) -> Claim[]`, per-person, never a collection) | **NO** | -- | -- | no | `Key.visibility` (public/semi_public_observers/private_observers, keys.py:100-111) encodes *who may see* a Key, but nothing computes a per-witness `Claim` object — no function anywhere takes `(Person, Event)` and returns anything. `articulation.py`'s `render_protagonist_lens`/`generate_ch
    34: 
  >>35: def render_protagonist_lens(actor_id: str, world: GameState):
    36:     return stubwire.stub_resolve(

### engine/cross_scale/articulation.py:116-169   [logs: PR337,PR338]
  CLAIM: | A337-185 | mechanic | `articulation.subscribe_all` — 13 typed no-op callbacks, wired independently of emission | `_v1_part2.md` §"Cross-Scale Plumbing" | 11/13 subscribed types never emitted; of 5 types emitted, only 2 subscribed; `scene.contest_resolved` (state-writing) subscribed by nobody. | BUILT (no-op) | yes, `articulation.py:116-169` |
  CLAIM: | A338-046 | mechanic | `articulation.subscribe_all` | `articulation.py:116-169` | Subscribes 13 type_ids; all 13 callbacks typed no-ops; 11/13 never emitted by any producer; of 5 emitted types only 2 subscribed, and `scene.contest_resolved` (state-writing) subscribed by nobody | BUILT (no-op) | wired, inert |
    115: # it fires zero times today).
  >>116: _TRIGGER_TYPE_IDS = (
    117:     'state.scar_acquired',
    118:     'state.coup_attempted',
    119:     'state.succession',
    120:     'mechanical.mission_shift',
    121:     'da.covert_betrayal',
    122:     'meta.knot_formed',
    123:     'meta.knot_ruptured',

### engine/cross_scale/combat_bridge.py:102-111   [logs: PR341]
  CLAIM: | 2 | `combat_bridge.py:103-111` derives one field, `history = max(1, round(f.Mil))`, and labels the Combatant with the faction id | `01_findings_T1_T3.md:36`, `00_INDEX.md:84` | **VERIFIED-TRUE** — read `_combatant_from_faction_mil` at `combat_bridge.py:102-111`; exact match |
    101: 
  >>102: 
    103: def _combatant_from_faction_mil(fid, world):
    104:     """Derive ONE personal-combat side from `fid`'s own aggregate `Mil` stat (see module
    105:     docstring). Returns `None` if `fid` does not name a live faction on `world`."""
    106:     f = getattr(world, "factions", {}).get(fid)
    107:     if f is None:
    108:         return None
    109:     history = max(1, round(f.Mil))

### engine/cross_scale/combat_bridge.py:103-128   [logs: PR337,PR338]
  CLAIM: | A337-183 | mechanic | `combat_bridge.derive_parties`/`resolve` — dead twice over | `_v1_part2.md` §"Cross-Scale Plumbing" | `history=max(1,round(f.Mil))`; returns `None` on gap rather than fabricating an actor; gated `DISPATCH_COMBAT_BRIDGE` default OFF AND no `queue_scene("combat",...)` call site exists even if ON. | INERT | VERIFIED-TRUE, `combat_bridge.py:103-128`; flag `mc_v18.py:79-89` |
  CLAIM: | A338-043 | mechanic | `combat_bridge.derive_parties`/`resolve` | `combat_bridge.py:103-128`; flag `mc_v18.py:79-89` | Faction→Personal; builds combat actor from `history=max(1,round(f.Mil))`; correctly returns None on gap rather than fabricating; gated behind `DISPATCH_COMBAT_BRIDGE` default OFF, unreachable even ON (no `queue_scene("combat",...)` call site) | INERT (dead twice over) | no |
    102: 
  >>103: def _combatant_from_faction_mil(fid, world):
    104:     """Derive ONE personal-combat side from `fid`'s own aggregate `Mil` stat (see module
    105:     docstring). Returns `None` if `fid` does not name a live faction on `world`."""
    106:     f = getattr(world, "factions", {}).get(fid)
    107:     if f is None:
    108:         return None
    109:     history = max(1, round(f.Mil))
    110:     _combatant_mod, _ = _load_engine()

### engine/cross_scale/combat_bridge.py:103-111   [logs: PR341]
  CLAIM: `combat_bridge.py:103-111` were each read in full to confirm the shared reasoning.
  CLAIM: | 2 | `combat_bridge.py:103-111` derives one field, `history = max(1, round(f.Mil))`, and labels the Combatant with the faction id | `01_findings_T1_T3.md:36`, `00_INDEX.md:84` | **VERIFIED-TRUE** — read `_combatant_from_faction_mil` at `combat_bridge.py:102-111`; exact match |
  CLAIM: | A341-045 | function/signature | `_combatant_from_faction_mil(fid, world)` | `combat_bridge.py:103-111` | Derives ONE personal-combat side from faction's aggregate `Mil`; `history = max(1, round(f.Mil))` | VERIFIED TRUE | yes — live code |
    102: 
  >>103: def _combatant_from_faction_mil(fid, world):
    104:     """Derive ONE personal-combat side from `fid`'s own aggregate `Mil` stat (see module
    105:     docstring). Returns `None` if `fid` does not name a live faction on `world`."""
    106:     f = getattr(world, "factions", {}).get(fid)
    107:     if f is None:
    108:         return None
    109:     history = max(1, round(f.Mil))
    110:     _combatant_mod, _ = _load_engine()

### engine/cross_scale/combat_bridge.py:112-128   [logs: PR337]
  CLAIM: | `combat_bridge.derive_parties` returns `None` on a derivation gap and `DISPATCH_COMBAT_BRIDGE` defaults OFF | A337-032/183 | **VERIFIED-TRUE** — `engine/cross_scale/combat_bridge.py:112-128` (explicit `if a is None or b is None: return None`), `engine/mc_v18.py:79-89` |
    111:     return _combatant_mod.Combatant(label=fid, history=history)
  >>112: 
    113: 
    114: def derive_parties(ctx, world):
    115:     """Derive the two `Combatant` sides for a queued `combat` scene from `ctx['factions'] =
    116:     (fid_a, fid_b)` — two DIFFERENT factions in conflict, each contributing its own Combatant via
    117:     `_combatant_from_faction_mil`. Returns `(Combatant, Combatant)`, or `None` on a
    118:     context-derivation gap (missing/short `ctx['factions']`, or either id not resolving to a live
    119:     faction) — the caller (scene_dispatch) is responsible for flagging that gap, never this

### engine/cross_scale/combat_bridge.py:115   [logs: PR341]
  CLAIM: | A341-046 | function/signature | `derive_parties(ctx, world)` | `combat_bridge.py:115+` | Derives two Combatant sides for a queued combat scene | VERIFIED (context) | yes — live code |
    114: def derive_parties(ctx, world):
  >>115:     """Derive the two `Combatant` sides for a queued `combat` scene from `ctx['factions'] =
    116:     (fid_a, fid_b)` — two DIFFERENT factions in conflict, each contributing its own Combatant via

### engine/cross_scale/combat_bridge.py:140   [logs: R1_engine]
  CLAIM: 8. **`World.rng` is a single shared stream; the design's per-Person `choose` would need to reason about whether per-actor determinism/replay matters, and nothing in the current substrate supports keying an RNG sub-stream per Person** (only per-*resolver-call*, via the `getrandbits(32)`-derive pattern in `combat_bridge.py:140`). If the design wants reproducible-per-actor decisions independent of dr
  CLAIM: - **Sub-stream derivation, not reseeding**: `combat_bridge.resolve()` (combat_bridge.py:140) does `fight_rng = random.Random(rng.getrandbits(32))` — derives an independent `random.Random` from the caller's stream via one `getrandbits` draw, so combat's internal draw count can't perturb the campaign stream's future draws.
    139:     _, _wrapper_mod = _load_engine()
  >>140:     fight_rng = _random_mod.Random(rng.getrandbits(32))
    141:     result = _wrapper_mod.fight(a, b, rng=fight_rng)

### engine/cross_scale/domain_echo.py:51   [logs: R1_engine]
  CLAIM: | `DomainEchoResult` / `AccordEchoResult` / `ThreadEchoResult` | `cross_scale/domain_echo.py:51/61/71` | stat-delta shapes | `compute_domain_echo`/`compute_accord_echo`/`compute_thread_echo` | none |
    50: @dataclass
  >>51: class DomainEchoResult:
    52:     fires: bool                 # False if Sufficient Scope (§7) not met

### engine/cross_scale/domain_echo.py:128   [logs: PR337,PR338]
  CLAIM: | A338-034 | mechanic | Accord Domain Echo | `scale_transitions_v30.md:208`; `domain_echo.py:128` + `echo_transport.py` | Personal scenes shift settlement Accord ±1/territory/Zoom-In; province Accord recalcs as `floor(mean(settlement Order))`; territorial transfer sets Accord=2 outright; does not stack with same-season faction Govern | BUILT (dormant) | code runs, never fires (no producer sets `ec
  CLAIM: | A337-180 | formula | Accord Domain Echo — BUILT but dormant | `_v1_part2.md` §"Cross-Scale Plumbing" | Personal scenes shift Accord ±1/territory/Zoom-In, targets the settlement; province Accord recalculates via `floor(mean(settlement Order))`. | BUILT (dormant) | wired, `domain_echo.py:128`+`echo_transport.py` |
    127: 
  >>128: def compute_accord_echo(scene_outcome: str, degree: str, world=None) -> AccordEchoResult:
    129:     """Compute §5.5 Accord Domain Echo for personal scenes producing Accord changes.

### engine/cross_scale/domain_echo.py:186   [logs: PR338]
  CLAIM: | A338-036 | mechanic | Thread Domain Echo | `domain_echo.py:186` | Fixed effects per event (Dissolution Stability−1, Mending Mandate+1, Gap Stability−1, unauthorized Lock Mandate−1), queued to Accounting | INERT | no callers |
    185: 
  >>186: def compute_thread_echo(thread_event: dict, world=None) -> ThreadEchoResult:
    187:     """Compute §5.6 Thread Domain Echo per ED-673."""

### engine/cross_scale/echo_transport.py:96-103   [logs: PR341]
  CLAIM: | A341-063 | param/constant | `ECHO_EMISSIONS_PER_TICK_MAX` | `echo_transport.py:96-103`, ED-IN-0026 | Tunable constant, explicitly "CALLER-SUPPLIED … NOT canonical mechanical constants" | live default ON (ECHO_TRANSPORT), Jordan 2026-07-08 | yes — live |
    95: 
  >>96: 
    97: # Operational scheduler caps — CALLER-SUPPLIED bounds, NOT canonical mechanical constants.
    98: # OF-CAP is an open fork; §5.7 ruled ACCEPT-default 2026-07-07 (ED-IN-0026): the caller
    99: # supplies caps, no fabricated canonical constant enters the repo. domain_echo emits exactly
    100: # one non-cascading Key per resolved scene, so depth 0 suffices; the per-tick ceiling is a
    101: # safety bound, caller-tunable via run_campaign params.
    102: DEFAULT_CASCADE_DEPTH_MAX = 0  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP caller cap, ED-IN-0026]
    103: DEFAULT_EMISSIONS_PER_TICK_MAX = 64  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP operational cap, ED-IN-0026]

### engine/cross_scale/echo_transport.py:96-97   [logs: R1_engine]
  CLAIM: - `echo_transport.py:96-97` — `DEFAULT_CASCADE_DEPTH_MAX=0`, `DEFAULT_EMISSIONS_PER_TICK_MAX=64`.
    95: 
  >>96: 
    97: # Operational scheduler caps — CALLER-SUPPLIED bounds, NOT canonical mechanical constants.
    98: # OF-CAP is an open fork; §5.7 ruled ACCEPT-default 2026-07-07 (ED-IN-0026): the caller

### engine/cross_scale/echo_transport.py:102   [logs: PR337,PR338,PR340]
  CLAIM: | 2 | `DEFAULT_CASCADE_DEPTH_MAX = 0`, self-labelled provisional (01 pt2 §9.3) | **VERIFIED-TRUE** — `engine/cross_scale/echo_transport.py:102` |
  CLAIM: | A338-027 | invariant/refusal | `TickScheduler.schedule_emission` (cascade path) is switched OFF | `keys.py:525`; `echo_transport.py:102` | `DEFAULT_CASCADE_DEPTH_MAX = 0` — would raise a termination breach if triggered; zero production callers | INERT (deliberately) | no |
  CLAIM: | A337-175 | invariant/refusal | `TickScheduler.schedule_emission` — cascade path, switched off | `_v1_part2.md` §"Cross-Scale Plumbing" | `DEFAULT_CASCADE_DEPTH_MAX=0` — would raise a termination breach if it ever ran. | INERT | VERIFIED-TRUE, `keys.py:525`, cap at `echo_transport.py:102` |
  CLAIM: | `TickScheduler.schedule_emission`'s cascade path is capped at `DEFAULT_CASCADE_DEPTH_MAX = 0` | A337-175 | **VERIFIED-TRUE** — `engine/cross_scale/echo_transport.py:102` |
    101: # safety bound, caller-tunable via run_campaign params.
  >>102: DEFAULT_CASCADE_DEPTH_MAX = 0  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP caller cap, ED-IN-0026]
    103: DEFAULT_EMISSIONS_PER_TICK_MAX = 64  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP operational cap, ED-IN-0026]

### engine/cross_scale/echo_transport.py:103   [logs: PR340]
  CLAIM: | 3 | `DEFAULT_EMISSIONS_PER_TICK_MAX = 64` (11 §3.3) | **VERIFIED-TRUE** — `engine/cross_scale/echo_transport.py:103` |
  CLAIM: | A340-180 | invariant/refusal | Rate bound proofs (§3.2/3.3) | 11 §3 | per-row: `cooldown≥1` required at load → max fires ≤⌈S/cooldown⌉ over S seasons; global: worst-case world.event_fired count ≤ place_node_count+faction_count, checked against real DEFAULT_EMISSIONS_PER_TICK_MAX=64 | n/a | PROPOSED, falsifiers named, checked against a real existing engine constant | VERIFIED-TRUE (constant confi
    102: DEFAULT_CASCADE_DEPTH_MAX = 0  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP caller cap, ED-IN-0026]
  >>103: DEFAULT_EMISSIONS_PER_TICK_MAX = 64  # [canonical: designs/architecture/key_echo_armature_v1.md §5.7 — OF-CAP operational cap, ED-IN-0026]
    104: 

### engine/cross_scale/echo_transport.py:145   [logs: PR338]
  CLAIM: | A338-035 | function/signature | `classify_scene_outcome(scene_type, degree, echo)` | `echo_transport.py:145` | Deliberately refuses to infer outcome category from `scene_type` alone; requires explicit `echo['scene_outcome']` | BUILT | yes, but caller never sets the required field |
    144: 
  >>145: def classify_scene_outcome(scene_type: str, degree: str, echo: dict) -> Optional[str]:
    146:     """§5.5 Accord Domain Echo outcome classification at the echo boundary (OI-03).

### engine/cross_scale/echo_transport.py:184   [logs: R1_engine]
  CLAIM: | `TickScheduler` | `substrate/keys.py:422` | log, caps, flags, subscriptions, `_queue`, `_pending_apply`, phase counters | `echo_transport.make_scheduler` (echo_transport.py:184) | `emit`/`schedule_emission`/`drain_tick`/`accounting_boundary`/`next_tick` |
    183: 
  >>184: def make_scheduler(cascade_depth_max: int = DEFAULT_CASCADE_DEPTH_MAX,
    185:                    emissions_per_tick_max: int = DEFAULT_EMISSIONS_PER_TICK_MAX) -> TickScheduler:

### engine/cross_scale/echo_transport.py:302-313   [logs: PR337]
  CLAIM: | A337-041 | mechanic | Accord echo — two rulings needed, not one field | `HANDOFF_IN.md:3783-3786` | `echo_transport.py:302-313` also requires `echo_ctx["target_settlement"]`, absent from contest branch's echo block (`scene_dispatch.py:344-345`, 4 keys); `scene_outcome` must be a validated §5.5 member the module refuses to infer. | BUILT, dormant (2 gates) | no (dormant) |
    301:     """
  >>302:     sid = echo_ctx.get("target_settlement")
    303:     settlements = getattr(world, "settlements", None) or {}
    304:     settlement = settlements.get(sid) if sid else None
    305:     detail = {"scene_outcome": scene_outcome, "target_settlement": sid,
    306:               "accord_delta": ar.accord_delta, "rs_delta": ar.rs_delta,
    307:               "notes": list(ar.notes),
    308:               "caused_by_key_id": caused_by_key_id}  # W3 OI-28 threading, see docstring above.
    309:     if settlement is None:

### engine/cross_scale/echo_transport.py:320-350   [logs: PR337,PR338]
  CLAIM: | A338-045 | mechanic | `scene.accord_echo → Settlement.order` | `echo_transport.py:320-350` | The ONE fully closed Key-driven state-write loop in the engine; never fires because nothing sets `echo['scene_outcome']` | BUILT (dormant) | VERIFIED-TRUE (scene_outcome grep confirms) |
  CLAIM: | A337-184 | mechanic | `scene.accord_echo`→`Settlement.order` — one fully closed Key-driven write loop, never fires | `_v1_part2.md` §"Cross-Scale Plumbing" | Scene resolves→Key emitted with honest `causes[]`→writes settlement order at accounting boundary — but no live producer sets `echo['scene_outcome']`, and the classifier refuses to infer one. | BUILT (dormant) | wired, `echo_transport.py:320
    319:     season = int(getattr(world, "season", 0))
  >>320:     key = Key(
    321:         id=f"scene.accord_echo.s{season}.n{seq}",
    322:         type="scene.accord_echo",
    323:         emitted_at=EmittedAt(season_index=season),
    324:         # causes[] population (OI-28 LIVE, W3): [caused_by_key_id] when the sibling §5.2 leg fired
    325:         # for this SAME scene (genuinely already in-log by construction -- see the docstring's "W3
    326:         # THREADING" section), [] when it did not (no genuine upstream Key exists to cite --
    327:         # never fabricated, per the honesty test in engine/tests/test_accord_echo.py).

### engine/cross_scale/echo_transport.py:320   [logs: PR338]
  CLAIM: | A338-047 | claim-about-repo | Live Key emission surface = 5/55 types, 4 call sites | `echo_transport.py:320,427`; `parliamentary_transfer.py:228`; `faction_action.py:392` | scene.accord_echo, scene.contest_resolved, scene.combat_resolved (state-writing); da.public_governance, scene.battle_concluded (log-only, both in swallowing try/except) | measured | VERIFIED (per synthesis, consistent w/ dire
    319:     season = int(getattr(world, "season", 0))
  >>320:     key = Key(
    321:         id=f"scene.accord_echo.s{season}.n{seq}",

### engine/cross_scale/echo_transport.py:328   [logs: PR337,PR338,PR341]
  CLAIM: | A337-171 | class/type | `Key.causes[]` — provenance chain, one honest production writer | `_v1_part2.md` §"Cross-Scale Plumbing" | Written at `echo_transport.py:328`, disciplined never to fabricate. | BUILT | yes |
  CLAIM: | A338-021 | key | `Key.causes[]` provenance chain | `echo_transport.py:328` | Sole honest production writer; discipline: cite upstream Key id only if one genuinely fired for the same scene | BUILT | yes (sparse) |
  CLAIM: (`faction_action.py:399`, `parliamentary_transfer.py:232`, `echo_transport.py:328`) was read directly
    327:         # never fabricated, per the honesty test in engine/tests/test_accord_echo.py).
  >>328:         causes=[caused_by_key_id] if caused_by_key_id else [],
    329:         scale_signature=["settlement"],

### engine/cross_scale/echo_transport.py:371-392   [logs: PR338]
  CLAIM: | A338-044 | mechanic | `emit_scene_echo` | `echo_transport.py:371-392` | Maps resolved scene → Key type (contest→scene.contest_resolved, combat→scene.combat_resolved) | BUILT | yes |
    370: 
  >>371: def emit_scene_echo(scene_type: str, result, ctx: dict, world) -> dict:
    372:     """Route one resolved scene through domain_echo → substrate Key (deferred faction apply),
    373:     and (OI-03) through §5.5 Accord Domain Echo (`compute_accord_echo`).
    374: 
    375:     Fires ONLY when `world.echo_scheduler` is attached AND `ctx` carries an explicit `echo`
    376:     block. Returns a `scene_outcomes` dict for zoom_out — `{}` when nothing fires, which is
    377:     the byte-exact fallback (identical to the historical `zoom_out({})`). Both legs now defer to
    378:     the accounting boundary via OF-7 (`sched.emit(key, apply=...)`): the §5.2 Domain Echo

### engine/cross_scale/handoff_rules.py:73   [logs: R1_engine]
  CLAIM: | `HandoffResult` | `cross_scale/handoff_rules.py:73` | (validity result shape) | `apply_handoff()` | none |
    72: @dataclass
  >>73: class HandoffResult:
    74:     """Descriptor of a scale transition's procedure + side effects."""

### engine/cross_scale/handoff_rules.py:92   [logs: PR337,PR338]
  CLAIM: | A337-181 | mechanic | `apply_handoff` — 8 §3 handoffs, exactly 1 pair reachable | `_v1_part2.md` §"Cross-Scale Plumbing" mechanics | Personal→Thread/Faction/Scene, Scene→Faction, Thread→Faction/Mass, Mass→Personal, Scene→Mass — only (Scene,Faction) reachable via a 2-entry dispatch dict whose entries both map to it. | BUILT (1/8) | yes, `handoff_rules.py:92`; dispatch at `scene_dispatch.py:158-16
  CLAIM: | A338-040 | mechanic | `apply_handoff` — the eight §3 cross-scale handoffs | `handoff_rules.py:92`; dispatch dict `scene_dispatch.py:158-161` | Personal→Thread/Faction/Scene, Scene→Faction, Thread→Faction/Mass, Mass→Personal, Scene→Mass. Only (Scene,Faction) is production-reachable via a 2-entry dispatch dict both mapping to the same pair | BUILT (1/8 reachable) | 1/8 reachable |
    91: 
  >>92: def apply_handoff(from_scale: str, to_scale: str, payload: dict, world=None) -> HandoffResult:
    93:     """Look up and return the §3 handoff rule for the given scale transition.

### engine/cross_scale/parliamentary_bridge.py:88-107   [logs: PR337,PR338]
  CLAIM: | A337-104 | process | Parliamentary bridge — runs every season, derives who never what | `_v1.md` §3 | Lowest-Stability faction proposes on Projection, highest-Mandate defends on Memory; `motion_id=f"parl_s{season}"`, identical contentless motion every season. | BUILT | yes, `parliamentary_bridge.py:88-107` |
  CLAIM: | A338-142 | function/signature | `_derive_vote` — parliamentary bridge | `engine/cross_scale/parliamentary_bridge.py:88-107`; `mc_v18.py:152-158` | Runs every season by default: lowest-Stability eligible faction proposes, highest-Mandate defends, `motion_id=f"parl_s{season}"` — derives WHO never WHAT; identical contentless motion every season | BUILT | yes |
    87: _SIDE_B_GENRE = "Memory"
  >>88: 
    89: 
    90: def _derive_vote(world):
    91:     """Derive a two-pole §10 motion from aggregate faction state. Returns (motion, declarations,
    92:     proposer_name, establishment_name) or None when fewer than two eligible factions exist."""
    93:     eligible = [name for name, f in world.factions.items()
    94:                 if getattr(f, "parliamentary", False) and getattr(f, "territories", None)]
    95:     if len(eligible) < 2:

### engine/cross_scale/scene_dispatch.py:76-107   [logs: PR337,PR338,PR340]
  CLAIM: | A338-041 | mechanic | `evaluate_triggers`→`queue_triggered_scenes` | `scene_dispatch.py:76-107` | World→scene bridge; 8 mandatory Priority-0 triggers specified; only Stability Crisis (`Faction.Sta<=2`) evaluable | BUILT (1/8) | 1/8 evaluable |
  CLAIM: | 8 | `evaluate_triggers` only ever fires `scene_type:"contest"`; the one `queue_scene` call site only ever passes that (12 §3.1) | **VERIFIED-TRUE** — `engine/cross_scale/scene_dispatch.py:76-107` |
  CLAIM: | A337-182 | mechanic | `evaluate_triggers`→`queue_triggered_scenes` — 1/8 mandatory triggers evaluable | `_v1_part2.md` §"Cross-Scale Plumbing" | Only Stability Crisis (`Faction.Sta<=2`) evaluates; other 7 returned `deferred`, not faked. | BUILT (1/8) | yes, `scene_dispatch.py:76-107` |
    75: # ─────────────────────────────────────────────────────────────────────────
  >>76: def evaluate_triggers(world):
    77:     """Return (fired, deferred). `fired` = list of scene specs; `deferred` =
    78:     list of canonical trigger names whose conditions are not evaluable against
    79:     the current aggregate world schema (flagged, not faked)."""
    80:     fired = []
    81:     # Stability Crisis (§4.3.2): Faction.Sta <= 2 -> emergency council (contest)
    82:     for fid, f in getattr(world, "factions", {}).items():
    83:         sta = getattr(f, "Sta", None)

### engine/cross_scale/scene_dispatch.py:120-139   [logs: PR337]
  CLAIM: | A337-090 | process | Emergency Council — one faction arguing with itself | `_v1.md` §3 | `side_a=max(1,round(L))`, `side_b=max(1,round(7−Sta))`, both derived from same faction's own aggregates, both run identical default policy. | BUILT | yes — VERIFIED consistent, `scene_dispatch.py:120-139` |
    119: 
  >>120: 
    121: def _emergency_council_parties(fid, world):
    122:     """Derive the two internal sides of a faction's Emergency Council — fired by the Stability
    123:     Crisis trigger (scale_transitions_v30.md:137: "Emergency faction council: social contest ...
    124:     revealing the source of the crisis"). No player-character schema exists at this (aggregate,
    125:     Monte-Carlo) scale — CLAUDE.md §5's no-fabrication rule and this module's own DELIBERATE
    126:     BOUNDARIES note both forbid inventing a personal actor. Both sides are therefore derived
    127:     from the SAME faction's own already-cited aggregate stats, not a new invented attribute:

### engine/cross_scale/scene_dispatch.py:121-139   [logs: PR338]
  CLAIM: | A338-042 | mechanic | `_emergency_council_parties` | `scene_dispatch.py:121-139` | The one crossing that actually executes; derives BOTH sides from the SAME faction's own aggregates (side A = round(L), side B = round(7−Sta)) — a faction arguing with itself | BUILT | yes (degenerate) |
    120: 
  >>121: def _emergency_council_parties(fid, world):
    122:     """Derive the two internal sides of a faction's Emergency Council — fired by the Stability
    123:     Crisis trigger (scale_transitions_v30.md:137: "Emergency faction council: social contest ...
    124:     revealing the source of the crisis"). No player-character schema exists at this (aggregate,
    125:     Monte-Carlo) scale — CLAUDE.md §5's no-fabrication rule and this module's own DELIBERATE
    126:     BOUNDARIES note both forbid inventing a personal actor. Both sides are therefore derived
    127:     from the SAME faction's own already-cited aggregate stats, not a new invented attribute:
    128:       side_a = the sitting leadership's case to stay the course — faculty = round(Faction.L),

### engine/cross_scale/scene_dispatch.py:158-161   [logs: PR337,PR338]
  CLAIM: | A337-181 | mechanic | `apply_handoff` — 8 §3 handoffs, exactly 1 pair reachable | `_v1_part2.md` §"Cross-Scale Plumbing" mechanics | Personal→Thread/Faction/Scene, Scene→Faction, Thread→Faction/Mass, Mass→Personal, Scene→Mass — only (Scene,Faction) reachable via a 2-entry dispatch dict whose entries both map to it. | BUILT (1/8) | yes, `handoff_rules.py:92`; dispatch at `scene_dispatch.py:158-16
  CLAIM: | A338-040 | mechanic | `apply_handoff` — the eight §3 cross-scale handoffs | `handoff_rules.py:92`; dispatch dict `scene_dispatch.py:158-161` | Personal→Thread/Faction/Scene, Scene→Faction, Thread→Faction/Mass, Mass→Personal, Scene→Mass. Only (Scene,Faction) is production-reachable via a 2-entry dispatch dict both mapping to the same pair | BUILT (1/8 reachable) | 1/8 reachable |
    157: # SCALE_SCENE is illustrative, not exhaustive, per that citation.
  >>158: _HANDOFF_SCALE_PAIR_BY_SCENE_TYPE = {
    159:     "combat": (handoff_rules.SCALE_SCENE, handoff_rules.SCALE_FACTION),
    160:     "contest": (handoff_rules.SCALE_SCENE, handoff_rules.SCALE_FACTION),
    161: }
    162: 

### engine/cross_scale/scene_dispatch.py:217-395   [logs: R1_engine]
  CLAIM: | **RESOLVE** (loop step 4) | **YES, loosely** | `scene_dispatch._resolve_slot()` + `combat_bridge.resolve()` + `parliamentary_bridge.run_parliamentary_scene()` — genuinely a resolution phase that takes queued "acts" (scenes) and world state and produces outcomes | `cross_scale/scene_dispatch.py:217-395` | partial | Real resolution exists, dispatched per scene_type with bespoke per-type logic (com
    216: # ─────────────────────────────────────────────────────────────────────────
  >>217: def _resolve_slot(slot, world, rng):
    218:     st = slot.scene_type
    219:     ctx = slot.context or {}
    220:     zi = zoom_in_out.zoom_in(ctx.get("from_phase", "after_phase_6_step_1"),
    221:                              ctx.get("board_degree"), world)
    222:     out = {"scene_type": st, "resolved": False, "reason": None,
    223:            "scene_ob_modifier": zi.scene_ob_modifier}
    224:     try:

### engine/cross_scale/scene_dispatch.py:299   [logs: R1_engine]
  CLAIM: - **Single seeded stream**: `World.rng: random.Random` (`game_state.py:265`), constructed as `random.Random(seed)` in `create_world` (game_state.py:306) and stored on `World` (339). This is **the** campaign RNG; every subsystem receives it (or a value derived from it) rather than drawing from the global `random` module directly — with one documented, deliberate exception (`scene_dispatch.py:299`, 
    298:             try:
  >>299:                 random.seed(rng.getrandbits(32))
    300:                 built = build_contest(parts[0], parts[1], venue=proceeding)

### engine/cross_scale/scene_dispatch.py:299-306   [logs: R1_engine]
  CLAIM: - **Documented exception — global-state save/restore**: `scene_dispatch.py:299-306` — the contest kernel resolves off the **global** `random` module (not an injectable rng), so this call site does `prev_random_state = random.getstate(); random.seed(rng.getrandbits(32)); ... finally: random.setstate(prev_random_state)`. Comment explicitly notes reseeding from a **derived** value rather than a fixed
    298:             try:
  >>299:                 random.seed(rng.getrandbits(32))
    300:                 built = build_contest(parts[0], parts[1], venue=proceeding)
    301:                 (verdict, verdict_reason), _bout = resolve_contest(built)
    302:             finally:
    303:                 random.setstate(prev_random_state)
    304:             out["resolved"] = True
    305:             # 'verdict'/'verdict_reason' are the promoted kernel's own shape (a win-condition band
    306:             # or side label, plus 'win'|'draw'|'clinch:...') — NOT the deprecated stub's

### engine/cross_scale/scene_dispatch.py:344-345   [logs: PR337]
  CLAIM: | A337-041 | mechanic | Accord echo — two rulings needed, not one field | `HANDOFF_IN.md:3783-3786` | `echo_transport.py:302-313` also requires `echo_ctx["target_settlement"]`, absent from contest branch's echo block (`scene_dispatch.py:344-345`, 4 keys); `scene_outcome` must be a validated §5.5 member the module refuses to infer. | BUILT, dormant (2 gates) | no (dormant) |
    343:                 fid = ctx.get("faction")
  >>344:                 ctx["echo"] = {"actor_faction": fid, "target_faction": fid,
    345:                               "most_relevant_stat": "L", "degree": echo_degree}
    346:         elif st in ("fieldwork", "investigation"):

### engine/cross_scale/zoom_in_out.py:48   [logs: R1_engine]
  CLAIM: | `ZoomInResult` / `ZoomOutResult` / `ZoomTrigger` | `cross_scale/zoom_in_out.py:48/56/64` | scene_ob_modifier / domain_echoes_queued / trigger_name+... | `zoom_in()`/`zoom_out()`/`check_mandatory_triggers()` | none |
    47: @dataclass
  >>48: class ZoomInResult:
    49:     valid: bool

### engine/cross_scale/zoom_in_out.py:48-54   [logs: R1_engine]
  CLAIM: | **View** | **NO** | -- | -- | no | No type. `zoom_in_out.zoom_in()` returns a `ZoomInResult{scene_ob_modifier}` (zoom_in_out.py:48-54) — a single numeric modifier, not a per-person situational View. |
  CLAIM: 5. **No per-person View/Sensation assembly exists -- `zoom_in()` returns one global numeric modifier, not a per-actor situational snapshot.** `ZoomInResult{scene_ob_modifier: float}` (zoom_in_out.py:48-54) is the entire "context a scene sees" mechanism today; it is not keyed to a Person and carries no visibility/knowledge model.
    47: @dataclass
  >>48: class ZoomInResult:
    49:     valid: bool
    50:     deferred_to: Optional[str]   # If mid-phase-5, the phase to defer to
    51:     scene_ob_modifier: int       # Per §4.1 board-degree table
    52:     notes: list[str] = field(default_factory=list)
    53: 
    54: 
    55: @dataclass

### engine/cross_scale/zoom_in_out.py:138-149   [logs: PR337,PR338]
  CLAIM: | A337-208 | formula | Personal-to-mass leverage — both failure modes coded, unreachable | `_v1_part2.md` §"Mass-Battle Seam" | `pc_incapacitated` flat regardless of size (dominant small N); `contested_figure_wounded` +0.15 Ob decaying `1/√N` (evaporates large N) — both carriers unreachable because nothing queues a personal scene from a battle. | BUILT (unreachable) | yes, `zoom_in_out.py:138-149`
  CLAIM: | A338-073 | formula | Personal-to-mass leverage — both failure poles coded | `zoom_in_out.py:138-149` | `pc_incapacitated` flat regardless of battle size (dominant small-N); `contested_figure_wounded` +0.15 Ob decaying as 1/√N (evaporates large-N); both unreachable, sixteen lines apart | BUILT (unreachable) | yes, unreachable |
    137: 
  >>138:     pc_incap = scene_outcomes.get('pc_incapacitated', False)
    139:     if pc_incap:
    140:         # [canonical: §4.2 — "PC incapacitation consequences (Stage 1) apply
    141:         #  immediately on Zoom Out (ED-159)"]
    142:         notes.append("PC incapacitation Stage 1 applies immediately (ED-159)")
    143: 
    144:     # ED-PC-0006 (2026-07-08): the flat +1 Ob (Jordan ruling 2026-07-08, ED-PC-0005, reversing PP-716's
    145:     # -1D unification) is now +0.15 Ob -- reuses ED-1041's combat "attacking" magnitude (a commander

### engine/cross_scale/zoom_in_out.py:149   [logs: PR338]
  CLAIM: | A338-039 | formula | Contested Figure wound modifier | `zoom_in_out.py:149` | +0.15 to commander's remaining battle-tactic Obs per wound (reduced from flat +1 by ED-PC-0006), NOT cumulative | BUILT (unreachable) | code runs, path unreachable |
    148:     # ED-167 note) -- a true wound-counter model for the CF is future work.
  >>149:     wound_ob = 0.15 if scene_outcomes.get('contested_figure_wounded', False) else 0.0
    150:     if wound_ob:

### engine/engine_params/descriptors.json:119-123   [logs: PR341]
  CLAIM: | A341-025 | container/registry | `prac.thread_sensitivity` | `engine/engine_params/descriptors.json:119-123` | `floor: 0, ceiling: null, open_ceiling_reference: 100` | EXISTS-DEFECTIVE — verified byte-for-byte | yes — data exists in shipped registry |
    118:   "practitioner_stats": {
  >>119:     "prac.thread_sensitivity": {
    120:       "name": "Thread Sensitivity",
    121:       "floor": 0,
    122:       "ceiling": null,
    123:       "open_ceiling_reference": 100
    124:     },

### engine/engine_params/descriptors.json:119-129   [logs: PR341]
  CLAIM: | 6 | `prac.thread_sensitivity` has `floor: 0, ceiling: null, open_ceiling_reference: 100`; `prac.tps` has `floor: null, ceiling: null` and is named nowhere in the suite | `05_independent_verification.md §2.5` | **VERIFIED-TRUE** — `engine/engine_params/descriptors.json:119-129` matches exactly |
    118:   "practitioner_stats": {
  >>119:     "prac.thread_sensitivity": {
    120:       "name": "Thread Sensitivity",
    121:       "floor": 0,
    122:       "ceiling": null,
    123:       "open_ceiling_reference": 100
    124:     },
    125:     "prac.tps": {
    126:       "name": "Thread Pool Score",

### engine/engine_params/descriptors.json:125-129   [logs: PR341]
  CLAIM: | A341-026 | container/registry | `prac.tps` | `engine/engine_params/descriptors.json:125-129` | `floor: null, ceiling: null` — worse than TS and named nowhere in the suite | EXISTS-DEFECTIVE — verified byte-for-byte | yes — data exists in shipped registry |
    124:     },
  >>125:     "prac.tps": {
    126:       "name": "Thread Pool Score",
    127:       "floor": null,
    128:       "ceiling": null
    129:     }
    130:   },

### engine/engine_params/params_tables.yaml:1   [logs: R1_engine]
  CLAIM: `_generated` header (params_tables.yaml:1): "GENERATED by tools/export_params_constants.py from engine/params/**/*.md. NEVER hand-edit... Where a value here disagrees with code, THE CODE WINS." `file_count: 37`, `table_count: 258`, `row_count: 1367`. This is reference-only — `dice_engine.degree_from_net`'s own docstring (dice_engine.py:264-270) explicitly disclaims it: the capture still shows the 
  >>1: _generated: 'GENERATED by tools/export_params_constants.py from engine/params/**/*.md. NEVER hand-edit: re-run the tool. This is a FAITHFUL STRUCTURAL CAPTURE of the parameter tables (cells verbatim), not a typ
    2: schema_version: 1

### engine/mc_v18.py:79-89   [logs: PR337,PR338]
  CLAIM: | A337-183 | mechanic | `combat_bridge.derive_parties`/`resolve` — dead twice over | `_v1_part2.md` §"Cross-Scale Plumbing" | `history=max(1,round(f.Mil))`; returns `None` on gap rather than fabricating an actor; gated `DISPATCH_COMBAT_BRIDGE` default OFF AND no `queue_scene("combat",...)` call site exists even if ON. | INERT | VERIFIED-TRUE, `combat_bridge.py:103-128`; flag `mc_v18.py:79-89` |
  CLAIM: | 7 | `DISPATCH_COMBAT_BRIDGE` defaults OFF (`mc_v18.py:79-89`) | **VERIFIED-TRUE** |
  CLAIM: | A338-043 | mechanic | `combat_bridge.derive_parties`/`resolve` | `combat_bridge.py:103-128`; flag `mc_v18.py:79-89` | Faction→Personal; builds combat actor from `history=max(1,round(f.Mil))`; correctly returns None on gap rather than fabricating; gated behind `DISPATCH_COMBAT_BRIDGE` default OFF, unreachable even ON (no `queue_scene("combat",...)` call site) | INERT (dead twice over) | no |
  CLAIM: | A337-032 | node/godot-adjacent | `combat_bridge.py` behind `DISPATCH_COMBAT_BRIDGE`, default OFF | `cross_scale_action_catalogue_v1.md` §8.3 | Bridge resolves combat scenes if flagged on, but no trigger anywhere queues a combat scene. | BUILT, dead-flagged | VERIFIED-TRUE — `mc_v18.py:79-89` defaults OFF; `derive_parties` returns `None` on gap |
  CLAIM: | `combat_bridge.derive_parties` returns `None` on a derivation gap and `DISPATCH_COMBAT_BRIDGE` defaults OFF | A337-032/183 | **VERIFIED-TRUE** — `engine/cross_scale/combat_bridge.py:112-128` (explicit `if a is None or b is None: return None`), `engine/mc_v18.py:79-89` |
    78: def _dispatch_combat_bridge_on(effective_params: dict) -> bool:
  >>79:     """DISPATCH_COMBAT_BRIDGE flag (ED-IN-0091, plan §2.2, OI-01) — **default OFF**, mirroring
    80:     `_echo_transport_on`'s params-override-then-env-var resolution (ED-IN-0028) as the plan's §2.2
    81:     "find the existing flag pattern... and mirror it; single owner" instruction directs. With the
    82:     flag off `world.dispatch_combat_bridge` is False and scene_dispatch's combat branch takes the
    83:     UNCHANGED historical path (the deprecated `systems.combat.sim.combat.resolve_combat_round`
    84:     call stays in place — byte-identical to pre-bridge behaviour, per the plan's "ship-flag-off"
    85:     term). The flip to ON is a deliberate, separately-scheduled IN action after PC's E0-E3 batches
    86:     merge (plan §0/§2.2), never a side effect of this wave."""

### engine/mc_v18.py:91   [logs: R1_engine]
  CLAIM: | `CampaignResult` | `mc_v18.py:91` | winner, season, surviving, battle_count, scenes_resolved, insurgencies_formed, npcs_generated, stub_hits, accord_drift_probe_hits, key_log_hash, keys_emitted, final_state | `run_campaign()` | none |
    90: 
  >>91: 
    92: @dataclass

### engine/mc_v18.py:109   [logs: R1_engine]
  CLAIM: | `BatchResult` | `mc_v18.py:109` | n, win_share, all_winners, battles_mean | `run_batch()` | none |
    108:                                      # registry.province_accord and Territory.accord this campaign.
  >>109:                                      # 0 when nothing diverges; never writes either compared value
    110:                                      # (OI-37/SE routes the actual write-model reconciliation).

### engine/mc_v18.py:120-207   [logs: R1_engine]
  CLAIM: | **DELIBERATE** (loop step 3) | **NO** | closest: `_faction_actions_callback`'s per-faction `composition.require('faction_action')` call (mc_v18.py:138) — but this is a **stochastic candidate-selection algorithm inside `systems/`**, not a `choose(Person, View, Sensation)->Act` call, and it operates on Factions, not Persons | `mc_v18.py:120-207` | no | Wholesale different mechanism: deterministic-
  CLAIM: b. **ACTION** — `action_callback(world)` = `mc_v18._faction_actions_callback` (mc_v18.py:120-207): for each parliamentary faction holding territory, `composition.require('faction_action')(faction, world, world.rng)` (138, wrapped in try/except that prints to stderr on error, never swallows silently — 139-144); then `scene_dispatch.run_scene_phase(world, world.rng)` (149) which drains `scene_slate`
    119:     win_share: dict[str, float] = field(default_factory=dict)
  >>120:     all_winners: dict[str, int] = field(default_factory=dict)
    121:     battles_mean: float = 0.0
    122: 
    123: 
    124: def _faction_actions_callback(world):
    125:     """Per-season faction action dispatch — passed to season.run_season.
    126: 
    127:     GD-2 mandatory-actions precedence is enforced inside faction_take_action

### engine/mc_v18.py:124-217   [logs: PR341]
  CLAIM: | T8-8 | T8 | DEFECT | Claimed vs executing: suite's contribution is 0% executable; only pre-suite loop executes headless today (`mc_v18.py:124-217` + `accounting.py:96-120`) | none proposed | y |
  CLAIM: | A341-059 | loop/phase | pre-suite strategic loop (what executes today) | `mc_v18.py:124-217` + `accounting.py:96-120` | Faction actions, contest dispatch, parliamentary vote; CI, MS, insurgency emergence and promotion, NPE stance drift | VERIFIED TRUE — this is the entirety of what runs headless today | yes — live |
    123: 
  >>124: def _faction_actions_callback(world):
    125:     """Per-season faction action dispatch — passed to season.run_season.
    126: 
    127:     GD-2 mandatory-actions precedence is enforced inside faction_take_action
    128:     (mandatory pass before stochastic candidates per HR-9). Parliamentary +
    129:     territory-holding gate matches the pre-migration inline block at
    130:     mc_v18 L75-87 prior to 2026-05-20.
    131:     """

### engine/mc_v18.py:130-145   [logs: PR338]
  CLAIM: | A338-086 | process | `faction_take_action` | `faction_action.py:208-273`; `mc_v18.py:130-145` | Season faction step: signals→re-weight→1 draw→dispatch through 4 non-elif buckets; `'invalid'` sentinel falls through to terminal unconditional `_try_govern`, caller discards return | BUILT | yes (structural defect) |
    129:     territory-holding gate matches the pre-migration inline block at
  >>130:     mc_v18 L75-87 prior to 2026-05-20.
    131:     """
    132:     for fn, faction in world.factions.items():
    133:         if not faction.parliamentary:
    134:             continue
    135:         if not faction.territories:
    136:             continue
    137:         try:

### engine/mc_v18.py:132-136   [logs: PR338]
  CLAIM: | A338-006 | claim-about-repo | Faction pass gates on 2 conditions only | `engine/mc_v18.py:132-136` | Gates on `faction.parliamentary` and `faction.territories`; calls `faction_take_action` unconditionally, no person consulted | measured | VERIFIED-TRUE (per H1/H9 lane citation; not independently re-verified line range) |
    131:     """
  >>132:     for fn, faction in world.factions.items():
    133:         if not faction.parliamentary:
    134:             continue
    135:         if not faction.territories:
    136:             continue
    137:         try:

### engine/mc_v18.py:138   [logs: R1_engine]
  CLAIM: | **DELIBERATE** (loop step 3) | **NO** | closest: `_faction_actions_callback`'s per-faction `composition.require('faction_action')` call (mc_v18.py:138) — but this is a **stochastic candidate-selection algorithm inside `systems/`**, not a `choose(Person, View, Sensation)->Act` call, and it operates on Factions, not Persons | `mc_v18.py:120-207` | no | Wholesale different mechanism: deterministic-
  CLAIM: - **Propagation**: `world.rng` is passed explicitly to `faction_action` (mc_v18.py:138), `scene_dispatch.run_scene_phase` (149), `parliamentary_bridge.run_parliamentary_scene` (158).
    137:         try:
  >>138:             composition.require('faction_action')(faction, world, world.rng)
    139:         except Exception as e:

### engine/mc_v18.py:152-158   [logs: PR338]
  CLAIM: | A338-142 | function/signature | `_derive_vote` — parliamentary bridge | `engine/cross_scale/parliamentary_bridge.py:88-107`; `mc_v18.py:152-158` | Runs every season by default: lowest-Stability eligible faction proposes, highest-Mandate defends, `motion_id=f"parl_s{season}"` — derives WHO never WHAT; identical contentless motion every season | BUILT | yes |
    151: 
  >>152:     # Parliamentary vote (ED-SC-0006/0007, Jordan ruling "wire the canonical Parliamentary vote"):
    153:     # the faction-scale §10 vote resolves directly on aggregate state, applies the §10 loser Mandate
    154:     # penalty, and composes a winner Domain Echo (ED-SC-0002 composed keying) through the substrate.
    155:     # Flag-gated by the scheduler's presence — a no-op when ECHO_TRANSPORT is off.
    156:     if getattr(world, "echo_scheduler", None) is not None:
    157:         from engine.cross_scale import parliamentary_bridge
    158:         _pr = parliamentary_bridge.run_parliamentary_scene(world, world.rng)
    159:         if _pr.get("resolved"):

### engine/mc_v18.py:190-206   [logs: R1_engine]
  CLAIM: | **CENSUS** (loop step 6 — birth/death/demographic envelope) | **partial, and explicitly deferred** | `World.npc_counter`, `mc_v18`'s `stub_resolve('generate_npc', ...)` and `stub_resolve('form_knot', ...)` (mc_v18.py:190-206) — both **explicitly stub-wired, no automatic call site**; `victory.py`'s territory-count check is the only per-season "count something" pass | `mc_v18.py:190-206` | no (del
  CLAIM: 6. **CENSUS-shaped generation (birth/death) is explicitly and deliberately UNBUILT, not merely unlinked.** `mc_v18.py:190-206`'s two `stub_resolve()` calls document that no canon source names a world-gen population count or a season-tick generation trigger -- this is a genuine content/design gap upstream of any engine change, not an engine limitation per se, but it means CENSUS has literally nothi
    189:     # is an unresolved Open Question, not a ratified number — not usable as a cited constant).
  >>190:     # Per the no-fabrication rule (CLAUDE.md §5/§7, this wave's own instruction), the honest move
    191:     # is to generate none automatically rather than invent a count or a trigger. Recorded via
    192:     # stubwire (not silent) so the gap is greppable/counted, same discipline as the knots stub
    193:     # immediately below.
    194:     stubwire.stub_resolve(
    195:         'engine.mc_v18', 'generate_npc(world-gen|season-tick)',
    196:         reason="OI-05: investigation_systems_v30.md SYSTEM 1 Two-Tier Generation is scene-"
    197:                "specification-driven only (\"Scene specification declares density and "

### engine/mc_v18.py:194-202   [logs: PR341]
  CLAIM: | A341-047 | function/signature | `stub_resolve('engine.mc_v18','generate_npc(world-gen\|season-tick)', reason=...)` | `mc_v18.py:194-202` | Honest deferral of NPC generation; reason: OI-05, canon has no world-gen count/trigger | VERIFIED TRUE (§8) | no — explicitly stubbed |
  CLAIM: 6. **The organizing spine (3 stubs, 1 reason)** — `mc_v18.py:212-218`, `mc_v18.py:194-202`, and
    193:     # immediately below.
  >>194:     stubwire.stub_resolve(
    195:         'engine.mc_v18', 'generate_npc(world-gen|season-tick)',
    196:         reason="OI-05: investigation_systems_v30.md SYSTEM 1 Two-Tier Generation is scene-"
    197:                "specification-driven only (\"Scene specification declares density and "
    198:                "composition\") — no world-gen initial count and no season-tick generation "
    199:                "trigger exist in canon to cite; NPE-02's proposed persistence-cap number is an "
    200:                "unresolved Open Question. Honest deferral, not fabrication (CLAUDE.md §5/§7). "
    201:                "simulate_npc_actions (the drift half) is already wired every season via "

### engine/mc_v18.py:195   [logs: PR338]
  CLAIM: | A338-008 | claim-about-repo | `generate_npc` has no world-gen/season-tick call site | `systems/world/sim/npe.py:226-350`; `engine/mc_v18.py:195` | Complete two-tier generator, only referenced via a `stub_resolve('generate_npc(...)')` string, and in tests | measured | VERIFIED-TRUE |
    194:     stubwire.stub_resolve(
  >>195:         'engine.mc_v18', 'generate_npc(world-gen|season-tick)',
    196:         reason="OI-05: investigation_systems_v30.md SYSTEM 1 Two-Tier Generation is scene-"

### engine/mc_v18.py:198-201   [logs: R1_engine]
  CLAIM: | **CENSUS** (loop step 6 — birth/death/demographic envelope) | **partial, and explicitly deferred** | `World.npc_counter`, `mc_v18`'s `stub_resolve('generate_npc', ...)` and `stub_resolve('form_knot', ...)` (mc_v18.py:190-206) — both **explicitly stub-wired, no automatic call site**; `victory.py`'s territory-count check is the only per-season "count something" pass | `mc_v18.py:190-206` | no (del
    197:                "specification-driven only (\"Scene specification declares density and "
  >>198:                "composition\") — no world-gen initial count and no season-tick generation "
    199:                "trigger exist in canon to cite; NPE-02's proposed persistence-cap number is an "
    200:                "unresolved Open Question. Honest deferral, not fabrication (CLAUDE.md §5/§7). "
    201:                "simulate_npc_actions (the drift half) is already wired every season via "
    202:                "accounting.run_accounting — see the comment above this call.")

### engine/mc_v18.py:203-217   [logs: PR341]
  CLAIM: | 1 | `mc_v18.py:212-218` defers knot formation with the exact quoted reasoning about personal-scale actor fields (Disposition, Bonds, TS) absent from the aggregate strategic World, citing the same "context-derivation gap" `scene_dispatch.py`'s docstring names | `00_INDEX.md:83`, `05_independent_verification.md §2.6` | **VERIFIED-TRUE** — `mc_v18.py:203-217` (line numbers off by ~1-2 but content m
    202:                "accounting.run_accounting — see the comment above this call.")
  >>203: 
    204:     # OI-07 (world.knots half) — form_knot gets NO auto-call either, for the same no-fabrication
    205:     # reason, re-verified against systems/fieldwork/knots_v30.md §3.1 this wave: Prerequisites
    206:     # ("Disposition +5 with target NPC", "PC Bonds >= 5", "PC's current Knot count < "
    207:     # "floor(Bonds/2) + 1") are personal-scale actor fields (Disposition, Bonds) that do not
    208:     # exist anywhere on the aggregate strategic World — the same "context-derivation gap" the
    209:     # scene_dispatch.py module docstring already names for combat/contest actor derivation, not
    210:     # a new one. No world-gen or season-tick formation rule exists in canon to cite. Honest

### engine/mc_v18.py:204-205   [logs: PR341]
  CLAIM: | A341-082 | module/subsystem | `knots.py` | claimed "only live module" | Zero production callers; `mc_v18.py:204-205` records a `stub_resolve` under literal name `'form_knot(world-gen\|season-tick)'` | consistent with A341-048 (VERIFIED) | no — stubbed |
    203: 
  >>204:     # OI-07 (world.knots half) — form_knot gets NO auto-call either, for the same no-fabrication
    205:     # reason, re-verified against systems/fieldwork/knots_v30.md §3.1 this wave: Prerequisites
    206:     # ("Disposition +5 with target NPC", "PC Bonds >= 5", "PC's current Knot count < "

### engine/mc_v18.py:212-218   [logs: PR341]
  CLAIM: | 1 | `mc_v18.py:212-218` defers knot formation with the exact quoted reasoning about personal-scale actor fields (Disposition, Bonds, TS) absent from the aggregate strategic World, citing the same "context-derivation gap" `scene_dispatch.py`'s docstring names | `00_INDEX.md:83`, `05_independent_verification.md §2.6` | **VERIFIED-TRUE** — `mc_v18.py:203-217` (line numbers off by ~1-2 but content m
  CLAIM: 6. **The organizing spine (3 stubs, 1 reason)** — `mc_v18.py:212-218`, `mc_v18.py:194-202`, and
  CLAIM: | A341-048 | function/signature | `stub_resolve('engine.mc_v18','form_knot(world-gen\|season-tick)', reason=...)` | `mc_v18.py:212-218` | Honest deferral of knot formation; reason: OI-07, prerequisites are personal-scale actor fields (Disposition, Bonds, TS) absent from aggregate strategic World | VERIFIED TRUE (§8), exact text match | no — explicitly stubbed |
    211:     # deferral beats invented knots (this wave's own instruction).
  >>212:     stubwire.stub_resolve(
    213:         'engine.mc_v18', 'form_knot(world-gen|season-tick)',
    214:         reason="OI-07: knots_v30.md §3.1 Prerequisites require personal-scale actor fields "
    215:                "(Disposition, Bonds, TS) absent from the aggregate strategic World — no "
    216:                "world-gen or season-tick formation rule exists in canon to cite. Honest "
    217:                "deferral, not fabrication (CLAUDE.md §5/§7); world.knots stays empty this wave.")
    218: 
    219: 

### engine/mc_v18.py:213-283   [logs: R1_engine]
  CLAIM: `run_campaign(seed, max_seasons, params)` (`mc_v18.py:213-283`):
    212:     stubwire.stub_resolve(
  >>213:         'engine.mc_v18', 'form_knot(world-gen|season-tick)',
    214:         reason="OI-07: knots_v30.md §3.1 Prerequisites require personal-scale actor fields "
    215:                "(Disposition, Bonds, TS) absent from the aggregate strategic World — no "
    216:                "world-gen or season-tick formation rule exists in canon to cite. Honest "
    217:                "deferral, not fabrication (CLAUDE.md §5/§7); world.knots stays empty this wave.")
    218: 
    219: 
    220: def run_campaign(seed: int | None = None, max_seasons: int = 50,

### engine/mc_v18.py:222-241   [logs: R1_engine]
  CLAIM: | `World` | `autoload/game_state.py:256` | factions, territories, clocks, season, arc, winner, battle_count, scenes_resolved, rng, **14 schema-migration registries** (practitioners, insurgencies, uncontrolled_streaks, npcs, npc_counter, treaties, convictions, beliefs, knots, knot_id_counter, territory_infrastructure, npc_drift_state, threadcut_beings, comovement_deck) + **settlements** | `create_w
    221:                  params: dict | None = None) -> CampaignResult:
  >>222:     """Run a single campaign to completion."""
    223:     if seed is None:
    224:         seed = int(time.time()) & 0xFFFFFFFF
    225: 
    226:     # F7-pattern telemetry (ED-IN-0091, plan §2.1): snapshot the module-cumulative stubwire
    227:     # counter before the campaign runs so stub_hits below is THIS campaign's delta, not the
    228:     # process-lifetime total (the counter is intentionally process-cumulative — see
    229:     # engine/substrate/stubwire.py's docstring — so a delta is how a single campaign reads it).

### engine/mc_v18.py:263-267   [logs: R1_engine]
  CLAIM: 3. **Victory check** (mc_v18.py:263-267): `victory.check_all_factions(world)` each season; first `won=True` result sets `world.winner` and breaks the season loop.
    262:         # mc_v18.py this wave — WORLD lane does). Registers the §3.1 trigger-table type_ids on
  >>263:         # this campaign's scheduler; each fired trigger routes to a typed stubwire no-op (the
    264:         # render layer stays ED-IN-0073's docket, unbuilt).
    265:         from engine.cross_scale import articulation as _articulation
    266:         _articulation.subscribe_all(world.echo_scheduler)
    267: 
    268:     for _ in range(max_s):

### engine/mc_v18.py:281-282   [logs: R1_engine]
  CLAIM: - **Replay/hashing**: `KeyLog.content_hash()` = `sha256(serialize())`, `serialize()` = newline-joined sorted-key JSON per Key in log order (keys.py:414-419). `CampaignResult.key_log_hash`/`keys_emitted` surface this per campaign (mc_v18.py:281-282). Two campaigns with the same seed and same code path produce byte-identical `key_log_hash` — pinned by `test_parliamentary_bridge.py` (`_ON_KEYLOG_HASH
    280:         # === VICTORY CHECK (GD-1) ===
  >>281:         results = victory.check_all_factions(world)
    282:         for vr in results:
    283:             if vr.won:

### engine/mc_v18.py:286-303   [logs: R1_engine]
  CLAIM: `run_batch(n, base_seed, params)` (mc_v18.py:286-303) just loops `run_campaign` with `seed = base_seed + i` and aggregates win-share/battle-count.
    285:                 break
  >>286: 
    287:     # Fallback winner by territory count (v17 L753-761)
    288:     if not world.winner:
    289:         scores = {}
    290:         for fn, f in world.factions.items():
    291:             if not f.parliamentary:
    292:                 continue
    293:             held = sum(1 for tid in game_state.ALL_PLAYABLE_15

### engine/substrate/canon_buckets.py:35-46   [logs: R1_engine]
  CLAIM: | **Query** (never stored, always recomputed) | **partial precedent exists, unnamed** | `canonical_accord()`/`canonical_pt()` (pure bucket functions, no storage) are Query-shaped; `descriptors.faction_bounds()` is Query-shaped (reads registry, computes, returns — never stores); by contrast `engine/`'s wider corpus term `Derived` means the **opposite** — a **stored** per-character value (see Sectio
    34: """
  >>35: from __future__ import annotations
    36: 
    37: 
    38: def canonical_accord(continuous_accord: float) -> int:
    39:     """Map continuous Accord (range 0.5-7.0 per `game_state.ACCORD_MAP`) → canonical integer 0-4.
    40:     ACCORD_MAP: 1.0, 2.5, 4.0, 5.5, 7.0. Midpoints: 1.75, 3.25, 4.75, 6.25.
    41: 
    42:     Uses nearest-neighbor with midpoints between successive canonical floats — direct `int()`

### engine/substrate/composition.py:1-24   [logs: PR344,R3_godot]
  CLAIM: Python (`engine/substrate/composition.py:1-24`, which matches `04_GODOT_IMPLEMENTABILITY.md §8.1`'s
  CLAIM: verified independently in this pass: `engine/substrate/composition.py:1-24` (Python, live,
  CLAIM: §8.1 "A subsystem is swapped by editing a registry row" — PROVEN on both sides already. Python: `engine/substrate/composition.py:1-24` resolves a role to a callable by string from `engine/engine_params/composition.json`, "Adding a subsystem to the campaign loop is a row in the registry, not an import in the engine" — late failure prevented because "the exporter imports and resolves every declared 
  >>1: """engine.substrate.composition — resolve a ROLE to a subsystem callable, without naming it.
    2: 
    3: Status: [live, 2026-08-20]
    4: 
    5: WHY. `systems/` stems from `engine/` and `references/`. The campaign driver contradicted that:
    6: `engine/mc_v18.py` imported `systems.factions.sim.faction_action` and `systems.overview.sim.season`
    7: at module level, so the root named its own dependents and the package graph carried a cycle
    8: (`faction_action.py` -> `engine.autoload.game_state` -> `systems.factions.sim.treaty`).
    9: 

### engine/substrate/composition.py:1-46   [logs: R1_engine]
  CLAIM: 1. **A composable, role-resolved dependency substrate already exists and solves exactly the problem a carrier/edge redesign would reopen.** `engine/substrate/composition.py` resolves 27 declared roles by string from `references/module_contracts.yaml`, with a blocking export-time round-trip check (composition.py:1-46). Any new design primitive that needs to call into `systems/` should route through
  >>1: """engine.substrate.composition — resolve a ROLE to a subsystem callable, without naming it.
    2: 
    3: Status: [live, 2026-08-20]
    4: 
    5: WHY. `systems/` stems from `engine/` and `references/`. The campaign driver contradicted that:
    6: `engine/mc_v18.py` imported `systems.factions.sim.faction_action` and `systems.overview.sim.season`
    7: at module level, so the root named its own dependents and the package graph carried a cycle
    8: (`faction_action.py` -> `engine.autoload.game_state` -> `systems.factions.sim.treaty`).
    9: 

### engine/substrate/descriptors.py:57-60   [logs: PR339]
  CLAIM: | A339-125 | claim-about-repo | "engine/substrate/descriptors.py owns the attribute roster... nine defined, a tenth ruled and unnamed, exposed as ATTRIBUTES_PENDING_TENTH" | 01§1.1.1 | cites existing repo mechanism | — | VERIFIED TRUE (descriptors.py:57-60) |
  CLAIM: | 2 | `engine/substrate/descriptors.py` roster is nine attributes, tenth pending, exposed as `ATTRIBUTES_PENDING_TENTH` | 01§1.1.1 | VERIFIED TRUE — descriptors.py:57-60 |
    56: #: Character attribute roster, in registry order. NOTE: Jordan ruled 2026-08-14 that this WILL BE
  >>57: #: ten; the registry ships nine and the tenth is unnamed. `ATTRIBUTES_PENDING_TENTH` is non-None
    58: #: while that is true, so a reader cannot mistake the current roster for a closed one.
    59: ATTRIBUTES = tuple(_DATA['attributes']['roster'])
    60: ATTRIBUTES_PENDING_TENTH = _DATA['attributes'].get('pending_tenth')
    61: ATTRIBUTE_FLOOR = _DATA['attributes']['scale']['floor']

### engine/substrate/descriptors.py:64-120   [logs: R1_engine]
  CLAIM: 6. **A registry-driven bound/clamp mechanism for stat mutation already exists** (`descriptors.faction_bounds()`, game_state.py:153-196) -- floor/ceiling per stat pulled from a cooked registry rather than hardcoded, with a load-time completeness check (`assert_faction_roster_is_covered`, descriptors.py:64-120) that halts import if the registry and the dataclass disagree. This is a working instance 
    63: 
  >>64: #: {registry key -> {name, floor, ceiling}} for each declared domain.
    65: FACTION_STATS = _DATA['faction_stats']
    66: SETTLEMENT_STATS = _DATA['settlement_stats']
    67: PRACTITIONER_STATS = _DATA['practitioner_stats']
    68: TERRITORY_STATS = _DATA['territory_stats']
    69: 
    70: #: {registry key -> the field name engine.autoload.game_state.Faction actually uses}.
    71: FACTION_FIELD_MAP = _DATA['faction_field_map']

### engine/substrate/descriptors.py:98-108   [logs: R1_engine]
  CLAIM: | **Query** (never stored, always recomputed) | **partial precedent exists, unnamed** | `canonical_accord()`/`canonical_pt()` (pure bucket functions, no storage) are Query-shaped; `descriptors.faction_bounds()` is Query-shaped (reads registry, computes, returns — never stores); by contrast `engine/`'s wider corpus term `Derived` means the **opposite** — a **stored** per-character value (see Sectio
    97: def assert_faction_roster_is_covered(implemented_fields):
  >>98:     """Raise if the REGISTRY declares a faction stat the executable model does not implement.
    99: 
    100:     THIS IS THE POINT OF THE MODULE: it makes `references/descriptor_registry.yaml` load-bearing at
    101:     RUNTIME rather than by convention. Add a stat to the registry without adding its field and the
    102:     engine stops importing, instead of silently running on a roster that no longer matches canon.
    103: 
    104:     IT ITERATES `FACTION_STATS`, WHICH IS THE REGISTRY-DERIVED HALF OF THE ARTIFACT, AND THAT IS THE
    105:     WHOLE CORRECTNESS ARGUMENT. Until 2026-08-21 it iterated `FACTION_FIELD_MAP` instead — the

### engine/substrate/descriptors.py:98-172   [logs: R1_engine]
  CLAIM: 3. **Confirms** the `engine/` Section 0.05 claim about `descriptors.faction_bounds()` closing the 5-vs-6 gap and Legitimacy's floor-0/ceiling-7 ruling -- `Faction.adjust()`'s docstring (game_state.py:153-196) and `descriptors.py:98-172`'s docstrings are internally consistent with each other and with CLAUDE.md's account, including the still-unreachable `fac.intel` case (game_state.py:164-171, `MULT
    97: def assert_faction_roster_is_covered(implemented_fields):
  >>98:     """Raise if the REGISTRY declares a faction stat the executable model does not implement.
    99: 
    100:     THIS IS THE POINT OF THE MODULE: it makes `references/descriptor_registry.yaml` load-bearing at
    101:     RUNTIME rather than by convention. Add a stat to the registry without adding its field and the
    102:     engine stops importing, instead of silently running on a roster that no longer matches canon.
    103: 
    104:     IT ITERATES `FACTION_STATS`, WHICH IS THE REGISTRY-DERIVED HALF OF THE ARTIFACT, AND THAT IS THE
    105:     WHOLE CORRECTNESS ARGUMENT. Until 2026-08-21 it iterated `FACTION_FIELD_MAP` instead — the

### engine/substrate/keys.py:44   [logs: R1_engine]
  CLAIM: **Axes** (keys.py:44): `("hierarchical", "sacred", "instrumental", "traditional")` — the canonical 4-axis Conviction set.
    43: KeyLog gained an OPTIONAL `stat_vocabulary` hook — a candidate WARN-tier
  >>44: invariant 9, deliberately separate from invariants 1-8 (which raise). Default
    45: None preserves prior behavior exactly: stat_deltas key NAMES were never

### engine/substrate/keys.py:47   [logs: R1_engine]
  CLAIM: **Roles** (keys.py:47): `("subject", "object", "witness", "beneficiary", "bystander")` — target roles.
    46: checked before this (only field presence was — the C6-F1/C7-F6 gap the
  >>47: extension names). See KeyLog._check_stat_vocabulary.
    48: """

### engine/substrate/keys.py:50   [logs: R1_engine]
  CLAIM: | **Rung** | **NO**, but a shadow exists | `scale_signature` (list of `SCALES`) on every `Key`, plus informal "scale" vocabulary in `handoff_rules`/`zoom_in_out`/`scene_dispatch` (`SCALE_PERSONAL`, `SCALE_SCENE`, `SCALE_FACTION` constants) | `substrate/keys.py:50` (`SCALES`), `cross_scale/handoff_rules.py` (`SCALE_*` constants) | partial | `SCALES = ("personal", "settlement", "territory", "peninsu
  CLAIM: **Scales** (keys.py:50): `("personal", "settlement", "territory", "peninsula")`.
    49: 
  >>50: from __future__ import annotations
    51: 

### engine/substrate/keys.py:52   [logs: R1_engine]
  CLAIM: **Permanence** (keys.py:52): `("transient", "persistent", "indelible")`. **Time horizon** (keys.py:53): `("immediate", "near", "far")`.
    51: 
  >>52: import hashlib
    53: import json

### engine/substrate/keys.py:53   [logs: R1_engine]
  CLAIM: **Permanence** (keys.py:52): `("transient", "persistent", "indelible")`. **Time horizon** (keys.py:53): `("immediate", "near", "far")`.
    52: import hashlib
  >>53: import json
    54: import re

### engine/substrate/keys.py:65   [logs: PR337,PR338]
  CLAIM: | `Key.scale_signature`/`SCALES` is exactly `("personal","settlement","territory","peninsula")` | A337-172 | **VERIFIED-TRUE** — `engine/substrate/keys.py:65` |
  CLAIM: | A338-023 | key | `Key.scale_signature` / `SCALES` enum | `keys.py:65` | Runtime enum = exactly 4: personal, settlement, territory, peninsula | BUILT | yes |
  CLAIM: | A337-172 | class/type | `Key.scale_signature` — runtime enum of exactly 4 members | `_v1_part2.md` §"Cross-Scale Plumbing" | `("personal","settlement","territory","peninsula")`. | BUILT | VERIFIED-TRUE, `keys.py:65` |
  CLAIM: | A338-024 | gap | Five parallel scale vocabularies disagree | `keys.py:65` vs `key_type_registry_v30.md` vs `module_contracts.yaml` (7-value) vs `scale_hierarchy_v1` (Country>Duchy>Province>Territory>Settlement) vs `scale_transitions_v30` (Object/Personal/Relational/Territorial/Structural) | Only "Personal" appears in all five; held at ED-IN-0103 fork 1 | DESIGNED (unreconciled) | n/a |
    64: # §2.1 scale_signature members (key_substrate_v30.md).
  >>65: SCALES = ("personal", "settlement", "territory", "peninsula")
    66: 

### engine/substrate/keys.py:66   [logs: R1_engine]
  CLAIM: | `KeyValidationError` / `TerminationBreach` | `substrate/keys.py:66/71` | exception subtypes | raised throughout `keys.py` | n/a |
    65: SCALES = ("personal", "settlement", "territory", "peninsula")
  >>66: 
    67: PERMANENCE_VALUES = ("transient", "persistent", "indelible")

### engine/substrate/keys.py:74   [logs: R2_systems]
  CLAIM: | `KeyValidationError`, `TerminationBreach` | `engine/substrate/keys.py:74,78` | -- | _architecture |
    73: 
  >>74: class KeyValidationError(ValueError):
    75:     """A Key failed a §2.3 universal invariant or its type's payload contract."""

### engine/substrate/keys.py:83-97   [logs: R1_engine]
  CLAIM: **Target** (keys.py:83-97): `actor_id, role, impact_vector: dict[axis->signed magnitude], stat_deltas: dict[stat->delta]`.
    82:     re-entrant runaway, and a silent clamp would hide the defect the
  >>83:     propagation spec's Theorem B is meant to make loud.
    84:     """
    85: 
    86: 
    87: @dataclass
    88: class Target:
    89:     """One targets[] entry (§2.1). Wide fan-out is one Key, N targets — the
    90: 

### engine/substrate/keys.py:83   [logs: R1_engine]
  CLAIM: | `Target` | `substrate/keys.py:83` | actor_id, role, impact_vector, stat_deltas | emitters building a Key | none post-construction |
    82:     re-entrant runaway, and a silent clamp would hide the defect the
  >>83:     propagation spec's Theorem B is meant to make loud.
    84:     """

### engine/substrate/keys.py:88   [logs: R2_systems]
  CLAIM: | `Target`, `Visibility`, `EmittedAt` | `engine/substrate/keys.py:88,109,125` | -- | _architecture |
    87: @dataclass
  >>88: class Target:
    89:     """One targets[] entry (§2.1). Wide fan-out is one Key, N targets — the

### engine/substrate/keys.py:100-111   [logs: R1_engine]
  CLAIM: | **WITNESS** (loop step 5) | **NO** | nearest: `Key.visibility` block (who MAY see a Key) — but no step computes actual per-person Claims from it | `substrate/keys.py:100-111` | no | see Claim row above |
  CLAIM: **Visibility** (keys.py:100-111): `public: bool = True`, `semi_public_observers: list`, `private_observers: list` — invariant 8 forces exactly one shape (public XOR exactly-one non-empty observer list).
  CLAIM: | **Claim** (`witness : (Person, Event) -> Claim[]`, per-person, never a collection) | **NO** | -- | -- | no | `Key.visibility` (public/semi_public_observers/private_observers, keys.py:100-111) encodes *who may see* a Key, but nothing computes a per-witness `Claim` object — no function anywhere takes `(Person, Event)` and returns anything. `articulation.py`'s `render_protagonist_lens`/`generate_ch
    99:     def to_obj(self) -> dict:
  >>100:         return {
    101:             "actor_id": self.actor_id,
    102:             "role": self.role,
    103:             "impact_vector": dict(self.impact_vector),
    104:             "stat_deltas": dict(self.stat_deltas),
    105:         }
    106: 
    107: 

### engine/substrate/keys.py:100   [logs: R1_engine]
  CLAIM: | `Visibility` | `substrate/keys.py:100` | public, semi_public_observers, private_observers | Key construction | none |
    99:     def to_obj(self) -> dict:
  >>100:         return {
    101:             "actor_id": self.actor_id,

### engine/substrate/keys.py:114-121   [logs: R1_engine]
  CLAIM: **EmittedAt** (keys.py:114-121): `season_index: int`, `sub_step_index: int = -1` — sub_step_index is assigned by `KeyLog.append` (append order), never by the emitter.
    113:     semi_public_observers: list = field(default_factory=list)
  >>114:     private_observers: list = field(default_factory=list)
    115: 
    116:     def to_obj(self) -> dict:
    117:         return {
    118:             "public": self.public,
    119:             "semi_public_observers": list(self.semi_public_observers),
    120:             "private_observers": list(self.private_observers),
    121:         }

### engine/substrate/keys.py:114   [logs: R1_engine]
  CLAIM: | `EmittedAt` | `substrate/keys.py:114` | season_index, sub_step_index | emitter (season_index) | `KeyLog.append` sets sub_step_index (keys.py:307) |
    113:     semi_public_observers: list = field(default_factory=list)
  >>114:     private_observers: list = field(default_factory=list)
    115: 

### engine/substrate/keys.py:126-158   [logs: R1_engine]
  CLAIM: **Key** (`engine/substrate/keys.py:126-158`, dataclass):
    125: class EmittedAt:
  >>126:     """§2.1 ordered position. sub_step_index is assigned by KeyLog.append
    127: 
    128:     (append order, SSI-1); callers pass season_index only."""
    129: 
    130:     season_index: int
    131:     sub_step_index: int = -1  # -1 = not yet appended
    132: 
    133:     def to_obj(self) -> dict:

### engine/substrate/keys.py:126   [logs: R1_engine]
  CLAIM: | `Key` | `substrate/keys.py:126` | id, type, emitted_at, source_actor, causes, targets, scale_signature, symbolic_dimensions, visibility, time_horizon, permanence, payload | any emitter (`echo_transport`, `articulation`, tests) | `TypeRegistry.apply_defaults` mutates scale_signature/permanence/time_horizon in place (keys.py:294-303) |
  CLAIM: | **StateChange := (subject, mode, driver)** | **NO** as a unified triple, **partial** as two separate unrelated mechanisms | (1) `Key` (mode-like via `type`, no subject/driver partition); (2) `stubwire.StubResult` (marks "not built" rather than modeling a change) | `substrate/keys.py:126`, `substrate/stubwire.py:39` | no | `Key` carries `source_actor`/`targets[]`/`type`, but nothing partitions ch
  CLAIM: | **Event** (`resolve : (Act[], World) -> Event[]`) | **partial** | `Key` (as the emitted/logged record of something having happened) functions as an Event-like artifact, but nothing in `engine/` takes `Act[]` and produces `Key[]` through a pure `resolve` — the actual resolution paths (`faction_take_action`, `resolve_contest`, `wrapper.fight`) live in `systems/` and are called through `composition
    125: class EmittedAt:
  >>126:     """§2.1 ordered position. sub_step_index is assigned by KeyLog.append
    127: 

### engine/substrate/keys.py:135   [logs: R1_engine]
  CLAIM: | **Tenure** (7 kinds incl. `hold`, `oblige`, `commit`) | **NO** | -- | -- | no | No edge type exists anywhere in `engine/`. Relationships are expressed as: (a) scalar dict membership (`Faction.territories: list[str]` — game_state.py:124 — a faction "holds" a territory only via list membership, no edge object, no kind, no provenance); (b) `Key.causes: list[str]` (keys.py:135) which is a **causal D
    134:         return {"season_index": self.season_index, "sub_step_index": self.sub_step_index}
  >>135: 
    136: 

### engine/substrate/keys.py:138   [logs: R2_systems]
  CLAIM: | **`Key`** | `engine/substrate/keys.py:138` (schema authored `systems/_architecture/key_substrate_v30.md` 2) | id, type, source_actor, emitted_at{season_index,sub_step_index}, causes[], targets[]{actor_id,role,impact_vector,stat_deltas}, scale_signature[], symbolic_dimensions, visibility{public,semi_public_observers,private_observers}, time_horizon, permanence, payload | **_architecture -- THE ex
  CLAIM: | **Event** | **Key** | `systems/_architecture/key_substrate_v30.md` 2 (schema lines 27-76); executable `engine/substrate/keys.py:138` | **strong match, CANONICAL, executable** | this is the design's single biggest miss -- see 7 |
    137: @dataclass
  >>138: class Key:
    139:     """The §2.1 universal Key. NOTE (SSI-4): cascade_depth is deliberately

### engine/substrate/keys.py:141-155   [logs: R1_engine]
  CLAIM: `.to_obj()` (keys.py:141-155) is the canonical serialization — sorted-key JSON, list order preserved (semantic).
    140: 
  >>141:     NOT a field here — it is scheduler-internal state, never logged."""
    142: 
    143:     id: str
    144:     type: str
    145:     emitted_at: EmittedAt
    146:     source_actor: str | None = None
    147:     causes: list = field(default_factory=list)
    148:     targets: list = field(default_factory=list)          # list[Target]

### engine/substrate/keys.py:145   [logs: PR344]
  CLAIM: **§2.2 Identity:** every carrier/edge/Act/Event/Claim/Date/DocketItem/Record carries an `id`, minted from the determinism substream: `id(x) = H(world_seed, tick, subject_id, purpose)`. One mechanism closes two gaps: deterministic+order-independent+unique ids with **no shared allocator** (which would break per-person parallelism), and `purpose` absorbs the missing subject for a `mint` (object doesn
  CLAIM: ### §12 PRECEDENT APPENDIX — what the executable substrate already does (8 rows, all "precedents to copy, not claims the design uses them"): no id on any record→`Key.id:str`(`keys.py:145`); no uniqueness check→invariant 1(`:379-381`); no referential integrity→invariant 3(`:384-388`); cycles in a citation graph→cycle-freedom by construction(`:389-392`); no lookup by id→`lookup(key_id)`(`:363-365`);
  CLAIM: Full placement table (21 objects): Person→RC one class_name; Cohort→SAME class_name as Person (weight≥1) — "two class_names is the rule broken at declaration" (M-10); Container/Rung→RC under a different name (F-3); Office→RC; Site→RC (condition primary state); Tenure→row in a TenureStore RC (largest object count, cardinality per kind, object-side index derived — one object per edge makes every der
    144:     type: str
  >>145:     emitted_at: EmittedAt
    146:     source_actor: str | None = None

### engine/substrate/keys.py:166   [logs: PR341]
  CLAIM: | A341-091 | owner-rule | Key.causes single-mutation-site rule | `keys.py:166,385` | Only `keys.py` itself touches `.causes`; construction sites are the entire population | VERIFIED TRUE by reading, not grepping | yes — live |
  CLAIM: | A341-018 | key | `Key.causes` | `keys.py:166,385` | Provenance list; only two non-test sites touch `.causes`, both inside `keys.py`; nothing appends post-construction | VERIFIED (see §8) — P0-11 states the fix direction backwards | partial (some emitters pass `[]`) |
    165:             "emitted_at": self.emitted_at.to_obj(),
  >>166:             "causes": list(self.causes),
    167:             "targets": [t.to_obj() for t in self.targets],

### engine/substrate/keys.py:167   [logs: R1_engine]
  CLAIM: **TypeRegistry** (keys.py:190-315): loads either the authored markdown (`systems/_architecture/key_type_registry_v30.md`, parsed by a tolerant line-based `_parse_entry`) or the cooked `engine/engine_params/key_types.json` — dispatched by file suffix (keys.py:207-217). **55 types**, pinned identical between the two representations by `test_key_substrate.py::test_json_and_markdown_registries_are_ide
    166:             "causes": list(self.causes),
  >>167:             "targets": [t.to_obj() for t in self.targets],
    168:             "scale_signature": list(self.scale_signature),

### engine/substrate/keys.py:184   [logs: R2_systems]
  CLAIM: | `TypeRegistry`, `KeyLog`, `TickScheduler` | `engine/substrate/keys.py:184,336,463` | -- | _architecture -- TickScheduler is the closest existing analog to a loop-step orchestrator |
    183: 
  >>184: class TypeRegistry:
    185:     """Loader/validator for the Key-type registry, from JSON or from the authored markdown.

### engine/substrate/keys.py:190-315   [logs: R1_engine]
  CLAIM: **TypeRegistry** (keys.py:190-315): loads either the authored markdown (`systems/_architecture/key_type_registry_v30.md`, parsed by a tolerant line-based `_parse_entry`) or the cooked `engine/engine_params/key_types.json` — dispatched by file suffix (keys.py:207-217). **55 types**, pinned identical between the two representations by `test_key_substrate.py::test_json_and_markdown_registries_are_ide
    189:     where review happens. What changed (ED-IN-0136) is what CODE reads: `tools/export_key_types.py`
  >>190:     cooks that markdown into `engine/engine_params/key_types.json` and callers load the JSON.
    191: 
    192:     WHY, since the old docstring argued the opposite and was right at the time. Parsing prose at
    193:     runtime beat duplicating a 55-type roster in Python. It does not beat cooking it, because:
    194:       * **Godot re-implements the logic and cannot parse markdown.** Its answer today is four
    195:         HAND-MADE `.tres` files covering 4 of 55 types — a hand shadow of a machine-readable thing.
    196:       * **The roster had already drifted in every hand-maintained copy**, including this class's own
    197:         docstring, which said "44-type" for the file it parses (ED-IN-0134).

### engine/substrate/keys.py:190   [logs: R1_engine]
  CLAIM: | `TypeRegistry` | `substrate/keys.py:190` | `types: dict` | `TypeRegistry.load`/`load_json` classmethods | none (read-only after load) |
    189:     where review happens. What changed (ED-IN-0136) is what CODE reads: `tools/export_key_types.py`
  >>190:     cooks that markdown into `engine/engine_params/key_types.json` and callers load the JSON.
    191: 

### engine/substrate/keys.py:207-217   [logs: R1_engine]
  CLAIM: **TypeRegistry** (keys.py:190-315): loads either the authored markdown (`systems/_architecture/key_type_registry_v30.md`, parsed by a tolerant line-based `_parse_entry`) or the cooked `engine/engine_params/key_types.json` — dispatched by file suffix (keys.py:207-217). **55 types**, pinned identical between the two representations by `test_key_substrate.py::test_json_and_markdown_registries_are_ide
    206:     """
  >>207: 
    208:     def __init__(self, types: dict):
    209:         # dict preserves registry order (ORD-1 discipline).
    210:         self.types = types
    211: 
    212:     @classmethod
    213:     def load(cls, registry_path: str | Path) -> "TypeRegistry":
    214:         """Load from `.json` (cooked) or markdown (authored). Suffix decides."""

### engine/substrate/keys.py:229-256   [logs: R1_engine]
  CLAIM: **Load-time invariants**: `TypeRegistry.load_json` (keys.py:229-256) additionally validates malformed type ids, non-dict entries, and a self-declared `type_count` against the actual roster size — parity checks the markdown path enforces structurally that the JSON path had to add explicitly (keys.py:242-247 comment names this a real historical gap).
    228:             raise KeyValidationError(f"no key types parsed from {registry_path}")
  >>229:         return cls(types)
    230: 
    231:     @classmethod
    232:     def load_json(cls, path: str | Path) -> "TypeRegistry":
    233:         """Load the cooked registry (`tools/export_key_types.py` output).
    234: 
    235:         Key order is REGISTRY order and significant (ORD-1); `json.load` preserves object order, so
    236:         the invariant survives the round trip. An empty or type-less document raises rather than

### engine/substrate/keys.py:242-247   [logs: R1_engine]
  CLAIM: **Load-time invariants**: `TypeRegistry.load_json` (keys.py:229-256) additionally validates malformed type ids, non-dict entries, and a self-declared `type_count` against the actual roster size — parity checks the markdown path enforces structurally that the JSON path had to add explicitly (keys.py:242-247 comment names this a real historical gap).
    241:         types = doc.get("types")
  >>242:         if not isinstance(types, dict) or not types:
    243:             raise KeyValidationError(f"no key types in {path}")
    244:         # PARITY WITH THE MARKDOWN PATH. Without these, this loader was strictly WEAKER than
    245:         # load(): the heading regex `[a-z_]+\.[a-z_]+` (_TYPE_HEADING) makes a malformed id
    246:         # unrepresentable in markdown, and _parse_entry always returns a dict — so
    247:         # `{"types": {"NotAnId": 42}}` was REJECTED by markdown and ACCEPTED here. Two consumers
    248:         # of a CI-pinned file made that survivable; W4b binds three more, and W4c binds Godot.

### engine/substrate/keys.py:294-303   [logs: R1_engine]
  CLAIM: | `Key` | `substrate/keys.py:126` | id, type, emitted_at, source_actor, causes, targets, scale_signature, symbolic_dimensions, visibility, time_horizon, permanence, payload | any emitter (`echo_transport`, `articulation`, tests) | `TypeRegistry.apply_defaults` mutates scale_signature/permanence/time_horizon in place (keys.py:294-303) |
    293:                     current_list = entry[field]
  >>294:                 elif value.startswith("[") and value.endswith("]"):
    295:                     entry[field] = [v.strip() for v in value[1:-1].split(",") if v.strip()]
    296:                     current_list = None
    297:                 else:
    298:                     entry[field] = value
    299:                     current_list = None
    300:             # indented continuation lines of nested structures: ignored (not consumed)
    301:         return entry

### engine/substrate/keys.py:307   [logs: R1_engine]
  CLAIM: | `EmittedAt` | `substrate/keys.py:114` | season_index, sub_step_index | emitter (season_index) | `KeyLog.append` sets sub_step_index (keys.py:307) |
    306:         return self.types[type_id]
  >>307: 
    308:     def validate_payload(self, key: Key) -> None:

### engine/substrate/keys.py:319-419   [logs: R1_engine]
  CLAIM: **KeyLog** (keys.py:319-419): append-only. `append()` runs `_validate()` (keys.py:355-406), enforcing all **8 numbered invariants**:
    318:             raise KeyValidationError(
  >>319:                 f"key {key.id!r} type {key.type!r} missing required payload fields: {missing}"
    320:             )
    321: 
    322:     def apply_defaults(self, key: Key) -> None:
    323:         """Fill §1-template defaults for fields the emitter left empty."""
    324:         entry = self.require(key.type)
    325:         if not key.scale_signature and entry.get("default_scale_signature"):
    326:             key.scale_signature = list(entry["default_scale_signature"])

### engine/substrate/keys.py:319   [logs: R1_engine]
  CLAIM: | `KeyLog` | `substrate/keys.py:319` | registry, `_entries`, `_ids`, `_season_counters`, stat_vocabulary, stat_vocabulary_warnings | `echo_transport._registry()`-adjacent callers, tests | `append()` |
    318:             raise KeyValidationError(
  >>319:                 f"key {key.id!r} type {key.type!r} missing required payload fields: {missing}"
    320:             )

### engine/substrate/keys.py:355-406   [logs: R1_engine]
  CLAIM: **KeyLog** (keys.py:319-419): append-only. `append()` runs `_validate()` (keys.py:355-406), enforcing all **8 numbered invariants**:
    354:         # deliberately not alongside the hard invariants 1-8).
  >>355:         self.stat_vocabulary = stat_vocabulary
    356:         self.stat_vocabulary_warnings: list = []
    357: 
    358:     def __len__(self) -> int:
    359:         return len(self._entries)
    360: 
    361:     def __iter__(self):
    362:         return iter(self._entries)

### engine/substrate/keys.py:357-358   [logs: R1_engine]
  CLAIM: 1. unique id (keys.py:357-358)
    356:         self.stat_vocabulary_warnings: list = []
  >>357: 
    358:     def __len__(self) -> int:
    359:         return len(self._entries)

### engine/substrate/keys.py:379-392   [logs: PR344]
  CLAIM: §9.3 The Godot mechanics and the silent corruption: **M-5 · MAJOR** — `.tres` is the wrong save format, three reasons: (a) embeds script paths, breaks when a script moves; (b) `ResourceLoader` INSTANTIATES scripts — a save file becomes an execution surface the moment it's shared/edited; (c) the resource cache returns the same instance for a path — reload-after-save can hand back the pre-save objec
    378:     def _validate(self, key: Key) -> None:
  >>379:         # §2.3 invariant 1 — id unique across the log.
    380:         if key.id in self._ids:
    381:             raise KeyValidationError(f"duplicate key id {key.id!r} (§2.3 invariant 1)")
    382:         # invariant 2 + per-type payload contract (§2.3 preamble).
    383:         self.registry.validate_payload(key)
    384:         # invariant 3 — causes[] only references Keys already in the log.
    385:         for cause_id in key.causes:
    386:             if cause_id not in self._ids:

### engine/substrate/keys.py:389-392   [logs: PR344]
  CLAIM: **§3.4 Cycles** — `contain`(Rung→Rung→…) NO (this IS a change — cardinality stated over contain subjects generally, not Persons only; the prior brief scoped single-parent to Persons and licensed a multi-parent Rung graph while citing the same authority against it). `succeed∘contain`(Rung→Person→Rung) YES and it's the NORMAL case (heir lives in the hearth) — "the reference graph is not a DAG; every
    388:                     f"key {key.id!r} cites unknown cause {cause_id!r} (§2.3 invariant 3)"
  >>389:                 )
    390:         # invariant 4 (cycle-freedom) holds by construction for an append-only
    391:         # log whose causes[] may only cite already-logged Keys: no logged Key
    392:         # can ever gain an edge back to a later one (§4.6 BFS check subsumed).
    393:         # invariant 5 — ordering. season monotonicity is the scheduler's job;

### engine/substrate/keys.py:414-417   [logs: R1_engine]
  CLAIM: `content_hash()` (keys.py:418-419) is `sha256(serialize())` — `serialize()` (keys.py:414-417) is newline-joined per-Key sorted-key JSON — the determinism surface cited throughout the corpus as "key_log_hash".
    413:             raise KeyValidationError(f"key {key.id!r} has empty scale_signature (§2.3 invariant 7)")
  >>414:         for scale in key.scale_signature:
    415:             if scale not in SCALES:
    416:                 raise KeyValidationError(
    417:                     f"key {key.id!r} has non-canonical scale {scale!r} (§2.1)"
    418:                 )

### engine/substrate/keys.py:414-419   [logs: R1_engine]
  CLAIM: - **Replay/hashing**: `KeyLog.content_hash()` = `sha256(serialize())`, `serialize()` = newline-joined sorted-key JSON per Key in log order (keys.py:414-419). `CampaignResult.key_log_hash`/`keys_emitted` surface this per campaign (mc_v18.py:281-282). Two campaigns with the same seed and same code path produce byte-identical `key_log_hash` — pinned by `test_parliamentary_bridge.py` (`_ON_KEYLOG_HASH
    413:             raise KeyValidationError(f"key {key.id!r} has empty scale_signature (§2.3 invariant 7)")
  >>414:         for scale in key.scale_signature:
    415:             if scale not in SCALES:
    416:                 raise KeyValidationError(
    417:                     f"key {key.id!r} has non-canonical scale {scale!r} (§2.1)"
    418:                 )
    419:         # invariant 8 — exactly one visibility shape.
    420:         v = key.visibility

### engine/substrate/keys.py:418-419   [logs: R1_engine]
  CLAIM: `content_hash()` (keys.py:418-419) is `sha256(serialize())` — `serialize()` (keys.py:414-417) is newline-joined per-Key sorted-key JSON — the determinism surface cited throughout the corpus as "key_log_hash".
    417:                     f"key {key.id!r} has non-canonical scale {scale!r} (§2.1)"
  >>418:                 )
    419:         # invariant 8 — exactly one visibility shape.
    420:         v = key.visibility

### engine/substrate/keys.py:422-601   [logs: R1_engine]
  CLAIM: **TickScheduler** (keys.py:422-601): the emission/scheduling owner. Constructor **requires** `cascade_depth_max` and `emissions_per_tick_max` (no default — OF-CAP is an open fork, keys.py:428-437 docstring: "no fabricated constant enters the repo"). `emit()` (keys.py:460-471) is root emission (depth 0), raises `TerminationBreach` if called during a drain when `no_sync_reentry` is on. `schedule_emi
    421:         if v.public:
  >>422:             if v.semi_public_observers or v.private_observers:
    423:                 raise KeyValidationError(
    424:                     f"key {key.id!r}: public=true forbids observer lists (§2.3 invariant 8)"
    425:                 )
    426:         elif not (bool(v.semi_public_observers) ^ bool(v.private_observers)):
    427:             raise KeyValidationError(
    428:                 f"key {key.id!r}: public=false requires exactly one non-empty observer list "
    429:                 "(§2.3 invariant 8)"

### engine/substrate/keys.py:422   [logs: R1_engine]
  CLAIM: | `TickScheduler` | `substrate/keys.py:422` | log, caps, flags, subscriptions, `_queue`, `_pending_apply`, phase counters | `echo_transport.make_scheduler` (echo_transport.py:184) | `emit`/`schedule_emission`/`drain_tick`/`accounting_boundary`/`next_tick` |
    421:         if v.public:
  >>422:             if v.semi_public_observers or v.private_observers:
    423:                 raise KeyValidationError(

### engine/substrate/keys.py:428-437   [logs: R1_engine]
  CLAIM: **TickScheduler** (keys.py:422-601): the emission/scheduling owner. Constructor **requires** `cascade_depth_max` and `emissions_per_tick_max` (no default — OF-CAP is an open fork, keys.py:428-437 docstring: "no fabricated constant enters the repo"). `emit()` (keys.py:460-471) is root emission (depth 0), raises `TerminationBreach` if called during a drain when `no_sync_reentry` is on. `schedule_emi
    427:             raise KeyValidationError(
  >>428:                 f"key {key.id!r}: public=false requires exactly one non-empty observer list "
    429:                 "(§2.3 invariant 8)"
    430:             )
    431:         if key.time_horizon not in TIME_HORIZON_VALUES:
    432:             raise KeyValidationError(f"key {key.id!r} invalid time_horizon {key.time_horizon!r}")
    433:         if key.permanence not in PERMANENCE_VALUES:
    434:             raise KeyValidationError(f"key {key.id!r} invalid permanence {key.permanence!r}")
    435: 

### engine/substrate/keys.py:460-471   [logs: R1_engine]
  CLAIM: **TickScheduler** (keys.py:422-601): the emission/scheduling owner. Constructor **requires** `cascade_depth_max` and `emissions_per_tick_max` (no default — OF-CAP is an open fork, keys.py:428-437 docstring: "no fabricated constant enters the repo"). `emit()` (keys.py:460-471) is root emission (depth 0), raises `TerminationBreach` if called during a drain when `no_sync_reentry` is on. `schedule_emi
    459:     def content_hash(self) -> str:
  >>460:         return hashlib.sha256(self.serialize().encode("utf-8")).hexdigest()
    461: 
    462: 
    463: class TickScheduler:
    464:     """The engine_clock-shaped emission seam (propagation_spec §1 O.2, §4.2).
    465: 
    466:     Owns the tick-scoped queue, the cascade_depth meter (scheduler-internal,
    467:     NEVER written onto Keys — SSI-4), the Level-B termination guard, the B1

### engine/substrate/keys.py:506   [logs: PR338]
  CLAIM: | A338-026 | container/registry | `TickScheduler.subscribe` | `keys.py:506` | One production call site | BUILT | yes (minimal) |
    505:     # -- subscription ------------------------------------------------------
  >>506:     def subscribe(self, type_id: str, callback) -> None:
    507:         self.subscriptions.setdefault(type_id, []).append(callback)

### engine/substrate/keys.py:510   [logs: PR337,PR338]
  CLAIM: | A338-025 | container/registry | `TickScheduler.emit(key, apply=fn)` — deferred-apply channel | `keys.py:510` | stat_deltas collected/logged at emission, written at accounting boundary | BUILT | yes |
  CLAIM: | A337-174 | mechanic | `TickScheduler.emit(key, apply=fn)` — deferred-apply channel | `_v1_part2.md` §"Cross-Scale Plumbing" | Logged at emission, state written at accounting boundary — preserves same-tick `causes[]` chaining. | BUILT | yes, `keys.py:510` |
    509:     # -- emission paths ----------------------------------------------------
  >>510:     def emit(self, key: Key, apply=None) -> Key:
    511:         """Root emission (cascade_depth 0). §4.1 steps 1-2 + 5 (subset).

### engine/substrate/keys.py:519   [logs: PR341]
  CLAIM: | A341-056 | loop/phase | emission cap per tick | `keys.py:519,532,557,562,599` (`TerminationBreach`) | `DEFAULT_EMISSIONS_PER_TICK_MAX = 64`; cap **raises** an exception rather than clamping | VERIFIED TRUE — confirmed in keys.py | yes — live |
    518:         if self._in_drain and self.no_sync_reentry:
  >>519:             raise TerminationBreach(
    520:                 "synchronous re-entry: consumers must route new Keys through "

### engine/substrate/keys.py:525   [logs: PR337,PR338]
  CLAIM: | A337-175 | invariant/refusal | `TickScheduler.schedule_emission` — cascade path, switched off | `_v1_part2.md` §"Cross-Scale Plumbing" | `DEFAULT_CASCADE_DEPTH_MAX=0` — would raise a termination breach if it ever ran. | INERT | VERIFIED-TRUE, `keys.py:525`, cap at `echo_transport.py:102` |
  CLAIM: | A338-027 | invariant/refusal | `TickScheduler.schedule_emission` (cascade path) is switched OFF | `keys.py:525`; `echo_transport.py:102` | `DEFAULT_CASCADE_DEPTH_MAX = 0` — would raise a termination breach if triggered; zero production callers | INERT (deliberately) | no |
    524: 
  >>525:     def schedule_emission(self, key: Key, apply=None) -> None:
    526:         """B1 path: a consumer reacting to an observed Key enqueues its new Key

### engine/substrate/keys.py:538   [logs: PR340]
  CLAIM: | 1 | `drain_tick` has zero production callers; `next_tick` raises `TerminationBreach` on non-empty queue (01 pt2 §9.3, 09 pt2 §8, many others) | **VERIFIED-TRUE** — `engine/substrate/keys.py:538` (drain_tick, only called from `tests/valoria/test_key_substrate.py`), `:593-599` (next_tick raises) |
    537: 
  >>538:     def drain_tick(self) -> int:
    539:         """Drain the tick-scoped queue to empty (Theorem-B guarded). Returns

### engine/substrate/stubwire.py:39   [logs: R1_engine]
  CLAIM: | **StateChange := (subject, mode, driver)** | **NO** as a unified triple, **partial** as two separate unrelated mechanisms | (1) `Key` (mode-like via `type`, no subject/driver partition); (2) `stubwire.StubResult` (marks "not built" rather than modeling a change) | `substrate/keys.py:126`, `substrate/stubwire.py:39` | no | `Key` carries `source_actor`/`targets[]`/`type`, but nothing partitions ch
  CLAIM: | `StubResult` | `substrate/stubwire.py:39` (frozen) | module, io_contract, reason, `stub: bool` (init=False, always True) | `stub_resolve()` only | immutable |
    38: 
  >>39: from dataclasses import dataclass, field
    40: 

### engine/tests/test_combat_bridge_seam.py:44   [logs: PR340]
  CLAIM: | 7 | Nothing in production constructs `ctx['factions']`; only 2 test call sites hardcode `("Crown","Church")` (12 §3.1) | **VERIFIED-TRUE** — grep across `engine/` and `systems/` finds only `engine/tests/test_pipeline_reach.py:277,756` and `engine/tests/test_combat_bridge_seam.py:44+` |
    43:     w = _world_with_factions(mil_a=4.0, mil_b=6.0)
  >>44:     parts = combat_bridge.derive_parties({"factions": ("Crown", "Church")}, w)
    45:     assert parts is not None

### engine/tests/test_mc_v18_regression.py:126-129   [logs: R1_engine]
  CLAIM: - **Seeded regression goldens**: `engine/tests/test_mc_v18_regression.py` pins `run_batch(n=2, base_seed=0)` output (`GOLDEN_WIN_SHARE`, `GOLDEN_WINNERS`, `GOLDEN_BATTLES_MEAN` — test_mc_v18_regression.py:126-129) and asserts determinism (`test_mc_v18_batch_is_deterministic`, line 132: two runs of the same seed must be `==`). `engine/tests/test_f7_smoke_oracle.py` pins `run_batch(n=8, base_seed=42
    125: # 50pp steps, so it is the LEAST sensitive of the three pins and its stability here is not evidence.
  >>126: GOLDEN_WIN_SHARE = {'Crown': 50.0, 'Church': 0.0, 'Hafenmark': 0.0, 'Varfell': 50.0}
    127: GOLDEN_WINNERS = {'Crown': 1, 'Varfell': 1}
    128: # RE-PINNED 2026-08-21, M1 juncture 1: fractional dice pools (ED-IN-0187). `sigma_leverage.roll_net_continuous` no longer rounds its pool, so every sampled value changes and the RNG stream diverges. NOT a balan
    129: GOLDEN_BATTLES_MEAN = 36.0
    130: 

### engine/tests/test_pipeline_reach.py:277   [logs: PR340]
  CLAIM: | 7 | Nothing in production constructs `ctx['factions']`; only 2 test call sites hardcode `("Crown","Church")` (12 §3.1) | **VERIFIED-TRUE** — grep across `engine/` and `systems/` finds only `engine/tests/test_pipeline_reach.py:277,756` and `engine/tests/test_combat_bridge_seam.py:44+` |
    276:     slot = scene_slate.SceneSlot(scene_type="combat",
  >>277:                                   context={"factions": ("Crown", "Church")}, priority=0)
    278:     res = scene_dispatch._resolve_slot(slot, world, world.rng)

### .github/workflows/valoria-ci.yml:126-150   [logs: R3_godot]
  CLAIM: `tools/export_*.py` and a blocking `--check` mode wired into `.github/workflows/valoria-ci.yml:126-150`
    125:           v python3 tools/ci_names_consistency.py
  >>126:           v python3 tools/export_engine_params.py --check
    127:           v python3 tools/export_key_types.py --check
    128:           # ED — the writer half of the ttrpg -> Godot constants bridge. Blocking for the same
    129:           # reason as its two siblings: it is deterministic and has no external pins. Its READER
    130:           # half lives in the jordanelias/valoria-game repo, as a constants-parity checker run by
    131:           # its own `Constants Parity (vs design oracle)` CI job, and cannot be
    132:           # gated from here — that cross-repo hop is the residual, recorded in
    133:           # proposals/2026-08-20-return-to-game-plan-v1.md Act C1.

### references/atomization_rules.yaml:135   [logs: R4_registries]
  CLAIM: | **`root`** | graph sense `sovereign_fraction(root)`, provenance "root token", `conferral_path` reaching root (3) | Repo-native organizational sense not in the design's table: **"root" = repository root / root-level file**, used constantly (e.g. `atomization_rules.yaml:135` "root index + lane files", `id_reservations.yaml:117` "root-cause audit", `canonical_sources.yaml:471` "root PP-686 doc"). L
    134:   # ────────────────────────────────────────────────────────────
  >>135:   # HANDOFF / CONTINUITY — root index + lane files
    136:   # ────────────────────────────────────────────────────────────

### references/canonical_sources.yaml:471   [logs: R4_registries]
  CLAIM: | **`root`** | graph sense `sovereign_fraction(root)`, provenance "root token", `conferral_path` reaching root (3) | Repo-native organizational sense not in the design's table: **"root" = repository root / root-level file**, used constantly (e.g. `atomization_rules.yaml:135` "root index + lane files", `id_reservations.yaml:117` "root-cause audit", `canonical_sources.yaml:471` "root PP-686 doc"). L
    470: # LPS-2+ consumer wiring (2026-05-30): faction_behavior/faction_canon/faction_state_authoring/stats_1_7_scale/derived_stats wired to settlement-grain + size-weighted Mandate; settlement_layer §1.8 (LPS-2e) is a
  >>471: # LPS-2+ wiring follow-up (2026-05-30): faction_behavior §3.4/§3.5/§3.6 terminology territory->settlement (root PP-686 doc fully consistent with §1.8).
    472: # LPS-2e audit-patch (2026-05-30): completed settlement re-grain across all consumers (faction_behavior §4 Mandate block, faction_canon 6-stat resolution + Excommunication, stats_1_7_scale Church L-strip + seiz

### references/ci_checks_registry.yaml:124-495   [logs: R6_execution]
  CLAIM: lines (`references/ci_checks_registry.yaml:124-495`, ~28 roles documented) and
    123:     posture: harden
  >>124:     role: "Token-threshold enforcement on governed files (session log, ledgers, registers, canonical_sources)."
    125:     ci_job: validators
    126:     notes: "External enforcement gate; runs outside Claude."
    127: 
    128:   # RETIRED culling wave 3 (ED-IN-0194, 2026-08-21): tools/ci_hooks_verifier.py
    129: 
    130:   - path: tools/ci_co_file_checker.py
    131:     level: 5

### references/ci_checks_registry.yaml:466   [logs: R4_registries]
  CLAIM: | **`condition`** | `condition(site)`, convening condition, stasis-ladder "named condition" (3) | Generic-English uses only (`ci_checks_registry.yaml:466` "pre-existing condition"); no additional load-bearing repo sense found. | Sufficient. |
    465:       a bare --check has no baseline and would red on arrival, refusing unrelated authors' commits
  >>466:       for a pre-existing condition. ED-IN-0112 paid for that mistake once. Carved OUT of culling
    467:       wave 1 by Jordan's ruling 2026-08-21 — wave 1 as ratified deleted it, one day before §0.2

### references/descriptor_registry.yaml:16-19   [logs: R4_registries]
  CLAIM: | **`kind`** | `Rung.kind`, `Tenure.kind`, mark/need/stance-referent kind, `Record.kind`, `MatterKind` (7) | **Two additional repo-native taxonomy fields not cross-checked**: (a) `references/descriptor_registry.yaml:16-19` — the registry's own `KIND:` enum (`attribute_scalar | attribute_aggregate | faction_stat | settlement_stat | practitioner_stat | territory_stat | conviction_weight | ethical_ax
    15: #
  >>16: # KIND: attribute_scalar | attribute_aggregate | faction_stat | settlement_stat |
    17: #       practitioner_stat | territory_stat | conviction_weight | ethical_axis |
    18: #       conviction_axis_map | orientation_scalar | contest_style | temperament_type | template |
    19: #       personal_track
    20: #   practitioner_stat and territory_stat added 2026-07-08 (ED-IN-0029 docket, OPT-AV-3, D2/D3):

### references/descriptor_registry.yaml:49-58   [logs: PR344]
  CLAIM: ### §12 PRECEDENT APPENDIX — what the executable substrate already does (8 rows, all "precedents to copy, not claims the design uses them"): no id on any record→`Key.id:str`(`keys.py:145`); no uniqueness check→invariant 1(`:379-381`); no referential integrity→invariant 3(`:384-388`); cycles in a citation graph→cycle-freedom by construction(`:389-392`); no lookup by id→`lookup(key_id)`(`:363-365`);
    48:   body:
  >>49:     - {key: attr.body.strength,  name: Strength,  aliases: []}
    50:     - {key: attr.body.endurance, name: Endurance, aliases: [Vitality]}
    51:     - {key: attr.body.agility,   name: Agility,   aliases: [Dexterity]}
    52:   mind:
    53:     - {key: attr.mind.focus,  name: Focus,  aliases: []}
    54:     - {key: attr.mind.acuity, name: Acuity, aliases: [Reasoning, Cognition]}   # [ASSUMPTION] legacy 'Cognition' folds to Acuity -- Jordan veto
    55:     - {key: attr.mind.will,   name: Will,   aliases: [Spirit]}                  # 'spirit move': formerly standalone Spirit; legacy formulas resolve via alias
    56:   social:

### references/descriptor_registry.yaml:58   [logs: R4_registries]
  CLAIM: | **`presence`** | Query `presence(prop,c)`, "deposits by presence", `enforcer_presence` (3) | **Additional, high-value repo meaning**: `references/glossary.md:61` — **`Presence`** was the original name of the Core Attribute now aliased to `Charisma` in `descriptor_registry.yaml:58` (`attr.social.charisma... aliases: [Influence, Presence]`). This is a THIRD live meaning (a per-character attribute 
    57:     - {key: attr.social.attunement, name: Attunement, aliases: [Perception]}
  >>58:     - {key: attr.social.charisma,   name: Charisma,   aliases: [Influence, Presence]}
    59:     - {key: attr.social.bonds,      name: Bonds,      aliases: [Sincerity, Affability]}

### references/descriptor_registry.yaml:284   [logs: R4_registries]
  CLAIM: | **`Derived`** | design's retired query-category name | See §3 — **CONFIRMED, three independent live sources** (`glossary.md:65-82`, `params_tables.yaml`, `descriptor_registry.yaml:284`). | N/A — already resolved by rename. |
  CLAIM: **`references/descriptor_registry.yaml:284`** — the `not_descriptors.derived_values:` list, a **flat array of 20 bare strings** (`[Health, Stamina, Composure, Concentration, Thread Fatigue, Resolve, Garrison Strength, Local Economy, Mandate, Wound Interval, Max Wounds, Face, TroopCount, "Legitimacy (faction, derived)", Treasury, Levies Available, Reputation, "Discipline (faction)", Intelligence Ho
    283: not_descriptors:
  >>284:   derived_values: [Health, Stamina, Composure, Concentration, Thread Fatigue, Resolve, Garrison Strength, Local Economy, Mandate, Wound Interval, Max Wounds, Face, TroopCount, "Legitimacy (faction, derived)", T
    285:   tracks:  [Piety, Disposition, Renown, Standing, Persuasion, Coherence, Warden Recognition]

### references/descriptor_registry.yaml:315-317   [logs: R4_registries]
  CLAIM: - `HANDOFF_IN.md` (3888 lines — by far the largest): dozens of open architecture items; most load-bearing: ED-1051 (module-contract gaps blocking Gate-0/`engine_clock`), the armature §5 fork docket, D15 (`contracts_bucket`<->registry-KIND crosswalk, explicitly REJECTED/not filed per `descriptor_registry.yaml:315-317`).
    314:     (D5's derived companion, no `derived.*` KIND exists in this file so it's a plain name here, same
  >>315:     treatment as the other derived_values). Two proposed deltas remain REJECTED, not filed, by both
    316:     this pass and the prior ratification pass: D11 (`pool.knot`/`track.persuasion` cross-link -- no
    317:     linkage exists at either cited source) and D15 (`contracts_bucket`<->KIND crosswalk field --
    318:     `not_descriptors` carries no KIND field to cross against). Also flagged, not fixed: the Coherence

### references/id_reservations.yaml:117   [logs: R4_registries]
  CLAIM: | **`root`** | graph sense `sovereign_fraction(root)`, provenance "root token", `conferral_path` reaching root (3) | Repo-native organizational sense not in the design's table: **"root" = repository root / root-level file**, used constantly (e.g. `atomization_rules.yaml:135` "root index + lane files", `id_reservations.yaml:117` "root-cause audit", `canonical_sources.yaml:471` "root PP-686 doc"). L
    116:     # ED-MB-0001: movement/pathing audit (ED-1096) fix-plan execution + adversarial review, 2026-07-02
  >>117:     # ED-MB-0002: Cannae gauge-band root-cause audit, ratified + DG-3/DG-4 ruled 2026-07-04
    118:     # ED-MB-0003: D1-D4 defects found+fixed, DG-1/DG-3 completion + DG-2-as-workplan ruled+executed, 2026-07-05

### references/id_reservations.yaml:234   [logs: R4_registries]
  CLAIM: | **`degree`** | commitment degree 0-5, degree-of-success band, knot `depth` (3) | Consistent with, not additional to, the design's "degree band" sense: `id_reservations_history.md:96` "ED-IN-0170 — degree-vocabulary equivalence census" and `id_reservations.yaml:234` "0187 degree ladder" are the SAME degree-of-success-band collision the repo has already been actively resolving (ED-SC-0031/0032, `H
  CLAIM: **Every architecture/keys/vocabulary/Godot entry regardless of date** (beyond the above): ED-IN-0192 "resolver architecture" and ED-IN-0193 "§5-§7 restore" (`id_reservations.yaml:234`, dates not in the 08-25+ window but architecture-subject); ED-GO-0001 (GO lane ACTIVE, `CURRENT.md:40`); ED-1051 (module-contract gaps, `needs_jordan`, cited repeatedly as blocking Gate-0 and `engine_clock`'s `doc:` 
    233:     # needs_jordan 79->97). status resolved, no ruling needed. next_free 72->73.
  >>234:     IN: { name: "Infrastructure",      next_free: 202 }   # DELIBERATE GAP: CSO holds 0103-0111 (pinned by tests/valoria/test_id_reservations_walkback.py, which requires that string to stay in THIS file). Lates
    235:     # [ED-IN-0064 DUP-KEY REPAIR] A stale duplicate `IN:` mapping once shadowed the authoritative one above (YAML last-key-wins). Neutralized; full incident write-up, including why a duplicate key is silent rat

### references/module_contracts.yaml:105-120   [logs: R4_registries]
  CLAIM: | `Act` / `Event` / `Claim` / `View` / `Sensation` / `World` | `Act` — generic English only (§6). `Event` — module_contracts.yaml uses `mechanical.*`/`scene.*`/`state.*` event **type strings**, not a first-class `Event` record. `Claim` — one hit, `module_contracts.yaml:1182` "Baralta Crown Claim" (a narrative-flavor term for a succession contest, unrelated meaning). `View` — see §6, third meaning 
    104:     target: "systems.factions.sim.parliamentary_transfer:derive_transfer_candidate"
  >>105:     needed_by: "engine/cross_scale/parliamentary_bridge.py — OI-04 derivation of (initiator, territory, mode)"
    106:     note: "MOVED here from the bridge at S5a. It reads only parliamentary_transfer's own CB machinery (_available_cb / _MODE_CB / PARL_LAST_TERRITORY_FLOOR / MODES), so the engine was reaching into a subsystem'
    107:   territory_transfer_proposal:
    108:     target: "systems.factions.sim.parliamentary_transfer:propose_transfer"
    109:     needed_by: "engine/cross_scale/parliamentary_bridge.py — the §§1-4 CB-gated transfer motion itself"
    110: 
    111:   # ── World composition: the eleven seams inside engine/autoload/game_state.py (S5a, 2026-08-22) ─
    112:   # These were FUNCTION-LOCAL imports, which is why they were invisible to the first ratchet and

### references/module_contracts.yaml:111   [logs: R4_registries]
  CLAIM: | `Act` / `Event` / `Claim` / `View` / `Sensation` / `World` | `Act` — generic English only (§6). `Event` — module_contracts.yaml uses `mechanical.*`/`scene.*`/`state.*` event **type strings**, not a first-class `Event` record. `Claim` — one hit, `module_contracts.yaml:1182` "Baralta Crown Claim" (a narrative-flavor term for a succession contest, unrelated meaning). `View` — see §6, third meaning 
    110: 
  >>111:   # ── World composition: the eleven seams inside engine/autoload/game_state.py (S5a, 2026-08-22) ─
    112:   # These were FUNCTION-LOCAL imports, which is why they were invisible to the first ratchet and

### references/module_contracts.yaml:171-178   [logs: R4_registries]
  CLAIM: | **`kind`** | `Rung.kind`, `Tenure.kind`, mark/need/stance-referent kind, `Record.kind`, `MatterKind` (7) | **Two additional repo-native taxonomy fields not cross-checked**: (a) `references/descriptor_registry.yaml:16-19` — the registry's own `KIND:` enum (`attribute_scalar | attribute_aggregate | faction_stat | settlement_stat | practitioner_stat | territory_stat | conviction_weight | ethical_ax
    170:     needed_by: "engine/cross_scale/scene_dispatch.py — builds the contest the resolver then runs"
  >>171:   # The two side LABELS the verdict is compared against. `kind: value` because they are module
    172:   # constants ("a"/"b", contest/contract.py:7), not callables — the only two `value` rows in this
    173:   # block, and the reason that kind exists at all. The engine must not hardcode a subsystem's
    174:   # vocabulary: rename them in `contest` and the exporter's blocking gate catches it, where a
    175:   # literal "a" in scene_dispatch.py would silently start comparing false.
    176:   contest_side.a:
    177:     target: "systems.social_contest.sim.contest:A"
    178:     kind: value

### references/module_contracts.yaml:331   [logs: PR341]
  CLAIM: | `state.belief_revised` | **EXISTS**, reserved for the creed beat · attribution conflict `[OPEN — Jordan]` at `module_contracts.yaml:331` |
  CLAIM: | A341-005 | key | `state.belief_revised` | `module_contracts.yaml:331` | Reserved for the creed beat; attribution conflict `[OPEN — Jordan]` | EXISTS, OPEN | no |
    330:       - {type: "state.concern_resolved", terminal: false}   # Procedure B (doc-12 §3, §7.4)
  >>331:       - {type: "state.belief_revised", terminal: false}   # Procedure B Resolution (doc-12 §8) — registry attributes this type to fieldwork: attribution conflict [OPEN — Jordan]
    332:       - {type: "scene.displacement", terminal: false}   # DA Proposal Phase, displacement_neglect_observed (doc-12 §8 verbatim) — IN registry (ED-935, key_type_registry_v30 §5, 2026-06-14; emitting_systems: [np

### references/module_contracts.yaml:343   [logs: PR340]
  CLAIM: Seven closed term kinds a `term_i` may read: gauge band, form value, tag existence, **tag age** (added — `season − tag.created_season` against a declared window; needed to make the ratified COLLISION arc shape expressible, since two grievances 3 seasons apart vs 30 seasons apart must read differently), post holder, edge state, season index. **`01 §5.2`'s own draft had a `progress` Gauge row — CUT*
    342:       - {name: "concerns", bucket: track, writable: true}   # doc-12 §3 generation/resolution
  >>343:       - {name: "projects", bucket: clock, writable: true}   # bucket track->clock [verification A4: 0->10 monotonic progress toward completion/stall threshold] doc-12 §4 advancement/stall
    344:       - {name: "arc state", bucket: clock, writable: true}   # bucket track->clock [verification A4: one-way FSM, advances to terminal arc phase] npc_behavior §5 state machine

### references/module_contracts.yaml:380-386   [logs: PR341]
  CLAIM: | A341-021 | container/registry | `npc_memory` module | `module_contracts.yaml:380-386` | Declared `doc: null`, no sim | ABSENT/unbuilt — re-verified | no |
  CLAIM: | A341-080 | module/subsystem | `npc_memory` | `module_contracts.yaml:380-386` | `doc: null`, no sim — unbuilt | ABSENT, re-verified by critics | no |
    379: 
  >>380:   - module: npc_memory
    381:     registry_system: "npc_memory"   # Key Type Registry vocabulary
    382:     doc: null
    383:     sim_module: none   # OI-54 (ED-IN-0097, W4): doc:null/no-sim per the W4 preflight; re-verified
    384:       # (find/grep for npc_memory* across the tree returns nothing).
    385:     scales: [personal]
    386:     resolver: state_reader   # [ASSUMPTION] Memory store written from Keys; queried by Procedures (doc-12 §2.3 bridge)
    387:     consumes:

### references/module_contracts.yaml:388   [logs: PR341]
  CLAIM: | A341-019 | key | `scene.gossip` | `module_contracts.yaml:388` | Consumed by `npc_memory`; nothing connects it to a Holding | orphaned edge | no (npc_memory unbuilt) |
    387:     consumes:
  >>388:       - {type: "scene.gossip", from: [npc_behavior]}
    389:       - {type: "scene.interaction", from: [npc_behavior]}

### references/module_contracts.yaml:630   [logs: PR341]
  CLAIM: | `scene.witness` | **EXISTS** as an emission — `module_contracts.yaml:630` (`scene_slate`) · consumer edge unclaimed |
  CLAIM: | A341-004 | key | `scene.witness` | `module_contracts.yaml:630` | Emitted by `scene_slate`; consumer edge unclaimed | EXISTS as emission | partial — no consumer |
    629:       - {type: "scene.threat", terminal: false}
  >>630:       - {type: "scene.witness", terminal: false}
    631:     state: []

### references/module_contracts.yaml:1128-1136   [logs: PR344]
  CLAIM: §8.5 The bridge nobody has noticed — **O-2 · OBSERVATION** — this design SUPPLIES the canon `engine_clock` has never had: `references/module_contracts.yaml:1128-1136` shows `doc:null`, `sim_module:none`, with a comment recording `engine/autoload/season_manager.py` advances the season counter but emits neither declared Key type — the temporal-spine gap `CLAUDE.md` §6 names. "CALENDAR·MATTER·DELIBER
  CLAIM: Status: PROPOSED. Owns "how one season executes on the primitives." Adds no primitive. ⚠ **This document IS the `engine_clock` contract, and it has never existed** — `references/module_contracts.yaml:1128-1136` carries `engine_clock` with `doc: null` (one of nine modules with no home design doc), and `CLAUDE.md` §6 names `engine_clock` as where the port must start. The six steps plus the §9 write 
    1127: 
  >>1128:   - module: engine_clock
    1129:     registry_system: "engine_clock"   # Key Type Registry vocabulary
    1130:     doc: null
    1131:     sim_module: none   # OI-54 (ED-IN-0097, W4): doc:null/no-sim per the W4 preflight; re-verified —
    1132:       # grep for mechanical.season_change / mechanical.accounting Key-type strings across
    1133:       # systems/+engine/ finds no emitter. engine/autoload/season_manager.py advances the season
    1134:       # counter but does not emit either declared Key type — the temporal-spine gap CLAUDE.md §6
    1135:       # names (doc: stays null pending ED-1051, §5 row 3 of this program's Jordan docket).

### references/module_contracts.yaml:1128-1160   [logs: R3_godot]
  CLAIM: contract, and it holds up well but is not a free lunch.** `module_contracts.yaml:1128-1160` (verified
    1127: 
  >>1128:   - module: engine_clock
    1129:     registry_system: "engine_clock"   # Key Type Registry vocabulary
    1130:     doc: null
    1131:     sim_module: none   # OI-54 (ED-IN-0097, W4): doc:null/no-sim per the W4 preflight; re-verified —
    1132:       # grep for mechanical.season_change / mechanical.accounting Key-type strings across
    1133:       # systems/+engine/ finds no emitter. engine/autoload/season_manager.py advances the season
    1134:       # counter but does not emit either declared Key type — the temporal-spine gap CLAUDE.md §6
    1135:       # names (doc: stays null pending ED-1051, §5 row 3 of this program's Jordan docket).

### references/module_contracts.yaml:1182   [logs: R4_registries]
  CLAIM: | `Act` / `Event` / `Claim` / `View` / `Sensation` / `World` | `Act` — generic English only (§6). `Event` — module_contracts.yaml uses `mechanical.*`/`scene.*`/`state.*` event **type strings**, not a first-class `Event` record. `Claim` — one hit, `module_contracts.yaml:1182` "Baralta Crown Claim" (a narrative-flavor term for a succession contest, unrelated meaning). `View` — see §6, third meaning 
    1181:       - {name: "Coup posture", bucket: track, writable: true}   # §1.1d/§2.1 (Löwenritter Grand Master row): "Coup Counter mechanisms (original factions_ttrpg_v30 §8.9)"; classification per §10.1 ED-POL-09 ("ty
  >>1182:       - {name: "Succession status", bucket: track, writable: true}   # §1.0 intro: Leadership is reached "via the succession mechanisms in Part 2 of the original register (SUC-01-03, LIN-01-04)"; per-faction St
    1183:     transitions: []

### references/module_contracts.yaml:1418   [logs: PR340]
  CLAIM: | 15 | `references/module_contracts.yaml:1418` still annotates `mechanical.project_advanced`/`state.project_failed`/`state.project_completed` `[unreg]` while `:362`/`:364` record ED-935 registered them 2026-06-14 — a self-contradiction (09 pt2 §10.1) | **VERIFIED-TRUE** — both the `[unreg]` comments and the SUPERSEDED notes found exactly as cited |
    1417:   - phase: C_project
  >>1418:     does: "Procedure C: mechanical.project_advanced [unreg]; state.project_failed [unreg] (stall >= 8); state.project_completed [unreg]"
    1419:     source: "doc-12 §8"

### references/module_contracts.yaml:1543   [logs: R4_registries]
  CLAIM: | `Person` (carrier) | **NO** — zero hits for `\bPerson\b` in references/registers/canon | — | — | **`references/module_contracts.yaml:1543` is the closest existing statement of this exact gap**: *"No Character/Actor dataclass in World; 9- vs 10-attribute rival rosters, neither wired. Every personal-scale port needs this first."* `descriptor_registry.yaml` would need a `domain: person` (it current
  CLAIM: 1. **No carrier-level registry exists.** `descriptor_registry.yaml` registers attributes/stats *of* an actor/settlement, never the actor/settlement *as a typed entity*. There is no `carriers.yaml` or equivalent. `module_contracts.yaml:1543` names this exact gap for the Person/Character case.
    1542:     godot: no-oracle
  >>1543:     note: No Character/Actor dataclass in World; 9- vs 10-attribute rival rosters, neither wired. Every
    1544:       personal-scale port needs this first.

### references/name_collision_database.yaml:63   [logs: R4_registries]
  CLAIM: | **`act`** | record `Act`, `remit.acts`, currency sense (3) | `references/name_collision_database.yaml:63` — `CB: {term: "Casus Belli"..., silo: 11}` glosses CB as "standing right to **act**" (English verb only, not a collision). No additional binding sense. | Sufficient. |
    62:   AP:  {term: "Church Attention Pool", silo: 9,  status: enforced}    # ci_political
  >>63:   CB:  {term: "Casus Belli", silo: 11, status: enforced}             # faction_layer §3.5 — standing right to act
    64:   POI: {term: "Points of Interest", silo: 8,  status: enforced}      # investigation_systems

### references/name_collision_database.yaml:421-425   [logs: PR337]
  CLAIM: | A337-106 | gap | "Sanction" naming collision | `_v1.md` §3 | `name_collision_database.yaml:421-425` prescribes renaming Authority pressure point to "Sanction" — collides with live 5-tier ladder. | gap | n/a |
    420: 
  >>421:   - term: "Authority (pressure point)"
    422:     replacement: "Sanction"
    423:     date: "2026-05-08"
    424:     reason: "Collision with Conviction #2 Authority"
    425:     ambiguous: true
    426: 

### references/names_index.yaml:30-32   [logs: PR344]
  CLAIM: ### §12 PRECEDENT APPENDIX — what the executable substrate already does (8 rows, all "precedents to copy, not claims the design uses them"): no id on any record→`Key.id:str`(`keys.py:145`); no uniqueness check→invariant 1(`:379-381`); no referential integrity→invariant 3(`:384-388`); cycles in a citation graph→cycle-freedom by construction(`:389-392`); no lookup by id→`lookup(key_id)`(`:363-365`);
    29: #     context:   [<regex terms>]  — OPTIONAL. When the `canonical` display collides with a
  >>30: #                  common English word (Faith / Order / Reason / …), the disambiguation terms
    31: #                  that must co-occur for a prose hit to count as THIS definition. The single
    32: #                  home for the §3.5 disambiguation gate — vector_audit reads it FROM here
    33: #                  (CLAUDE.md §8), migrated verbatim from the prior hardcoded SEED table so P2

### systems/characters/sim/beliefs.py:49-58   [logs: PR341]
  CLAIM: | A341-037 | class/type | `Belief` (engine class) | `systems/characters/sim/beliefs.py:49-58` | Per-actor rows with `position ∈ strong/wavering/revised` | LIVE, zero decision-bearing importers except snapshot/storage role | yes — code exists, VERIFIED |
    48: 
  >>49: @dataclass
    50: class Belief:
    51:     """A single Belief held by an actor."""
    52:     belief_id: str
    53:     actor: str
    54:     statement: str             # Free-text canonical statement
    55:     position: str              # Current orientation ('strong' / 'wavering' / 'revised')
    56:     underlying_convictions: list[str] = field(default_factory=list)  # Conviction names from taxonomy

### systems/characters/sim/beliefs.py:50   [logs: R2_systems]
  CLAIM: | `Belief` | `systems/characters/sim/beliefs.py:50` | `belief_id, actor, statement, position, underlying_convictions[], revision_pressure, history[]` | characters -- closest `Claim`-shaped per-actor durable record (NOT per-witnessing; durable & mutable, not recomputed) |
  CLAIM: | **Claim** | `Belief` (durable, mutable per-actor opinion) + NPC `Memory` (per-Key salience reference, 4.3-4.4 of key_substrate) | `systems/characters/sim/beliefs.py:50`; `key_substrate_v30.md:266-309` | partial | `Belief` is durable/mutable, not recomputed-per-witnessing like the design's `Claim`; NPC Memory's `memory_query()` IS closer -- an indexed, queryable, NOT globally-stored-as-truth per-
    49: @dataclass
  >>50: class Belief:
    51:     """A single Belief held by an actor."""

### systems/characters/sim/beliefs.py:90   [logs: R2_systems]
  CLAIM: | `RevisionResult` | `systems/characters/sim/beliefs.py:90` | actor, belief_id, accepted, old/new position, momentum_delta, notification_sent, reason | characters |
    89: @dataclass
  >>90: class RevisionResult:
    91:     actor: str

### systems/characters/sim/beliefs.py:121   [logs: PR341]
  CLAIM: | A341-038 | function/signature | `add_belief(actor, belief_id, statement, position='strong', underlying_convictions=None, world=None)` | `systems/characters/sim/beliefs.py:121` | Entry point to add a Belief | VERIFIED TRUE | yes |
    120: 
  >>121: def add_belief(actor: str, belief_id: str, statement: str,
    122:                position: str = 'strong',

### systems/characters/sim/beliefs.py:140   [logs: PR341]
  CLAIM: | A341-039 | function/signature | `revise_belief(actor, belief_id, new_position, evidence=None, world=None)` | `systems/characters/sim/beliefs.py:140` | Entry point to revise a Belief | VERIFIED TRUE | yes |
    139: 
  >>140: def revise_belief(actor: str, belief_id: str, new_position: str,
    141:                   evidence: Any = None,

### systems/characters/sim/conviction.py:59   [logs: PR337,PR338]
  CLAIM: | A337-151 | class/type | Conviction roster (13, single owner since 2026-08-24) | `_v1_part2.md` §"People" | registry→export→`descriptors.CONVICTIONS`; unknown name now raises rather than scoring 0 silently. | BUILT | yes, `engine/substrate/descriptors.py`, consumed at `conviction.py:59` |
  CLAIM: | A338-114 | primitive | Conviction roster (13, single owner since 2026-08-24) | `descriptors.py`; `conviction.py:59` | Faith/Authority/Order/Scholastic/Utility/Equity/Liberty/Precedent/Community/Identity/Warden/Virtue/Honor; unknown name now RAISES | BUILT | yes |
    58: # [canonical: conviction_taxonomy_v30 §2 via references/descriptor_registry.yaml:conviction_roster]
  >>59: CONVICTIONS = descriptors.CONVICTIONS
    60: 

### systems/characters/sim/conviction.py:119   [logs: R2_systems]
  CLAIM: | `ConvictionState`, `ConvictionThresholdState`, `ScarRecord` | `systems/characters/sim/conviction.py:119,160,94` | per-actor conviction state | characters |
    118: @dataclass
  >>119: class ConvictionState:
    120:     """Per-actor conviction-track state per §2."""

### systems/characters/sim/conviction.py:177-241   [logs: PR338]
  CLAIM: | A338-116 | mechanic | `apply_conviction_scar` | `conviction.py:177-241` | Correctly implements per-conviction scar ladder (PP-718); only caller `knots.apply_knot_loss` itself has zero production callers — unreachable end to end | INERT (2-deep) | code correct, unreachable |
    176: 
  >>177: def apply_conviction_scar(actor: str, source: str, magnitude: int,
    178:                           conviction: Optional[str] = None,
    179:                           certainty: int = 3,   # [canonical: §2 — default certainty tier]
    180:                           season: int = 0,
    181:                           world=None) -> ScarRecord:
    182:     """Apply a Scar on a specific Conviction.
    183: 
    184:     conviction: which Conviction is Scarred. Required per PP-718 (per-

### systems/combat/combat_engine_v1/config.py:273   [logs: PR337]
  CLAIM: | A337-154 | class/type | Disposition (-5..+5) — designed universal scalar, NO code implements it | `_v1_part2.md` §"People" | Nearest runtime relative is `affiliation_loyalty` (0-3), different range; separate homonym in `combat_engine_v1/config.py:273` (1-7 aggression temperament). | DESIGNED | no |
    272:   INDES_COMMIT_K=0.4, INDES_READ_K=0.15, INDES_SCALE_FLOOR=0.5, INDES_SCALE_CEIL=2.0,
  >>273:   # disposition (temperament, aggression axis; base param disp 1-7, 4=neutral, lean=(disp-4)/3). Orthogonal to tradition
    274:   # (tradition=competence, disposition=propensity). THREE hooks: (1) COMMIT skew — aggressive leans deep (4,5), cautious

### systems/combat/combat_engine_v1/core.py:57   [logs: PR337]
  CLAIM: | `combat_engine_v1/core.py` still defines its own `degree(net, ob)` function separate from the single-owned ladder (the declared HELD exception) | A337-006 | **VERIFIED-TRUE** — `systems/combat/combat_engine_v1/core.py:57 def degree(net, ob)` exists as a distinct function from `dice_engine.degree_from_net` |
  CLAIM: | A337-006 | invariant/refusal | Degree-ladder single-owner guard, with one declared HELD exception | `cross_scale_action_catalogue_v1.md` §0.1 [pre-existing] | `tests/valoria/test_degree_ladder_single_owner.py` enrols 8/9 ladders on the shared owner; `combat_engine_v1/core.py:degree` is deliberately HELD on the pre-2026-08-14 bands. | canon | yes — VERIFIED-TRUE, `combat_engine_v1/core.py:57 def 
    56: def roll_net(pool, rng): return SL.roll_net_continuous(pool, TN, rng=rng)
  >>57: def degree(net, ob):
    58:     """⚠ HELD AT THE PRE-2026-08-14 LADDER, DELIBERATELY. Everything below this paragraph describes

### systems/factions/sim/absolution.py:33-107   [logs: PR337]
  CLAIM: | A337-071 | formula | Absolution formula, `[M6_ASSUMPTION_ONE]` unratified Ob | `_v1.md` §3 | `Pool=int(Church.I)`; `Ob=3` self-flagged unratified; `Church.L−1` regardless; Failure decrements `Church.standing`, not `L`. | BUILT | yes, `absolution.py:33-107` |
    32: # ── M6_ASSUMPTION_ONE constants ──────────────────────────────────────────────
  >>33: ABSOLUTION_OB = 3  # [M6_ASSUMPTION_ONE: faction_canon §8.2 specifies +1 Stab effect, M6 declares Ob=3]
    34: ABSOLUTION_CHURCH_L_COST = -1  # [canonical: faction_canon §8.3 — "at cost of 1 Mandate"]
    35: ABSOLUTION_TARGET_STA_BONUS = 1  # [canonical: faction_canon §8.2 — "+1 to target"]
    36: 
    37: 
    38: @dataclass
    39: class AbsolutionResult:
    40:     status: str

### systems/factions/sim/absolution.py:86   [logs: PR338]
  CLAIM: | A338-078 | primitive | `Faction.standing` — 2 things, 1 name | `game_state.py:129`; write sites `crown_initiative.py:81,98,116,119,167,177,254,267,270`; `absolution.py:86`; `parliamentary_transfer.py:379` | Bare unbounded int mutated at 11 sites via +=/-=, never through adjust() (escapes registry clamps); feeds 2 dice pools whose outcomes write it back — 2 undamped positive-feedback loops; DIFFE
    85:     else:  # FAILURE
  >>86:         church.standing -= 1
    87:         result.effects_applied.append("Church.standing -1 [Failure: public ceremony failed]")

### systems/factions/sim/council_solmund.py:30-33   [logs: PR337]
  CLAIM: | A337-070 | formula | Council of Solmund formula | `_v1.md` §3 | `Ob=floor(CI/30)+2`; `Pool=int(Church.L)`; 1/arc. | BUILT | yes, `council_solmund.py:30-33,49-92` |
    29: 
  >>30: def council_ob(world) -> int:
    31:     """Ob = floor(CI / 30) + 2 — easier when CI high [part10 §5.1]."""
    32:     ci = world.clocks.get('CI', 0.0)
    33:     return math.floor(ci / 30) + COUNCIL_OB_BASE
    34: 

### systems/factions/sim/crown_initiative.py:39-122   [logs: PR337]
  CLAIM: | A337-067 | formula | Royal Progress formula | `_v1.md` §3 | `Ob=max(2,floor((7n_owned−Σaccord)/2))`; `Pool=int(I)+standing`; `W−2` regardless of outcome. | BUILT | yes, `crown_initiative.py:39-122` |
    38: 
  >>39: # ── Mode I — Royal Progress (part10 §3.2 — POST-ED-840 closure formula) ──────
    40: 
    41: ROYAL_PROGRESS_WEALTH_COST = -2  # canonical "W -2" [part10 §3.2]
    42: ROYAL_PROGRESS_OB_FLOOR = 2  # canonical "Floor at 2" [part10 §3.2 ED-840]
    43: ROYAL_PROGRESS_MAX_ACCORD_PER_TERR = 7  # canonical "sum_max = 7 * num_owned" [part10 §3.2 ED-840]
    44: 
    45: 
    46: def royal_progress_ob(world, faction) -> int:

### systems/factions/sim/crown_initiative.py:81   [logs: PR338]
  CLAIM: | A338-078 | primitive | `Faction.standing` — 2 things, 1 name | `game_state.py:129`; write sites `crown_initiative.py:81,98,116,119,167,177,254,267,270`; `absolution.py:86`; `parliamentary_transfer.py:379` | Bare unbounded int mutated at 11 sites via +=/-=, never through adjust() (escapes registry clamps); feeds 2 dice pools whose outcomes write it back — 2 undamped positive-feedback loops; DIFFE
    80:     ob = royal_progress_ob(world, crown)
  >>81:     pool = int(crown.I) + crown.standing  # [canonical: part10 §3.2 — "Influence + Standing modifier"]
    82: 

### systems/factions/sim/crown_initiative.py:91   [logs: PR337,PR338]
  CLAIM: | A338-165 | claim-about-repo | Wealth has 4 write sites total, all costs, no income | `faction_action.py:546`; `crown_initiative.py:91,161,246` | Wealth monotonically non-increasing across a campaign; "no surveyed title has a resource with no source" | measured | BUILT (structural gap) |
  CLAIM: | A338-083 | formula | Muster's Wealth cost off by 100× | `faction_action.py:77,546` vs `crown_initiative.py:91,161,246` | `adjust('W',-1)` passes granular delta / `MULTS['W']=100` → costs 0.01 Wealth, not 1; sibling sites multiply by 100 first | BUILT (defect) | yes |
  CLAIM: | A337-109 | formula | Wealth write sites — 4 total, all costs, no income anywhere | `_v1.md` §3 | Wealth monotonically non-increasing across a campaign. | BUILT | yes, `faction_action.py:546`, `crown_initiative.py:91,161,246` |
    90:     # Cost paid regardless of outcome [part10 §3.2 — "cost paid" on all rows]
  >>91:     crown.adjust('W', ROYAL_PROGRESS_WEALTH_COST * _MULTS_W)
    92:     result.effects_applied.append(f"Crown.W -2.0 stat-tier [§3.2 cost]")

### systems/factions/sim/crown_initiative.py:127-181   [logs: PR337]
  CLAIM: | A337-068 | formula | Great Work formula, multi-season pledge deferred | `_v1.md` §3 | `Pool=int(L)`; `Ob=4`; pays all 3 seasons' Wealth up front; multi-season tracking deferred. | BUILT (simplified) | yes, `crown_initiative.py:127-181` |
    126: 
  >>127: GREAT_WORK_WEALTH_COST_PER_SEASON = -1  # canonical "W -1 per season" [part10 §3.3]
    128: GREAT_WORK_SEASONS = 3  # canonical "3 seasons" [part10 §3.3]
    129: GREAT_WORK_FINAL_OB = 4  # canonical "Ob 4" [part10 §3.3]
    130: 
    131: 
    132: def attempt_great_work(crown, world, rng) -> InitiativeResult:
    133:     """Multi-season Open Pledge simplified to instant resolution at full cost.
    134: 

### systems/factions/sim/crown_initiative.py:189-273   [logs: PR337]
  CLAIM: | A337-069 | formula | Coronation Renewal formula (matches score/2 ruling) | `_v1.md` §3 | `Ob=floor(Church.L/2)+1`; `Pool=Crown.I`; `W−2`; lifts Excommunication. | BUILT | yes, `crown_initiative.py:189-273` |
    188: 
  >>189: def coronation_renewal_ob(church_l: float) -> int:
    190:     """Ob = floor(Church Mandate / 2) + 1 [part10 §3.4]."""
    191:     return math.floor(church_l / 2) + 1
    192: 
    193: 
    194: def coronation_renewal_prereq(crown, church, world) -> tuple[bool, str]:
    195:     """Truce/Peace/Alliance/Crown Treaty with Church AND Church not actively
    196:     prosecuting an Excommunication against Crown this season.

### systems/factions/sim/crown_initiative.py:294-317   [logs: PR337]
  CLAIM: | A337-081 | mechanic | Crown Initiative dispatcher | `_v1.md` §3 mechanics | Heuristic picks Royal Progress/Great Work/Coronation Renewal (last only while excommunicated). | BUILT | yes, `crown_initiative.py:294-317` |
    293: 
  >>294: def select_initiative_mode(crown, world, rng) -> str | None:
    295:     """Mode selection heuristic.
    296: 
    297:     Coronation Renewal is ONLY for Excommunication recovery — never selected
    298:     when Crown is not excommunicated (it burns Wealth without strategic gain
    299:     against Ob = floor(Church.L/2)+1, which is non-trivial).
    300: 
    301:     Royal Progress when Ob viable (Ob ≤ Pool + reasonable margin).

### systems/factions/sim/excommunication.py:163-171   [logs: PR341]
  CLAIM: `ci_track.py:177`; `mass_seizure.py:214`; `excommunication.py:163-171`. Conclusion: no site anywhere
  CLAIM: | A341-086 | module/subsystem | `excommunication.py` | `excommunication.py:163-171` | Routes through `ci_track.apply_ci_delta` — literal CI | claimed by synthesis §2.1; not independently re-opened | UNVERIFIED (not directly opened in this pass) |
    162:             )
  >>163:             old_ci = world.clocks.get('CI', 0.0)
    164:             # [2026-05-20 migration] route through ci_track.apply_ci_delta — single
    165:             # canonical surface for CI arithmetic. Was: inline ceiling clamp.
    166:             from systems.overview.sim.ci_track import apply_ci_delta
    167:             new_ci = apply_ci_delta(EXCOMM_FORMAL_CI_DELTA,
    168:                                     source=f"excommunication §7.1 formal",
    169:                                     world=world)
    170:             result.effects_applied.append(

### systems/factions/sim/faction_action.py:55-58   [logs: PR337]
  CLAIM: | A337-062 | formula | Action-selection policy formula (0.30/0.35/0.20/0.15 re-weighted) | `_v1.md` §3 derivatives | Base priors re-weighted by 3 RNG-free signals, renormalised, one `rng.random()` draw. | BUILT | yes, `faction_action.py:55-58,208-273` |
    54: # [canonical: faction_action.py M7_ASSUMPTION_SIX prior; re-grounded ED-FA-0012 (FA-5)]
  >>55: BASE_W_UNIQUE = 0.30      # [canonical: M7_ASSUMPTION_SIX faction-unique share, ED-FA-0012 prior]
    56: BASE_W_CONQUEST = 0.35    # [canonical: M7_ASSUMPTION_SIX Conquest share, ED-FA-0012 prior]
    57: BASE_W_MUSTER = 0.20      # [canonical: M7_ASSUMPTION_SIX Muster share, ED-FA-0012 prior]
    58: BASE_W_GOVERN = 0.15      # [canonical: M7_ASSUMPTION_SIX Govern share, ED-FA-0012 prior]
    59: 

### systems/factions/sim/faction_action.py:77   [logs: PR338]
  CLAIM: | A338-083 | formula | Muster's Wealth cost off by 100× | `faction_action.py:77,546` vs `crown_initiative.py:91,161,246` | `adjust('W',-1)` passes granular delta / `MULTS['W']=100` → costs 0.01 Wealth, not 1; sibling sites multiply by 100 first | BUILT (defect) | yes |
    76: # rather than the old "free unless you fail" inversion.
  >>77: MUSTER_WEALTH_COST = 1       # [canonical: Redlich 1964 (military-enterpriser); Tilly 1990 — ED-FA-0009 (FA-2)]
    78: MUSTER_WEALTH_TO_POOL_DIV = 2  # [canonical: ED-FA-0009 (FA-2) — pool = Mil + floor(W/2), money raises troops]

### systems/factions/sim/faction_action.py:78   [logs: PR341]
  CLAIM: | A341-067 | param/constant | `MUSTER_WEALTH_TO_POOL_DIV` | `faction_action.py:78`, ED-FA-0009 (FA-2) | `pool = Mil + floor(W/2)` — money raises troops | VERIFIED TRUE — cited by T8 finding 7 to show wealth gates nothing when W=0 | yes — live |
  CLAIM: | 8 | `faction_action.py:544-549`: `pool = Mil + floor(W/2)`, so a W=0 faction musters on Mil alone ("Wealth gates nothing") | `03_findings_T7_T9.md T8 finding 7` | **VERIFIED-TRUE** — `systems/factions/sim/faction_action.py:78` and surrounding comments state exactly `pool = Mil + floor(W/2)` (ED-FA-0009 FA-2) |
    77: MUSTER_WEALTH_COST = 1       # [canonical: Redlich 1964 (military-enterpriser); Tilly 1990 — ED-FA-0009 (FA-2)]
  >>78: MUSTER_WEALTH_TO_POOL_DIV = 2  # [canonical: ED-FA-0009 (FA-2) — pool = Mil + floor(W/2), money raises troops]
    79: 

### systems/factions/sim/faction_action.py:196-206   [logs: PR341]
  CLAIM: | A341-053 | function/signature | `_threat_signal` | `faction_action.py:196-206` | Walks adjacency, compares `world.factions[t.owner].Mil > faction.Mil` — perfect knowledge of every neighbour's military strength | VERIFIED TRUE — no belief term | yes — live code |
    195:     [canonical: ED-FA-0012 (FA-5) Muster weight — proximate-threat term]
  >>196:     """
    197:     adj = set()
    198:     for tid in faction.territories:
    199:         adj |= ADJACENCY.get(tid, set())
    200:     for tid in adj:
    201:         t = world.territories.get(tid)
    202:         if t and t.owner and t.owner != faction.name and t.owner in world.factions:
    203:             if world.factions[t.owner].Mil > faction.Mil:

### systems/factions/sim/faction_action.py:208-273   [logs: PR337,PR338,PR341]
  CLAIM: | A338-086 | process | `faction_take_action` | `faction_action.py:208-273`; `mc_v18.py:130-145` | Season faction step: signals→re-weight→1 draw→dispatch through 4 non-elif buckets; `'invalid'` sentinel falls through to terminal unconditional `_try_govern`, caller discards return | BUILT | yes (structural defect) |
  CLAIM: | A337-089 | process | `faction_take_action` — 4 non-`elif` buckets, `'invalid'` sentinel fallthrough | `_v1.md` §3 processes | A bucket returning `'invalid'` falls through to terminal unconditional `_try_govern`, whose return the caller discards. | BUILT (quirky) | yes, `faction_action.py:208-273` |
  CLAIM: 7. **The shipped NPC chooser** — `faction_action.py:208-273` was read in full, tracing all four state
  CLAIM: | A341-054 | function/signature | NPC strategic chooser (4 signals) | `faction_action.py:208-273` | `_conquest_targets`, `_mil_advantage_signal`, `_undergoverned_share`, `_threat_signal` re-weight 4 prior buckets, then `rng.random()` dispatches | VERIFIED TRUE | yes — live code |
    207: 
  >>208: def faction_take_action(faction, world, rng) -> str:
    209:     """Select and execute one action for a faction this season.
    210: 
    211:     GD-2: mandatory threat-response before stochastic selection.
    212:     ED-FA-0012 (FA-5): the four action buckets' PRIOR weights (30/35/20/15, M7_ASSUMPTION_SIX) are
    213:     re-weighted by faction state BEFORE the single selection draw, then renormalized to sum 1.0, so
    214:     the cumulative-threshold dispatch below is identical in structure to v17 — only the boundaries
    215:     move. In a neutral state the mix is exactly the old 30/35/20/15. Grounding: Levy 1983 / Blainey

### systems/factions/sim/faction_action.py:251   [logs: PR340]
  CLAIM: | 9 | `faction_action.py` draws `roll = rng.random()` at line ~251 for action selection, and `rng.choice` for targets (05 Overrides O-5.9) | **VERIFIED-TRUE** — `systems/factions/sim/faction_action.py:251` (rng.random), `:457`/`:566`-area `rng.choice` calls confirmed |
  CLAIM: | A340-077 | function/signature | fa.select's softmax draw (O-5.9) | 05 §3.5 | `P(action)=exp(appeal/T)/Σexp(appeal(a)/T)`, T=APPEAL_TEMPERATURE; seed(post)=H(campaign_seed‖accounting_index‖post_id) — per-post substream, NOT shared sequential | derived + draw | PROPOSED, restores stochastic selection the ratified engine already has (faction_action.py:251) | VERIFIED-TRUE (faction_action.py rng.ran
    250: 
  >>251:     roll = rng.random()
    252: 

### systems/factions/sim/faction_action.py:308   [logs: PR337]
  CLAIM: | A337-055 | mechanic | Faction personality via `if faction.name == 'Crown'` string comparison | `_v1.md` §3 | Hafenmark/Varfell have no branch — renaming factions in the starting table changes nothing. | BUILT (defect) | yes, `faction_action.py:308,324,346` |
    307:     this season — at which point _try_faction_unique applies the Parliamentary-Censure fallback."""
  >>308:     if faction.name == 'Crown':
    309:         mode = crown_initiative.select_initiative_mode(faction, world, rng)

### systems/factions/sim/faction_action.py:324-344   [logs: PR337]
  CLAIM: | A337-082 | mechanic | Church priority chain (Excomm→Council→Absolution) | `_v1.md` §3 | Fixed order, each with its own gate; Mass Seizure NOT in the chain. | BUILT | yes, `faction_action.py:324-344` |
    323: 
  >>324:     if faction.name == 'Church':
    325:         # Priority 1: Excommunication (primary offensive, high strategic impact)
    326:         if faction.L >= excommunication.EXCOMM_PREREQ_L_LIGHT:
    327:             target = excommunication.select_excommunication_target(faction, world, rng)
    328:             if target is not None:
    329:                 r = excommunication.attempt_excommunication(faction, target, world, rng)
    330:                 if r.status == 'resolved':
    331:                     return f'Excommunication:{r.degree.value}'

### systems/factions/sim/faction_action.py:392-402   [logs: PR337,PR338]
  CLAIM: | A337-207 | mechanic | `scene.battle_concluded` — log-only, no subscriber | `_v1_part2.md` §"Mass-Battle Seam" | Emitted on every conquest with battle_id/victor/casualties/outcome; no `apply=`, `causes=[]` honestly cited. | BUILT (log-only) | yes, `faction_action.py:392-402` |
  CLAIM: | A338-071 | mechanic | `scene.battle_concluded` | `faction_action.py:392-402` | Emitted on every conquest w/ battle_id, victor, casualties, territorial outcome; log-only (no apply=, no subscriber) | BUILT (log-only) | yes, inert telemetry |
    391:         b_pct = float(battle.get('defender_size_pct') or 0.0)
  >>392:         key = Key(
    393:             id=f'scene.battle_concluded.s{season}.n{seq}',
    394:             type='scene.battle_concluded',
    395:             emitted_at=EmittedAt(season_index=season),
    396:             # No upstream Key exists to cite: resolve_mass_battle is a plain call, not an emission.
    397:             # [] is the honest value — a fabricated cause is worse than no cause (see the
    398:             # causes[]-population note in engine/cross_scale/echo_transport.py).
    399:             causes=[],

### systems/factions/sim/faction_action.py:392   [logs: PR338]
  CLAIM: | A338-047 | claim-about-repo | Live Key emission surface = 5/55 types, 4 call sites | `echo_transport.py:320,427`; `parliamentary_transfer.py:228`; `faction_action.py:392` | scene.accord_echo, scene.contest_resolved, scene.combat_resolved (state-writing); da.public_governance, scene.battle_concluded (log-only, both in swallowing try/except) | measured | VERIFIED (per synthesis, consistent w/ dire
    391:         b_pct = float(battle.get('defender_size_pct') or 0.0)
  >>392:         key = Key(
    393:             id=f'scene.battle_concluded.s{season}.n{seq}',

### systems/factions/sim/faction_action.py:399   [logs: PR341]
  CLAIM: | 4 | `faction_action.py:399` passes `causes=[]` with a comment that no upstream Key exists to cite | `05_independent_verification.md §2.2` | **VERIFIED-TRUE** — `systems/factions/sim/faction_action.py:399` reads `causes=[],` |
  CLAIM: (`faction_action.py:399`, `parliamentary_transfer.py:232`, `echo_transport.py:328`) was read directly
    398:             # causes[]-population note in engine/cross_scale/echo_transport.py).
  >>399:             causes=[],
    400:             # 'territory', not 'provincial': the substrate's canonical roster is

### systems/factions/sim/faction_action.py:432-528   [logs: PR337]
  CLAIM: | A337-076 | formula | Conquest formula — delegates to `resolve_mass_battle` | `_v1.md` §3 | On attacker win: territory moves, loser `adjust('L',−10)` granular = −0.5 points, garrison set. | BUILT | yes, `faction_action.py:432-528` |
    431: 
  >>432: 
    433: def _try_conquest(faction, world, rng) -> str:
    434:     """Attempt military conquest of adjacent territory.
    435: 
    436:     Phase 7 (§4.10 sub-step 3): single-roll path replaced with
    437:     `resolve_mass_battle` engine invocation per
    438:     systems/mass_battle/mass_battle_integration_v30.md §4.10.
    439:     GD-1 binding: produces faction stat / territorial-control deltas only —

### systems/factions/sim/faction_action.py:467   [logs: PR337,PR338]
  CLAIM: | A338-068 | primitive | `terrain` param dead at both ends | `massbattle.py:99` signature; `faction_action.py:467` caller | Declared param never referenced in function body; only caller passes `terrain=None` | measured (defect) | INERT |
  CLAIM: | A337-203 | param/constant | `terrain` parameter — declared, appears nowhere in function body | `_v1_part2.md` §"Mass-Battle Seam" | Only caller passes `terrain=None` with a deferral comment. | INERT | VERIFIED-TRUE, signature at `massbattle.py:99`; caller `faction_action.py:467` |
    466:         faction_b=defender_faction,
  >>467:         terrain=None,  # [GAP: terrain modifiers deferred to Phase 7 follow-on Steps 2-9]
    468:         world=world,

### systems/factions/sim/faction_action.py:508-524   [logs: PR337]
  CLAIM: | A337-077 | mechanic | Terms/Storm fork | `_v1.md` §3 | Defender-not-routed (Success)→Accord Terms; routed (Overwhelming)→Storm baseline; AI always takes Terms when available. | BUILT | yes, `faction_action.py:508-524` |
    507:         # whenever it is available (defender NOT routed → degree 'Success'). Terms is strictly cheaper
  >>508:         # for the attacker (lighter Accord penalty, no reputational Storm cost) and nothing in the
    509:         # current AI prefers the costlier path — so Terms is the defensible default, open to a future
    510:         # explicit AI-policy choice. [canonical: settlement_layer_v30 §5.1 FA-6 branch (a); Grotius III]
    511:         if deg == 'Success':
    512:             # Branch (a) Accept surrender on terms — Grotius III / Parker 1994 (honors of war).
    513:             t.adjust_accord(ACCORD_TERMS)  # [canonical: settlement_layer_v30 §5.1 FA-6(a) lighter Accord — ED-FA-0013]
    514:             # §5.3 Entry Terms: "surrender on terms" makes Confirm Privileges the natural fork, which
    515:             # seeds the new settlement's Legitimacy at 3 (Joyeuse Entrée; ED-SE-0011). This module is

### systems/factions/sim/faction_action.py:531-559   [logs: PR337,PR338]
  CLAIM: | A337-063 | formula | Muster formula: `pool=Mil+floor(W/2)`, Ob=1, Wealth charged up front | `_v1.md` §3 | Overwhelming Mil+5, Success +3; no failure penalty (up-front cost prices it). | BUILT | yes, `faction_action.py:531-559` |
  CLAIM: | A338-164 | mechanic | `_try_muster` | `faction_action.py:531-559` | `pool=Mil+floor(W/2)`; Ob=1; Wealth charged up front unconditionally; Overwhelming +5/Success +3 granular Military; no failure penalty; per critique §7.6 this is actually the "professional" recruitment model (ED-FA-0009 grounds it in Wallenstein), mislabeled as a generic levy | BUILT | yes |
    530: 
  >>531: def _try_muster(faction, world, rng) -> str:
    532:     """Muster military strength in owned territory.
    533: 
    534:     ED-FA-0009 (FA-2): Muster is a fiscal-military PURCHASE, not a free-except-on-failure roll. The
    535:     military enterpriser is paid regardless of outcome (Redlich 1964, the Wallenstein/Mansfeld
    536:     contractor model; Tilly 1990, coercion-and-capital), so Wealth is charged UP FRONT on every
    537:     attempt and money buys pool: pool = Mil + floor(W/2). The old "W-3 only on Failure" inversion is
    538:     retired — the up-front cost now serves the failure-penalty role (no double-charge).

### systems/factions/sim/faction_action.py:544-549   [logs: PR341]
  CLAIM: | 8 | `faction_action.py:544-549`: `pool = Mil + floor(W/2)`, so a W=0 faction musters on Mil alone ("Wealth gates nothing") | `03_findings_T7_T9.md T8 finding 7` | **VERIFIED-TRUE** — `systems/factions/sim/faction_action.py:78` and surrounding comments state exactly `pool = Mil + floor(W/2)` (ED-FA-0009 FA-2) |
    543:     # Pay the enterpriser up front, ALWAYS — before rolling (Redlich 1964; Tilly 1990). Faction.adjust
  >>544:     # clamps at its Wealth floor, so an already-broke faction still fires the attempt (the proposal
    545:     # specifies a cost, not a hard Wealth gate) — it simply pays what it can.
    546:     faction.adjust('W', -MUSTER_WEALTH_COST)  # [canonical: ED-FA-0009 (FA-2) up-front W cost; Redlich 1964 / Tilly 1990]
    547: 
    548:     # Money raises troops: pool = Mil + floor(W/2). [canonical: ED-FA-0009 (FA-2); Redlich 1964]
    549:     pool = faction.Mil + math.floor(faction.W / MUSTER_WEALTH_TO_POOL_DIV)
    550:     ob = 1  # [FORK: mc_v17.py Muster Ob 1 — superseded by mc_v18, source at ref c451bcb]

### systems/factions/sim/faction_action.py:546   [logs: PR337,PR338]
  CLAIM: | A338-165 | claim-about-repo | Wealth has 4 write sites total, all costs, no income | `faction_action.py:546`; `crown_initiative.py:91,161,246` | Wealth monotonically non-increasing across a campaign; "no surveyed title has a resource with no source" | measured | BUILT (structural gap) |
  CLAIM: | A337-109 | formula | Wealth write sites — 4 total, all costs, no income anywhere | `_v1.md` §3 | Wealth monotonically non-increasing across a campaign. | BUILT | yes, `faction_action.py:546`, `crown_initiative.py:91,161,246` |
    545:     # specifies a cost, not a hard Wealth gate) — it simply pays what it can.
  >>546:     faction.adjust('W', -MUSTER_WEALTH_COST)  # [canonical: ED-FA-0009 (FA-2) up-front W cost; Redlich 1964 / Tilly 1990]
    547: 

### systems/factions/sim/faction_action.py:549   [logs: PR340]
  CLAIM: | 10 | Muster pool = `faction.Mil + math.floor(faction.W / 2)`, "Wealth is a discount on muster, never a gate" (13 §2) | **VERIFIED-TRUE** — `systems/factions/sim/faction_action.py:549`, tagged `[canonical: ED-FA-0009 (FA-2)]` |
  CLAIM: | A340-200 | claim-about-repo | Ordering-hazard fact partially stale, conclusion survives (§2) | 13 §2 | the cited audit's supporting fact ("Faction.adjust floors at 0.5") is now FALSE — game_state.py:153-180 now reads per-stat bounds, all 6 declared stats floor at 0 per 2 Jordan rulings dated 2026-08-23; but the hazard itself survives via a DIFFERENT fact: muster pool = Mil+floor(W/2), so W=0 sti
    548:     # Money raises troops: pool = Mil + floor(W/2). [canonical: ED-FA-0009 (FA-2); Redlich 1964]
  >>549:     pool = faction.Mil + math.floor(faction.W / MUSTER_WEALTH_TO_POOL_DIV)
    550:     ob = 1  # [FORK: mc_v17.py Muster Ob 1 — superseded by mc_v18, source at ref c451bcb]

### systems/factions/sim/faction_action.py:562-581   [logs: PR337]
  CLAIM: | A337-065 | formula | Govern formula: `pool=I`, `Ob=2` fixed | `_v1.md` §3 | Overwhelming Accord+15/Success+10/Failure Sta−5. | BUILT | yes, `faction_action.py:562-581` |
    561: 
  >>562: def _try_govern(faction, world, rng) -> str:
    563:     """Govern an owned territory to improve Accord."""
    564:     if not faction.territories:
    565:         return _NOOP
    566:     target = rng.choice(list(faction.territories))
    567:     t = world.territories.get(target)
    568:     if not t or t.owner != faction.name:
    569:         return _NOOP

### systems/factions/sim/mass_seizure.py:53   [logs: PR338]
  CLAIM: | A338-095 | mechanic | `seizure_ob_modifier`/`count_infrastructure` | `infrastructure.py`, read by `mass_seizure.py:53,159,260` | Church presence lowers Mass Seizure Ob against a territory; ONE settlement-adjacent mechanic with a live production consumer | BUILT | yes (but Mass Seizure itself is uncalled, A338-084) |
    52: from systems.settlements.sim.infrastructure import (
  >>53:     count_infrastructure, seizure_ob_modifier, BUILDING_CHAPEL,
    54:     BUILDING_CHURCH, BUILDING_CATHEDRAL,

### systems/factions/sim/mass_seizure.py:86   [logs: R2_systems]
  CLAIM: | `SeizureDeclaration`, `SeizureResult` | `systems/factions/sim/mass_seizure.py:86,96` | -- | factions |
    85: @dataclass
  >>86: class SeizureDeclaration:
    87:     declared: bool

### systems/factions/sim/mass_seizure.py:214   [logs: PR341]
  CLAIM: `ci_track.py:177`; `mass_seizure.py:214`; `excommunication.py:163-171`. Conclusion: no site anywhere
  CLAIM: | A341-085 | module/subsystem | `mass_seizure.py` | `mass_seizure.py:214` | Writes a module constant key, not a variable, into clocks | claimed by synthesis §2.1; not independently re-opened | UNVERIFIED (not directly opened in this pass) |
    213:     if declared:
  >>214:         world.clocks[MASS_SEIZURE_FLAG_CLOCK] = 1
    215: 

### systems/factions/sim/mass_seizure.py:293   [logs: R4_registries]
  CLAIM: | FA | ED-FA-0037 | 08-25 | landed | Mass seizure wrote a canonical Accord TIER into the CONTINUOUS `Territory.accord` field (`mass_seizure.py:293`) |
    292:             # field. ACCORD_MAP is the tier->continuous table and canon_buckets.canonical_accord
  >>293:             # its inverse, so a raw `float(tier)` write loses tiers: canonical_accord(2.0) == 1
    294:             # and canonical_accord(3.0) == 1, and 2/3 are the only values a seizure can produce.

### systems/factions/sim/parliamentary_action.py:97-160   [logs: PR337]
  CLAIM: | A337-098 | mechanic | Parliamentary Censure | `_v1.md` §3 | Self-gates GD-3+L≥2; targets highest-L rival; on pass target Sta−1, L−1. Universal unique-slot fallback. | BUILT | yes, `parliamentary_action.py:97-160` |
    96: 
  >>97: def propose_censure(proposer, world, rng) -> str:
    98:     """Propose a Censure motion (mildest Parliamentary Sanction tier, faction_layer_v30 §5.4).
    99: 
    100:     Flow: eligibility gate → target selection → declaration assembly → run_parliamentary_vote →
    101:     apply the Censure-specific target effect on a pass. Returns a dispatch-style result string
    102:     ('ParliamentarySanction_Censure:' + vote status) for a later wiring lane, or the '{_NOOP}'
    103:     sentinel when the action is unavailable (ineligible proposer / no valid target).
    104: 

### systems/factions/sim/parliamentary_action.py:154-160   [logs: PR337,PR338]
  CLAIM: | A338-145 | formula | Censure stacking to −2 Mandate (unratified emergent composition) | `parliamentary_action.py:154-160` | On Total Victory pass, §10 rider + §5.4 Censure effect compose to −2 Mandate; module's own SEED/NEEDS-JORDAN note flags it as unratified | BUILT (emergent, unratified) | yes |
  CLAIM: | A337-099 | formula | Censure stacking — unratified emergent composition | `_v1.md` §3 | On a Total Victory pass, §10 rider + §5.4 effect compose to −2 Mandate; module flags this itself as unratified. | BUILT (unratified emergent) | yes, `parliamentary_action.py:154-160` |
    153:     # ratified design intent.
  >>154:     if result.status == "passed":
    155:         # MULTS-multiplied granular_delta so .adjust() lands a full −1 point, matching
    156:         # parliamentary_vote.py's own `adjust("L", DELTA * MULTS["L"])` convention.
    157:         target.adjust("Sta", CENSURE_STABILITY_DELTA * MULTS["Sta"])  # [canonical: §5.4 Stability −1]
    158:         target.adjust("L", CENSURE_MANDATE_DELTA * MULTS["L"])        # [canonical: §5.4 Mandate −1]
    159: 
    160:     return f"ParliamentarySanction_Censure:{result.status}"
    161: 

### systems/factions/sim/parliamentary_transfer.py:76-121   [logs: PR338]
  CLAIM: | A338-147 | gap | Casus Belli gate — 8 sources, only 1 populated | `parliamentary_transfer.py:76-121`; `faction_systems_overview_v30.md:175-186` | Only `crown_constitutional_restoration` populated, maps to `adversarial` alone; other 3 modes+7 sources unreachable in fresh campaign; 2 "canonical" enumerations of the 8 disagree on 5 names | INERT (7/8) | 1/8 reachable |
    75: # §2 mode -> qualifying CB sources (§3 source names). crown_constitutional_restoration -> adversarial (FLAG).
  >>76: _MODE_CB = {
    77:     "adversarial": {"military", "military_conquest", "adjacent_instability",
    78:                     "einhir_revival_partial", "parliamentary_transfer_partial",
    79:                     "crown_constitutional_restoration"},
    80:     "consensual":  {"negotiated_agreement"},
    81:     "punishment":  {"excommunication", "conviction_scar", "treaty_violation"},
    82:     "appeasement": {"crisis_stability", "peninsular_strain_severe",
    83:                     "insurgency_emergence", "war_readiness"},

### systems/factions/sim/parliamentary_transfer.py:88   [logs: R2_systems]
  CLAIM: | `TransferResult` | `systems/factions/sim/parliamentary_transfer.py:88` | -- | factions -- office/territory transfer (closest `Tenure` re-assignment event) |
    87: @dataclass
  >>88: class TransferResult:
    89:     status: str                    # 'transferred' | 'partial' | 'failed' | 'blocked' | 'invalid'

### systems/factions/sim/parliamentary_transfer.py:111-121   [logs: PR338]
  CLAIM: | A338-079 | primitive | `world.casus_belli` | `parliamentary_transfer.py:111-121,331-335` | Optional duck-typed dict; only Crown's auto-refreshing restoration clause populates it; zero writers for the other 7 sources | INERT | no |
    110: 
  >>111: def _available_cb(initiator, holder, world):
    112:     """§3 CB sources available to (initiator, holder): auto Crown-restoration + duck-typed ledger."""
    113:     sources = []
    114:     fac = world.factions.get(initiator)
    115:     if (initiator == "Crown" and fac is not None
    116:             and len(fac.territories) < PARL_CROWN_RESTORATION_TERRITORY_MAX):
    117:         sources.append("crown_constitutional_restoration")          # [§3 auto, refreshes per arc]
    118:     ledger = getattr(world, "casus_belli", None)

### systems/factions/sim/parliamentary_transfer.py:228   [logs: PR338]
  CLAIM: | A338-047 | claim-about-repo | Live Key emission surface = 5/55 types, 4 call sites | `echo_transport.py:320,427`; `parliamentary_transfer.py:228`; `faction_action.py:392` | scene.accord_echo, scene.contest_resolved, scene.combat_resolved (state-writing); da.public_governance, scene.battle_concluded (log-only, both in swallowing try/except) | measured | VERIFIED (per synthesis, consistent w/ dire
    227:         season = int(getattr(world, 'season', 0))
  >>228:         key = Key(
    229:             id=f'da.public_governance.s{season}.n{seq}',

### systems/factions/sim/parliamentary_transfer.py:232   [logs: PR341]
  CLAIM: (`faction_action.py:399`, `parliamentary_transfer.py:232`, `echo_transport.py:328`) was read directly
    231:             emitted_at=EmittedAt(season_index=season),
  >>232:             causes=[],
    233:             scale_signature=['territory'],

### systems/factions/sim/parliamentary_transfer.py:248-386   [logs: PR337,PR338]
  CLAIM: | A338-146 | mechanic | Parliamentary Territory Transfer | `parliamentary_transfer.py:248-386` | `Pool=max(0,I+vote_mod[-1,0,1])`; `Ob=holder.L+2`; 4 narrative modes; 1/arc/faction guard; Partial grants retry CB | BUILT | yes |
  CLAIM: | A337-102 | mechanic | Parliamentary Territory Transfer | `_v1.md` §3 | `Pool=max(0,I+vote_mod)`; `Ob=holder.L+2`; 4 narrative modes; 1/arc/faction guard. | BUILT | yes, `parliamentary_transfer.py:248-386` |
    247: 
  >>248: def propose_transfer(initiator, target_territory, mode, world, *,
    249:                      side_a_allies=None, side_b_allies=None, rng=None) -> TransferResult:
    250:     """§§1-4: propose a Parliamentary Territory Transfer. CB-gated, vote-wrapped, Influence-vs-Legitimacy roll."""
    251:     res = TransferResult(status="invalid", initiator=initiator, target_territory=target_territory, mode=mode)
    252: 
    253:     if mode not in MODES:
    254:         res.notes.append(f"invalid mode '{mode}'; expected one of {MODES}")
    255:         return res

### systems/factions/sim/parliamentary_transfer.py:257   [logs: PR337]
  CLAIM: | A337-007 | ownership/gap | score/2 obstacle ruling — implemented at exactly one site | `cross_scale_action_catalogue_v1.md` §0.2 [pre-existing] | Jordan ruled (2026-08-14) an obstacle vs a character/faction is their score/2 plus modifiers; only `crown_initiative.coronation_renewal_ob` matches; `tribunal.py` halves conditionally; `parliamentary_transfer.py:257` uses the full score, contradicting 
    256:     fac = world.factions.get(initiator)
  >>257:     if fac is None:
    258:         res.notes.append(f"unknown initiator '{initiator}'")

### systems/factions/sim/parliamentary_transfer.py:379   [logs: PR338]
  CLAIM: | A338-078 | primitive | `Faction.standing` — 2 things, 1 name | `game_state.py:129`; write sites `crown_initiative.py:81,98,116,119,167,177,254,267,270`; `absolution.py:86`; `parliamentary_transfer.py:379` | Bare unbounded int mutated at 11 sites via +=/-=, never through adjust() (escapes registry clamps); feeds 2 dice pools whose outcomes write it back — 2 undamped positive-feedback loops; DIFFE
    378:         if mode == "punishment":
  >>379:             holder_fac.standing -= 1                                  # §2 Punishment — Standing -1 (no L+1)
    380:             res.effects.append(f"§2 Punishment: holder '{holder}' Standing -1 (no public-sympathy L+1).")

### systems/factions/sim/treaty.py:62   [logs: R2_systems]
  CLAIM: | `TreatyRecord`, `TreatyResult`, `ExpirationResult` | `systems/factions/sim/treaty.py:62,86,93` | parties[], terms{}, bound_arc, bound_season | factions -- closest multi-party obligation/commit edge |
    61: @dataclass
  >>62: class TreatyRecord:
    63:     parties: tuple

### systems/factions/sim/treaty.py:99-160   [logs: PR338]
  CLAIM: | A338-081 | mechanic | Treaty pipeline | `treaty.py:99-160` | `propose_treaty` typed no-op; `register_treaty` test-only; `process_treaty_expirations` fully implemented at flat 0.90 hazard w/ zero production callers; with no RNG the fallback roll fixes at 0.95 (0.95<0.90 → lapse impossible) | INERT | no |
    98: 
  >>99: def propose_treaty(parties: list, terms: dict, world=None) -> TreatyResult:
    100:     """Propose a treaty between parties.
    101: 
    102:     Canon (treaty_expiration_v30.md, 2026-05-17) specifies the Crown re-binding
    103:     path as the Senator Outward action (§2), which is resolved in
    104:     crown_initiative — not through a generic propose_treaty. No canonized
    105:     non-Senator-Outward treaty-formation protocol exists yet, so this entry
    106:     point raises. Use register_treaty for scaffolding/test insertion and

### systems/factions/sim/treaty.py:120-160   [logs: PR338]
  CLAIM: | A338-080 | primitive | `world.treaties` | `game_state.py`; `treaty.py:120-160` | Real serialized dict of TreatyRecord; no module owns creation/end | INERT | no |
    119: 
  >>120: 
    121: def process_treaty_expirations(world, lapse_rate: float = TREATY_LAPSE_RATE_DEFAULT) -> list[ExpirationResult]:
    122:     """Apply §4.5 Treaty Expiration at arc boundary.
    123: 
    124:     For each active treaty, roll against TREATY_LAPSE_RATE. Lapsed treaties
    125:     are marked inactive (cleared from registers per §4.5). Caller is
    126:     responsible for invoking this only at arc boundaries (season_manager
    127:     detects via check_arc_boundary).

### systems/factions/sim/tribunal.py:60   [logs: R2_systems]
  CLAIM: | `TribunalResult` | `systems/factions/sim/tribunal.py:60` | -- | factions |
    59: @dataclass
  >>60: class TribunalResult:
    61:     """Outcome of one Excommunication Tribunal resolution."""

### systems/factions/sim/tribunal.py:95-140   [logs: PR337]
  CLAIM: | A337-072 | formula | Excommunication Tribunal formula | `_v1.md` §3 | `Pool=int(church.L)`+1D formal grounds; `Ob=max(1,round(accused.L))`, halved on formal grounds. | BUILT | yes, `tribunal.py:95-140` |
    94:       - Ob: target.L (resistance) — halved when formal grounds (Resistance halved)
  >>95:       - TN 7 (params/core.md social-contest default)
    96: 
    97:     `formal_grounds=None` -> auto-detect via formal_grounds_check.
    98: 
    99:     Returns TribunalResult; consequences applied by the caller (excommunication.py).
    100:     """
    101:     if formal_grounds is None:
    102:         formal_grounds = formal_grounds_check(church, world)

### systems/fieldwork/sim/fieldwork.py:38-59   [logs: PR341]
  CLAIM: | A341-081 | module/subsystem | `fieldwork.py` / `investigation.py` | `fieldwork.py:38-59`, `investigation.py:30-51` | Six typed no-ops, design-gated on ED-916 | claimed by T9 finding 4; not independently re-opened in this pass | UNVERIFIED (not directly opened in this pass) |
    37: 
  >>38: def run_fieldwork_scene(scene: FieldworkScene):
    39:     return stubwire.stub_resolve(
    40:         'systems.fieldwork.sim.fieldwork',
    41:         'run_fieldwork_scene(scene: FieldworkScene) -> FieldworkResult',
    42:         reason='Pass 2l armature stub, design-gated on ED-916 (zero continuous-engine validation '
    43:                'at fieldwork parameters); OI-02, ED-IN-0091 plan §2.2')
    44: 
    45: 

### systems/fieldwork/sim/investigation.py:30-51   [logs: PR341]
  CLAIM: | A341-081 | module/subsystem | `fieldwork.py` / `investigation.py` | `fieldwork.py:38-59`, `investigation.py:30-51` | Six typed no-ops, design-gated on ED-916 | claimed by T9 finding 4; not independently re-opened in this pass | UNVERIFIED (not directly opened in this pass) |
    29: 
  >>30: def resolve_npe_response(npc_id: str, prompt: dict, world: GameState):
    31:     return stubwire.stub_resolve(
    32:         'systems.fieldwork.sim.investigation',
    33:         'resolve_npe_response(npc_id: str, prompt: dict, world: GameState) -> NPEResponse',
    34:         reason='Pass 2l armature stub, design-gated on ED-916 (zero continuous-engine validation '
    35:                'at fieldwork parameters); OI-02, ED-IN-0091 plan §2.2')
    36: 
    37: 

### systems/fieldwork/sim/knots.py:111   [logs: R2_systems]
  CLAIM: | `Knot`, `KnotState` | `systems/fieldwork/sim/knots.py:111,145` | actor_a, actor_b, strain, rupture state | fieldwork -- Person-Person relational edge (not Person-container) |
    110: @dataclass
  >>111: class Knot:
    112:     """A Knot between two actors per §1-§5."""

### systems/fieldwork/sim/knots.py:345-365   [logs: PR337,PR338]
  CLAIM: | A337-164 | mechanic | Close-Knot-break Scar — hardcodes `'Honor'` as a session call, not canon | `_v1_part2.md` §"People" | Canon says both partners scar; code scars only the actor passed in. | BUILT (defect) | yes, `knots.py:345-365` |
  CLAIM: | A338-117 | mechanic | Close-Knot-break Scar — session judgment call | `systems/fieldwork/sim/knots.py:345-365` | Passes `'Honor'` (fixed 2026-08-24 from an unregistered name); no ruling names which conviction for ED-912 §6.1; canon says BOTH partners scar, code scars only the passed actor | BUILT (2 open calls) | yes, partially correct |
    344:         # ED-912: a Close Knot that broke from POSITIVE strain (reached +5) → Conviction Scar +1
  >>345:         if knot.tier == TIER_CLOSE and knot.strain > 0:
    346:             consequences['conviction_scar'] = 1
    347:             # Late-import conviction
    348:             try:
    349:                 from systems.characters.sim.conviction import apply_conviction_scar
    350:                 # Which Conviction a broken Close Knot Scars: HONOR — "pledged oath, honor-code,
    351:                 # reputation as binding" (descriptor_registry.yaml:conviction_roster). A Close Knot
    352:                 # IS a pledged bond, so breaking one is the honor-code case; no other canonical

### systems/mass_battle/sim/config.py:269   [logs: R4_registries]
  CLAIM: 4. **`hold` (§6) is a five-way collision, and the mass-battle sense is both live code and untouched by the design's disambiguation rules.** The design's rule ("edge kind always written `Tenure(kind=hold)`") does not protect `STANCE_SPEED_MOD['hold']` in `systems/mass_battle/sim/config.py:269`, which is bare, lower-case, unqualified, and load-bearing (a `-99` speed multiplier gating an entire tacti
  CLAIM: | **`hold`/`HOLDS`** | Tenure kind; Proposition mood `HOLDS`; predicate `HOLDS(p,x)`; coercion quantity (4) | **5th, live CODE meaning, not in prose at all**: a mass-battle unit **tactical stance**. `systems/mass_battle/sim/config.py:269` — `STANCE_SPEED_MOD = {"aggressive": 1, "balanced": 0, "hold": -99, "retreat": 0}`; consumed at `systems/mass_battle/sim/hierarchy/units.py:1416,1701` (`if self.
    268: # [CALIBRATED-DEBT: -99 is a structural sentinel (effectively-zero speed for 'hold') — no external source; was tagged `canonical: sim_verification_ledger.json`, the bare-integer self-whitelist deleted in ED-MB-
  >>269: STANCE_SPEED_MOD = {"aggressive": 1, "balanced": 0, "hold": -99, "retreat": 0}
    270: # [ED-MB-0029, Jordan directive 2026-07-23: "a subunit trying to hold ground and defend versus a subunit

### systems/mass_battle/sim/config.py:414   [logs: PR338]
  CLAIM: | A338-011 | claim-about-repo | `derive_command` falls back to hardcoded `command=4` | `massbattle.py:64-95` (`_faction_to_unit`), `core/exchange.py:42-52`, `hierarchy/units.py:2388`, `config.py:414` | `COMMAND_SIGMA_ENABLED` defaults ON but adapter never sets charisma/cognition | measured | VERIFIED-TRUE |
    413: # Charisma (primary weight) + Cognition (secondary weight). All class-B sim-tunable, Jordan-vetoable.
  >>414: COMMAND_SIGMA_ENABLED = _sigma_os.environ.get('COMMAND_SIGMA_ENABLED', '1') == '1'  # default ON per directive; OFF = canonical Size-based pool
    415: COMMAND_POOL_MULT = float(_sigma_os.environ.get('COMMAND_POOL_MULT', '2'))  # [canonical: Jordan directive 2026-06-02 — Command-only base = MULT×Command; =2 matches min(Size,Command)+Command at Size≥Command; cl

### systems/mass_battle/sim/core/exchange.py:42-52   [logs: PR338]
  CLAIM: | A338-011 | claim-about-repo | `derive_command` falls back to hardcoded `command=4` | `massbattle.py:64-95` (`_faction_to_unit`), `core/exchange.py:42-52`, `hierarchy/units.py:2388`, `config.py:414` | `COMMAND_SIGMA_ENABLED` defaults ON but adapter never sets charisma/cognition | measured | VERIFIED-TRUE |
    41: 
  >>42: def derive_command(charisma, cognition):
    43:     """Command DERIVED from Charisma (primary weight) + Cognition (secondary weight).
    44:     [canonical: Jordan canon-structure directive] Command = leadership leverage:
    45:     Charisma primary (inspire/hold the line), Cognition secondary (tactical read).
    46:     Weighted mean on the 1-7 scale, rounded and clamped to 1-7 (derived_stats weighting convention).
    47:     """
    48:     w = CMD_CHA_WEIGHT + CMD_COG_WEIGHT
    49:     val = round((CMD_CHA_WEIGHT * charisma + CMD_COG_WEIGHT * cognition) / w)

### systems/mass_battle/sim/engine.py:387   [logs: R4_registries]
  CLAIM: | **`hold`/`HOLDS`** | Tenure kind; Proposition mood `HOLDS`; predicate `HOLDS(p,x)`; coercion quantity (4) | **5th, live CODE meaning, not in prose at all**: a mass-battle unit **tactical stance**. `systems/mass_battle/sim/config.py:269` — `STANCE_SPEED_MOD = {"aggressive": 1, "balanced": 0, "hold": -99, "retreat": 0}`; consumed at `systems/mass_battle/sim/hierarchy/units.py:1416,1701` (`if self.
    386:     permanently at `stance='hold'` for the whole battle instead of releasing into 'envelop' at
  >>387:     `release_tick`. `stance == 'hold'` is an immediate early-return at the top of BOTH `_node_advance`
    388:     and the legacy `advance_cells` (confirmed by direct read, hierarchy/units.py) -- fully sufficient

### systems/mass_battle/sim/hierarchy/units.py:1416   [logs: R4_registries]
  CLAIM: | **`hold`/`HOLDS`** | Tenure kind; Proposition mood `HOLDS`; predicate `HOLDS(p,x)`; coercion quantity (4) | **5th, live CODE meaning, not in prose at all**: a mass-battle unit **tactical stance**. `systems/mass_battle/sim/config.py:269` — `STANCE_SPEED_MOD = {"aggressive": 1, "balanced": 0, "hold": -99, "retreat": 0}`; consumed at `systems/mass_battle/sim/hierarchy/units.py:1416,1701` (`if self.
    1415:         # Same staleness class as the halted-cell branch below; same fix.
  >>1416:         if self.stance == "hold":
    1417:             for _cid in self.cell_last_speed:

### systems/mass_battle/sim/hierarchy/units.py:2388   [logs: PR338]
  CLAIM: | A338-011 | claim-about-repo | `derive_command` falls back to hardcoded `command=4` | `massbattle.py:64-95` (`_faction_to_unit`), `core/exchange.py:42-52`, `hierarchy/units.py:2388`, `config.py:414` | `COMMAND_SIGMA_ENABLED` defaults ON but adapter never sets charisma/cognition | measured | VERIFIED-TRUE |
    2387:         # Cognition (secondary) when both supplied; else the explicit command stat is used.
  >>2388:         if COMMAND_SIGMA_ENABLED and self.charisma is not None and self.cognition is not None:
    2389:             self.command = derive_command(self.charisma, self.cognition)

### systems/mass_battle/sim/massbattle.py:37-48   [logs: PR337,PR338]
  CLAIM: | A338-069 | formula | Survivor-ratio degree map — 2nd degree semantics | `massbattle.py:37-48` | Thresholds 0.75/0.25/0.50 → Overwhelming/Success/Partial/Failure; own comment states these are NOT the canonical degree ladder | BUILT (unreconciled 2nd ladder) | yes |
  CLAIM: | A337-204 | formula | Survivor-ratio degree map — a second, self-disclosed degree semantics | `_v1_part2.md` §"Mass-Battle Seam" | Thresholds 0.75/0.25/0.50; own comment states these are NOT the canonical degree ladder. | BUILT (unreconciled) | yes, `massbattle.py:37-48` |
    36: 
  >>37: #: Size-ratio -> degree thresholds. CARRIED OVER VERBATIM from the pre-port adapter so that the
    38: #: golden movement this commit causes is attributable to the engine swap and nothing else. These are
    39: #: NOT the canonical degree ladder (`engine/autoload/dice_engine.degree_from_net`, margin-based);
    40: #: they are a bespoke post-hoc classification of a finished battle's survivor ratios, and
    41: #: reconciling the two is open MB-lane work, not a port concern.
    42: # [canonical: carried over unchanged from the pre-port adapter, systems/mass_battle/sim/massbattle.py
    43: #  @ FORK ref e4070d4^ — see this module's header. NOT independently derived: they are the same three
    44: #  survivor-ratio thresholds the campaign has used since Phase 7, preserved verbatim so the engine

### systems/mass_battle/sim/massbattle.py:51-62   [logs: PR337,PR338]
  CLAIM: | A338-070 | primitive | `_GarrisonStub` | `massbattle.py:51-62` | Uncontrolled territory's defense = faction-shaped object, Mil=1.5, chosen to approximate pre-engine Ob 2 vs Ob 4 spread | BUILT (recorded gap) | yes |
  CLAIM: | A337-206 | class/type | `_GarrisonStub` — approximated Mil=1.5 for uncontrolled-territory defence | `_v1_part2.md` §"Mass-Battle Seam" | Recorded gap, not canon. | BUILT (`[GAP]`) | yes, `massbattle.py:51-62` |
    50: 
  >>51: class _GarrisonStub:
    52:     """Minimal faction-shaped stub for an uncontrolled territory's garrison.
    53: 
    54:     [GAP: defenderless-territory garrison strength lacks canonical spec; Mil=1.5 roughly matches the
    55:      pre-mass-battle v17 Ob 2 vs Ob 4 single-roll spread. Carried over unchanged.]
    56:     """
    57: 
    58:     def __init__(self, name, Mil):

### systems/mass_battle/sim/massbattle.py:64-95   [logs: PR337,PR338]
  CLAIM: | A338-011 | claim-about-repo | `derive_command` falls back to hardcoded `command=4` | `massbattle.py:64-95` (`_faction_to_unit`), `core/exchange.py:42-52`, `hierarchy/units.py:2388`, `config.py:414` | `COMMAND_SIGMA_ENABLED` defaults ON but adapter never sets charisma/cognition | measured | VERIFIED-TRUE |
  CLAIM: | A337-201 | mechanic | `_faction_to_unit` — `[GAP]`-flagged minimum-viable defaults | `_v1_part2.md` §"Mass-Battle Seam" | `power=max(1,round(faction.Mil))`; everything else is a carried-over default; own header states no canonical spec exists for faction→Unit construction. | BUILT (`[GAP]`) | yes, `massbattle.py:64-95` |
    63: def _faction_to_unit(faction):
  >>64:     """Build a canon-engine Unit from a strategic-layer faction.
    65: 
    66:     Field-for-field identical to the pre-port construction. The canon engine's `Unit`/`Subunit`
    67:     dataclasses accept the same names, which is why this survived the swap unchanged — and it is
    68:     also why the swap is a clean single-variable experiment on the RESOLUTION model.
    69:     """
    70:     # [canonical: mass_battle_integration_v30.md §4.10 sub-step 3 — the strategic entry point. ⚠ THE
    71:     #  VALUES BELOW ARE NOT CANON AND THIS COMMENT DOES NOT CLAIM THEY ARE. They are the pre-port

### systems/mass_battle/sim/massbattle.py:64-96   [logs: PR338]
  CLAIM: | 8 | `_faction_to_unit` hardcodes command=4, discipline=5, morale=5, position (8,12) for both sides | **VERIFIED-TRUE** (`massbattle.py:64-96`) |
    63: def _faction_to_unit(faction):
  >>64:     """Build a canon-engine Unit from a strategic-layer faction.
    65: 
    66:     Field-for-field identical to the pre-port construction. The canon engine's `Unit`/`Subunit`
    67:     dataclasses accept the same names, which is why this survived the swap unchanged — and it is
    68:     also why the swap is a clean single-variable experiment on the RESOLUTION model.
    69:     """
    70:     # [canonical: mass_battle_integration_v30.md §4.10 sub-step 3 — the strategic entry point. ⚠ THE
    71:     #  VALUES BELOW ARE NOT CANON AND THIS COMMENT DOES NOT CLAIM THEY ARE. They are the pre-port

### systems/mass_battle/sim/massbattle.py:99-146   [logs: PR337,PR338]
  CLAIM: | A337-200 | mechanic | `resolve_mass_battle(a,b,terrain,world)` — only crossing from faction scale into battle engine | `_v1_part2.md` §"Mass-Battle Seam" | Builds two Units, runs 18-tick battle under scoped RNG holder, reads survivor ratios, returns degree. | BUILT | yes, `massbattle.py:99-146` |
  CLAIM: | A338-067 | mechanic | `resolve_mass_battle(a,b,terrain,world)` | `systems/mass_battle/sim/massbattle.py:99-146` | Strategic entry point; builds two Units, runs 18-tick battle under scoped RNG holder, reads survivor ratios, returns a degree; only crossing from faction scale into battle engine | BUILT | yes |
    98: 
  >>99: def resolve_mass_battle(faction_a, faction_b, terrain, world):
    100:     """Strategic entry point for Military Conquest resolution.
    101: 
    102:     Per `mass_battle_integration_v30.md §4.10 sub-step 3` and `canon/02_canon_constraints.md` §B
    103:     GD-1: produces faction stat / territorial-control deltas only — no mass_battle_outcome ->
    104:     game_victory trigger.
    105: 
    106:     Returns {'attacker_wins', 'degree', 'attacker_size_pct', 'defender_size_pct'}.

### systems/mass_battle/sim/massbattle.py:99   [logs: PR337,PR338]
  CLAIM: | A338-068 | primitive | `terrain` param dead at both ends | `massbattle.py:99` signature; `faction_action.py:467` caller | Declared param never referenced in function body; only caller passes `terrain=None` | measured (defect) | INERT |
  CLAIM: | A337-203 | param/constant | `terrain` parameter — declared, appears nowhere in function body | `_v1_part2.md` §"Mass-Battle Seam" | Only caller passes `terrain=None` with a deferral comment. | INERT | VERIFIED-TRUE, signature at `massbattle.py:99`; caller `faction_action.py:467` |
    98: 
  >>99: def resolve_mass_battle(faction_a, faction_b, terrain, world):
    100:     """Strategic entry point for Military Conquest resolution.

### systems/overview/sim/accounting.py:54-93   [logs: PR337]
  CLAIM: | A337-138 | mechanic | Province-Accord drift probe — the only thing connecting two uncoordinated Accord models | `_v1.md` §3 | `accounting.py:54-93`, report-only. | BUILT, report-only | yes |
    53: 
  >>54: def _probe_province_accord_drift(world) -> None:
    55:     """W3 Handoff item 2 (ED-IN-0091 plan §3 Wave 3, 2026-07-29) — REPORT-ONLY.
    56: 
    57:     Compares, per province, `registry.province_accord` (the §1.3 floor(mean settlement Order)
    58:     aggregate, canonical-index 0-4 [`math.floor` of an average over 0-5 members clamps the same
    59:     range `canonical_accord` below produces]) against the live `Territory.accord` field
    60:     (game_state.py's own continuous 0.5-7.0 representation), converted to ITS OWN canonical-index
    61:     0-4 via `game_state.canonical_accord` for a like-for-like comparison — never mixing the two

### systems/overview/sim/accounting.py:96-143   [logs: PR337,PR338]
  CLAIM: | A338-087 | process | `run_accounting` | `systems/overview/sim/accounting.py:96-143` | CI, MS year-end decay, insurgency triggers/promotion, NPC ecology, accord-drift probe; NO faction Mandate aggregation, NO Treasury accrual, NO upkeep, NO Stability check | BUILT | yes |
  CLAIM: | A337-091 | process | `run_accounting` — no faction step at all | `_v1.md` §3 | CI/MS decay, insurgency triggers/promotion, NPC ecology, accord drift probe; no Mandate aggregation, Treasury accrual, upkeep, or Stability check. | BUILT | yes, `accounting.py:96-143` |
    95: 
  >>96: def run_accounting(world):
    97:     """End-of-season accounting pass.
    98: 
    99:     Order:
    100:       1. CI seasonal calculation (PP-412 5-step) — every season
    101:       2. MS baseline decay (PP-255) — Year-End only (every SEASONS_PER_YEAR seasons)
    102:       3. Insurgency triggers (GD-3 a-b: emergence) — every season
    103:       4. Insurgency promotions (GD-3 c-e: insurgency→faction) — every season

### systems/overview/sim/accounting.py:96-120   [logs: PR341]
  CLAIM: | T8-8 | T8 | DEFECT | Claimed vs executing: suite's contribution is 0% executable; only pre-suite loop executes headless today (`mc_v18.py:124-217` + `accounting.py:96-120`) | none proposed | y |
  CLAIM: | A341-059 | loop/phase | pre-suite strategic loop (what executes today) | `mc_v18.py:124-217` + `accounting.py:96-120` | Faction actions, contest dispatch, parliamentary vote; CI, MS, insurgency emergence and promotion, NPE stance drift | VERIFIED TRUE — this is the entirety of what runs headless today | yes — live |
    95: 
  >>96: def run_accounting(world):
    97:     """End-of-season accounting pass.
    98: 
    99:     Order:
    100:       1. CI seasonal calculation (PP-412 5-step) — every season
    101:       2. MS baseline decay (PP-255) — Year-End only (every SEASONS_PER_YEAR seasons)
    102:       3. Insurgency triggers (GD-3 a-b: emergence) — every season
    103:       4. Insurgency promotions (GD-3 c-e: insurgency→faction) — every season

### systems/overview/sim/accounting.py:124-133   [logs: PR337]
  CLAIM: | A337-143 | process | GD-3 Revolt→Insurgency→Faction pipeline — stages 3-4 built, 1-2 stub, promotion unreachable | `_v1.md` §3 | Called from `accounting.py:124-133`. | mixed | partial |
    123:     # invoke check_insurgency_triggers directly.
  >>124:     # [canonical: canon/02_canon_constraints.md §B GD-3 a-b]
    125:     check_insurgency_triggers(world)
    126: 
    127:     # GD-3 (c)-(e) — Insurgency promotion. Checks each existing insurgency for
    128:     # L≥3 / 2+ territories / Accord≥4 / 2-season streak; promotes to either
    129:     # parliamentary candidate (PT≥3 avg) or RM extra-parliamentary (PT<3 avg).
    130:     # Iterate over a snapshot since promotion may mutate the insurgencies dict.
    131:     # [canonical: canon/02_canon_constraints.md §B GD-3 c-e]

### systems/overview/sim/accounting.py:139   [logs: PR337]
  CLAIM: | A337-162 | mechanic | `simulate_npc_actions` — runs every season over an empty dict, return discarded | `_v1_part2.md` §"People" | Called unconditionally from accounting step 5. | BUILT (no-op) | yes, called at `accounting.py:139` |
  CLAIM: | A337-040 | determinism | Dedicated NPE `random.Random` substream, before any NPC loader | `HANDOFF_IN.md:3779-3782` | A two-NPC load moved the seed-42 campaign winner via `world.rng` phase (`npe.py:361,385` draws inside a per-pair loop, wired at `accounting.py:139`); population guards observe `world.npc_counter`, which a direct loader never touches. | FILED (next action, precondition) | VERIFIED
    138:     # [canonical: systems/fieldwork/investigation_systems_v30.md SYSTEM 1 §Persistence]
  >>139:     simulate_npc_actions(world)
    140: 

### systems/overview/sim/ci_track.py:177   [logs: PR341]
  CLAIM: | A341-084 | module/subsystem | `ms_track.py` / `ci_track.py` | `ms_track.py:69,90`, `ci_track.py:177` | Write literal `'MS'` / `'CI'` clocks | VERIFIED TRUE — the only two hand-written clock trackers in the engine | yes — live |
  CLAIM: `ci_track.py:177`; `mass_seizure.py:214`; `excommunication.py:163-171`. Conclusion: no site anywhere
    176:     new_value = max(CI_FLOOR, min(CI_CEILING, current + delta))
  >>177:     world.clocks['CI'] = new_value
    178:     return new_value

### systems/overview/sim/ms_track.py:69   [logs: PR341]
  CLAIM: | A341-084 | module/subsystem | `ms_track.py` / `ci_track.py` | `ms_track.py:69,90`, `ci_track.py:177` | Write literal `'MS'` / `'CI'` clocks | VERIFIED TRUE — the only two hand-written clock trackers in the engine | yes — live |
  CLAIM: opened and read (not just counted): `game_state.py:338,368,439`; `victory.py:73`; `ms_track.py:69,90`;
    68:     new_value = _clamp(current + MS_BASELINE_DECAY_PER_YEAR)
  >>69:     world.clocks['MS'] = new_value
    70:     return new_value

### systems/overview/sim/season.py:42   [logs: R2_systems]
  CLAIM: | `SeasonResult` | `systems/overview/sim/season.py:42` | season, arc, new_arc, accounting_run | overview -- three-phase loop return shape (see 9) |
    41: @dataclass
  >>42: class SeasonResult:
    43:     """Per-season summary for caller (mc_v18, Godot scene controller, tests)."""

### systems/overview/sim/season.py:59-72   [logs: R2_systems]
  CLAIM: | **Six loop steps (CALENDAR.MATTER.DELIBERATE.RESOLVE.WITNESS.CENSUS)** | three-phase tick: `SEASON_TICK -> ACTION -> ACCOUNTING_BOUNDARY` | `engine/autoload/engine_clock.py:90` (`run_tick`); documented `systems/overview/sim/season.py:59-72` | **coarser, not a match** | engine_clock owns composition (ED-IN-0199, code-side ruling); no MATTER/DELIBERATE/WITNESS/CENSUS split exists -- see 8 GAPS and
    58: 
  >>59:     The composition is canonical:
    60:     The composition below is engine_clock's, restated for a reader of this file — it is NOT
    61:     performed here and editing this docstring changes nothing:
    62:       SEASON_TICK: season_manager.advance_season — advances season counter,
    63:               detects arc boundary, fires per-arc and per-season faction
    64:               flag resets (Faction.reset_arc / reset_seasonal).
    65:       ACTION: action_callback(world) — faction actions, scene resolution,
    66:               whatever the caller wants to inject this season.

### systems/settlements/sim/adjacency.py:9-26   [logs: PR338]
  CLAIM: | A338-102 | primitive | `ADJACENCY` — asymmetric graph | `systems/settlements/sim/adjacency.py:9-26` | 16 keys (T1-T15,T17); T16 only a value in T1's set — any BFS walk to T16 dead-ends | measured | VERIFIED-TRUE |
    8: 
  >>9: ADJACENCY: dict[str, set[str]] = {
    10:     'T1':  {'T2', 'T5', 'T14', 'T16'},
    11:     'T2':  {'T1', 'T3', 'T9', 'T14'},
    12:     'T3':  {'T2', 'T9', 'T17'},
    13:     'T4':  {'T7', 'T12', 'T14'},
    14:     'T5':  {'T1', 'T6', 'T14'},
    15:     'T6':  {'T5', 'T13', 'T15'},
    16:     'T7':  {'T4', 'T8'},

### systems/settlements/sim/infrastructure.py:78   [logs: R2_systems]
  CLAIM: | `InfrastructureState`, `BuildResult` | `systems/settlements/sim/infrastructure.py:78,103` | territory_id-keyed infra tiers | settlements |
    77: @dataclass
  >>78: class InfrastructureState:
    79:     """Per-territory Church infrastructure state per §1.5 four-axis model."""

### systems/settlements/sim/infrastructure.py:236   [logs: PR337]
  CLAIM: | A337-075 | formula | Seizure Ob modifier stack (Chapel 0…Inquisitor −1, capped −4) | `_v1.md` §3 | `infrastructure.py:236`. | BUILT (unreached) | yes, called by `mass_seizure.py` |
    235: 
  >>236: def seizure_ob_modifier(territory_id: str, world=None) -> int:
    237:     """Per §1.5 Seizure Ob Modifiers — stacking, capped at -4.

### systems/settlements/sim/ledger.py:30   [logs: PR337,PR338]
  CLAIM: | `ledger.py`'s `TAG_KINDS` is closed at `{Precedent, Grudge, Debt, Reputation, Leverage}` (5, not 6) | A337-001/223 | **VERIFIED-TRUE** — `systems/settlements/sim/ledger.py:30` |
  CLAIM: | A337-001 | invariant/refusal | `ledger.py` `TAG_KINDS` closed 5-family enum (Leverage, not Compact) | `research/cross_scale_action_catalogue_v1.md:267-272` (diff, PR #337) | Corrects the catalogue's own prior error: the enum is `{Precedent, Grudge, Debt, Reputation, Leverage}`, closed at `systems/settlements/sim/ledger.py:30`. | FILED (fix) | yes — VERIFIED-TRUE, `ledger.py:30` |
  CLAIM: | A338-089 | primitive | `Settlement.ledger` / `TAG_KINDS` (5-family enum) | `registry.py:88`; `ledger.py:30` | `{Precedent, Grudge, Debt, Reputation, Leverage}` closed set; Compact ruled a Debt subtype (ED-IN-0046 D3, 2026-07-13); nothing writes tags | BUILT (enum)/INERT (no writers) | VERIFIED-TRUE (grep confirms `Leverage`, not `Compact`) |
  CLAIM: | 6 | `TAG_KINDS` = `{Precedent, Grudge, Debt, Reputation, Leverage}` (Leverage, not Compact) | **VERIFIED-TRUE** (`ledger.py:30`) |
    29: # §1.6 tag kinds
  >>30: TAG_KINDS = {"Precedent", "Grudge", "Debt", "Reputation", "Leverage"}
    31: # Reputation is a single read of the governor — latest wins (replaces prior).

### systems/settlements/sim/ledger.py:36   [logs: R2_systems]
  CLAIM: | `LedgerTag` | `systems/settlements/sim/ledger.py:36` | (kind, key, value, season ...) | settlements -- closest `StateChange`-log primitive |
    35: @dataclass
  >>36: class LedgerTag:
    37:     kind: str

### systems/settlements/sim/registry.py:54   [logs: PR337]
  CLAIM: | A337-114 | class/type | Settlement — 25 fields, 8 populated | `_v1.md` §3 settlement-governance | `registry.py:54`. | BUILT (8/25) | partial |
    53: 
  >>54: @dataclass
    55: class Settlement:

### systems/settlements/sim/registry.py:55   [logs: R2_systems]
  CLAIM: | `Settlement` | `systems/settlements/sim/registry.py:55` | `sid, name, stype, province_id, owner_faction, governor_id, prosperity, defense, order, fort_level, garrison, legitimacy, popular_support, facility_tier, suspicion, pressure, active_directive, religious_building, church_attention, governor_emergence, subnational{}, npc_ids[], ledger[], open_needs[], deck_state{}` + `.ap` property | settle
  CLAIM: | **Site / settlement** | `Settlement` dataclass | `systems/settlements/sim/registry.py:55` | good match | closest existing 1:1 analog to the design's `Site` |
    54: @dataclass
  >>55: class Settlement:
    56:     sid: str

### systems/settlements/sim/registry.py:61   [logs: PR338]
  CLAIM: | A338-010 | claim-about-repo | `Settlement.governor_id` is None on all 37 settlements | `registry.py:61` | No writer other than the uncalled `succeed_governor` | measured | VERIFIED (consistent with #9) |
    60:     owner_faction: str | None = None
  >>61:     governor_id: str | None = None
    62:     # §1.3 stats (0-5)

### systems/settlements/sim/registry.py:69-74   [logs: PR338]
  CLAIM: | A338-088 | primitive | `Settlement.legitimacy`/`popular_support` (LPS) | `registry.py:69-74` | Aggregation input to faction Mandate + consent gate on governance cascade; declared/serialised, ZERO reads/writes outside to_dict/from_dict | INERT | no |
    68:     # §1.8 per-settlement political acceptance (0-7)
  >>69:     # [PRE-LPS-1 (schema gap, NOT a port block) — ED-FA-0004, 2026-07-07: legitimacy/popular_support are declared
    70:     #  but NEVER READ OR WRITTEN anywhere in sim/ (zero non-definition references) — an INERT LPS-1
    71:     #  schema stub, not a working per-settlement L/PS pipeline (R2's qualification to U-1). Wiring
    72:     #  them into a Mandate aggregate is ED-FA-0004 Stratum-B work. The self-imposed port block is
    73:     #  DELETED (Jordan, 2026-08-15 — ED-IN-0193); the gap is unchanged and ED-FA-0004 is still open.]
    74:     legitimacy: int = 0
    75:     popular_support: int = 0

### systems/settlements/sim/registry.py:79   [logs: PR338]
  CLAIM: | A338-090 | primitive | `Settlement.pressure` (Π), default 4.0 | `registry.py:79` | Would drive event-deck draw count + family bias; zero writers, no homeostat code on main | INERT | no |
    78:     suspicion: int = 0
  >>79:     pressure: float = 4.0
    80:     # deck/dossier-referenced state (verify sim-F1/F2/F3/F5/F9)

### systems/settlements/sim/registry.py:82   [logs: PR338]
  CLAIM: | A338-094 | mechanic | Church infrastructure store — wrong grain | `infrastructure.py` vs `registry.py:82` | `InfrastructureState` keyed by TERRITORY; canon specifies per-SETTLEMENT — silently coarsened | BUILT at wrong grain | yes |
    81:     active_directive: str | None = None
  >>82:     religious_building: str = "None"   # None|Chapel|Church|Cathedral
    83:     church_attention: int = 0

### systems/settlements/sim/registry.py:88   [logs: PR338]
  CLAIM: | A338-089 | primitive | `Settlement.ledger` / `TAG_KINDS` (5-family enum) | `registry.py:88`; `ledger.py:30` | `{Precedent, Grudge, Debt, Reputation, Leverage}` closed set; Compact ruled a Debt subtype (ED-IN-0046 D3, 2026-07-13); nothing writes tags | BUILT (enum)/INERT (no writers) | VERIFIED-TRUE (grep confirms `Leverage`, not `Compact`) |
    87:     npc_ids: list = field(default_factory=list)
  >>88:     ledger: list = field(default_factory=list)        # list[LedgerTag]
    89:     open_needs: list = field(default_factory=list)    # [(card_id, pressure_if_ignored)]

### systems/settlements/sim/registry.py:92-97   [logs: PR338]
  CLAIM: | A338-092 | formula | `AP = 2 + facility_tier + (1 if Seat/Cathedral)` | `registry.py:92-97` | Season action budget; property computes correctly, ZERO readers; all 37 settlements load facility_tier=0 | INERT | no |
    91: 
  >>92:     @property
    93:     def ap(self) -> int:
    94:         """Administration Points/season = 2 + FacilityTier, +1 at a Seat/Cathedral
    95:         (governance_play_redesign §1.1)."""
    96:         bonus = 1 if self.stype in ("Seat", "Cathedral", "Cathedral-City") else 0
    97:         return 2 + self.facility_tier + bonus
    98: 

### systems/settlements/sim/registry.py:94-96   [logs: PR337]
  CLAIM: | A337-110 | class/type | Settlement AP — `AP=2+facility_tier`, only live seasonal budget primitive | `_v1.md` §3 | `registry.py:94-96`; +1 at Seat/Cathedral. | BUILT | yes, but zero readers per Part 1 §3 settlement flatten |
    93:     def ap(self) -> int:
  >>94:         """Administration Points/season = 2 + FacilityTier, +1 at a Seat/Cathedral
    95:         (governance_play_redesign §1.1)."""
    96:         bonus = 1 if self.stype in ("Seat", "Cathedral", "Cathedral-City") else 0
    97:         return 2 + self.facility_tier + bonus

### systems/settlements/sim/registry.py:185   [logs: PR337]
  CLAIM: | A337-120 | formula | `province_accord=floor(mean member Order)` — one report-only production caller | `_v1.md` §3 | `registry.py:185`. | BUILT | yes, read-only |
    184: 
  >>185: def province_accord(province_id: str, world=None) -> int:
    186:     """§1.3: province Accord = floor(mean settlement Order) over real members.

### systems/settlements/sim/registry.py:199-208   [logs: PR338]
  CLAIM: | A338-009 | claim-about-repo | `succeed_governor` has zero production callers | `systems/settlements/sim/registry.py:199-208` | Correctly swaps governor + sweeps ledger (durable tags survive); its only "caller" is its own docstring comment | measured | VERIFIED-TRUE |
    198: 
  >>199: def succeed_governor(sid: str, new_governor: str | None, world=None, season: int = 0):
    200:     """Replace the governor; durable Ledger (ttl=None) and settlement stats
    201:     survive — the player->world persistence guarantee (sim_build_spec §2).
    202:     Transient tags (ttl set) are swept on the season boundary."""
    203:     s = get_settlement(sid, world)
    204:     if s is None:
    205:         raise KeyError(f"settlement not in registry: {sid}")
    206:     s.governor_id = new_governor

### systems/settlements/sim/registry.py:212-270   [logs: PR338]
  CLAIM: | A338-097 | process | World-gen settlement load | `registry.py:212-270` `populate_from_geography` | Registers 37 settlements from `settlements:` block only — 8/25 fields load; the `provinces:` block (17 provinces, anchor/polygon/fort_level/spiritual_weight/proximity_calamity/starting_pros/membership) has NO code reader | BUILT (partial) | yes, incomplete |
    211: def reset_registry(world=None):
  >>212:     """Test helper."""
    213:     settlement_store(world).clear()
    214: 
    215: 
    216: def populate_from_geography(world, path: str | None = None) -> int:
    217:     """OI-07 (ED-IN-0091 plan §3 Wave 2 item 4) — register every settlement from the canonical
    218:     geography source at world-gen. Deterministic: no RNG draw, so this cannot move any
    219:     RNG-derived campaign golden (win_share / battles_mean / scenes_resolved all read

### systems/settlements/sim/settlement.py:45   [logs: R2_systems]
  CLAIM: | `ProvinceState` / `SettlementState` | `systems/settlements/sim/settlement.py:45,65` | derived/computed per-tick state | settlements |
    44: @dataclass
  >>45: class SettlementState:
    46:     """Derived-value view of a settlement per §1.3.

### systems/settlements/sim/temperaments.py:117   [logs: R4_registries]
  CLAIM: | SE | ED-SE-0050 | 08-25 | landed | Drift-store read/write asymmetry (`temperaments.py:117` vs `:153`) |
    116: 
  >>117:     Returns dict with keys: alpha, beta, temperament, drift, drift_applied.
    118:     """

### systems/social_contest/sim/parliamentary_vote.py:130   [logs: PR337]
  CLAIM: | A337-094 | class/type | Persuasion Track — one-call-lifetime int, 1-10 start 5 | `_v1.md` §3 | The vote's only state. | BUILT | yes, `parliamentary_vote.py:130,161-205` |
    129:     with parliamentary == False are ineligible (excluded from the vote entirely).
  >>130:     """
    131:     res = VoteResult(status="committee", final_track=PERSUASION_TRACK_START_DEFAULT)

### systems/social_contest/sim/parliamentary_vote.py:139-144   [logs: PR337]
  CLAIM: | A337-093 | class/type | `Faction.parliamentary` boolean — entire franchise model | `_v1.md` §3 | Enforces GD-3 at both proposal and ballot. | BUILT | yes, `game_state.py:111`, `parliamentary_vote.py:139-144` |
    138:         if f is None:
  >>139:             res.notes.append(f"declaration for unknown faction '{d.faction_name}' ignored")
    140:             continue
    141:         if not f.parliamentary:                       # [canonical: GD-3 — extra-parliamentary cannot cast votes]
    142:             res.ineligible.append(d.faction_name)
    143:             res.notes.append(f"GD-3: '{d.faction_name}' extra-parliamentary (parliamentary=False) — excluded")
    144:             continue
    145:         declared.add(d.faction_name)

### systems/social_contest/sim/parliamentary_vote.py:154-159   [logs: PR337]
  CLAIM: | A337-096 | formula | Abstainer resistance formula | `_v1.md` §3 | +1 per abstaining faction at Sta≥6, capped +2 — only way non-declaring faction affects a vote. | BUILT | yes, `parliamentary_vote.py:154-159` |
    153:     # §10 step 3 — resistance: +1 per abstaining Stability>=6 faction, cap +2.
  >>154:     resistance = 0
    155:     for name in res.abstainers:
    156:         if world.factions[name].Sta >= BG_VOTE_ABSTAIN_STABILITY_THRESHOLD:
    157:             resistance += BG_VOTE_ABSTAIN_RESISTANCE_PER
    158:     res.resistance = resistance = min(resistance, BG_VOTE_RESISTANCE_MAX)
    159: 
    160:     # §10 step 4 + ED-621 — starting track = 5 ± lobbying (offset capped ±2), start clamped to [4,6].

### systems/social_contest/sim/parliamentary_vote.py:165-205   [logs: PR337,PR338]
  CLAIM: | A337-095 | mechanic | §10 vote resolver | `_v1.md` §3 | Per-side pool=sum of `int(L)` across declaring factions +1D genre +1D audience; TN7; track≥7 pass, ≤3 fail, else committee. | BUILT | yes, `parliamentary_vote.py:165-205` |
  CLAIM: | A338-143 | mechanic | §10 vote resolver (Parliamentary) | `systems/social_contest/sim/parliamentary_vote.py:165-205` | Per-side pool=Σint(L) of declaring factions +1D genre match +1D audience match; TN7; track≥7 pass, ≤3 fail, else committee | BUILT | yes |
    164: 
  >>165:     # Resolution: per-side Mandate pool (sum of L) + genre/audience bonuses; roll TN 7.
    166:     def _resolve(decls):
    167:         if not decls:
    168:             return 0, 0, []
    169:         mandate = sum(int(world.factions[d.faction_name].L) for d in decls)  # [canonical: §10 "sum of Mandate"; Mandate==Faction.L]
    170:         genre = _side_genre(decls)
    171:         pool = mandate
    172:         if genre == motion.primary_genre:

### systems/social_contest/sim/parliamentary_vote.py:207-219   [logs: PR337,PR338]
  CLAIM: | A337-097 | formula | Total Victory rider — permanent penalty (defect) | `_v1.md` §3 | Track≥9 or ≤1 strips L−1 from losing coalition's highest-L faction; own comment says "one-season" but `season_manager` has no temporary-modifier facility, so it's permanent and compounds. | BUILT (defect) | yes, `parliamentary_vote.py:207-219`, defect at `:216-218` |
  CLAIM: | A338-144 | formula | Total Victory rider (permanent, self-flagged bug) | `parliamentary_vote.py:207-219,216-218` | Track≥9/≤1 strips L−1 (20 granular) from losing coalition's highest-L faction; own comment says "[one-season penalty...deferred to season_manager]" — `season_manager` has no temp-modifier facility; penalty IS permanent and compounds | BUILT (defect, self-disclosed) | yes |
    206: 
  >>207:     # §10 Total Victory — track >= 9 or <= 1 -> losing coalition's dominant faction Mandate -1.
    208:     if track >= PERSUASION_TOTAL_VICTORY or track <= PERSUASION_TOTAL_DEFEAT:
    209:         res.total_victory = True
    210:         losing = side_b if track >= PERSUASION_TOTAL_VICTORY else side_a
    211:         losing_names = [d.faction_name for d in losing]
    212:         if losing_names:
    213:             dominant = max(losing_names, key=lambda n: world.factions[n].L)   # dominant = highest Mandate (L)
    214:             world.factions[dominant].adjust("L", BG_VOTE_TOTAL_VICTORY_MANDATE_DELTA * MULTS["L"])

### systems/world/sim/insurgency_pipeline.py:139-196   [logs: PR338]
  CLAIM: | A338-107 | mechanic | Insurgency formation/promotion | `systems/world/sim/insurgency_pipeline.py:139-196,199-255` | Formation: 2+ contiguous Uncontrolled territories, sustained 2 seasons. Promotion: L≥3 ∧ territories≥2 ∧ avg Accord≥4, sustained 2. `InsurgencyRecord.L` set once to 1.0 at formation, NEVER written again → promotion unreachable | BUILT + unreachable | formation yes, promotion no |
    138: 
  >>139: def check_insurgency_triggers(world) -> list[InsurgencyEvent]:
    140:     """Check GD-3 (a)-(b) Insurgency formation triggers.
    141: 
    142:     Detects groups of 2+ contiguous Uncontrolled territories. Tracks
    143:     consecutive-season streaks; when a group sustains 2+ seasons, fires
    144:     an Insurgency formation event.
    145:     """
    146:     events = []

### systems/world/sim/npe.py:107-122   [logs: PR337]
  CLAIM: | `world.npc_counter` is incremented ONLY inside `generate_npc`, never by a direct NPC-object construction | A337-146/216 | **VERIFIED-TRUE** — `systems/world/sim/npe.py:107-122`; the counter's only writer is the module-level `_bump_counter`-style logic inside `generate_npc`'s call path |
    106: _npcs_by_territory: dict[str, list["NPC"]] = {}
  >>107: _npc_counter = [0]
    108: 
    109: 
    110: def _npc_store(world):
    111:     if world is not None and hasattr(world, 'npcs'):
    112:         return world.npcs
    113:     return _npcs_by_territory
    114: 

### systems/world/sim/npe.py:116-122   [logs: PR337]
  CLAIM: | A337-146 | class/type | `world.npc_counter` — increment-only, bumped only inside `generate_npc` | `_v1_part2.md` §"People" | The three "world is empty" guards observe THIS, not `world.npcs`. | BUILT | VERIFIED-TRUE, `npe.py:116-122` |
    115: 
  >>116: def _next_npc_id(world):
    117:     """Increment and return the next NPC id counter (world-keyed or module-level)."""
    118:     if world is not None and hasattr(world, 'npc_counter'):
    119:         world.npc_counter += 1
    120:         return world.npc_counter
    121:     _npc_counter[0] += 1
    122:     return _npc_counter[0]
    123: 

### systems/world/sim/npe.py:118-122   [logs: PR337]
  CLAIM: | A337-040 | determinism | Dedicated NPE `random.Random` substream, before any NPC loader | `HANDOFF_IN.md:3779-3782` | A two-NPC load moved the seed-42 campaign winner via `world.rng` phase (`npe.py:361,385` draws inside a per-pair loop, wired at `accounting.py:139`); population guards observe `world.npc_counter`, which a direct loader never touches. | FILED (next action, precondition) | VERIFIED
    117:     """Increment and return the next NPC id counter (world-keyed or module-level)."""
  >>118:     if world is not None and hasattr(world, 'npc_counter'):
    119:         world.npc_counter += 1
    120:         return world.npc_counter
    121:     _npc_counter[0] += 1
    122:     return _npc_counter[0]
    123: 

### systems/world/sim/npe.py:125-145   [logs: PR337,PR338]
  CLAIM: | A337-144 | class/type | `NPC` dataclass — 5 axes + provenance, 12 fields | `_v1_part2.md` §"People" primitives | Jordan ruled 2026-08-11 no PC/NPC split — this IS the character schema: stance, worldview, affiliation, compromise, volatility + ids/provenance. | BUILT | yes, `npe.py:125-145` |
  CLAIM: | A338-112 | class/type | `NPC` dataclass — 5 axes + provenance | `systems/world/sim/npe.py:125-145` | stance, worldview, affiliation_faction/loyalty, hidden_allegiance, compromise_category, volatility, persistent_state; Jordan ruled 2026-08-11 no PC/NPC split — this IS the character schema | BUILT | class exists, population empty |
    124: 
  >>125: @dataclass
    126: class NPC:
    127:     """Generated NPC matching §NPC Genome Record 5-axis structure."""
    128:     npc_id: str
    129:     territory_id: str
    130:     # Axis 1: Stance (per active issue, 1-5)
    131:     stance: dict[str, int] = field(default_factory=dict)
    132:     # Axis 2: Worldview (1-2 convictions from taxonomy)

### systems/world/sim/npe.py:226-350   [logs: PR338]
  CLAIM: | A338-008 | claim-about-repo | `generate_npc` has no world-gen/season-tick call site | `systems/world/sim/npe.py:226-350`; `engine/mc_v18.py:195` | Complete two-tier generator, only referenced via a `stub_resolve('generate_npc(...)')` string, and in tests | measured | VERIFIED-TRUE |
    225: 
  >>226: def generate_npc(faction: Optional[str], role: Optional[str], world,
    227:                  territory_id: Optional[str] = None,
    228:                  rng=None) -> NPC:
    229:     """Generate an NPC per §Two-Tier Generation.
    230: 
    231:     Tier 1: Archetype seed — ecology weights for the territory populate the
    232:             5 axes with locally-typical values.
    233:     Tier 2: Deviation roll — d6; on 5-6 one axis flips to ecology's opposite

### systems/world/sim/npe.py:326   [logs: PR338]
  CLAIM: | A338-113 | primitive | `NPC.hidden_allegiance` computed then dropped | `npe.py:326` computed, `:336-347` ctor call omits it | One field modeling divergent-interest agent; always None in practice | INERT | computed, never stored |
    325:             # Hidden allegiance != affiliation
  >>326:             other = [f for f in ('Crown', 'Church', 'Hafenmark', 'Varfell', 'RM') if f != npc_faction]
    327:             hidden_allegiance = rng.choice(other) if other else None

### systems/world/sim/npe.py:353-400   [logs: PR338]
  CLAIM: | A338-157 | mechanic (built) | Stance convergence rule | `systems/world/sim/npe.py:353-400` `simulate_npc_actions` | The ONE NPC mechanic that runs every season; walks world.npcs for same-territory pairs sharing a conviction, differing by 1 step, rolls d6 vs avg Volatility; runs over an EMPTY dict, caller discards return | BUILT (no-op given empty population) | yes, runs on nothing |
    352: 
  >>353: def simulate_npc_actions(world) -> list[NPCAction]:
    354:     """End-of-season NPC actions per §Persistence "NPCs with shared worldview
    355:     and adjacent Stance positions make a Volatility check. On pass: both
    356:     shift toward each other's Stance by 1."
    357: 
    358:     Returns list of NPCAction descriptors (mutation already applied).
    359:     """
    360:     actions = []

### systems/world/sim/npe.py:361   [logs: PR337]
  CLAIM: | A337-040 | determinism | Dedicated NPE `random.Random` substream, before any NPC loader | `HANDOFF_IN.md:3779-3782` | A two-NPC load moved the seed-42 campaign winner via `world.rng` phase (`npe.py:361,385` draws inside a per-pair loop, wired at `accounting.py:139`); population guards observe `world.npc_counter`, which a direct loader never touches. | FILED (next action, precondition) | VERIFIED
    360:     actions = []
  >>361:     rng = world.rng if hasattr(world, 'rng') else None
    362:     if rng is None:

### GONE:11   [logs: R3_godot]
  CLAIM: | 11 | `proposals/2026-08-20-return-to-game-plan-v1.md:101` (row D5) | *"**Godot version conflict.** `valoria-game/project.godot:11` declares `features=("4.3")` and CI pins the 4.3 binary… One of the two is wrong; a 4.3 binary parsing a 4.6-authored tree can mis-count the ratchet."* — `OPEN — §7 (Jordan)` |
  CLAIM: | 1 | `CLAUDE.md:10` | *"`project.godot:11` declares `features=("4.3")` and CI pins the 4.3 binary, while this document and `godot/` say 4.6. One of them is wrong… Awaiting a ruling (plan Q3); do not pick one by editing this line."* |
  CLAIM: currently prose-only where the "4.3" side has a `git`-verifiable `project.godot:11` and two headless

### GONE:3371   [logs: PR344]
  CLAIM: | 6 | `principals` | — | WEAK — ordinary usage gives 3 readings + a homophone ("principle") this design uses heavily; `systems/factions/_identifier_census.yaml:3371` uses "principals" for a FOURTH thing (parties present in a scene) | Recommend `leaders(prop,node)` | CLOSED — final arch uses `leaders` |

### godot/skeleton/core/engine_manifest.gd:1-4   [logs: PR344,R3_godot]
  CLAIM: 2026-08-20) and `godot/skeleton/core/engine_manifest.gd:1-4` (GDScript, non-compiling but
  CLAIM: §8.1 "A subsystem is swapped by editing a registry row" — PROVEN on both sides already. Python: `engine/substrate/composition.py:1-24` resolves a role to a callable by string from `engine/engine_params/composition.json`, "Adding a subsystem to the campaign loop is a row in the registry, not an import in the engine" — late failure prevented because "the exporter imports and resolves every declared 
  >>1: ## engine_manifest.gd — one engine's discovery manifest (data). MechanicsRegistry.load_manifests
    2: ## reads data/engines/<id>/<id>.tres, calls to_spec(), and the Kernel builds the engine from it —
    3: ## a new engine is registered by DATA, with zero edits to Kernel/Bus/BaseEngine. Mirrors the
    4: ## references/module_contracts.yaml entry for the engine.
    5: class_name EngineManifest

### godot/skeleton/engines/combat/combat_engine.gd:13   [logs: R3_godot]
  CLAIM: | **The four carriers (Person/Cohort/Rung/Office/Site)** | `RefCounted` per section 2/7 above | **Partially** — `godot/skeleton/`'s `EngineManifest`/`KeyTypeResource` establish the *manifest-as-data* pattern the design's Venue/MatterKind rows already match, but nothing in `godot/` names Person/Cohort/Rung/Office/Site at all — the skeleton only covers `personal_combat`, which is actor-agnostic (wor
    12: class_name CombatEngine
  >>13: extends BaseEngine
    14: 

### godot/skeleton/engines/combat/combat_engine.gd:60   [logs: PR344,R3_godot]
  CLAIM: `combat_engine.gd:60` (`KeyBus.emit_key`) all reach global state from inside a `RefCounted`
  CLAIM: ⚠ **§3.1a — the purity guarantee does NOT survive the port as a type-level guarantee**, and this document downgrades rather than repeats the claim. GDScript has no module system/visibility modifiers/scoping; an autoload is a global identifier reachable from ANY script, RefCounted and Resource included; `class_name` statics and `load()`/`preload()` by string are two more doors. So "omniscience is n
  CLAIM: §3.2 Why FALSE in GDScript — THREE mechanisms, none of which is `Node`: (1) **An autoload is a global identifier, not a Node privilege** — registering `GameState` makes the bare token resolve inside EVERY script, RefCounted/Resource/static function/inner class included. NOT hypothetical: `strike_module.gd:38-39` calls `GameState.get_actor(...)` from inside a resolution module, `:67` writes `GameSt
    59: 	k.payload = {"scene_id": fight_id, "participants": fight.get("participants", [])}
  >>60: 	KeyBus.emit_key(k)
    61: 	_active_fights.erase(fight_id)

### godot/skeleton/engines/combat/modules/strike_module.gd:36   [logs: R3_godot]
  CLAIM: | **The three signatures** (`choose`/`resolve`/`witness`) | as section 3/6 above (World-first param, no signals for witness) | **`resolve` partially exists** as `CombatEngine.resolve_round`/`StrikeModule.resolve(key: Key)`, but takes no `World` parameter at all — it reaches `GameState` globally instead (exactly F-1's finding). `choose` and `witness` have **no analogue anywhere in `godot/`.** | `st
    35: 
  >>36: func resolve(key: Key) -> void:
    37: 	var cfg: CombatConfig = config if config != null else CombatConfig.new()

### godot/skeleton/engines/combat/modules/strike_module.gd:38-39   [logs: PR344,R3_godot]
  CLAIM: hypothetical: `strike_module.gd:38-39` (`GameState.get_actor`), `:67` (`GameState.rng.seed =`),
  CLAIM: ⚠ **§3.1a — the purity guarantee does NOT survive the port as a type-level guarantee**, and this document downgrades rather than repeats the claim. GDScript has no module system/visibility modifiers/scoping; an autoload is a global identifier reachable from ANY script, RefCounted and Resource included; `class_name` statics and `load()`/`preload()` by string are two more doors. So "omniscience is n
  CLAIM: §3.2 Why FALSE in GDScript — THREE mechanisms, none of which is `Node`: (1) **An autoload is a global identifier, not a Node privilege** — registering `GameState` makes the bare token resolve inside EVERY script, RefCounted/Resource/static function/inner class included. NOT hypothetical: `strike_module.gd:38-39` calls `GameState.get_actor(...)` from inside a resolution module, `:67` writes `GameSt
    37: 	var cfg: CombatConfig = config if config != null else CombatConfig.new()
  >>38: 	var atk = GameState.get_actor(key.payload.get("attacker", ""))
    39: 	var dfn = GameState.get_actor(key.payload.get("defender", ""))
    40: 	if atk == null or dfn == null:

### godot/skeleton/engines/combat/modules/strike_module.gd:67   [logs: PR344,R3_godot]
  CLAIM: | **Determinism/RNG** (`substream(world_seed,tick,subject_id,purpose)`) | one `RandomNumberGenerator` per operation, constructed-used-discarded | **Direct conflict with the existing skeleton, and the design doc already found it (§7.2/M-15)**: `strike_module.gd:67` — `GameState.rng.seed = key.rng_seed` — is exactly the anti-pattern (shared mutable global RNG, re-seeded in place) the new design's ru
  CLAIM: §7.2 The Godot implementation: **ONE `RandomNumberGenerator` PER OPERATION, CONSTRUCTED FROM THE SUBSTREAM, USED, AND DISCARDED. NEVER A SHARED INSTANCE RE-SEEDED IN PLACE.** Worked code given (`static func substream(...)->RandomNumberGenerator`). **M-15 · MAJOR** — the skeleton's pattern is the one to ABANDON: `strike_module.gd:67` writes `GameState.rng.seed = key.rng_seed` — a single global RNG 
  CLAIM: §7.3 The checklist of what breaks order independence in practice (7 items, each mapped to a Godot landing site): shared RNG (`strike_module.gd:67`, existing precedent); two draws sharing a purpose within one (subject,tick) (m-1 · MINOR — widen the purpose-uniqueness rule to per-draw not merely per mint slot); collecting map results by append (M-11); float summation order (F-4); Dictionary iteratio
    66: 	var pool: int = maxi(cfg.pool_floor, int(round(atk.stat("history"))) + cfg.pool_base)
  >>67: 	GameState.rng.seed = key.rng_seed                                     # per-Key seed → replay-deterministic
    68: 	var net: float = _roll_net_continuous(pool, cfg) + cfg.soft_cap(net_sigma) * cfg.sigma_n(pool)

### godot/skeleton/engines/combat/modules/wound_module.gd:40   [logs: PR344]
  CLAIM: §3.2 Why FALSE in GDScript — THREE mechanisms, none of which is `Node`: (1) **An autoload is a global identifier, not a Node privilege** — registering `GameState` makes the bare token resolve inside EVERY script, RefCounted/Resource/static function/inner class included. NOT hypothetical: `strike_module.gd:38-39` calls `GameState.get_actor(...)` from inside a resolution module, `:67` writes `GameSt
    39: 			continue
  >>40: 		var actor = GameState.get_actor(t.get("actor_id", ""))
    41: 		if actor == null:

### godot/skeleton/engines/combat/modules/wound_module.gd:55   [logs: R3_godot]
  CLAIM: 6. **New finding, not in either audit**: `godot/skeleton/engines/combat/modules/wound_module.gd:55`
    54: 		actor.set("wounds", mini(int(cum / wi), max_w + 1))          # the Wounds track → bilateral-Ob source
  >>55: 		actor.set("health", maxf(0.0, health_full - cum))            # derived display value (read-only)
    56: 

### godot/skeleton/engines/combat/resources/combat_config.gd:9   [logs: PR344]
  CLAIM: §8.3 ⚠ "The module tree is the containment ladder" does NOT cash out in Godot — class_name namespace is FLAT AND GLOBAL, no `Settlement.Person`/`Territory.Person`, one `Person` project-wide. So a module tree can be a directory tree or a data hierarchy, but NOT a type hierarchy mirroring containment. **M-1 · MAJOR** — `Derived` must not be the code-side name, worse in Godot than the design's own re
    8: ##
  >>9: ## Canon model: combat_engine_v1 (ED-900/904; docket ED-1029). D_SIGMA sigma-leverage resolution,
    10: ## additive-coupled damage, bilateral-Ob wounds (ED-1041). NOT the v30 (Agi×2 / TN-7 / mult-STR) model.

### godot/skeleton/engines/combat/resources/combat_config.gd:11-40   [logs: PR344]
  CLAIM: §5.1 Four candidate shapes judged: Resource+@export+.tres (right for authored low-cardinality tables like Venue/MatterKind, wrong for per-entity runtime state — `combat_config.gd:11-40` is the correct use, `:5-9` records the defect it fixed, "two scales, one live, one dead, editing the dead one changed nothing"); a data autoload (right for read-only constants, wrong for live world state); generate
  CLAIM: Full placement table (21 objects): Person→RC one class_name; Cohort→SAME class_name as Person (weight≥1) — "two class_names is the rule broken at declaration" (M-10); Container/Rung→RC under a different name (F-3); Office→RC; Site→RC (condition primary state); Tenure→row in a TenureStore RC (largest object count, cardinality per kind, object-side index derived — one object per edge makes every der
    10: ## additive-coupled damage, bilateral-Ob wounds (ED-1041). NOT the v30 (Agi×2 / TN-7 / mult-STR) model.
  >>11: class_name CombatConfig
    12: extends Resource
    13: 
    14: # ── resolution (r1_sigma_resolution / m1_dice_sigma_core) ──
    15: @export var decisive_ob: float = 3.0          # base Ob, FIXED; sigma-leverage boosts the ROLL, not the Ob (mu-shift)
    16: @export var tn_standard: int = 7              # PER_DIE[7] = (mu 0.40, sigma 0.800)
    17: @export var pool_base: int = 6                # pool = max(pool_floor, History + pool_base)
    18: @export var pool_floor: int = 5

### godot/skeleton/engines/combat/resources/combat_config.gd:29-38   [logs: R3_godot]
  CLAIM: | **typed `Dictionary`** (`Dictionary[K,V]`) — §5, m-4 | **not available**; `@export var qual: Dictionary` is untyped, every read is `Variant`, casts required (`combat_config.gd:29-38`) | **available** (introduced 4.4) | **Yes, and the fallback becomes unnecessary.** Under a genuine 4.6 target the design's own m-4 fallback (`const` array + name→index map, matching `STRAT:150`'s conviction-vector p
    28: ## DELIVERY[head] — head-shape delivery coefficient
  >>29: @export var delivery: Dictionary = {
    30: 	"blunt": 1.6, "point": 1.45, "cut_thrust": 1.35,
    31: 	"straight_cut": 1.5, "curved_cut": 1.5, "cut": 1.5,
    32: }
    33: ## RESIST[material][mode] — material resistance per damage mode, in [0,1]
    34: @export var resist: Dictionary = {
    35: 	"none":  {"percussion": 0.0,  "shear": 0.0,  "puncture": 0.0},
    36: 	"cloth": {"percussion": 0.10, "shear": 0.35, "puncture": 0.15},

### registers/supersession_register.yaml:406-418   [logs: PR337]
  CLAIM: | A337-002 | class/type | Compact-as-`Debt`-subtype | `cross_scale_action_catalogue_v1.md:269-272` (diff) | A recurring term-limited claim is `Debt(key="compact:<quota>", ttl=term, recurs=True)`, not a sixth tag family; ruled ED-IN-0046 D3, 2026-07-13 (`supersession_register.yaml:406-418`). | FILED (fix) | no — the subtype shape has no code writer (INERT primitive) |
    405: 
  >>406:   - superseded_id: 'Compact as a new "5th ledger tag-family" (compendium Part 44/45; grounded_event_card_deck_v1 C.3; governance_play_redesign_v1 §1.3a/§1.6; ED-SE-0018/0019 framing)'
    407:     scope: >
    408:       Multiple governance-cluster docs frame "Compact" (fixed-quota extraction, Encabezamiento) as a new
    409:       5th Ledger-of-Consequence tag-family alongside Precedent/Grudge/Debt/Reputation. The built code
    410:       (systems/settlements/sim/ledger.py:30) has Leverage as the 5th family and no Compact.
    411:     superseded_by: "ED-IN-0046 (governance_consolidation_v1 D3, RATIFIED 2026-07-13)"
    412:     superseded_date: "2026-07-13"
    413:     replacement: >

### skills/valoria-vector-audit/scripts/vector_audit.py:217   [logs: PR340]
  CLAIM: | 12 | `victory.py:73` reads `world.clocks.get('Turmoil', 0.0)`; `game_state.py:338` initializes Turmoil to 0.0; nothing else writes it (13 §4, P0-7) | **VERIFIED-TRUE** — both lines confirmed; grep for all non-test `Turmoil` occurrences finds exactly these two plus one unrelated pattern-matcher (`skills/valoria-vector-audit/scripts/vector_audit.py:217`, not a writer) |
    216:                              'scale': 'peninsula', 'status': 'canonical', 'source': 'seed'},
  >>217:     'Turmoil':    {'patterns': ['Turmoil', 'peninsular_strain'],
    218:                              'scale': 'peninsula', 'status': 'canonical', 'source': 'seed'},

### tests/valoria/test_engine_does_not_import_systems.py:212   [logs: PR340,R1_engine]
  CLAIM: 2. **Confirms exactly** the `engine/` Section 3 CLAUDE.md claim about the `PATH_SEAM_ALLOWED` bare-name seam: `tests/valoria/test_engine_does_not_import_systems.py:212` -- `PATH_SEAM_ALLOWED = {'cross_scale/combat_bridge.py'}`, matching `combat_bridge.py`'s own extensive docstring about the lazy, memoized `sys.path` insert. No contradiction found.
  CLAIM: | 6 | `PATH_SEAM_ALLOWED = {'cross_scale/combat_bridge.py'}`, one entry, shrink-only (12 §3.1) | **VERIFIED-TRUE** — `tests/valoria/test_engine_does_not_import_systems.py:212,288-292` |
    211: #: It can only shrink. Converting it deletes this entry.
  >>212: PATH_SEAM_ALLOWED = {'cross_scale/combat_bridge.py'}
    213: 

### tools/build_contract_index.py:653   [logs: PR341]
  CLAIM: | T8-3 | T8 | UPHELD (wording NIT) | "live: 2 of 27" reproduces but cites a retired tool (`wiring_map_check --summary`); live instrument is `tools/build_contract_index.py:653` | cite the correct tool | y |
  CLAIM: `wiring_map_check --summary`, versus the live `tools/build_contract_index.py:653` (C-5).
    652: 
  >>653: def cmd_summary(contracts):
    654:     """Build-state / godot-status counts over all 35 conversion units.

### tools/export_key_types.py:17-19   [logs: PR344]
  CLAIM: §5.2 THE RULING: the pipeline has FOUR STAGES, ONE GENERATOR PER STAGE BOUNDARY: code/authored prose → `engine/engine_params/*.json` (exists, behind a blocking --check) → generated `res://data/*.tres` (NEVER hand-authored) → one typed RefCounted holder loaded once, owned by the driver, read by value. "This is not a new recommendation — it is the one the port already made and has not executed." `ST
    16: 
  >>17: 1. **Godot re-implements the logic** (Jordan, 2026-08-04) and **cannot parse markdown.** The port's
    18:    answer today is `godot/skeleton/data/key_types/*.tres` — four files, HAND-MADE, covering 4 of 55
    19:    types. A hand shadow of a machine-readable thing is a drift generator; this one is 93% incomplete
    20:    before it has even started drifting.

### workplans/return_to_game_queue.yaml:74-79   [logs: R3_godot]
  CLAIM: | 10 | `workplans/return_to_game_queue.yaml:74-79` | `baselines.game_compile`: *"taken 2026-08-19, **Godot 4.3.stable.official.77dcf97d8**, headless --editor --quit, valoria-game@5e01065"* — `stock: {failed_to_load: 54, parse_errors: 169, broken_scripts: 61}`, down to `{5, 14, 8}` after five defect fixes + one project setting |
    73:   game_compile:
  >>74:     taken: "2026-08-19, Godot 4.3.stable.official.77dcf97d8, headless --editor --quit, valoria-game@5e01065"
    75:     method: "./Godot_v4.3-stable --headless --path . --editor --quit 2>&1 | grep -cE 'Failed to load script|Parse Error'"
    76:     stock:                  { failed_to_load: 54, parse_errors: 169, broken_scripts: 61 }
    77:     setting_only:           { failed_to_load: 54, parse_errors: 161, broken_scripts: 61 }
    78:     five_defects_plus_setting: { failed_to_load: 5, parse_errors: 14, broken_scripts: 8 }
    79:     note: >
    80:       REFUTES the 2026-08-18 handoff's framing that the project setting was "worth more than all

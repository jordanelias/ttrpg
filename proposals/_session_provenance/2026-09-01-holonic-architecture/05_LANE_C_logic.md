# Lane C — logic, sequence, flexibility, emergence

**Model:** Fable 5.1 · **Agent:** `valoria-critic` (Read/Grep/Glob only) · Grep and Glob worked.

## Verdict (lane's own words, condensed)

The central factual claim — exactly two of six steps partition per container — **does not survive the
head's own text**: MATTER is not rung-local, while **CALENDAR and CENSUS are per-owner by exactly the
criterion used to admit MATTER**, so "two of six" is *"an artefact of applying one test to two steps
and a different test to the other four."* The termination argument **contradicts itself between §4.3
and §7**, cites a **cross-season** debt no within-tick bound can touch, and overlooks that **the head
already permits within-tick reaction** bounded by `max_depth`, not barriers. The wrapper survives its
own admission test *"only because the N-line's second question was not pressed."* **The descent and the
census are the strongest parts.** *"The proposal's REFUSALS mostly stand; its REASONS mostly do not."*

## The findings that changed the proposal

| # | claim | what is wrong |
|---|---|---|
| **F1** | MATTER is per-rung, *"nothing crosses"* | **CRITICAL.** *"**MATTER touches persons**, not only places: subsistence drawn from `stores`"* — `stores` is Rung matter, the person is at a person rung. Death: *"`destroy` sets `until` on every Tenure whose subject **or object** is the destroyed id"*, and `tie`/`knot` are *"stored ONCE, on the endpoint with the lower id"* — **so death writes on another person's rung.** Travel legs have **no ownership row at all** |
| **F2** | CALENDAR is global | **CRITICAL — the proposal has it backwards.** By its own criterion CALENDAR is *more* local than MATTER: it writes dates and dockets owned by one Rung or Office, and its predicate *"may read **only** the holder's own state, an R-1 aggregate over its descendants, or the calendar."* **And the ground offered (the season counter) is a module the proposal's own §0 rules out of scope.** The real reason it is not per-*rung* is that dates are held by Offices with `rung? = null` and containerless Venues — **per-HOLDER, not per-rung** |
| **F3** | CENSUS/WITNESS are global | **CRITICAL.** CENSUS writes per owner; its one global requirement is a single snapshot — *"the same shape as DELIBERATE."* WITNESS **stage 2** is *"per person, any order… into that person's own ledger and no other"* — the same map shape. **Only WITNESS stage 1 and RESOLVE's ordered fold are genuinely global bodies** |
| **F4** | DELIBERATE listed under "per-rung or global" | **Per-person ≠ per-container.** *"The `person` Rung is the address slot"*, so a per-person map is trivially per-leaf and *which* container runs it is undefined |
| **F5** | *"Four barriers bound within-tick propagation"* | **CRITICAL — contradicted by the proposal itself**, which also says *"a spiral inside one tick is still unbounded."* **One of the two is false.** The head decides it: a contest *"runs the same steps… inside RESOLVE"* and *"a contest can open a contest"* — **within-tick reaction already exists and its bound is `max_depth`, not the barriers** |
| **F6** | the termination debt | **The cited debt is CROSS-SEASON** — *"four arcs plus the King are spirals"*, and arcs run over seasons. **A within-tick bound cannot address it in either direction**; *"the barriers stand between the design and a DIFFERENT spiral"* |
| **F7** | N-line: *"nothing stops a module emitting a Key two rungs away"* | **CRITICAL — motivated reasoning at exactly the site the task named.** *"Nothing in the head LETS a module emit a targeted Key at all."* The head's own adversarial pass: *"the only transport the suite defines **is** a chain of `tell` acts… **there is no non-act news transport anywhere in the shape**."* Emitters of Candidates do not target either. **A fair Q2 returns FALSE** |
| **F8** | the ONE RULE | **CRITICAL.** The head is not a tree of up/down influence: `knot`/`tie` at any distance, `commit` spanning rungs, `hold` at distance (= annexation), Offices with *"no containment node"*, containerless Venues, and a dispensation whose *"scope enumerates EXECUTORS, not places"*. **A Dicastery's dispensation across three duchies is neither** |
| **F9** | *"COMPLETE"* | **F8 names the second missing property: lateral (heterarchical) relations.** R-1/R-2 govern only the containment tree; **C4's own edge list is mostly non-containment edges.** *"Complete" is not earned* |
| **F10** | the wrapper is *"invisible to every seat"* | **The falsifier fires on the proposal's own D3** — a wrapper-passed extension *"vetoes an Overwhelming"*, which is an outcome differing by wrapper, by design |
| **F12** | the scope rule | **CRITICAL — applied once and waived five times.** Incumbent B's transport; M2's three authored surfaces (all pre-#337 `references/` artifacts); CALENDAR's ground; the "coarser tick" hedge; SUBSYSTEM as a repo lane. *"The rule as written deletes M2; as practised it binds only R-2"* |
| **F13** | D-14: the `Event` record *"never written"* | **True of #350, false of the chain**: an earlier in-chain revision defines it. *"The census's own definition of dropped fits exactly"* — **and that record still has no target, which is F7** |
| **F14** | C4's *"freely and at any depth"* | R-1 licenses an aggregate over **descendants**; following a `knot` reaches another duchy. *"The real split is resolver-side versus rung-local, not structural-versus-monotone"* |
| **F17** | *"Nothing depends on the step count"* | **False for the checks that justify the axis** — under a coarser vocabulary three steps collapse into one column and check 3 is unaskable |
| **F18** | `phase:` single-valued | The matrix writes Date at CALENDAR **and** RESOLVE; *"a write class is not a phase."* **Multi-valued fixes it and weakens check 1** |

Plus minor: two mis-citations, a Godot layout addition not marked as a departure, the `grade`
vocabulary presented as a citation, and *"`04` §7 lists it as mechanical via the return shape"* —
the head says *"the return shape, **plus the order-independence test**."*

## Corrections it would NOT make

- **Not rewrite C1's five-row table** — *"a legible reasoning trail… deleting the foil loses the
  argument's shape for no change in conclusion."*
- **Not correct the `(#340)` attribution** — UNVERIFIED without git; *"a guess is worse than the
  possible error."*
- **Not revert the `grade` merge** — *"one vocabulary with `absent` is arguably better than two; the
  defect is presenting it as a citation."*
- **Not touch the head's own internal contradictions** — *"a proposal that 'fixes' its head in passing
  is the drop-at-restatement mechanism the census diagnoses."*
- **Not recommend the uniform-cut branch of the scope fix.** *"It is the consistent reading, and it
  deletes M2's three authored surfaces, incumbent B, and the six-step refinement's ground in one
  stroke. The defect is real; the consistent fix costs the proposal's best part. Narrow the sentence."*
- **Not add a rung↔subsystem mapping to rescue the wrapper** — *"a new registry surface with no head
  referent… Let the N-line decide the wrapper's fate first."*

## Disposition in this session

**F1–F4 → §31 rewritten per owner**, with MATTER's three cross-owner operations named. **F5, F6 →
§40 re-grounded on `max_depth`**, and §40.2 states the unreconciled "no reaction inside a season"
tension rather than quoting one side. **F7, F8, F9 → the wrapper deleted; §6 added.** **F10 →
§44.2's surviving injector is stated as varying outcomes by declared type, so the invisibility claim
is not made.** **F12 → §0.1's three qualifications.** **F13 → census D-14 sharpened.** **F14 →
§22.4's containment-subtree restriction.** **F17, F18 → §41.4.**

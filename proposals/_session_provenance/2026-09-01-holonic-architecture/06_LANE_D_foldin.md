# Lane D — adjudicating the dropped rulings for fold-in

**Model:** Fable 5.1 · **Agent:** `valoria-critic` (Read/Grep/Glob only).

## Verdict (lane's own words, condensed)

**Of 27 items, 23 confirmed as real drops** against the named sections, and the four hand-verified ones
reproduce exactly. **One is a STRIKE** (item 16). **Three are overstated** in ways that change their
disposition. **Two are sharper than the census says.** *"Thirteen items merit folding in, but not as
thirteen edits: six carry most of the value, and the rest either ride on those six or should wait."*

## The verdicts that changed the proposal

| item | verdict | why |
|---|---|---|
| **14 · the `Event` record** | **FOLD-IN, sharpened** | *"Three partial `Event` records exist in-chain… and one Key field, `source_actor`, is **FORBIDDEN** by the head — so **'compose onto Key' is not field-neutral**."* **The wrapper's direction check is unstatable without a locus/target field** |
| **17 · the contract tree** | **FOLD-IN** | Specified in-chain and licensed as one of exactly two guards; the successor's execution path has no step for it. *"On merit the descent has no other transport"* |
| **16 · twelve argument faults** | ⛔ **STRIKE** | *"Not silently lost"* — scoped out by a declared narrowing. **Fails the census's own fourth test** |
| **24 · the 3/4/5 scene model** | **DEFER (half STRIKE)** | The head's overturn is **loud, not silent** — *"the census's type is wrong"*. The true residue is a stale *"three routes"* closure |
| **15 · `Coherence`** | **FOLD-IN, softened** | *"gone from both registers" is false* — only the successor dropped it |
| **8 · the four small edits** | **RECORD-ONLY, half softened** | E4 *"survives unnamed"* |
| **7 · `transfer`'s precondition** | **FOLD-IN** | ⚠ *"It is a prerequisite of the head's own contention fold"* — *"the second claimant on an emptied granary gets a different Event" is **false** if `transfer` can mint from a negative larder* |
| **21 · per-Conviction scarring** | **FOLD-IN, amended** | The source says *written at WITNESS*, which **breaks two things**: the moral layer's WITNESS row is *nothing*, and a scar written there is an Event writing a social row — **L4**. Lawful form: **RESOLVE, ACTS class, `social: true`, axis on L3's registry, crisis as an L5 edge** |
| **20 · the memberless faction's hold** | **FOLD-IN, amended** | **`confer` becomes ELIGIBLE when the holder-Proposition has zero live `commit` edges; nothing writes `until` — the successful confer does.** ⛔ **REFUSE the census's second half** — an actorless social write outside the one declared seam |
| **19 · `disclosure:`** | **FOLD-IN, amended** | Passes all three boundary tests. ⚠ **Amendment:** a hidden grade only for registry tallies, and **disclosure rows for owned fields only, never for Queries** — *"an aggregate across holders has no row to publish a band on, which is L3 clause 2 arriving on the read side"* |
| **18 · the person roster** | **FOLD-IN, amended** | **Adopt the SHAPE, not the number.** *"Step 3 seeds only leaders and governors, and T3 needs a person with **zero `hold` Tenures** — T3 is unreachable until a postless person exists"* |
| **22 · the twelve transitions** | **FOLD-IN** | *"The only Jordan-supplied acceptance cases in the range"*, and **no case runner has run one.** ⚠ The lane confirmed the census's figure was wrong |
| **23 · the hook grammar** | ⛔ **REFUSE** | *"A guaranteed fire at threshold… a fixed consequence"* versus L5's *"may never produce an outcome"*. **N-line false** — the lawful residue is already `ConveningCondition` + act-declared terms |
| **27 · the Parliament rider** | ⛔ **REFUSE** | It writes a field L3 forbids and the head deletes. *"Running the oracle would measure a mechanism the head has no carrier for"* |

## Set-level analysis (the part the brief most wanted)

**Conflicts between adoptions.** Items 9 + 10 *"do not collide with each other; they collide with 24"* —
and the close rule the set produces is **petitions never close each other; a matter closes at RESOLVE
by scarcity and at CALENDAR by lapse** — ⚠ *"provided item 7 lands."* Item 21 collides with the write
matrix cell and **must name the overturn.** Item 20 keeps C4 intact *"without touching the Partition
seam."* Item 19 + `phase:` **compose** (different spine levels) but 19 collides with a hidden tally and
needs the narrow amendment. Item 6 must precede 21 (axis naming).

**Order:** `14 → 17 → 6 → 7 → 13 → 19 → 21 → 11 → 20 → 15 → 18 → 22 → (24) → 9/10`.
*"The Event record is genuinely upstream: 21's scar is a StateChange carried in `Event.changes[]`;
11's clearance keys on a Claim landing; 20's confer emits the Event whose cardinality forces `until`;
22's twelve cases each name the Event a transition produces."*

**Budget.** *"Thirteen FOLD-INs is a grab-bag if landed as thirteen edits across nine sections."* The
core six: **14, 17, 13, 21, 20, 22.** From its own list it would fold 15 and 18 into 13 as graded rows,
demote 6 to a row inside 21, and **cut 19 if forced** — *"the largest surface addition, and its failure
mode is unmeasured."*

**The one thing.** *"**14** — write `Event :=` and map it onto the Key, field by field, with
`source_actor` explicitly not carried.** The proposal's load-bearing rule is stated over an object with
no fields; 21, 11, 20 and 22 all depend on it; and the head already rules one Key field out, so
'compose onto Key' is a decision with a cost the chain has never priced."*

## Disposition in this session

**All fourteen FOLD-INs and three REFUSEs → `ARCHITECTURE.md` Part VII §54**, in the lane's dependency
order, with each amendment carried as the lane stated it. **The STRIKE → census §8 row 3.** **The one
thing → §19 and §54.4.** **Item 7's prerequisite status → §27.1's warning that the fold's own scarcity
claim is false without it.** ⚠ **Item 9's held cost clause was subsequently LIFTED by a Jordan ruling
(petition spray is allowable) — see `ARCHITECTURE.md` §26.3 and §0.2 row 13.**

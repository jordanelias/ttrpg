"""systems/ — Valoria design docs by subsystem (ED-IN-0071 P4, §2a).

Python package so each subsystem's co-located sim/ imports as
systems.<subsystem>.sim.<module>. Doc-only subsystems (npcs, articulation,
ui) are plain directories under this package; sim-bearing ones add __init__.py.
"""

# tests/sim_framework/ — early sim-harness attempt (separate from sim/)

**Not the `sim/` package.** This is an earlier, separate sim-harness attempt (`engine.py`, `state.py`
plus its own workplan/canon-audit docs). It is not consumed by `sim/` or by `mc_v18.py` — the two
codebases are independent.

See `sim/README.md` for the full disambiguation against `sim/` and `tests/sim/`.
